# 사진첩 계약서 → 이알피 계약추가 가져오기 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** pu-erp.html 계약추가 창에서, 로그인한 사람이 사진첩에 올려둔 OCR 계약서 중 이름이 비슷한 것을 찾아 계약금·부가세(착수금/계약금 쪽)·계약일·계약기간·업무요약을 한 번에 채워 넣는다.

**Architecture:** pu-photos.html이 이미 쓰는 공용 저장 층 `js/pu-photo-store.js`(PuPhotoStore)를 pu-erp.html에 처음으로 로드해 본인 소유 사진 메타데이터만 읽는다. 매칭·필드계산은 전부 순수 함수로 분리해 vm 기반으로 단위 테스트하고, ContractModal 안에는 그 순수 함수를 부르는 얇은 UI 배선(배지·목록 팝업·미리보기 카드)만 둔다.

**Tech Stack:** Preact(h/useState/useEffect, 빌드 없음), Firebase RTDB(compat SDK), Node `vm` 모듈 기반 커스텀 테스트 하네스(이 저장소의 pu-erp.html 테스트 관례).

## Global Constraints

- 검색 범위는 **로그인한 사람 본인**의 사진첩 데이터만 (Firebase 규칙: `puphotos/u/{uid}`는 `auth.uid===$uid` 또는 admin만 읽기 허용 — 규칙 변경 없음).
- 매칭 기준은 **회사명**(느슨한 검색)만. 사업자번호는 계약서에 흔히 없어 기준으로 안 씀.
- **계약유형(`f.kinds`)을 정확히 하나만 체크했을 때만** 가져오기 배지를 보여준다(0개·2개 이상이면 안 보임).
- 부가세는 `fields.vat` 텍스트에 "포함"이 있으면 체크, "별도"가 있으면 해제, 그 외(빈칸·애매함)는 **건드리지 않음** — 잘못 추측해 세금 계산이 틀어지는 것을 막는다.
- CMS 자동이체·개인입금 필드는 OCR에 데이터 자체가 없어 대상에서 제외(설계서 확정 사항, 재론하지 않음).
- 사진첩 쪽 실시간 구독 없음 — 계약추가 창을 열 때 한 번만 읽어 캐시.
- 새로 추가하는 순수 함수는 모두 최상위(top-level) `function` 선언으로 — vm 슬라이스 테스트가 가능해야 한다.
- 테스트에서 최상위 함수를 꺼낼 때는 `fn('함수이름')` 헬퍼를 쓴다(Task 2에서 추가). `slice(a, b)`는 끝 표식을 **포함하지 않아** `'\n}\n'` 같은 표식으로는 닫는 중괄호가 빠져 SyntaxError 가 난다 — 함수 추출에 쓰지 말 것. `slice`는 함수가 아닌 블록(예: 최상위 `var` 선언 여러 줄)을 꺼낼 때만 쓴다.
- 참고 설계서: `docs/superpowers/specs/2026-08-15-사진첩계약서-가져오기-design.md`

---

## 사전 준비 — 격리 작업트리

이 계획은 `pu-erp.html`(74,000줄대 단일 파일)을 여러 지점에서 고친다. 다른 세션과 같은
작업트리를 공유하는 이 저장소의 특성상, 반드시 전용 작업트리에서 진행한다.

```bash
git fetch origin main -q
git worktree add "C:/Users/fair0/Documents/pu-wt-photo-contract" -b feat/photo-contract-import origin/main
```

이후 모든 파일 경로는 `C:/Users/fair0/Documents/pu-wt-photo-contract/` 기준이다.

---

### Task 1: `js/pu-photo-store.js` 로드 + 로그인 사용자 uid 연결

**Files:**
- Modify: `pu-erp.html:126` (스크립트 include 추가)
- Modify: `pu-erp.html:1911-1917` (Firebase 인증 완료 콜백에 `PuPhotoStore.init` 호출 추가)
- Test: `tests/erp-photo-contract-import.test.js` (새 파일, 이후 태스크에서 계속 채움)

**Interfaces:**
- Consumes: 없음(외부 파일 `js/pu-photo-store.js`가 이미 export하는 `PuPhotoStore.init(o)`, `PuPhotoStore.listYears(owner)`, `PuPhotoStore.listYear(year, owner)` — 수정하지 않음)
- Produces: 전역 `PuPhotoStore` 객체가 pu-erp.html에서 사용 가능해지고, `PuPhotoStore.init`이 `fbAuthUid`로 이미 호출된 상태 — 이후 태스크의 `erpLoadMyContractPhotos`가 이걸 전제로 한다.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/erp-photo-contract-import.test.js` 새로 만들기:

```js
/* 사진첩 계약서 → 이알피 계약추가 가져오기 */
const fs = require('fs');
const vm = require('vm');
const path = require('path');

const HTML = path.join(__dirname, '..', 'pu-erp.html');
const src = fs.readFileSync(HTML, 'utf8').replace(/\r\n/g, '\n');

function slice(a, b){
  const i = src.indexOf(a);
  if(i < 0) throw new Error('시작 표식 못찾음: ' + a);
  const j = src.indexOf(b, i);
  if(j < 0) throw new Error('끝 표식 못찾음: ' + b);
  return src.slice(i, j);
}

// 최상위 function 선언 하나를 통째로 꺼낸다 — 중괄호를 세어 끝을 찾는다.
// ('\n}\n' 같은 표식은 slice 가 끝을 포함하지 않아 닫는 중괄호를 빠뜨린다)
function fn(name){
  const start = src.indexOf('function ' + name + '(');
  if(start < 0) throw new Error('함수 못찾음: ' + name);
  let depth = 0;
  for(let i = src.indexOf('{', start); i < src.length; i++){
    if(src[i] === '{') depth++;
    else if(src[i] === '}'){ depth--; if(depth === 0) return src.slice(start, i + 1); }
  }
  throw new Error('함수 끝을 못찾음: ' + name);
}

let pass = 0, fail = 0;
const t = (name, got, want) => {
  const G = JSON.stringify(got), W = JSON.stringify(want);
  if(G === W) pass++;
  else { fail++; console.log('FAIL ' + name + '\n  got  = ' + G + '\n  want = ' + W); }
};

/* ═══ 1. PuPhotoStore 로드 + 초기화 ═══ */
{
  t('★ pu-photo-store.js 를 로드한다', /<script src="js\/pu-photo-store\.js\?v=\d+"><\/script>/.test(src), true);
  t('★ 인증 완료 시 PuPhotoStore.init 을 부른다', /PuPhotoStore\.init\(\s*\{\s*db:\s*fbDb,\s*uid:\s*user\.uid\s*\}\s*\)/.test(src), true);
}

console.log('\n  === ' + pass + ' 통과 / ' + fail + ' 실패 ===');
process.exit(fail ? 1 : 0);
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `node tests/erp-photo-contract-import.test.js`
Expected: `FAIL ★ pu-photo-store.js 를 로드한다` 와 `FAIL ★ 인증 완료 시 PuPhotoStore.init 을 부른다` 두 개 다 실패, `=== 0 통과 / 2 실패 ===`

- [ ] **Step 3: 최소 구현**

`pu-erp.html:126` 근처(`<script src="js/utils.js"></script>` 바로 다음 줄)에 추가:

```html
<script src="js/utils.js"></script>
<script src="js/pu-photo-store.js?v=1"></script>
```

`pu-erp.html:1909-1917`(현재 아래와 같음)

```js
        firebase.auth().onAuthStateChanged(function(user){
          if(user){
            fbAuthUid = user.uid;
            fbAuthReady = true;
            try { window.dispatchEvent(new CustomEvent('fb_auth_ready', {detail: user.uid})); } catch(e){ window._erpErrLog && window._erpErrLog(e); }
            try { initDeviceLock(); } catch(_){}  // 서버 기반 기기 잠금 시작
            // 명함첩 경량 검색 인덱스(pucards/idx) 로드 — 계약창 "과거 회사 불러오기"에서 명함첩 검색용
            try { if(!window._pcIdxHooked){ window._pcIdxHooked=true; window.pucardsIdx={};
              fbDb.ref('pucards/idx').on('value', function(s){ window.pucardsIdx = s.val() || {}; }); } } catch(_){}
```

이렇게 바꾼다(`initDeviceLock()` 호출 다음 줄에 추가):

```js
        firebase.auth().onAuthStateChanged(function(user){
          if(user){
            fbAuthUid = user.uid;
            fbAuthReady = true;
            try { window.dispatchEvent(new CustomEvent('fb_auth_ready', {detail: user.uid})); } catch(e){ window._erpErrLog && window._erpErrLog(e); }
            try { initDeviceLock(); } catch(_){}  // 서버 기반 기기 잠금 시작
            // 사진첩 저장 층 연결 — 계약창의 "사진첩 계약서 가져오기"가 이 uid 자리만 읽는다
            try { if(typeof PuPhotoStore !== 'undefined') PuPhotoStore.init({ db: fbDb, uid: user.uid }); } catch(_){}
            // 명함첩 경량 검색 인덱스(pucards/idx) 로드 — 계약창 "과거 회사 불러오기"에서 명함첩 검색용
            try { if(!window._pcIdxHooked){ window._pcIdxHooked=true; window.pucardsIdx={};
              fbDb.ref('pucards/idx').on('value', function(s){ window.pucardsIdx = s.val() || {}; }); } } catch(_){}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `node tests/erp-photo-contract-import.test.js`
Expected: `=== 2 통과 / 0 실패 ===`

- [ ] **Step 5: 캐시 버전 점검**

`pu-photo-store.js`는 새로 참조하는 것이라 기존 버전 검사에 걸리지 않지만, 커밋 전에 한 번 확인한다.

Run: `node scripts/check-cache-version.js --all`
Expected: 종료 코드 0 (지적 없음)

- [ ] **Step 6: 커밋**

```bash
git add pu-erp.html tests/erp-photo-contract-import.test.js
git commit -m "feat(계약추가): 사진첩 저장 층(PuPhotoStore) 연결"
```

---

### Task 2: `erpVatTextToFlag` — 부가세 문구 판정 순수 함수

**Files:**
- Modify: `pu-erp.html:11866-11867` 사이(= `PucardsCompanyPickerModal` 함수가 끝나는 줄과 `// ============ 계약 모달` 주석 사이)에 새 함수 블록 삽입
- Test: `tests/erp-photo-contract-import.test.js` (이어서 추가)

**Interfaces:**
- Consumes: 없음
- Produces: `function erpVatTextToFlag(text)` → `true | false | null`. Task 4가 이 함수를 그대로 부른다.

- [ ] **Step 1: 실패하는 테스트 추가**

`tests/erp-photo-contract-import.test.js`의 `console.log('\n  === ...` 줄 **바로 위**에 추가:

```js
/* ═══ 2. erpVatTextToFlag — 부가세 문구 판정 ═══ */
{
  const c = vm.createContext({});
  vm.runInContext(fn('erpVatTextToFlag'), c);
  t('"포함" 이 들어있으면 체크', c.erpVatTextToFlag('부가세 포함'), true);
  t('"별도" 이 들어있으면 해제', c.erpVatTextToFlag('부가세 별도'), false);
  t('빈 문자열은 건드리지 않음(null)', c.erpVatTextToFlag(''), null);
  t('애매한 문구도 건드리지 않음(null)', c.erpVatTextToFlag('추후 협의'), null);
  t('undefined 도 null', c.erpVatTextToFlag(undefined), null);
}
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `node tests/erp-photo-contract-import.test.js`
Expected: `시작 표식 못찾음: function erpVatTextToFlag(` 에러로 죽음(함수가 아직 없으므로)

- [ ] **Step 3: 최소 구현**

`pu-erp.html:11866` 바로 앞(`// ============ 계약 모달 (3탭: 기업정보 / 계약정보 / 담당자정보) ============` 주석 줄 바로 위)에 새 블록 삽입:

```js
// ============ 사진첩 계약서 가져오기 (계약추가 창 전용) ============
// 부가세 문구는 자유 텍스트("별도"/"포함"/빈칸)라, 잘못 추측해 체크박스를 틀리게
// 채우면 세금 계산이 바로 틀어진다. 애매하면 아예 안 건드리는 쪽이 안전하다.
function erpVatTextToFlag(text){
  var t = String(text || '');
  if(t.indexOf('포함') >= 0) return true;
  if(t.indexOf('별도') >= 0) return false;
  return null;
}

// ============ 계약 모달 (3탭: 기업정보 / 계약정보 / 담당자정보) ============
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `node tests/erp-photo-contract-import.test.js`
Expected: 새로 추가된 5개 테스트 전부 통과, 누적 `=== 7 통과 / 0 실패 ===`

- [ ] **Step 5: 커밋**

```bash
git add pu-erp.html tests/erp-photo-contract-import.test.js
git commit -m "feat(계약추가): 부가세 문구 판정 순수 함수(erpVatTextToFlag)"
```

---

### Task 3: `erpBuildContractPhotoMatches` — 회사명으로 계약서 사진 후보 찾기

**Files:**
- Modify: `pu-erp.html` — Task 2에서 넣은 `erpVatTextToFlag` 함수 바로 아래에 이어서 추가
- Test: `tests/erp-photo-contract-import.test.js`

**Interfaces:**
- Consumes: 이미 존재하는 `erpNormName(s)` (`pu-erp.html:4680`), `_erpNameCmp(a, b)` (`pu-erp.html:4705`, `{score, reason}` 반환) — 수정하지 않음.
- Produces: `function erpBuildContractPhotoMatches(companyName, items)` — `items`는 `{id, year, fields, at}` 형태의 배열(이미 `kind==='contract'`로 걸러진 것을 가정). 반환값: `score` 내림차순 정렬된 `{id, year, fields, at, score}` 배열. Task 7의 UI가 이 배열의 길이로 배지를 띄운다.

- [ ] **Step 1: 실패하는 테스트 추가**

```js
/* ═══ 3. erpBuildContractPhotoMatches — 회사명으로 후보 찾기 ═══ */
{
  const c = vm.createContext({});
  vm.runInContext(fn('erpNormName'), c);
  vm.runInContext(slice('// 두 문자열의 최장 공통 부분문자열 길이', 'function _erpNameCmp('), c);
  vm.runInContext(fn('_erpNameCmp'), c);
  vm.runInContext(fn('erpBuildContractPhotoMatches'), c);

  const items = [
    { id:'p1', year:'2026', fields:{ company:'주식회사 자차에프앤비' }, at:1000 },
    { id:'p2', year:'2025', fields:{ company:'카타엔지니어링' }, at:2000 },
    { id:'p3', year:'2026', fields:{ company:'' }, at:3000 },            // 회사명 없음 — 후보에서 빠져야 함
    { id:'p4', year:'2024', fields:{ company:'자차에프앤비 지점' }, at:4000 }, // 부분 일치도 잡혀야 함
  ];

  const r1 = c.erpBuildContractPhotoMatches('자차에프앤비', items);
  t('회사명이 일치/포함되는 사진만 후보로 나온다', r1.map(x => x.id).sort(), ['p1', 'p4']);
  t('회사명이 없는 사진은 후보에서 빠진다', r1.some(x => x.id === 'p3'), false);
  t('회사명이 2글자 미만이면 후보 없음', c.erpBuildContractPhotoMatches('유', items), []);
  t('빈 배열이면 후보 없음', c.erpBuildContractPhotoMatches('자차에프앤비', []), []);
}
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `node tests/erp-photo-contract-import.test.js`
Expected: `시작 표식 못찾음: function erpBuildContractPhotoMatches(` 에러

- [ ] **Step 3: 최소 구현**

`erpVatTextToFlag` 함수 다음에 이어서 추가:

```js
// 사진첩에서 온 회사명과 «지금 입력 중인 회사명»을 견주어 후보를 추린다.
// 이름 없는 사진(사업자번호만 있는 계약서 등)은 근거가 없어 후보로 안 낸다.
var ERP_PHOTO_MATCH_MIN_SCORE = 60;   // _erpNameCmp 의 "이름 부분 일치" 이상만 인정
function erpBuildContractPhotoMatches(companyName, items){
  var qn = erpNormName(companyName);
  if(!qn || qn.length < 2) return [];
  var out = [];
  (items || []).forEach(function(it){
    if(!it || !it.fields) return;
    var cn = erpNormName(it.fields.company);
    if(!cn) return;
    var cmp = _erpNameCmp(qn, cn);
    if(cmp.score >= ERP_PHOTO_MATCH_MIN_SCORE){
      out.push({ id:it.id, year:it.year, fields:it.fields, at:it.at, score:cmp.score });
    }
  });
  out.sort(function(a, b){ return b.score - a.score; });
  return out;
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `node tests/erp-photo-contract-import.test.js`
Expected: 누적 `=== 11 통과 / 0 실패 ===`

- [ ] **Step 5: 일부러 망가뜨려 확인**

`ERP_PHOTO_MATCH_MIN_SCORE`를 임시로 `95`로 바꾸고 다시 실행 →
`회사명이 일치/포함되는 사진만 후보로 나온다` 테스트가 실패하는지 확인(부분 일치 p4가 빠짐).
확인 후 `60`으로 되돌린다.

- [ ] **Step 6: 커밋**

```bash
git add pu-erp.html tests/erp-photo-contract-import.test.js
git commit -m "feat(계약추가): 회사명으로 사진첩 계약서 후보 찾기(erpBuildContractPhotoMatches)"
```

---

### Task 4: `erpContractPhotoApplyPatch` — 고른 계약서로 채울 값 계산

**Files:**
- Modify: `pu-erp.html` — Task 3의 함수 바로 아래에 이어서 추가
- Test: `tests/erp-photo-contract-import.test.js`

**Interfaces:**
- Consumes: `erpVatTextToFlag` (Task 2), 계약추가 창의 `f` 상태 모양(`f.amounts{}`, `f.briefs{}`, `f.company{ceo,address,bizNo}`, `f.contractFeeVatIncluded`) — `pu-erp.html:11884-11926`에 정의된 그대로.
- Produces: `function erpContractPhotoApplyPatch(fields, kindV, f)` → `{ patch, previewLines, vatRaw }`. `patch`는 `setF(prev => Object.assign({}, prev, patch))`에 그대로 넘길 수 있는 얕은 병합 객체. `previewLines`는 미리보기 카드에 한 줄씩 보여줄 문자열 배열. Task 7이 그대로 소비한다.

- [ ] **Step 1: 실패하는 테스트 추가**

```js
/* ═══ 4. erpContractPhotoApplyPatch — 고를 값 계산 ═══ */
{
  const c = vm.createContext({});
  vm.runInContext(fn('erpVatTextToFlag'), c);
  vm.runInContext(fn('erpContractPhotoApplyPatch'), c);

  const baseF = {
    amounts:{ consulting:0 }, briefs:{ consulting:'' },
    company:{ name:'자차에프앤비', bizNo:'', ceo:'', address:'' },
    contractFeeVatIncluded:false
  };
  const fields1 = {
    deposit:'500000', scope:'취업규칙 정비', signDate:'2026-07-01',
    startDate:'2026-07-01', endDate:'2027-06-30', vat:'별도',
    ceo:'김대표', address:'서울시 강남구', bizno:'1234567890'
  };
  const r1 = c.erpContractPhotoApplyPatch(fields1, 'consulting', baseF);
  t('계약금이 채워진다', r1.patch.amounts.consulting, 500000);
  t('업무요약이 채워진다', r1.patch.briefs.consulting, '취업규칙 정비');
  t('계약일이 채워진다', r1.patch.signDate, '2026-07-01');
  t('계약기간이 채워진다', [r1.patch.startDate, r1.patch.endDate], ['2026-07-01', '2027-06-30']);
  t('★ 부가세 "별도" → 체크 해제', r1.patch.contractFeeVatIncluded, false);
  t('비어 있던 대표자·주소·사업자번호가 덤으로 채워진다',
    [r1.patch.company.ceo, r1.patch.company.address, r1.patch.company.bizNo],
    ['김대표', '서울시 강남구', '1234567890']);
  t('미리보기 줄에 계약금이 사람이 읽을 형태로 들어간다',
    r1.previewLines.some(l => l.indexOf('500,000') >= 0), true);

  // 부가세 문구가 애매하면 그 칸은 patch 에 아예 없어야 한다(안 건드림)
  const fields2 = Object.assign({}, fields1, { vat:'추후 협의' });
  const r2 = c.erpContractPhotoApplyPatch(fields2, 'consulting', baseF);
  t('★ 부가세가 애매하면 patch 에 그 키 자체가 없다', 'contractFeeVatIncluded' in r2.patch, false);
  t('애매한 부가세 원문을 미리보기에 남긴다', r2.previewLines.some(l => l.indexOf('추후 협의') >= 0), true);

  // 이미 채워진 대표자·주소·사업자번호는 안 건드린다
  const filledF = Object.assign({}, baseF, { company: Object.assign({}, baseF.company, { ceo:'기존대표' }) });
  const r3 = c.erpContractPhotoApplyPatch(fields1, 'consulting', filledF);
  t('★ 이미 채워진 대표자는 안 건드린다', r3.patch.company.ceo, '기존대표');
  t('비어 있던 주소는 그대로 채워진다', r3.patch.company.address, '서울시 강남구');
}
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `node tests/erp-photo-contract-import.test.js`
Expected: `시작 표식 못찾음: function erpContractPhotoApplyPatch(` 에러

- [ ] **Step 3: 최소 구현**

`erpBuildContractPhotoMatches` 함수 다음에 이어서 추가:

```js
// 고른 계약서 사진의 OCR 값으로, 계약추가 창에 실제로 덮어쓸 patch 와
// 미리보기 문구를 함께 계산한다. UI(Task 7)는 이 결과를 그대로 쓰기만 한다 —
// 값을 계산하는 로직을 컴포넌트 안에 두지 않아야 이 단위검사로 지킬 수 있다.
function erpContractPhotoApplyPatch(fields, kindV, f){
  fields = fields || {};
  var patch = {};
  var lines = [];

  var amt = parseInt(fields.deposit, 10) || parseInt(fields.fee, 10) || 0;
  if(amt > 0){
    patch.amounts = Object.assign({}, f.amounts || {});
    patch.amounts[kindV] = amt;
    lines.push('계약금 ' + amt.toLocaleString() + '원');
  }

  if(fields.scope){
    var briefMax = (typeof BRIEF_MAX !== 'undefined') ? BRIEF_MAX : 40;
    patch.briefs = Object.assign({}, f.briefs || {});
    patch.briefs[kindV] = String(fields.scope).slice(0, briefMax);
    lines.push('업무 ' + patch.briefs[kindV]);
  }

  if(fields.signDate){ patch.signDate = fields.signDate; lines.push('계약일 ' + fields.signDate); }
  if(fields.startDate) patch.startDate = fields.startDate;
  if(fields.endDate) patch.endDate = fields.endDate;
  if(fields.startDate || fields.endDate){
    lines.push('기간 ' + (fields.startDate || '?') + '~' + (fields.endDate || '?'));
  }

  var vatFlag = erpVatTextToFlag(fields.vat);
  if(vatFlag !== null){
    patch.contractFeeVatIncluded = vatFlag;
    lines.push('부가세 ' + (vatFlag ? '포함' : '별도'));
  } else if(fields.vat){
    lines.push('부가세 판단 안 됨 — 원문 "' + fields.vat + '"');
  }

  var nextCompany = Object.assign({}, f.company || {});
  var companyChanged = false;
  [['ceo','ceo'], ['address','address'], ['bizno','bizNo']].forEach(function(pair){
    var src = pair[0], dest = pair[1];
    if(!nextCompany[dest] && fields[src]){ nextCompany[dest] = fields[src]; companyChanged = true; }
  });
  if(companyChanged) patch.company = nextCompany;

  return { patch: patch, previewLines: lines, vatRaw: fields.vat || '' };
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `node tests/erp-photo-contract-import.test.js`
Expected: 누적 `=== 22 통과 / 0 실패 ===`

- [ ] **Step 5: 일부러 망가뜨려 확인**

`if(!nextCompany[dest] && fields[src])`를 임시로 `if(fields[src])`(빈칸 체크 제거)로 바꾸고 재실행 →
`★ 이미 채워진 대표자는 안 건드린다` 테스트가 실패하는지 확인. 확인 후 원복.

- [ ] **Step 6: 커밋**

```bash
git add pu-erp.html tests/erp-photo-contract-import.test.js
git commit -m "feat(계약추가): 고른 계약서로 채울 값 계산(erpContractPhotoApplyPatch)"
```

---

### Task 5: `erpLoadMyContractPhotos` — 내 계약서 사진 캐시 로더

**Files:**
- Modify: `pu-erp.html` — Task 4의 함수 바로 아래에 이어서 추가
- Test: `tests/erp-photo-contract-import.test.js`

**Interfaces:**
- Consumes: `PuPhotoStore.listYears(owner)`, `PuPhotoStore.listYear(year, owner)` (Task 1에서 로드됨, 시그니처는 `js/pu-photo-store.js:1066`/`1011` 그대로), 전역 `fbAuthUid`.
- Produces: `function erpLoadMyContractPhotos(cb)` — lazy singleton 캐시. `cb(items)`를 호출하며 `items`는 Task 3의 `erpBuildContractPhotoMatches`가 받는 바로 그 모양(`{id, year, fields, at}[]`, `kind==='contract'`인 것만). 두 번째 호출부터는 캐시된 값을 즉시 콜백. Task 7이 컴포넌트 마운트 시 한 번 호출한다.

- [ ] **Step 1: 실패하는 테스트 추가**

```js
/* ═══ 5. erpLoadMyContractPhotos — 캐시 로더 ═══ */
{
  const c = vm.createContext({
    window: { _erpErrLog: null },
    fbAuthUid: 'u1',
  });
  vm.runInContext(slice('var _erpMyContractPhotos = null;', 'function erpLoadMyContractPhotos('), c); // 전역 변수 3개
  vm.runInContext(fn('erpLoadMyContractPhotos'), c);

  let listYearCalls = 0;
  c.PuPhotoStore = {
    listYears: function(){ return Promise.resolve(['2026', '2025']); },
    listYear: function(year){
      listYearCalls++;
      if(year === '2026'){
        return Promise.resolve({
          a1: { read: { kind:'contract', fields:{ company:'가나다' } }, __year:'2026', upAt: 111 },
          a2: { read: { kind:'card', fields:{} }, __year:'2026' }   // 계약서가 아닌 것 — 걸러져야 함
        });
      }
      return Promise.resolve({
        b1: { read: { kind:'contract', fields:{ company:'라마바' } }, __year:'2025', upAt: 222 }
      });
    }
  };

  return new Promise(function(resolve){
    c.erpLoadMyContractPhotos(function(items){
      t('계약서 kind 만 걸러진다(2건)', items.length, 2);
      t('연도별 항목이 다 합쳐진다', items.map(x => x.id).sort(), ['a1', 'b1']);
      // 두 번째 호출은 캐시에서 바로 온다 — listYear 가 다시 불리지 않아야 한다
      const callsBefore = listYearCalls;
      c.erpLoadMyContractPhotos(function(items2){
        t('두 번째 호출은 캐시를 그대로 돌려준다', items2.length, 2);
        t('두 번째 호출은 새로 안 읽는다', listYearCalls, callsBefore);
        resolve();
      });
    });
  });
}
```

`console.log('\n  === ...` 출력 직전에 이 블록이 있으면 Promise가 완료되기 전에 통과/실패 집계가
출력될 수 있으므로, 이 테스트 파일 맨 끝을 아래처럼 async 즉시실행 함수로 감싼다(다른 동기 블록은
그대로 위에 둔다):

```js
(async () => {
  // ↑ 위의 모든 동기 테스트 블록은 그대로 위에 남긴다.
  // 5번 블록(Promise 반환)만 아래처럼 await 한다.
  await (/* 위 5번 블록 전체를 여기로 옮기고 */ Promise.resolve());
  console.log('\n  === ' + pass + ' 통과 / ' + fail + ' 실패 ===');
  process.exit(fail ? 1 : 0);
})();
```

구체적으로는: 파일 맨 아래 `console.log('\n  === ...')`와 `process.exit(...)` 두 줄을 지우고,
5번 블록 전체를 `await`로 감싸 아래 형태로 파일 끝에 둔다:

```js
(async () => {
  await (async () => {
    const c = vm.createContext({ window: { _erpErrLog: null }, fbAuthUid: 'u1' });
    vm.runInContext(slice('var _erpMyContractPhotos = null;', 'function erpLoadMyContractPhotos('), c);
    vm.runInContext(fn('erpLoadMyContractPhotos'), c);
    let listYearCalls = 0;
    c.PuPhotoStore = {
      listYears: function(){ return Promise.resolve(['2026', '2025']); },
      listYear: function(year){
        listYearCalls++;
        if(year === '2026'){
          return Promise.resolve({
            a1: { read: { kind:'contract', fields:{ company:'가나다' } }, __year:'2026', upAt: 111 },
            a2: { read: { kind:'card', fields:{} }, __year:'2026' }
          });
        }
        return Promise.resolve({
          b1: { read: { kind:'contract', fields:{ company:'라마바' } }, __year:'2025', upAt: 222 }
        });
      }
    };
    await new Promise(function(resolve){
      c.erpLoadMyContractPhotos(function(items){
        t('계약서 kind 만 걸러진다(2건)', items.length, 2);
        t('연도별 항목이 다 합쳐진다', items.map(x => x.id).sort(), ['a1', 'b1']);
        const callsBefore = listYearCalls;
        c.erpLoadMyContractPhotos(function(items2){
          t('두 번째 호출은 캐시를 그대로 돌려준다', items2.length, 2);
          t('두 번째 호출은 새로 안 읽는다', listYearCalls, callsBefore);
          resolve();
        });
      });
    });
  })();
  console.log('\n  === ' + pass + ' 통과 / ' + fail + ' 실패 ===');
  process.exit(fail ? 1 : 0);
})();
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `node tests/erp-photo-contract-import.test.js`
Expected: `시작 표식 못찾음: var _erpMyContractPhotos = null;` 에러

- [ ] **Step 3: 최소 구현**

`erpContractPhotoApplyPatch` 함수 다음에 이어서 추가:

```js
// 내(로그인한 사람) 사진첩의 계약서 사진만 한 번 긁어와 캐시한다.
// 실시간 구독이 아니다 — 계약추가 창을 새로 열어야 새 사진이 반영된다(설계 확정 사항).
var _erpMyContractPhotos = null;
var _erpMyContractPhotosLoading = false;
var _erpMyContractPhotosWaiters = [];
function erpLoadMyContractPhotos(cb){
  if(_erpMyContractPhotos){ cb && cb(_erpMyContractPhotos); return; }
  if(_erpMyContractPhotosLoading){ if(cb) _erpMyContractPhotosWaiters.push(cb); return; }
  if(typeof PuPhotoStore === 'undefined' || !fbAuthUid){ cb && cb([]); return; }
  _erpMyContractPhotosLoading = true;
  PuPhotoStore.listYears().then(function(years){
    return Promise.all(years.map(function(y){ return PuPhotoStore.listYear(y); }));
  }).then(function(yearMaps){
    var out = [];
    (yearMaps || []).forEach(function(map){
      Object.keys(map || {}).forEach(function(id){
        var meta = map[id];
        if(meta && meta.read && meta.read.kind === 'contract'){
          out.push({ id:id, year:meta.__year, fields:meta.read.fields || {}, at: meta.upAt || meta.takenAt || 0 });
        }
      });
    });
    _erpMyContractPhotos = out;
    _erpMyContractPhotosLoading = false;
    var waiters = _erpMyContractPhotosWaiters; _erpMyContractPhotosWaiters = [];
    cb && cb(out);
    waiters.forEach(function(w){ w(out); });
  }).catch(function(e){
    window._erpErrLog && window._erpErrLog(e);
    _erpMyContractPhotosLoading = false;
    var waiters = _erpMyContractPhotosWaiters; _erpMyContractPhotosWaiters = [];
    cb && cb([]);
    waiters.forEach(function(w){ w([]); });
  });
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `node tests/erp-photo-contract-import.test.js`
Expected: 누적 `=== 26 통과 / 0 실패 ===`

- [ ] **Step 5: 커밋**

```bash
git add pu-erp.html tests/erp-photo-contract-import.test.js
git commit -m "feat(계약추가): 내 사진첩 계약서 캐시 로더(erpLoadMyContractPhotos)"
```

---

### Task 6: `PhotoContractPickerModal` — 후보 목록 팝업

**Files:**
- Modify: `pu-erp.html` — Task 5의 함수 바로 아래, `// ============ 계약 모달` 주석 위에 이어서 추가
- Test: `tests/erp-photo-contract-import.test.js`

**Interfaces:**
- Consumes: 전역 `h`(preact), `useEscClose`, `fmtDate(d)`(`pu-erp.html:30279`) — 전부 기존 함수, 수정 없음.
- Produces: `function PhotoContractPickerModal(props)` — `props = {items, onClose, onSelect}`. `items`는 Task 3의 반환값 그대로. `onSelect(item)`이 클릭한 한 건과 함께 불린다. Task 7이 이 컴포넌트를 렌더링한다.

- [ ] **Step 1: 실패하는 테스트 추가**

```js
/* ═══ 6. PhotoContractPickerModal — 후보 목록 팝업 ═══ */
{
  t('★ PhotoContractPickerModal 함수가 있다', /function PhotoContractPickerModal\(props\)\{/.test(src), true);
  const blk = fn('PhotoContractPickerModal');
  t('제목에 "사진첩 계약서" 문구가 있다', /사진첩 계약서에서 가져오기/.test(blk), true);
  t('항목을 누르면 onSelect 를 부른다', /onClick:function\(\)\{\s*props\.onSelect\(it\);\s*\}/.test(blk), true);
  t('닫기는 onClose', /onClick:props\.onClose/.test(blk), true);
}
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `node tests/erp-photo-contract-import.test.js`
Expected: 4개 다 실패(함수가 아직 없음)

- [ ] **Step 3: 최소 구현**

`erpLoadMyContractPhotos` 함수 다음, `// ============ 계약 모달 ...` 주석 바로 위에 추가:

```js
// 회사명으로 찾은 계약서 사진 후보 목록 — PucardsCompanyPickerModal 과 같은 모양을 그대로 본뜬다.
function PhotoContractPickerModal(props){
  useEscClose(props.onClose);
  var items = props.items || [];
  return h('div', {
    style:{ position:'fixed', inset:0, background:'rgba(0,0,0,0.5)', zIndex:210, display:'flex', alignItems:'center', justifyContent:'center' },
    onClick:function(e){ e.stopPropagation(); if(e.target === e.currentTarget) props.onClose(); }
  },
    h('div', { style:{ background:'#fff', borderRadius:'8px', width:'420px', maxWidth:'95vw', maxHeight:'70vh', display:'flex', flexDirection:'column' } },
      h('div', { style:{ padding:'14px 18px', borderBottom:'1px solid #e2e8f0', display:'flex', justifyContent:'space-between', alignItems:'center' } },
        h('div', { style:{ fontSize:'14px', fontWeight:700 } }, '📷 사진첩 계약서에서 가져오기'),
        h('button', { onClick:props.onClose, style:{ background:'none', border:'none', fontSize:'18px', cursor:'pointer', color:'#64748b' } }, '×')
      ),
      h('div', { style:{ flex:1, overflow:'auto', padding:'8px' } },
        items.map(function(it, i){
          var d = it.fields.signDate || (it.at ? fmtDate(it.at).slice(0, 10) : '');
          return h('div', { key:i,
            onClick:function(){ props.onSelect(it); },
            style:{ padding:'10px 14px', borderBottom:'1px solid #f8fafc', cursor:'pointer' },
            onMouseEnter:function(e){ e.currentTarget.style.background = '#f8fafc'; },
            onMouseLeave:function(e){ e.currentTarget.style.background = ''; }
          },
            h('div', { style:{ fontSize:'12.5px', fontWeight:600, color:'#1e293b' } }, d || '날짜 미상'),
            h('div', { style:{ fontSize:'10.5px', color:'#64748b', marginTop:'2px' } },
              it.fields.docName || it.fields.company || '(문서명 없음)')
          );
        })
      )
    )
  );
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `node tests/erp-photo-contract-import.test.js`
Expected: 누적 `=== 30 통과 / 0 실패 ===`

- [ ] **Step 5: 커밋**

```bash
git add pu-erp.html tests/erp-photo-contract-import.test.js
git commit -m "feat(계약추가): 사진첩 계약서 후보 목록 팝업(PhotoContractPickerModal)"
```

---

### Task 7: ContractModal 배선 — 배지 · 목록 열기 · 미리보기 · 적용

**Files:**
- Modify: `pu-erp.html:12018-12019`(새 useState 3개 추가), `pu-erp.html:13546`(회사명 입력 바로 아래에 배지 삽입), `pu-erp.html:14685`(모달 목록에 새 팝업 2개 추가)
- Test: `tests/erp-photo-contract-import.test.js`

**Interfaces:**
- Consumes: Task 2~6의 모든 함수(`erpLoadMyContractPhotos`, `erpBuildContractPhotoMatches`, `erpContractPhotoApplyPatch`, `PhotoContractPickerModal`), 기존 `f`/`setF`/`useEffect`.
- Produces: 없음(최종 UI 배선 — 더 이상 소비하는 태스크 없음)

- [ ] **Step 1: 실패하는 테스트 추가**

```js
/* ═══ 7. ContractModal 배선 ═══ */
{
  const blk = slice('function ContractModal(props){', '\n// ============ 방금 들어온 건 표시 ============');
  t('★ photoMatches state 가 있다', /var pm = useState\(\[\]\); var photoMatches = pm\[0\]; var setPhotoMatches = pm\[1\];/.test(blk), true);
  t('★ 계약유형 하나일 때만 매칭을 채운다', /\(f\.kinds\s*\|\|\s*\[\]\)\.length\s*!==\s*1/.test(blk), true);
  t('★ 배지는 photoMatches 가 있을 때만 보인다', /photoMatches\.length > 0 &&/.test(blk), true);
  t('★ 배지를 누르면 목록 팝업을 연다', /setPhotoPickerOpen\(true\)/.test(blk), true);
  t('★ 고르면 erpContractPhotoApplyPatch 를 부른다', /erpContractPhotoApplyPatch\(it\.fields, kindV, f\)/.test(blk), true);
  t('★ 적용을 누르면 patch 를 f 에 덮어쓴다', /Object\.assign\(\{\}, prev, photoPreview\.result\.patch\)/.test(blk), true);
  t('PhotoContractPickerModal 이 렌더링된다', /h\(PhotoContractPickerModal,/.test(blk), true);
}
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `node tests/erp-photo-contract-import.test.js`
Expected: 7개 다 실패

- [ ] **Step 3: state 추가**

`pu-erp.html:12018-12019`(현재):

```js
  var ar = useState([]); var autoResults = ar[0]; var setAutoResults = ar[1];
  var aidx = useState(-1); var autoIdx = aidx[0]; var setAutoIdx = aidx[1];   // 자동완성 키보드 선택 인덱스
```

이렇게 바꾼다(바로 아래에 3줄 추가):

```js
  var ar = useState([]); var autoResults = ar[0]; var setAutoResults = ar[1];
  var aidx = useState(-1); var autoIdx = aidx[0]; var setAutoIdx = aidx[1];   // 자동완성 키보드 선택 인덱스
  // 사진첩 계약서 가져오기 — 계약유형을 하나만 체크했을 때만 후보를 찾는다
  var cpl = useState(false); var contractPhotosLoaded = cpl[0]; var setContractPhotosLoaded = cpl[1];
  var pm = useState([]); var photoMatches = pm[0]; var setPhotoMatches = pm[1];
  var ppo = useState(false); var photoPickerOpen = ppo[0]; var setPhotoPickerOpen = ppo[1];
  var ppv = useState(null); var photoPreview = ppv[0]; var setPhotoPreview = ppv[1];
  useEffect(function(){ erpLoadMyContractPhotos(function(){ setContractPhotosLoaded(true); }); }, []);
  useEffect(function(){
    if(!contractPhotosLoaded || (f.kinds||[]).length !== 1){ setPhotoMatches([]); return; }
    setPhotoMatches(erpBuildContractPhotoMatches(f.company.name, _erpMyContractPhotos || []));
  }, [f.company.name, (f.kinds||[]).length, contractPhotosLoaded]);
```

- [ ] **Step 4: 배지 삽입**

`pu-erp.html:13546`(회사명 `fld` div가 닫히는 줄, 현재):

```js
        )
      ),
      fld('사업자번호',
```

이렇게 바꾼다(닫는 줄과 `fld('사업자번호'` 사이에 배지 블록 삽입):

```js
        )
      ),
      photoMatches.length > 0 && h('div', { style:{ marginTop:'4px' } },
        h('button', { type:'button',
          onClick:function(){ setPhotoPickerOpen(true); },
          style:{ padding:'4px 10px', fontSize:'11px', fontWeight:700, color:'#2563eb', background:'#eff6ff',
            border:'1px solid #bfdbfe', borderRadius:'6px', cursor:'pointer' } },
          '📷 계약서 ' + photoMatches.length + '건 발견')),
      fld('사업자번호',
```

- [ ] **Step 5: 목록·미리보기 팝업 렌더링 추가**

`pu-erp.html:14680-14685`(현재):

```js
    pcCoPick && h(PucardsCompanyPickerModal, {
      initialQuery: ((f.company && f.company.name) || '').trim().length >= 2 ? (f.company.name || '').trim() : '',
      want: pcCoPick,
      onClose: function(){ setPcCoPick(''); },
      onSelect: function(row){ var w = pcCoPick; setPcCoPick(''); fillCompanyImagesFromPucards(row, w); }
    })
  );
```

이렇게 바꾼다(`pcCoPick` 블록 다음에 콤마로 이어 새 블록 2개 추가):

```js
    pcCoPick && h(PucardsCompanyPickerModal, {
      initialQuery: ((f.company && f.company.name) || '').trim().length >= 2 ? (f.company.name || '').trim() : '',
      want: pcCoPick,
      onClose: function(){ setPcCoPick(''); },
      onSelect: function(row){ var w = pcCoPick; setPcCoPick(''); fillCompanyImagesFromPucards(row, w); }
    }),
    // 사진첩 계약서 후보 목록 — 고르면 미리보기로 넘어간다(바로 적용하지 않는다)
    photoPickerOpen && h(PhotoContractPickerModal, {
      items: photoMatches,
      onClose: function(){ setPhotoPickerOpen(false); },
      onSelect: function(it){
        setPhotoPickerOpen(false);
        var kindV = (f.kinds || [])[0];
        var result = erpContractPhotoApplyPatch(it.fields, kindV, f);
        setPhotoPreview({ item: it, kindV: kindV, result: result });
      }
    }),
    // 사진첩 계약서 미리보기 — 적용을 눌러야 실제로 덮어쓴다(사람이 확인한 뒤 안전하게)
    photoPreview && h('div', {
      style:{ position:'fixed', inset:0, background:'rgba(0,0,0,0.5)', zIndex:211, display:'flex', alignItems:'center', justifyContent:'center' },
      onClick:function(e){ if(e.target === e.currentTarget) setPhotoPreview(null); }
    },
      h('div', { style:{ background:'#fff', borderRadius:'8px', width:'380px', maxWidth:'95vw', padding:'16px' } },
        h('div', { style:{ fontSize:'13px', fontWeight:700, marginBottom:'8px' } }, '이렇게 바뀝니다'),
        h('div', { style:{ fontSize:'12px', color:'#334155', lineHeight:1.8 } },
          photoPreview.result.previewLines.map(function(line, i){
            return h('div', { key:i }, '· ' + line);
          })
        ),
        h('div', { style:{ display:'flex', gap:'8px', marginTop:'14px', justifyContent:'flex-end' } },
          h('button', { onClick:function(){ setPhotoPreview(null); },
            style:{ padding:'6px 14px', border:'1px solid #cbd5e1', borderRadius:'6px', background:'#fff', cursor:'pointer', fontSize:'12px' } }, '취소'),
          h('button', { onClick:function(){
              setF(function(prev){ return Object.assign({}, prev, photoPreview.result.patch); });
              setPhotoPreview(null);
            },
            style:{ padding:'6px 14px', border:'none', borderRadius:'6px', background:'#2563eb', color:'#fff', cursor:'pointer', fontSize:'12px', fontWeight:700 } }, '적용')
        )
      )
    )
  );
```

- [ ] **Step 6: 테스트 통과 확인**

Run: `node tests/erp-photo-contract-import.test.js`
Expected: 누적 `=== 37 통과 / 0 실패 ===`

- [ ] **Step 7: 전체 회귀 검사**

Run: `node scripts/check-cache-version.js --all && node --test tests/*.test.js`
Expected: 캐시 버전 검사 종료 코드 0, 전체 테스트 스위트 전부 통과(`fail 0`)

- [ ] **Step 8: 일부러 망가뜨려 확인**

`(f.kinds||[]).length !== 1`를 임시로 `(f.kinds||[]).length < 1`(2개 이상 체크해도 통과되게)로 바꾸고
`node tests/erp-photo-contract-import.test.js` 재실행 →
`★ 계약유형 하나일 때만 매칭을 채운다` 테스트가 실패하는지 확인. 확인 후 원복.

- [ ] **Step 9: 커밋**

```bash
git add pu-erp.html tests/erp-photo-contract-import.test.js
git commit -m "feat(계약추가): 사진첩 계약서 가져오기 배지·목록·미리보기 배선"
```

---

## 마무리 — PR

```bash
git push -u origin feat/photo-contract-import
gh pr create --base main --head feat/photo-contract-import \
  --title "feat(계약추가): 사진첩 계약서에서 계약금·부가세·계약일 등 가져오기" \
  --body "설계서: docs/superpowers/specs/2026-08-15-사진첩계약서-가져오기-design.md
계약유형을 하나만 체크한 상태에서, 회사명이 비슷한 본인 사진첩 계약서(OCR)가 있으면
배지로 알려주고, 골라서 미리보기 확인 뒤 계약금·부가세(착수금/계약금 쪽)·계약일·
계약기간·업무요약을 채운다. CMS·개인입금은 OCR 데이터 자체가 없어 대상 아님."
```

## Self-Review 체크리스트 (구현 담당자가 PR 올리기 전 직접 확인)

1. **설계서 커버리지**: 설계서의 "흐름" 6단계 — Task 7이 전부 구현(1~2단계: 캐시+매칭 useEffect, 3단계: 배지, 4단계: 목록 팝업, 5단계: 미리보기+적용, 6단계: 계약유형 하나일 때만). "필드 매핑" 표 — Task 4가 전부 구현. "권한·검색 범위" — Task 1·5가 본인 uid만 읽음을 보장. 빠진 것 없음.
2. **플레이스홀더 스캔**: 이 계획에 TBD/TODO 없음, 모든 스텝에 실제 코드 있음.
3. **타입 일관성**: `erpBuildContractPhotoMatches`가 반환하는 `{id, year, fields, at, score}` 모양이 `PhotoContractPickerModal`의 `it.fields`/`it.at` 사용과 `erpContractPhotoApplyPatch`의 `fields` 인자와 전부 일치함을 확인함.
