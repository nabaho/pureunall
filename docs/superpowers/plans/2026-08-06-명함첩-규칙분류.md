# 명함첩 규칙 일괄 분류 (1단계) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 미분류로 쌓인 명함 약 5,000장을, 회사명 키워드 규칙으로 미리보기한 뒤 한 번에 폴더로 옮기고, 잘못되면 통째로 되돌린다.

**Architecture:** `pu-cards.html` 한 파일에 세 겹으로 넣는다 — ①순수 로직(규칙 매칭·대상 고르기, 전역 상태를 안 쓰므로 Node에서 그대로 검사 가능) ②저장 계층(`Store` 에 규칙 읽고 쓰기 추가, Firebase RTDB `pucards/classifyRules`·`pucards/classifyLog`) ③화면(환경설정 → 🧹정리 탭 안에 인라인, 팝업 없음). 적용은 명함을 하나씩 쓰지 않고 **한 번의 `update()` 다중 경로 쓰기**로 몰아 보낸다.

**Tech Stack:** 빌드 없는 단일 HTML + 바닐라 JS · Firebase RTDB(compat) · 검사는 `node --test` + `vm` 샌드박스

## Global Constraints

- 대상 파일은 `C:\Users\fair0\Documents\pureunall\pu-cards.html` 하나. 빌드 단계가 없다. TypeScript 문법을 쓰지 않는다.
- **팝업을 새로 만들지 않는다.** 환경설정 안에서는 `_panelTarget==='inline'` 일 때 `showPanel(html)` 로 `#setInline` 에 그린다. 목록 화면에서 부르면 기존대로 팝업으로 나간다.
- **규칙은 저절로 돌지 않는다.** 사람이 `적용`을 누를 때만 실행한다. 로그인·저장·새로고침 때 자동 실행 금지.
- **미분류(`String(it.group||'')===''`)만 대상.** 이미 폴더에 든 명함은 절대 건드리지 않는다.
- **잠긴 폴더로 보내지 않는다.** 보낼 폴더가 `g.locked` 면 그 규칙은 막고 이유를 화면에 적는다.
- 명함 수천 장을 쓸 때는 `Store.put` 을 반복 호출하지 않는다. `Store.db.ref(DB_ROOT).update(upd)` 한 번으로 보낸다 (`rebuildIdxAll` 이 쓰는 방식).
- 폴더가 바뀌면 검색색인도 같은 `update()` 안에서 함께 고친다.
- CI(`node --test tests/*.test.js`)가 `deploy-pages.yml` 의 배포를 막는다. **새 검사가 통과해야 사이트가 나간다.**
- 저장소는 여러 세션이 동시에 쓴다. `git add -A` 를 쓰지 않는다. 건드린 파일만 골라 담는다.

## File Structure

| 파일 | 역할 | 변경 |
|---|---|---|
| `pu-cards.html` | 규칙 로직·저장·화면 전부 | 수정 — 4곳 |
| `tests/cards-classify-rules.test.js` | 순수 로직 검사 (미분류만·먼저 맞은 하나·잠긴 폴더·두 번 적용) | 신규 |

`pu-cards.html` 안 삽입 위치 4곳:

1. `const state = {...}` (747행) — `rules:{}` 추가
2. `Store` 객체 안 `putView`/`delView` 옆 (862~866행) — `putRule`/`delRule`/`_lsRules` 추가
3. `subscribe()` 안 `views` 구독 옆 (768~772행) — `classifyRules` 구독 추가
4. `function _go(...)` 끝난 뒤 · `const SET_TABS` 앞 (4167~4170행) — **규칙 분류 블록 전체**

4번 위치를 고른 이유: `_panelTarget`·`showPanel`·`setSub`·`_go`·`_refresh` 가 모두 그 위에 이미 정의돼 있어, 화면 함수가 그것들을 바로 쓸 수 있다.

---

### Task 1: 규칙 저장 계층과 기본 규칙 4개

**Files:**
- Modify: `C:\Users\fair0\Documents\pureunall\pu-cards.html:747` (state에 `rules:{}`)
- Modify: `C:\Users\fair0\Documents\pureunall\pu-cards.html:866` (Store에 규칙 읽고 쓰기)
- Modify: `C:\Users\fair0\Documents\pureunall\pu-cards.html:772` (Firebase 구독)
- Modify: `C:\Users\fair0\Documents\pureunall\pu-cards.html:4167` (기본 규칙 상수와 시드 함수)

**Interfaces:**
- Consumes: 없음 (첫 작업)
- Produces:
  - `state.rules` — `{ [key]: {key, order, enabled, words, toGroupName} }`
  - `Store.putRule(r)` → void
  - `Store.delRule(key)` → void
  - `CLASSIFY_DEFAULTS` — 기본 규칙 배열 (Task 2·3이 읽는다)
  - `seedClassifyRules()` → void
  - `classifyRuleList()` → `order` 오름차순 규칙 배열 (Task 2·3이 쓴다)

- [ ] **Step 1: `state` 에 규칙 칸 추가**

747행의 `const state = {...}` 에서 `views:{},` 바로 뒤에 `rules:{},` 를 끼워 넣는다.

```js
unlocked:{}, items:{}, groups:{}, trash:{}, views:{}, rules:{}, view:'list', vtab:'' };
```

- [ ] **Step 2: `Store` 에 규칙 읽고 쓰기 추가**

866행 `_lsViews(){...}` 줄 **바로 아래**에 넣는다.

```js
  /* 분류 규칙 — 전 직원 공유. 명함을 건드리지 않고 '어떤 단어면 어느 폴더로' 만 담는다 */
  putRule(r){ if (this.mode==='firebase') this.db.ref(`${DB_ROOT}/classifyRules/${r.key}`).set(r);
    else { state.rules[r.key]=r; this._lsRules(); render(); } },
  delRule(key){ if (this.mode==='firebase') this.db.ref(`${DB_ROOT}/classifyRules/${key}`).remove();
    else { delete state.rules[key]; this._lsRules(); render(); } },
  _lsRules(){ try{ localStorage.setItem('pucards_local_rules', JSON.stringify(state.rules||{})); }catch(e){} },
```

- [ ] **Step 3: Firebase 구독 추가**

772행 근처, `views` 를 구독하는 줄들 옆에 한 줄 더한다. `subscribe()` 안이다.

```js
        this.db.ref(DB_ROOT+'/classifyRules').on('value', s => { state.rules = s.val()||{};
          if(!Store._ruleSeedDone){ Store._ruleSeedDone=true; setTimeout(seedClassifyRules, 3000); } });
```

`render()` 를 부르지 않는 이유: 규칙은 목록 화면에 안 나온다. 환경설정을 열 때 그려진다.

- [ ] **Step 4: 기본 규칙과 시드 함수 넣기**

4167행 `function _go(sub, modalCall){...}` 끝난 **바로 다음 줄**에 넣는다.

```js
/* ══════ 규칙 분류 — 순수 로직 (테스트 대상) ══════
   미분류로 쌓인 명함을 회사명 단어로 폴더에 나눠 담는다.
   '청' 처럼 짧은 말은 청소·청과에도 걸려 일부러 뺐다 — 시청·구청처럼 길게 적는다. */
const CLASSIFY_DEFAULTS = [
  { key:'nomu',   order:1, enabled:true, toGroupName:'노무사',
    words:['노무법인','노무사사무소','노무사무소'] },
  { key:'expert', order:2, enabled:true, toGroupName:'전문가',
    words:['세무','회계','법무','변호사','법률','변리','특허','감정평가'] },
  { key:'public', order:3, enabled:true, toGroupName:'기관·공공',
    words:['공단','공사','시청','구청','군청','도청','교육청','경찰청','소방서','협회','진흥원','재단','연구원','공제회'] },
  { key:'life',   order:4, enabled:true, toGroupName:'생활·기타',
    words:['배달','꽃배달','보험','갈비','식당','마트','택배','렌터카','화수목','미용실','세탁','피자','치킨'] }
];
```

- [ ] **Step 5: 시드 함수와 목록 함수 넣기**

Step 4 바로 아래에 이어 붙인다.

```js
/* 규칙이 하나도 없을 때만 기본 4개를 심는다.
   기록 이름(key)을 고정으로 써서, 두 사람이 동시에 심어도 같은 4개로 모인다. */
function seedClassifyRules(){
  try{
    if(Store.mode!=='firebase') return;
    if(Object.keys(state.rules||{}).length) return;
    CLASSIFY_DEFAULTS.forEach(r=>{ Store.putRule({...r, words:r.words.slice()}); });
  }catch(e){ console.warn('[seedClassifyRules]', e&&e.message); }
}
/* 지금 쓸 규칙 — 저장된 게 있으면 그것, 없으면 기본값. 항상 order 오름차순 */
function classifyRuleList(){
  const saved = Object.values(state.rules||{}).filter(r=>r&&r.key);
  const list = saved.length ? saved : CLASSIFY_DEFAULTS.map(r=>({...r, words:r.words.slice()}));
  return list.slice().sort((a,b)=>(a.order||0)-(b.order||0));
}
```

- [ ] **Step 6: 문법 검사**

Run:
```bash
python "C:/Users/fair0/AppData/Local/Temp/claude/C--Users-fair0-OneDrive-----------------/caac592f-d4d4-4b0b-96a0-58fa98afa93d/scratchpad/extract_scripts.py" "C:/Users/fair0/Documents/pureunall/pu-cards.html"
```
Expected: 모든 블록 `OK` 로 끝나고 마지막 줄 `ALL OK`

- [ ] **Step 7: 커밋**

```bash
git add pu-cards.html
git commit -m "feat(명함첩): 분류 규칙 저장 계층 — 전 직원이 함께 쓰는 규칙 4개"
```

---

### Task 2: 규칙 매칭 엔진 — 순수 로직과 검사

전역 상태(`state`·`myEmail`)를 쓰지 않게 만든다. 그래야 Node에서 그대로 돌려 검사할 수 있고, 화면 없이도 옳고 그름을 가릴 수 있다.

**Files:**
- Modify: `C:\Users\fair0\Documents\pureunall\pu-cards.html` — Task 1 Step 5 코드 바로 아래
- Create: `C:\Users\fair0\Documents\pureunall\tests\cards-classify-rules.test.js`

**Interfaces:**
- Consumes: `CLASSIFY_DEFAULTS`, `classifyRuleList()` (Task 1)
- Produces:
  - `ruleHit(it, r)` → `boolean`
  - `classifyTargets(items, rules, kind)` → `{ byKey:{[key]:item[]}, unmatched:item[], keys:string[] }`
  - `findRuleGroup(groups, r, kind)` → `{ gid:string|null, missing?:true, locked?:true }`

- [ ] **Step 1: 실패하는 검사부터 쓴다**

Create `tests/cards-classify-rules.test.js`:

```js
/* 규칙 일괄 분류 — 틀리는 방향이 둘이다.
   놓치면 5,000장이 그대로 남고, 지나치면 손으로 정리해 둔 폴더를 덮어쓴다.
   특히 '미분류만 건드린다'와 '먼저 맞은 규칙 하나만'은 조용히 어긋나기 쉽다. */
const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');
const vm = require('node:vm');

const HTML = path.join(__dirname, '..', 'pu-cards.html');
const src = fs.readFileSync(HTML, 'utf8');

function slice(a, b){
  const i = src.indexOf(a);
  if(i < 0) throw new Error('시작 표식 못찾음: ' + a);
  const j = src.indexOf(b, i);
  if(j < 0) throw new Error('끝 표식 못찾음: ' + b);
  return src.slice(i, j);
}

/* 순수 로직 토막만 떼어 돌린다. 화면·저장은 들어오지 않는다. */
function load(){
  const code = slice(
    '/* ══════ 규칙 분류 — 순수 로직 (테스트 대상) ══════',
    '/* ══════ 규칙 분류 — 화면 ══════');
  const ctx = {
    console, Object, Array, String, JSON, Date, Math, RegExp,
    /* 파일 위쪽(1037행)에 있는 것과 같은 정의 — 앞의 번호를 떼고 공백을 지운다 */
    _canon: s => String(s||'').replace(/^\s*\d+\s*[.)\-]?\s*/,'').replace(/\s/g,''),
    state: { rules:{} },
    Store: { mode:'local' }
  };
  vm.createContext(ctx);
  vm.runInContext(code, ctx);
  /* const 로 선언한 값은 샌드박스 전역 객체에 붙지 않는다 (function 선언만 붙는다).
     식을 평가해 꺼내 온다. */
  ctx.read = expr => vm.runInContext(expr, ctx);
  return ctx;
}

/* 샌드박스가 만든 배열·객체는 바깥 realm 의 Array 를 상속하지 않아
   assert.deepEqual(=strict) 이 "구조는 같은데 다르다"며 떨어진다.
   이 파일 옆 dup-sweep.test.js 와 같이 글자로 바꿔 비교한다. */
const S = v => JSON.stringify(v);
const ids = list => Array.from(list||[]).map(x=>x.id).join(',');

const card = (id, company, group) => ({ id, kind:'card', company, name:'', group:group||'' });

test('기본 규칙 4개는 모두 갈 곳과 단어를 갖는다', () => {
  const c = load();
  const D = c.read('CLASSIFY_DEFAULTS');
  assert.equal(D.length, 4);
  D.forEach(r => {
    assert.ok(r.key, '규칙에 key가 있어야 한다');
    assert.ok(r.toGroupName, r.key + ' 에 보낼 폴더 이름이 없다');
    assert.ok(r.words && r.words.length, r.key + ' 에 단어가 없다');
  });
  /* 짧은 '청'은 청소·청과에도 걸린다 — 들어가 있으면 안 된다 */
  const pub = D.find(r => r.key === 'public');
  assert.equal(Array.from(pub.words).indexOf('청'), -1);
});

test('회사명에 단어가 있으면 걸린다', () => {
  const c = load();
  const r = { key:'nomu', enabled:true, words:['노무법인'] };
  assert.equal(c.ruleHit(card('a','노무법인로율'), r), true);
  assert.equal(c.ruleHit(card('b','좋은상사'), r), false);
});

test('꺼 둔 규칙은 걸리지 않는다', () => {
  const c = load();
  assert.equal(c.ruleHit(card('a','노무법인로율'), { key:'nomu', enabled:false, words:['노무법인'] }), false);
});

test('이름 칸에만 있어도 걸린다 — 회사 칸이 비어 들어온 명함이 많다', () => {
  const c = load();
  const it = { id:'a', kind:'card', company:'', name:'동북아노무법인', group:'' };
  assert.equal(c.ruleHit(it, { key:'nomu', enabled:true, words:['노무법인'] }), true);
});

test('미분류만 대상 — 이미 폴더에 든 명함은 건드리지 않는다', () => {
  const c = load();
  const items = [
    card('a','노무법인로율'),            /* 미분류 → 대상 */
    card('b','노무법인가나','g-지인')     /* 이미 지인 폴더 → 손대면 안 됨 */
  ];
  const out = c.classifyTargets(items, c.read('CLASSIFY_DEFAULTS'), 'card');
  assert.equal(ids(out.byKey.nomu), 'a');
});

test('먼저 맞은 규칙 하나만 — 여러 규칙에 걸려도 위쪽이 이긴다', () => {
  const c = load();
  /* '세무법인 공단로' 는 expert(세무)와 public(공단)에 둘 다 걸린다 */
  const items = [ card('a','세무법인 공단로') ];
  const out = c.classifyTargets(items, c.read('CLASSIFY_DEFAULTS'), 'card');
  assert.equal(ids(out.byKey.expert), 'a');
  assert.equal(ids(out.byKey.public), '');
});

test('order 를 바꾸면 이기는 규칙도 바뀐다', () => {
  const c = load();
  const rules = [
    { key:'public', order:1, enabled:true, toGroupName:'기관·공공', words:['공단'] },
    { key:'expert', order:2, enabled:true, toGroupName:'전문가',   words:['세무'] }
  ];
  const out = c.classifyTargets([card('a','세무법인 공단로')], rules, 'card');
  assert.equal(ids(out.byKey.public), 'a');
});

test('명함과 사업자등록증은 섞이지 않는다', () => {
  const c = load();
  const items = [
    card('a','노무법인로율'),
    { id:'b', kind:'biz', company:'노무법인가나', name:'', group:'' }
  ];
  const D = c.read('CLASSIFY_DEFAULTS');
  assert.equal(ids(c.classifyTargets(items, D, 'card').byKey.nomu), 'a');
  assert.equal(ids(c.classifyTargets(items, D, 'biz').byKey.nomu), 'b');
});

test('아무 규칙에도 안 걸리면 남는 쪽으로 간다', () => {
  const c = load();
  const out = c.classifyTargets([card('a','그냥회사')], c.read('CLASSIFY_DEFAULTS'), 'card');
  assert.equal(ids(out.unmatched), 'a');
});

test('두 번 적용해도 안전 — 폴더가 정해진 뒤에는 대상이 0장', () => {
  const c = load();
  const D = c.read('CLASSIFY_DEFAULTS');
  const items = [ card('a','노무법인로율') ];
  assert.equal(c.classifyTargets(items, D, 'card').byKey.nomu.length, 1);
  items[0].group = 'g-노무사';           /* 적용된 뒤의 모습 */
  assert.equal(c.classifyTargets(items, D, 'card').byKey.nomu.length, 0);
});

test('보낼 폴더를 이름으로 찾는다 — 앞 번호는 무시한다', () => {
  const c = load();
  const groups = { g1:{ id:'g1', name:'1. 노무사', kind:'card' } };
  const r = { key:'nomu', toGroupName:'노무사' };
  assert.equal(S(c.findRuleGroup(groups, r, 'card')), S({ gid:'g1' }));
});

test('폴더가 없으면 만들어야 한다고 알린다', () => {
  const c = load();
  const r = { key:'public', toGroupName:'기관·공공' };
  assert.equal(c.findRuleGroup({}, r, 'card').missing, true);
});

test('잠긴 폴더로는 보내지 않는다', () => {
  const c = load();
  const groups = { g1:{ id:'g1', name:'노무사', kind:'card', locked:true, lockOwner:'남@x.com' } };
  const got = c.findRuleGroup(groups, { key:'nomu', toGroupName:'노무사' }, 'card');
  assert.equal(got.locked, true);
  assert.equal(got.gid, null);
});

test('같은 이름이라도 명함용 폴더와 사업자용 폴더는 다르다', () => {
  const c = load();
  const groups = {
    g1:{ id:'g1', name:'노무사', kind:'card' },
    g2:{ id:'g2', name:'노무사', kind:'biz' }
  };
  const r = { key:'nomu', toGroupName:'노무사' };
  assert.equal(c.findRuleGroup(groups, r, 'card').gid, 'g1');
  assert.equal(c.findRuleGroup(groups, r, 'biz').gid, 'g2');
});
```

- [ ] **Step 2: 검사가 실패하는지 확인**

Run:
```bash
node --test tests/cards-classify-rules.test.js
```
Expected: FAIL — `시작 표식 못찾음: /* ══════ 규칙 분류 — 순수 로직 (테스트 대상) ══════`
(Task 1에서 시작 표식은 넣었으나 끝 표식 `규칙 분류 — 화면` 이 아직 없으므로 `끝 표식 못찾음` 으로 실패할 수도 있다. 둘 중 어느 쪽이든 실패가 정상이다.)

- [ ] **Step 3: 매칭 엔진 구현**

`pu-cards.html` 의 `classifyRuleList()` 함수 **바로 아래**에 넣는다.

```js
/* 이 명함이 이 규칙에 걸리는가 — 회사명과 이름에서 단어를 찾는다.
   회사 칸이 비고 이름 칸에 상호가 들어온 명함이 많아 둘 다 본다. */
function ruleHit(it, r){
  if(!it || !r || r.enabled===false) return false;
  const hay = String(it.company||'') + ' ' + String(it.name||'');
  const ws = Array.isArray(r.words) ? r.words : [];
  for(let i=0;i<ws.length;i++){
    const w = String(ws[i]||'').trim();
    if(w && hay.indexOf(w) >= 0) return true;
  }
  return false;
}
/* 규칙별로 옮길 명함을 고른다.
   ① 미분류만 — 손으로 넣어 둔 폴더는 절대 안 건드린다
   ② 지금 보는 종류(명함/사업자)만
   ③ 위에서 아래로 먼저 맞은 규칙 하나만 (한 장이 두 폴더로 갈 수 없다) */
function classifyTargets(items, rules, kind){
  const on = (rules||[]).filter(r=>r && r.key && r.enabled!==false)
                        .slice().sort((a,b)=>(a.order||0)-(b.order||0));
  const byKey = {}; on.forEach(r=>{ byKey[r.key] = []; });
  const unmatched = [];
  (items||[]).forEach(it=>{
    if(!it || !it.id) return;
    if(((it.kind==='biz') ? 'biz' : 'card') !== kind) return;
    if(String(it.group||'') !== '') return;
    let hit = null;
    for(let i=0;i<on.length;i++){ if(ruleHit(it, on[i])){ hit = on[i]; break; } }
    if(hit) byKey[hit.key].push(it); else unmatched.push(it);
  });
  return { byKey, unmatched, keys: on.map(r=>r.key) };
}
/* 규칙이 보낼 폴더 찾기 — id 가 아니라 이름으로 가리킨다.
   폴더를 지웠다 다시 만들어도 규칙이 살아남게 하려는 것이다 (autoFolder 의 ensure 와 같은 뜻).
   잠긴 폴더로는 보내지 않는다 — 남의 개인 폴더에 명함이 섞이면 되돌리기도 어렵다. */
function findRuleGroup(groups, r, kind){
  const nm = _canon(r && r.toGroupName);
  const g = Object.values(groups||{}).find(x =>
    x && _canon(x.name) === nm && ((x.kind||'card') === kind));
  if(!g) return { gid:null, missing:true };
  if(g.locked) return { gid:null, locked:true };
  return { gid:g.id };
}
/* ══════ 규칙 분류 — 화면 ══════ */
```

마지막 줄 `/* ══════ 규칙 분류 — 화면 ══════ */` 이 검사가 잘라 읽는 **끝 표식**이다. Task 3의 화면 코드가 이 줄 아래에 붙는다.

- [ ] **Step 4: 검사 통과 확인**

Run:
```bash
node --test tests/cards-classify-rules.test.js
```
Expected: PASS — `# pass 14` / `# fail 0`

- [ ] **Step 5: 문법 검사**

Run:
```bash
python "C:/Users/fair0/AppData/Local/Temp/claude/C--Users-fair0-OneDrive-----------------/caac592f-d4d4-4b0b-96a0-58fa98afa93d/scratchpad/extract_scripts.py" "C:/Users/fair0/Documents/pureunall/pu-cards.html"
```
Expected: `ALL OK`

- [ ] **Step 6: 커밋**

```bash
git add pu-cards.html tests/cards-classify-rules.test.js
git commit -m "feat(명함첩): 규칙 매칭 — 미분류만, 먼저 맞은 하나만, 잠긴 폴더는 뺀다"
```

---

### Task 3: 환경설정 🧹정리 탭 안 인라인 화면

**Files:**
- Modify: `C:\Users\fair0\Documents\pureunall\pu-cards.html` — Task 2의 `/* ══════ 규칙 분류 — 화면 ══════ */` 바로 아래
- Modify: `C:\Users\fair0\Documents\pureunall\pu-cards.html:3261` 근처 (`openCleanupCenter` 에 줄 추가)
- Modify: `C:\Users\fair0\Documents\pureunall\pu-cards.html:4244` 근처 (`SUB` 표에 등록)

**Interfaces:**
- Consumes: `classifyRuleList()`, `classifyTargets()`, `findRuleGroup()` (Task 1·2), `showPanel()`, `setSub()`, `_go()`, `_panelTarget`, `esc()`, `toast()`
- Produces:
  - `classifyPlan()` → `{ kind:'card'|'biz', rules:rule[], plan:{byKey,unmatched,keys}, blocked:{[key]:'locked'}, targetN:number, unmatchedN:number }` — Task 4가 그대로 쓴다
  - `openClassifyRules()` → void (화면 그리기)
  - `toggleClassifyRule(key)` · `editRuleWords(key)` → void

- [ ] **Step 1: 지금 화면 기준으로 계산해 주는 함수**

`/* ══════ 규칙 분류 — 화면 ══════ */` 바로 아래에 넣는다.

```js
/* 지금 보고 있는 종류(명함/사업자) 기준으로 규칙별 걸린 장수를 센다.
   화면과 적용이 같은 답을 보게 이 한 곳에서만 계산한다. */
function classifyPlan(){
  const kind  = state.tab==='biz' ? 'biz' : 'card';
  const rules = classifyRuleList();
  const items = Object.values(state.items||{});
  const plan  = classifyTargets(items, rules, kind);
  const blocked = {};
  rules.forEach(r=>{
    const t = findRuleGroup(state.groups||{}, r, kind);
    if(t.locked) blocked[r.key] = 'locked';
  });
  const targetN = rules.reduce((s,r)=> s + (blocked[r.key] ? 0 : ((plan.byKey[r.key]||[]).length)), 0);
  return { kind, rules, plan, blocked, targetN, unmatchedN: plan.unmatched.length };
}
```

- [ ] **Step 2: 화면 함수**

Step 1 바로 아래에 이어 붙인다.

```js
/* 규칙 분류 화면 — 환경설정 안에서는 팝업 없이 그 자리에 펼친다. */
let _rulePreview = '';              /* 지금 펼쳐 본 규칙 key */
function openClassifyRules(){
  const P = classifyPlan();
  const kindName = P.kind==='biz' ? '사업자등록증' : '명함';
  const inline = _panelTarget==='inline';
  const midN = Object.values(state.items||{})
    .filter(it=>it && ((it.kind==='biz')?'biz':'card')===P.kind && String(it.group||'')==='').length;

  let h = inline
    ? `<p class="setnote">폴더가 정해지지 않은 <b>${kindName} ${midN.toLocaleString()}장</b>을 회사명 단어로 나눠 담습니다.
       <b>이미 폴더에 든 명함은 건드리지 않습니다.</b></p>`
    : `<div class="mhead"><b>🗂 규칙으로 한 번에 분류</b>${_closeBtn()}</div>
       <p style="font-size:12.5px;color:var(--sub);line-height:1.7;margin-bottom:10px">
       폴더가 정해지지 않은 <b>${kindName} ${midN.toLocaleString()}장</b>을 회사명 단어로 나눠 담습니다.
       <b>이미 폴더에 든 명함은 건드리지 않습니다.</b></p>`;

  P.rules.forEach(r=>{
    const n = (P.plan.byKey[r.key]||[]).length;
    const off = r.enabled===false;
    const lock = P.blocked[r.key]==='locked';
    const words = (r.words||[]).join(' · ');
    h += `<div class="ditem" style="align-items:flex-start;flex-direction:column;gap:6px">
      <div style="display:flex;align-items:center;gap:8px;width:100%">
        <label style="display:flex;align-items:center;gap:6px;cursor:pointer;font-weight:800;font-size:13px">
          <input type="checkbox" ${off?'':'checked'} ${lock?'disabled':''}
            onchange="toggleClassifyRule('${r.key}')"> → 📁 ${esc(r.toGroupName)}</label>
        <b style="margin-left:auto;font-size:13px;color:${lock?'var(--red)':(off?'#9aa6bd':'var(--orange)')}">
          ${lock ? '잠긴 폴더 — 보낼 수 없음' : (off ? '꺼짐' : n.toLocaleString()+'장')}</b>
      </div>
      <div style="font-size:12px;color:var(--sub);line-height:1.6;word-break:keep-all">${esc(words)}</div>
      <div style="display:flex;gap:6px">
        <button class="btn ghost" style="padding:5px 11px;font-size:12px"
          onclick="editRuleWords('${r.key}')">단어 고치기</button>
        ${n && !off && !lock ? `<button class="btn ghost" style="padding:5px 11px;font-size:12px"
          onclick="previewRule('${r.key}')">${_rulePreview===r.key?'접기':'미리보기'}</button>` : ''}
      </div>
      ${_rulePreview===r.key ? ruleSampleHtml(P.plan.byKey[r.key]||[]) : ''}
    </div>`;
  });

  h += `<div class="ditem" style="align-items:center"><div class="v">
      <b style="color:var(--sub)">규칙에 안 걸림</b>
      <br><span style="font-size:12px;color:var(--sub)">손으로 정리하거나 단어를 더하세요</span></div>
      <b style="flex:none;font-size:13px;color:var(--sub)">${P.unmatchedN.toLocaleString()}장</b></div>`;

  h += P.targetN
    ? `<button class="btn primary" style="width:100%;margin-top:12px"
         onclick="applyClassifyRules()">✅ ${P.targetN.toLocaleString()}장 지금 분류하기</button>`
    : `<p style="font-size:12.5px;color:var(--green);padding:12px 4px;text-align:center">
         ✓ 규칙으로 옮길 ${kindName}이 없습니다</p>`;
  h += `<div id="undoBar"></div>`;     /* 되돌리기 줄 — Task 4에서 채운다 */
  h += `<p style="font-size:11.5px;color:var(--sub);line-height:1.7;margin-top:10px">
    ※ 폴더만 바뀝니다. 명함은 지워지지 않고, 적용 뒤 <b>되돌리기</b>로 통째로 되돌릴 수 있어요.</p>`;
  showPanel(h);
}
/* 미리보기 — 걸린 명함 20장까지 이름·회사를 그 자리에 펼친다 */
function ruleSampleHtml(list){
  const rows = list.slice(0,20).map(it=>
    `<div style="font-size:12px;color:var(--ink);padding:3px 0;border-top:1px solid var(--line)">
      ${esc(it.name||'')}${it.name&&it.company?' · ':''}<span style="color:var(--sub)">${esc(it.company||'')}</span></div>`).join('');
  const more = list.length>20 ? `<div style="font-size:11.5px;color:var(--sub);padding-top:5px">…외 ${(list.length-20).toLocaleString()}장</div>` : '';
  return `<div style="width:100%;background:var(--bg2);border-radius:8px;padding:8px 10px;margin-top:2px">${rows}${more}</div>`;
}
function previewRule(key){ _rulePreview = (_rulePreview===key ? '' : key); _refresh(openClassifyRules); }
/* 규칙 켜고 끄기 — 저장된 규칙이 없으면 기본값을 먼저 심고 바꾼다 */
function toggleClassifyRule(key){
  const cur = classifyRuleList().find(r=>r.key===key); if(!cur) return;
  Store.putRule({ ...cur, words:(cur.words||[]).slice(), enabled: cur.enabled===false });
  setTimeout(()=>_refresh(openClassifyRules), 60);
}
/* 단어 고치기 — 쉼표로 나눠 적는다 */
function editRuleWords(key){
  const cur = classifyRuleList().find(r=>r.key===key); if(!cur) return;
  const now = (cur.words||[]).join(', ');
  const txt = prompt(`📁 ${cur.toGroupName} 로 보낼 단어를 쉼표로 나눠 적으세요.\n\n`
    + `· 회사명이나 이름에 이 말이 있으면 이 폴더로 갑니다\n`
    + `· 짧은 말은 엉뚱한 곳에 걸립니다 (예: '청' → 청소·청과)`, now);
  if(txt===null) return;
  const words = txt.split(',').map(s=>s.trim()).filter(Boolean);
  if(!words.length) return toast('단어가 비어 있습니다');
  Store.putRule({ ...cur, words, enabled: cur.enabled!==false });
  setTimeout(()=>_refresh(openClassifyRules), 60);
}
```

되돌리기 줄이 들어갈 `<div id="undoBar"></div>` 만 자리로 남겨 둔다. Task 4가 이 안을 채운다.
이 단계에서 없는 함수를 부르지 않으므로, Task 3만 배포해도 화면은 정상으로 돈다.

- [ ] **Step 3: 정리 센터에 줄 추가**

3261행 `h+=row('🗑','휴지통',...)` **바로 위**에 넣는다.

```js
  h+=row('🗂','규칙으로 한 번에 분류','폴더가 안 정해진 명함을 회사명 단어로 나눠 담습니다 — 미리보기 후 적용, 되돌리기 가능',
         classifyPlan().targetN,'장',_go('rules','openClassifyRules()'),'분류');
```

- [ ] **Step 4: 하위 화면 표에 등록**

4244행 `const SUB={ dedup:openDedup, ... }` 에 `rules` 를 더한다.

```js
      const SUB={ dedup:openDedup, similar:openSimilar, mixed:openMixedFix, namefix:openNameFix,
                  trash:openTrash, erpname:openErpNameCheck, rules:openClassifyRules };
```

- [ ] **Step 5: 문법 검사**

Run:
```bash
python "C:/Users/fair0/AppData/Local/Temp/claude/C--Users-fair0-OneDrive-----------------/caac592f-d4d4-4b0b-96a0-58fa98afa93d/scratchpad/extract_scripts.py" "C:/Users/fair0/Documents/pureunall/pu-cards.html"
```
Expected: `ALL OK`

- [ ] **Step 6: 기존 검사가 안 깨졌는지 확인**

Run:
```bash
node --test tests/cards-classify-rules.test.js
```
Expected: PASS — 끝 표식 아래에 화면 코드가 붙었으므로 잘라 읽는 범위는 그대로여야 한다. `# fail 0`

- [ ] **Step 7: 커밋**

```bash
git add pu-cards.html
git commit -m "feat(명함첩): 규칙 분류 화면 — 정리 탭 안에서 팝업 없이 미리보기"
```

---

### Task 4: 한 번에 적용하기와 되돌리기

**Files:**
- Modify: `C:\Users\fair0\Documents\pureunall\pu-cards.html` — Task 3의 `undoBarHtml()` 임시 정의를 대체하고 그 아래에 이어 붙임

**Interfaces:**
- Consumes: `classifyPlan()` (Task 3), `findRuleGroup()` (Task 2), `idxRecord(it)`·`uid()`·`Store`·`DB_ROOT`
- Produces: `applyClassifyRules()` · `undoClassify(batchId)` · `undoBarHtml()`

- [ ] **Step 1: 적용 함수 — 통신 한 번으로 몰아 보낸다**

Task 3에서 넣은 `openClassifyRules` 함수가 끝나는 `}` 다음 줄, `ruleSampleHtml` 정의 **바로 위**에 넣는다.

```js
/* 규칙 적용 — 명함 수천 장을 하나씩 쓰면 수천 번 통신한다.
   items·idx·되돌리기 기록을 한 번의 update() 로 몰아 보낸다 (rebuildIdxAll 과 같은 방식). */
async function applyClassifyRules(){
  if(Store.mode!=='firebase') return toast('클라우드에 연결된 상태에서만 됩니다');
  const P = classifyPlan();
  if(!P.targetN) return toast('옮길 명함이 없습니다');

  /* 보낼 폴더를 미리 확보한다 — 없으면 이 자리에서 만든다 */
  const gidOf = {};
  for(const r of P.rules){
    if(r.enabled===false || P.blocked[r.key]) continue;
    if(!(P.plan.byKey[r.key]||[]).length) continue;
    const t = findRuleGroup(state.groups||{}, r, P.kind);
    if(t.locked) continue;
    if(t.gid){ gidOf[r.key] = t.gid; continue; }
    const g = { id:uid(), name:r.toGroupName, kind:P.kind, order:Date.now() };
    Store.putGroup(g); state.groups[g.id] = g;      /* 폴더는 몇 개뿐이라 따로 써도 된다 */
    gidOf[r.key] = g.id;
  }

  const lines = P.rules.filter(r=>gidOf[r.key])
    .map(r=>`· ${r.toGroupName} ← ${(P.plan.byKey[r.key]||[]).length.toLocaleString()}장`).join('\n');
  if(!confirm(`아래대로 ${P.targetN.toLocaleString()}장을 옮깁니다.\n\n${lines}\n\n`
    + `· 폴더만 바뀝니다. 명함은 지워지지 않습니다\n`
    + `· 이미 폴더에 든 명함은 건드리지 않습니다\n`
    + `· 적용 뒤 [되돌리기]로 통째로 되돌릴 수 있습니다\n\n진행할까요?`)) return;

  const batchId = uid();
  const now = Date.now();
  const upd = {}, from = {};
  let n = 0;
  P.rules.forEach(r=>{
    const gid = gidOf[r.key]; if(!gid) return;
    (P.plan.byKey[r.key]||[]).forEach(it=>{
      from[it.id] = String(it.group||'');            /* 되돌릴 때 쓸 원래 폴더 */
      upd[`items/${it.id}/group`]     = gid;
      upd[`items/${it.id}/updatedAt`] = now;
      /* 폴더가 바뀌면 검색색인에 들어갈지 여부도 달라진다 — 같은 통신에 함께 고친다 */
      upd[`idx/${it.id}`] = idxRecord({ ...it, group:gid });
      n++;
    });
  });
  upd[`classifyLog/${batchId}`] = { id:batchId, at:now, by:(myEmail||''), n, kind:P.kind, from };

  toast(`분류하는 중… ${n.toLocaleString()}장`, 30000);
  try{
    await Store.db.ref(DB_ROOT).update(upd);
    await pruneClassifyLog();
    toast(`✅ ${n.toLocaleString()}장을 폴더로 옮겼습니다 — 되돌리려면 [되돌리기]`, 8000);
    setTimeout(()=>_refresh(openClassifyRules), 400);
  }catch(e){
    toast('❌ 실패: ' + ((e&&e.message)||'알 수 없음'), 6000);
  }
}
```

- [ ] **Step 2: 되돌리기 기록 읽기·줄이기**

Step 1 바로 아래에 이어 붙인다.

```js
/* 되돌리기 기록은 최근 10건만 남긴다 — 5,000장 기록이 쌓이면 무거워진다 */
let _classifyLog = {};
function watchClassifyLog(){
  try{
    if(Store.mode!=='firebase') return;
    Store.db.ref(DB_ROOT+'/classifyLog').on('value', s=>{ _classifyLog = s.val()||{}; });
  }catch(e){ console.warn('[watchClassifyLog]', e&&e.message); }
}
async function pruneClassifyLog(){
  try{
    const all = Object.values(_classifyLog||{}).filter(x=>x&&x.id).sort((a,b)=>(b.at||0)-(a.at||0));
    if(all.length <= 10) return;
    const upd = {};
    all.slice(10).forEach(x=>{ upd[`classifyLog/${x.id}`] = null; });
    await Store.db.ref(DB_ROOT).update(upd);
  }catch(e){ console.warn('[pruneClassifyLog]', e&&e.message); }
}
/* 되돌리기 줄 — 가장 최근 적용 한 건을 화면에 채운다.
   Task 3이 남겨 둔 <div id="undoBar"> 안에만 쓴다. 그 자리가 없으면 아무것도 안 한다. */
function renderUndoBar(){
  const el = $('undoBar'); if(!el) return;
  const last = Object.values(_classifyLog||{}).filter(x=>x&&x.id)
    .sort((a,b)=>(b.at||0)-(a.at||0))[0];
  if(!last){ el.innerHTML=''; return; }
  const d = new Date(last.at||0);
  const when = `${d.getMonth()+1}월 ${d.getDate()}일 ${String(d.getHours()).padStart(2,'0')}:${String(d.getMinutes()).padStart(2,'0')}`;
  el.innerHTML = `<div class="ditem" style="align-items:center;margin-top:10px">
    <div class="v"><b>↩️ 마지막 분류</b>
      <br><span style="font-size:12px;color:var(--sub)">${when} · ${(last.n||0).toLocaleString()}장</span></div>
    <button class="btn ghost" style="flex:none;padding:8px 14px"
      onclick="undoClassify('${last.id}')">되돌리기</button></div>`;
}
/* 되돌리기 — 기록해 둔 '원래 폴더'로 그대로 되돌린다. 이것도 통신 한 번. */
async function undoClassify(batchId){
  if(Store.mode!=='firebase') return toast('클라우드에 연결된 상태에서만 됩니다');
  const log = (_classifyLog||{})[batchId];
  if(!log || !log.from) return toast('되돌릴 기록을 찾지 못했습니다');
  const ids = Object.keys(log.from);
  if(!confirm(`${ids.length.toLocaleString()}장을 분류하기 전 상태로 되돌립니다.\n\n진행할까요?`)) return;
  const now = Date.now();
  const upd = {};
  ids.forEach(id=>{
    const it = state.items[id]; if(!it) return;      /* 그 사이 지워졌으면 건너뛴다 */
    const back = String(log.from[id]||'');
    upd[`items/${id}/group`]     = back;
    upd[`items/${id}/updatedAt`] = now;
    upd[`idx/${id}`] = idxRecord({ ...it, group:back });
  });
  upd[`classifyLog/${batchId}`] = null;
  toast('되돌리는 중…', 30000);
  try{
    await Store.db.ref(DB_ROOT).update(upd);
    toast(`↩️ ${ids.length.toLocaleString()}장을 되돌렸습니다`, 6000);
    setTimeout(()=>_refresh(openClassifyRules), 400);
  }catch(e){
    toast('❌ 되돌리기 실패: ' + ((e&&e.message)||'알 수 없음'), 6000);
  }
}
```

- [ ] **Step 3: 화면에서 되돌리기 줄을 그리게 잇기**

Task 3의 `openClassifyRules` 맨 끝 `showPanel(h);` **다음 줄**에 한 줄 더한다.
`showPanel` 이 화면을 그린 뒤라야 `#undoBar` 가 생겨 있다.

```js
  showPanel(h);
  renderUndoBar();
}
```

- [ ] **Step 4: 기록 구독 켜기**

Task 1 Step 3에서 넣은 `classifyRules` 구독은 `.on('value', ...)` 호출 하나다.
그 호출이 끝나는 `});` 다음 줄에, **같은 `subscribe()` 안에서** 한 줄 더한다.

```js
        this.db.ref(DB_ROOT+'/classifyRules').on('value', s => { state.rules = s.val()||{};
          if(!Store._ruleSeedDone){ Store._ruleSeedDone=true; setTimeout(seedClassifyRules, 3000); } });
        watchClassifyLog();
```

위 두 줄은 Task 1에서 이미 넣은 것이고, 이번에 더하는 것은 마지막 `watchClassifyLog();` 한 줄뿐이다.

- [ ] **Step 5: 문법 검사**

Run:
```bash
python "C:/Users/fair0/AppData/Local/Temp/claude/C--Users-fair0-OneDrive-----------------/caac592f-d4d4-4b0b-96a0-58fa98afa93d/scratchpad/extract_scripts.py" "C:/Users/fair0/Documents/pureunall/pu-cards.html"
```
Expected: `ALL OK`

- [ ] **Step 6: 전체 검사 통과 확인**

Run:
```bash
node --test tests/*.test.js
```
Expected: `# fail 0` — CI가 배포를 막지 않으려면 여기서 하나도 실패하면 안 된다.

- [ ] **Step 7: 커밋**

```bash
git add pu-cards.html
git commit -m "feat(명함첩): 규칙 적용은 통신 한 번으로 · 되돌리기 10건 보관"
```

---

### Task 5: 브라우저에서 진짜로 돌려 보고 배포

문법과 순수 로직은 검사로 잡았다. 남은 것은 화면이 실제로 그려지고, 저장·되돌리기가 손으로 눌렀을 때 도는지다.

**Files:**
- Create: `<scratchpad>/pu-cards-test.html` (원본 복사본, 검사용 — 저장소에 넣지 않는다)

**Interfaces:**
- Consumes: Task 1~4의 모든 함수
- Produces: 배포된 `origin/main` 커밋

- [ ] **Step 1: 검사용 사본 만들고 서버 띄우기**

```bash
cp "C:/Users/fair0/Documents/pureunall/pu-cards.html" "$SCRATCH/pu-cards-test.html"
```
그다음 `$SCRATCH` 에서 `python -m http.server 8791` 를 띄우고 브라우저로 `http://localhost:8791/pu-cards-test.html` 를 연다.

- [ ] **Step 2: 가짜 자료를 넣고 화면이 그려지는지 본다**

브라우저에서 실행:

```js
Store.mode='local'; window.myEmail='나@x.com';
state.items = {
  a1:{id:'a1',kind:'card',name:'',company:'노무법인로율',group:''},
  a2:{id:'a2',kind:'card',name:'동북아노무법인',company:'',group:''},
  a3:{id:'a3',kind:'card',name:'',company:'세무법인다솔',group:''},
  a4:{id:'a4',kind:'card',name:'',company:'전국꽃배달59000원',group:''},
  a5:{id:'a5',kind:'card',name:'',company:'그냥회사',group:''},
  a6:{id:'a6',kind:'card',name:'',company:'노무법인손대지마',group:'gz'}  /* 이미 폴더에 든 것 */
};
state.groups = { gz:{id:'gz',name:'지인',kind:'card'} };
state.rules={}; state.trash={}; state.views={};
document.body.classList.add('pc'); document.getElementById('pcRoot').hidden=false;
openSettingsPage(); setSetTab('clean'); setSub('rules');
document.getElementById('setInline').innerHTML.slice(0,400);
```

Expected: `#setInline` 에 규칙 화면이 들어 있고, `dedupBg` 는 열려 있지 않다 (팝업 없음).

- [ ] **Step 3: 셈이 맞는지 확인**

```js
const P = classifyPlan();
JSON.stringify({
  nomu: P.plan.byKey.nomu.map(x=>x.id),
  expert: P.plan.byKey.expert.map(x=>x.id),
  life: P.plan.byKey.life.map(x=>x.id),
  unmatched: P.plan.unmatched.map(x=>x.id),
  targetN: P.targetN
});
```

Expected: `nomu:['a1','a2']`, `expert:['a3']`, `life:['a4']`, `unmatched:['a5']`, `targetN:4`
**`a6` 이 어디에도 없어야 한다** — 이미 폴더에 든 명함은 안 건드린다.

- [ ] **Step 4: 잠긴 폴더가 막히는지 확인**

```js
state.groups.gl = {id:'gl',name:'노무사',kind:'card',locked:true,lockOwner:'남@x.com'};
const P2 = classifyPlan();
JSON.stringify({ blocked:P2.blocked, targetN:P2.targetN });
```

Expected: `blocked:{nomu:'locked'}`, `targetN:2` — 노무사로 갈 2장이 빠진다.

- [ ] **Step 5: 미리보기와 켜고 끄기 확인**

```js
delete state.groups.gl;
previewRule('nomu');
const html = document.getElementById('setInline').innerHTML;
JSON.stringify({ 미리보기: html.includes('노무법인로율'), 팝업열림: dedupBg.classList.contains('open') });
```

Expected: `{"미리보기":true,"팝업열림":false}`

- [ ] **Step 6: 목록 화면에서는 팝업으로 나오는지 확인**

```js
closeSettingsPage(); _panelTarget='modal';
openClassifyRules();
JSON.stringify({ 팝업열림: dedupBg.classList.contains('open') });
```

Expected: `{"팝업열림":true}` — 환경설정 밖에서는 예전처럼 팝업이 맞다.

- [ ] **Step 7: 서버 끄고 사본 지우기**

`http.server` 프로세스를 끝내고 `$SCRATCH/pu-cards-test.html` 를 지운다.

- [ ] **Step 8: 배포 — 다른 세션 파일은 건드리지 않는다**

이 저장소는 여러 세션이 동시에 쓴다. `origin/main` 이 앞서 있으면 임시 작업폴더로 옮겨 심는다.

먼저 옮길 커밋을 확인한다. Task 1~4에서 만든 커밋만 골라야 한다.

```bash
git log --oneline origin/main..HEAD -- pu-cards.html tests/cards-classify-rules.test.js
```

나온 해시를 **오래된 것부터 차례로** 넣는다 (`git log` 는 최신이 위이므로 순서를 뒤집는다).

```bash
git fetch origin main -q
git worktree add /c/Users/fair0/Documents/pu-main-wt-rules origin/main
git -C /c/Users/fair0/Documents/pu-main-wt-rules cherry-pick <Task1해시> <Task2해시> <Task3해시> <Task4해시>
git -C /c/Users/fair0/Documents/pu-main-wt-rules push origin HEAD:main
git worktree remove /c/Users/fair0/Documents/pu-main-wt-rules --force
```

`push` 가 `non-fast-forward` 로 거절되면 그 사이 다른 세션이 올린 것이다.
작업폴더를 지우고 `git fetch` 부터 다시 한다.

Expected: `push` 가 `HEAD -> main` 으로 끝나고, `git log --oneline -1 origin/main` 이 마지막 커밋을 가리킨다.

- [ ] **Step 9: 배포 결과 확인**

```bash
git fetch origin main -q
git log --oneline -5 origin/main
```
Expected: Task 1~4 커밋 4개가 모두 올라가 있다.

---

## 다음 단계 (이 계획 밖)

2단계 「보낼 곳」 패널·여러 장 끌기·탭 열·우클릭은 **이 계획을 배포한 뒤 따로 계획을 세운다.**
1단계를 실제로 돌려 5,000장 중 몇 장이 남는지 본 뒤라야 3단계(빠른 분류 모드)가 필요한지 판단할 수 있다.

## 되짚어 볼 위험

| 위험 | 이 계획에서 막는 곳 |
|---|---|
| 손으로 정리한 폴더를 덮어씀 | Task 2 — 미분류만 고른다. 검사로 못 박음 |
| 한 장이 두 폴더로 감 | Task 2 — 먼저 맞은 규칙 하나만. 검사로 못 박음 |
| 남의 개인 폴더에 명함이 섞임 | Task 2 `findRuleGroup` 이 `locked` 를 막고, Task 3이 화면에 이유를 적는다 |
| 5,000번 통신으로 앱이 멈춤 | Task 4 — `update()` 한 번 |
| 검색색인이 옛 폴더로 남음 | Task 4 — 같은 `update()` 에 `idx` 를 함께 넣는다 |
| 잘못 적용해 되돌릴 수 없음 | Task 4 — `classifyLog` 에 원래 폴더를 남기고 10건 보관 |
| 규칙이 저절로 돌아 사고 | 어디에서도 자동 호출하지 않는다. `applyClassifyRules` 는 버튼에만 붙는다 |
| CI 실패로 사이트 배포가 막힘 | Task 4 Step 5 — `node --test tests/*.test.js` 전체 통과 확인 |
