# kcareer 증명서 판정 확장 구현계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 증명서 판정을 "이름 어느 자리에든"으로 확장해 누락된 190건을 살리고, 외부기관 발급 실적증명 67건을 발급기관까지 채워 등록한다.

**Architecture:** `js/kcareer-scan.js`의 `RESULT_END`에서 증명서 낱말을 떼어 새 `CERT_ANY`로 옮긴다. 위촉장·표창·자격증은 "이름 끝" 규칙 그대로다. `classify`는 certdoc일 때 `certKind`(`ext`/`own`)를 함께 돌려주고, `kcareer.html`의 증명서 스캔이 그 값으로 두 묶음을 갈라 미리보기에 세고, 발급기관은 폴더 경로에서 추정한다.

**Tech Stack:** 순수 ES5 스타일 JS(빌드 없음), `node:test` + `node:assert/strict`

**설계서:** `docs/superpowers/specs/2026-08-06-kcareer-증명서판정확장-design.md`

## Global Constraints

- **위촉장·표창·자격증·수료증·협약서 판정은 바뀌면 안 된다.** 회귀테스트(상공회의소 위촉장, 위촉장 목록·위촉식 시나리오)를 유지하고 통과해야 한다.
- **`NEGATIVE` 부정어와 `ORIG_EXT` 확장자 제한은 증명서에도 그대로 적용.** 실측으로 검증됨 — 부정어가 막은 3건은 전부 신청서 묶음·빈 양식이었다.
- ⚠ **부정어에 `회의`를 넣지 말 것** — `상공회의소 위촉장`이 오탐된다.
- **이번 스캔은 외부기관(`ext`)만 기본 체크.** 본인 경력·재직증명(`own`) 146건은 사용자가 체크를 켜야 들어온다.
- **기관 후보 사전을 새로 만들지 않는다** — pu-erp 유형 코드표의 `agency`, 기존 실적의 `agency`, 기존 `certdoc`의 `issuer`에서만 모은다.
- **`made`(직접 만든 증명서)는 스캔이 건드리지 않는다.**
- 원본은 복사하지 않고 경로 참조(`src:'fs'`, `relPath`). 미리보기 → [등록], `scanId` 되돌리기, 등록된 `relPath`는 건너뛴다.
- 반복문 안에서 `set()` 금지 — 스토어 단일 쓰기.
- 작업 폴더는 `C:\Users\fair0\Documents\pureunall-kcareer` (main 고정). 매 태스크 끝 즉시 커밋, push 전 `git pull --rebase origin main`.
- 한국어 주석. 커밋 메시지는 한 줄 또는 `git commit -F <파일>`.

---

## File Structure

| 파일 | 역할 |
|---|---|
| `js/kcareer-scan.js` (수정) | `CERT_ANY`·`certKindOf` 신설, `RESULT_END`에서 증명서 낱말 제거, `classify`가 `certKind` 반환 |
| `tests/kcareer-scan.test.js` (수정) | 새 규칙 테스트 + 기존 deepEqual 기대값 보정 + 위촉장 회귀 유지 |
| `kcareer.html` (수정) | 증명서 스캔이 `certKind`로 갈라 세고, 발급기관을 경로에서 추정 |
| `tests/kcareer-html.test.js` (수정) | 정적 검사 추가 |

## 코드 사실 (2026-08-06 확인)

- `js/kcareer-scan.js:52` `RESULT_END`에 증명서 낱말 4개가 들어 있다:
  `경력증명서|실적증명서|참여확인서|수행확인서`. `KIND_MAP` 마지막 항목(`store:'certdoc'`)도 같은 목록을 쓴다.
- `classify`(같은 파일 74~85행)는 `{level, store, type, titleHint}`를 돌려준다. `certKind`는 없다.
- ⚠ `tests/kcareer-scan.test.js:67~68`이 `2.권형하노무사 경력증명서.pdf`에 대해
  `deepEqual`로 `{level:'sure', store:'certdoc', type:'', titleHint:''}`를 기대한다.
  **`certKind`를 더하면 이 테스트가 깨진다** — 기대값에 `certKind:'own'`을 넣어 함께 고친다.
- `tests/kcareer-scan.test.js:83`은 `능률협회,공인노무사회경력증명서.zip` → `maybe`를 기대한다.
  `.zip`은 `ORIG_EXT`가 아니므로 변화 없다.
- `kcareer.html:3162`이 증명서 스캔의 유일한 `KcareerScan.classify` 호출부(`cdScanNow` 안)다.
- 실측(2026-08-06): 증명서 낱말 든 원본파일 **205** / 현재 규칙이 잡는 것 **15** / 누락 **190**.
  외부기관 계열 **67** · 본인 계열 **146** · 경로에서 기관이 잡히는 것 **26/67**.

---

## Task 1: 증명서 낱말을 "어느 자리에든"으로 — 판정 모듈

**Files:**
- Modify: `js/kcareer-scan.js`
- Test: `tests/kcareer-scan.test.js`

**Interfaces:**
- Produces:
  - `KcareerScan.certKindOf(core) → 'ext' | 'own' | ''`
  - `KcareerScan.classify(name) → { level, store?, type?, titleHint?, certKind? }`
    - certdoc으로 판정될 때만 `certKind`가 붙는다. 그 밖의 판정에는 붙지 않는다.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/kcareer-scan.test.js`의 기존 certdoc 기대값을 고치고(67~68행) 새 테스트를 추가한다.

먼저 기존 줄을 이렇게 바꾼다:

```js
  assert.deepEqual(KS.classify('2.권형하노무사 경력증명서.pdf'),
    { level: 'sure', store: 'certdoc', type: '', titleHint: '', certKind: 'own' });
```

그리고 파일 끝에 추가:

```js
test('certKindOf: 외부기관 발급과 본인 경력증명을 가른다', () => {
  assert.equal(KS.certKindOf('2024 구조혁신지원사업 컨설팅 수행실적 증명서_라온전자(주)_권형하'), 'ext');
  assert.equal(KS.certKindOf('실적증명서_(주)라온테크_권'), 'ext');
  assert.equal(KS.certKindOf('2.공인노무사회 정부위탁사업 참여확인서_권형하노무사'), 'ext');
  assert.equal(KS.certKindOf('2025년 산업·일자리전환 지원 컨설팅 수행 확인(푸른노무법인)'), 'ext');
  assert.equal(KS.certKindOf('경력증명서-푸른'), 'own');
  assert.equal(KS.certKindOf('재직증명서-권형하'), 'own');
  assert.equal(KS.certKindOf('2015 체당금국선노무사 위촉장'), '');
});

test('classify: 증명서 낱말은 이름 끝이 아니어도 승격한다 (실사용 190건 누락)', () => {
  // 증명서 파일명은 '증명서_대상_본인이름' 꼴이 흔해 '이름 끝' 규칙으로는 전부 놓쳤다
  assert.deepEqual(KS.classify('2024 구조혁신지원사업 컨설팅 수행실적 증명서_(주)다라인터네셔널_권형하.pdf'),
    { level: 'sure', store: 'certdoc', type: '', titleHint: '', certKind: 'ext' });
  assert.deepEqual(KS.classify('경력증명서-푸른.pdf'),
    { level: 'sure', store: 'certdoc', type: '', titleHint: '', certKind: 'own' });
  assert.deepEqual(KS.classify('재직증명서-권형하.pdf'),
    { level: 'sure', store: 'certdoc', type: '', titleHint: '', certKind: 'own' });
  assert.deepEqual(KS.classify('실적증명서- 중소벤처기업부 비즈니스지원단.pdf'),
    { level: 'sure', store: 'certdoc', type: '', titleHint: '', certKind: 'ext' });
  assert.deepEqual(KS.classify('실적증명서_(주)가온_권.jpg'),
    { level: 'sure', store: 'certdoc', type: '', titleHint: '', certKind: 'ext' });
  assert.deepEqual(KS.classify('2025년 산업·일자리전환 지원 컨설팅 수행 확인(푸른노무법인).pdf'),
    { level: 'sure', store: 'certdoc', type: '', titleHint: '', certKind: 'ext' });
});

test('classify 증명서: 부정어·확장자 제한은 그대로 걸러낸다', () => {
  // 실측으로 확인된 3건 — 신청서 묶음과 빈 양식
  assert.equal(KS.classify('신청서 및 실적증명서.pdf').level, 'submission');
  assert.equal(KS.classify('실적증명서_양식.hwp').level, 'submission');
  assert.equal(KS.classify('붙임4. 컨설팅 수행실적 증명서 표준양식.hwp').level, 'submission');
  // 목록·엑셀은 계속 제외
  assert.equal(KS.classify('컨설팅_실적증명_목록.xlsx').level, 'submission');
  assert.equal(KS.classify('능률협회,공인노무사회경력증명서.zip').level, 'maybe');   // zip은 원본 확장자 아님
});

test('classify 회귀: 증명서 규칙이 위촉장으로 새지 않는다', () => {
  // 위촉장·표창·자격증은 '이름 끝' 규칙 그대로여야 한다
  assert.equal(KS.classify('위촉장 목록.xlsx').level, 'submission');
  assert.equal(KS.classify('★ 노동권익보호관 위촉식 시나리오.hwp').level, 'submission');
  assert.equal(KS.classify('★ 제1기 노동권익보호관 위촉 대상자 명단.hwp').level, 'submission');
  assert.equal(KS.classify('위촉 동의서.hwp').level, 'submission');
  assert.equal(KS.classify('위촉장 사본 스캔.pdf').level, 'maybe');                 // 끝이 '스캔' → 애매 유지
  // 위촉장 판정에는 certKind가 붙지 않는다
  assert.deepEqual(KS.classify('2015 질병판정위원회 위원 위촉장 (2015.05.07).pdf'),
    { level: 'sure', store: 'wiccok', type: '위촉장', titleHint: '' });
});
```

- [ ] **Step 2: 실패 확인**

```bash
node --test tests/kcareer-scan.test.js
```

기대: `certKindOf is not a function`과 `certKind` 누락으로 새 테스트 및 고친 기존 테스트가 실패.

- [ ] **Step 3: 최소 구현**

`js/kcareer-scan.js`의 정규식 블록(52~55행 부근)을 이렇게 바꾼다. **증명서 낱말 4개를 `RESULT_END`에서 뺀다**:

```js
  var ORIG_EXT = /\.(pdf|jpg|jpeg|png|hwp|hwpx)$/i;
  /* 위촉장·표창·자격증 계열 — '이름 끝'만 인정한다('위촉장 목록'·'위촉식 시나리오' 오탐 방지) */
  var RESULT_END = /(위촉장|위촉계약서|재위촉|위촉서|표창장|표창|포상|상장|자격증|수료증|이수증|협약서)$/;
  /* 증명서 계열 — 이름 어느 자리에 있어도 인정한다.
     '증명서_대상_본인이름' 꼴이 흔해 끝만 보면 205건 중 15건밖에 못 잡았다(실측 2026-08-06). */
  var CERT_ANY = /실적\s*증명|수행실적\s*증명|경력증명|재직증명|참여확인서|수행확인서|용역수행|수행\s*확인/;
  var RESULT_ANY = /위촉장|위촉계약|재위촉|표창|포상|상장|자격증|수료증|이수증|협약서|경력증명|실적증명|참여확인|수행확인/;
  // ⚠ 부정어에 '회의'를 넣지 말 것 — '상공회의소 위촉장'이 오탐된다(설계서 7.3).
  var NEGATIVE = /동의서|신청서|제출|공고|양식|서식|명단|시나리오|좌석|추천\s*계획|모집|목록|초안|회의록|회의자료|교재|자료집|매뉴얼/;
```

`KIND_MAP`의 마지막 항목(증명서)을 **지운다**. 증명서는 `CERT_ANY`로 따로 처리한다:

```js
  // 이름 끝 단어 → 어느 목록으로 갈지 (증명서는 CERT_ANY로 따로 판정한다)
  var KIND_MAP = [
    { re: /(위촉장|위촉계약서|재위촉|위촉서)$/,                  store: 'wiccok',  type: '위촉장', titleHint: '' },
    { re: /협약서$/,                                          store: 'wiccok',  type: '협약서', titleHint: '' },
    { re: /(표창장|표창|포상|상장)$/,                           store: 'wiccok',  type: '표창',   titleHint: '' },
    { re: /자격증$/,                                          store: 'cert',    type: '',       titleHint: '자격' },
    { re: /(수료증|이수증)$/,                                  store: 'cert',    type: '',       titleHint: '수료' }
  ];
```

`mapKind` 아래에 추가:

```js
  /* 증명서 성격 — 기관이 내 실적을 증명한 것(ext)과 푸른이 본인 경력을 증명한 것(own).
     외부기관 실적 탭 기관 묶음에는 ext만 붙어야 한다(설계서 5). */
  var CERT_EXT = /실적\s*증명|수행실적\s*증명|참여확인서|수행확인서|용역수행|수행\s*확인/;
  var CERT_OWN = /경력증명|재직증명/;
  function certKindOf(core) {
    var s = String(core || '');
    if (CERT_EXT.test(s)) return 'ext';
    if (CERT_OWN.test(s)) return 'own';
    return '';
  }
```

`classify`를 이렇게 바꾼다 — **증명서를 위촉장 계열보다 먼저** 본다(둘 다 걸리는 이름이 없으므로 순서 자체는 무해하지만, 증명서 판정이 `RESULT_END`에 의존하지 않음을 코드로 분명히 한다):

```js
  function classify(name) {
    if (isIgnoredFile(name)) return { level: 'ignore' };
    var core = cleanCore(name);
    var neg = NEGATIVE.test(core);
    var isOrig = ORIG_EXT.test(String(name));
    if (isOrig && !neg) {
      /* 증명서 — 이름 어느 자리에든 */
      if (CERT_ANY.test(core)) {
        var ck = certKindOf(core);
        if (ck) return { level: 'sure', store: 'certdoc', type: '', titleHint: '', certKind: ck };
      }
      /* 위촉장·표창·자격증 계열 — 이름 끝만 */
      if (RESULT_END.test(core)) {
        var k = mapKind(core);
        if (k) return { level: 'sure', store: k.store, type: k.type, titleHint: k.titleHint };
      }
    }
    if (RESULT_ANY.test(core) && !neg) return { level: 'maybe' };
    return { level: 'submission' };
  }
```

`api` 객체에 추가:

```js
    certKindOf: certKindOf,
```

- [ ] **Step 4: 통과 확인**

```bash
node --test tests/kcareer-scan.test.js
```

기대: 전부 통과. 특히 `classify 회귀: 증명서 규칙이 위촉장으로 새지 않는다`가 통과해야 한다.

- [ ] **Step 5: 커밋**

```bash
git add js/kcareer-scan.js tests/kcareer-scan.test.js && git commit -m "feat(kcareer): 증명서는 이름 어느 자리에든 인정 - 205건 중 15건만 잡던 문제"
```

---

## Task 2: 실자료 전량 재검증 (승인 게이트)

**Files:**
- 수정 없음 — 기존 `tools/kcareer-scan-report.js`를 그대로 돌린다

**Interfaces:**
- Consumes: Task 1의 `classify`

- [ ] **Step 1: 리포트 재실행**

```bash
node tools/kcareer-scan-report.js "C:\Users\fair0\OneDrive\바탕 화면\권형하 개인 이력등"
```

- [ ] **Step 2: 숫자 확인**

| 항목 | 변경 전 | 기대 |
|---|---|---|
| 판정 대상 | 3,976 | **3,976** (불변) |
| 확실 | 239 | **약 306** (+67 안팎, 증명서가 늘어난 만큼) |
| 애매 | 220 | 소폭 감소 (애매였던 증명서가 확실로 이동) |
| 검산 | 통과 | **통과** (확실+애매+제출서류 = 판정 대상) |
| 상공회의소 회귀 | 4건 | **4건** (불변 — 안 나오면 즉시 중단) |

**확실이 306 근처가 아니면 멈추고 원인을 본다.** 400을 넘으면 본인 경력증명서 146건까지
`sure`로 들어온 것이므로 정상이다(§Task 3에서 `certKind`로 갈라 담는다).

- [ ] **Step 3: 증명서만 따로 세기**

```bash
node -e "const fs=require('fs'),KS=require('./js/kcareer-scan.js');const p=require('path');let ext=0,own=0;(function w(d,pre){for(const e of fs.readdirSync(d,{withFileTypes:true})){const r=pre?pre+'/'+e.name:e.name;const f=p.join(d,e.name);let st;try{st=fs.statSync(f)}catch(x){continue}if(st.isDirectory()){w(f,r);continue}const c=KS.classify(e.name);if(c.level==='sure'&&c.store==='certdoc'){c.certKind==='ext'?ext++:own++}}})(process.argv[1],'');console.log('외부기관 ext:',ext,' 본인 own:',own)" "C:\Users\fair0\OneDrive\바탕 화면\권형하 개인 이력등"
```

기대: `외부기관 ext: 67` 안팎, `본인 own: 146` 안팎.

- [ ] **Step 4: 사용자 확인 (승인 게이트)**

`_scan_out/kcareer_판정.csv`에서 `판정=확실`·종류가 `certdoc`인 행을 훑어 **위촉장·표창이 섞이지
않았는지**, **빈 양식이 들어오지 않았는지** 사용자에게 확인받는다. 확인 없이 Task 3으로 넘어가지 않는다.

- [ ] **Step 5: 커밋 없음**

이 태스크는 검증만 한다. 코드 변경이 없으므로 커밋하지 않는다.
`_scan_out/`은 `.gitignore`에 있어 산출물이 커밋되지 않는다.

---

## Task 3: 스캔이 외부기관·본인을 갈라 센다

**Files:**
- Modify: `kcareer.html`
- Test: `tests/kcareer-html.test.js`

**Interfaces:**
- Consumes: `KcareerScan.classify`의 `certKind` (Task 1)
- Produces:
  - `_cdScanCtx = { scanId, ext:[], own:[], attaches:[], pickOwn:false, total }`
  - `toggleCdOwn()` — 본인 경력증명서 묶음 체크 토글

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/kcareer-html.test.js` 끝에 추가:

```js
test('증명서 스캔은 외부기관과 본인 것을 갈라 담는다', () => {
  const m = source.match(/async function cdScanNow\([\s\S]*?\n\}/);
  assert.ok(m, 'cdScanNow 함수가 있어야 합니다');
  assert.match(m[0], /certKind/, 'certKind로 갈라야 합니다');
  assert.match(m[0], /ext:/);
  assert.match(m[0], /own:/);
});

test('본인 경력증명서는 기본 체크 해제 상태다', () => {
  // 146건이 기본으로 들어오면 외부기관 기관 묶음이 지저분해진다(설계서 5)
  const m = source.match(/async function cdScanNow\([\s\S]*?\n\}/);
  assert.match(m[0], /pickOwn:\s*false/);
  assert.match(source, /function toggleCdOwn\(/);
});

test('cdScanCommit은 체크된 묶음만 저장한다', () => {
  const src = funcSource('cdScanCommit');
  assert.match(src, /pickOwn/, '체크 여부를 봐야 합니다');
  assert.match(src, /certKind/, '레코드에 성격을 남겨야 합니다');
});
```

- [ ] **Step 2: 실패 확인**

```bash
node --test tests/kcareer-html.test.js
```

- [ ] **Step 3: 구현**

**3-1.** `cdScanNow`의 분류 루프를 바꾼다. 현재 `adds` 하나에 담는 것을 `ext`/`own`으로 나눈다:

```js
async function cdScanNow(){
  var files = await fsScanAll(function(n){ toast('스캔 중… ' + n + '개'); });
  if(!files) return;
  var db = get('certdoc');
  var known = {};
  db.forEach(function(r){ if(r.relPath) known[r.relPath] = true; });
  var ext = [], own = [], attaches = [], seen = {};
  files.forEach(function(f){
    var c = KcareerScan.classify(f.name);
    if(c.level !== 'sure' || c.store !== 'certdoc') return;
    if(known[f.relPath]) return;                      /* 이미 등록된 경로 */
    var ck = f.name + '|' + f.size;
    if(seen[ck]) return; seen[ck] = true;              /* 같은 파일 사본 */
    var t = _cdFindAttachTarget(f, db);
    if(t){ attaches.push({ id: t.id, f: f }); return; }
    /* 외부기관이 발급한 실적증명(ext)과 본인 경력·재직증명(own)을 갈라 담는다 */
    (c.certKind === 'own' ? own : ext).push({ f: f, certKind: c.certKind || 'ext' });
  });
  _cdScanCtx = { scanId: 'CS' + new Date().toISOString().slice(0,10).replace(/-/g,'') + '-' + String(Date.now()).slice(-4),
                 ext: ext, own: own, attaches: attaches, pickOwn: false, total: files.length };
  renderCdScanPreview();
  $('modalCdScan').classList.add('open');
}
```

**3-2.** `renderCdScanPreview`를 두 묶음으로 바꾼다:

```js
function renderCdScanPreview(){
  if(!_cdScanCtx) return;
  var c = _cdScanCtx;
  var willSave = c.ext.length + (c.pickOwn ? c.own.length : 0);
  $('cdScanBody').innerHTML = '<div style="font-size:12px;background:#F3F8F6;border:1px solid var(--border);border-radius:8px;padding:8px 10px;margin-bottom:10px">'
    + '저장될 증명서 <b>' + willSave + '건</b> <span style="color:var(--text-mute)">(원본 없는 기존 증명서에 붙임 ' + c.attaches.length + '건 별도)</span>'
    + '<div style="color:var(--text-mute);margin-top:4px">원본은 복사하지 않고 경로만 저장합니다. 직접 만든 증명서는 건드리지 않습니다.</div></div>'
    + '<div style="display:flex;gap:8px;margin-bottom:10px;flex-wrap:wrap">'
    + '<div class="card" style="flex:1;min-width:120px;text-align:center"><div style="font-size:20px;font-weight:700;color:var(--teal)">' + c.ext.length + '</div><div style="font-size:11px;color:var(--text-mute)">🏛 외부기관 발급 실적증명</div></div>'
    + '<div class="card" style="flex:1;min-width:120px;text-align:center"><div style="font-size:20px;font-weight:700;color:' + (c.pickOwn ? 'var(--navy)' : 'var(--text-mute)') + '">' + c.own.length + '</div><div style="font-size:11px;color:var(--text-mute)">📄 본인 경력·재직증명</div></div>'
    + '</div>'
    + '<label style="display:flex;gap:6px;align-items:center;font-size:12px;padding:6px 0;margin-bottom:8px">'
    + '<input type="checkbox" id="cdOwnChk"' + (c.pickOwn ? ' checked' : '') + ' onchange="toggleCdOwn()">'
    + '<span>본인 경력·재직증명서 <b>' + c.own.length + '건</b>도 함께 등록 <span style="color:var(--text-mute)">(대개 같은 증명서의 연도별 사본입니다)</span></span></label>'
    + '<div style="font-size:12px;font-weight:600;margin:8px 0 4px;color:var(--teal)">🏛 외부기관 발급 실적증명 (' + c.ext.length + ')</div>'
    + c.ext.slice(0,50).map(function(a){ return '<div style="font-size:12px;padding:3px 0;border-bottom:1px solid var(--border-soft)">' + escapeHtml(a.f.name) + '</div>'; }).join('')
    + (c.ext.length>50 ? '<div style="font-size:11px;color:var(--text-mute)">…외 ' + (c.ext.length-50) + '건</div>' : '')
    + (c.attaches.length ? '<div style="font-size:12px;font-weight:600;margin:12px 0 4px">📎 기존 증명서에 원본만 붙임</div>'
        + c.attaches.map(function(a){ return '<div style="font-size:12px;padding:3px 0">' + escapeHtml(a.id) + ' ← ' + escapeHtml(a.f.name) + '</div>'; }).join('') : '');
}
function toggleCdOwn(){ if(!_cdScanCtx) return; _cdScanCtx.pickOwn = !_cdScanCtx.pickOwn; renderCdScanPreview(); }
```

**3-3.** `cdScanCommit`의 저장 루프를 바꾼다. 기존 `c.adds.forEach(...)` 블록을 **아래 코드로 통째로 교체**한다.

`_cdGuessIssuer`는 Task 4에서 만들므로, Task 3 단계에서는 **없으면 빈 문자열을 쓰는 방식**으로
호출해 둔다. 이렇게 하면 Task 3만 적용한 상태에서도 앱이 정상 동작하고, Task 4를 넣는 순간
발급기관이 자동으로 채워진다 — 이 줄을 다시 고칠 필요가 없다.

```js
  /* 외부기관은 항상, 본인 것은 체크했을 때만 저장한다 */
  var rows = c.ext.concat(c.pickOwn ? c.own : []);
  rows.forEach(function(a){
    var f = a.f;
    /* 발급기관 추정은 Task 4에서 들어온다 — 없으면 비워 두고 행의 🔍로 채운다 */
    var issuer = (typeof _cdGuessIssuer === 'function') ? _cdGuessIssuer(f) : '';
    db.unshift({ id: nextId(), kind: KcareerScan.cleanCore(f.name), org: '', issuer: issuer, usage: '', coverage: '',
      year: (String(f.name).match(/(20\d{2})/)||[])[1] || String(f.mtime||'').slice(0,4), date: '', memo: '',
      docSrc: 'received', certKind: a.certKind, src: 'fs', relPath: f.relPath, fileSize: f.size, fileMtime: f.mtime,
      scanId: c.scanId, submits: [], savedAt: new Date().toISOString() });
    n++;
  });
```

토스트도 고친다(본인 것을 함께 넣었는지 알 수 있게):

```js
  toast('✅ 증명서 새로 ' + n + '건' + (c.pickOwn ? ' (본인 경력증명 포함)' : '') + ' · 기존에 원본 붙임 ' + att + '건');
```

- [ ] **Step 4: 통과 확인**

```bash
node --test tests/kcareer-html.test.js
```

- [ ] **Step 5: 커밋**

```bash
git add kcareer.html tests/kcareer-html.test.js && git commit -m "feat(kcareer): 증명서 스캔이 외부기관과 본인 것을 갈라 센다 - 본인 것은 기본 해제"
```

---

## Task 4: 발급기관을 폴더 경로에서 추정

**Files:**
- Modify: `kcareer.html`
- Test: `tests/kcareer-html.test.js`

**Interfaces:**
- Consumes: `_agencyNorm`(기존), `get`, `PU_SYNC_STORES`
- Produces:
  - `_cdAgencyNames() → string[]` (기관 후보 — 시스템이 아는 이름만)
  - `_cdGuessIssuer(f) → string` (경로 → 파일명 순서로 찾고 없으면 `''`)

- [ ] **Step 1: 실패하는 테스트 작성**

```js
test('_cdAgencyNames는 새 사전을 만들지 않고 시스템이 아는 이름만 모은다', () => {
  const store = {
    consult: [{ agency: '한국능률협회' }, { agency: '충남경제진흥원' }, { agency: '' }],
    case: [], fund: [], etc: [],
    certdoc: [{ issuer: '공인노무사회' }, { issuer: '' }]
  };
  const ctx = {
    get: (k) => (store[k] || []).slice(),
    PU_SYNC_STORES: ['case', 'consult', 'fund', 'etc'],
    _puTypeAgencies: () => ['노사발전재단']
  };
  vm.runInNewContext(funcSource('_cdAgencyNames') + '\nglobalThis.__n = _cdAgencyNames();', ctx);
  const got = ctx.__n;
  ['한국능률협회', '충남경제진흥원', '공인노무사회', '노사발전재단'].forEach((a) => {
    assert.ok(got.includes(a), a + '가 후보에 있어야 합니다');
  });
  assert.ok(!got.includes(''), '빈 값은 후보에 넣지 않습니다');
});

test('_cdGuessIssuer는 폴더 경로에서 먼저 찾고 파일명으로 보완한다', () => {
  const ctx = {
    _cdAgencyNames: () => ['한국능률협회', '능률협회', '공인노무사회', '충남경제진흥원'],
    _agencyNorm: (s) => String(s || '').replace(/[\s\(\)（）\-·]/g, '').toLowerCase()
  };
  vm.runInNewContext(funcSource('_cdGuessIssuer'), ctx);
  assert.equal(ctx._cdGuessIssuer({
    name: '2024 구조혁신지원사업 컨설팅 수행실적 증명서_라온전자(주)_권형하.pdf',
    relPath: '6. 컨설팅 실적증명/구조혁신(능률협회)/2024/2024 구조혁신지원사업 컨설팅 수행실적 증명서_라온전자(주)_권형하.pdf'
  }), '능률협회');
  assert.equal(ctx._cdGuessIssuer({
    name: '2.공인노무사회 정부위탁사업 참여확인서_권형하노무사.pdf',
    relPath: '6. 컨설팅 실적증명/2.공인노무사회 정부위탁사업 참여확인서_권형하노무사.pdf'
  }), '공인노무사회');
  assert.equal(ctx._cdGuessIssuer({ name: '실적증명서.pdf', relPath: '6. 컨설팅 실적증명/실적증명서.pdf' }), '');
});
```

- [ ] **Step 2: 실패 확인**

```bash
node --test tests/kcareer-html.test.js
```

- [ ] **Step 3: 구현**

`_cdFindAttachTarget` 위에 추가:

```js
/* pu-erp 유형 코드표의 수행기관 이름들 — 코드표는 동기화 때 읽으므로 캐시에서 가져온다 */
function _puTypeAgencies(){
  var out = [];
  try{
    var cached = JSON.parse(LS.get(NS + 'pu_type_agencies') || '[]');
    if(Array.isArray(cached)) out = cached;
  }catch(e){}
  return out;
}

/* 기관 후보 — 새 사전을 만들지 않는다. 시스템이 이미 아는 이름만 모은다(설계서 6). */
function _cdAgencyNames(){
  var set = {};
  function add(s){ s = String(s || '').trim(); if(s) set[s] = 1; }
  _puTypeAgencies().forEach(add);
  PU_SYNC_STORES.forEach(function(k){ get(k).forEach(function(r){ if(r) add(r.agency); }); });
  get('certdoc').forEach(function(r){ if(r) add(r.issuer); });
  /* 긴 이름이 먼저 매칭되게 정렬 — '한국능률협회'가 '능률협회'보다 앞 */
  return Object.keys(set).sort(function(a,b){ return b.length - a.length; });
}

/* 발급기관 추정 — 폴더 경로에서 먼저, 없으면 파일명에서. 못 찾으면 빈 문자열(행의 🔍로 보정) */
function _cdGuessIssuer(f){
  var names = _cdAgencyNames();
  if(!names.length) return '';
  var dir = String(f.relPath || '');
  var i = dir.lastIndexOf('/');
  dir = i >= 0 ? dir.slice(0, i) : '';           /* 파일명을 뺀 경로만 */
  var hayDir = _agencyNorm(dir), hayName = _agencyNorm(f.name || '');
  for(var n = 0; n < names.length; n++){
    if(hayDir.indexOf(_agencyNorm(names[n])) >= 0) return names[n];
  }
  for(var m = 0; m < names.length; m++){
    if(hayName.indexOf(_agencyNorm(names[m])) >= 0) return names[m];
  }
  return '';
}
```

동기화가 코드표를 읽을 때 수행기관 이름을 캐시에 남긴다. `_puFetchPlan`의 코드표 읽기 루프
바로 뒤에 추가:

```js
  /* 증명서 발급기관 추정에 쓸 기관 이름을 남겨둔다 */
  try{
    var ags = {};
    for(var ak in typeMap){ (typeMap[ak] || []).forEach(function(t){ if(t && t.agency) ags[t.agency] = 1; }); }
    LS.set(NS + 'pu_type_agencies', JSON.stringify(Object.keys(ags)));
  }catch(e){}
```

`cdScanCommit`은 고칠 필요가 없다 — Task 3에서 `typeof _cdGuessIssuer === 'function'`으로
호출해 두었으므로 이 함수가 생기는 순간 자동으로 쓰인다.

⚠ `_puTypeAgencies`가 읽는 캐시(`pu_type_agencies`)는 **실적 동기화를 한 번 돌린 뒤에** 채워진다.
아직 동기화를 안 했다면 후보가 기존 실적의 `agency`와 `certdoc`의 `issuer`뿐이라 추정률이 낮다.
실사용 확인 전에 `🔄 실적 동기화`를 한 번 눌러 캐시를 채우는 것이 좋다.

- [ ] **Step 4: 통과 확인**

```bash
node --test tests/kcareer-scan.test.js tests/kcareer-pusync.test.js tests/kcareer-html.test.js
```

기대: 전부 통과.

- [ ] **Step 5: 브라우저 실사용 확인 (사용자와 함께)**

```bash
cd /c/Users/fair0/Documents/pureunall-kcareer && python -m http.server 8789
```

`http://localhost:8789/kcareer.html`

1. `이력서관리 › 경력증명서 보관` → `🔍 서류 폴더 스캔`
2. 미리보기에 **🏛 외부기관 67 · 📄 본인 146**이 갈라 보이고, **본인 쪽 체크가 꺼져** 있는지
3. `등록` → 표에 67건이 들어오고 **발급기관 칸에 능률협회·공인노무사회 등이 26건 안팎 채워졌는지**
4. `실적관리 › 외부기관 실적` → 능률협회·경제진흥원 묶음 아래 **"이 기관이 발급한 증명서 N건"이 0이 아닌지**
5. 발급기관이 빈 행에서 `🔍` 1~2건 → 채워지는지
6. `↩ 이번 스캔 되돌리기` → 원래대로
7. **위촉장 목록 197건이 그대로인지**(증명서 규칙이 위촉장으로 새지 않았는지)

- [ ] **Step 6: 커밋 + push**

```bash
git add kcareer.html tests/kcareer-html.test.js && git commit -m "feat(kcareer): 증명서 발급기관을 폴더 경로에서 추정 - 기관 후보는 시스템이 아는 이름만"
```

```bash
git pull --rebase origin main && node --test tests/kcareer-scan.test.js tests/kcareer-pusync.test.js tests/kcareer-html.test.js
```

전부 통과 후 push. CLAUDE.md §2에 이번 작업을 반영한다.

---

## 완료 조건

- [ ] `node --test tests/kcareer-scan.test.js tests/kcareer-pusync.test.js tests/kcareer-html.test.js` 전부 통과
- [ ] 리포트의 `확실`이 239 → 306 안팎으로 늘고 **검산 통과**
- [ ] 상공회의소 위촉장 회귀 4건 그대로, `위촉장 목록`·`위촉식 시나리오` 여전히 제외
- [ ] 미리보기가 외부기관 67 / 본인 146을 갈라 세고 본인 쪽 기본 해제
- [ ] 등록 후 발급기관이 26건 안팎 자동 채워짐
- [ ] 외부기관 실적 탭 기관 묶음의 증명서 건수가 0이 아님
- [ ] 위촉장 197건·직접 만든 증명서(`made`) 불변
