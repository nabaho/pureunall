# 서류 판독 층 구현 계획 — `js/pu-doc-read.js`

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 사진 한 장을 넣으면 그것이 명함인지 사업자등록증인지 중소기업확인서인지 가리고, 필요한 항목을 읽어 내고, **사업자등록번호가 맞는지 기계로 검증하고**, 각 앱의 필드 이름으로 바꿔 주는 공용 조각을 만든다. 저장은 하지 않는다.

**Architecture:** 판독을 아는 유일한 파일을 하나 둔다 — `js/pu-photo-store.js`가 "저장을 아는 유일한 파일"인 것과 같은 원리다. 사진첩·명함첩·푸른이알피가 모두 이 파일을 쓴다. 판독 방식(AI 모델·프롬프트)이 바뀌어도 앱은 손대지 않는다. Gemini 호출·프롬프트·체크섬은 저장소에 이미 있는 것을 옮겨와 한 곳으로 모은다(새로 발명하지 않는다).

**Tech Stack:** Gemini 2.0 Flash (`generativelanguage.googleapis.com`) · 국세청 사업자상태조회(odcloud.kr) · 빌드 없음 · `node --test`

## Global Constraints

- **판독 결과를 저장하지 않는다.** 이 층은 읽고 검증하고 변환만 한다. 저장은 부르는 쪽 책임(다음 단계).
- **`fetch`·키 읽기를 주입받는다.** 노드에서 가짜로 갈아끼워 검사할 수 있어야 한다 — **실제 Gemini·국세청에 절대 붙지 않는다.**
- **기존 것을 옮겨 온다, 새로 쓰지 않는다:**
  - 명함 프롬프트 = `pu-cards.html:1715` 문장 그대로
  - 사업자등록증 프롬프트 = `pu-cards.html:1716` 문장 그대로
  - 사업자번호 체크섬 = `pu-erp.html:52292-52298` `_validBizNo` 그대로
  - 국세청 조회 = `pu-erp.html:1047-1056` `lookupBizNoFromNTS` 형태 그대로
  - AI 키 3단 폴백 = `pu-camera.html:226-236` (로컬 → `pucards/config/geminiKey` → `data/app_config/geminiKey`)
- **AI가 읽은 사업자번호는 검증을 통과해야 '확정'이다.** 체크섬 실패 = 확정 아님. 국세청 조회는 키가 없으면 건너뛰되 **건너뛴 사실을 결과에 남긴다**(조회한 것처럼 보이면 안 된다).
- **필드 이름이 앱마다 다르다** — `bizno`(명함첩) / `bizNo`(푸른이알피) / `biz_no`(기금관리). 매핑표를 이 파일 한 곳에 둔다. 앱이 각자 바꾸면 조용히 안 붙는다.
- 한국어 주석 · ES5 스타일(`var`/`function`, 화살표 함수 금지 — `pu-photo-store.js`와 통일).
- `git add .` 금지, 커밋은 경로 지정, 커밋 직후 `git show --stat HEAD` 확인.
- 브랜치는 사진첩 B단계 브랜치가 아니라 **새 브랜치** `feat/pu-doc-read`. main 직접 push 금지.

## File Structure

| 파일 | 책임 |
|---|---|
| `js/pu-doc-read.js` (신규) | 서류 판독 층. 종류 판정 · 항목 판독 · 사업자번호 검증 · 앱별 필드 변환. **판독을 아는 유일한 파일** |
| `tests/pu-doc-read.test.js` (신규) | 단위 검사 (가짜 fetch·가짜 키) |

앱에 붙이는 일은 이 계획에 없다(다음 계획). 이 계획이 끝나면 **노드 검사로 판독 층이 증명된 상태**가 산출물이다.

## 서류 세 종류와 읽을 항목

| 종류 | `kind` | 읽을 항목 | 갈 곳 |
|---|---|---|---|
| 명함 | `card` | name, company, dept, title, mobile, tel, fax, email, companyTel, companyFax, companyAddr, website, address, memo | 명함첩 |
| 사업자등록증 | `bizreg` | company, ceo, bizno, corpno, openDate, bizType, bizItem, companyTel, companyFax, address, memo | 명함첩(`kind:'biz'`) → 사업장 정보 |
| 중소기업확인서 | `sme` | company, bizno, ceo, smeType(기업규모), issueNo(발급번호), issueDate(발급일), expiry(유효기간 만료일), industry(주업종) | 사업장 정보 + 만료 알림 |

`sme`의 `expiry`는 `kcareer.html:4568`의 `id_doc` 프롬프트가 `expiry`를 쓰는 선례를 따른다.

---

## Task 1: 사업자번호 검증 (순수 함수)

가장 먼저 만든다 — 이것이 "검증 통과하면 자동, 걸리면 사람 확인"의 판단 근거이고, AI 없이 시험할 수 있다.

**Files:**
- Create: `js/pu-doc-read.js`
- Test: `tests/pu-doc-read.test.js`

**Interfaces:**
- Produces: `window.PuDocRead` 에
  - `bizNoValid(v: string): boolean` — 하이픈·공백 섞여도 됨. 10자리 아니면 false
  - `bizNoDigits(v: string): string` — 숫자만
  - `fmtBizNo(v: string): string` — `123-45-67890` 꼴

- [x] **Step 1: 실패하는 테스트를 쓴다**

```js
'use strict';
// js/pu-doc-read.js 단위 검사 — 실행: node --test tests/*.test.js
const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');
const vm = require('node:vm');

function loadRead(env) {
  const src = fs.readFileSync(path.join(__dirname, '..', 'js', 'pu-doc-read.js'), 'utf8');
  const sandbox = Object.assign({ window: {}, console }, env || {});
  sandbox.globalThis = sandbox;
  vm.createContext(sandbox);
  new vm.Script(src, { filename: 'pu-doc-read.js' }).runInContext(sandbox);
  return sandbox.window.PuDocRead;
}

test('판독 층이 window에 붙는다', () => {
  assert.ok(loadRead(), 'window.PuDocRead 가 없습니다');
});

test('사업자등록번호 체크섬 — 실제 형식의 유효/무효를 가른다', () => {
  const R = loadRead();
  // 체크섬이 맞는 번호(계산으로 만든 값)
  assert.equal(R.bizNoValid('123-81-20031'), true);
  assert.equal(R.bizNoValid('1238120031'), true);
  assert.equal(R.bizNoValid('220 81 62517'), true);
  // 한 자리만 틀리면 걸러진다 — AI 오인식을 잡는 지점
  assert.equal(R.bizNoValid('123-81-20177'), false);
  // 자리 수가 안 맞거나 값이 없으면 false
  assert.equal(R.bizNoValid('220-81-6251'), false);
  assert.equal(R.bizNoValid(''), false);
  assert.equal(R.bizNoValid(null), false);
  assert.equal(R.bizNoValid(undefined), false);
});

test('사업자등록번호 표기 정리', () => {
  const R = loadRead();
  assert.equal(R.bizNoDigits('123-81-20031'), '1238120031');
  assert.equal(R.fmtBizNo('1238120031'), '123-81-20031');
  // 이상한 값이 와도 터지지 않는다
  assert.equal(R.fmtBizNo(''), '');
  assert.equal(R.fmtBizNo(null), '');
});
```

- [x] **Step 2: 실패를 확인한다**

Run: `node --test tests/pu-doc-read.test.js`
Expected: FAIL — `js/pu-doc-read.js` 가 없어 `Cannot find module` 또는 `PuDocRead 가 없습니다`

- [x] **Step 3: 최소 구현**

```js
/* 푸른통합시스템 — 서류 판독 층
   사진 한 장이 명함인지 사업자등록증인지 중소기업확인서인지 가리고,
   항목을 읽어 내고, 사업자등록번호가 맞는지 기계로 검증하는 유일한 파일이다.
   사진첩·명함첩·푸른이알피가 모두 이 파일을 쓴다 — 판독 방식이 바뀌어도 앱은 안 고친다. */
(function (global) {
  'use strict';

  function bizNoDigits(v) { return String(v == null ? '' : v).replace(/\D/g, ''); }

  /* 국세청 사업자등록번호 체크섬. AI가 한 자리 잘못 읽으면 여기서 걸린다 —
     '검증 통과하면 자동 입력'의 근거가 이 함수다. (pu-erp.html 의 _validBizNo 와 같은 식) */
  function bizNoValid(v) {
    var d = bizNoDigits(v);
    if (!/^\d{10}$/.test(d)) return false;
    var w = [1, 3, 7, 1, 3, 7, 1, 3, 5], s = 0;
    for (var i = 0; i < 9; i++) s += parseInt(d[i], 10) * w[i];
    s += Math.floor(parseInt(d[8], 10) * 5 / 10);
    return (10 - (s % 10)) % 10 === parseInt(d[9], 10);
  }

  function fmtBizNo(v) {
    var d = bizNoDigits(v);
    return d.length === 10 ? d.slice(0, 3) + '-' + d.slice(3, 5) + '-' + d.slice(5) : d;
  }

  global.PuDocRead = {
    bizNoDigits: bizNoDigits,
    bizNoValid: bizNoValid,
    fmtBizNo: fmtBizNo
  };
})(typeof window !== 'undefined' ? window : globalThis);
```

- [x] **Step 4: 통과를 확인한다**

Run: `node --test tests/pu-doc-read.test.js`
Expected: PASS 3/3

- [x] **Step 5: 커밋**

```bash
git add js/pu-doc-read.js tests/pu-doc-read.test.js
git commit -m "feat(서류판독): 사업자등록번호 체크섬 검증" -- js/pu-doc-read.js tests/pu-doc-read.test.js
```

---

## Task 2: 앱별 필드 이름 변환표

**Files:**
- Modify: `js/pu-doc-read.js`
- Test: `tests/pu-doc-read.test.js`

**Interfaces:**
- Produces: `mapTo(target: 'cards'|'erp'|'fund', kind: 'card'|'bizreg'|'sme', fields: Object): Object`
  - `cards` = 명함첩 레코드 필드(`bizno`) · `erp` = 사업장 정보(`bizNo`) · `fund` = 기금 참여사업장(`biz_no`)

- [x] **Step 1: 실패하는 테스트**

```js
test('사업자등록증 → 명함첩 필드로 바꾼다', () => {
  const R = loadRead();
  const out = R.mapTo('cards', 'bizreg', {
    company: '가나상사', ceo: '홍길동', bizno: '123-81-20031',
    openDate: '2020-01-02', bizType: '제조업', bizItem: '금속가공', address: '천안시'
  });
  assert.equal(out.kind, 'biz');
  assert.equal(out.company, '가나상사');
  assert.equal(out.bizno, '123-81-20031');   // 명함첩은 소문자 bizno
  assert.equal(out.bizItem, '금속가공');
});

test('사업자등록증 → 사업장 정보(푸른이알피) 필드로 바꾼다', () => {
  const R = loadRead();
  const out = R.mapTo('erp', 'bizreg', {
    company: '가나상사', ceo: '홍길동', bizno: '1238120031',
    bizType: '제조업', bizItem: '금속가공', address: '천안시', companyTel: '041-000-0000'
  });
  assert.equal(out.name, '가나상사');        // 회사명은 name
  assert.equal(out.bizNo, '123-81-20031');   // 대문자 N + 보기 좋은 꼴로
  assert.equal(out.bizCategory, '금속가공');  // 종목은 bizCategory
  assert.equal(out.bizType, '제조업');
  assert.equal(out.phone, '041-000-0000');
  assert.equal(out.address, '천안시');
  assert.ok(!('company' in out), '원래 이름이 남아 있으면 앱이 못 읽습니다');
});

test('사업자등록증 → 기금 참여사업장 필드로 바꾼다', () => {
  const R = loadRead();
  const out = R.mapTo('fund', 'bizreg', { company: '가나상사', ceo: '홍길동', bizno: '1238120031', bizType: '제조업', address: '천안시' });
  assert.equal(out.name, '가나상사');
  assert.equal(out.biz_no, '123-81-20031'); // 기금은 스네이크
  assert.equal(out.biz_type, '제조업');
  assert.equal(out.address, '천안시');
});

test('명함 → 명함첩 필드는 이름이 그대로다', () => {
  const R = loadRead();
  const out = R.mapTo('cards', 'card', { name: '홍길동', company: '가나상사', mobile: '010-1234-5678' });
  assert.equal(out.kind, 'card');
  assert.equal(out.name, '홍길동');
  assert.equal(out.mobile, '010-1234-5678');
});

test('빈 값은 실어 보내지 않는다 — 기존 값을 빈 값으로 덮어쓰면 안 된다', () => {
  const R = loadRead();
  const out = R.mapTo('erp', 'bizreg', { company: '가나상사', ceo: '', bizno: '', address: null });
  assert.equal(out.name, '가나상사');
  assert.ok(!('ceo' in out), '빈 대표자가 실려 기존 대표자를 지웁니다');
  assert.ok(!('bizNo' in out));
  assert.ok(!('address' in out));
});

test('모르는 목적지·종류는 빈 객체 (조용히 엉뚱한 곳에 쓰지 않는다)', () => {
  const R = loadRead();
  assert.deepEqual({ ...R.mapTo('아무거나', 'bizreg', { company: 'x' }) }, {});
  assert.deepEqual({ ...R.mapTo('erp', '아무거나', { company: 'x' }) }, {});
});
```

- [x] **Step 2: 실패 확인**
- [x] **Step 3: 구현** — 매핑표를 상수로 두고(`MAP.cards.bizreg` 꼴), 값이 빈 것은 빼고, 사업자번호는 `fmtBizNo`를 거쳐 담는다. `kind`는 명함첩만 붙인다(`card`→`'card'`, `bizreg`→`'biz'`).
- [x] **Step 4: 통과 확인**
- [x] **Step 5: 커밋**

---

## Task 3: 종류 판정 + 항목 판독 (Gemini)

**Files:**
- Modify: `js/pu-doc-read.js`
- Test: `tests/pu-doc-read.test.js`

**Interfaces:**
- Consumes: `init({ fetch, getKey })` — `getKey(): Promise<string>` (앱이 3단 폴백을 넣어 준다)
- Produces:
  - `PROMPTS: { card, bizreg, sme }` — 프롬프트 문장(검사에서 내용 확인용)
  - `read(dataUrl): Promise<Result>` — 종류 판정과 항목 판독을 **한 번의 호출로** 한다
  - `Result` = `{ kind:'card'|'bizreg'|'sme'|'other', fields:Object, bizNoOk:boolean|null, ntsChecked:boolean, ntsState:string|null, error:string|null }`

한 번의 호출로 하는 이유: 종류 판정과 항목 판독을 따로 부르면 사진을 두 번 올려 시간·비용이 두 배가 되고, 판정과 판독이 서로 다른 판단을 할 수 있다.

- [x] **Step 1: 실패하는 테스트**

```js
// Gemini 흉내 — 실제 서버에 붙지 않는다.
function fakeFetch(reply, opts) {
  const calls = [];
  const fn = function (url, init) {
    calls.push({ url: url, body: JSON.parse(init.body) });
    if (opts && opts.httpFail) return Promise.resolve({ ok: false, status: 429, json: () => Promise.resolve({}) });
    if (opts && opts.network) return Promise.reject(new Error('네트워크 없음'));
    return Promise.resolve({
      ok: true, status: 200,
      json: () => Promise.resolve({ candidates: [{ content: { parts: [{ text: reply }] } }] })
    });
  };
  fn.calls = calls;
  return fn;
}
const DUMMY_IMG = 'data:image/jpeg;base64,AAAA';

test('사업자등록증을 읽고 번호 검증까지 한다', async () => {
  const R = loadRead();
  R.init({ fetch: fakeFetch('```json\n{"kind":"bizreg","company":"가나상사","ceo":"홍길동","bizno":"123-81-20031"}\n```'),
           getKey: () => Promise.resolve('KEY') });
  const r = await R.read(DUMMY_IMG);
  assert.equal(r.kind, 'bizreg');
  assert.equal(r.fields.company, '가나상사');
  assert.equal(r.bizNoOk, true);
  assert.equal(r.error, null);
  // 국세청 키를 안 넣었으므로 조회하지 않았다고 정확히 말해야 한다
  assert.equal(r.ntsChecked, false);
  assert.equal(r.ntsState, null);
});

test('번호가 한 자리 틀리면 검증에 걸린다 — 자동 입력하면 안 되는 경우', async () => {
  const R = loadRead();
  R.init({ fetch: fakeFetch('{"kind":"bizreg","company":"가나상사","bizno":"123-81-20177"}'),
           getKey: () => Promise.resolve('KEY') });
  const r = await R.read(DUMMY_IMG);
  assert.equal(r.bizNoOk, false);
});

test('명함은 번호 검증 대상이 아니다 (null, false 아님)', async () => {
  const R = loadRead();
  R.init({ fetch: fakeFetch('{"kind":"card","name":"홍길동","mobile":"010-1234-5678"}'),
           getKey: () => Promise.resolve('KEY') });
  const r = await R.read(DUMMY_IMG);
  assert.equal(r.kind, 'card');
  assert.equal(r.bizNoOk, null, '검증 대상이 아닌데 false 로 두면 화면이 실패로 오해합니다');
});

test('중소기업확인서는 유효기간을 읽는다', async () => {
  const R = loadRead();
  R.init({ fetch: fakeFetch('{"kind":"sme","company":"가나상사","bizno":"1238120031","smeType":"소기업","expiry":"2027-03-31"}'),
           getKey: () => Promise.resolve('KEY') });
  const r = await R.read(DUMMY_IMG);
  assert.equal(r.kind, 'sme');
  assert.equal(r.fields.expiry, '2027-03-31');
  assert.equal(r.bizNoOk, true);
});

test('서류가 아니면 other 로 두고 억지로 맞추지 않는다', async () => {
  const R = loadRead();
  R.init({ fetch: fakeFetch('{"kind":"other"}'), getKey: () => Promise.resolve('KEY') });
  const r = await R.read(DUMMY_IMG);
  assert.equal(r.kind, 'other');
});

test('프롬프트에 세 종류와 읽을 항목이 모두 들어 있다', () => {
  const R = loadRead();
  const p = R.PROMPTS.all;
  for (const w of ['명함', '사업자등록증', '중소기업확인서', 'bizno', 'expiry', 'kind']) {
    assert.ok(p.includes(w), '프롬프트에 ' + w + ' 가 없습니다');
  }
  assert.ok(/JSON 외|JSON만|JSON으로만/.test(p), 'JSON만 답하라는 지시가 없습니다');
});

test('AI 키가 없으면 부르지 않고 한국어로 알려준다', async () => {
  const R = loadRead();
  const f = fakeFetch('{}');
  R.init({ fetch: f, getKey: () => Promise.resolve('') });
  const r = await R.read(DUMMY_IMG);
  assert.equal(r.kind, 'other');
  assert.match(r.error, /키/);
  assert.equal(f.calls.length, 0, '키가 없는데 서버를 불렀습니다');
});

test('서버 오류·네트워크 끊김에도 예외를 던지지 않는다', async () => {
  const R1 = loadRead();
  R1.init({ fetch: fakeFetch('{}', { httpFail: true }), getKey: () => Promise.resolve('KEY') });
  const a = await R1.read(DUMMY_IMG);
  assert.equal(a.kind, 'other');
  assert.ok(a.error);

  const R2 = loadRead();
  R2.init({ fetch: fakeFetch('{}', { network: true }), getKey: () => Promise.resolve('KEY') });
  const b = await R2.read(DUMMY_IMG);
  assert.equal(b.kind, 'other');
  assert.ok(b.error);
});

test('AI가 JSON이 아닌 말을 해도 터지지 않는다', async () => {
  const R = loadRead();
  R.init({ fetch: fakeFetch('죄송합니다, 읽을 수 없습니다.'), getKey: () => Promise.resolve('KEY') });
  const r = await R.read(DUMMY_IMG);
  assert.equal(r.kind, 'other');
  assert.ok(r.error);
});

test('사진 데이터를 그대로 싣고 키를 주소에 넣는다', async () => {
  const R = loadRead();
  const f = fakeFetch('{"kind":"card"}');
  R.init({ fetch: f, getKey: () => Promise.resolve('KEY123') });
  await R.read('data:image/jpeg;base64,ZZZZ');
  assert.equal(f.calls.length, 1);
  assert.ok(f.calls[0].url.includes('KEY123'));
  assert.equal(f.calls[0].body.contents[0].parts[0].inline_data.data, 'ZZZZ',
    '사진 데이터가 실리지 않았습니다');
});
```

- [x] **Step 2: 실패 확인**
- [x] **Step 3: 구현**

한 번의 호출로 종류와 항목을 함께 받는 프롬프트를 만든다. 기존 두 프롬프트(`pu-cards.html:1715`·`1716`)의 키 목록을 그대로 품고, `kind`와 중소기업확인서 항목을 더한다. 응답은 ```` ```json ```` 껍데기를 벗기고 `JSON.parse`. 모든 실패는 `{kind:'other', error:'…'}` 로 돌려주고 **예외를 밖으로 던지지 않는다** — 사진 한 장 판독 실패가 올리기 전체를 멈추면 안 된다.

- [x] **Step 4: 통과 확인**
- [x] **Step 5: 커밋**

---

## Task 4: 국세청 실제 조회 (있을 때만)

**Files:**
- Modify: `js/pu-doc-read.js`
- Test: `tests/pu-doc-read.test.js`

**Interfaces:**
- Consumes: `init({ ..., getNtsKey })` — 없으면 조회를 건너뛴다
- Produces: `read()` 결과의 `ntsChecked`·`ntsState` 를 채운다. `ntsState` 는 국세청이 주는 `b_stt`(계속사업자/폐업자/휴업자)

- [x] **Step 1: 실패하는 테스트**

```js
test('국세청 키가 있으면 조회해서 상태를 담는다', async () => {
  const R = loadRead();
  let ntsCalled = 0;
  const f = function (url, init) {
    if (url.indexOf('odcloud') >= 0) {
      ntsCalled++;
      return Promise.resolve({ ok: true, json: () => Promise.resolve({ data: [{ b_stt: '계속사업자' }] }) });
    }
    return Promise.resolve({ ok: true, json: () => Promise.resolve({
      candidates: [{ content: { parts: [{ text: '{"kind":"bizreg","company":"가나상사","bizno":"123-81-20031"}' }] } }] }) });
  };
  R.init({ fetch: f, getKey: () => Promise.resolve('KEY'), getNtsKey: () => Promise.resolve('NTS') });
  const r = await R.read(DUMMY_IMG);
  assert.equal(ntsCalled, 1);
  assert.equal(r.ntsChecked, true);
  assert.equal(r.ntsState, '계속사업자');
});

test('체크섬에서 이미 걸린 번호는 국세청에 묻지 않는다', async () => {
  const R = loadRead();
  let ntsCalled = 0;
  const f = function (url) {
    if (url.indexOf('odcloud') >= 0) { ntsCalled++; return Promise.resolve({ ok: true, json: () => Promise.resolve({ data: [] }) }); }
    return Promise.resolve({ ok: true, json: () => Promise.resolve({
      candidates: [{ content: { parts: [{ text: '{"kind":"bizreg","bizno":"123-81-20177"}' }] } }] }) });
  };
  R.init({ fetch: f, getKey: () => Promise.resolve('KEY'), getNtsKey: () => Promise.resolve('NTS') });
  const r = await R.read(DUMMY_IMG);
  assert.equal(ntsCalled, 0, '틀린 번호로 국세청을 부르면 헛일입니다');
  assert.equal(r.ntsChecked, false);
});

test('국세청 조회가 실패해도 판독 결과는 살린다', async () => {
  const R = loadRead();
  const f = function (url) {
    if (url.indexOf('odcloud') >= 0) return Promise.reject(new Error('국세청 응답 없음'));
    return Promise.resolve({ ok: true, json: () => Promise.resolve({
      candidates: [{ content: { parts: [{ text: '{"kind":"bizreg","company":"가나상사","bizno":"123-81-20031"}' }] } }] }) });
  };
  R.init({ fetch: f, getKey: () => Promise.resolve('KEY'), getNtsKey: () => Promise.resolve('NTS') });
  const r = await R.read(DUMMY_IMG);
  assert.equal(r.fields.company, '가나상사');
  assert.equal(r.bizNoOk, true);
  assert.equal(r.ntsChecked, false, '조회 못 했는데 했다고 하면 안 됩니다');
});

test('폐업자로 나오면 상태를 그대로 남긴다 (판단은 부르는 쪽이 한다)', async () => {
  const R = loadRead();
  const f = function (url) {
    if (url.indexOf('odcloud') >= 0) return Promise.resolve({ ok: true, json: () => Promise.resolve({ data: [{ b_stt: '폐업자' }] }) });
    return Promise.resolve({ ok: true, json: () => Promise.resolve({
      candidates: [{ content: { parts: [{ text: '{"kind":"bizreg","bizno":"123-81-20031"}' }] } }] }) });
  };
  R.init({ fetch: f, getKey: () => Promise.resolve('KEY'), getNtsKey: () => Promise.resolve('NTS') });
  const r = await R.read(DUMMY_IMG);
  assert.equal(r.ntsState, '폐업자');
  assert.equal(r.bizNoOk, true, '번호 자체는 맞으므로 검증은 통과다');
});
```

- [x] **Step 2: 실패 확인**
- [x] **Step 3: 구현** — 체크섬을 통과한 번호만, 국세청 키가 있을 때만 조회. 어떤 실패든 `ntsChecked:false` 로 두고 판독 결과는 그대로 돌려준다.
- [x] **Step 4: 통과 확인**
- [x] **Step 5: 커밋**

---

## Task 5: 자동 입력 판정 한 줄로

"검증 통과하면 자동, 걸리면 사람 확인" 판단을 앱마다 다시 쓰지 않도록 한 함수로 굳힌다.

**Files:**
- Modify: `js/pu-doc-read.js`
- Test: `tests/pu-doc-read.test.js`

**Interfaces:**
- Produces: `autoOk(result): { auto: boolean, why: string }` — `why` 는 화면에 그대로 띄울 한국어

- [x] **Step 1: 실패하는 테스트**

```js
test('사업자등록증 — 번호 검증을 통과하면 자동 입력', () => {
  const R = loadRead();
  const v = R.autoOk({ kind: 'bizreg', fields: { company: '가나상사' }, bizNoOk: true, ntsChecked: false, error: null });
  assert.equal(v.auto, true);
});

test('사업자등록증 — 번호가 걸리면 사람 확인, 이유를 한국어로', () => {
  const R = loadRead();
  const v = R.autoOk({ kind: 'bizreg', fields: {}, bizNoOk: false, ntsChecked: false, error: null });
  assert.equal(v.auto, false);
  assert.match(v.why, /사업자등록번호/);
  assert.ok(!/[A-Za-z]{4,}/.test(v.why), '영어 내부 용어가 노출됩니다: ' + v.why);
});

test('폐업자는 사람이 봐야 한다', () => {
  const R = loadRead();
  const v = R.autoOk({ kind: 'bizreg', fields: {}, bizNoOk: true, ntsChecked: true, ntsState: '폐업자', error: null });
  assert.equal(v.auto, false);
  assert.match(v.why, /폐업/);
});

test('명함 — 검증할 것이 없으므로 자동', () => {
  const R = loadRead();
  assert.equal(R.autoOk({ kind: 'card', fields: { name: '홍길동' }, bizNoOk: null, error: null }).auto, true);
});

test('이름도 회사도 못 읽었으면 자동으로 넣지 않는다', () => {
  const R = loadRead();
  assert.equal(R.autoOk({ kind: 'card', fields: {}, bizNoOk: null, error: null }).auto, false);
  assert.equal(R.autoOk({ kind: 'bizreg', fields: { bizno: '123-81-20031' }, bizNoOk: true, error: null }).auto, false);
});

test('중소기업확인서 — 유효기간이 지났으면 사람 확인', () => {
  const R = loadRead();
  const past = R.autoOk({ kind: 'sme', fields: { company: '가나상사', expiry: '2020-01-01' }, bizNoOk: true, error: null }, new Date(2026, 7, 3).getTime());
  assert.equal(past.auto, false);
  assert.match(past.why, /유효기간|만료/);
  const ok = R.autoOk({ kind: 'sme', fields: { company: '가나상사', expiry: '2027-03-31' }, bizNoOk: true, error: null }, new Date(2026, 7, 3).getTime());
  assert.equal(ok.auto, true);
});

test('판독 자체가 실패했거나 서류가 아니면 자동이 아니다', () => {
  const R = loadRead();
  assert.equal(R.autoOk({ kind: 'other', fields: {}, error: null }).auto, false);
  assert.equal(R.autoOk({ kind: 'card', fields: { name: '홍길동' }, error: '키가 없습니다' }).auto, false);
});
```

- [x] **Step 2: 실패 확인**
- [x] **Step 3: 구현** — `why` 는 짧고 쉬운 한국어. 영어 내부 용어를 노출하지 않는다(저장소 규칙).
- [x] **Step 4: 통과 확인**
- [x] **Step 5: 커밋**

---

## Task 6: 총괄 검증 + 기록

- [x] `node --test tests/*.test.js` 전체 통과 (기존 스위트 회귀 없음)
- [x] `fund-erp/STATUS.md` 변경 로그에 한 줄 추가
- [x] 설계서 `docs/superpowers/specs/2026-08-02-푸른사진첩-design.md` 6장(자동 분류)에 이번 결정 반영 — 서류 3종 · 검증 통과 시 자동 입력
- [x] 이 계획서 체크박스 갱신
- [x] push 후 PR 생성 (base main). 병합은 대표님.

---

## 다음 계획 (이 계획에 포함하지 않음)

각 단계가 끝나면 그것만으로 쓸 수 있어야 하므로 나눈다.

| 다음 | 내용 | 이것까지 하면 |
|---|---|---|
| 붙이기 1 | 사진첩에서 「서류 고르기」로 올린 사진을 자동 판독 → 결과를 사진첩 메타에 저장 → 화면에 "무엇으로 읽혔는지 · 자동 입력 가능한지" 표시 (**저장만, 등록은 안 함**) | 대표님이 판독 정확도를 눈으로 확인 가능 |
| 붙이기 2 | 자동 등록 — 통과분은 명함첩에 바로, 걸린 것만 확인 카드. 사업자번호로 기존 업체를 찾아 붙여 **중복을 만들지 않는다**. 사진첩 사진과 레코드를 서로 잇는 고리(`photoId`) | 대표 요구 완성 |
| 붙이기 3 | 중소기업확인서 기록 자리 + **유효기간 만료 임박 알림** | 정부지원사업 자격 관리가 저절로 됨 |

**붙이기 2에서 반드시 지킬 것 (미리 적어 둔다):**
- 명함첩에 쓸 때 **`pucards/idx/{id}`(검색 인덱스)도 같이 쓴다.** 안 쓰면 다른 앱이 그 명함을 못 찾는다.
- 상위 노드를 통째로 `set` 하지 않는다 — 다중 경로 `update` 한 번. (2026-07 실데이터 사고)
- 중복 판정 기준을 명함첩과 같게 쓴다 — 명함은 휴대폰 숫자, 사업자등록증은 사업자번호 숫자.
- 사진첩 화면은 실시간DB를 직접 만지지 않는다(지금 정적 검사가 강제). 쓰기는 공용 조각을 통해서만.
- **사업장을 새로 만드는 것은 사업자번호가 있을 때만.** 번호 없이 만들면 지금 중복(126쌍)이 생긴 그 경로를 다시 연다.

## Self-Review 기록

- 대표 지시 대비: "서류를 나누어 자동 배분" → Task 3(종류 판정)·Task 2(앱별 변환). "각자의 프로그램에 입력" → Task 2 변환표 + 붙이기 2. "검증 통과하면 자동" → Task 1·4·5.
- 자리표시자 없음. 이름 일치 확인: `bizNoValid`/`bizNoDigits`/`fmtBizNo`/`mapTo`/`read`/`autoOk`/`PROMPTS`/`init`.
- 검사에 쓴 사업자번호 `123-81-20031`는 체크섬 계산으로 만든 값이고 실제 사업장을 가리키지 않는다. 실데이터를 검사에 넣지 않는다.
- 이 계획에는 저장·등록이 없다 → 실데이터를 건드릴 경로가 아예 없다(2026-07 사고 예방).
