# 폰에서 기업 상세 쓰기 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 폰(좁은 화면)에서도 기업 상세(회사 목록·상세·폴더·태그·이알피 가져오기)를 PC와 같은 기능으로 쓸 수 있게 한다.

**Architecture:** `pu-cards.html`은 이미 PC(`#pcRoot`, `IS_PC`)와 폰(`#tabs`/`#list`, `renderMobile`) 두 갈래로 나뉘어 있다. 폴더·태그·이알피 가져오기의 실제 로직(`_coFolders`, `coMoveToFolder`, `openCoFolderDialog` 등)은 이미 이 갈래 밖의 공용 자리에 있으므로 그대로 재사용한다. 새로 만드는 것은 (1) 상단 탭에 세 번째 버튼, (2) 카드 목록을 그리는 폰 전용 함수, (3) 상세 패널을 폰 너비에서 전체화면으로 보이게 하는 CSS, (4) 폴더/태그를 고르는 바텀시트, (5) 여러 곳 골라 옮기기 뿐이다. 저장 후 다시 그리는 지점들은 PC/폰을 자동으로 가리는 `render()`를 통하도록 통일한다.

**Tech Stack:** 순수 JS(빌드 없음), Firebase Realtime Database, `node:test`+`node:vm` 기반 테스트(소스에서 함수를 잘라 실제 실행).

## Global Constraints

- 설계서: `docs/superpowers/specs/2026-08-15-폰-기업상세-design.md` — 모든 과제는 이 설계서의 요구를 벗어나지 않는다.
- PC 관리자 화면(`#pcRoot`, `renderPC()`, `renderCoPage()` 등)은 **한 글자도 안 바꾼다** — 이번 범위는 폰뿐이다.
- 표 정렬·열 깔때기(`coSortBy`, `coApplyColFilter`, `openCoColFilter`)는 이번 범위 밖이다 — 손대지 않는다.
- 회사를 폰에서 손으로 만들거나 지우지 않는다(설계서·이전 설계서 모두 동일 원칙).
- **★ `state.tab` 에 `'co'` 를 절대 넣지 않는다.** `state.tab` 은 `'card'|'biz'` 두 값뿐이고,
  `it.kind===state.tab`(회사엔 그런 kind 가 없다)·`state.tab==='card'?'명함':'사업자등록증'`
  (화면이 「사업자등록증」으로 잘못 불린다) 같은 자리가 75곳이다. 셋째 값을 끼우면 검색·중복정리·
  내보내기·개인폴더가 조용히 어긋난다(대표 지시 2026-08-12, `tests/cards-co-info.test.js`
  「기업정보는 갈래가 아니라 화면이다」가 이것을 막고 있다). **기업 상세는 «갈래»가 아니라
  «화면»이므로 PC 와 똑같이 `state.view==='co'` 를 쓴다.** 폰 탭 단추는 겉모습만 탭이고
  속으로는 화면을 바꾸는 것이다.
- 커밋 전 항상 `node --test tests/*.test.js` 전체 실행, 뮤테이션 테스트로 새 검사가 실제로 잡아내는지 확인.
- 파일 수정은 Edit/Write 도구만 쓴다 — PowerShell `Set-Content`/`Out-File` 왕복은 UTF-8 파일을 깨뜨린 전례가 있다(STATUS.md 2026-08-11 교훈).

---

### Task 1: 상단 탭에 "🏢 기업 상세" 추가 + 진입 배선

**Files:**
- Modify: `pu-cards.html:1130-1133` (`#tabs` 마크업)
- Modify: `pu-cards.html:4913-4917` (`setTab`)
- Modify: `pu-cards.html:5045` (`function render(){...}` — 이것이 나중에 `renderMobile`로 별칭됨)
- Modify: `pu-cards.html:1142` (`#search` 의 `oninput`)
- Test: `tests/cards-co-mobile-tab.test.js`

**Interfaces:**
- Consumes: `state.view`(기존 — `'list'|'mail'|'mat'|'settings'|'co'`. PC 기업 상세가 이미 `'co'` 를 쓴다).
- Produces: `openCoMobile()` — 새 함수, `state.view='co'` 로 바꾸고 `render()`. `state.tab` 은 **안 건드린다**. `renderCoMobileList()`(Task 2에서 본체 구현, 이 과제에서는 빈 틀만) — `function renderCoMobileList(){ $('list').innerHTML=''; }`로 임시 구현해 다음 과제가 이어받는다. `onMobileSearchInput(v)` — 새 함수, 기업 상세 화면일 때만 `state.coQ`를 채우고 `render()`를 부른다.

**⚠ 이 과제의 핵심 제약**: `state.tab==='co'` 를 쓰면 안 된다(Global Constraints ★ 참고). `tests/cards-co-info.test.js` 가 그 글자를 발견하면 즉시 실패한다. 기업 상세는 `state.view==='co'` 로 판별한다.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/cards-co-mobile-tab.test.js` 새로 작성:

```javascript
/* 폰 상단 「기업 상세」 단추 — 겉모습만 탭이고 속으로는 화면(state.view)을 바꾼다.
   ⚠ state.tab 에 'co' 를 넣으면 안 된다(대표 지시 2026-08-12). state.tab 은
     'card'|'biz' 두 값뿐이라고 믿는 자리가 75곳이다 — it.kind===state.tab 은 회사를
     하나도 못 찾고, state.tab==='card'?'명함':'사업자등록증' 은 화면을 「사업자등록증」
     이라 잘못 부른다. PC 기업 상세가 쓰는 state.view==='co' 를 폰도 그대로 쓴다.
     tests/cards-co-info.test.js 가 이 규칙을 지키고 있다. */
const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');
const vm = require('node:vm');

const source = fs.readFileSync(path.join(__dirname, '..', 'pu-cards.html'), 'utf8');

test('#tabs 에 기업 상세 단추가 있고 openCoMobile 을 부른다', () => {
  const at = source.indexOf('<div id="tabs">');
  const end = source.indexOf('</div>', at);
  const block = source.slice(at, end);
  assert.match(block, /id="tabCo"/, 'tabCo 단추를 찾지 못했습니다');
  assert.match(block, /onclick="openCoMobile\(\)"/);
  assert.match(block, /기업 상세/);
});

test('★ state.tab 에 co 를 끼우지 않았다 (대표 지시 2026-08-12)', () => {
  assert.doesNotMatch(source, /state\.tab==='co'/,
    "state.tab 은 'card'|'biz' 두 값뿐이어야 한다 — 기업 상세는 state.view 로 가른다");
});

test('#search 의 oninput 이 onMobileSearchInput 을 쓴다', () => {
  const at = source.indexOf('id="searchbar"');
  const end = source.indexOf('\n', at);
  assert.match(source.slice(at, end), /oninput="onMobileSearchInput\(this\.value\)"/);
});

function loadTabBlock(){
  const at = source.indexOf('function setTab(tab)');
  const end = source.indexOf('\nfunction toggleSort', at);
  assert.ok(at > 0 && end > at, 'setTab~toggleSort 사이를 찾지 못했습니다');
  const ctx = {
    state: { tab:'card', view:'list' },
    calls: { toggled: {}, placeholder: '', rendered: 0 },
    $: id => {
      if(id==='tabCard') return { classList: { toggle:(c,on)=>{ ctx.calls.toggled.tabCard = on; } } };
      if(id==='tabBiz')  return { classList: { toggle:(c,on)=>{ ctx.calls.toggled.tabBiz = on; } } };
      if(id==='tabCo')   return { classList: { toggle:(c,on)=>{ ctx.calls.toggled.tabCo = on; } } };
      if(id==='search')  return { set placeholder(v){ ctx.calls.placeholder = v; }, get placeholder(){ return ctx.calls.placeholder; } };
      return null;
    },
    render: () => { ctx.calls.rendered++; }
  };
  vm.createContext(ctx);
  vm.runInContext(source.slice(at, end), ctx);
  return ctx;
}

test('openCoMobile 은 state.view 를 co 로 바꾸고 state.tab 은 안 건드린다', () => {
  const c = loadTabBlock();
  c.state.tab = 'biz';
  c.openCoMobile();
  assert.equal(c.state.view, 'co');
  assert.equal(c.state.tab, 'biz', 'state.tab 을 건드리면 안 된다 — 갈래가 아니라 화면이다');
  assert.equal(c.calls.toggled.tabCo, true);
  assert.equal(c.calls.toggled.tabCard, false);
  assert.equal(c.calls.toggled.tabBiz, false);
  assert.equal(c.calls.rendered, 1);
});

test('openCoMobile 은 찾기 칸 안내문구를 회사용으로 바꾼다', () => {
  const c = loadTabBlock();
  c.openCoMobile();
  assert.match(c.calls.placeholder, /상호|사업자번호|대표자/);
});

test('명함·사업자 탭으로 돌아가면 기업 상세 화면에서 빠져나온다', () => {
  const c = loadTabBlock();
  c.openCoMobile();
  assert.equal(c.state.view, 'co');
  c.setTab('card');
  assert.equal(c.state.view, 'list', '기업 상세 화면에 머문 채로 명함 탭을 그리면 안 된다');
  assert.equal(c.state.tab, 'card');
  assert.equal(c.calls.toggled.tabCo, false);
  assert.equal(c.calls.toggled.tabCard, true);
});

function loadMobileSearchBlock(){
  const at = source.indexOf('function onMobileSearchInput');
  const end = source.indexOf('\n}', at) + 2;
  const ctx = { state: { view:'list', coQ:'' }, render: () => { ctx.rendered = true; } };
  vm.createContext(ctx);
  vm.runInContext(source.slice(at, end), ctx);
  return ctx;
}

test('onMobileSearchInput 은 기업 상세 화면일 때만 state.coQ 를 채운다', () => {
  const c1 = loadMobileSearchBlock();
  c1.state.view = 'co';
  c1.onMobileSearchInput('대명');
  assert.equal(c1.state.coQ, '대명');
  assert.ok(c1.rendered);

  const c2 = loadMobileSearchBlock();
  c2.state.view = 'list';
  c2.onMobileSearchInput('홍길동');
  assert.equal(c2.state.coQ, '', '명함 화면일 때는 회사 찾기말을 안 건드려야 합니다');
  assert.ok(c2.rendered);
});

test('render 가 기업 상세 화면이면 카드 목록으로 갈라진다', () => {
  const at = source.indexOf('function render(){');
  const end = source.indexOf('\n', at);
  const fn = source.slice(at, end);
  assert.match(fn, /state\.view==='co'/);
  assert.match(fn, /renderCoMobileList\(\)/);
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `node --test tests/cards-co-mobile-tab.test.js`
Expected: FAIL — `id="tabCo"` 없음, `openCoMobile`·`onMobileSearchInput` 정의 안 됨 등.

- [ ] **Step 3: `#tabs` 에 단추 추가**

`pu-cards.html` 의 `<div id="tabs">` 덩어리를 다음으로 바꾼다:

```html
<div id="tabs">
  <button id="tabCard" class="on" onclick="setTab('card')">명함</button>
  <button id="tabBiz" onclick="setTab('biz')">사업자등록증</button>
  <button id="tabCo" onclick="openCoMobile()">🏢 기업 상세</button>
</div>
```

- [ ] **Step 4: `#search` 의 `oninput` 을 바꾼다**

`<div id="searchbar">` 줄을 다음으로 바꾼다:

```html
<div id="searchbar"><input id="search" placeholder="이름·회사·전화번호 검색" oninput="onMobileSearchInput(this.value)"></div>
```

- [ ] **Step 5: `setTab` 을 고치고 `openCoMobile` 을 그 뒤에 붙인다**

현재 코드:

```javascript
function setTab(tab){ state.tab=tab; state.group='all';
  $('tabCard').classList.toggle('on', tab==='card');
  $('tabBiz').classList.toggle('on', tab==='biz');
  $('search').placeholder = tab==='card' ? '이름·회사·전화번호 검색' : '상호·대표자·사업자번호 검색';
  render(); }
```

다음으로 바꾼다(`setTab` 은 기업 상세 «화면»에서 빠져나오게 하고, 새 함수를 바로 뒤에 둔다):

```javascript
function setTab(tab){ state.tab=tab; state.group='all';
  /* 기업 상세는 갈래가 아니라 화면이다 — 명함·사업자로 돌아올 때 그 화면에서 나온다.
     안 그러면 명함 탭을 눌러도 회사 목록이 그대로 남는다. */
  if(state.view==='co') state.view='list';
  $('tabCard').classList.toggle('on', tab==='card');
  $('tabBiz').classList.toggle('on', tab==='biz');
  $('tabCo').classList.toggle('on', false);
  $('search').placeholder = tab==='card' ? '이름·회사·전화번호 검색' : '상호·대표자·사업자번호 검색';
  render(); }
/* 폰 「🏢 기업 상세」 단추 — 겉모습은 탭이지만 갈래(state.tab)가 아니라 화면(state.view)을
   바꾼다. state.tab 에 셋째 값을 끼우면 it.kind===state.tab 인 자리들이 회사를 하나도
   못 찾고, 「사업자등록증」으로 잘못 불리는 안내문이 여럿 생긴다(대표 지시 2026-08-12). */
function openCoMobile(){
  state.view='co';
  $('tabCard').classList.toggle('on', false);
  $('tabBiz').classList.toggle('on', false);
  $('tabCo').classList.toggle('on', true);
  $('search').placeholder = '상호·사업자번호·대표자로 찾기';
  render();
}
```

- [ ] **Step 6: `onMobileSearchInput` 과 `renderCoMobileList` 뼈대를 추가**

`const renderMobile = render;` 바로 앞에 추가한다:

```javascript
/* 명함·사업자 찾기칸을 기업 상세와 같이 쓴다 — 칸을 하나로 유지해야 한다
   (같은 일을 하는 칸이 둘이면 어느 쪽에 쳐야 하는지 헷갈린다, 대표 지시 2026-08-13
   기업정보 화면의 원칙과 동일). 기업 상세 화면일 때만 coQ 를 채워 coVisible() 이 읽게 한다. */
function onMobileSearchInput(v){
  if(state.view==='co') state.coQ = v;
  render();
}
/* Task 2 가 실제 카드 목록으로 채운다. 지금은 render() 분기가 안전하게 동작하도록
   빈 목록만 그린다. */
function renderCoMobileList(){
  const el = $('list'); if(el) el.innerHTML = '';
}
```

- [ ] **Step 7: `render()`(폰 쪽, 나중에 `renderMobile`)를 기업 상세로 가지치기**

현재 코드:

```javascript
function render(){ if(_quiet) return; saveLastScreen(); renderSubbar(); renderSidebar(); renderList(); }
```

다음으로 바꾼다:

```javascript
function render(){ if(_quiet) return; saveLastScreen();
  if(state.view==='co'){ renderCoMobileList(); return; }
  renderSubbar(); renderSidebar(); renderList(); }
```

⚠ 이 `render()` 는 나중에 `const renderMobile = render;` 로 별칭이 붙어 **폰 전용**이 된다
(PC 는 그 아래에서 `renderPC()` 로 간다). 그러니 여기 `state.view==='co'` 분기는 PC 동작에
영향을 주지 않는다 — PC 는 이 함수를 안 탄다.

- [ ] **Step 8: 테스트가 통과하는지 확인**

Run: `node --test tests/cards-co-mobile-tab.test.js`
Expected: PASS (8 tests)

- [ ] **Step 9: 전체 테스트 확인 후 커밋**

특히 `tests/cards-co-info.test.js` 의 「기업정보는 갈래가 아니라 화면이다」가 통과해야 한다.

```bash
node --test tests/*.test.js
git add pu-cards.html tests/cards-co-mobile-tab.test.js
git commit -m "feat(폰): 상단에 기업 상세 진입 단추 — 갈래가 아니라 화면으로"
```


---

### Task 2: 폰 카드 목록 — `renderCoMobileList()` 실제 구현

**Files:**
- Modify: `pu-cards.html` (Task 1이 만든 `renderCoMobileList()` 뼈대를 채운다, `#tabs` 근처 CSS 추가)
- Test: `tests/cards-co-mobile-list.test.js`

**Interfaces:**
- Consumes: `coVisible()`(기존, 인자 없이 지금 화면에 보일 회사 배열을 준다), `coTagsOf(o)`(기존), `_coFolders`(기존, `{id:{name,...}}`), `state.coSel`(기존, `{key:1}` 맵), `esc(s)`(기존), `pickCo(k)`(기존, 상세 열기/닫기 토글).
- Produces: `renderCoMobileList()` — `#list` 에 카드 HTML을 그린다. 다음 과제(3~6)가 이 함수와 그 안에서 만드는 카드 마크업을 그대로 쓴다.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/cards-co-mobile-list.test.js`:

```javascript
/* 폰 기업 상세 카드 목록 — 명함 목록(renderList)과 같은 .row/.rowmain 결로 그린다.
   ⚠ PC 표(coListHtml)는 안 건드린다 — 이 함수는 완전히 새 것이다. */
const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');
const vm = require('node:vm');

const source = fs.readFileSync(path.join(__dirname, '..', 'pu-cards.html'), 'utf8');

function loadBlock(list){
  const at = source.indexOf('function renderCoMobileList');
  const end = source.indexOf('\nfunction ', at + 10);
  assert.ok(at > 0, 'renderCoMobileList 를 찾지 못했습니다');
  const calls = { html:'' };
  const ctx = {
    esc: s => String(s ?? '').replace(/[&<>"']/g, c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),
    state: { coSel:{}, selMode:false },
    _coFolders: {},
    coVisible: () => list,
    coTagsOf: o => Object.keys(o.tags||{}),
    $: id => id==='list' ? { set innerHTML(v){ calls.html=v; }, get innerHTML(){ return calls.html; } } : null
  };
  vm.createContext(ctx);
  vm.runInContext(source.slice(at, end), ctx);
  ctx._calls = calls;
  return ctx;
}

test('회사가 없으면 안내문구를 보여준다', () => {
  const c = loadBlock([]);
  c.renderCoMobileList();
  assert.match(c._calls.html, /찾지 못했습니다|없습니다/);
});

test('회사마다 상호·유형·사업자번호·담당을 카드로 그린다', () => {
  const c = loadBlock([{ key:'k1', name:'나라크라샤', bizno:'123-81-20012', erp:{ type:'자문', main:'김보람' }, folder:'', cards:[], docs:0, tags:{} }]);
  c.renderCoMobileList();
  assert.match(c._calls.html, /class="row"/);
  assert.match(c._calls.html, /나라크라샤/);
  assert.match(c._calls.html, /자문/);
  assert.match(c._calls.html, /123-81-20012/);
  assert.match(c._calls.html, /김보람/);
});

test('폴더에 든 회사는 카드에 폴더 이름이 보인다', () => {
  const c = loadBlock([{ key:'k1', name:'나라크라샤', bizno:'', erp:null, folder:'f1', cards:[], docs:0, tags:{} }]);
  c._coFolders = { f1:{ id:'f1', name:'현장클리닉' } };
  c.renderCoMobileList();
  assert.match(c._calls.html, /현장클리닉/);
});

test('카드를 누르면 pickCo(key) 를 부른다', () => {
  const c = loadBlock([{ key:'k1', name:'나라크라샤', bizno:'', erp:null, folder:'', cards:[], docs:0, tags:{} }]);
  c.renderCoMobileList();
  assert.match(c._calls.html, /onclick="pickCo\('k1'\)"/);
});

test('선택 모드일 때는 체크 표시를 그리고, 누르면 coToggle 을 부른다', () => {
  const c = loadBlock([{ key:'k1', name:'나라크라샤', bizno:'', erp:null, folder:'', cards:[], docs:0, tags:{} }]);
  c.state.selMode = true;
  c.renderCoMobileList();
  assert.match(c._calls.html, /onclick="coToggle\('k1'\)"/);
  assert.doesNotMatch(c._calls.html, /onclick="pickCo\('k1'\)"/, '선택 모드에서 누르면 상세가 아니라 선택이 되어야 합니다');
});

test('선택된 회사는 체크 표시가 켜진다', () => {
  const c = loadBlock([{ key:'k1', name:'나라크라샤', bizno:'', erp:null, folder:'', cards:[], docs:0, tags:{} }]);
  c.state.selMode = true; c.state.coSel = { k1:1 };
  c.renderCoMobileList();
  assert.match(c._calls.html, /✅/);
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `node --test tests/cards-co-mobile-list.test.js`
Expected: FAIL — `$('list')`가 빈 값만 받아 대부분의 `assert.match`가 실패.

- [ ] **Step 3: `renderCoMobileList()` 실제 구현**

Task 1에서 만든 뼈대를 지운다:

```javascript
function renderCoMobileList(){
  const el = $('list'); if(el) el.innerHTML = '';
}
```

다음으로 바꾼다:

```javascript
/* 폰 기업 상세 카드 목록 — 명함 목록(renderList)과 같은 .row/.rowmain/.nm/.l1/.l2
   클래스를 그대로 쓴다(대표가 고른 A안: 카드 목록 + 전체화면 상세, 2026-08-15).
   담는 자료는 PC 표(coListHtml)와 똑같이 coVisible() 을 쓴다 — 회사 목록 계산을
   두 벌로 두면 한쪽만 고쳤을 때 어긋난다(기업정보 폴더 설계서의 교훈과 같다). */
function renderCoMobileList(){
  const el = $('list'); if(!el) return;
  const list = coVisible();
  if(!list.length){
    el.innerHTML = `<div class="empty">회사를 찾지 못했습니다.</div>`;
    return;
  }
  el.innerHTML = list.map(o=>{
    const folder = o.folder ? _coFolders[o.folder] : null;
    const tags = coTagsOf(o);
    const click = state.selMode ? `coToggle('${esc(o.key)}')` : `pickCo('${esc(o.key)}')`;
    const badges = [
      o.erp && o.erp.type ? `<span class="tg">${esc(o.erp.type)}</span>` : '',
      folder ? `<span class="tg">📁 ${esc(folder.name)}</span>` : '',
      ...tags.map(t=>`<span class="tg">${esc(t)}</span>`)
    ].join('');
    const l1 = [o.bizno||'', o.erp && o.erp.main ? '담당 '+o.erp.main : ''].filter(Boolean).join(' · ');
    return `<div class="row" onclick="${click}">
      ${state.selMode?`<div class="selmark">${state.coSel[o.key]?'✅':'⚪'}</div>`:''}
      <div class="rowmain"><div class="nm">${esc(o.name)}</div>
        ${l1?`<div class="l1">${esc(l1)}</div>`:''}
        ${badges?`<div class="l2">${badges}</div>`:''}
      </div>
    </div>`;
  }).join('');
}
```

- [ ] **Step 4: `.tg` 배지 CSS가 이미 있는지 확인, 없으면 추가**

`pu-cards.html`에서 `.tg{` 를 검색한다. 명함 목록의 태그 배지(`coTagsOf` 표시에
쓰던 `.tg` 클래스, `renderCoPage` 근처)가 이미 있으면 그대로 재사용하고, 없으면
`.rowmain .nm` 규칙 근처에 추가한다:

```css
.rowmain .l2 .tg{display:inline-block;font-size:11px;font-weight:700;color:#5a6885;
  background:#eef2f8;border-radius:6px;padding:1px 7px;margin-right:5px}
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `node --test tests/cards-co-mobile-list.test.js`
Expected: PASS (6 tests)

- [ ] **Step 6: 브라우저로 실제 확인**

폰 너비(375px)로 열어 목업(A안)과 같은 모양인지, 회사 여러 곳이 카드로 나오는지 확인.

- [ ] **Step 7: 전체 테스트 확인 후 커밋**

```bash
node --test tests/*.test.js
git add pu-cards.html tests/cards-co-mobile-list.test.js
git commit -m "feat(폰): 기업 상세 카드 목록 구현"
```

---

### Task 3: 상세 패널을 폰에서 전체화면으로

**Files:**
- Modify: `pu-cards.html:977-981` (`#pcDetail` CSS)
- Test: `tests/cards-co-mobile-detail.test.js`

**Interfaces:**
- Consumes: `openCoDetailPanel(key)`, `coDetailPanelHtml(o)`, `closePcDetail()`, `pickCo(k)` — 전부 기존 그대로, 한 글자도 안 바꾼다.
- Produces: 없음 — CSS만 추가하는 과제라 다음 과제에 새로 주는 함수가 없다.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/cards-co-mobile-detail.test.js`:

```javascript
/* #pcDetail 은 원래 body.pc 일 때만 옆에 붙는 패널로 보였다(display:none 이 기본).
   폰(760px 이하)에서는 .open 이 붙으면 전체화면으로 보이게 하는 CSS 를 확인한다.
   ⚠ 내용(coDetailPanelHtml 등)은 손대지 않는다 — CSS 만 추가하는 과제다. */
const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');

const source = fs.readFileSync(path.join(__dirname, '..', 'pu-cards.html'), 'utf8');

test('#pcDetail 은 여전히 body.pc 가 아니면 기본은 숨겨져 있다', () => {
  assert.match(source, /#pcDetail\{[^}]*display:none/);
});

test('760px 이하에서 #pcDetail.open 은 전체화면으로 뜬다', () => {
  const at = source.indexOf('#pcDetail{');
  assert.ok(at > 0);
  const around = source.slice(at, at + 1500);
  assert.match(around, /@media\(max-width:760px\)\{[^}]*#pcDetail\.open\{[^}]*display:flex/s);
  assert.match(around, /@media\(max-width:760px\)\{[^}]*#pcDetail\.open\{[^}]*position:fixed;\s*inset:0/s);
});

test('coDetailPanelHtml·openCoDetailPanel·closePcDetail 은 그대로다(내용은 안 건드림)', () => {
  assert.match(source, /function coDetailPanelHtml\(o\)/);
  assert.match(source, /function openCoDetailPanel\(key\)/);
  assert.match(source, /function closePcDetail\(\)/);
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `node --test tests/cards-co-mobile-detail.test.js`
Expected: FAIL — `@media(max-width:760px)` 규칙이 아직 없음.

- [ ] **Step 3: CSS 추가**

`pu-cards.html:977-981`의 현재 코드:

```css
#pcDetail{position:fixed;top:0;right:0;width:420px;max-width:90vw;height:100vh;background:#fff;
  border-left:1px solid #e4e8f0;box-shadow:-8px 0 24px rgba(20,30,50,.08);
  transform:translateX(100%);transition:transform .22s ease;z-index:120;overflow-y:auto;display:none;flex-direction:column}
body.pc #pcDetail{display:flex}
#pcDetail.open{transform:translateX(0)}
```

다음으로 바꾼다(마지막 줄 뒤에 미디어쿼리 추가):

```css
#pcDetail{position:fixed;top:0;right:0;width:420px;max-width:90vw;height:100vh;background:#fff;
  border-left:1px solid #e4e8f0;box-shadow:-8px 0 24px rgba(20,30,50,.08);
  transform:translateX(100%);transition:transform .22s ease;z-index:120;overflow-y:auto;display:none;flex-direction:column}
body.pc #pcDetail{display:flex}
#pcDetail.open{transform:translateX(0)}
/* 폰 너비 — 옆에 붙는 패널이 아니라 전체화면 시트로 (대표 승인 A안, 2026-08-15).
   body.pc 가 아니어도 .open 이 붙으면 보이게 display:none 을 직접 뒤집는다. */
@media(max-width:760px){
  #pcDetail.open{display:flex;position:fixed;inset:0;width:100%;max-width:100%;height:100%;
    transform:none;border-left:none;box-shadow:none}
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `node --test tests/cards-co-mobile-detail.test.js`
Expected: PASS (3 tests)

- [ ] **Step 5: 브라우저로 실제 확인**

폰 너비에서 카드를 눌러 상세가 전체화면으로 뜨는지, ✕ 로 닫히는지, 이알피 이력·폴더 표시가 PC와 같은 내용으로 보이는지 확인.

- [ ] **Step 6: 전체 테스트 확인 후 커밋**

```bash
node --test tests/*.test.js
git add pu-cards.html tests/cards-co-mobile-detail.test.js
git commit -m "feat(폰): 기업 상세 패널을 전체화면 시트로"
```

---

### Task 4: 폴더 · 태그 고르기 바텀시트

**Files:**
- Modify: `pu-cards.html` (`#subbar`의 `groupBtn` onclick 분기, 새 함수 `openTopSheet()`/`openCoFolderSheet()`)
- Test: `tests/cards-co-folder-sheet.test.js`

**Interfaces:**
- Consumes: `_coFolders`(기존), `coTagList(coList())`(기존), `state.coFolder`/`state.coTag`/`state.coErpOnly`(기존), `pickCoFolder(k)`(기존 — `'erp'`, `'t:이름'`, `'f:id'`, `''` 네 가지 키를 받는다), `openCoFolderDialog()`(기존, 새 폴더 만들기/이알피 가져오기).
- Produces: `openCoFolderSheet()` — 새 함수. `openTopSheet()` — `#subbar`의 groupBtn이 부르는 새 분기 함수(카드/사업자 탭이면 기존 `openGroupSheet()`, 기업 상세 탭이면 `openCoFolderSheet()`).

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/cards-co-folder-sheet.test.js`:

```javascript
/* 폰에서 기업 상세 폴더/태그를 고르는 바텀시트 — 명함 탭의 그룹 선택(openGroupSheet)과
   같은 모양(.mhead/.sheetbtn)으로 맞춘다. 고른 뒤에는 기존 pickCoFolder(k) 를 그대로 부른다
   — 여기서 화면을 다시 그리는 로직을 새로 안 만든다(대표 승인 설계 4번). */
const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');
const vm = require('node:vm');

const source = fs.readFileSync(path.join(__dirname, '..', 'pu-cards.html'), 'utf8');

test('groupBtn 은 이제 openTopSheet 을 부른다', () => {
  const at = source.indexOf('id="groupBtn"');
  const end = source.indexOf('\n', at);
  assert.match(source.slice(at, end), /onclick="openTopSheet\(\)"/);
});

/* ⚠ 자르는 끝을 renderSubbar 바로 앞으로 잡는다. renderList 까지 넓게 자르면 그 사이의
   진짜 openGroupSheet 정의가 함께 딸려 들어와, 아래에서 쥐여준 openGroupSheet 스텁을
   덮어써 버린다(그러면 state.items·canSeeGroup 을 찾다 ReferenceError 가 난다).
   그래서 openTopSheet·openCoFolderSheet 두 함수는 반드시 renderSubbar 바로 앞에
   나란히 둔다 — Step 5 가 그 자리에 넣는다. */
function loadBlock(){
  const at = source.indexOf('function openTopSheet');
  const end = source.indexOf('\nfunction renderSubbar', at);
  assert.ok(at > 0 && end > at, 'openTopSheet~renderSubbar 사이를 찾지 못했습니다');
  const calls = { html:'', opened:false, groupSheetCalled:false };
  const ctx = {
    esc: s => String(s ?? ''),
    state: { view:'list', coFolder:'', coTag:'', coErpOnly:false },
    _coFolders: {},
    coList: () => [],
    coTagList: () => [],
    openGroupSheet: () => { calls.groupSheetCalled = true; },
    openCoFolderDialog: () => { calls.dialogOpened = true; },
    $: id => {
      if(id==='groupSheetM' || id==='folderSheetM') return { set innerHTML(v){ calls.html=v; }, get innerHTML(){ return calls.html; } };
      if(id==='groupSheetBg' || id==='folderSheetBg') return { classList: { add(){ calls.opened=true; }, remove(){ calls.opened=false; } } };
      return null;
    }
  };
  vm.createContext(ctx);
  vm.runInContext(source.slice(at, end), ctx);
  ctx._calls = calls;
  return ctx;
}

test('명함·사업자 화면이면 openTopSheet 이 기존 openGroupSheet 을 그대로 부른다', () => {
  const c = loadBlock();
  c.state.view = 'list';
  c.openTopSheet();
  assert.ok(c._calls.groupSheetCalled, '명함 화면에서는 기존 그룹 시트를 그대로 써야 합니다');
});

test('기업 상세 화면이면 openCoFolderSheet 이 전체·거래처만·폴더·태그를 나열한다', () => {
  const c = loadBlock();
  c.state.view = 'co';
  c._coFolders = { f1:{ id:'f1', name:'현장클리닉' } };
  c.coList = () => [{ key:'k1' }];
  c.coTagList = () => [{ t:'일터상생혁신', n:2 }];
  c.openTopSheet();
  assert.match(c._calls.html, /전체/);
  assert.match(c._calls.html, /거래처만/);
  assert.match(c._calls.html, /현장클리닉/);
  assert.match(c._calls.html, /일터상생혁신/);
  assert.ok(c._calls.opened);
});

test('폴더를 고르면 pickCoFolder(\'f:id\') 를 부르는 onclick 이 있다', () => {
  const c = loadBlock();
  c.state.view = 'co';
  c._coFolders = { f1:{ id:'f1', name:'현장클리닉' } };
  c.openCoFolderSheet();
  assert.match(c._calls.html, /onclick="[^"]*pickCoFolder\('f:f1'\)/);
});

test('시트 안에 새 폴더 만들기·이알피 가져오기로 가는 길이 있다', () => {
  const c = loadBlock();
  c.openCoFolderSheet();
  assert.match(c._calls.html, /openCoFolderDialog\(\)/);
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `node --test tests/cards-co-folder-sheet.test.js`
Expected: FAIL — `openTopSheet`/`openCoFolderSheet`가 아직 없음.

- [ ] **Step 3: `groupBtn`의 onclick을 바꾼다**

`pu-cards.html:1136`의 현재 코드:

```html
<button id="groupBtn" onclick="openGroupSheet()">전체 (0) <span class="v">▼</span></button>
```

다음으로 바꾼다:

```html
<button id="groupBtn" onclick="openTopSheet()">전체 (0) <span class="v">▼</span></button>
```

- [ ] **Step 4: 바텀시트용 모달 DOM 추가**

`pu-cards.html:1165`(`<div class="modalbg" id="groupSheetBg" ...>`) 바로 뒤에 추가:

```html
<div class="modalbg" id="folderSheetBg" onclick="if(event.target===this)this.classList.remove('open')"><div class="modal" id="folderSheetM"></div></div>
```

- [ ] **Step 5: `openTopSheet()`/`openCoFolderSheet()` 구현**

`renderSubbar()`(`pu-cards.html:5094` 부근) 바로 앞에 추가:

```javascript
/* 옆줄 「그룹」 자리를 명함·사업자·기업 상세가 함께 쓴다 — 탭마다 무엇을 고르는
   시트를 열지만 다르다(대표 지시 2026-08-15, 기업 상세도 폰에 생기며 추가). */
function openTopSheet(){
  if(state.view==='co') openCoFolderSheet();
  else openGroupSheet();
}
/* 기업 상세의 폴더·태그를 고르는 바텀시트 — 명함 탭의 openGroupSheet 과 같은 모양.
   고른 뒤에는 기존 pickCoFolder(k) 를 그대로 부른다(render() 를 이미 통해 다시 그린다,
   Task 6 에서 그 render() 가 폰에서도 맞게 그려지도록 손본다). */
function openCoFolderSheet(){
  const all = coList();
  const erpN = all.filter(o=>o.erp).length;
  const tags = coTagList(all);
  const folders = Object.values(_coFolders).sort((a,b)=>(a.order||0)-(b.order||0));
  let h = `<div class="mhead"><b>기업 상세 — 폴더/태그 선택</b><button class="iconbtn" onclick="folderSheetBg.classList.remove('open')">✕</button></div>`;
  const mk=(k,label,n,on)=>`<button class="sheetbtn" onclick="folderSheetBg.classList.remove('open');pickCoFolder('${k}')">
     ${on?'✅':'📁'} ${esc(label)}<span style="margin-left:auto;color:var(--sub);font-size:13px">${n}</span></button>`;
  const noneOn = !state.coErpOnly && !state.coTag && !state.coFolder;
  h += mk('', '전체', all.length, noneOn);
  h += mk('erp', '거래처만', erpN, state.coErpOnly);
  folders.forEach(f=> h += mk('f:'+f.id, f.name, all.filter(o=>o.folder===f.id).length, state.coFolder===f.id));
  tags.forEach(x=> h += mk('t:'+x.t, x.t, x.n, state.coTag===x.t));
  h += `<button class="sheetbtn" style="color:var(--orange)" onclick="folderSheetBg.classList.remove('open');openCoFolderDialog()">＋ 새 폴더 / 이알피에서 가져오기</button>`;
  $('folderSheetM').innerHTML = h;
  $('folderSheetBg').classList.add('open');
}
```

- [ ] **Step 6: 테스트 통과 확인**

Run: `node --test tests/cards-co-folder-sheet.test.js`
Expected: PASS (5 tests)

- [ ] **Step 7: 기존 명함/사업자 그룹 선택이 안 깨졌는지 확인**

Run: `node --test tests/cards-*.test.js` (그룹 관련 기존 검사 포함, 전부 통과해야 함)

- [ ] **Step 8: 전체 테스트 확인 후 커밋**

```bash
node --test tests/*.test.js
git add pu-cards.html tests/cards-co-folder-sheet.test.js
git commit -m "feat(폰): 기업 상세 폴더·태그 고르는 바텀시트"
```

---

### Task 5: 선택 모드 + 여러 곳 한 번에 옮기기 · 태그 담기

**Files:**
- Modify: `pu-cards.html` (`toggleSelMode()`, `#subbar` 선택 후 동작 바 추가)
- Test: `tests/cards-co-mobile-select.test.js`

**Interfaces:**
- Consumes: `state.coSel`(기존), `coMoveToFolder()`(기존, 폴더 목록 모달을 띄움), `coAssignTag()`(기존, 태그 담기 모달을 띄움), `coToggle(k)`(기존).
- Produces: `toggleSelMode()`가 `state.coSel`도 함께 비우도록 확장(기존 함수 수정, 새 이름 없음). `#subbar`에 기업 상세 전용 동작 바 마크업 없음 — 기존 `coMoveToFolder()`/`coAssignTag()`가 이미 만드는 `#folderDlgBg` 모달을 그대로 띄우는 버튼 두 개를 목록 위에 추가(Task 2가 만든 `renderCoMobileList()` 안에서 `state.selMode`일 때만).

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/cards-co-mobile-select.test.js`:

```javascript
/* 폰 기업 상세 선택 모드 — 명함의 ✎ 편집(state.selMode)과 같은 스위치를 쓰되,
   고른 회사는 state.coSel(기존 PC 표가 쓰던 것과 같은 자리)에 담는다.
   ⚠ toggleSelMode() 는 명함(state.sel)과 회사(state.coSel) 둘 다 비워야 한다 —
   탭을 넘나들며 고른 것이 남아 있으면 엉뚱한 곳에 옮겨진다. */
const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');
const vm = require('node:vm');

const source = fs.readFileSync(path.join(__dirname, '..', 'pu-cards.html'), 'utf8');

function loadToggleSelMode(){
  const at = source.indexOf('function toggleSelMode');
  const end = source.indexOf('\n}', at) + 2;
  const ctx = {
    state: { selMode:false, sel:{a:1}, coSel:{b:1} },
    $: id => id==='selLabel' ? { set textContent(v){ ctx._label=v; } } : (id==='fab' ? { style:{} } : null)
  };
  vm.createContext(ctx);
  vm.runInContext(source.slice(at, end), ctx);
  return ctx;
}

test('toggleSelMode 는 명함 선택과 회사 선택을 둘 다 비운다', () => {
  const c = loadToggleSelMode();
  c.toggleSelMode();
  assert.deepEqual(c.state.sel, {});
  assert.deepEqual(c.state.coSel, {}, '회사 선택(coSel)도 함께 비워야 탭을 넘나들 때 안 섞입니다');
});

function loadListBlockWithSelBar(items){
  const at = source.indexOf('function renderCoMobileList');
  const end = source.indexOf('\nfunction ', at + 10);
  const calls = { html:'' };
  const ctx = {
    esc: s => String(s ?? ''),
    state: { coSel:{}, selMode:true },
    _coFolders: {},
    coVisible: () => items,
    coTagsOf: () => [],
    $: id => id==='list' ? { set innerHTML(v){ calls.html=v; }, get innerHTML(){ return calls.html; } } : null
  };
  vm.createContext(ctx);
  vm.runInContext(source.slice(at, end), ctx);
  ctx._calls = calls;
  return ctx;
}

test('선택 모드에서 목록 위에 폴더로 옮기기·탭에 담기 동작 바가 보인다', () => {
  const c = loadListBlockWithSelBar([{ key:'k1', name:'나라크라샤', bizno:'', erp:null, folder:'', cards:[], docs:0, tags:{} }]);
  c.state.coSel = { k1:1 };
  c.renderCoMobileList();
  assert.match(c._calls.html, /coMoveToFolder\(\)/);
  assert.match(c._calls.html, /coAssignTag\(\)/);
});

test('아무 것도 안 골랐으면 동작 바가 안 보인다', () => {
  const c = loadListBlockWithSelBar([{ key:'k1', name:'나라크라샤', bizno:'', erp:null, folder:'', cards:[], docs:0, tags:{} }]);
  c.state.coSel = {};
  c.renderCoMobileList();
  assert.doesNotMatch(c._calls.html, /coMoveToFolder\(\)/);
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `node --test tests/cards-co-mobile-select.test.js`
Expected: FAIL — `state.coSel`이 안 비워짐, 동작 바가 없음.

- [ ] **Step 3: `toggleSelMode()`를 확장한다**

`pu-cards.html:5380-5384`의 현재 코드:

```javascript
function toggleSelMode(){
  state.selMode=!state.selMode; state.sel={};
  $('selLabel').textContent = state.selMode ? '완료' : '편집';
  $('fab').style.display = state.selMode ? 'none' : '';
```

다음으로 바꾼다(둘째 줄만 변경):

```javascript
function toggleSelMode(){
  state.selMode=!state.selMode; state.sel={}; state.coSel={};
  $('selLabel').textContent = state.selMode ? '완료' : '편집';
  $('fab').style.display = state.selMode ? 'none' : '';
```

- [ ] **Step 4: `renderCoMobileList()`에 선택 동작 바를 추가**

Task 2가 만든 함수의 시작 부분(`const list = coVisible();` 다음 줄)에 동작 바를
끼워 넣는다. 현재:

```javascript
function renderCoMobileList(){
  const el = $('list'); if(!el) return;
  const list = coVisible();
  if(!list.length){
```

다음으로 바꾼다:

```javascript
function renderCoMobileList(){
  const el = $('list'); if(!el) return;
  const list = coVisible();
  const nSel = Object.keys(state.coSel).length;
  const selbar = (state.selMode && nSel) ? `<div class="coselbar">
      <b>${nSel}곳 선택</b>
      <button onclick="coMoveToFolder()">📁 폴더로 옮기기</button>
      <button onclick="coAssignTag()">🏷 탭에 담기</button>
    </div>` : '';
  if(!list.length){
```

그리고 함수 마지막의 `el.innerHTML = list.map(...).join('');`을
`el.innerHTML = selbar + list.map(...).join('');`로 바꾼다.

- [ ] **Step 5: 테스트 통과 확인**

Run: `node --test tests/cards-co-mobile-select.test.js`
Expected: PASS (3 tests)

- [ ] **Step 6: 기존 `cards-co-info.test.js`/`cards-co-folders.test.js` 등 co 관련 검사가 안 깨졌는지 확인**

Run: `node --test tests/cards-co-*.test.js`
Expected: 전부 PASS (이번 과제가 `toggleSelMode`를 건드렸으니 명함 쪽 선택 모드
검사도 함께 통과해야 한다)

- [ ] **Step 7: 전체 테스트 확인 후 커밋**

```bash
node --test tests/*.test.js
git add pu-cards.html tests/cards-co-mobile-select.test.js
git commit -m "feat(폰): 기업 상세 선택 모드 — 폴더로 옮기기·탭에 담기"
```

---

### Task 6: `render()` 통일 — PC/폰 어느 쪽이든 맞게 다시 그리기

**Files:**
- Modify: `pu-cards.html` (아래 12곳 — `renderCoAny()` 신설 + 각 지점 교체)
- Test: `tests/cards-co-render-any.test.js`

**Interfaces:**
- Consumes: `render()`(기존, PC/폰을 가리는 진짜 진입점), `renderCoMobileList()`(Task 2), `renderPC()`(기존, PC 전용).
- Produces: `renderCoAny()` — 새 함수. 지금 활성 화면(PC 표 또는 폰 카드 목록)만 정확히 다시 그린다.

**★ Task 5 리뷰에서 추가된 요구 (2026-08-15) — 이것부터 고친다**

Task 5 리뷰가 실제로 화면이 깨지는 길을 찾았다. 아래 넷을 이 과제에서 함께 고친다.

**(가) [Critical] `toggleSelMode()` 가 기업 상세 화면을 명함 목록으로 덮어쓴다.**
`toggleSelMode()` 의 마지막 줄이 `renderSelbar(); renderList();` 라 화면을 안 가린다.
`#subbar` 의 「✎ 편집」 단추는 기업 상세 화면에서도 그대로 보이므로(폰에서 숨기는 규칙이
없다 — `body.pc` 에서만 숨는다), **회사를 고르려고 편집을 누르는 순간 회사 목록이
명함 목록으로 바뀐다.** 이 기능이 존재하는 이유 그 자체가 막히는 셈이다.
→ `renderSelbar(); renderList();` 를 화면을 가리는 길로 바꾼다. 기업 상세 화면이면
   `renderCoMobileList()` 만 다시 그리고 명함용 `#selbar` 는 손대지 않는다. 명함·사업자
   화면에서의 동작은 **한 글자도 달라지면 안 된다**(`renderSelbar()`+`renderList()` 그대로).
   ⚠ `render()` 를 그냥 부르면 안 된다 — `render()` 는 `renderSelbar()` 를 안 부르므로
   명함 화면에서 선택 띠가 안 그려진다. 화면별로 갈라라.

**(나) [Important] 화면을 오가면 고른 것이 남는다.**
`setTab()`·`openCoMobile()` 은 `state.view` 만 바꾸고 `state.sel`/`state.coSel`/
`state.selMode` 는 안 건드린다. 기업 상세에서 회사를 고른 뒤 명함으로 갔다가 돌아오면
예전에 고른 것이 그대로 남아, 사람이 뜻하지 않은 회사에 폴더·탭을 붙일 수 있다.
→ 화면을 바꿀 때 고른 것과 편집 모드를 푼다(`state.sel={}; state.coSel={};
   state.selMode=false;` + `#selLabel`·`#fab` 되돌리기). 두 함수가 같은 일을 하므로
   작은 helper 하나로 모아라 — 두 곳에 베끼면 한쪽만 고쳐져 어긋난다.

**(다) [Important] `tests/cards-co-mobile-select.test.js` 의 `esc` 스텁이 무동작이다.**
`esc: s => String(s ?? '')` 로 되어 있어, 이스케이프 **순서**를 바꿔도 검사가 안 잡는다.
같은 함수를 보는 `tests/cards-co-mobile-list.test.js` 는 진짜 이스케이프를 쓴다.
→ 진짜 구현으로 바꾼다(그 파일의 스텁 형태를 그대로 베낀다).

**(라) [Minor] `toggleSelMode` 검사가 `#selLabel`·`#fab` 을 안 본다.**
스텁으로 받아만 두고 assert 를 안 한다. 이 둘을 실제로 확인하는 assert 를 더한다
(명함 쪽 동작이 안 깨졌다는 것을 지키는 유일한 검사다).

---

**⚠ 주의**: 아래 12곳은 전부 **저장 후 다시 그리는 지점**이다. 표 정렬(`coSortBy`,
`coApplyColFilter`, `openCoColFilter`)과 PC 전용 찾기칸(`onPcSearchInput`,
`clearPcSearch`)은 **이 과제에서 손대지 않는다** — 표는 이번 범위 밖(설계서 "안
하기로 한 것"), PC 찾기칸은 `#pcSearch`라는 PC 전용 DOM에 묶여 있어 폰에서 애초에
불릴 일이 없다.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/cards-co-render-any.test.js`:

```javascript
/* renderCoAny() — 폴더·태그를 저장한 뒤 지금 보고 있는 화면(PC 표 또는 폰 카드 목록)만
   정확히 다시 그린다. 최종 전체 리뷰(2026-08-14)가 잡았던 "가회사 이력이 나회사 칸에
   써지는" 것과 같은 종류의 사고 — "PC 화면인데 폰 함수가 돌거나, 그 반대" — 를 막는다. */
const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');
const vm = require('node:vm');

const source = fs.readFileSync(path.join(__dirname, '..', 'pu-cards.html'), 'utf8');

function loadRenderAny(isPc){
  const at = source.indexOf('function renderCoAny');
  const end = source.indexOf('\n}', at) + 2;
  const calls = { mobile:0, pc:0 };
  const ctx = {
    state: { view:'list' },
    document: { body: { classList: { contains: c => c==='pc' && !!isPc } } },
    renderCoMobileList: () => calls.mobile++,
    renderPC: () => calls.pc++
  };
  vm.createContext(ctx);
  vm.runInContext(source.slice(at, end), ctx);
  ctx._calls = calls;
  return ctx;
}

test('폰에서 기업 상세를 보고 있으면 renderCoMobileList 만 부른다', () => {
  const c = loadRenderAny(false);
  c.state.view = 'co';
  c.renderCoAny();
  assert.equal(c._calls.mobile, 1);
  assert.equal(c._calls.pc, 0);
});

test('PC에서 기업 상세를 보고 있으면 renderPC 만 부른다', () => {
  const c = loadRenderAny(true);
  c.state.view = 'co';
  c.renderCoAny();
  assert.equal(c._calls.mobile, 0);
  assert.equal(c._calls.pc, 1);
});

test('기업 상세 화면이 아니면 아무 것도 안 부른다 (폰·PC 둘 다)', () => {
  [false, true].forEach(isPc=>{
    const c = loadRenderAny(isPc);
    c.state.view = 'list';
    c.renderCoAny();
    assert.equal(c._calls.mobile, 0);
    assert.equal(c._calls.pc, 0);
  });
});

test('PC/폰 판별은 render() 와 같은 방법(body.pc)을 쓴다', () => {
  const at = source.indexOf('function renderCoAny');
  const fn = source.slice(at, source.indexOf('\n}', at));
  assert.match(fn, /document\.body\.classList\.contains\('pc'\)/,
    '판별이 두 가지가 되면 한쪽만 고쳤을 때 어긋난다 — render() 와 같은 방법을 써야 한다');
});

const SITES = [
  { fn: 'pickCoTag', desc: '태그 고르기' },
  { fn: 'hideCoTag', desc: '태그 숨기기' },
  { fn: 'deleteCoFolder', desc: '폴더 삭제' },
  { fn: 'toggleCoErpOnly', desc: '거래처만 보기' },
  { fn: 'coToggle', desc: '체크 토글' },
  { fn: 'coMoveSelTo', desc: '폴더 이동' },
  { fn: 'coApplyTag', desc: '태그 담기' },
  { fn: 'coSelAll', desc: '전체 선택' },
  { fn: 'coImportFolderFromType', desc: '이알피 가져오기' },
];

for (const { fn, desc } of SITES){
  test(`${fn}(${desc}) 은 renderPC 를 직접 안 부르고 renderCoAny 를 부른다`, () => {
    const at = source.indexOf(`function ${fn}(`);
    assert.ok(at > 0, `${fn} 을 찾지 못했습니다`);
    const end = source.indexOf('\nfunction ', at + 10);
    const body = source.slice(at, end);
    assert.doesNotMatch(body, /\brenderPC\(\)/, `${fn} 이 아직 renderPC() 를 직접 부릅니다`);
    assert.doesNotMatch(body, /\brenderCoPage\(\)/, `${fn} 이 아직 renderCoPage() 를 직접 부릅니다`);
    assert.match(body, /\brenderCoAny\(\)/, `${fn} 이 renderCoAny() 를 불러야 합니다`);
  });
}

test('_coInfo/_coTagHidden/_coFolders 구독 콜백도 renderCoAny 를 쓴다', () => {
  ['coInfo','coTagHidden','coFolders'].forEach(k=>{
    const at = source.indexOf(`DB_ROOT+'/${k}'`);
    assert.ok(at > 0, `${k} 구독을 찾지 못했습니다`);
    const end = source.indexOf('\n', at);
    const line = source.slice(at, end);
    assert.match(line, /renderCoAny\(\)/, `${k} 구독 콜백이 renderCoAny() 를 불러야 합니다`);
  });
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `node --test tests/cards-co-render-any.test.js`
Expected: FAIL — `renderCoAny`가 아직 없고, 각 함수가 여전히 `renderPC()`/`renderCoPage()`를 직접 부름.

- [ ] **Step 3: `renderCoAny()` 추가**

`pu-cards.html`에서 `function renderCoMobileList(){` 바로 앞(Task 2가 만든 자리)에 추가:

```javascript
/* 기업 상세는 PC(표)·폰(카드 목록) 두 갈래로 그려진다. 저장 후 다시 그릴 때
   "지금 보고 있는 쪽"만 정확히 다시 그리려면 이 함수 하나를 거쳐야 한다 —
   renderPC() 를 폰에서 직접 부르면 존재하지도 않는 #pcCo 를 그리려다 아무 일도
   안 일어나고, renderCoMobileList() 를 PC에서 부르면 존재하지도 않는 #list 를
   찾다가 조용히 어긋난다(대표 지시 2026-08-15, 폰 기업 상세 추가로 처음 생긴
   문제 — 예전엔 기업 상세가 PC에만 있어 state.view==='co' 하나로 충분했다). */
function renderCoAny(){
  if(state.view!=='co') return;
  if(document.body.classList.contains('pc')) renderPC();
  else renderCoMobileList();
}
```

⚠ PC/폰 판별은 `render()` 가 이미 쓰는 것과 **똑같은 방법**(`document.body.classList.contains('pc')`)을 쓴다. `IS_PC` 상수를 다시 재는 것도, 창 너비를 새로 재는 것도 아니다 — 판별이 두 가지가 되면 한쪽만 고쳤을 때 어긋난다.

- [ ] **Step 4: 12곳을 하나씩 바꾼다**

각 지점은 **정확히 그 줄만** 바꾼다(주변 로직은 그대로 둔다).

**`_coInfo` 구독**(`Store.db.ref(DB_ROOT+'/coInfo')` 근처):
```javascript
// 이전: Store.db.ref(DB_ROOT+'/coInfo').on('value', s=>{ _coInfo = s.val() || {}; if(state.view==='co') renderPC(); });
Store.db.ref(DB_ROOT+'/coInfo').on('value', s=>{ _coInfo = s.val() || {}; renderCoAny(); });
```

**`pickCoTag`**:
```javascript
// 이전: function pickCoTag(t){ state.coTag = (state.coTag===t ? '' : t); closePcDetail(); renderCoPage(); }
function pickCoTag(t){ state.coTag = (state.coTag===t ? '' : t); closePcDetail(); renderCoAny(); }
```

**`_coTagHidden` 구독**:
```javascript
// 이전: Store.db.ref(DB_ROOT+'/coTagHidden').on('value', s=>{ _coTagHidden = s.val() || {}; if(state.view==='co') renderPC(); });
Store.db.ref(DB_ROOT+'/coTagHidden').on('value', s=>{ _coTagHidden = s.val() || {}; renderCoAny(); });
```

**`hideCoTag` 안**:
```javascript
// 이전: if(state.coTag===tag){ state.coTag=''; if(state.view==='co') renderPC(); }
if(state.coTag===tag){ state.coTag=''; renderCoAny(); }
```

**`_coFolders` 구독**:
```javascript
// 이전: Store.db.ref(DB_ROOT+'/coFolders').on('value', s=>{ _coFolders = s.val() || {}; if(state.view==='co') renderPC(); });
Store.db.ref(DB_ROOT+'/coFolders').on('value', s=>{ _coFolders = s.val() || {}; renderCoAny(); });
```

**`coImportFolderFromType` 끝**:
```javascript
// 이전: if(state.view==='co') renderPC();
renderCoAny();
```

**`deleteCoFolder` 안**:
```javascript
// 이전: if(state.coFolder===id){ state.coFolder=''; if(state.view==='co') renderPC(); }
if(state.coFolder===id){ state.coFolder=''; renderCoAny(); }
```

**`toggleCoErpOnly`**:
```javascript
// 이전: function toggleCoErpOnly(){ state.coErpOnly = !state.coErpOnly; closePcDetail();
//         try{ localStorage.setItem('pucards_co_erponly', state.coErpOnly?'1':''); }catch(_){}
//         renderCoPage(); }
function toggleCoErpOnly(){ state.coErpOnly = !state.coErpOnly; closePcDetail();
  try{ localStorage.setItem('pucards_co_erponly', state.coErpOnly?'1':''); }catch(_){}
  renderCoAny(); }
```

**`coToggle`**:
```javascript
// 이전: function coToggle(k){ if(state.coSel[k]) delete state.coSel[k]; else state.coSel[k]=1; renderCoPage(); }
function coToggle(k){ if(state.coSel[k]) delete state.coSel[k]; else state.coSel[k]=1; renderCoAny(); }
```

**`coMoveSelTo` 끝**:
```javascript
// 이전: if(state.view==='co') renderPC();
renderCoAny();
```

**`coApplyTag`(코드상 `coApplyTag` 함수 끝, 위와 같은 패턴) 끝**:
```javascript
// 이전: if(state.view==='co') renderPC();
renderCoAny();
```

**`coSelAll`**:
```javascript
// 이전: function coSelAll(on){ const list = coVisible(); state.coSel = {};
//         if(on) list.forEach(o=>{ state.coSel[o.key]=1; }); renderCoPage(); }
function coSelAll(on){ const list = coVisible(); state.coSel = {};
  if(on) list.forEach(o=>{ state.coSel[o.key]=1; }); renderCoAny(); }
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `node --test tests/cards-co-render-any.test.js`
Expected: PASS (13 tests)

- [ ] **Step 6: 뮤테이션 테스트 — `renderCoAny()`의 두 줄 중 하나를 지워서 실제로 잡히는지 확인**

`renderCoAny()`의 `if(state.view==='co') renderPC();` 줄을 잠시 지우고 실행:

```bash
node --test tests/cards-co-render-any.test.js
```

Expected: `'PC에서 기업 상세 화면을 보고 있으면 renderPC 만 부른다'` 테스트가 FAIL.
확인 후 그 줄을 되돌린다(Edit 도구로 정확히 복원, 파일 바이트 확인).

- [ ] **Step 7: 전체 테스트 확인**

```bash
node --test tests/*.test.js
```

Expected: 전부 PASS. 기존 `cards-co-*.test.js`(폴더·태그·이력 등)가 이번 배선
변경으로 안 깨졌는지 특히 확인.

- [ ] **Step 8: 브라우저로 실제 확인 (PC + 폰 둘 다)**

- PC(1280px)에서: 폴더 만들기→회사로 옮기기→태그 담기→이알피 가져오기가 전부
  예전처럼 그 자리에서 다시 그려지는지.
- 폰(375px)에서: 같은 조작이 카드 목록에 바로 반영되는지, 폴더 시트에서 고른
  값이 목록에 즉시 반영되는지.

- [ ] **Step 9: 커밋**

```bash
git add pu-cards.html tests/cards-co-render-any.test.js
git commit -m "fix(기업 상세): PC/폰 어느 쪽을 보고 있어도 맞게 다시 그리도록 통일"
```

---

## 전체 완료 후

- `node --test tests/*.test.js` 전체 통과 재확인.
- 설계서(`docs/superpowers/specs/2026-08-15-폰-기업상세-design.md`)의 "만들 것" 1~6번을 다시 훑어 빠진 것이 없는지 확인:
  1. 진입(탭) — Task 1 ✅
  2. 목록 카드 — Task 2 ✅
  3. 상세 — Task 3 ✅
  4. 폴더·태그·이알피 가져오기 — Task 4(고르기) + 기존 재사용(만들기/가져오기) ✅
  5. 여러 곳 선택 — Task 5 ✅
  6. 배선 정리 — Task 6 ✅
- 배포(GitHub Pages) 후 실제 폰(또는 375px 브라우저)에서 전체 흐름을 한 번 더 확인.
