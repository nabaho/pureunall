# 기업정보 폴더와 탭 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 기업정보 화면에 명함·사업자 갈래와 같은 「손으로 만드는 폴더」와 「여럿에 겹쳐 붙는 탭」을 더해, 컨설팅 신청이 들어온 사업장을 사업 단위로 모아 관리할 수 있게 한다.

**Architecture:** 새 회사 「갈래」를 만들지 않는다 — 기존 `pucards/coInfo/<회사열쇠>` 레코드에 `folder`(문자열 하나)와 `tags`(이미 있는 필드)를 더 쓴다. 폴더 목록은 `pucards/coFolders/<id>`에 따로 둔다. 화면은 이미 있는 명함 폴더 이동 UI(`#folderDlg`/`#folderDlgBg`, `.movefolder`)를 그대로 재사용한다.

**Tech Stack:** 빌드 없는 단일 HTML(`pu-cards.html`) + Firebase Realtime Database. 검사는 `node:test` + `node:assert/strict`, 순수 함수는 `node:vm`으로 `pu-cards.html`에서 코드 조각을 뽑아 실행한다(`tests/cards-hwp-integration.test.js`의 `loadMgTableHtml` 방식을 그대로 따른다).

## Global Constraints

- 새 `kind`(`'co'` 등)나 `state.tab` 값을 만들지 않는다 — 기업정보는 화면이지 갈래가 아니다(설계서 §"안 하기로 한 것").
- 회사를 가르는 열쇠는 **사업자번호**다(`coKeyOf`). 폴더·탭은 이 열쇠에 붙는다.
- 폴더는 회사 하나에 **하나만**(`coInfo/<key>/folder`, 문자열). 탭은 **여럿**(`coInfo/<key>/tags/<탭이름>`, 불리언 맵) — 이미 있는 구조를 그대로 쓴다.
- 탭 이름에서 `. # $ [ ] /` 를 뺀다 — 실시간DB 열쇠에 못 쓰는 글자다. `js/pu-doc-file.js`의 `sendToCoInfo`가 이미 하는 것과 같은 규칙(`replace(/[.#$/[\]]/g, ' ').replace(/\s+/g, ' ').trim()`)을 화면 쪽에도 둔다.
- 폴더를 지워도 회사는 안 지운다 — `folder` 값만 비운다.
- 탭은 **지우지 않는다** — 숨기기만 둔다(`pucards/coTagHidden/<탭이름>`).
- 회사 열쇠가 이름 열쇠(`n...`)에서 사업자번호 열쇠로 바뀌면, 옛 이름 열쇠에 있던 `folder`·`tags`가 안 보이게 된다. **조용한 배경 쓰기로 옮기지 않는다** — 읽을 때 두 열쇠의 값을 합쳐 보여준다(`coEffectiveExtra`). 사람이 다시 손대면 그때부터 새 열쇠에 남는다.
- Firebase 다중 경로 쓰기는 `Store.db.ref(DB_ROOT).update(upd)` 하나로 모은다 — 회사 하나씩 따로 쓰면 중간에 실패했을 때 절반만 반영된다.
- 파일은 UTF-8. 주석과 화면 글은 한국어, 이 저장소 말투(왜 이렇게 했는지)를 따른다.
- 커밋 메시지는 한국어, `feat(기업정보): …` 또는 `test(기업정보): …` 꼴.
- 모든 과제 끝에 `node --test tests/*.test.js` 가 `fail 0` 이어야 한다.

---

### Task 1: 회사 열쇠 이동 대비 — `coEffectiveExtra` 순수 함수

**Files:**
- Modify: `pu-cards.html` — `function coList()` 바로 앞(정확히는 `const coKeyOf = it => ...` 줄 바로 뒤)에 새 함수를 넣고, `coList()` 안의 `Object.values(by).forEach(...)` 블록에서 `_coInfo[o.key]`를 직접 읽던 것을 이 함수로 바꾼다.
- Test: `tests/cards-co-key-migration.test.js` (새 파일)

**Interfaces:**
- Consumes: 없음(순수 함수, `_norm`만 안다 — 이미 `pu-cards.html`에 있다)
- Produces: `coEffectiveExtra(key, name, coInfoMap)` → `object | null`
  - `coInfoMap`은 `{ [열쇠]: { folder?, tags?, docs?, ...서식칸 } }` 꼴
  - 반환값은 `folder`(문자열), `tags`(객체), `docs`(객체)를 포함해 `coDetailHtml`/`coTagsOf`/`coList`가 그대로 쓸 수 있는 모양

- [ ] **Step 1: 실패하는 검사를 쓴다**

`tests/cards-co-key-migration.test.js` 파일을 새로 만든다.

```javascript
/* 회사 열쇠가 이름 열쇠(n...)에서 사업자번호 열쇠로 바뀌면, 옛 이름 열쇠에 있던
   folder·tags 가 안 보이게 된다 — 사업자등록증이 나중에 들어온 회사가 이 경우다.
   조용한 배경 쓰기로 옮기지 않는다. 읽을 때 두 열쇠의 값을 합쳐 보여준다. */
const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');
const vm = require('node:vm');

const source = fs.readFileSync(path.join(__dirname, '..', 'pu-cards.html'), 'utf8');

function loadCoEffectiveExtra(){
  const normAt = source.indexOf('const _norm = s =>');
  assert.ok(normAt > 0, '_norm 정의를 찾지 못했습니다');
  const normEnd = source.indexOf('\n', normAt);
  const at = source.indexOf('function coEffectiveExtra');
  assert.ok(at > 0, 'coEffectiveExtra 를 찾지 못했습니다');
  /* coEffectiveExtra 는 coKeyOf 뒤, coList() 앞에 넣는다 — 그 사이만 자른다 */
  const end = source.indexOf('\nfunction coList', at);
  assert.ok(end > at, 'coEffectiveExtra 끝(coList 시작)을 찾지 못했습니다');
  const code = source.slice(normAt, normEnd) + '\n' + source.slice(at, end);
  const ctx = {};
  vm.createContext(ctx);
  vm.runInContext(code, ctx);
  return ctx.coEffectiveExtra;
}

test('이름 열쇠와 사업자번호 열쇠가 같은 회사면 옛 기록을 합친다', () => {
  const fn = loadCoEffectiveExtra();
  const map = {
    'n마루정공': { tags:{'2026 통합기술보호지원반':true}, folder:'f1' },
    '3128100001': { docName:'사업자등록증' }
  };
  const r = fn('3128100001', '마루정공', map);
  assert.equal(r.folder, 'f1', '옛 열쇠의 폴더가 안 보인다');
  assert.deepEqual(r.tags, {'2026 통합기술보호지원반':true}, '옛 열쇠의 탭이 안 보인다');
  assert.equal(r.docName, '사업자등록증', '새 열쇠의 값이 없어졌다');
});

test('새 열쇠에도 같은 이름의 값이 있으면 새 열쇠가 이긴다', () => {
  /* 사람이 새 열쇠에서 폴더를 다시 정했으면 그것을 따라야 한다 */
  const fn = loadCoEffectiveExtra();
  const map = {
    'n마루정공': { folder:'f1' },
    '3128100001': { folder:'f2' }
  };
  const r = fn('3128100001', '마루정공', map);
  assert.equal(r.folder, 'f2');
});

test('탭은 옛 열쇠·새 열쇠 것을 모두 합친다', () => {
  const fn = loadCoEffectiveExtra();
  const map = {
    'n마루정공': { tags:{'옛탭':true} },
    '3128100001': { tags:{'새탭':true} }
  };
  const r = fn('3128100001', '마루정공', map);
  assert.deepEqual(r.tags, {'옛탭':true, '새탭':true});
});

test('지금 열쇠가 이미 이름 열쇠면 그대로 돌려준다 — 합칠 상대가 없다', () => {
  const fn = loadCoEffectiveExtra();
  const map = { 'n마루정공': { folder:'f1' } };
  const r = fn('n마루정공', '마루정공', map);
  assert.equal(r.folder, 'f1');
});

test('둘 다 없으면 null', () => {
  const fn = loadCoEffectiveExtra();
  assert.equal(fn('3128100001', '마루정공', {}), null);
});

test('옛 열쇠만 있고 새 열쇠는 없으면 옛 열쇠 것을 그대로', () => {
  const fn = loadCoEffectiveExtra();
  const map = { 'n마루정공': { folder:'f1', docName:'서식' } };
  const r = fn('3128100001', '마루정공', map);
  assert.equal(r.folder, 'f1');
  assert.equal(r.docName, '서식');
});

test('상호에 (주)·띄어쓰기가 있어도 같은 이름 열쇠로 찾는다', () => {
  const fn = loadCoEffectiveExtra();
  const map = { 'n마루정공': { folder:'f1' } };
  const r = fn('3128100001', '(주) 마루정공', map);
  assert.equal(r.folder, 'f1', '_norm 을 안 거쳐 못 찾았다');
});
```

- [ ] **Step 2: 검사가 실패하는 것을 확인한다**

```bash
node --test tests/cards-co-key-migration.test.js
```

Expected: FAIL — `coEffectiveExtra 를 찾지 못했습니다`(6개 모두 실패)

- [ ] **Step 3: `coEffectiveExtra`를 만든다**

`pu-cards.html`에서 `const coKeyOf = it => { const d=digits(it.bizno||''); return d.length>=10 ? d : ('n'+_norm(it.company||'')); };` 줄 **바로 뒤**에 넣는다.

```javascript

/* 회사 열쇠가 이름 열쇠(n...)에서 사업자번호 열쇠로 바뀌면, 옛 이름 열쇠에 있던
   folder·tags 가 안 보이게 된다 — 사업자등록증이 나중에 들어온 회사가 이 경우다
   (설계서 2026-08-13, "회사 열쇠가 바뀌어 폴더가 끊어짐" 대비).
   ⚠ 조용한 배경 쓰기로 옮기지 않는다. 옮기다 실패하면 대표가 모르는 새 사라진다.
     읽을 때 두 열쇠의 값을 합쳐 보여준다 — 사람이 다시 손대면 그때부터 새 열쇠에 남는다. */
function coEffectiveExtra(key, name, coInfoMap){
  const cur = coInfoMap[key];
  const nameKey = 'n'+_norm(name||'');
  if(nameKey===key) return cur || null;   /* 합칠 상대가 없다 */
  const old = coInfoMap[nameKey];
  if(!old) return cur || null;
  if(!cur) return old;
  /* 폴더는 하나만 있을 수 있다 — 새 열쇠에서 사람이 다시 정한 것을 우선한다.
     탭은 여럿 겹칠 수 있다 — 옛 것도 새 것도 버리지 않는다. */
  return Object.assign({}, old, cur, {
    tags: Object.assign({}, old.tags||{}, cur.tags||{}),
    docs: Object.assign({}, old.docs||{}, cur.docs||{}),
    folder: cur.folder || old.folder || ''
  });
}
```

이제 `coList()` 안에서 `_coInfo[o.key]`를 직접 읽던 곳을 바꾼다. 현재 코드:

```javascript
  /* ③ 푸른이알피 — 업체유형·담당 노무사 */
  Object.values(by).forEach(o=>{
    const m = ErpMatch.match({ bizno:o.bizno, company:o.name });
    if(m){ o.erp = m; if(!o.ceo && m.ceoRaw) o.ceo = m.ceoRaw; }
    /* ④ 서식에서 읽은 칸 */
    const ex = _coInfo[o.key]; if(ex) o.extra = ex;
  });
```

이렇게 바꾼다.

```javascript
  /* ③ 푸른이알피 — 업체유형·담당 노무사 */
  Object.values(by).forEach(o=>{
    const m = ErpMatch.match({ bizno:o.bizno, company:o.name });
    if(m){ o.erp = m; if(!o.ceo && m.ceoRaw) o.ceo = m.ceoRaw; }
    /* ④ 서식에서 읽은 칸 + 폴더·탭. 열쇠가 바뀌었어도 옛 기록을 합쳐 보여준다. */
    const ex = coEffectiveExtra(o.key, o.name, _coInfo);
    if(ex){ o.extra = ex; o.folder = ex.folder || ''; }
  });
```

- [ ] **Step 4: 검사가 통과하는 것을 확인한다**

```bash
node --test tests/cards-co-key-migration.test.js
```

Expected: PASS — `tests 7`, `fail 0`

- [ ] **Step 5: 문법을 확인하고 전체 검사를 돌린다**

```bash
node "<scratchpad>/jscheck.js" pu-cards.html
node --test tests/*.test.js
```

Expected: `검사한 블록 7개 — ALL OK`, 전체 검사 `fail 0`. `<scratchpad>`가 없으면 다음 내용으로 만든다.

```javascript
const fs = require('node:fs');
const vm = require('node:vm');
const file = process.argv[2];
const src = fs.readFileSync(file, 'utf8');
const blocks = [...src.matchAll(/<script(?:\s[^>]*)?>([\s\S]*?)<\/script>/g)].map(m => m[1]);
let bad = 0, n = 0;
blocks.forEach((code, i) => {
  if (!code.trim()) return;
  n++;
  try { new vm.Script(code, { filename: `block${i}.js` }); }
  catch (e) { bad++; console.log(`블록 ${i}: 문법 오류 — ${e.message}`); }
});
console.log(bad ? `\n${bad}개 블록에 문법 오류` : `\n검사한 블록 ${n}개 — ALL OK`);
process.exit(bad ? 1 : 0);
```

- [ ] **Step 6: 커밋**

```bash
git add pu-cards.html tests/cards-co-key-migration.test.js
git commit -m "feat(기업정보): 회사 열쇠가 바뀌어도 폴더·탭 기록을 잃지 않는다"
```

---

### Task 2: 폴더 — 만들기·이름바꾸기·지우기, 옆줄에 넣기

**Files:**
- Modify: `pu-cards.html`
  - CSS: `/* ── 기업정보 화면 ── */` 블록(660번째 줄 근처)에 추가
  - `state` 기본값: `coErpOnly:false, coSel:{}, coSort:{...}` 뒤에 `coFolder:''` 추가
  - 옆줄: `if(state.view==='co'){ ... }` 블록(현재 7688~7701줄 근처) 수정
  - `pickCoFolder(k)` 함수 수정
  - `coVisible()` 함수 수정
  - `openCoPage()` 함수 수정(폴더 불러오기 추가)
  - `coDetailHtml(o)` 함수 수정(폴더 딱지 표시)
  - 새 함수들을 `pickCoFolder` 정의 앞뒤에 추가
- Test: `tests/cards-co-folders.test.js` (새 파일)

**Interfaces:**
- Consumes (Task 1에서): `coEffectiveExtra`, `o.folder` (이미 `coList()`가 채워 준다)
- Consumes (기존 코드): `pcItem(attrs,label,n,on)`, `$`, `esc`, `toast`, `uid()`, `Store.db`, `DB_ROOT`, `closeFolderMenu()`, `#folderMenu`, `.fmi`/`.fmi.danger`, `.gname`/`.gcount`/`.gmenu`/`.folderrow` CSS
- Produces:
  - `let _coFolders = {}` — `{ [id]: {id,name,order,at,by} }`
  - `loadCoFolders(cb)`
  - `putCoFolder(folder)`
  - `openCoFolderDialog()`, `confirmNewCoFolder()`
  - `renameCoFolder(id)`, `deleteCoFolder(id)`
  - `openCoFolderMenu(ev, id)`
  - `pickCoFolder(k)` — `k`가 `'f:<id>'`면 `state.coFolder=<id>`를 켠다(이미 있는 `''`/`'erp'`/`'t:...'` 분기에 추가)
  - `coVisible()` — `state.coFolder`가 있으면 `o.folder===state.coFolder`로 거른다

- [ ] **Step 1: 실패하는 검사를 쓴다**

`tests/cards-co-folders.test.js` 파일을 새로 만든다.

```javascript
/* 기업정보 폴더 — 명함·사업자와 같은 손놀림으로 손으로 만들고 회사를 담는다.
   ⚠ 회사는 폴더 하나에만 든다(명함 폴더와 같은 규칙). 여러 사업에 걸치는 것은
     탭이 맡는다 — 둘을 한 가지로 만들면 「이 회사가 왜 여기 있지」가 된다. */
const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');
const vm = require('node:vm');

const source = fs.readFileSync(path.join(__dirname, '..', 'pu-cards.html'), 'utf8');

test('폴더 저장소 함수가 있다', () => {
  assert.match(source, /let _coFolders = \{\}/);
  assert.match(source, /function loadCoFolders/);
  assert.match(source, /function putCoFolder/);
  assert.match(source, /DB_ROOT\+'\/coFolders'/);
});

test('만들기·이름바꾸기·지우기가 있다', () => {
  assert.match(source, /function openCoFolderDialog/);
  assert.match(source, /function confirmNewCoFolder/);
  assert.match(source, /function renameCoFolder/);
  assert.match(source, /function deleteCoFolder/);
});

test('폴더를 지워도 회사는 안 지운다 — 폴더 값만 비운다', () => {
  const at = source.indexOf('function deleteCoFolder');
  const fn = source.slice(at, at + 700);
  assert.match(fn, /confirm\(/, '지우기 전에 확인을 안 받는다');
  assert.match(fn, /folder.*=\s*null/s, '회사의 folder 값을 안 지운다');
  assert.doesNotMatch(fn, /delete state\.items/, '회사(명함·사업자)를 지우면 안 된다');
});

test('옆줄에 폴더 목록과 ＋가 있다', () => {
  const at = source.indexOf("if(state.view==='co'){");
  const fn = source.slice(at, at + 1400);
  assert.match(fn, /onclick="openCoFolderDialog\(\)"/, '＋ 를 못 찾았다');
  assert.match(fn, /_coFolders/, '옆줄이 폴더 목록을 안 그린다');
  assert.match(fn, /pickCoFolder\('f:/, "폴더를 눌러 고르는 길이 없다");
});

test("pickCoFolder 는 'f:' 로 시작하는 값을 폴더로 받는다", () => {
  const at = source.indexOf('function pickCoFolder');
  const fn = source.slice(at, at + 500);
  assert.match(fn, /k\.indexOf\('f:'\)===0/);
  assert.match(fn, /state\.coFolder\s*=\s*k\.slice\(2\)/);
});

test('coVisible 은 골라 둔 폴더로 거른다', () => {
  const at = source.indexOf('function coVisible');
  const fn = source.slice(at, source.indexOf('\n}', at));
  assert.match(fn, /if\(state\.coFolder\) list = list\.filter\(o=>o\.folder===state\.coFolder\)/);
});

test('회사 상세에 폴더 이름을 보여준다', () => {
  const at = source.indexOf('function coDetailHtml');
  const fn = source.slice(at, source.indexOf('\n}', at));
  assert.match(fn, /_coFolders\[o\.folder\]/);
});

test('openCoPage 가 폴더 목록도 불러온다', () => {
  const at = source.indexOf('function openCoPage');
  const fn = source.slice(at, at + 300);
  assert.match(fn, /loadCoFolders\(/);
});
```

- [ ] **Step 2: 검사가 실패하는 것을 확인한다**

```bash
node --test tests/cards-co-folders.test.js
```

Expected: FAIL — 8개 모두 실패

- [ ] **Step 3: 저장소 함수를 만든다**

`function pickCoTag(t){ state.coTag = (state.coTag===t ? '' : t); state.coPick=''; renderCoPage(); }` 줄 **바로 뒤**에 넣는다(`pickCoFolder` 정의 바로 앞).

```javascript

/* ── 기업정보 폴더 ── 손으로 만들고 회사를 담는다.
   명함이 폴더를 갖듯 회사도 갖는다. 회사는 폴더 하나에만 든다 — 여러 사업에 걸치는
   것은 탭이 맡는다(설계서 2026-08-13, "폴더와 탭을 왜 둘 다 두나"). */
let _coFolders = {};
let _coFoldersOn = false;
function loadCoFolders(cb){
  if(_coFoldersOn || Store.mode!=='firebase'){ cb && cb(); return; }
  _coFoldersOn = true;
  /* 사업자·자료함처럼 renderPC() 를 부른다 — 폴더 개수가 옆줄에도 보이므로
     renderCoPage() 만 부르면 옆줄 숫자가 안 바뀐다. */
  Store.db.ref(DB_ROOT+'/coFolders').on('value', s=>{ _coFolders = s.val() || {}; if(state.view==='co') renderPC(); });
  cb && cb();
}
function putCoFolder(folder){
  if(Store.mode!=='firebase') return toast('클라우드에 연결된 상태에서만 됩니다');
  Store.db.ref(DB_ROOT+'/coFolders/'+folder.id).set(folder);
}
function openCoFolderDialog(){
  $('folderDlg').innerHTML = `
    <div class="mhead"><b>새 폴더 만들기</b>
      <button class="iconbtn" onclick="folderDlgBg.classList.remove('open')">✕</button></div>
    <div class="field"><input id="newCoFolderName" placeholder="폴더 이름 (예: 컨설팅 신청)"
       onkeydown="if(event.key==='Enter')confirmNewCoFolder()"></div>
    <div class="mfoot">
      <button class="btn primary" onclick="confirmNewCoFolder()">만들기</button>
      <button class="btn ghost" onclick="folderDlgBg.classList.remove('open')">취소</button></div>`;
  $('folderDlgBg').classList.add('open');
  setTimeout(()=>{ const i=$('newCoFolderName'); if(i) i.focus(); }, 50);
}
function confirmNewCoFolder(){
  const name=$('newCoFolderName').value.trim(); if(!name) return toast('폴더 이름을 입력하세요');
  let by=''; try{ by=(firebase.auth().currentUser||{}).email||''; }catch(_){}
  putCoFolder({ id:uid(), name, order:Date.now(), at:Date.now(), by });
  $('folderDlgBg').classList.remove('open');
  toast('폴더를 만들었습니다');
}
function renameCoFolder(id){
  const f=_coFolders[id]; if(!f) return;
  const name=prompt('폴더 이름 변경', f.name);
  if(name===null || !name.trim()) return;
  putCoFolder(Object.assign({}, f, { name:name.trim() }));
}
/* 폴더를 지워도 회사는 안 지운다 — 폴더 값만 비운다. 회사 자체(명함·사업자등록증)는
   기업정보가 손댈 물건이 아니다(설계서 "안 하기로 한 것": 회사를 손으로 새로 만들지도
   지우지도 않는다). */
function deleteCoFolder(id){
  const f=_coFolders[id]; if(!f) return;
  if(!confirm(`「${f.name}」 폴더를 삭제할까요?\n(안에 있던 회사는 폴더 없음으로 돌아갑니다)`)) return;
  const upd = {};
  Object.keys(_coInfo).forEach(k=>{ if(_coInfo[k] && _coInfo[k].folder===id) upd['coInfo/'+k+'/folder']=null; });
  upd['coFolders/'+id] = null;
  Store.db.ref(DB_ROOT).update(upd).then(()=>{
    if(state.coFolder===id) state.coFolder='';
    toast('폴더를 삭제했습니다');
  });
}
function openCoFolderMenu(ev, id){
  const m=$('folderMenu');
  const f=_coFolders[id]; if(!f) return;
  m.innerHTML = `
    <div class="fmi" onclick="closeFolderMenu();renameCoFolder('${id}')">✏️ 이름 변경</div>
    <div class="fmi danger" onclick="closeFolderMenu();deleteCoFolder('${id}')">🗑 폴더 삭제</div>`;
  m.style.display='block';
  const x=Math.min(ev.clientX, window.innerWidth-170), y=Math.min(ev.clientY, window.innerHeight-120);
  m.style.left=x+'px'; m.style.top=y+'px';
  setTimeout(()=>document.addEventListener('click', closeFolderMenu, {once:true}), 0);
}
```

- [ ] **Step 4: `pickCoFolder`와 `coVisible`을 고친다**

현재:

```javascript
function pickCoFolder(k){
  state.coPick=''; state.coSel={};
  if(k==='erp'){ state.coErpOnly=true; state.coTag=''; }
  else if(k.indexOf('t:')===0){ state.coTag=k.slice(2); state.coErpOnly=false; }
  else { state.coErpOnly=false; state.coTag=''; }
  try{ localStorage.setItem('pucards_co_erponly', state.coErpOnly?'1':''); }catch(_){}
  render();
}
```

이렇게 바꾼다.

```javascript
function pickCoFolder(k){
  state.coPick=''; state.coSel={};
  if(k==='erp'){ state.coErpOnly=true; state.coTag=''; state.coFolder=''; }
  else if(k.indexOf('t:')===0){ state.coTag=k.slice(2); state.coErpOnly=false; state.coFolder=''; }
  else if(k.indexOf('f:')===0){ state.coFolder=k.slice(2); state.coErpOnly=false; state.coTag=''; }
  else { state.coErpOnly=false; state.coTag=''; state.coFolder=''; }
  try{ localStorage.setItem('pucards_co_erponly', state.coErpOnly?'1':''); }catch(_){}
  render();
}
```

현재:

```javascript
function coVisible(){
  const q = String(state.coQ||'').trim().toLowerCase();
  let list = coList();
  if(state.coErpOnly) list = list.filter(o=>o.erp);
  if(state.coTag) list = list.filter(o=>coTagsOf(o).indexOf(state.coTag)>=0);
  if(q) list = list.filter(o=>(o.name+' '+(o.bizno||'')+' '+(o.ceo||'')).toLowerCase().indexOf(q)>=0);
  return coSorted(list);
}
```

이렇게 바꾼다.

```javascript
function coVisible(){
  const q = String(state.coQ||'').trim().toLowerCase();
  let list = coList();
  if(state.coErpOnly) list = list.filter(o=>o.erp);
  if(state.coFolder) list = list.filter(o=>o.folder===state.coFolder);
  if(state.coTag) list = list.filter(o=>coTagsOf(o).indexOf(state.coTag)>=0);
  if(q) list = list.filter(o=>(o.name+' '+(o.bizno||'')+' '+(o.ceo||'')).toLowerCase().indexOf(q)>=0);
  return coSorted(list);
}
```

- [ ] **Step 5: `state` 기본값과 `openCoPage`를 고친다**

`coErpOnly:false, coSel:{}` 뒤에 이어지는 `coSort:{key:'name',dir:'asc'}` 를 찾아 그 뒤에 `, coFolder:''`를 붙인다.

```javascript
coErpOnly:false, coSel:{}, coSort:{key:'name',dir:'asc'}, coFolder:'',
```

`openCoPage()`의 현재 코드:

```javascript
function openCoPage(){ state.view='co'; state.coPick='';
  try{ state.coErpOnly = !!localStorage.getItem('pucards_co_erponly'); }catch(_){}
  render();
  loadCoInfo(); try{ $('pcMain').scrollTop=0; }catch(_){} }
```

이렇게 바꾼다.

```javascript
function openCoPage(){ state.view='co'; state.coPick='';
  try{ state.coErpOnly = !!localStorage.getItem('pucards_co_erponly'); }catch(_){}
  render();
  loadCoInfo(); loadCoFolders(); try{ $('pcMain').scrollTop=0; }catch(_){} }
```

- [ ] **Step 6: 옆줄에 폴더 목록을 그린다**

현재(7688~7701줄 근처):

```javascript
  if(state.view==='co'){
    const cos = coList();
    const erpN = cos.filter(o=>o.erp).length;
    h += `<div class="pcsec">폴더</div>`;
    h += pcItem(`onclick="pickCoFolder('')"`, '📋 전체', cos.length, !state.coErpOnly && !state.coTag);
    h += pcItem(`onclick="pickCoFolder('erp')"`, '🏢 거래처만', erpN, state.coErpOnly && !state.coTag);
    const tags = coTagList(cos);
    if(tags.length){
      h += `<div class="pcsec">사업별</div>`;
      tags.forEach(x=>{ h += pcItem(`onclick="pickCoFolder('t:${esc(x.t)}')"`, '📁 '+esc(x.t), x.n, state.coTag===x.t); });
    }else{
      h += `<div class="pchintside">사진첩에서 서식·신청서를<br>기업정보로 보내면<br>그 서류 이름으로 폴더가 생깁니다</div>`;
    }
    $('pcSide').innerHTML = h; return;
  }
```

이렇게 바꾼다.

```javascript
  if(state.view==='co'){
    const cos = coList();
    const erpN = cos.filter(o=>o.erp).length;
    h += `<div class="pcsec" style="display:flex;align-items:center">폴더
        <span style="margin-left:auto;cursor:pointer;font-weight:700" onclick="openCoFolderDialog()" title="새 폴더">＋</span></div>`;
    h += pcItem(`onclick="pickCoFolder('')"`, '📋 전체', cos.length, !state.coErpOnly && !state.coTag && !state.coFolder);
    h += pcItem(`onclick="pickCoFolder('erp')"`, '🏢 거래처만', erpN, state.coErpOnly && !state.coTag && !state.coFolder);
    /* 손으로 만든 폴더 — 명함 폴더와 같은 결(gname·gcount·gmenu)로 그린다 */
    Object.values(_coFolders).sort((a,b)=>(a.order||0)-(b.order||0)).forEach(f=>{
      const n = cos.filter(o=>o.folder===f.id).length;
      h += `<div class="pcitem folderrow ${state.coFolder===f.id?'on':''}" style="padding-left:11px">
          <span class="gname" onclick="pickCoFolder('f:${f.id}')">📁 ${esc(f.name)}</span>
          <span class="gcount">${n}</span>
          <span class="gmenu" onclick="event.stopPropagation();openCoFolderMenu(event,'${f.id}')" title="관리">⋮</span>
        </div>`;
    });
    const tags = coTagList(cos);
    if(tags.length){
      h += `<div class="pcsec">사업별</div>`;
      tags.forEach(x=>{ h += pcItem(`onclick="pickCoFolder('t:${esc(x.t)}')"`, '📁 '+esc(x.t), x.n, state.coTag===x.t); });
    }else{
      h += `<div class="pchintside">사진첩에서 서식·신청서를<br>기업정보로 보내면<br>그 서류 이름으로 폴더가 생깁니다</div>`;
    }
    $('pcSide').innerHTML = h; return;
  }
```

- [ ] **Step 7: 회사 상세에 폴더 딱지를 보여준다**

현재:

```javascript
function coDetailHtml(o){
  const val = f => o.extra[f] || o[f] || '';
  const rows = CO_FIELDS.filter(([f])=>val(f)).map(([f,label])=>
    `<div class="cof"><span>${esc(label)}</span><b>${esc(val(f))}</b></div>`).join('');
  return `<div class="codet">
    <div class="h"><button class="setback" onclick="pickCo('')">← 회사 목록</button>
      <b>${esc(o.name)}</b>${o.erp&&o.erp.type?`<span class="ty">${esc(o.erp.type)}</span>`:''}</div>
```

`</div>` 다음 줄까지 찾아, 헤더 줄만 이렇게 바꾼다.

```javascript
function coDetailHtml(o){
  const val = f => o.extra[f] || o[f] || '';
  const rows = CO_FIELDS.filter(([f])=>val(f)).map(([f,label])=>
    `<div class="cof"><span>${esc(label)}</span><b>${esc(val(f))}</b></div>`).join('');
  const folder = o.folder ? _coFolders[o.folder] : null;
  return `<div class="codet">
    <div class="h"><button class="setback" onclick="pickCo('')">← 회사 목록</button>
      <b>${esc(o.name)}</b>${o.erp&&o.erp.type?`<span class="ty">${esc(o.erp.type)}</span>`:''}
      ${folder?`<span class="ty" style="background:#fdf6e8;color:#92400e;border-color:#eeddb5">📁 ${esc(folder.name)}</span>`:''}</div>
```

- [ ] **Step 8: 검사가 통과하는 것을 확인한다**

```bash
node --test tests/cards-co-folders.test.js
node "<scratchpad>/jscheck.js" pu-cards.html
node --test tests/*.test.js
```

Expected: 새 검사 8개 PASS, 문법 OK, 전체 `fail 0`

- [ ] **Step 9: 커밋**

```bash
git add pu-cards.html tests/cards-co-folders.test.js
git commit -m "feat(기업정보): 폴더 만들기·이름바꾸기·지우기 — 옆줄에서 명함첩과 같은 손놀림"
```

---

### Task 3: 목록에서 골라 폴더로 옮기기 · 탭에 담기

**Files:**
- Modify: `pu-cards.html`
  - `coListHtml(list)` 맨 앞에 선택 도구줄 추가
  - CSS: `.coselbar` 추가
  - 새 함수들: `coCleanTagName`, `coMoveToFolder`, `coMoveSelTo`, `coAssignTag`, `coApplyTag`
- Test: `tests/cards-co-assign.test.js` (새 파일)

**Interfaces:**
- Consumes (Task 2에서): `_coFolders`
- Consumes (기존 코드): `state.coSel`(이미 있음), `coList()`, `coTagList()`, `Store.db`, `DB_ROOT`, `$`, `esc`, `toast`, `.movefolder`/`.mfic`/`.mhead`/`.mfoot`/`.field`/`.btn` CSS
- Produces:
  - `coCleanTagName(s)` → `string` — 순수 함수, `. # $ [ ] /` 를 빼고 다듬는다
  - `coMoveToFolder()`, `coMoveSelTo(folderId)`
  - `coAssignTag()`, `coApplyTag(raw)`

- [ ] **Step 1: 실패하는 검사를 쓴다**

`tests/cards-co-assign.test.js` 파일을 새로 만든다.

```javascript
/* 목록에서 회사를 골라 폴더로 옮기거나 탭에 담는다 — 이미 있는 명함 폴더 이동
   그림(#folderDlg, .movefolder)을 그대로 쓴다. 탭은 손으로 새 이름을 적으면
   그 자리에서 새로 생긴다 — 사진첩이 자동으로 만드는 것과 같은 자리(tags)를 쓴다. */
const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');
const vm = require('node:vm');

const source = fs.readFileSync(path.join(__dirname, '..', 'pu-cards.html'), 'utf8');

/* coCleanTagName 은 순수 함수라 실제로 돌려서 증명한다 */
function loadCoCleanTagName(){
  const at = source.indexOf('function coCleanTagName');
  assert.ok(at > 0, 'coCleanTagName 을 찾지 못했습니다');
  const end = source.indexOf('\n}', at) + 2;
  const ctx = {};
  vm.createContext(ctx);
  vm.runInContext(source.slice(at, end), ctx);
  return ctx.coCleanTagName;
}

test('탭 이름에서 실시간DB 가 못 쓰는 글자를 뺀다', () => {
  const fn = loadCoCleanTagName();
  assert.equal(fn('2026. 기술/경영 [혁신] #지원'), '2026 기술 경영 혁신 지원');
});

test('앞뒤 공백을 지운다', () => {
  const fn = loadCoCleanTagName();
  assert.equal(fn('  2026 통합기술보호지원반  '), '2026 통합기술보호지원반');
});

test('빈 이름은 빈 문자열', () => {
  const fn = loadCoCleanTagName();
  assert.equal(fn('   '), '');
  assert.equal(fn(undefined), '');
});

/* 실제 Firebase 쓰기까지 실행해 본다 — coMoveSelTo·coApplyTag 가 만드는
   update 꾸러미가 정확한지는 실제로 돌려야 증명된다(js/pu-doc-file.js 검사와 같은 방식). */
function loadAssignBlock(){
  const cleanAt = source.indexOf('function coCleanTagName');
  const cleanEnd = source.indexOf('\n}', cleanAt) + 2;
  const at = source.indexOf('function coMoveToFolder');
  assert.ok(at > 0, 'coMoveToFolder 를 찾지 못했습니다');
  const end = source.indexOf('\nfunction ', source.indexOf('function coApplyTag', at) + 10);
  const code = source.slice(cleanAt, cleanEnd) + '\n' + source.slice(at, end);

  const writes = [];
  const ctx = {
    state: { coSel: {} },
    _coFolders: {},
    esc: s => String(s),
    toast: () => {},
    confirm: () => true,
    $: id => ({ innerHTML:'', value:'', classList:{ add(){}, remove(){} } }),
    folderDlgBg: { classList: { add(){}, remove(){} } },
    Store: { db: { ref: p => ({ update: v => { writes.push({ path:p, val:v }); return Promise.resolve(); } }) } },
    DB_ROOT: 'pucards'
  };
  vm.createContext(ctx);
  vm.runInContext(code, ctx);
  ctx._writes = writes;
  return ctx;
}

test('고른 회사를 폴더로 옮기면 그 회사들의 folder 값만 바뀐다', async () => {
  const c = loadAssignBlock();
  c.state.coSel = { 'a':1, 'b':1 };
  c._coFolders = { f1:{id:'f1',name:'컨설팅 신청'} };
  await c.coMoveSelTo('f1');
  const w = c._writes[0];
  assert.equal(w.val['coInfo/a/folder'], 'f1');
  assert.equal(w.val['coInfo/b/folder'], 'f1');
  assert.equal(Object.keys(w.val).length, 2, '고르지 않은 회사까지 건드리면 안 된다');
});

test('폴더 없음으로 옮기면 folder 를 비운다', async () => {
  const c = loadAssignBlock();
  c.state.coSel = { 'a':1 };
  await c.coMoveSelTo('');
  assert.equal(c._writes[0].val['coInfo/a/folder'], null);
});

test('옮긴 뒤 고른 것을 비운다 — 다음 조작에 그대로 남으면 실수로 또 옮긴다', async () => {
  const c = loadAssignBlock();
  c.state.coSel = { 'a':1 };
  await c.coMoveSelTo('f1');
  assert.deepEqual(c.state.coSel, {});
});

test('탭에 담으면 고른 회사마다 그 탭 자리에 true 를 쓴다', async () => {
  const c = loadAssignBlock();
  c.state.coSel = { 'a':1, 'b':1 };
  await c.coApplyTag('2026 통합기술보호지원반');
  const w = c._writes[0];
  assert.equal(w.val['coInfo/a/tags/2026 통합기술보호지원반'], true);
  assert.equal(w.val['coInfo/b/tags/2026 통합기술보호지원반'], true);
});

test('탭 이름을 만들면서 못 쓰는 글자를 뺀다', async () => {
  const c = loadAssignBlock();
  c.state.coSel = { 'a':1 };
  await c.coApplyTag('2026. 기술/경영 [혁신]');
  const key = Object.keys(c._writes[0].val)[0];
  assert.equal(key, 'coInfo/a/tags/2026 기술 경영 혁신');
});

test('빈 탭 이름은 아무것도 쓰지 않는다', async () => {
  const c = loadAssignBlock();
  c.state.coSel = { 'a':1 };
  await c.coApplyTag('   ');
  assert.equal(c._writes.length, 0);
});

test('회사를 하나도 안 고르면 아무 일도 안 한다', () => {
  const c = loadAssignBlock();
  c.state.coSel = {};
  c.coMoveToFolder();
  c.coAssignTag();
  assert.equal(c._writes.length, 0);
});

test('옮기기 전에 고른 수를 보여준다 — 모르고 몇백 곳을 옮기면 안 된다', () => {
  /* 확인창을 따로 띄우지 않는다 — 이미 있는 명함 폴더 이동(selPickGroup/moveSelTo)과
     같은 결로, 숫자가 박힌 제목 자체가 확인 화면이고 폴더를 누르는 것이 확정이다. */
  const c = loadAssignBlock();
  c.state.coSel = { 'a':1, 'b':1, 'c':1 };
  let shown = '';
  /* value 는 안 쓰지만 innerHTML 은 값과 겉함수(getter/setter)를 한 객체에 같이
     두면 안 된다 — 자바스크립트가 어느 쪽을 따를지 애매해진다. getter/setter 만 둔다. */
  c.$ = id => ({ classList:{ add(){}, remove(){} },
    set innerHTML(v){ shown = v; }, get innerHTML(){ return shown; } });
  c.coMoveToFolder();
  assert.match(shown, /3곳을 옮길 폴더/);
});

test('화면: 선택 도구줄이 목록 위에 있다', () => {
  const at = source.indexOf('function coListHtml');
  const fn = source.slice(at, source.indexOf('function coDocsHtml', at));
  assert.match(fn, /class="coselbar"/);
  assert.match(fn, /onclick="coMoveToFolder\(\)"/);
  assert.match(fn, /onclick="coAssignTag\(\)"/);
});
```

- [ ] **Step 2: 검사가 실패하는 것을 확인한다**

```bash
node --test tests/cards-co-assign.test.js
```

Expected: FAIL — 모두 실패(`coCleanTagName 을 찾지 못했습니다` 등)

- [ ] **Step 3: 순수 함수와 배정 함수를 만든다**

`function coToggle(k){ if(state.coSel[k]) delete state.coSel[k]; else state.coSel[k]=1; renderCoPage(); }` 줄 **바로 뒤**에 넣는다(정확히는 `coToggle`과 `coSelAll` 정의 사이 — 함수 선언 순서는 동작에 영향을 안 주지만, 이 자리가 `coDetailHtml` 앞이라 관련 함수끼리 모인다).

```javascript

/* 탭 이름 다듬기 — js/pu-doc-file.js 의 sendToCoInfo 와 같은 규칙이다.
   ⚠ 두 곳이 같은 규칙을 따로 두고 있다. 한쪽만 고치면 사진첩에서 자동으로 붙는 탭과
     여기서 손으로 만드는 탭의 이름 규칙이 어긋난다 — 다음에 규칙을 바꿀 때 두 곳을 같이 본다. */
function coCleanTagName(s){
  return String(s||'').trim().replace(/[.#$/[\]]/g,' ').replace(/\s+/g,' ').trim();
}

/* ── 골라 둔 회사를 폴더로 옮기거나 탭에 담기 ──
   이미 있는 명함 폴더 이동 그림(#folderDlg, .movefolder — selPickGroup 이 쓰는 것과
   같다)을 그대로 쓴다. 새로 그림을 만들지 않는다. */
function coMoveToFolder(){
  const keys = Object.keys(state.coSel);
  if(!keys.length) return toast('회사를 먼저 선택하세요');
  let opts = `<div class="movefolder" onclick="coMoveSelTo('')"><span class="mfic">📭</span> 폴더 없음</div>`;
  opts += Object.values(_coFolders).sort((a,b)=>(a.order||0)-(b.order||0))
    .map(f=>`<div class="movefolder" onclick="coMoveSelTo('${f.id}')"><span class="mfic">📁</span> ${esc(f.name)}</div>`).join('');
  $('folderDlg').innerHTML = `
    <div class="mhead"><b>${keys.length}곳을 옮길 폴더</b>
      <button class="iconbtn" onclick="folderDlgBg.classList.remove('open')">✕</button></div>
    <div style="font-size:12.5px;color:#8b98b3;padding:0 6px 10px">아래 폴더를 누르면 선택한 회사가 옮겨집니다</div>
    <div style="max-height:46vh;overflow:auto;padding:2px;display:flex;flex-direction:column;gap:6px">${opts}</div>
    <div class="mfoot"><button class="btn ghost" onclick="folderDlgBg.classList.remove('open')">취소</button></div>`;
  $('folderDlgBg').classList.add('open');
}
async function coMoveSelTo(folderId){
  const keys = Object.keys(state.coSel);
  const upd = {};
  keys.forEach(k=>{ upd['coInfo/'+k+'/folder'] = folderId || null; });
  await Store.db.ref(DB_ROOT).update(upd);
  $('folderDlgBg').classList.remove('open');
  state.coSel={};
  toast(`${keys.length}곳을 옮겼습니다`);
  renderCoPage();
}
function coAssignTag(){
  const keys = Object.keys(state.coSel);
  if(!keys.length) return toast('회사를 먼저 선택하세요');
  const tags = coTagList(coList()).map(x=>x.t);
  const opts = tags.map(t=>`<div class="movefolder" onclick="coApplyTag('${esc(t).replace(/'/g,"\\'")}')"><span class="mfic">🏷</span> ${esc(t)}</div>`).join('');
  $('folderDlg').innerHTML = `
    <div class="mhead"><b>${keys.length}곳에 붙일 탭</b>
      <button class="iconbtn" onclick="folderDlgBg.classList.remove('open')">✕</button></div>
    <div class="field"><input id="newCoTagName" placeholder="새 탭 이름 (예: 2026 통합기술보호지원반)"
       onkeydown="if(event.key==='Enter')coApplyTag($('newCoTagName').value)"></div>
    ${opts?`<div style="font-size:11.5px;color:#8b98b3;padding:4px 6px 6px">이미 있는 탭</div>
      <div style="max-height:30vh;overflow:auto;padding:2px;display:flex;flex-direction:column;gap:6px">${opts}</div>`:''}
    <div class="mfoot">
      <button class="btn primary" onclick="coApplyTag($('newCoTagName').value)">담기</button>
      <button class="btn ghost" onclick="folderDlgBg.classList.remove('open')">취소</button></div>`;
  $('folderDlgBg').classList.add('open');
  setTimeout(()=>{ const i=$('newCoTagName'); if(i) i.focus(); }, 50);
}
async function coApplyTag(raw){
  const tag = coCleanTagName(raw);
  if(!tag) return toast('탭 이름을 입력해 주세요');
  const keys = Object.keys(state.coSel);
  if(!keys.length) return toast('회사를 먼저 선택하세요');
  const upd = {};
  keys.forEach(k=>{ upd['coInfo/'+k+'/tags/'+tag] = true; });
  await Store.db.ref(DB_ROOT).update(upd);
  $('folderDlgBg').classList.remove('open');
  state.coSel={};
  toast(`${keys.length}곳에 「${tag}」 탭을 붙였습니다`);
  renderCoPage();
}
```

**주의**: `coMoveToFolder()`/`coAssignTag()`는 회사를 하나도 안 골랐으면 `toast`만 부르고 끝나야 한다 — `$('folderDlg')`를 건드리기 전에 `if(!keys.length) return`을 반드시 먼저 실행한다(검사 "회사를 하나도 안 고르면 아무 일도 안 한다"가 이것을 본다).

- [ ] **Step 4: 목록 위에 선택 도구줄을 넣는다**

`coListHtml(list)`의 시작 부분, 현재:

```javascript
function coListHtml(list){
  if(!list.length) return `<div class="cpnone" style="padding:20px 2px">회사를 찾지 못했습니다</div>`;
  const allOn = list.length>0 && list.every(o=>state.coSel[o.key]);
```

이렇게 바꾼다.

```javascript
function coListHtml(list){
  if(!list.length) return `<div class="cpnone" style="padding:20px 2px">회사를 찾지 못했습니다</div>`;
  const allOn = list.length>0 && list.every(o=>state.coSel[o.key]);
  const nSel = Object.keys(state.coSel).length;
  const selbar = nSel ? `<div class="coselbar">
      <b>${nSel}곳 선택</b>
      <button onclick="coMoveToFolder()">📁 폴더로 옮기기</button>
      <button onclick="coAssignTag()">🏷 탭에 담기</button>
      <button onclick="state.coSel={};renderCoPage()">선택 해제</button>
    </div>` : '';
```

그리고 함수 맨 끝의 `return` 문, 현재:

```javascript
  return `<table class="cotbl">
```

이렇게 바꾼다.

```javascript
  return `${selbar}<table class="cotbl">
```

- [ ] **Step 5: CSS를 넣는다**

`/* ── 기업정보 화면 ── */` 블록 안, `.cosep{width:1px;background:#dbe3f0;margin:2px 4px}` 줄 **바로 뒤**에 넣는다.

```css
/* 목록에서 회사를 고르면 뜨는 도구줄 — 명함첩 선택 도구줄과 같은 결 */
.coselbar{display:flex;align-items:center;gap:8px;background:#eef3fc;border:1px solid #b5d4f4;border-radius:10px;padding:9px 13px;margin:0 0 10px;font-size:13px}
.coselbar b{color:#185fa5;margin-right:4px}
.coselbar button{border:1px solid #b5d4f4;background:#fff;border-radius:8px;padding:6px 11px;font-size:12.5px;font-weight:700;cursor:pointer;color:#185fa5;font-family:inherit}
.coselbar button:hover{background:#dbe9fb}
```

- [ ] **Step 6: 검사가 통과하는 것을 확인한다**

```bash
node --test tests/cards-co-assign.test.js
node "<scratchpad>/jscheck.js" pu-cards.html
node --test tests/*.test.js
```

Expected: 새 검사 11개 PASS, 문법 OK, 전체 `fail 0`

- [ ] **Step 7: 되돌려서 검사가 실제로 잡는지 확인한다**

```bash
# coMoveSelTo 에서 state.coSel={} 를 지우고 돌려본다 — 실패해야 한다
```

`coMoveSelTo` 안의 `state.coSel={};` 줄을 잠깐 지우고 `node --test tests/cards-co-assign.test.js`를 돌려 "옮긴 뒤 고른 것을 비운다" 검사가 실패하는지 확인한 뒤 되돌린다. `coApplyTag`의 `coCleanTagName(raw)` 호출을 `raw`로 바꿔 "못 쓰는 글자를 뺀다" 검사가 실패하는지도 같은 방식으로 확인한다.

- [ ] **Step 8: 커밋**

```bash
git add pu-cards.html tests/cards-co-assign.test.js
git commit -m "feat(기업정보): 목록에서 골라 폴더로 옮기거나 탭에 담는다"
```

---

### Task 4: 탭 숨기기 — 사업이 끝나도 지우지 않는다

**Files:**
- Modify: `pu-cards.html`
  - `state` 기본값에 `coShowHidden:false` 추가
  - 옆줄 「사업별」 목록 부분(Task 2에서 손댄 자리) 수정
  - 새 함수: `loadCoTagHidden`, `hideCoTag`, `unhideCoTag`
- Test: `tests/cards-co-tag-hide.test.js` (새 파일)

**Interfaces:**
- Consumes (Task 2/3에서): `coTagList`, `pcItem`
- Produces:
  - `let _coTagHidden = {}`
  - `loadCoTagHidden(cb)`
  - `hideCoTag(tag)`, `unhideCoTag(tag)`
  - `state.coShowHidden` — 숨긴 탭을 볼지

- [ ] **Step 1: 실패하는 검사를 쓴다**

`tests/cards-co-tag-hide.test.js` 파일을 새로 만든다.

```javascript
/* 탭은 지우지 않는다 — 숨기기만 둔다. 지우면 어느 회사가 그 사업이었는지 잃는다
   (설계서 2026-08-13, "안 하기로 한 것: 탭 지우기"). */
const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');

const source = fs.readFileSync(path.join(__dirname, '..', 'pu-cards.html'), 'utf8');

test('탭 지우기 함수가 없다 — 숨기기만 있다', () => {
  assert.doesNotMatch(source, /function deleteCoTag/, '탭을 지우는 함수를 만들면 안 된다');
  assert.match(source, /function hideCoTag/);
  assert.match(source, /function unhideCoTag/);
});

test('숨김 저장소가 있다', () => {
  assert.match(source, /let _coTagHidden = \{\}/);
  assert.match(source, /function loadCoTagHidden/);
  assert.match(source, /DB_ROOT\+'\/coTagHidden'/);
});

test('hideCoTag 는 지우지 않고 숨김 표시만 단다', () => {
  const at = source.indexOf('function hideCoTag');
  const fn = source.slice(at, at + 300);
  assert.doesNotMatch(fn, /tags\/'\s*\+\s*tag\]\s*=\s*null/, '탭 배정을 지우면 안 된다');
  assert.match(fn, /coTagHidden\/'\s*\+\s*tag\]\s*=\s*true/);
});

test('옆줄 사업별 목록은 숨긴 탭을 건너뛴다', () => {
  const at = source.indexOf("if(state.view==='co'){");
  const fn = source.slice(at, at + 1900);
  assert.match(fn, /_coTagHidden\[x\.t\]/, '숨긴 탭을 거르지 않는다');
});

test('숨긴 탭이 있으면 보기 링크가 뜬다', () => {
  const at = source.indexOf("if(state.view==='co'){");
  const fn = source.slice(at, at + 1900);
  assert.match(fn, /state\.coShowHidden/);
});
```

- [ ] **Step 2: 검사가 실패하는 것을 확인한다**

```bash
node --test tests/cards-co-tag-hide.test.js
```

Expected: FAIL — 5개 모두 실패

- [ ] **Step 3: 숨김 저장소 함수를 만든다**

`function pickCoTag(t){ state.coTag = (state.coTag===t ? '' : t); state.coPick=''; renderCoPage(); }` 줄 **바로 뒤**에 넣는다.

```javascript

/* 탭은 지우지 않는다 — 숨기기만 둔다. 사업이 끝나도 「그때 누가 신청했었나」를
   나중에 다시 봐야 할 수 있다. 지우면 그 연결이 영영 사라진다. */
let _coTagHidden = {};
let _coTagHiddenOn = false;
function loadCoTagHidden(cb){
  if(_coTagHiddenOn || Store.mode!=='firebase'){ cb && cb(); return; }
  _coTagHiddenOn = true;
  Store.db.ref(DB_ROOT+'/coTagHidden').on('value', s=>{ _coTagHidden = s.val() || {}; if(state.view==='co') renderPC(); });
  cb && cb();
}
function hideCoTag(tag){
  Store.db.ref(DB_ROOT).update({ ['coTagHidden/'+tag]: true });
}
function unhideCoTag(tag){
  Store.db.ref(DB_ROOT).update({ ['coTagHidden/'+tag]: null });
}
function toggleCoShowHidden(){ state.coShowHidden = !state.coShowHidden; renderPC(); }
```

`openCoPage()`에 `loadCoTagHidden()`도 불러오게 한다. Task 2에서 만든 줄:

```javascript
  loadCoInfo(); loadCoFolders(); try{ $('pcMain').scrollTop=0; }catch(_){} }
```

이렇게 바꾼다.

```javascript
  loadCoInfo(); loadCoFolders(); loadCoTagHidden(); try{ $('pcMain').scrollTop=0; }catch(_){} }
```

`state` 기본값의 `coFolder:''` 뒤에 `coShowHidden:false`를 붙인다.

```javascript
coErpOnly:false, coSel:{}, coSort:{key:'name',dir:'asc'}, coFolder:'', coShowHidden:false,
```

- [ ] **Step 4: 옆줄 「사업별」 목록에서 숨긴 탭을 거른다**

Task 2에서 만든 이 부분:

```javascript
    const tags = coTagList(cos);
    if(tags.length){
      h += `<div class="pcsec">사업별</div>`;
      tags.forEach(x=>{ h += pcItem(`onclick="pickCoFolder('t:${esc(x.t)}')"`, '📁 '+esc(x.t), x.n, state.coTag===x.t); });
    }else{
      h += `<div class="pchintside">사진첩에서 서식·신청서를<br>기업정보로 보내면<br>그 서류 이름으로 폴더가 생깁니다</div>`;
    }
```

이렇게 바꾼다.

```javascript
    const tags = coTagList(cos);
    const shown = tags.filter(x=>state.coShowHidden || !_coTagHidden[x.t]);
    const hiddenN = tags.length - shown.length;
    if(tags.length){
      h += `<div class="pcsec">사업별</div>`;
      shown.forEach(x=>{ h += `<div class="pcitem folderrow ${state.coTag===x.t?'on':''}" style="padding-left:11px">
          <span class="gname" onclick="pickCoFolder('t:${esc(x.t)}')">📁 ${esc(x.t)}</span>
          <span class="gcount">${x.n}</span>
          ${_coTagHidden[x.t]
            ? `<span class="gmenu" onclick="event.stopPropagation();unhideCoTag('${esc(x.t).replace(/'/g,"\\'")}')" title="다시 보이기">🙈</span>`
            : `<span class="gmenu" onclick="event.stopPropagation();hideCoTag('${esc(x.t).replace(/'/g,"\\'")}')" title="목록에서 숨기기(지우지 않음)">👁</span>`}
        </div>`; });
      if(hiddenN && !state.coShowHidden){
        h += `<div class="pchintside" style="cursor:pointer;text-decoration:underline" onclick="toggleCoShowHidden()">숨긴 사업 ${hiddenN}개 보기</div>`;
      }else if(state.coShowHidden && hiddenN){
        h += `<div class="pchintside" style="cursor:pointer;text-decoration:underline" onclick="toggleCoShowHidden()">숨긴 사업 접기</div>`;
      }
    }else{
      h += `<div class="pchintside">사진첩에서 서식·신청서를<br>기업정보로 보내면<br>그 서류 이름으로 폴더가 생깁니다</div>`;
    }
```

- [ ] **Step 5: 검사가 통과하는 것을 확인한다**

```bash
node --test tests/cards-co-tag-hide.test.js
node "<scratchpad>/jscheck.js" pu-cards.html
node --test tests/*.test.js
```

Expected: 새 검사 5개 PASS, 문법 OK, 전체 `fail 0`

- [ ] **Step 6: 커밋**

```bash
git add pu-cards.html tests/cards-co-tag-hide.test.js
git commit -m "feat(기업정보): 탭 숨기기 — 사업이 끝나도 지우지 않는다"
```

---

### Task 5: 실제 화면으로 끝까지 확인한다

**Files:**
- 없음(코드 변경 없음). 로컬 서버 + 브라우저로 확인만 한다.

**Interfaces:**
- Consumes: Task 1~4의 모든 결과물

- [ ] **Step 1: 로컬 서버를 띄운다**

```bash
python -m http.server 8863
```

- [ ] **Step 2: 브라우저에서 실제 데이터로 흐름을 돌린다**

`pu-cards.html`을 열고 로그인 화면·탭바를 가린 뒤(이 세션에서 계속 써 온 방식대로 `cardsLogin`/`appbar`/`tabs`/`subbar`/`main`/`fab`를 `display:none`으로 숨기고 `pcRoot`를 `flex`로 켠다), 아래 시나리오를 콘솔에서 돌린다 — 실제 컨설팅 사업(캡쳐4·5·6) 상황을 흉내낸다.

```javascript
state.items = {
  b1:{id:'b1',kind:'biz',company:'라온프라텍',bizno:'123-81-20308',ceo:'임하람',createdAt:Date.now()},
  c1:{id:'c1',kind:'card',name:'임하람',company:'라온프라텍',createdAt:Date.now()},
  b2:{id:'b2',kind:'biz',company:'가나상사',bizno:'123-81-20411',createdAt:Date.now()},
  c2:{id:'c2',kind:'card',name:'김보람',company:'가나상사',createdAt:Date.now()},
  /* 사업자등록증이 아직 없는 회사 — 이름 열쇠로만 잡힌다 */
  c3:{id:'c3',kind:'card',name:'박한별',company:'다라기업',createdAt:Date.now()}
};
state.groups={}; ErpMatch.ready=true; ErpMatch.byBiz={}; ErpMatch.byName={};
_coInfo = {
  /* 다라기업은 아직 사업자번호가 없다 — 이름 열쇠(n다라기업)에 탭이 붙어 있다.
     Task 1 이 이걸 새 열쇠가 생겨도 잃지 않는지 증명한다. */
  'n다라기업': { tags:{'2026 통합기술보호지원반':true} }
};
openCoPage();
```

확인할 것:

1. 옆줄에 `폴더` 머리 옆 `＋`가 있고, 누르면 이름 입력창이 뜬다. `컨설팅 신청`으로 폴더를 만든다.
2. `전체` 목록에서 `라온프라텍`·`가나상사`를 네모로 고른다. 도구줄에 `2곳 선택`이 뜬다.
3. `📁 폴더로 옮기기` → `컨설팅 신청` 클릭 → 옆줄의 `컨설팅 신청` 폴더 숫자가 `2`로 바뀐다.
4. 그 폴더를 눌러 두 회사만 나오는지 확인한다.
5. `전체`로 돌아와 `다라기업`을 확인한다 — **사업자번호가 아직 없는데도** 옆줄 `사업별`에 `2026 통합기술보호지원반 1`이 보이고, 그 회사를 열면 상세에 아무 것도 안 지워진 채 나온다(Task 1의 `coEffectiveExtra` 증명).
6. 다라기업의 사업자등록증을 흉내내 추가한다.

```javascript
state.items.b3 = {id:'b3',kind:'biz',company:'다라기업',bizno:'312-81-00003',createdAt:Date.now()};
renderCoPage();
```

7. `다라기업`을 다시 찾는다 — 열쇠가 사업자번호로 바뀌었는데도 `2026 통합기술보호지원반` 탭이 **그대로 붙어 있는지** 확인한다(끊기지 않았다).
8. `가나상사`를 골라 `🏷 탭에 담기` → 새 탭 이름 `2026 통합기술보호지원반` 입력 → 담기. 그 탭에 `가나상사`가 추가로 나오는지 확인한다(다라기업과 함께 2곳).
9. 옆줄에서 그 탭 옆 `👁`를 눌러 숨긴다 — `사업별` 목록에서 사라지고 `숨긴 사업 1개 보기`가 뜨는지 확인한다. 눌러 다시 보이는지, `🙈`로 다시 숨기는지 확인한다.
10. 폴더를 지운다(`컨설팅 신청` ⋮ → 삭제) — 확인창이 뜨고, 지운 뒤 `라온프라텍`·`가나상사`가 **여전히 전체 목록에 있는지**(회사 자체는 안 지워졌는지) 확인한다.

- [ ] **Step 3: 전체 검사를 마지막으로 한 번 더 돌린다**

```bash
node --test tests/*.test.js
```

Expected: `fail 0`

- [ ] **Step 4: 서버를 내리고 정리한다**

브라우저 콘솔에서 확인한 내용을 간단히 기록만 하고(코드 변경 없음), 로컬 서버 프로세스를 종료한다.

---

## 이 계획이 다루지 않는 것

설계서에 이미 "안 하기로 한 것"으로 적혀 있다.

- **폴더 안 폴더** — 명함·사업자도 1단계다.
- **회사를 손으로 새로 만들기** — 기업정보는 이미 있는 것을 모아 보는 화면이다.
- **탭 지우기** — 숨기기만 둔다(Task 4).
- **열쇠 이동의 자동 서버 쓰기** — Task 1은 **읽을 때 합쳐 보여줄 뿐**, 옛 열쇠의 기록을 새 열쇠로 옮겨 쓰지 않는다. 조용한 배경 쓰기는 실패했을 때 대표가 모르는 새 데이터가 사라지는 위험이 있다.
