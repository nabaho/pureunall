# 서류 자동 등록 구현 계획 — 판독 결과를 각 앱에 넣기

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 판독이 끝난 서류를 각 프로그램에 넣는다. 기계 검증을 통과한 것은 자동으로, 걸린 것만 사람이 확인한다.

**Architecture:** 사진첩은 판독 결과를 갖고 있고, 어디에 어떻게 넣을지는 **등록 층 `js/pu-doc-file.js`(신규)** 가 안다. 판독 층(`pu-doc-read.js`)이 "읽기"를 아는 유일한 파일인 것과 짝을 이룬다. 화면은 등록 층 함수 하나를 부르고 결과 문구만 띄운다.

**Tech Stack:** Firebase RTDB compat · 빌드 없음 · `node --test`

## 착수 전 관문 (아직 안 열림)

- [ ] **대표님이 서류 몇 장을 올려 판독 정확도를 확인했다.** 잘못 읽는다면 이 계획이 아니라 프롬프트를 먼저 고친다.
- [ ] **업체관리의 `🔗 업체 연결`·`🌐 바깥 앱 연결 고정` 실행 완료.** 업체에 `co_id`가 안 붙어 있으면 "각자의 프로그램"이 같은 회사를 못 알아본다.

## Global Constraints

- **설계서** `docs/superpowers/specs/2026-08-02-푸른사진첩-design.md` 6-0장이 우선.
- **다중 경로 `update` 한 번만.** 상위 노드 `set` 금지 (2026-07 실데이터 사고).
- **명함첩에 쓸 때 검색 인덱스 `pucards/idx/{id}` 를 반드시 함께 쓴다.** 안 쓰면 다른 앱이 그 명함을 못 찾는다(명함첩의 `idxRecord` 형태를 그대로 따른다: `n c m t e ti d ct ad bz ceo k`).
- **사업자등록번호 없이 업체를 새로 만들지 않는다.** 그게 지금 업체 중복 126쌍이 생긴 경로다.
- **잠긴 폴더(개인잠금) 명함은 idx에 넣지 않는다** — 명함첩의 기존 규칙과 같게.
- 한국어 주석 · ES5 스타일 · 브라우저 시험에서는 **저장 함수를 반드시 가짜로 갈아끼운다**.
- 커밋은 경로 지정. 여러 세션이 같은 작업트리를 공유한다.

## 핵심 설계 결정 — 기존 업체를 만나면 "빈 칸만" 채운다

사업자등록증을 읽었을 때 갈 곳이 두 가지다.

| 상황 | 무엇을 하나 | 왜 |
|---|---|---|
| **사업자번호로 기존 업체를 찾았다** | 그 업체의 **빈 칸만** 채운다 (대표자·업태·종목·개업일·주소 중 비어 있는 것) | 기존 값을 절대 덮지 않으므로 사고가 없다. 그리고 **사업자번호가 없던 업체 63개를 메우는 것이 이 기능의 실제 값**이다 |
| **찾지 못했다** | 명함첩에 `kind:'biz'` 레코드로 남긴다. **사업장 신규 생성은 하지 않는다** | 새 업체를 만드는 일은 계약·청구가 걸린 결정이다. 사진 한 장으로 자동 생성하면 유령 업체가 쌓인다 |

명함은 더 단순하다 — 휴대폰 번호로 중복을 걸러 명함첩에 넣는다(위험이 낮고 명함첩이 이미 그렇게 한다).

중소기업확인서는 기존 업체를 찾았을 때 **기업규모와 유효기간만** 채운다.

## File Structure

| 파일 | 책임 |
|---|---|
| `js/pu-doc-file.js` (신규) | 등록 층. 기존 업체 찾기 · 빈 칸만 채우기 · 명함첩 레코드+인덱스 쓰기 · 중복 판정. **어디에 넣는지 아는 유일한 파일** |
| `pu-photos.html` (수정) | 판독 결과 패널에 「등록」 결과 표시. 자동 등록 알림 |
| `tests/pu-doc-file.test.js` (신규) | 가짜 db로 등록 동작 검사 |

---

## Task 1: 기존 업체 찾기 (사업자번호 → 업체)

**Files:** Create `js/pu-doc-file.js` · Test `tests/pu-doc-file.test.js`

**Interfaces:**
- `init({ db })`
- `findCompanyByBizNo(bizno): Promise<{id, rec}|null>` — `data/companies/v` 를 훑어 숫자만 비교해 찾는다(표기가 제각각이라 문자열 비교로는 못 찾는다)

- [ ] **Step 1: 실패하는 테스트**

```js
test('사업자번호로 기존 업체를 찾는다 — 하이픈이 달라도', async () => {
  const F = loadFile();
  F.init({ db: fakeDb({ 'data/companies/v': {
    c1: { id: 'c1', name: '가나상사', bizNo: '123-81-20031' },
    c2: { id: 'c2', name: '다라상사', bizNo: '1234567890' } } }) });
  const hit = await F.findCompanyByBizNo('1238120031');
  assert.equal(hit.id, 'c1');
  assert.equal((await F.findCompanyByBizNo('123-45-67890')).id, 'c2');
  assert.equal(await F.findCompanyByBizNo('999-99-99999'), null);
});

test('사업자번호가 없거나 이상하면 찾지 않는다', async () => {
  const F = loadFile();
  F.init({ db: fakeDb({}) });
  assert.equal(await F.findCompanyByBizNo(''), null);
  assert.equal(await F.findCompanyByBizNo(null), null);
});

test('업체 목록이 배열이어도 객체여도 동작한다', async () => {
  // 이 저장소는 두 꼴이 섞여 있다(명함첩의 sendToErp 도 둘 다 대응한다)
  const F = loadFile();
  F.init({ db: fakeDb({ 'data/companies/v': [ { id: 'c1', name: '가나상사', bizNo: '1238120031' } ] }) });
  assert.equal((await F.findCompanyByBizNo('123-81-20031')).id, 'c1');
});
```

- [ ] Step 2~5: 실패 확인 → 구현 → 통과 확인 → 커밋

## Task 2: 빈 칸만 채우는 변경분 만들기

**Interfaces:**
- `fillGaps(existing, incoming): Object` — 기존 레코드에서 **비어 있는 칸에만** 새 값을 넣은 변경분. 채울 것이 없으면 `{}`

- [ ] **Step 1: 실패하는 테스트**

```js
test('빈 칸만 채운다 — 기존 값은 절대 덮지 않는다', () => {
  const F = loadFile();
  const out = F.fillGaps(
    { name: '가나상사', ceo: '김대표', bizNo: '', bizType: '' },
    { name: '가나상사(주)', ceo: '홍길동', bizNo: '123-81-20031', bizType: '제조업' });
  assert.deepEqual({ ...out }, { bizNo: '123-81-20031', bizType: '제조업' });
  assert.ok(!('ceo' in out), '기존 대표자를 덮으려 합니다');
  assert.ok(!('name' in out), '기존 회사명을 덮으려 합니다');
});

test('채울 것이 없으면 빈 변경분 — 쓸데없는 쓰기를 하지 않는다', () => {
  const F = loadFile();
  assert.deepEqual({ ...F.fillGaps({ bizNo: '123-81-20031' }, { bizNo: '123-81-20031' }) }, {});
});

test('공백만 있는 기존 값은 비어 있는 것으로 본다', () => {
  const F = loadFile();
  assert.deepEqual({ ...F.fillGaps({ ceo: '   ' }, { ceo: '홍길동' }) }, { ceo: '홍길동' });
});
```

- [ ] Step 2~5

## Task 3: 명함첩에 넣기 (레코드 + 검색 인덱스 + 중복 판정)

**Interfaces:**
- `findCard(kind, fields): Promise<string|null>` — 중복 판정. 명함은 휴대폰 숫자, 사업자등록증은 사업자번호 숫자
- `putCard(kind, fields, photoId): Promise<{id, created:boolean}>` — 다중 경로 update 한 번으로 `pucards/items/{id}` + `pucards/idx/{id}`

- [ ] **Step 1: 실패하는 테스트** — 아래를 반드시 포함한다

```js
test('명함첩 레코드와 검색 인덱스를 한 번에 쓴다', async () => {
  // 인덱스를 안 쓰면 다른 앱이 그 명함을 못 찾는다
  const F = loadFile(); const db = fakeDb({});
  F.init({ db });
  const r = await F.putCard('card', { name: '홍길동', mobile: '010-1234-5678', company: '가나상사' }, 'photo1');
  assert.equal(db.calls.update.length, 1);
  const keys = Object.keys(db.calls.update[0].u).sort();
  assert.ok(keys.some(k => k.indexOf('pucards/items/') === 0), '레코드를 안 썼습니다');
  assert.ok(keys.some(k => k.indexOf('pucards/idx/') === 0), '검색 인덱스를 안 썼습니다');
});

test('사진과 레코드를 서로 잇는다 — 같은 사진을 두 번 등록하지 않기 위해', async () => {
  const F = loadFile(); const db = fakeDb({});
  F.init({ db });
  await F.putCard('card', { name: '홍길동', mobile: '010-1234-5678' }, 'photo1');
  const rec = Object.values(db.calls.update[0].u).find(v => v && v.name === '홍길동');
  assert.equal(rec.photoId, 'photo1');
});

test('휴대폰이 같은 명함이 이미 있으면 새로 만들지 않는다', async () => { /* findCard 가 그 id 를 준다 */ });
test('사업자번호가 같은 사업자등록증이 이미 있으면 새로 만들지 않는다', async () => { /* 같은 방식 */ });
test('상위 노드를 통째로 쓰지 않는다', async () => {
  // 경로가 pucards/items/{id} 꼴이어야 한다 — pucards 나 pucards/items 로 끝나면 남의 명함이 지워진다
  const F = loadFile(); const db = fakeDb({});
  F.init({ db });
  await F.putCard('card', { name: '홍길동' }, 'p1');
  for (const k of Object.keys(db.calls.update[0].u)) {
    assert.ok(/^pucards\/(items|idx)\/[^/]+$/.test(k), '위험한 경로입니다: ' + k);
  }
});
```

- [ ] Step 2~5

## Task 4: 등록 한 번에 — `fileDoc()`

**Interfaces:**
- `fileDoc(read, photoId): Promise<{done:string[], asked:string|null}>`
  - `read` = 판독 층 결과(+`auto`/`why`)
  - `done` = 사람에게 보여줄 한국어 결과 줄(예: `가나상사 — 사업장 정보의 빈 칸 3개를 채웠습니다`)
  - `asked` = 사람 확인이 필요한 이유(자동이 아니면 여기에 담고 아무것도 쓰지 않는다)

- [ ] **Step 1: 실패하는 테스트** — 반드시 포함

```js
test('자동이 아니면 아무것도 쓰지 않는다', async () => {
  const F = loadFile(); const db = fakeDb({});
  F.init({ db });
  const r = await F.fileDoc({ kind: 'bizreg', fields: { company: '가나상사' }, auto: false, why: '번호를 확인해 주세요' }, 'p1');
  assert.equal(db.calls.update.length, 0, '확인이 필요한데 저장했습니다');
  assert.match(r.asked, /번호/);
});

test('기존 업체를 찾으면 빈 칸만 채우고 무엇을 채웠는지 알려준다', async () => { /* … */ });
test('업체를 못 찾으면 명함첩에만 남기고 업체를 새로 만들지 않는다', async () => {
  // 유령 업체가 쌓이는 것을 막는다
});
test('판독 결과가 없으면 조용히 아무것도 하지 않는다', async () => { /* … */ });
```

- [ ] Step 2~5

## Task 5: 화면에 붙이기

- [ ] 판독이 끝나고 `auto`면 곧바로 `fileDoc()` → 판독 줄에 등록 결과 한 줄 더 (`✓ 사업장 정보의 빈 칸 3개를 채웠습니다`)
- [ ] `auto`가 아니면 등록하지 않고 이유만 (지금과 같음)
- [ ] 크게 보기 패널에 「지금 등록」 버튼 — 사람이 확인한 뒤 손으로 넣는 길
- [ ] 같은 사진을 두 번 등록하지 않는다(`read.filed` 표시)
- [ ] 정적 검사: 화면에 `pucards` 문자열이 없어야 한다(등록 층이 안다)

## Task 6: 총괄 검증 + 기록

- [ ] `node --test tests/*.test.js` 전체 통과
- [ ] 브라우저 시험 — **저장 함수를 가짜로 갈아끼우고** `window.__wrote === 0` 확인 후 보고
- [ ] `fund-erp/STATUS.md` · 설계서 6-0장 갱신
- [ ] PR (base main). 병합은 대표님.

## 나중에 (이 계획 밖)

- **중소기업확인서 유효기간 만료 알림** — 별도 계획. 알림을 어디에 띄울지(업무관리 띠? 기금관리?)부터 정해야 한다.
- **컨설팅 앱 자동 입력** — `scal_cos` 에 사업장 정보 필드가 아예 없다(회사 이름만). 칸 신설이 먼저다.
- **명함첩 → 사업장 신규 생성** — 사람이 확인해 만드는 기존 통로(`sendToErp`)를 그대로 쓴다.
