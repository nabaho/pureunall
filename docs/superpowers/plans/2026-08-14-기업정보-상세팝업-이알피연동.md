# 기업정보 상세 팝업 · 이알피 연동 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 기업정보 회사 상세를 화면 전환 대신 오른쪽 팝업(`#pcDetail` 재사용)으로 바꾸고, 그 팝업에 푸른이알피(`pu-erp.html`)의 컨설팅·사건 이력을 사업자번호로 매칭해 읽기 전용으로 보여주며, 새 폴더를 이알피 사업 유형에서 골라 한꺼번에 만드는 기능을 더한다.

**Architecture:** `pu-cards.html`은 이알피의 Firebase 데이터(`data/cases/v/*`, `data/consultings/v/*`, `data/biz_cons_types`)를 읽기 전용으로 소비한다. 기업정보 폴더는 이알피 원본을 참조만 하고 손대지 않는다 — 회사 열쇠(사업자번호 10자리)로 이어 붙인다. 명함 상세에서 이미 쓰는 "PC 오른쪽 상세 패널" 패턴(`#pcDetail`/`#pcDetailOverlay`/`closeDetail()`/`closePcDetail()`)을 그대로 재사용한다.

**Tech Stack:** 빌드 없는 단일 HTML(`pu-cards.html`) + Firebase Realtime Database. 검사는 `node:test` + `node:assert/strict`, 순수 함수·렌더 함수는 `node:vm`으로 `pu-cards.html`에서 코드 조각을 뽑아 실제로 실행해 증명한다(정적 규칙 검사가 아니다 — 이 세션에서 확립된 필수 관례).

## Global Constraints

- 이알피 원본(`data/cases`, `data/consultings`, `data/biz_cons_types`)을 기업정보 쪽에서 절대 쓰지 않는다 — 읽기만 한다.
- 새 `kind`나 `state.tab` 값을 만들지 않는다.
- 폴더-이알피 실시간 연동을 하지 않는다 — "가져오기"는 그 순간의 스냅샷만 담고, 이후 자동으로 안 따라간다(조용한 배경 쓰기 금지 원칙과 같다).
- 회사 매칭은 `digits(bizno)` 10자리 기준 — `coKeyOf`와 같은 규칙.
- 매칭 안 된 회사는 침묵하지 않는다 — 몇 곳을 건너뛰었는지 안내한다.
- Firebase 다중 경로 쓰기는 `Store.db.ref(DB_ROOT).update(upd)` 하나로 모은다. 여섯 개 기존 쓰기 함수(`putCoFolder` 등)가 이미 하는 것처럼 실패하면 `.catch(e=>toast('...실패: '+((e&&e.message)||'')))`로 안내한다.
- 파일은 UTF-8. 주석과 화면 글은 한국어, 이 저장소 말투(왜 이렇게 했는지)를 따른다.
- 커밋 메시지는 한국어, `feat(기업정보): …` 또는 `test(기업정보): …` 꼴. 특수문자·여러 줄이 있으면 BOM 없는 UTF-8 임시 파일로 만들어 `git commit -F <파일>`을 쓴다.
- 모든 과제 끝에 `node --test tests/*.test.js` 가 `fail 0` 이어야 한다.
- 이 계획서가 다루는 파일은 `pu-cards.html`뿐이다 — `pu-erp.html`은 손대지 않는다(읽기 전용 소비자로만 참여).

---

### Task 1: 회사 상세 — 오른쪽 팝업으로 전환

**Files:**
- Modify: `pu-cards.html`
  - `pickCo(k)` (현재 9557줄), `closePcDetail()`, `renderCoPage()` (현재 9775줄), `coDetailHtml(o)`(현재 9977~9992줄, 삭제하고 대체)
  - `coListHtml(list)`의 행 onclick은 그대로 `pickCo('${esc(o.key)}')`를 쓴다(안 바뀜)
- Test: `tests/cards-co-detail-panel.test.js` (새 파일)

**Interfaces:**
- Consumes (기존 코드): `$`, `esc`, `coList()`, `CO_FIELDS`, `_coFolders`, `coDocsHtml(o)`, `closeDetail()`, `#pcDetail`/`#pcDetailOverlay` DOM, `.pdhead`/`.pdbody`/`.pdname`/`.pdsub`/`.pdgrid`/`.pdrow .k`/`.pdrow .v`/`.pdsec`/`.cof` CSS(모두 이미 있음)
- Produces:
  - `coDetailPanelHtml(o)` → `string` — `#pcDetail`의 `.pdbody` 안에 넣을 내용
  - `openCoDetailPanel(key)` — `#pcDetail`을 채우고 연다
  - `closePcDetail()` — 기존 함수를 고쳐 `state.coPick`도 같이 비운다
  - `pickCo(k)` — 지금과 시그니처는 같지만 동작이 바뀐다(토글 시 패널을 열고 닫음, `renderCoPage()`를 다시 안 부른다)

- [ ] **Step 1: 실패하는 검사를 쓴다**

`tests/cards-co-detail-panel.test.js` 파일을 새로 만든다.

```javascript
/* 회사 상세를 화면 전환이 아니라 오른쪽 팝업(#pcDetail, 명함 상세와 같은 패널)으로
   보여준다. 목록은 그대로 두고 패널만 열고 닫는다(대표 지시 2026-08-14). */
const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');
const vm = require('node:vm');

const source = fs.readFileSync(path.join(__dirname, '..', 'pu-cards.html'), 'utf8');

test('coDetailHtml(예전 화면-전환용 함수)이 없다 — 팝업으로 대체됐다', () => {
  assert.doesNotMatch(source, /function coDetailHtml/, 'coDetailHtml 을 지우지 않고 남겨 두면 죽은 코드가 된다');
});

test('renderCoPage 는 이제 항상 목록만 그린다 — state.coPick 으로 안 갈라진다', () => {
  const at = source.indexOf('function renderCoPage');
  const end = source.indexOf('\nfunction coListHtml', at);
  const fn = source.slice(at, end);
  assert.doesNotMatch(fn, /coDetailHtml/, 'renderCoPage 가 여전히 coDetailHtml 을 부르면 안 된다');
  assert.match(fn, /coListHtml\(list\)/);
});

/* coDetailPanelHtml·openCoDetailPanel·pickCo·closePcDetail 을 실제로 돌려서 증명한다.
   cards-co-col-filter.test.js 와 같은 방식 — 필요한 것만 손으로 쥐여준다. */
function loadPanelBlock(items){
  const pickAt = source.indexOf('function pickCo(');
  const closeAt = source.indexOf('function closePcDetail');
  const closeEnd = source.indexOf('\n', closeAt);
  const panelAt = source.indexOf('function coDetailPanelHtml');
  assert.ok(panelAt > 0, 'coDetailPanelHtml 을 찾지 못했습니다');
  const openAt = source.indexOf('function openCoDetailPanel');
  assert.ok(openAt > panelAt, 'openCoDetailPanel 을 찾지 못했습니다');
  const openEnd = source.indexOf('\nfunction ', openAt + 10);
  const pickEnd = source.indexOf('\n', pickAt);

  const calls = { panelHtml:'', panelOpen:false, overlayOn:false, detailClosed:0 };
  const ctx = {
    esc: s => String(s ?? '').replace(/[&<>"']/g, c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),
    state: { coPick:'' },
    _coFolders: {},
    coList: () => items.slice(),
    coDocsHtml: () => '',
    CO_FIELDS: [['bizno','사업자번호'],['ceo','대표자']],
    closeDetail: () => { calls.detailClosed++; },
    loadErpCaseCons: cb => cb && cb(null),
    $: id => {
      if(id==='pcDetail') return { set innerHTML(v){ calls.panelHtml=v; }, get innerHTML(){ return calls.panelHtml; },
        classList: { add(){ calls.panelOpen=true; }, remove(){ calls.panelOpen=false; } } };
      if(id==='pcDetailOverlay') return { style:{ set display(v){ calls.overlayOn = (v==='block'); } } };
      return null;
    }
  };
  const code = source.slice(panelAt, openEnd) + '\n' + source.slice(pickAt, pickEnd) + '\n' + source.slice(closeAt, closeEnd);
  vm.createContext(ctx);
  vm.runInContext(code, ctx);
  ctx._calls = calls;
  return ctx;
}

test('openCoDetailPanel 은 이 회사 내용으로 패널을 채우고 연다', () => {
  const c = loadPanelBlock([{ key:'k1', name:'나라크라샤', bizno:'123-81-20012', ceo:'강대호', cards:[], extra:{}, folder:'' }]);
  c.openCoDetailPanel('k1');
  assert.match(c._calls.panelHtml, /나라크라샤/);
  assert.match(c._calls.panelHtml, /강대호/);
  assert.equal(c._calls.panelOpen, true);
  assert.equal(c._calls.overlayOn, true);
});

test('pickCo 는 같은 회사를 두 번 누르면 패널을 닫는다', () => {
  const c = loadPanelBlock([{ key:'k1', name:'나라크라샤', bizno:'', ceo:'', cards:[], extra:{}, folder:'' }]);
  c.pickCo('k1');
  assert.equal(c.state.coPick, 'k1');
  c.pickCo('k1');
  assert.equal(c.state.coPick, '', '같은 회사를 다시 누르면 골라둔 것이 풀려야 한다');
  assert.equal(c._calls.detailClosed, 1);
});

test('closePcDetail 은 state.coPick 을 비운다', () => {
  const c = loadPanelBlock([]);
  c.state.coPick = 'k1';
  c.closePcDetail();
  assert.equal(c.state.coPick, '');
  assert.equal(c._calls.detailClosed, 1);
});

test('폴더에 든 회사는 상세 패널에 폴더 딱지가 보인다', () => {
  const c = loadPanelBlock([{ key:'k1', name:'나라크라샤', bizno:'', ceo:'', cards:[], extra:{}, folder:'f1' }]);
  c._coFolders = { f1:{ id:'f1', name:'현장클리닉' } };
  c.openCoDetailPanel('k1');
  assert.match(c._calls.panelHtml, /현장클리닉/);
});
```

- [ ] **Step 2: 검사가 실패하는 것을 확인한다**

```bash
node --test tests/cards-co-detail-panel.test.js
```

Expected: FAIL — `coDetailPanelHtml`/`openCoDetailPanel`을 못 찾아 대부분 실패. (`coDetailHtml`이 없다는 검사와 `renderCoPage`가 그걸 안 부른다는 검사만 아직 통과일 수도, 실패일 수도 있다 — 지금은 아직 안 고쳤으니 실패가 정상이다.)

- [ ] **Step 3: `coDetailHtml`을 `coDetailPanelHtml`로 바꾼다**

현재(9977~9992줄):

```javascript
function coDetailHtml(o){
  const val = f => o.extra[f] || o[f] || '';
  const rows = CO_FIELDS.filter(([f])=>val(f)).map(([f,label])=>
    `<div class="cof"><span>${esc(label)}</span><b>${esc(val(f))}</b></div>`).join('');
  const folder = o.folder ? _coFolders[o.folder] : null;
  return `<div class="codet">
    <div class="h"><button class="setback" onclick="pickCo('')">← 회사 목록</button>
      <b>${esc(o.name)}</b>${o.erp&&o.erp.type?`<span class="ty">${esc(o.erp.type)}</span>`:''}
      ${folder?`<span class="ty" style="background:#fdf6e8;color:#92400e;border-color:#eeddb5">📁 ${esc(folder.name)}</span>`:''}</div>
    ${rows || '<div class="cpnone" style="padding:14px 2px">아직 읽어 둔 항목이 없습니다</div>'}
    ${coDocsHtml(o)}
    ${o.cards.length?`<div class="pcsec" style="margin:16px 4px 8px">이 회사 사람 ${o.cards.length}명</div>
      ${o.cards.map(c=>`<div class="cof" onclick="openDetail('${c.id}')" style="cursor:pointer">
        <span>${esc(c.title||'')}</span><b>${esc(c.name||'')}</b></div>`).join('')}`:''}
  </div>`;
}
```

이렇게 바꾼다(오른쪽 패널의 `.pdname`/`.pdsub`/`.pdgrid`/`.pdrow` 결로 다시 짠다 — 명함 상세 `openPcDetail`과 같은 클래스):

```javascript
/* 회사 상세 — 오른쪽 팝업(#pcDetail, 명함 상세와 같은 패널) 안에 넣을 내용만 만든다.
   목록 화면을 통째로 바꿔 그리던 예전 coDetailHtml 은 없앤다(대표 지시 2026-08-14,
   "화면 전환 대신 오른쪽 팝업으로"). */
function coDetailPanelHtml(o){
  const val = f => o.extra[f] || o[f] || '';
  const rows = CO_FIELDS.filter(([f])=>val(f)).map(([f,label])=>
    `<div class="pdrow"><div class="k">${esc(label)}</div><div class="v">${esc(val(f))}</div></div>`).join('');
  const folder = o.folder ? _coFolders[o.folder] : null;
  return `
    <div class="pdname">${esc(o.name)}${o.erp&&o.erp.type?` <span class="ty">${esc(o.erp.type)}</span>`:''}</div>
    ${folder?`<div class="pdsub">📁 ${esc(folder.name)}</div>`:''}
    <div class="pdgrid" style="margin-top:12px">
      ${rows || '<div class="pdrow"><div class="v" style="color:#8b98b3">아직 읽어 둔 항목이 없습니다</div></div>'}
    </div>
    ${coDocsHtml(o)}
    ${o.cards.length?`<div class="pcsec" style="margin:16px 4px 8px">이 회사 사람 ${o.cards.length}명</div>
      ${o.cards.map(c=>`<div class="cof" onclick="openDetail('${c.id}')" style="cursor:pointer">
        <span>${esc(c.title||'')}</span><b>${esc(c.name||'')}</b></div>`).join('')}`:''}
    <div id="coErpHistBox"></div>`;
}
/* ⚠ #coErpHistBox 는 빈 채로 둔다 — 이알피 이력은 Task 3 에서 loadErpCaseCons 로
   따로 불러온 뒤 이 칸에 채운다(패널을 열 때마다 이알피에 새로 안 물어보려면
   상세 내용과 이력 내용을 분리해 둬야 한다). */
function openCoDetailPanel(key){
  const o = coList().find(x=>x.key===key); if(!o) return;
  $('pcDetail').innerHTML = `
    <div class="pdhead"><span class="ttl">기업정보</span>
      <button class="x" onclick="closePcDetail()">✕</button></div>
    <div class="pdbody">${coDetailPanelHtml(o)}</div>`;
  $('pcDetail').classList.add('open');
  const ov=$('pcDetailOverlay'); if(ov) ov.style.display='block';
  loadErpCaseCons(data=>renderCoErpHistory(o, data));
}
```

`loadErpCaseCons`/`renderCoErpHistory`는 Task 2·3에서 만든다 — 지금은 아직 없으므로, 이 Step 3 시점에는 `openCoDetailPanel` 안의 이 마지막 줄(`loadErpCaseCons(...)`)을 넣으면 `ReferenceError`가 난다. **Task 1 시점에는 이 줄을 아직 넣지 않는다** — 아래처럼 Task 1에서는 그 줄 없이 커밋하고, Task 3에서 그 한 줄을 추가한다.

Task 1에서 실제로 넣을 `openCoDetailPanel`(마지막 줄 없이):

```javascript
function openCoDetailPanel(key){
  const o = coList().find(x=>x.key===key); if(!o) return;
  $('pcDetail').innerHTML = `
    <div class="pdhead"><span class="ttl">기업정보</span>
      <button class="x" onclick="closePcDetail()">✕</button></div>
    <div class="pdbody">${coDetailPanelHtml(o)}</div>`;
  $('pcDetail').classList.add('open');
  const ov=$('pcDetailOverlay'); if(ov) ov.style.display='block';
}
```

- [ ] **Step 4: `pickCo`·`closePcDetail`을 고친다**

현재(9557줄):

```javascript
function pickCo(k){ state.coPick = (state.coPick===k ? '' : k); renderCoPage(); }
```

이렇게 바꾼다:

```javascript
function pickCo(k){
  if(state.coPick===k){ state.coPick=''; closePcDetail(); return; }
  state.coPick = k;
  openCoDetailPanel(k);
}
```

현재:

```javascript
function closeDetail(){ $('detailBg').classList.remove('open');
  const pd=$('pcDetail'); if(pd) pd.classList.remove('open');
  const ov=$('pcDetailOverlay'); if(ov) ov.style.display='none'; }
function closePcDetail(){ closeDetail(); }
```

`closePcDetail`만 이렇게 바꾼다(`closeDetail`은 그대로 — 명함·사업자 상세도 같이 쓰는 공용 함수라 손대지 않는다):

```javascript
function closePcDetail(){ state.coPick=''; closeDetail(); }
```

- [ ] **Step 5: `renderCoPage`를 고친다**

현재(9775~9801줄) 중 `const one = ...` 줄과 마지막 `el.innerHTML` 줄:

```javascript
  const one = state.coPick ? list.find(o=>o.key===state.coPick) : null;
```

이 줄을 지운다(더 이상 안 쓴다).

```javascript
  el.innerHTML = `<div class="setwrap">${one ? coDetailHtml(one) : coListHtml(list)}</div>`;
```

이렇게 바꾼다:

```javascript
  el.innerHTML = `<div class="setwrap">${coListHtml(list)}</div>`;
```

- [ ] **Step 6: 검사가 통과하는 것을 확인한다**

```bash
node --test tests/cards-co-detail-panel.test.js
node --test tests/*.test.js
```

또한 `pu-cards.html`의 모든 `<script>` 블록에 문법 오류가 없는지 확인한다(정규식으로 잘라 `new vm.Script(...)`).

Expected: 새 검사 6개 PASS, 문법 OK, 전체 `fail 0`.

- [ ] **Step 7: 커밋**

```bash
git add pu-cards.html tests/cards-co-detail-panel.test.js
git commit -m "feat(기업정보): 회사 상세를 화면 전환 대신 오른쪽 팝업으로"
```

---

### Task 2: 이알피 컨설팅·사건·유형 캐시 불러오기

**Files:**
- Modify: `pu-cards.html` — `pickCo(k)` 함수 뒤(또는 `loadCoTagHidden` 정의 앞)에 새 캐시 함수들을 넣는다
- Test: `tests/cards-erp-case-cons.test.js` (새 파일)

**Interfaces:**
- Consumes: `digits(s)`(이미 있음), `Store.mode`, `firebase.database()`
- Produces:
  - `let _erpCaseCons = null` — `{ byBiz: { [10자리 사업자번호]: [record, ...] } }` 또는 아직 안 불러왔으면 `null`
  - `let _erpConsTypes = null` — `[{code,short,name,agency,sortOrder}, ...]` (`biz_cons_types` 레지스트리, 실패해도 `[]`)
  - `function loadErpCaseCons(cb)` — `cb(byBizOrNull)`을 부른다. 세션 동안 한 번만 실제로 읽는다(캐시).
  - `function erpConsTypeName(typeCode)` → `string` — 컨설팅 유형 코드를 사람이 읽는 이름으로. 못 찾으면 코드 그대로.

**⚠ 이알피 데이터 모양(직접 `pu-erp.html` 코드를 읽어 확인함, 2026-08-14):**
- `data/cases/v/{id}`: `id, caseNo, typeCode, typeName(이미 저장돼 있음), companyName, bizNo, ceo, address, ..., managerMain, managerSubs[], receiveDate, dueDate, closeDate, retainerFee, successFee, status, brief, sourceContractNo`
- `data/consultings/v/{id}`: `id, no, typeCode(★ typeName 필드 없음 — 코드만 있다), companyName, bizNo, companyId, managerMain, startDate, endDate, fee, contractFee, balanceFee, status, brief, sourceContractNo`
- `data/biz_cons_types`: `[{code:'cons-ilteo', short:'일혁', name:'일터상생혁신', agency:'노사발전재단', sortOrder:10}, ...]` — 컨설팅 유형 코드→이름 사전. **레코드별(`/v/{id}`) 자리가 아니라 통째 배열 자리(`data/biz_cons_types`)다** — 경로가 다르니 헷갈리지 않는다.

- [ ] **Step 1: 실패하는 검사를 쓴다**

`tests/cards-erp-case-cons.test.js` 파일을 새로 만든다.

```javascript
/* 이알피(pu-erp.html)의 사건관리·컨설팅관리 기록을 읽기 전용으로 불러온다.
   ⚠ data/cases/v/{id}, data/consultings/v/{id} 에 있다 — 기업정보가 이미 쓰는
     data/companies(ErpMatch, 업체관리)와는 다른 자리다. 절대 여기에 쓰지 않는다.
   ⚠ data/biz_cons_types 는 레코드별 자리(.../v/{id})가 아니라 통째 배열 자리다 —
     컨설팅 기록엔 typeName 이 없고 typeCode 만 있어서, 사람이 읽는 이름은
     이 사전을 따로 찾아봐야 한다. */
const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');
const vm = require('node:vm');

const source = fs.readFileSync(path.join(__dirname, '..', 'pu-cards.html'), 'utf8');

function loadErpCaseConsBlock(){
  const digitsAt = source.indexOf('const digits = s =>');
  const digitsEnd = source.indexOf('\n', digitsAt);
  const at = source.indexOf('let _erpCaseCons');
  assert.ok(at > 0, '_erpCaseCons 캐시를 찾지 못했습니다');
  const end = source.indexOf('\nfunction erpConsTypeName', at);
  assert.ok(end > at, 'erpConsTypeName 앞까지 자르지 못했습니다');
  const nameAt = source.indexOf('function erpConsTypeName');
  const nameEnd = source.indexOf('\n}', nameAt) + 2;

  const calls = { onceCalls: [] };
  const ctx = {
    Store: { mode:'firebase' },
    firebase: { database: () => ({ ref: p => ({ once: evt => {
      calls.onceCalls.push(p);
      const val = ctx._fixtures[p];
      return Promise.resolve({ val: () => val });
    } }) }) },
    _fixtures: {}
  };
  const code = source.slice(digitsAt, digitsEnd) + '\n' + source.slice(at, end) + '\n' + source.slice(nameAt, nameEnd);
  vm.createContext(ctx);
  vm.runInContext(code, ctx);
  ctx._calls = calls;
  return ctx;
}

test('loadErpCaseCons 는 data/cases/v, data/consultings/v 두 자리만 읽는다', async () => {
  const c = loadErpCaseConsBlock();
  c._fixtures = { 'data/cases/v': { c1:{ id:'c1', bizNo:'123-81-20012', typeName:'부당해고' } },
                  'data/consultings/v': { k1:{ id:'k1', bizNo:'123-81-20012', typeCode:'cons-ilteo' } } };
  let got = null;
  await new Promise(res => c.loadErpCaseCons(data => { got = data; res(); }));
  assert.deepEqual(c._calls.onceCalls.sort(), ['data/cases/v','data/consultings/v']);
  assert.equal(got.byBiz['1238120012'].length, 2);
});

test('사업자번호 10자리 미만인 기록은 색인에서 뺀다', async () => {
  const c = loadErpCaseConsBlock();
  c._fixtures = { 'data/cases/v': { c1:{ id:'c1', bizNo:'123' } }, 'data/consultings/v': {} };
  let got = null;
  await new Promise(res => c.loadErpCaseCons(data => { got = data; res(); }));
  assert.deepEqual(got.byBiz, {});
});

test('한 번 불러온 뒤로는 다시 안 읽고 캐시를 쓴다', async () => {
  const c = loadErpCaseConsBlock();
  c._fixtures = { 'data/cases/v': {}, 'data/consultings/v': {} };
  await new Promise(res => c.loadErpCaseCons(() => res()));
  await new Promise(res => c.loadErpCaseCons(() => res()));
  assert.equal(c._calls.onceCalls.length, 2, '두 번째 부를 때는 실제로 안 읽어야 한다');
});

test('클라우드 모드가 아니면 콜백에 null 을 준다', async () => {
  const c = loadErpCaseConsBlock();
  c.Store.mode = 'demo';
  let got = 'unset';
  await new Promise(res => c.loadErpCaseCons(data => { got = data; res(); }));
  assert.equal(got, null);
  assert.equal(c._calls.onceCalls.length, 0);
});

test('erpConsTypeName 은 등록된 코드를 사람이 읽는 이름으로 바꾼다', () => {
  const c = loadErpCaseConsBlock();
  c._erpConsTypes = [{ code:'cons-ilteo', short:'일혁', name:'일터상생혁신', agency:'노사발전재단', sortOrder:10 }];
  assert.equal(c.erpConsTypeName('cons-ilteo'), '일터상생혁신');
});

test('erpConsTypeName 은 등록 안 된 코드면 코드를 그대로 돌려준다', () => {
  const c = loadErpCaseConsBlock();
  c._erpConsTypes = [];
  assert.equal(c.erpConsTypeName('cons-알수없음'), 'cons-알수없음');
});
```

- [ ] **Step 2: 검사가 실패하는 것을 확인한다**

```bash
node --test tests/cards-erp-case-cons.test.js
```

Expected: FAIL — 모두 실패(`_erpCaseCons`를 찾지 못했습니다 등).

- [ ] **Step 3: 캐시 함수를 만든다**

`function pickCo(k){...}` 정의 **바로 뒤**에 넣는다.

```javascript

/* ── 이알피(사무관리) 컨설팅·사건 기록 — 읽기 전용 ──
   회사 상세 팝업에 이력을 보여주고, 폴더를 이알피 사업 유형에서 골라 만들 때 쓴다
   (대표 지시 2026-08-14). ⚠ data/cases, data/consultings 는 이알피(pu-erp.html)의
   원장이다 — 여기서는 절대 쓰지 않는다, 읽기만 한다.
   ⚠ 사건관리는 레코드마다 typeName 이 이미 박혀 있지만, 컨설팅관리는 typeCode 만
     있고 사람이 읽는 이름은 별도 사전(data/biz_cons_types, 통째 배열 자리 — 레코드별
     자리가 아니다)에서 찾아야 한다. */
let _erpCaseCons = null;
let _erpCaseConsLoading = false;
let _erpConsTypes = null;
function loadErpCaseCons(cb){
  if(_erpCaseCons || _erpCaseConsLoading){ cb && cb(_erpCaseCons); return; }
  if(Store.mode!=='firebase' || typeof firebase==='undefined' || !firebase.database){ cb && cb(null); return; }
  _erpCaseConsLoading = true;
  Promise.all([
    firebase.database().ref('data/cases/v').once('value'),
    firebase.database().ref('data/consultings/v').once('value'),
    firebase.database().ref('data/biz_cons_types').once('value'),
  ]).then(([cs, ks, ts])=>{
    const byBiz = {};
    const push = (rec, kind) => {
      const b = digits(rec.bizNo||''); if(b.length<10) return;
      (byBiz[b] = byBiz[b]||[]).push(Object.assign({}, rec, { _kind: kind }));
    };
    Object.values(cs.val()||{}).forEach(r=>push(r,'case'));
    Object.values(ks.val()||{}).forEach(r=>push(r,'consulting'));
    _erpConsTypes = Array.isArray(ts.val()) ? ts.val() : Object.values(ts.val()||{});
    _erpCaseCons = { byBiz };
    _erpCaseConsLoading = false;
    cb && cb(_erpCaseCons);
  }).catch(e=>{
    _erpCaseConsLoading = false;
    console.warn('[erpCaseCons]', e&&e.message);
    cb && cb(null);
  });
}
function erpConsTypeName(typeCode){
  const t = (_erpConsTypes||[]).find(x=>x && x.code===typeCode);
  return t ? (t.name||typeCode) : typeCode;
}
```

**주의**: `Store.mode!=='firebase'` 가드를 `_erpCaseConsLoading=true`로 세팅하기 **전에** 검사해야 한다 — 순서가 바뀌면 데모 모드에서도 `_erpCaseConsLoading`이 영영 `true`로 남아 나중에 진짜 클라우드로 바뀌어도 다시 안 읽힌다.

- [ ] **Step 4: 검사가 통과하는 것을 확인한다**

```bash
node --test tests/cards-erp-case-cons.test.js
node --test tests/*.test.js
```

또한 문법 검사를 돌린다.

Expected: 새 검사 6개 PASS, 전체 `fail 0`.

- [ ] **Step 5: 커밋**

```bash
git add pu-cards.html tests/cards-erp-case-cons.test.js
git commit -m "feat(기업정보): 이알피 컨설팅·사건·유형 기록을 읽기 전용으로 불러온다"
```

---

### Task 3: 상세 팝업에 이알피 이력 표시

**Files:**
- Modify: `pu-cards.html`
  - `openCoDetailPanel(key)`(Task 1에서 만든 것)에 `loadErpCaseCons(...)` 호출 줄을 추가
  - 새 함수: `renderCoErpHistory(o, data)`, `erpHistCardHtml(rec)`
- Test: `tests/cards-co-erp-history.test.js` (새 파일)

**Interfaces:**
- Consumes (Task 2에서): `_erpCaseCons`(형태는 안 씀, `loadErpCaseCons`의 콜백 인자로만 받음), `erpConsTypeName(code)`
- Consumes (기존 코드): `$`, `esc`, `digits`, `ErpMatch.nameByEmail`(이알피 사번→사람 이름 매핑, 이미 있음)
- Produces:
  - `renderCoErpHistory(o, data)` — `#coErpHistBox`(Task 1이 만든 빈 칸)를 채운다. 기록이 없으면 아무 것도 안 채운다(칸 자체가 안 보임).
  - `erpHistCardHtml(rec)` → `string` — 기록 하나를 카드로 그린다

- [ ] **Step 1: 실패하는 검사를 쓴다**

`tests/cards-co-erp-history.test.js` 파일을 새로 만든다.

```javascript
/* 회사 상세 팝업에 이알피 컨설팅·사건 이력을 사업자번호로 매칭해 읽기 전용으로
   보여준다. 기록이 없으면 칸 자체를 안 그린다(메모 없으면 메모 칸을 안 그리는 것과
   같은 결). */
const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');
const vm = require('node:vm');

const source = fs.readFileSync(path.join(__dirname, '..', 'pu-cards.html'), 'utf8');

function loadHistBlock(){
  const nameAt = source.indexOf('function erpConsTypeName');
  const nameEnd = source.indexOf('\n}', nameAt) + 2;
  const cardAt = source.indexOf('function erpHistCardHtml');
  assert.ok(cardAt > 0, 'erpHistCardHtml 을 찾지 못했습니다');
  const renderAt = source.indexOf('function renderCoErpHistory');
  assert.ok(renderAt > 0, 'renderCoErpHistory 를 찾지 못했습니다');
  const renderEnd = source.indexOf('\nfunction ', source.indexOf('function renderCoErpHistory', renderAt) + 10);
  const code = source.slice(nameAt, nameEnd) + '\n' + source.slice(cardAt, renderEnd);

  const calls = { boxHtml: '' };
  const ctx = {
    esc: s => String(s ?? '').replace(/[&<>"']/g, c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),
    digits: s => String(s||'').replace(/\D/g,''),
    _erpConsTypes: [{ code:'cons-ilteo', name:'일터상생혁신', agency:'노사발전재단' }],
    ErpMatch: { nameByEmail: {} },
    $: id => { if(id==='coErpHistBox') return { set innerHTML(v){ calls.boxHtml=v; }, get innerHTML(){ return calls.boxHtml; } }; return null; }
  };
  vm.createContext(ctx);
  vm.runInContext(code, ctx);
  ctx._calls = calls;
  return ctx;
}

test('기록이 없으면 칸을 비운다', () => {
  const c = loadHistBlock();
  c.renderCoErpHistory({ bizno:'123-81-20012' }, null);
  assert.equal(c._calls.boxHtml, '');
  c.renderCoErpHistory({ bizno:'123-81-20012' }, { byBiz:{} });
  assert.equal(c._calls.boxHtml, '');
});

test('사업자번호로 매칭되는 기록만 카드로 나열한다', () => {
  const c = loadHistBlock();
  const data = { byBiz: { '1238120012': [
    { _kind:'case', typeName:'부당해고 구제신청', status:'pending', brief:'해고 구제 신청', managerMain:'p001' },
    { _kind:'consulting', typeCode:'cons-ilteo', status:'active', fee:1000000, contractFee:1000000, balanceFee:500000, startDate:'2025-03-01', endDate:'2025-06-30', managerMain:'p002' }
  ] } };
  c.renderCoErpHistory({ bizno:'123-81-20012' }, data);
  assert.match(c._calls.boxHtml, /부당해고 구제신청/);
  assert.match(c._calls.boxHtml, /일터상생혁신/, '컨설팅은 typeCode 를 사람이 읽는 이름으로 바꿔 보여줘야 한다');
  assert.match(c._calls.boxHtml, /1,500,000/, '컨설팅금과 잔금을 합쳐 보여줘야 한다');
});

test('다른 회사(다른 사업자번호)의 기록은 안 섞인다', () => {
  const c = loadHistBlock();
  const data = { byBiz: { '9999999999': [{ _kind:'case', typeName:'남의 회사 사건' }] } };
  c.renderCoErpHistory({ bizno:'123-81-20012' }, data);
  assert.equal(c._calls.boxHtml, '');
});

test('erpHistCardHtml 은 담당자 사번을 이름으로 바꿔 보여준다', () => {
  const c = loadHistBlock();
  c.ErpMatch.nameByEmail = { 'p001@pureun.kr': '김혜민' };
  const html = c.erpHistCardHtml({ _kind:'case', typeName:'부당해고', managerMain:'p001' });
  assert.match(html, /김혜민/);
});
```

- [ ] **Step 2: 검사가 실패하는 것을 확인한다**

```bash
node --test tests/cards-co-erp-history.test.js
```

Expected: FAIL — `erpHistCardHtml`/`renderCoErpHistory`를 못 찾아 실패.

- [ ] **Step 3: 표시 함수를 만든다**

`function erpConsTypeName(typeCode){...}` 정의 **바로 뒤**에 넣는다.

```javascript

/* 담당자 사번(managerMain, 예: 'p001')을 사람 이름으로 — user_dir 매핑은 ErpMatch.load()
   가 이미 만들어 둔 nameByEmail(사번@pureun.kr → 이름)을 그대로 쓴다(같은 규칙을
   두 곳에 안 둔다). */
function erpMgrName(sid){
  if(!sid) return '';
  const email = String(sid).toLowerCase().replace(/-/g,'') + '@pureun.kr';
  return ErpMatch.nameByEmail[email] || sid;
}
function erpHistCardHtml(rec){
  const isCase = rec._kind==='case';
  const typeLabel = isCase ? (rec.typeName||'(이름 없음)') : erpConsTypeName(rec.typeCode||'');
  const period = [rec.startDate||rec.receiveDate, rec.endDate||rec.closeDate].filter(Boolean).join(' ~ ');
  const fee = (rec.contractFee||0) + (rec.balanceFee||0) + (rec.retainerFee||0) + (rec.successFee||0) + (rec.fee&&!rec.contractFee ? rec.fee : 0);
  const STATUS_LABEL = { pending:'대기', active:'진행중', closed:'완료', cancelled:'취소' };
  return `<div class="erphist-card">
    <div style="display:flex;justify-content:space-between;align-items:baseline">
      <span style="font-weight:700">${esc(typeLabel)}</span>
      <span class="erphist-badge">${esc(STATUS_LABEL[rec.status]||rec.status||'')}</span>
    </div>
    <div class="erphist-meta">${esc(period)}${rec.managerMain?` · 담당 ${esc(erpMgrName(rec.managerMain))}`:''}</div>
    ${fee?`<div class="erphist-fee">받은 금액 ${fee.toLocaleString()}원</div>`:''}
    ${rec.brief?`<div class="erphist-brief">${esc(rec.brief)}</div>`:''}
  </div>`;
}
function renderCoErpHistory(o, data){
  const box = $('coErpHistBox'); if(!box) return;
  if(!data){ box.innerHTML=''; return; }
  const recs = data.byBiz[digits(o.bizno||'')] || [];
  if(!recs.length){ box.innerHTML=''; return; }
  box.innerHTML = `<div class="pdsec" style="margin-top:16px">이알피 컨설팅·사건 기록</div>
    ${recs.map(erpHistCardHtml).join('')}`;
}
```

- [ ] **Step 4: `openCoDetailPanel`에 이력 불러오기 줄을 더한다**

Task 1에서 만든 현재 코드:

```javascript
function openCoDetailPanel(key){
  const o = coList().find(x=>x.key===key); if(!o) return;
  $('pcDetail').innerHTML = `
    <div class="pdhead"><span class="ttl">기업정보</span>
      <button class="x" onclick="closePcDetail()">✕</button></div>
    <div class="pdbody">${coDetailPanelHtml(o)}</div>`;
  $('pcDetail').classList.add('open');
  const ov=$('pcDetailOverlay'); if(ov) ov.style.display='block';
}
```

이렇게 바꾼다(마지막 줄 추가):

```javascript
function openCoDetailPanel(key){
  const o = coList().find(x=>x.key===key); if(!o) return;
  $('pcDetail').innerHTML = `
    <div class="pdhead"><span class="ttl">기업정보</span>
      <button class="x" onclick="closePcDetail()">✕</button></div>
    <div class="pdbody">${coDetailPanelHtml(o)}</div>`;
  $('pcDetail').classList.add('open');
  const ov=$('pcDetailOverlay'); if(ov) ov.style.display='block';
  loadErpCaseCons(data=>renderCoErpHistory(o, data));
}
```

- [ ] **Step 5: CSS를 넣는다**

`#pcDetail .pdmemo{...}` 줄(1037번째 줄 근처) **바로 뒤**에 넣는다.

```css
.erphist-card{background:#f7f9fc;border:1px solid #eef1f6;border-radius:9px;padding:10px 12px;margin-bottom:8px;font-size:13px}
.erphist-badge{background:#eef3fc;color:#1d4fc4;font-size:10.5px;padding:1px 7px;border-radius:6px}
.erphist-meta{font-size:11.5px;color:#8b98b3;margin-top:4px}
.erphist-fee{font-size:13px;margin-top:6px;font-weight:700}
.erphist-brief{font-size:12.5px;color:#475569;margin-top:4px}
```

- [ ] **Step 6: 검사가 통과하는 것을 확인한다**

```bash
node --test tests/cards-co-erp-history.test.js
node --test tests/*.test.js
```

또한 문법 검사를 돌린다.

Expected: 새 검사 4개 PASS, 전체 `fail 0`.

- [ ] **Step 7: 커밋**

```bash
git add pu-cards.html tests/cards-co-erp-history.test.js
git commit -m "feat(기업정보): 상세 팝업에 이알피 컨설팅·사건 이력을 보여준다"
```

---

### Task 4: "이알피에서 가져오기" — 사업 유형별 목록·일괄 폴더 담기

**Files:**
- Modify: `pu-cards.html` — `putCoFolder` 정의 뒤(또는 `openCoFolderDialog` 정의 앞)에 새 함수들을 넣는다
- Test: `tests/cards-co-erp-import.test.js` (새 파일)

**Interfaces:**
- Consumes (Task 2에서): `loadErpCaseCons(cb)`, `erpConsTypeName(code)`
- Consumes (기존 코드): `coList()`, `_coFolders`, `putCoFolder(folder)`, `uid()`, `digits(s)`, `Store.db`, `DB_ROOT`, `toast`, `$`
- Produces:
  - `coErpFolderCandidates(cb)` — `cb(list)`를 부른다. `list`는 `[{ label, kind, n, recs }]`, `n` 내림차순 정렬.
  - `coImportFolderFromType(label, recs)` → `Promise` — 폴더를 만들거나 이어 담고, 매칭되는 회사를 그 폴더로 옮긴다.

- [ ] **Step 1: 실패하는 검사를 쓴다**

`tests/cards-co-erp-import.test.js` 파일을 새로 만든다.

```javascript
/* 이알피 컨설팅·사건 기록을 사업 유형별로 묶어 보여주고, 하나를 고르면 그 사업을
   한 회사 전부를 한꺼번에 폴더에 담는다(대표 지시 2026-08-14).
   ⚠ 이알피 원본은 안 건드린다 — coInfo/<열쇠>/folder 만 쓴다. */
const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');
const vm = require('node:vm');

const source = fs.readFileSync(path.join(__dirname, '..', 'pu-cards.html'), 'utf8');

function loadImportBlock(items){
  const digitsAt = source.indexOf('const digits = s =>');
  const digitsEnd = source.indexOf('\n', digitsAt);
  const nameAt = source.indexOf('function erpConsTypeName');
  const nameEnd = source.indexOf('\n}', nameAt) + 2;
  const at = source.indexOf('function coErpFolderCandidates');
  assert.ok(at > 0, 'coErpFolderCandidates 를 찾지 못했습니다');
  const end = source.indexOf('\nfunction ', source.indexOf('function coImportFolderFromType', at) + 10);
  const code = source.slice(digitsAt, digitsEnd) + '\n' + source.slice(nameAt, nameEnd) + '\n' + source.slice(at, end);

  const writes = []; const sets = [];
  const ctx = {
    _erpConsTypes: [{ code:'cons-ilteo', name:'일터상생혁신' }],
    loadErpCaseCons: cb => cb(ctx._erpFixture),
    _erpFixture: { byBiz: {} },
    coList: () => items.slice(),
    _coFolders: {},
    uid: () => 'uid_' + Math.random().toString(36).slice(2,8),
    toast: () => {},
    DB_ROOT: 'pucards',
    Store: { db: { ref: p => ({
      update: v => { writes.push({ path:p, v }); return Promise.resolve(); },
      set: v => { sets.push(v); return Promise.resolve(); }
    }) } },
    putCoFolder: f => { ctx._coFolders[f.id] = f; sets.push(f); }
  };
  vm.createContext(ctx);
  vm.runInContext(code, ctx);
  ctx._writes = writes; ctx._sets = sets;
  return ctx;
}

test('coErpFolderCandidates 는 사업 유형별로 묶어 개수와 함께 준다', () => {
  const c = loadImportBlock([]);
  c._erpFixture = { byBiz: {
    '1238120012':[{ _kind:'consulting', typeCode:'cons-ilteo', bizNo:'123-81-20012' }],
    '1238120411':[{ _kind:'consulting', typeCode:'cons-ilteo', bizNo:'123-81-20411' }],
    '1230720425':[{ _kind:'case', typeName:'부당해고', bizNo:'123-07-20425' }]
  } };
  let got = null;
  c.coErpFolderCandidates(list => { got = list; });
  assert.equal(got.length, 2);
  const ilteo = got.find(x=>x.label.indexOf('일터상생혁신')>=0);
  assert.equal(ilteo.n, 2);
  assert.equal(ilteo.kind, 'consulting');
  const dismiss = got.find(x=>x.label.indexOf('부당해고')>=0);
  assert.equal(dismiss.n, 1);
  assert.equal(dismiss.kind, 'case');
});

test('coImportFolderFromType 은 새 폴더를 만들고 매칭되는 회사를 담는다', async () => {
  const items = [
    { key:'1238120012', name:'나라크라샤', bizno:'123-81-20012' },
    { key:'n엉뚱회사', name:'엉뚱회사', bizno:'' }
  ];
  const c = loadImportBlock(items);
  const recs = [{ _kind:'consulting', typeCode:'cons-ilteo', bizNo:'123-81-20012' }];
  await c.coImportFolderFromType('컨설팅·일터상생혁신', recs);
  assert.equal(Object.keys(c._coFolders).length, 1);
  const folder = Object.values(c._coFolders)[0];
  assert.equal(folder.name, '일터상생혁신');
  const upd = c._writes[0].v;
  assert.equal(upd['coInfo/1238120012/folder'], folder.id);
});

test('이미 같은 이름의 폴더가 있으면 새로 안 만들고 이어 담는다', async () => {
  const items = [{ key:'1238120012', name:'나라크라샤', bizno:'123-81-20012' }];
  const c = loadImportBlock(items);
  c._coFolders = { f9:{ id:'f9', name:'일터상생혁신' } };
  await c.coImportFolderFromType('컨설팅·일터상생혁신', [{ _kind:'consulting', typeCode:'cons-ilteo', bizNo:'123-81-20012' }]);
  assert.equal(Object.keys(c._coFolders).length, 1, '새 폴더를 또 만들면 안 된다');
  assert.equal(c._writes[0].v['coInfo/1238120012/folder'], 'f9');
});

test('사업자번호로 못 찾은 회사는 건너뛴다', async () => {
  const items = [{ key:'1238120012', name:'나라크라샤', bizno:'123-81-20012' }];
  const c = loadImportBlock(items);
  const recs = [
    { _kind:'consulting', typeCode:'cons-ilteo', bizNo:'123-81-20012' },
    { _kind:'consulting', typeCode:'cons-ilteo', bizNo:'999-99-99999' }
  ];
  await c.coImportFolderFromType('컨설팅·일터상생혁신', recs);
  const upd = c._writes[0].v;
  assert.equal(Object.keys(upd).length, 1, '매칭 안 된 회사까지 upd 에 끼면 안 된다');
});

test('하나도 못 찾으면 update 를 안 부른다', async () => {
  const c = loadImportBlock([]);
  await c.coImportFolderFromType('컨설팅·일터상생혁신', [{ _kind:'consulting', typeCode:'cons-ilteo', bizNo:'999-99-99999' }]);
  assert.equal(c._writes.length, 0);
});
```

- [ ] **Step 2: 검사가 실패하는 것을 확인한다**

```bash
node --test tests/cards-co-erp-import.test.js
```

Expected: FAIL — 모두 실패.

- [ ] **Step 3: 목록·가져오기 함수를 만든다**

`function putCoFolder(folder){...}` 정의 **바로 뒤**에 넣는다.

```javascript

/* ── "이알피에서 가져오기" — 사업 유형별로 회사를 한꺼번에 폴더에 담는다 ──
   (대표 지시 2026-08-14) 폴더-이알피 실시간 연동은 안 한다 — 그 순간의 스냅샷만
   담는다. 회사는 사업자번호(10자리)로 매칭한다 — 기업정보에 실제로 있는(명함·
   사업자등록증) 회사만 담고, 없으면 건너뛰고 안내한다. */
function coErpFolderCandidates(cb){
  loadErpCaseCons(data=>{
    if(!data){ cb([]); return; }
    const byType = {};
    Object.values(data.byBiz).forEach(recs=>recs.forEach(rec=>{
      const label = (rec._kind==='case' ? '사건·' : '컨설팅·') +
        (rec._kind==='case' ? (rec.typeName||'(이름 없음)') : erpConsTypeName(rec.typeCode||'(이름 없음)'));
      (byType[label] = byType[label]||[]).push(rec);
    }));
    const list = Object.keys(byType).map(label=>({
      label, kind: byType[label][0]._kind, n: byType[label].length, recs: byType[label]
    })).sort((a,b)=>b.n-a.n);
    cb(list);
  });
}
async function coImportFolderFromType(label, recs){
  const name = label.replace(/^(사건·|컨설팅·)/,'');
  const existing = Object.values(_coFolders).find(f=>f.name===name);
  const folderId = existing ? existing.id : uid();
  if(!existing){
    let by=''; try{ by=(firebase.auth().currentUser||{}).email||''; }catch(_){}
    putCoFolder({ id:folderId, name, order:Date.now(), at:Date.now(), by });
  }

  const byKey = {}; coList().forEach(o=>{ const b=digits(o.bizno||''); if(b.length>=10) byKey[b]=o; });
  const upd = {}; let matched=0, skipped=0;
  recs.forEach(rec=>{
    const b = digits(rec.bizNo||'');
    const co = byKey[b];
    if(!co){ skipped++; return; }
    matched++;
    upd['coInfo/'+co.key+'/folder'] = folderId;
  });
  if(!matched){ toast(`「${name}」 사업에서 담을 회사를 찾지 못했습니다`); return; }
  try{
    await Store.db.ref(DB_ROOT).update(upd);
  }catch(e){
    return toast('가져오기 실패: '+((e&&e.message)||''));
  }
  toast(`${matched}곳을 「${name}」 폴더에 담았습니다` + (skipped?` · ${skipped}곳은 명함·사업자등록증이 없어 건너뛰었습니다`:''));
  if(state.view==='co') renderPC();
}
```

- [ ] **Step 4: 검사가 통과하는 것을 확인한다**

```bash
node --test tests/cards-co-erp-import.test.js
node --test tests/*.test.js
```

또한 문법 검사를 돌린다.

Expected: 새 검사 5개 PASS, 전체 `fail 0`.

- [ ] **Step 5: 되돌려서 검사가 실제로 잡는지 확인한다(뮤테이션 검사)**

`coImportFolderFromType`의 `if(!co){ skipped++; return; }` 줄을 잠깐 지워 "사업자번호로 못 찾은 회사는 건너뛴다" 검사가 실패하는지 확인한 뒤 되돌린다. `if(!matched){...return;}` 줄을 지워 "하나도 못 찾으면 update 를 안 부른다" 검사가 실패하는지도 확인한 뒤 되돌린다. 두 뮤테이션 모두 원본으로 정확히 되돌렸는지 diff로 확인한다.

- [ ] **Step 6: 커밋**

```bash
git add pu-cards.html tests/cards-co-erp-import.test.js
git commit -m "feat(기업정보): 이알피 사업 유형에서 폴더를 한꺼번에 만든다"
```

---

### Task 5: "새 폴더 만들기" 창에 "이알피에서 가져오기" 탭 추가

**Files:**
- Modify: `pu-cards.html` — `openCoFolderDialog()` 함수를 고친다
- Test: `tests/cards-co-erp-import-dialog.test.js` (새 파일)

**Interfaces:**
- Consumes (Task 4에서): `coErpFolderCandidates(cb)`, `coImportFolderFromType(label, recs)`
- Consumes (기존 코드): `$`, `esc`, `confirmNewCoFolder()`
- Produces:
  - `openCoFolderDialog()` — 이제 탭 두 개(이름으로 만들기/이알피에서 가져오기)를 보여준다
  - `switchCoFolderDialogTab(tab)` — `'name'` 또는 `'erp'`
  - `_coErpImportCache` — 마지막으로 불러온 후보 목록(탭을 다시 눌러도 안 다시 불러오게)

- [ ] **Step 1: 실패하는 검사를 쓴다**

`tests/cards-co-erp-import-dialog.test.js` 파일을 새로 만든다.

```javascript
/* "새 폴더 만들기" 창에 "이름으로 만들기"·"이알피에서 가져오기" 두 갈래를 둔다. */
const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');
const vm = require('node:vm');

const source = fs.readFileSync(path.join(__dirname, '..', 'pu-cards.html'), 'utf8');

function loadDialogBlock(){
  const at = source.indexOf('function openCoFolderDialog');
  assert.ok(at > 0, 'openCoFolderDialog 를 찾지 못했습니다');
  const end = source.indexOf('\nfunction confirmNewCoFolder', at);
  const code = source.slice(at, end);

  const calls = { dlgHtml:'', dlgOpen:false };
  const ctx = {
    esc: s => String(s ?? ''),
    coErpFolderCandidates: cb => cb(ctx._erpList || []),
    coImportFolderFromType: () => {},
    $: id => {
      if(id==='folderDlg') return { set innerHTML(v){ calls.dlgHtml=v; }, get innerHTML(){ return calls.dlgHtml; } };
      if(id==='folderDlgBg') return { classList:{ add(){ calls.dlgOpen=true; }, remove(){ calls.dlgOpen=false; } } };
      if(id==='newCoFolderName') return { focus(){} };
      return null;
    },
    setTimeout: f => f()
  };
  vm.createContext(ctx);
  vm.runInContext(code, ctx);
  ctx._calls = calls;
  return ctx;
}

test('처음 열면 이름으로 만들기 탭이 보인다', () => {
  const c = loadDialogBlock();
  c.openCoFolderDialog();
  assert.match(c._calls.dlgHtml, /id="newCoFolderName"/);
  assert.equal(c._calls.dlgOpen, true);
});

test('이알피에서 가져오기 탭으로 바꾸면 사업 유형 목록이 보인다', () => {
  const c = loadDialogBlock();
  c._erpList = [{ label:'컨설팅·일터상생혁신', kind:'consulting', n:8, recs:[] }];
  c.openCoFolderDialog();
  c.switchCoFolderDialogTab('erp');
  assert.match(c._calls.dlgHtml, /일터상생혁신/);
  assert.match(c._calls.dlgHtml, /8곳/);
});

test('두 탭 모두 있다', () => {
  const at = source.indexOf('function openCoFolderDialog');
  const end = source.indexOf('\nfunction confirmNewCoFolder', at);
  const fn = source.slice(at, end);
  assert.match(fn, /switchCoFolderDialogTab\('name'\)/);
  assert.match(fn, /switchCoFolderDialogTab\('erp'\)/);
});
```

- [ ] **Step 2: 검사가 실패하는 것을 확인한다**

```bash
node --test tests/cards-co-erp-import-dialog.test.js
```

Expected: FAIL — `switchCoFolderDialogTab`이 없어 대부분 실패.

- [ ] **Step 3: `openCoFolderDialog`를 고친다**

현재:

```javascript
function openCoFolderDialog(){
  $('folderDlg').innerHTML = `
    <div class="mhead"><b>새 폴더 만들기</b>
      <button class="iconbtn" onclick="folderDlgBg.classList.remove('open')">✕</button></div>
    <div class="field"><input id="newCoFolderName" placeholder="폴더 이름 (예: 컨설팅 신청)"
       onkeydown="if(event.key==='Enter')confirmNewCoFolder()"></div>
    <div class="mfoot">
      <button class="btn primary" onclick="confirmNewCoFolder()">만들기</button>
      <button class="btn ghost" onclick="folderDlgBg.classList.remove('open')">취소</button></div>`;
  $('folderDlgBg').classList.add('open');
  setTimeout(()=>{ const i=$('newCoFolderName'); if(i) i.focus(); }, 50);
}
```

이렇게 바꾼다:

```javascript
let _coErpImportCache = null;
function openCoFolderDialog(){
  _coErpImportCache = null;
  renderCoFolderDialogTab('name');
  $('folderDlgBg').classList.add('open');
  setTimeout(()=>{ const i=$('newCoFolderName'); if(i) i.focus(); }, 50);
}
function switchCoFolderDialogTab(tab){ renderCoFolderDialogTab(tab); }
function renderCoFolderDialogTab(tab){
  const tabsHtml = `<div style="display:flex;gap:6px;padding:2px 0 10px">
      <span class="cofdtab ${tab==='name'?'on':''}" onclick="switchCoFolderDialogTab('name')">이름으로 만들기</span>
      <span class="cofdtab ${tab==='erp'?'on':''}" onclick="switchCoFolderDialogTab('erp')">이알피에서 가져오기</span>
    </div>`;
  const body = tab==='erp' ? coErpImportTabHtml() : `
    <div class="field"><input id="newCoFolderName" placeholder="폴더 이름 (예: 컨설팅 신청)"
       onkeydown="if(event.key==='Enter')confirmNewCoFolder()"></div>
    <div class="mfoot">
      <button class="btn primary" onclick="confirmNewCoFolder()">만들기</button>
      <button class="btn ghost" onclick="folderDlgBg.classList.remove('open')">취소</button></div>`;
  $('folderDlg').innerHTML = `
    <div class="mhead"><b>새 폴더 만들기</b>
      <button class="iconbtn" onclick="folderDlgBg.classList.remove('open')">✕</button></div>
    ${tabsHtml}${body}`;
  if(tab==='erp' && !_coErpImportCache){
    coErpFolderCandidates(list=>{ _coErpImportCache = list; renderCoFolderDialogTab('erp'); });
  }
}
function coErpImportTabHtml(){
  if(_coErpImportCache===null) return `<p style="font-size:12.5px;color:#8b98b3;padding:8px 2px">불러오는 중…</p>`;
  if(!_coErpImportCache.length) return `<p style="font-size:12.5px;color:#8b98b3;padding:8px 2px">이알피에 컨설팅·사건 기록이 없습니다</p>`;
  return `<p style="font-size:12px;color:#8b98b3;margin:0 0 10px">사업을 고르면 그 사업을 한 회사 전부가 폴더에 담깁니다.</p>
    <div style="max-height:46vh;overflow:auto;display:flex;flex-direction:column;gap:6px">
    ${_coErpImportCache.map(x=>`<div class="erpimport-row" onclick="coImportFolderFromType('${esc(x.label.replace(/'/g,"\\'"))}', _coErpImportCache.find(y=>y.label==='${esc(x.label.replace(/'/g,"\\'"))}').recs);folderDlgBg.classList.remove('open')">
        <div><b>${esc(x.label.replace(/^(사건·|컨설팅·)/,''))}</b>
          <div style="font-size:11.5px;color:#8b98b3;margin-top:2px">이알피 ${x.kind==='case'?'사건관리':'컨설팅관리'} · ${x.n}곳</div></div>
        <span style="color:#1d4fc4;font-size:12px;font-weight:700">가져오기</span>
      </div>`).join('')}
    </div>
    <p style="font-size:11.5px;color:#8b98b3;margin-top:12px">사업자번호로 못 찾은 회사는 안내와 함께 건너뜁니다.</p>`;
}
```

**주의**: 위 코드는 `esc(x.label.replace(/'/g,"\\'"))` 순서다 — 어퍼스트로피를 먼저 바꾼 raw 문자열에 `esc()`를 나중에 씌운다. 순서를 반대로(`esc(x.label).replace(...)`) 쓰면 `esc()`가 이미 `'`를 `&#39;`로 바꿔 버린 뒤라 `.replace`가 아무것도 못 잡는 죽은 코드가 된다 — 최종 전체 리뷰(2026-08-14)에서 실제로 잡았던 결함과 같은 함정이니 순서를 그대로 지킨다. (사업 유형 이름은 이알피 등록 유형에서 오므로 실제로 어퍼스트로피가 낄 일은 드물지만, 이 순서를 지키는 데 비용이 들지 않으므로 그대로 따른다.)

- [ ] **Step 4: CSS를 넣는다**

`.coselbar button:hover{...}` 줄(Task 3 이전 기존 CSS) **바로 뒤**에 넣는다.

```css
.cofdtab{font-size:12.5px;color:#8b98b3;padding:6px 12px;border-radius:8px;cursor:pointer}
.cofdtab.on{font-weight:700;color:#1d4fc4;background:#eef3fc}
.erpimport-row{display:flex;align-items:center;justify-content:space-between;padding:11px 12px;background:#f7f9fc;border-radius:9px;cursor:pointer}
.erpimport-row:hover{background:#eef3fc}
```

- [ ] **Step 5: 검사가 통과하는 것을 확인한다**

```bash
node --test tests/cards-co-erp-import-dialog.test.js
node --test tests/*.test.js
```

또한 문법 검사를 돌린다.

Expected: 새 검사 3개 PASS, 전체 `fail 0`.

- [ ] **Step 6: 커밋**

```bash
git add pu-cards.html tests/cards-co-erp-import-dialog.test.js
git commit -m "feat(기업정보): 새 폴더 만들기 창에 이알피에서 가져오기 탭 추가"
```

---

### Task 6: 실제 화면으로 끝까지 확인한다

**Files:**
- 없음(코드 변경 없음). 로컬 서버 + 브라우저로 확인만 한다.

**Interfaces:**
- Consumes: Task 1~5의 모든 결과물

- [ ] **Step 1: 로컬 서버를 띄운다**

```bash
python -m http.server 8869
```

- [ ] **Step 2: 브라우저에서 실제 데이터로 흐름을 돌린다**

⚠ **이 워크트리는 실제 운영 Firebase 설정을 그대로 담고 있다.** 로그인 없이 화면만 열면 App Check·인증 규칙에 막혀 모든 읽기·쓰기가 `permission_denied`로 실패한다(2026-08-14 세션에서 실제로 이 상태의 헤드리스 접속이 App Check 를 24시간 동안 잠그는 사고로 이어졌다). 아래 시나리오는 **실제 Firebase 쓰기를 하지 않고** `state.items`/`_coInfo`/`_erpCaseCons` 등을 직접 손으로 채워 넣어 화면 로직만 확인한다 — Task 1~5의 유닛 검사가 이미 함수 자체는 실제로 실행해 증명했으므로, 여기서는 화면 전체가 맞물려 도는지만 본다.

```javascript
['cardsLogin','appbar','tabs','subbar','main','fab'].forEach(id=>{ const el=document.getElementById(id); if(el) el.style.display='none'; });
document.getElementById('pcRoot').hidden = false;
state.items = {
  b1:{id:'b1',kind:'biz',company:'나라크라샤',bizno:'123-81-20012',ceo:'강대호',createdAt:Date.now()},
  c1:{id:'c1',kind:'card',name:'강대호',company:'나라크라샤',createdAt:Date.now()}
};
state.groups={}; ErpMatch.ready=true; ErpMatch.byBiz={}; ErpMatch.byName={}; ErpMatch.nameByEmail={'p001@pureun.kr':'김혜민'};
_coInfo = {};
_erpCaseCons = { byBiz: { '1238120012': [
  { _kind:'consulting', typeCode:'cons-ilteo', bizNo:'123-81-20012', status:'active', startDate:'2025-03-02', endDate:'2025-06-30', contractFee:2000000, balanceFee:1200000, managerMain:'p001', brief:'근무시간·임금체계 개편 컨설팅' }
] } };
_erpConsTypes = [{ code:'cons-ilteo', name:'일터상생혁신' }];
openCoPage();
```

확인할 것:

1. `나라크라샤`를 누르면 목록이 그대로 남은 채 오른쪽에서 패널이 슬라이드로 열린다.
2. 패널 안에 "이알피 컨설팅·사건 기록"이 보이고 — 유형 "일터상생혁신"(코드가 이름으로 바뀜), 기간 2025-03-02~2025-06-30, 담당 "김혜민"(사번이 이름으로 바뀜), 받은 금액 3,200,000원(계약금+잔금 합), 수행내용이 나온다.
3. 패널 바깥을 누르거나 ✕를 누르면 닫히고 목록은 그대로다.
4. "+ 새 폴더"를 눌러 "이알피에서 가져오기" 탭으로 바꾸면 "일터상생혁신 · 1곳"이 보인다.
5. 그걸 누르면(실제 Firebase 쓰기는 막히므로, `coImportFolderFromType`이 만드는 `upd` 값을 콘솔에서 직접 확인 — `_coInfo['1238120012']={folder:'그 폴더id'}`를 손으로 흉내내고 `renderPC()`를 불러) 옆줄에 "일터상생혁신" 폴더가 생기고 1곳이 담긴 것처럼 보이는지 확인한다.

- [ ] **Step 3: 전체 검사를 마지막으로 한 번 더 돌린다**

```bash
node --test tests/*.test.js
```

Expected: `fail 0`

- [ ] **Step 4: 서버를 내리고 정리한다**

브라우저 콘솔에서 확인한 내용을 간단히 기록만 하고(코드 변경 없음), 로컬 서버 프로세스를 종료한다.

---

## 이 계획이 다루지 않는 것

설계서에 이미 "안 하기로 한 것"으로 적혀 있다.

- **이알피 기록을 기업정보에서 고치거나 지우지 않는다** — 읽기 전용.
- **폴더-이알피 실시간 연동** — "가져오기"는 스냅샷 한 번뿐, 자동으로 안 따라간다.
- **`data/cases`·`data/consultings`를 실시간 구독** — 필요할 때만 그때 읽고 세션 동안 캐시한다.
- **사업자번호가 없는 회사를 기업정보 쪽에서 새로 만들기** — 이알피에 있어도 기업정보(명함·사업자등록증)에 없으면 건너뛰고 안내만 한다.
