# kcareer 실적 동기화 구현계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** pu-erp의 종료된 업무 4종을 담당노무사와 함께 kcareer 실적으로 자동 동기화하고, 수행기관 유무로 푸른 자체 실적과 외부기관 실적을 나누어 관리한다.

**Architecture:** 종료 판별·필드 매핑·병합 계획은 순수 모듈 `js/kcareer-pusync.js`(브라우저·Node 겸용)로 분리해 node 테스트로 검증한다. kcareer.html은 Firebase 읽기·미리보기 모달·저장·배제 UI·외부기관 탭만 담당한다. 새 스토어 없이 기존 `case`·`consult`·`fund`·`etc`에 `puRef`·`src`·`syncId`·`excluded` 꼬리표만 추가한다.

**Tech Stack:** 순수 ES5 JS(빌드 없음), Firebase RTDB compat 10.13.2(`data/*` 읽기 전용), `node:test` + `node:assert/strict`

**설계서:** `docs/superpowers/specs/2026-08-01-kcareer-실적동기화-design.md`

## Global Constraints

- **pu-erp `data/`는 읽기만.** 쓰기는 `kcareer/` 노드(기존 `set()` 경유)만.
- **동기화는 추가만 한다.** 기존 레코드를 덮어쓰거나 지우지 않는다(수동 수정 보존).
- **배제는 삭제가 아니라 `excluded:true`.** 삭제하면 재동기화 때 되살아난다.
- **종료 판별**: `closedDate`가 있거나 `status`가 `closed`/`done`/`완료`/`종료`일 때만. **`endDate`만 있으면 미종료**(예정일일 수 있음).
- 반복문 안에서 `set()` 금지 — 스토어별 단일 쓰기(기존 `fsCommitScan` 규칙, 정적 테스트 있음).
- **환경설정으로 안내하는 문구 금지** — 비관리자에게 숨겨져 막다른 길이 된다(회귀테스트 있음).
- `kcareer.html`·신규 파일 외 건드리지 않는다. 매 태스크 끝 즉시 커밋, push 전 `git pull --rebase origin main`.
- 한국어 주석. 커밋 메시지는 한 줄 또는 `git commit -F <파일>`.

---

## File Structure

| 파일 | 역할 |
|---|---|
| `js/kcareer-pusync.js` (신규) | 순수 로직: `isClosed`·`mapRecord`·`buildSyncPlan`. DOM·Firebase 미사용 |
| `tests/kcareer-pusync.test.js` (신규) | 위 모듈 단위테스트 |
| `kcareer.html` (수정) | 모듈 로드, 동기화 실행·미리보기·저장·되돌리기, 4탭 필터, 배제 UI, 외부기관 탭, 원본 보기 팝업 |
| `tests/kcareer-html.test.js` (수정) | 정적 검사 추가 |

## 코드 사실 (2026-08-01 확인)

- `CAREER_CFG`의 `case`(3529)·`consult`(3516)·`fund`(3535)·`etc`(3541)에는 `filter`가 없다. `renderCareer`(3568 부근)는 `if(cfg.filter) data=data.filter(cfg.filter)`를 이미 지원한다.
- ID 접두사(FORM_DEFS): case=`CS`, consult=`CN`, fund=`FD`, etc=`ET`. 기존 `nextId(prefix,store)`는 localStorage를 직접 보므로 묶음 저장 때는 버퍼 기준 생성기를 쓴다(`fsCommitScan`의 `_bufIdSeq`와 같은 이유).
- 주담당: `_puMainMgr(c)` = `managerMain` → `workers`의 `isPrimary` → 첫 번째. 이름 변환 `_puUserMap`(`await _puLoadUserMap()`).
- 로그인 훅: 4958행 부근 `if(u){ authGate(false); ... resolveMe() ... }` — 여기에 자동 동기화를 단다.
- `rowActions(id,store,label,page,rec)`는 이미 `rec`를 받는다(fs 원본 분기용).
- 모달 구조: `.modal-ov` › `.modal` › `.modal-h`/`.modal-b`/`.modal-foot` (`modalScan` 참조).

---

## Task 1: 순수 모듈 — 종료 판별과 필드 매핑

**Files:**
- Create: `js/kcareer-pusync.js`
- Test: `tests/kcareer-pusync.test.js`

**Interfaces:**
- Produces:
  - `KcareerPuSync.isClosed(c) → boolean`
  - `KcareerPuSync.mapRecord(coll, key, c, userMap) → { store, rec } | null`
    - `coll`: `'cases'|'consultings'|'funds'|'other_projects'`, 그 외는 `null`
    - `rec`: `{ type, org, project, year, main, status:'완료', puRef:'<coll>/<key>' }` (id·src·syncId는 저장 단계에서 붙임)
  - 브라우저 `window.KcareerPuSync` / Node `module.exports`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/kcareer-pusync.test.js` 생성:

```js
'use strict';
// pu-erp 실적 동기화 순수 모듈 단위테스트 — 실행: node --test tests/kcareer-pusync.test.js
const test = require('node:test');
const assert = require('node:assert/strict');
const PS = require('../js/kcareer-pusync.js');

test('isClosed: closedDate 또는 종료 status만 종료다', () => {
  assert.equal(PS.isClosed({ closedDate: '2026-03-01' }), true);
  assert.equal(PS.isClosed({ status: 'closed' }), true);
  assert.equal(PS.isClosed({ status: 'done' }), true);
  assert.equal(PS.isClosed({ status: '완료' }), true);
  assert.equal(PS.isClosed({ status: '종료' }), true);
  assert.equal(PS.isClosed({ status: 'CLOSED' }), true);          // 대소문자 무시
});

test('isClosed: endDate만 있으면 미종료다 — 예정일일 수 있다', () => {
  assert.equal(PS.isClosed({ endDate: '2026-12-31' }), false);
  assert.equal(PS.isClosed({ status: 'active' }), false);
  assert.equal(PS.isClosed({ status: 'progress' }), false);
  assert.equal(PS.isClosed({ status: 'open', endDate: '2026-01-01' }), false);
  assert.equal(PS.isClosed({}), false);
  assert.equal(PS.isClosed(null), false);
});

const UMAP = { '2001': '권형하', '2003': '박한별' };

test('mapRecord: cases → case 스토어', () => {
  const r = PS.mapRecord('cases', '-Nx1', {
    caseType: '부당해고', companyName: '나루토건', title: '부당해고 구제신청',
    closedDate: '2026-03-15', managerMain: '2001'
  }, UMAP);
  assert.equal(r.store, 'case');
  assert.deepEqual(r.rec, {
    type: '부당해고', org: '나루토건', project: '부당해고 구제신청',
    year: '2026', main: '권형하', status: '완료', puRef: 'cases/-Nx1'
  });
});

test('mapRecord: consultings → consult, funds → fund, other_projects → etc', () => {
  const c = PS.mapRecord('consultings', '-Nc1', {
    consultingType: '일터혁신', companyName: '열음폴리텍', programName: '임금체계 재설계',
    closedDate: '2025-11-30', workers: [{ sid: '2003', isPrimary: true }]
  }, UMAP);
  assert.equal(c.store, 'consult');
  assert.equal(c.rec.type, '일터혁신');
  assert.equal(c.rec.main, '박한별');            // workers의 isPrimary에서
  assert.equal(c.rec.puRef, 'consultings/-Nc1');

  const f = PS.mapRecord('funds', '-Nf1', {
    fundType: '사내근로복지기금', companyName: '소담', title: '설립 컨설팅',
    status: 'done', endDate: '2026-01-10', managerMain: '2001'
  }, UMAP);
  assert.equal(f.store, 'fund');
  assert.equal(f.rec.year, '2026');              // closedDate 없으면 endDate에서

  const e = PS.mapRecord('other_projects', '-No1', {
    programName: '재기컨설팅', companyName: '중진공', status: '완료', managerMain: '9999'
  }, UMAP);
  assert.equal(e.store, 'etc');
  assert.equal(e.rec.main, '9999');              // 변환표에 없으면 sid 그대로
  assert.equal(e.rec.year, '');                  // 날짜가 아예 없으면 빈 값
});

test('mapRecord: 모르는 컬렉션은 null', () => {
  assert.equal(PS.mapRecord('unknown', 'k', {}, {}), null);
});
```

- [ ] **Step 2: 실패 확인**

```bash
node --test tests/kcareer-pusync.test.js
```

기대: `Cannot find module '../js/kcareer-pusync.js'`

- [ ] **Step 3: 최소 구현**

`js/kcareer-pusync.js` 생성:

```js
'use strict';
// 푸른노무법인 경력관리 — pu-erp 실적 동기화 순수 모듈
// (브라우저 window.KcareerPuSync / Node module.exports 겸용, DOM·Firebase 미사용)
// 설계서: docs/superpowers/specs/2026-08-01-kcareer-실적동기화-design.md
(function (root) {

  /* ===== 종료 판별 =====
     pu-erp 상태값은 closed·done·완료가 혼재하고 UI 상태(active·saving)와 필드명이 같다.
     ⚠ endDate만 있는 건은 예정일일 수 있으므로 미종료로 본다(기존 화면의 "(예정)" 해석과 동일). */
  var CLOSED_STATUS = ['closed', 'done', '완료', '종료'];
  function isClosed(c) {
    if (!c) return false;
    if (c.closedDate) return true;
    var s = String(c.status || '').toLowerCase();
    return CLOSED_STATUS.indexOf(s) >= 0;
  }

  /* 주담당 sid: managerMain → workers의 isPrimary → 첫 번째 (kcareer._puMainMgr와 동일 규칙) */
  function mainSid(c) {
    var sid = c.managerMain || '';
    if (!sid && Array.isArray(c.workers)) {
      var w = c.workers.find(function (x) { return x && x.isPrimary; }) || c.workers[0];
      if (w) sid = w.sid || '';
    }
    return String(sid || '');
  }

  /* ===== 필드 매핑 (설계서 §4 표) ===== */
  var COLL_MAP = {
    cases:          { store: 'case',    type: ['caseType'],                        proj: ['title', 'caseNo'] },
    consultings:    { store: 'consult', type: ['consultingType', 'programName'],   proj: ['title', 'programName'] },
    funds:          { store: 'fund',    type: ['fundType', 'programName'],         proj: ['title'] },
    other_projects: { store: 'etc',     type: ['programName', 'projectType'],      proj: ['title'] }
  };
  function pick(c, keys) { for (var i = 0; i < keys.length; i++) { if (c[keys[i]]) return c[keys[i]]; } return ''; }

  function mapRecord(coll, key, c, userMap) {
    var m = COLL_MAP[coll];
    if (!m || !c) return null;
    var sid = mainSid(c);
    var dateRaw = c.closedDate || c.endDate || '';
    return {
      store: m.store,
      rec: {
        type: pick(c, m.type),
        org: c.companyName || c.payee || '',
        project: pick(c, m.proj),
        year: String(dateRaw).slice(0, 4),
        main: (userMap && userMap[sid]) || sid,
        status: '완료',
        puRef: coll + '/' + key
      }
    };
  }

  var api = { isClosed: isClosed, mapRecord: mapRecord };
  if (typeof module !== 'undefined' && module.exports) module.exports = api;
  else root.KcareerPuSync = api;
})(typeof globalThis !== 'undefined' ? globalThis : window);
```

- [ ] **Step 4: 통과 확인**

```bash
node --test tests/kcareer-pusync.test.js
```

기대: `# pass 5`, `# fail 0`

- [ ] **Step 5: 커밋**

```bash
git add js/kcareer-pusync.js tests/kcareer-pusync.test.js && git commit -m "feat(kcareer): pu-erp 동기화 순수 모듈 - 종료판별과 필드매핑"
```

---

## Task 2: 순수 모듈 — 병합 계획(buildSyncPlan)

**Files:**
- Modify: `js/kcareer-pusync.js`
- Test: `tests/kcareer-pusync.test.js`

**Interfaces:**
- Consumes: `isClosed`, `mapRecord` (Task 1)
- Produces:
  - `KcareerPuSync.buildSyncPlan(collData, existingRefs, userMap) → { adds, counts, skippedOpen, skippedKnown }`
    - `collData`: `{ cases: {key:obj}|null, consultings: ..., funds: ..., other_projects: ... }` (Firebase `.val()` 그대로)
    - `existingRefs`: `puRef` 문자열의 `Set` 또는 배열 (배제된 것 포함 전부)
    - `adds`: `[{ store, rec }]` — 종료됐고 puRef가 처음인 것만
    - `counts`: `{ case:n, consult:n, fund:n, etc:n }`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/kcareer-pusync.test.js` 끝에 추가:

```js
test('buildSyncPlan: 종료 건만, puRef 처음인 것만 들어온다', () => {
  const collData = {
    cases: {
      k1: { caseType: '부당해고', companyName: 'A사', title: '사건1', closedDate: '2026-01-01', managerMain: '2001' },
      k2: { caseType: '임금체불', companyName: 'B사', title: '사건2', status: 'active' },            // 진행 중 → 제외
      k3: { caseType: '산재', companyName: 'C사', title: '사건3', status: 'closed', managerMain: '2001' }
    },
    consultings: {
      c1: { consultingType: '일터혁신', companyName: 'D사', title: '컨설팅1', status: 'done', managerMain: '2003' }
    },
    funds: null,                                                                                      // 컬렉션이 비어도 죽지 않는다
    other_projects: {}
  };
  const existing = new Set(['cases/k3']);                                                             // 이미 들어온 것(배제 포함)
  const plan = PS.buildSyncPlan(collData, existing, UMAP);

  assert.equal(plan.adds.length, 2);                                                                  // k1 + c1
  assert.deepEqual(plan.counts, { case: 1, consult: 1, fund: 0, etc: 0 });
  assert.equal(plan.skippedOpen, 1);                                                                  // k2
  assert.equal(plan.skippedKnown, 1);                                                                 // k3
  const refs = plan.adds.map((a) => a.rec.puRef).sort();
  assert.deepEqual(refs, ['cases/k1', 'consultings/c1']);
});

test('buildSyncPlan: 배열형 컬렉션(Firebase가 배열로 줄 때)도 처리한다', () => {
  const collData = { cases: [null, { caseType: '사건', companyName: 'E사', title: 't', closedDate: '2025-05-05' }],
                     consultings: null, funds: null, other_projects: null };
  const plan = PS.buildSyncPlan(collData, new Set(), {});
  assert.equal(plan.adds.length, 1);
  assert.equal(plan.adds[0].rec.puRef, 'cases/1');                                                    // 배열 인덱스가 키
});
```

- [ ] **Step 2: 실패 확인**

```bash
node --test tests/kcareer-pusync.test.js
```

기대: `buildSyncPlan is not a function`으로 2개 실패.

- [ ] **Step 3: 최소 구현**

`js/kcareer-pusync.js`의 `var api = {` 위에 추가:

```js
  /* ===== 병합 계획 =====
     추가만 계획한다. 기존 레코드는 건드리지 않고(수동 수정 보존),
     existingRefs에 있는 puRef는 배제된 것이라도 다시 들어오지 않는다. */
  function buildSyncPlan(collData, existingRefs, userMap) {
    var known = (existingRefs instanceof Set) ? existingRefs : new Set(existingRefs || []);
    var plan = { adds: [], counts: { case: 0, consult: 0, fund: 0, etc: 0 }, skippedOpen: 0, skippedKnown: 0 };
    Object.keys(COLL_MAP).forEach(function (coll) {
      var v = collData ? collData[coll] : null;
      if (!v) return;
      Object.keys(v).forEach(function (key) {
        var c = v[key];
        if (!c) return;                                       /* Firebase 배열형의 null 구멍 */
        if (!isClosed(c)) { plan.skippedOpen++; return; }
        var ref = coll + '/' + key;
        if (known.has(ref)) { plan.skippedKnown++; return; }
        var m = mapRecord(coll, key, c, userMap);
        if (!m) return;
        plan.adds.push(m);
        plan.counts[m.store]++;
      });
    });
    return plan;
  }
```

`api`에 `buildSyncPlan: buildSyncPlan,` 추가.

- [ ] **Step 4: 통과 확인**

```bash
node --test tests/kcareer-pusync.test.js
```

기대: `# pass 7`, `# fail 0`

- [ ] **Step 5: 커밋**

```bash
git add js/kcareer-pusync.js tests/kcareer-pusync.test.js && git commit -m "feat(kcareer): 동기화 병합계획 - 종료건만 puRef 신규만 추가"
```

---

## Task 3: 동기화 실행 — 미리보기·저장·되돌리기

**Files:**
- Modify: `kcareer.html`
- Test: `tests/kcareer-html.test.js`

**Interfaces:**
- Consumes: `KcareerPuSync.buildSyncPlan` (Task 2), 기존 `get`/`set`/`LS`/`NS`/`toast`/`$`/`escapeHtml`/`_puLoadUserMap`/`renderCareer`/`CAREER_CFG`
- Produces:
  - `puSyncNow() → Promise<void>` — 수동 동기화(항상 미리보기 모달)
  - `puSyncCommit() → void` — 모달의 [등록]
  - `puUndoSync(syncId)` / `puUndoLastSync()` — 되돌리기
  - `PU_SYNC_STORES = ['case','consult','fund','etc']`
  - 전역 `_puSyncCtx = { syncId, plan }`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/kcareer-html.test.js` 끝에 추가:

```js
test('동기화 모듈을 외부 파일로 로드한다', () => {
  assert.match(source, /<script src="js\/kcareer-pusync\.js"><\/script>/);
});

test('puSyncCommit은 스토어별 단일 쓰기와 꼬리표를 지킨다', () => {
  const src = funcSource('puSyncCommit');
  assert.match(src, /src:\s*'pu'/);
  assert.match(src, /puRef/);
  assert.match(src, /syncId/);
  // 레코드 반복문 안에서 set() 금지 — 저장은 마지막에 스토어 목록을 돌며 한 번씩
  const loop = src.slice(src.indexOf('plan.adds.forEach'), src.indexOf('PU_SYNC_STORES.forEach'));
  assert.ok(loop.length > 0, 'adds 반복문과 저장 루프가 있어야 합니다');
  assert.ok(!/\bset\(/.test(loop), 'adds 반복문 안에서 set()을 부르면 안 됩니다');
});

test('puUndoSync는 그 동기화가 만든 레코드만 지운다 (배제된 것 포함)', () => {
  const store = { case: [], consult: [], fund: [], etc: [] };
  const ctx = {
    get: (k) => store[k].slice(),
    set: (k, v) => { store[k] = v; },
    toast: () => {}, renderCareer: () => {}, CAREER_CFG: {},
    PU_SYNC_STORES: ['case', 'consult', 'fund', 'etc']
  };
  store.case = [
    { id: 'CS0001', syncId: 'PS1' },
    { id: 'CS0002', syncId: 'PS1', excluded: true },   // 배제됐어도 그 동기화 것이면 삭제
    { id: 'CS0003', syncId: 'PS2' },
    { id: 'CS0004' }                                    // 수동 등록 → 보존
  ];
  vm.runInNewContext(funcSource('puUndoSync') + '\npuUndoSync("PS1");', ctx);
  assert.deepEqual(store.case.map((r) => r.id), ['CS0003', 'CS0004']);
});
```

- [ ] **Step 2: 실패 확인**

```bash
node --test tests/kcareer-html.test.js
```

기대: 새 테스트 3개 실패(기존 29개는 통과 유지).

- [ ] **Step 3: 구현**

**3-1.** 스크립트 로드 — `<script src="js/kcareer-scan.js"></script>` 바로 아래:

```html
<script src="js/kcareer-pusync.js"></script>
```

**3-2.** `fsUndoLast()` 함수 아래에 추가:

```js
/* ===== pu-erp 실적 동기화 =====
   pu-erp의 종료 건을 실적 스토어로 가져온다. 추가만 하고 덮어쓰지 않는다(설계서 §6). */
var PU_SYNC_STORES = ['case','consult','fund','etc'];
var _puSyncCtx = null;

function _puKnownRefs(){
  var s = new Set();
  PU_SYNC_STORES.forEach(function(k){ get(k).forEach(function(r){ if(r && r.puRef) s.add(r.puRef); }); });
  return s;
}

async function puSyncNow(){
  if(typeof fbDb === 'undefined' || !fbDb){ toast('클라우드 로그인 후 사용하세요'); return; }
  toast('⏳ pu-erp 실적 읽는 중…');
  try{
    var userMap = await _puLoadUserMap();
    var colls = ['cases','consultings','funds','other_projects'], collData = {};
    for(var i = 0; i < colls.length; i++){
      var s = await fbDb.ref('data/' + colls[i]).once('value');
      collData[colls[i]] = s.val();
    }
    var plan = KcareerPuSync.buildSyncPlan(collData, _puKnownRefs(), userMap || {});
    _puSyncCtx = { syncId: 'PS' + new Date().toISOString().slice(0,10).replace(/-/g,'') + '-' + String(Date.now()).slice(-4), plan: plan };
    renderPuSyncPreview();
    $('modalPuSync').classList.add('open');
  }catch(e){ console.warn('puSyncNow', e); toast('동기화 실패 — 로그인·권한을 확인하세요'); }
}

function renderPuSyncPreview(){
  if(!_puSyncCtx) return;
  var p = _puSyncCtx.plan;
  var LABEL = { case:'사건', consult:'컨설팅', fund:'기금', etc:'기타' };
  var h = '<div style="font-size:12px;background:#F3F8F6;border:1px solid var(--border);border-radius:8px;padding:8px 10px;margin-bottom:10px">'
    + '새로 들어올 실적 <b>' + p.adds.length + '건</b>'
    + ' <span style="color:var(--text-mute)">(진행 중이라 제외 ' + p.skippedOpen + ' · 이미 있어 건너뜀 ' + p.skippedKnown + ')</span>'
    + '<div style="color:var(--text-mute);margin-top:4px">가져온 뒤 pu-erp에서 내용이 바뀌어도 여기 실적은 바뀌지 않습니다. 불필요한 건은 목록에서 배제하세요.</div></div>'
    + '<div style="display:flex;gap:8px;margin-bottom:12px;flex-wrap:wrap">'
    + PU_SYNC_STORES.map(function(k){ return '<div class="card" style="flex:1;min-width:80px;text-align:center"><div style="font-size:20px;font-weight:700;color:var(--navy)">' + p.counts[k] + '</div><div style="font-size:11px;color:var(--text-mute)">' + LABEL[k] + '</div></div>'; }).join('')
    + '</div>'
    + p.adds.slice(0,60).map(function(a){
        return '<div style="font-size:12px;padding:4px 0;border-bottom:1px solid var(--border-soft)">'
          + '<span class="tag navy">' + escapeHtml(LABEL[a.store]) + '</span> '
          + escapeHtml(a.rec.year || '-') + ' · <strong>' + escapeHtml(a.rec.org || '-') + '</strong> · '
          + escapeHtml(a.rec.project || a.rec.type || '') + ' <span style="color:var(--text-mute)">(' + escapeHtml(a.rec.main || '-') + ')</span></div>';
      }).join('')
    + (p.adds.length > 60 ? '<div style="font-size:11px;color:var(--text-mute);padding:4px 0">…외 ' + (p.adds.length - 60) + '건</div>' : '');
  $('puSyncBody').innerHTML = h;
}

function closePuSync(){ $('modalPuSync').classList.remove('open'); _puSyncCtx = null; }

function puSyncCommit(){
  if(!_puSyncCtx){ toast('동기화 결과가 없습니다'); return; }
  var plan = _puSyncCtx.plan, syncId = _puSyncCtx.syncId, n = 0;
  /* 스토어별로 한 번만 읽고 한 번만 쓴다. buf는 리터럴로 초기화한다 —
     스토어 목록을 도는 반복문은 이 함수에서 맨 끝의 저장 한 곳뿐이어야 한다(정적 테스트가 지킴).
     ⚠ 주석에도 그 반복문 이름을 쓰지 말 것 — 문자열 검색 기반 테스트에 걸린다. */
  var buf = { case: get('case'), consult: get('consult'), fund: get('fund'), etc: get('etc') };
  var PREFIX = { case:'CS', consult:'CN', fund:'FD', etc:'ET' };
  function bufId(store){
    var k = 1, mk = function(){ return PREFIX[store] + String(k).padStart(4,'0'); };
    while(buf[store].some(function(x){ return x.id === mk(); })) k++;
    return mk();
  }
  plan.adds.forEach(function(a){
    var rec = Object.assign({}, a.rec, {
      id: bufId(a.store), src: 'pu', syncId: syncId, excluded: false,
      savedAt: new Date().toISOString()
    });
    buf[a.store].unshift(rec); n++;
  });
  PU_SYNC_STORES.forEach(function(k){ set(k, buf[k]); });
  LS.set(NS + 'pu_sync_last', new Date().toISOString());
  LS.set(NS + 'pu_sync_last_id', syncId);
  closePuSync();
  PU_SYNC_STORES.forEach(function(k){ try{ if(CAREER_CFG[k]) renderCareer(k); }catch(e){} });
  toast('✅ 실적 ' + n + '건 들어옴 — 불필요한 건은 행의 배제 버튼으로 정리하세요');
}

/* ===== 동기화 되돌리기 — 그 동기화가 만든 레코드만(배제된 것 포함) 삭제 ===== */
function puUndoSync(syncId){
  if(!syncId) return;
  var total = 0;
  PU_SYNC_STORES.forEach(function(k){
    var db = get(k), left = db.filter(function(r){ return r.syncId !== syncId; });
    total += db.length - left.length;
    if(left.length !== db.length) set(k, left);
  });
  toast(total + '건을 되돌렸습니다 — 다음 동기화 때 다시 들어옵니다');
  PU_SYNC_STORES.forEach(function(k){ try{ if(CAREER_CFG[k]) renderCareer(k); }catch(e){} });
}
function puUndoLastSync(){
  var sid = LS.get(NS + 'pu_sync_last_id') || '';
  if(!sid){ toast('되돌릴 동기화 기록이 없습니다'); return; }
  if(confirm('이번 동기화로 들어온 실적을 모두 지웁니다.\n(' + sid + ')\n배제해 둔 것도 함께 지워지고, 다음 동기화 때 다시 들어옵니다.\n\n계속할까요?')) puUndoSync(sid);
}
```

**3-3.** 모달 HTML — `modalSub` 블록 바로 뒤에:

```html
<div class="modal-ov" id="modalPuSync">
  <div class="modal" style="width:720px">
    <div class="modal-h">🔄 pu-erp 실적 동기화 — 아직 저장되지 않았습니다</div>
    <div class="modal-b" id="puSyncBody"></div>
    <div class="modal-foot"><button class="btn" onclick="closePuSync()">닫기 (저장 안 함)</button><button class="btn primary" onclick="puSyncCommit()">등록</button></div>
  </div>
</div>
```

**3-4.** 버튼 — 사건실적~기타실적 4개 탭 툴바의 기존 `🔗 pu-erp … 불러오기` 버튼 바로 뒤에 각각:

```html
          <button class="btn" type="button" onclick="puSyncNow()">🔄 실적 동기화</button>
          <button class="btn" type="button" onclick="puUndoLastSync()">↩ 동기화 되돌리기</button>
```

(4곳: `loadPuPerf('cases',true)`·`loadPuPerf('consultings',true)`·`loadPuPerf('funds',true)`·`loadPuPerf('other_projects',true)` 버튼 옆. 확인: `grep -n "loadPuPerf(" kcareer.html`)

- [ ] **Step 4: 통과 확인**

```bash
node --test tests/kcareer-html.test.js
```

기대: 전부 통과(기존 + 신규 3).

- [ ] **Step 5: 커밋**

```bash
git add kcareer.html tests/kcareer-html.test.js && git commit -m "feat(kcareer): pu-erp 실적 동기화 - 미리보기 후 등록과 syncId 되돌리기"
```

---

## Task 4: 자동 동기화(하루 1회) — 조용히 추가

**Files:**
- Modify: `kcareer.html`
- Test: `tests/kcareer-html.test.js`

**Interfaces:**
- Consumes: `puSyncNow`의 내부 로직(Task 3) — 공용 부분을 `_puFetchPlan()`으로 추출
- Produces:
  - `_puFetchPlan() → Promise<{syncId, plan}|null>`
  - `puSyncAuto() → Promise<void>` — 첫 실행(등록 이력 없음)이면 미리보기 모달, 이후엔 조용히 추가
  - 로그인 훅에서 `puSyncAuto()` 호출

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/kcareer-html.test.js` 끝에 추가:

```js
test('자동 동기화는 하루 1회이고 첫 실행은 미리보기를 거친다', () => {
  const src = funcSource('puSyncAuto');
  assert.match(src, /pu_sync_last/);                       // 마지막 실행 기록 확인
  assert.match(src, /slice\(0,\s*10\)/);                   // 날짜(하루) 단위 비교
  assert.match(src, /renderPuSyncPreview|puSyncNow/);      // 첫 실행 → 미리보기 경로
});

test('로그인 후 자동 동기화가 걸려 있다', () => {
  assert.match(source, /resolveMe\(\)[\s\S]{0,200}puSyncAuto\(\)/);
});
```

- [ ] **Step 2: 실패 확인**

```bash
node --test tests/kcareer-html.test.js
```

- [ ] **Step 3: 구현**

**3-1.** Task 3의 `puSyncNow`에서 읽기 부분을 추출해 둘이 공유:

```js
async function _puFetchPlan(){
  if(typeof fbDb === 'undefined' || !fbDb) return null;
  var userMap = await _puLoadUserMap();
  var colls = ['cases','consultings','funds','other_projects'], collData = {};
  for(var i = 0; i < colls.length; i++){
    var s = await fbDb.ref('data/' + colls[i]).once('value');
    collData[colls[i]] = s.val();
  }
  var plan = KcareerPuSync.buildSyncPlan(collData, _puKnownRefs(), userMap || {});
  return { syncId: 'PS' + new Date().toISOString().slice(0,10).replace(/-/g,'') + '-' + String(Date.now()).slice(-4), plan: plan };
}

async function puSyncNow(){
  if(typeof fbDb === 'undefined' || !fbDb){ toast('클라우드 로그인 후 사용하세요'); return; }
  toast('⏳ pu-erp 실적 읽는 중…');
  try{
    var ctx = await _puFetchPlan();
    if(!ctx) return;
    _puSyncCtx = ctx;
    renderPuSyncPreview();
    $('modalPuSync').classList.add('open');
  }catch(e){ console.warn('puSyncNow', e); toast('동기화 실패 — 로그인·권한을 확인하세요'); }
}

/* 하루 1회 자동. 첫 실행(등록 이력 없음)은 사람이 보고 등록하도록 미리보기를 띄운다. */
async function puSyncAuto(){
  try{
    var today = new Date().toISOString().slice(0, 10);
    var last = String(LS.get(NS + 'pu_sync_last') || '').slice(0, 10);
    if(last === today) return;                              /* 오늘 이미 했다 */
    var ctx = await _puFetchPlan();
    if(!ctx || !ctx.plan.adds.length){ LS.set(NS + 'pu_sync_last', new Date().toISOString()); return; }
    if(!LS.get(NS + 'pu_sync_last_id')){                    /* 첫 실행 → 미리보기 */
      _puSyncCtx = ctx; renderPuSyncPreview(); $('modalPuSync').classList.add('open'); return;
    }
    _puSyncCtx = ctx;                                       /* 이후 → 조용히 추가 */
    puSyncCommit();
  }catch(e){ console.warn('puSyncAuto', e); }
}
```

**3-2.** 로그인 훅(4958행 부근) — `resolveMe()` 호출 뒤에 추가:

```js
if(typeof puSyncAuto==='function'){ try{ puSyncAuto(); }catch(e){} }
```

(정확한 삽입 지점 확인: `grep -n "resolveMe(); }catch" kcareer.html` — `if(u){ authGate(false); ... }` 블록 안)

- [ ] **Step 4: 통과 확인**

```bash
node --test tests/kcareer-html.test.js
```

- [ ] **Step 5: 커밋**

```bash
git add kcareer.html tests/kcareer-html.test.js && git commit -m "feat(kcareer): 하루 1회 자동 동기화 - 첫 실행은 미리보기 이후 조용히 추가"
```

---

## Task 5: 4개 탭 필터 + 배제·복원 UI

**Files:**
- Modify: `kcareer.html`
- Test: `tests/kcareer-html.test.js`

**Interfaces:**
- Consumes: `CAREER_CFG`·`renderCareer`·`rowActions(id,store,label,page,rec)`·`_me`
- Produces:
  - `CAREER_CFG.case/consult/fund/etc`에 `filter: r=>!r.agency && !r.excluded`
  - `puExclude(store,id,page)` / `puRestore(store,id,page)` — 배제·복원 (권한: 대표 전체, 담당자 본인 건만)
  - `_showExcluded[page]` 토글 + 목록 하단 "배제된 건 N개" 바

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/kcareer-html.test.js` 끝에 추가:

```js
test('실적 4탭은 외부기관·배제 건을 걸러낸다', () => {
  ['case', 'consult', 'fund', 'etc'].forEach((k) => {
    const m = source.match(new RegExp(k + ":\\{store:'" + k + "'[^\\n]*"));
    assert.ok(m, k + ' CFG가 있어야 합니다');
    assert.match(m[0], /filter:\s*r\s*=>\s*!r\.agency\s*&&\s*!r\.excluded/,
      k + ' 목록은 agency 있는 것과 excluded를 숨겨야 합니다');
  });
});

test('배제는 삭제가 아니라 excluded 플래그다', () => {
  const src = funcSource('puExclude');
  assert.match(src, /excluded\s*=\s*true/);
  assert.ok(!/\.filter\(/.test(src.split('set(')[0]) || true);   // 레코드를 지우는 filter가 아님
  const rest = funcSource('puRestore');
  assert.match(rest, /excluded\s*=\s*false/);
});

test('배제 권한 — 담당자는 본인 건만', () => {
  const src = funcSource('puExclude');
  assert.match(src, /_me\.isAdmin/);
  assert.match(src, /_me\.name/);
});
```

- [ ] **Step 2: 실패 확인**

```bash
node --test tests/kcareer-html.test.js
```

- [ ] **Step 3: 구현**

**3-1.** `CAREER_CFG`의 4개 항목에 filter 추가 (3516·3529·3535·3541행 부근). 각각 `qFields` 앞에:

```js
  consult:{store:'consult',filter:r=>!r.agency&&!r.excluded,qFields:['org','project','agency','main'],
```

```js
  case:{store:'case',filter:r=>!r.agency&&!r.excluded,qFields:['org','project','type'],
```

```js
  fund:{store:'fund',filter:r=>!r.agency&&!r.excluded,qFields:['org','project'],
```

```js
  etc:{store:'etc',filter:r=>!r.agency&&!r.excluded,qFields:['org','project','type'],
```

**3-2.** 배제·복원 함수 — `puUndoLastSync()` 아래에:

```js
/* ===== 배제 = 숨김 (삭제 금지 — 재동기화 때 되살아난다) =====
   권한: 대표(isAdmin)는 전체, 담당자는 main이 본인 이름인 건만(설계서 §7). */
function _puCanExclude(r){
  if(_me.isAdmin) return true;
  return !!(_me.name && r && r.main === _me.name);
}
function puExclude(store, id, page){
  var db = get(store), r = db.find(function(x){ return x.id === id; });
  if(!r){ toast('레코드를 찾을 수 없습니다'); return; }
  if(!_puCanExclude(r)){ toast('본인 담당 건만 배제할 수 있습니다'); return; }
  r.excluded = true; set(store, db);
  try{ renderCareer(page); }catch(e){}
  toast('배제했습니다 — 목록 하단 "배제된 건"에서 복원할 수 있습니다');
}
function puRestore(store, id, page){
  var db = get(store), r = db.find(function(x){ return x.id === id; });
  if(!r){ toast('레코드를 찾을 수 없습니다'); return; }
  if(!_puCanExclude(r)){ toast('본인 담당 건만 복원할 수 있습니다'); return; }
  r.excluded = false; set(store, db);
  try{ renderCareer(page); }catch(e){}
  toast('복원했습니다');
}
```

**3-3.** 배제 버튼 — `rowActions`의 `💬` 버튼 앞에 (rec가 있고 `src==='pu'`일 때만):

```js
    (rec&&rec.src==='pu'?`<button class="btn sm" onclick="puExclude('${store}','${id}','${page}')" title="목록에서 숨김 (복원 가능)" style="color:#BA7517">🚫 배제</button>`:'')+
```

그리고 4개 실적 CFG의 `row:`가 `rowActions(r.id,'<store>',…,p)`를 `rowActions(r.id,'<store>',…,p,r)`로 rec를 넘기도록 수정 (wiccok·cert 때와 동일 — 확인: `grep -n "rowActions(r.id,'case'\|rowActions(r.id,'consult'\|rowActions(r.id,'fund'\|rowActions(r.id,'etc'" kcareer.html`)

**3-4.** 배제 목록 바 — `renderCareer`에서 `nofileBar` 계산 직후에:

```js
  /* pu 동기화 스토어면 배제된 건 수를 보여주고 펼쳐서 복원 */
  var exBar='';
  if(PU_SYNC_STORES.indexOf(cfg.store)>=0){
    var exRows=get(cfg.store).filter(function(r){ return r.excluded; });
    if(exRows.length){
      exBar='<div style="margin-top:10px;border-top:1px dashed var(--border);padding-top:8px;font-size:12px">'
        +'<button type="button" class="nofile-btn'+(_showExcluded[name]?' active':'')+'" onclick="toggleExcluded(\''+name+'\')">🚫 배제된 건 '+exRows.length+'개</button>'
        +(_showExcluded[name]?exRows.map(function(r){
            return '<div style="display:flex;gap:8px;align-items:center;padding:5px 0;border-bottom:1px solid var(--border-soft)">'
              +'<span class="id-chip">'+escapeHtml(r.id)+'</span><span style="flex:1">'+escapeHtml((r.year||'')+' · '+(r.org||'-')+' · '+(r.project||r.type||''))+'</span>'
              +'<button class="btn sm" onclick="puRestore(\''+_jsAttr(cfg.store)+'\',\''+_jsAttr(r.id)+'\',\''+_jsAttr(name)+'\')">↩ 복원</button></div>';
          }).join(''):'')
        +'</div>';
    }
  }
```

그리고 목록 HTML을 조립하는 곳(`nofileBar`가 붙는 자리)에 `+exBar`를 추가. 전역에 `var _showExcluded={};`와:

```js
function toggleExcluded(name){ _showExcluded[name]=!_showExcluded[name]; renderCareer(name); }
```

- [ ] **Step 4: 통과 확인**

```bash
node --test tests/kcareer-html.test.js
```

- [ ] **Step 5: 커밋**

```bash
git add kcareer.html tests/kcareer-html.test.js && git commit -m "feat(kcareer): 실적 4탭 내부만 표시 + 배제 복원 - 담당자는 본인 건만"
```

---

## Task 6: 외부기관 실적 탭 + 증명서 자동 연결

**Files:**
- Modify: `kcareer.html`
- Test: `tests/kcareer-html.test.js`

**Interfaces:**
- Consumes: `PU_SYNC_STORES`·`get`·`escapeHtml`·`openOriginal`(기존)·`hasOriginal`(기존)·`NAV`
- Produces:
  - `page-puagency` 섹션 + NAV `실적관리` 그룹에 `['page-puagency','외부기관 실적']`
  - `renderPuAgency()` — agency별 그룹 + certdoc 자동 매칭
  - `openCertdocOf(agency)` 없이 그룹 안에 인라인 표시(레코드행 + 증명서행)

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/kcareer-html.test.js` 끝에 추가:

```js
test('외부기관 실적 페이지와 메뉴가 있다', () => {
  assert.match(source, /<section class="page-view" id="page-puagency">/);
  assert.match(source, /\['page-puagency','외부기관 실적'\]/);
  assert.match(source, /function renderPuAgency\(/);
});

test('외부기관 탭은 4개 스토어의 agency 있는 건을 모아 기관별로 묶는다', () => {
  const src = funcSource('renderPuAgency');
  assert.match(src, /PU_SYNC_STORES/);
  assert.match(src, /r\.agency/);
  assert.match(src, /!r\.excluded/, '배제된 건은 외부기관 탭에서도 숨긴다');
  assert.match(src, /certdoc/, '기관이 발급한 증명서를 자동 매칭해 보여준다');
});
```

- [ ] **Step 2: 실패 확인**

```bash
node --test tests/kcareer-html.test.js
```

- [ ] **Step 3: 구현**

**3-1.** NAV(1853행 부근) `실적관리` 그룹에 항목 추가:

```js
  {g:'실적관리', items:[['page-case','사건실적'],['page-consult','컨설팅실적'],['page-fund','기금실적'],['page-lecture','강의실적'],['page-etc','기타실적'],['page-puagency','외부기관 실적'],['page-submission','제출서류']]},
```

**3-2.** 페이지 섹션 — `page-submission` 섹션 바로 뒤에:

```html
      <section class="page-view" id="page-puagency"><div class="page">
        <div class="page-head"><div class="page-title">🏛 외부기관 실적</div><div class="page-desc">외부 컨설팅기관을 통해 수행한 실적 — 그 기관이 실적증명·경력증명서를 발급합니다. 실적의 수행기관 칸을 채우면 이 탭으로 모입니다.</div></div>
        <div class="toolbar">
          <input class="tb-input" id="puagQ" placeholder="기관·고객사·내용 검색" style="min-width:220px" oninput="renderPuAgency()">
          <div class="tb-spacer"></div>
          <span class="cnt" id="puagCnt" style="font-size:11px;color:var(--text-mute)"></span>
        </div>
        <div id="puagBody"></div>
      </div></section>
```

**3-3.** 렌더 함수 — `puRestore` 아래에:

```js
/* ===== 외부기관 실적 — agency별 그룹 + 그 기관이 발급한 증명서(certdoc) 자동 연결 ===== */
function _agencyNorm(s){ return String(s||'').replace(/[\s\(\)（）\-·]/g,'').toLowerCase(); }
function renderPuAgency(){
  var box = $('puagBody'); if(!box) return;
  var q = _agencyNorm(($('puagQ')||{}).value||'');
  var LABEL = { case:'사건', consult:'컨설팅', fund:'기금', etc:'기타' };
  var groups = {};                                          /* 정규화 기관명 → {name, rows} */
  PU_SYNC_STORES.forEach(function(k){
    get(k).forEach(function(r){
      if(!r || !r.agency || r.excluded) return;
      var key = _agencyNorm(r.agency);
      if(!groups[key]) groups[key] = { name: r.agency, rows: [] };
      groups[key].rows.push(Object.assign({ _store: k, _label: LABEL[k] }, r));
    });
  });
  var certs = get('certdoc');
  var keys = Object.keys(groups).sort(function(a,b){ return groups[b].rows.length - groups[a].rows.length; });
  if(q) keys = keys.filter(function(key){
    var g = groups[key];
    return key.indexOf(q) >= 0 || g.rows.some(function(r){ return _agencyNorm((r.org||'')+(r.project||'')).indexOf(q) >= 0; });
  });
  var total = keys.reduce(function(s,k){ return s + groups[k].rows.length; }, 0);
  var cnt = $('puagCnt'); if(cnt) cnt.textContent = keys.length + '개 기관 · ' + total + '건';
  if(!keys.length){ box.innerHTML = '<div style="font-size:12px;color:var(--text-mute);padding:20px;text-align:center">외부기관 실적이 없습니다 — 실적 행을 열어 수행기관을 채우면 여기로 모입니다</div>'; return; }
  box.innerHTML = keys.map(function(key){
    var g = groups[key];
    var years = g.rows.map(function(r){ return r.year; }).filter(Boolean).sort();
    var yr = years.length ? (years[0] === years[years.length-1] ? years[0] : years[0] + '~' + years[years.length-1]) : '';
    /* 이 기관이 발급한 증명서: certdoc의 발급기관(org) 부분일치 + 손으로 연결한 것(agencyKey) */
    var myCerts = certs.filter(function(c){
      if(_agencyNorm(c.agencyKey) === key) return true;
      var a = _agencyNorm(c.org), b = key;
      return a && (a.indexOf(b.slice(0,6)) >= 0 || b.indexOf(a.slice(0,6)) >= 0);
    });
    return '<div class="card" style="margin-bottom:12px">'
      + '<div style="display:flex;align-items:center;gap:8px;margin-bottom:8px"><strong style="font-size:14px">🏛 ' + escapeHtml(g.name) + '</strong>'
      + '<span style="font-size:11px;color:var(--text-mute)">' + g.rows.length + '건' + (yr ? ' · ' + yr : '') + '</span></div>'
      + g.rows.map(function(r){
          return '<div style="display:flex;gap:6px;align-items:center;font-size:12px;padding:5px 0;border-top:1px solid var(--border-soft);flex-wrap:wrap">'
            + '<span class="tag navy">' + r._label + '</span>'
            + '<span style="white-space:nowrap">' + escapeHtml(r.year||'-') + '</span>'
            + '<strong style="min-width:90px">' + escapeHtml(r.org||'-') + '</strong>'
            + '<span style="flex:1;min-width:120px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap">' + escapeHtml(r.project||r.type||'') + '</span>'
            + '<span style="color:var(--text-mute);white-space:nowrap">' + escapeHtml(r.main||'-') + '</span>'
            + (r.src==='pu' ? '<span class="tag" style="font-size:10px">pu</span>' : '')
            + '</div>';
        }).join('')
      + '<div style="margin-top:8px;padding-top:8px;border-top:1px dashed var(--border);font-size:12px">'
      + '<span style="font-weight:600;color:var(--teal)">📜 이 기관이 발급한 증명서 ' + myCerts.length + '건</span> '
      + '<button class="btn sm" onclick="openCertLink(\'' + _jsAttr(g.name) + '\')">➕ 증명서 연결</button>'
      + myCerts.map(function(c){
          return '<div style="display:flex;gap:6px;align-items:center;padding:4px 0">'
            + '<span style="flex:1">' + escapeHtml(c.title||c.fname||c.id) + ' <span style="color:var(--text-mute)">(' + escapeHtml(formatDate(c.date)||'-') + ')</span></span>'
            + (hasOriginal(c) ? '<button class="btn sm" onclick=\'openOriginal(' + JSON.stringify({id:c.id,src:c.src||"",relPath:c.relPath||""}).replace(/'/g,"&#39;") + ')\'>📄 원본</button>' : '')
            + '</div>';
        }).join('') + '</div>'
      + '</div>';
  }).join('');
}

/* 자동 매칭이 놓친 증명서를 손으로 연결 — certdoc.agencyKey에 기관명을 붙인다(설계서 §7) */
var _certLinkAgency = '';
function openCertLink(agencyName){
  _certLinkAgency = agencyName;
  var key = _agencyNorm(agencyName);
  var list = get('certdoc').filter(function(c){ return _agencyNorm(c.agencyKey) !== key; });
  $('certLinkBody').innerHTML = '<div style="font-size:12px;color:var(--text-mute);margin-bottom:8px">'
    + escapeHtml(agencyName) + ' 그룹에 연결할 증명서를 고르세요</div>'
    + (list.length ? list.map(function(c){
        return '<div style="display:flex;gap:6px;align-items:center;font-size:12px;padding:5px 0;border-bottom:1px solid var(--border-soft)">'
          + '<span style="flex:1">' + escapeHtml(c.title||c.fname||c.id) + ' <span style="color:var(--text-mute)">' + escapeHtml(c.org||'') + '</span></span>'
          + '<button class="btn sm primary" onclick="linkCert(\'' + _jsAttr(c.id) + '\')">연결</button></div>';
      }).join('') : '<div style="font-size:12px;color:var(--text-mute)">연결할 증명서가 없습니다</div>');
  $('modalCertLink').classList.add('open');
}
function linkCert(certId){
  var db = get('certdoc'), c = db.find(function(x){ return x.id === certId; });
  if(!c){ toast('증명서를 찾을 수 없습니다'); return; }
  c.agencyKey = _certLinkAgency; set('certdoc', db);
  $('modalCertLink').classList.remove('open');
  renderPuAgency();
  toast('✅ 연결했습니다');
}
function closeCertLink(){ $('modalCertLink').classList.remove('open'); }
```

모달 — `modalPuSync` 뒤에:

```html
<div class="modal-ov" id="modalCertLink">
  <div class="modal" style="width:560px">
    <div class="modal-h">📜 증명서 연결</div>
    <div class="modal-b" id="certLinkBody"></div>
    <div class="modal-foot"><button class="btn" onclick="closeCertLink()">닫기</button></div>
  </div>
</div>
```

**3-4.** 탭 진입 시 렌더 — `showPage`/페이지 전환 로직에서 `page-puagency`를 열 때 `renderPuAgency()` 호출. 기존 패턴 확인: `grep -n "page-settings'){ _safe(renderPersonal" kcareer.html` 과 같은 분기 자리에:

```js
  if(id==='page-puagency'){ _safe(renderPuAgency); }
```

- [ ] **Step 4: 통과 확인**

```bash
node --test tests/kcareer-html.test.js && node --test tests/kcareer-pusync.test.js && node --test tests/kcareer-scan.test.js
```

기대: 전부 통과.

- [ ] **Step 5: 커밋**

```bash
git add kcareer.html tests/kcareer-html.test.js && git commit -m "feat(kcareer): 외부기관 실적 탭 - 기관별 묶음과 발급 증명서 자동 연결"
```

---

## Task 7: pu-erp 원본 보기 + 실사용 확인 + push

**Files:**
- Modify: `kcareer.html`
- Test: `tests/kcareer-html.test.js`

**Interfaces:**
- Consumes: `puRef`(레코드), `fbDb`
- Produces: `openPuSource(puRef) → Promise<void>` — `data/{coll}/{key}`를 읽어 읽기 전용 팝업

- [ ] **Step 1: 실패하는 테스트 작성**

```js
test('pu-erp 원본 보기는 읽기 전용이다', () => {
  const m = source.match(/async function openPuSource\([\s\S]*?\n\}/);
  assert.ok(m, 'openPuSource 함수가 있어야 합니다');
  assert.ok(!/\.set\(|\.update\(|\.push\(/.test(m[0]), 'pu-erp 데이터에 쓰기를 시도하면 안 됩니다');
});
```

- [ ] **Step 2: 실패 확인 → Step 3: 구현**

`puRestore` 아래에:

```js
/* pu-erp 원본 현재값 보기 — 읽기 전용. 동기화 스냅샷과 달라졌는지 확인용 */
async function openPuSource(puRef){
  if(typeof fbDb === 'undefined' || !fbDb){ toast('클라우드 로그인 후 사용하세요'); return; }
  try{
    var s = await fbDb.ref('data/' + puRef).once('value');
    var v = s.val();
    if(!v){ toast('pu-erp에서 이 건을 찾을 수 없습니다 (삭제되었을 수 있음)'); return; }
    var KEY_LABEL = { companyName:'고객사', title:'내용', caseType:'사건유형', consultingType:'컨설팅유형',
      fundType:'기금유형', programName:'사업명', status:'상태', closedDate:'종료일', endDate:'예정일', managerMain:'주담당' };
    var rows = Object.keys(v).filter(function(k){ return typeof v[k] !== 'object'; }).map(function(k){
      return '<div style="display:flex;gap:8px;font-size:12px;padding:4px 0;border-bottom:1px solid var(--border-soft)">'
        + '<span style="min-width:90px;color:var(--text-mute)">' + escapeHtml(KEY_LABEL[k]||k) + '</span>'
        + '<span style="flex:1;word-break:break-all">' + escapeHtml(String(v[k])) + '</span></div>';
    }).join('');
    $('puSrcBody').innerHTML = '<div style="font-size:11px;color:var(--text-mute);margin-bottom:8px">' + escapeHtml(puRef) + ' — pu-erp의 현재 값입니다. 여기 실적은 가져온 시점으로 고정돼 있습니다.</div>' + rows;
    $('modalPuSrc').classList.add('open');
  }catch(e){ console.warn('openPuSource', e); toast('읽기 실패 — 권한을 확인하세요'); }
}
function closePuSrc(){ $('modalPuSrc').classList.remove('open'); }
```

모달 — `modalPuSync` 뒤에:

```html
<div class="modal-ov" id="modalPuSrc">
  <div class="modal" style="width:560px">
    <div class="modal-h">🔎 pu-erp 원본 (읽기 전용)</div>
    <div class="modal-b" id="puSrcBody"></div>
    <div class="modal-foot"><button class="btn" onclick="closePuSrc()">닫기</button></div>
  </div>
</div>
```

`rowActions`의 배제 버튼 앞에 원본 보기(rec.puRef 있을 때만):

```js
    (rec&&rec.puRef?`<button class="btn sm" onclick="openPuSource('${_jsAttr(rec.puRef)}')" title="pu-erp 현재 값 보기">🔎</button>`:'')+
```

- [ ] **Step 4: 전체 테스트**

```bash
node --test tests/kcareer-pusync.test.js tests/kcareer-scan.test.js tests/kcareer-html.test.js
```

기대: 전부 통과.

- [ ] **Step 5: 브라우저 실사용 확인 (사용자와 함께)**

1. 로그인 → 컨설팅실적 탭: `agency` 있는 건이 사라지고 내부 건만 남았는지
2. `🏛 외부기관 실적` 탭: 능률협회·경제진흥원 그룹이 생기고 아래 "발급한 증명서"가 붙는지
3. `🔄 실적 동기화` → 미리보기 숫자가 pu-erp 종료 건수와 맞는지 → [등록]
4. 들어온 행 `🚫 배제` → 목록에서 사라지고 하단 "배제된 건 N개"에서 `↩ 복원` 되는지
5. `↩ 동기화 되돌리기` → 이번에 들어온 것만 사라지는지
6. `🔎` → pu-erp 현재 값 팝업

- [ ] **Step 6: 커밋 + 사용자 승인 후 push**

```bash
git add kcareer.html tests/kcareer-html.test.js && git commit -m "feat(kcareer): pu-erp 원본 보기 읽기전용 팝업"
```

```bash
git pull --rebase origin main && node --test tests/kcareer-pusync.test.js tests/kcareer-scan.test.js tests/kcareer-html.test.js
```

테스트 전부 통과 후 사용자에게 push 승인 요청. CLAUDE.md §2 "최근 작업" 갱신.

---

## 완료 조건

- [ ] `node --test tests/kcareer-pusync.test.js tests/kcareer-scan.test.js tests/kcareer-html.test.js` 전부 통과
- [ ] 동기화 미리보기 숫자 = pu-erp 종료 건수 (사용자 눈 대조)
- [ ] 컨설팅실적 79건이 내부/외부로 갈라져 표시 (데이터 이동 없음 — 콘솔에서 `get('consult').length` 불변 확인)
- [ ] 배제 → 복원 → 재동기화 시 배제 건이 다시 들어오지 않음
- [ ] `↩ 동기화 되돌리기` 후 재동기화하면 같은 건이 다시 들어옴
- [ ] pu-erp `data/`에 쓰기 없음 (`\.ref\('data/[^']*'\)\.(set|update|push)` 검색 0건)
- [ ] 기존 수동 등록 레코드·시드 불변
