# 급여데이터함 값 층 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 급여데이터함에 담긴 서류를 사람이 「판독하기」로 읽어 사람별 값으로 정리하고, 월별 값 표에서 엑셀·복사로 바깥 급여프로그램에 넘길 수 있게 한다.

**Architecture:** 부품은 대부분 이미 있다(`buildValueRows`·`saveValues`·`findDuplicateValue`·`readWageTable`·`fileToDataUrl`). 이 계획은 **배선과 화면**을 만든다. 판독 결과 세 가지 모양을 `{name, pairs:[{item,value}]}` 하나로 맞추는 정규화 함수를 새로 두고, 그 뒤는 이미 있는 저장 부품에 그대로 흘려보낸다.

**Tech Stack:** 단일 파일 HTML(빌드 없음) · Firebase RTDB/Storage · `node --test` · 공용 판독 층 `js/pu-doc-read.js`(Gemini)

설계서: `docs/superpowers/specs/2026-08-15-급여데이터함-값층-design.md`
목업: `docs/mockups/paydata-read-value.html` · `docs/mockups/paydata-value-table.html`

## Global Constraints

- **공용 판독 층(`js/pu-doc-read.js`)에서 다른 앱이 기대는 동작을 바꾸지 않는다.** 사진첩·명함첩·업체관리가 함께 쓴다.
  - `read()` 와 `PROMPT_ALL`(그 안의 모든 kind) 은 **손대지 않는다** — 저 셋이 실제로 부르는 것이다.
  - `readWageTable`·`readChangeNotice` 는 급여데이터함만 쓴다. **겉모양(이름·인자·돌려주는 꼴)이 그대로면 속을 함께 쓰도록 묶어도 된다** — Task 2 가 그렇게 한다. 짝 검사(`tests/paydata-wage-read.test.js`)가 겉모양을 지킨다.
- **검사 판정은 종료코드로.** `node --test tests/*.test.js` 를 돌리고 `echo "EXIT:$?"` 가 `EXIT:0` 인지 본다. 출력만 골라 읽지 않는다.
- **커밋은 경로를 찍어서.** `git add -A` / `git add .` 금지 — 여러 세션이 같은 작업트리를 쓴다.
- **한국어 주석.** 화면 문구도 한국어. 영어 내부 용어를 사람에게 노출하지 않는다.
- **새 콘솔 규칙 없음.** `paydata/u/$owner/values/{slot}/{rowId}` 는 이미 열려 있다.
- **급여대장 판독은 만들되 꺼 둔다** — 처리위탁 근거 정리 전까지(설계서 9장). 상수 하나로 끈다.
- vm 샌드박스 안에서 만든 배열은 밖의 배열과 종류가 달라 `assert.deepEqual` 이 튕긴다. `.length` 나 `.join(',')` 로 견준다.

---

## File Structure

| 파일 | 무엇을 맡나 | 새로/고침 |
|---|---|---|
| `js/pu-paydata-store.js` | 판독 결과 → 값 줄 정규화(`rowsFromRead`), 서류종류 → 판독방식(`readKindFor`) | 고침(더하기) |
| `js/pu-doc-read.js` | 알림 캡처 판독(`readChangeNotice`) | 고침(**더하기만**) |
| `pu-paydata.html` | 판독 패널·값 표 편집·월별 값 표·내보내기 | 고침 |
| `tests/paydata-read-rows.test.js` | 정규화 검사 | 새로 |
| `tests/paydata-change-notice.test.js` | 알림 판독 검사 | 새로 |
| `tests/paydata-read-panel.test.js` | 판독 패널·저장·중복 검사 | 새로 |
| `tests/paydata-value-grid.test.js` | 월별 값 표·내보내기 검사 | 새로 |

---

### Task 1: 판독 결과를 값 줄 하나의 모양으로 맞춘다

세 가지 판독 결과(근태·급여대장·알림)가 모양이 다르다. `buildValueRows` 가 받는
`{name, pairs:[{item,value}]}` 로 맞추는 **순수 함수**를 저장 층에 둔다.
근태만 모양이 다르고(`paid`·`off` 날짜 배열), 나머지 둘은 이미 맞는 모양이다.

**Files:**
- Modify: `js/pu-paydata-store.js` (`buildValueRows` 바로 위)
- Test: `tests/paydata-read-rows.test.js`

**Interfaces:**
- Produces:
  - `S.readKindFor(kind)` → `'timesheet' | 'wage' | 'notice' | null` — 서랍 종류 키를 받아 판독 방식을 돌려준다. `null` 이면 판독 단추를 그리지 않는다.
  - `S.rowsFromRead(readKind, parsed)` → `[{name, pairs:[{item,value}]}]`

- [ ] **Step 1: 실패하는 검사를 쓴다**

`tests/paydata-read-rows.test.js` 를 만든다:

```js
'use strict';
// 판독 결과 → 값 줄 정규화. 실행: node --test tests/*.test.js
const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');
const vm = require('node:vm');

function loadStore() {
  const src = fs.readFileSync(path.join(__dirname, '..', 'js', 'pu-paydata-store.js'), 'utf8');
  const sandbox = { window: {}, console };
  sandbox.globalThis = sandbox;
  vm.createContext(sandbox);
  new vm.Script(src, { filename: 'store.js' }).runInContext(sandbox);
  return sandbox.window.PuPaydataStore;
}

test('★ 서랍 종류마다 판독 방식이 정해진다', () => {
  const S = loadStore();
  assert.equal(S.readKindFor('attend'), 'timesheet');
  assert.equal(S.readKindFor('ledger'), 'wage');
  assert.equal(S.readKindFor('etc'), 'notice');
});

test('★ 근로계약서·우리 산출물은 판독하지 않는다', () => {
  const S = loadStore();
  assert.equal(S.readKindFor('contract'), null, '계약서에서 값을 뽑지 않습니다');
  assert.equal(S.readKindFor('output'), null, '우리가 만든 것은 다시 읽을 이유가 없습니다');
  assert.equal(S.readKindFor(''), null);
});

test('★ 근태표: 날짜 배열을 일수로 바꾼다', () => {
  const S = loadStore();
  const parsed = { rows: [{ name: '배영승', paid: [1, 5, 25], off: [11, 19, 28], adj: '+4일', note: '' }] };
  const rows = S.rowsFromRead('timesheet', parsed);
  assert.equal(rows.length, 1);
  assert.equal(rows[0].name, '배영승');
  const m = {};
  rows[0].pairs.forEach(p => { m[p.item] = p.value; });
  assert.equal(m['유급일수'], '3일');
  assert.equal(m['휴무일수'], '3일');
  assert.equal(m['가감'], '+4일');
});

test('근태표: 빈 칸은 항목을 만들지 않는다 — 0 과 「없음」은 다르다', () => {
  const S = loadStore();
  const parsed = { rows: [{ name: '이산다라', paid: [], off: [], adj: '', note: '정상근무' }] };
  const rows = S.rowsFromRead('timesheet', parsed);
  const items = rows[0].pairs.map(p => p.item).join(',');
  assert.equal(items, '비고', '없는 항목까지 0 으로 만들면 안 됩니다: ' + items);
  assert.equal(rows[0].pairs[0].value, '정상근무');
});

test('★ 급여대장·알림은 이미 맞는 모양이라 그대로 온다', () => {
  const S = loadStore();
  const parsed = { rows: [{ name: '홍길동', pairs: [{ item: '기본급', value: '3,200,000' }] }] };
  ['wage', 'notice'].forEach(k => {
    const rows = S.rowsFromRead(k, parsed);
    assert.equal(rows[0].name, '홍길동');
    assert.equal(rows[0].pairs[0].item, '기본급');
    assert.equal(rows[0].pairs[0].value, '3,200,000');
  });
});

test('이름 없는 줄과 항목 없는 줄은 버린다', () => {
  const S = loadStore();
  const parsed = { rows: [
    { name: '', pairs: [{ item: '기본급', value: '1' }] },      // 이름이 없다 → 버린다
    { name: '홍길동', pairs: [] },                               // 항목이 하나도 없다 → 버린다
    { name: '김철수', pairs: [{ item: '기본급', value: '2' }] }  // 이것만 남는다
  ] };
  const rows = S.rowsFromRead('wage', parsed);
  assert.equal(rows.length, 1);
  assert.equal(rows[0].name, '김철수');
});

test('자료가 없어도 터지지 않는다', () => {
  const S = loadStore();
  assert.equal(S.rowsFromRead('timesheet', null).length, 0);
  assert.equal(S.rowsFromRead('wage', {}).length, 0);
  assert.equal(S.rowsFromRead('없는방식', { rows: [{ name: 'a', pairs: [] }] }).length, 0);
});
```

- [ ] **Step 2: 실패하는 것을 확인한다**

Run: `node --test tests/paydata-read-rows.test.js`
Expected: FAIL — `S.readKindFor is not a function`

- [ ] **Step 3: 최소 구현**

`js/pu-paydata-store.js` 에서 `function buildValueRows(` 바로 **위**에 넣는다:

```js
  /* ══════ 판독 결과 → 값 줄 (2026-08-15) ══════
     판독 결과가 서류마다 모양이 다르다. buildValueRows 가 받는
     {name, pairs:[{item,value}]} 하나로 맞춘다. 근태만 모양이 다르고
     (paid·off 가 날짜 배열) 나머지 둘은 이미 맞는 모양이라 그대로 흘린다. */

  /* 서랍 종류 → 판독 방식. null 이면 그 탭에는 판독 단추를 그리지 않는다.
     근로계약서는 값을 뽑을 것이 아니고, 우리 산출물은 우리가 만든 것이라
     다시 읽을 이유가 없다. */
  function readKindFor(kind) {
    if (kind === 'attend') return 'timesheet';
    if (kind === 'ledger') return 'wage';
    if (kind === 'etc') return 'notice';
    return null;
  }

  /* 값이 비면 그 항목을 아예 만들지 않는다 — 0 과 「없음」은 다르다.
     (월별 값 표에서 없는 항목은 0 이 아니라 「－」로 보여야 한다) */
  function pushPair(pairs, item, value) {
    var v = String(value == null ? '' : value).trim();
    if (v) pairs.push({ item: item, value: v });
  }

  function rowsFromRead(readKind, parsed) {
    var src = (parsed && parsed.rows) || [];
    var out = [];
    src.forEach(function (r) {
      var name = String((r && r.name) || '').trim();
      if (!name) return;
      var pairs = [];
      if (readKind === 'timesheet') {
        var paid = Array.isArray(r.paid) ? r.paid : [];
        var off = Array.isArray(r.off) ? r.off : [];
        if (paid.length) pushPair(pairs, '유급일수', paid.length + '일');
        if (off.length) pushPair(pairs, '휴무일수', off.length + '일');
        pushPair(pairs, '가감', r.adj);
        pushPair(pairs, '비고', r.note);
      } else if (readKind === 'wage' || readKind === 'notice') {
        (Array.isArray(r.pairs) ? r.pairs : []).forEach(function (p) {
          var item = String((p && p.item) || '').trim();
          if (item) pushPair(pairs, item, p && p.value);
        });
      } else {
        return;                       // 모르는 방식은 아무것도 만들지 않는다
      }
      if (!pairs.length) return;      // 항목이 하나도 없으면 값 줄이 아니다
      out.push({ name: name, pairs: pairs });
    });
    return out;
  }
```

내보내는 목록(`global.PuPaydataStore = {`)의 `buildValueRows: buildValueRows,` 바로 위에 넣는다:

```js
    readKindFor: readKindFor,
    rowsFromRead: rowsFromRead,
```

- [ ] **Step 4: 통과하는 것을 확인한다**

Run: `node --test tests/paydata-read-rows.test.js`
Expected: PASS 7/7

- [ ] **Step 5: 전체 검사**

Run: `node --test tests/*.test.js > /tmp_t.txt 2>&1; echo "EXIT:$?"; tail -8 /tmp_t.txt`
Expected: `EXIT:0`

- [ ] **Step 6: 커밋**

```bash
git add js/pu-paydata-store.js tests/paydata-read-rows.test.js
git commit -m "feat: 판독 결과를 값 줄 하나의 모양으로 맞춘다"
```

---

### Task 2: 알림 캡처 판독기(`readChangeNotice`)

문자·카톡 캡처에서 「누가 · 무엇이 · 언제부터」를 뽑는다. 표가 아니라 줄글이라
기존 판독기로는 안 된다. **공용 층에 새 함수만 더한다.**

**Files:**
- Modify: `js/pu-doc-read.js` (`readWageTable` 바로 아래, `function read(` 위)
- Test: `tests/paydata-change-notice.test.js`

**Interfaces:**
- Produces: `PuDocRead.readChangeNotice(dataUrl)` → `Promise<{ok, error, company, period, docName, rows:[{name, pairs:[{item,value}]}]}>` — `readWageTable` 과 같은 모양

- [ ] **Step 1: 실패하는 검사를 쓴다**

`tests/paydata-change-notice.test.js`:

```js
'use strict';
// 알림 캡처(입퇴사·수당변경) 판독. 실행: node --test tests/*.test.js
const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');
const vm = require('node:vm');

const SRC = path.join(__dirname, '..', 'js', 'pu-doc-read.js');

function loadRead() {
  const src = fs.readFileSync(SRC, 'utf8');
  const sandbox = { window: {}, console };
  sandbox.globalThis = sandbox;
  vm.createContext(sandbox);
  new vm.Script(src, { filename: 'pu-doc-read.js' }).runInContext(sandbox);
  return sandbox.window.PuDocRead;
}

function fakeFetch(reply) {
  const calls = [];
  const fn = function (url, init) {
    calls.push({ url: url, body: JSON.parse(init.body) });
    return Promise.resolve({ ok: true, status: 200,
      json: () => Promise.resolve({ candidates: [{ content: { parts: [{ text: reply }] } }] }) });
  };
  fn.calls = calls;
  return fn;
}
const IMG = 'data:image/jpeg;base64,AAAA';

test('판독 층에 readChangeNotice 가 붙는다', () => {
  assert.equal(typeof loadRead().readChangeNotice, 'function');
});

test('★ 입사 알림에서 누가·언제부터를 뽑는다', async () => {
  const R = loadRead();
  R.init({ fetch: fakeFetch(JSON.stringify({
    company: '다온원', period: '2026-08', docName: '입사 통보',
    rows: [{ name: '김신입', pairs: [{ item: '입사일', value: '2026-08-12' }] }]
  })), getKey: () => Promise.resolve('KEY') });
  const r = await R.readChangeNotice(IMG);
  assert.equal(r.ok, true);
  assert.equal(r.rows[0].name, '김신입');
  assert.equal(r.rows[0].pairs[0].item, '입사일');
  assert.equal(r.rows[0].pairs[0].value, '2026-08-12');
});

test('★ 수당 변경에서 항목·금액을 뽑는다', async () => {
  const R = loadRead();
  R.init({ fetch: fakeFetch(JSON.stringify({
    rows: [{ name: '홍길동', pairs: [{ item: '식대', value: '200,000' }] }]
  })), getKey: () => Promise.resolve('KEY') });
  const r = await R.readChangeNotice(IMG);
  assert.equal(r.rows[0].pairs[0].item, '식대');
});

test('이름 없는 줄·항목 없는 짝은 버린다', async () => {
  const R = loadRead();
  R.init({ fetch: fakeFetch(JSON.stringify({
    rows: [{ name: '', pairs: [{ item: '식대', value: '1' }] },
           { name: '홍길동', pairs: [{ item: '', value: '1' }, { item: '식대', value: '2' }] }]
  })), getKey: () => Promise.resolve('KEY') });
  const r = await R.readChangeNotice(IMG);
  assert.equal(r.rows.length, 1);
  assert.equal(r.rows[0].pairs.length, 1);
});

test('AI 키가 없으면 부르지 않고 한국어로 알린다', async () => {
  const R = loadRead();
  const f = fakeFetch('{}');
  R.init({ fetch: f, getKey: () => Promise.resolve('') });
  const r = await R.readChangeNotice(IMG);
  assert.equal(r.ok, false);
  assert.match(r.error, /키/);
  assert.equal(f.calls.length, 0);
});

test('AI 가 JSON 이 아닌 말을 해도 터지지 않는다', async () => {
  const R = loadRead();
  R.init({ fetch: fakeFetch('죄송합니다'), getKey: () => Promise.resolve('KEY') });
  const r = await R.readChangeNotice(IMG);
  assert.equal(r.ok, false);
  assert.ok(r.error);
});

test('★ 지어내지 말라고 못 박는다', () => {
  const src = fs.readFileSync(SRC, 'utf8');
  /* ⚠ `;\n` 이 아니라 `;\r?\n` 이어야 한다 — 이 저장소는 core.autocrlf=true 라
     윈도우 작업트리에서 파일이 CRLF 로 놓인다. 앞이 [\s\S] 인 자리는 \r 을 삼켜
     탈이 없지만, 글자 바로 뒤에 \n 을 붙이면 \r 에 걸려 못 찾는다. */
  const fn = src.match(/var NOTICE_PROMPT =[\s\S]*?;\r?\n/);
  assert.ok(fn, 'NOTICE_PROMPT 를 찾을 수 없습니다');
  assert.match(fn[0], /지어내지/);
  assert.match(fn[0], /주민등록번호/, '주민번호는 담지 않는다고 못 박아야 합니다');
});

test('★ 기존 함수·프롬프트가 그대로다 — 다른 앱이 함께 씁니다', () => {
  const R = loadRead();
  assert.equal(typeof R.read, 'function');
  assert.equal(typeof R.readWageTable, 'function');
  assert.match(R.PROMPTS.all, /금액과 사람 이름은 담지 마세요/);
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --test tests/paydata-change-notice.test.js`
Expected: FAIL — `readChangeNotice is not a function`

- [ ] **Step 3: 구현**

`js/pu-doc-read.js` 에서 `function read(dataUrl) {` **바로 위**에 넣는다:

```js
  /* ── 알림 캡처 판독 (급여데이터함 전용, 2026-08-15) ──
     문자·카톡으로 오는 「누가 며칠자 입사」·「OO씨 수당 얼마」를 읽는다.
     표가 아니라 줄글이라 기존 판독기로는 안 된다.
     결과 모양은 readWageTable 과 같게 맞춘다 — 부르는 쪽이 하나로 다룬다. */
  var NOTICE_PROMPT =
    '이 이미지는 급여 업무 관련 알림(문자·카카오톡·메일) 캡처입니다. 사람마다 무엇이 바뀌는지 JSON으로만 답하세요.' +
    '\n키: company(사업장·회사명이 보이면), period(귀속 연월 — 2026-08 형식, 없으면 빈 문자열), docName(무슨 알림인지 한 마디 — 예 입사 통보·수당 변경), rows(사람별 줄 — 아래 규칙).' +
    '\nrows 규칙: [{"name":"이름","pairs":[{"item":"바뀌는 것","value":"값"}]}] 배열입니다.' +
    ' item 은 무엇이 바뀌는지입니다 — 예 입사일·퇴사일·기본급·식대·직책수당.' +
    ' value 는 적힌 그대로 담으세요(날짜는 2026-08-12 형식, 금액은 적힌 표기 그대로).' +
    ' **흐릿하거나 확실하지 않으면 지어내지 말고 그 줄을 빼세요.**' +
    ' 인사말·잡담은 담지 마세요. **주민등록번호는 담지 마세요.**' +
    ' 사람 이름이 없으면 그 줄을 담지 마세요. JSON 외 텍스트 금지.';

  function readChangeNotice(dataUrl) {
    return readPairsWith(NOTICE_PROMPT, dataUrl);
  }
```

`readWageTable` 이 이미 같은 배관을 쓰므로 **공통 부분을 하나로 모은다.**
`function readWageTable(dataUrl) {` 부터 그 함수 끝까지를 아래로 바꾼다:

```js
  /* 프롬프트만 갈아 끼우고 나머지(모델·재시도·키 조달·결과 다듬기)는 함께 쓴다. */
  function readPairsWith(prompt, dataUrl) {
    if (!deps.fetch) return Promise.resolve(wageFail('판독 준비가 되지 않았습니다'));
    var imgs = (Array.isArray(dataUrl) ? dataUrl : [dataUrl])
      .map(function (u) { return String(u || '').split(',')[1] || ''; })
      .filter(Boolean);
    if (!imgs.length) return Promise.resolve(wageFail('사진을 읽을 수 없습니다'));

    var keyP = deps.getKey ? Promise.resolve().then(deps.getKey) : Promise.resolve('');
    return keyP.catch(function () { return ''; }).then(function (key) {
      if (!key) return wageFail('AI 키가 없습니다 — 포털 설정에서 등록해 주세요');
      var parts = imgs.map(function (b64) {
        return { inline_data: { mime_type: 'image/jpeg', data: b64 } };
      });
      parts.push({ text: prompt + (imgs.length > 1 ? MULTI_NOTE : '') });
      return askAny(key, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ contents: [{ parts: parts }], generationConfig: { temperature: 0 } })
      }).then(function (j) {
        var parsed = parseReply(j);
        if (!parsed) throw new Error('AI가 알아볼 수 없는 답을 보냈습니다');
        return afterWageRead(parsed);
      }).catch(function (e) {
        return wageFail((e && e.message) || String(e));
      });
    });
  }

  function readWageTable(dataUrl) { return readPairsWith(WAGE_PROMPT, dataUrl); }
```

내보내는 목록의 `readWageTable: readWageTable,` 아래에 넣는다:

```js
    readChangeNotice: readChangeNotice,
```

- [ ] **Step 4: 통과 확인**

Run: `node --test tests/paydata-change-notice.test.js tests/paydata-wage-read.test.js`
Expected: PASS — 급여대장 판독 검사도 그대로 통과해야 한다(배관을 함께 쓰게 바꿨으므로)

- [ ] **Step 5: 전체 검사 — 다른 앱 회귀 확인**

Run: `node --test tests/*.test.js > /tmp_t.txt 2>&1; echo "EXIT:$?"; tail -8 /tmp_t.txt`
Expected: `EXIT:0` (사진첩·명함첩 판독 검사가 모두 통과해야 한다)

- [ ] **Step 6: 커밋**

```bash
git add js/pu-doc-read.js tests/paydata-change-notice.test.js
git commit -m "feat: 알림 캡처(입퇴사·수당변경) 판독기 추가"
```

---

### Task 3: 판독 패널 자리 만들기 — 원본 옆 절반

뷰어를 왼쪽 원본 / 오른쪽 절반으로 가른다. 아직 판독은 안 한다 — **자리와 단추만.**

**Files:**
- Modify: `pu-paydata.html` (script 태그 · CSS · 뷰어 HTML · `renderViewer`)
- Test: `tests/paydata-read-panel.test.js`

**Interfaces:**
- Consumes: `S.readKindFor(kind)` (Task 1)
- Produces: `readPanelHtml()` → 오른쪽 패널 HTML 문자열. `App.readState` = `{status:'idle'|'reading'|'done'|'err', rows:[], err:''}`

- [ ] **Step 1: 실패하는 검사를 쓴다**

`tests/paydata-read-panel.test.js`:

```js
'use strict';
// 판독 패널 — 원본 옆 절반. 실행: node --test tests/*.test.js
const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');
const vm = require('node:vm');

const R = path.join(__dirname, '..');
const html = fs.readFileSync(path.join(R, 'pu-paydata.html'), 'utf8');
const store = fs.readFileSync(path.join(R, 'js', 'pu-paydata-store.js'), 'utf8');

function cut(name) {
  const m = html.match(new RegExp('function ' + name + '\\s*\\([\\s\\S]*?\\n\\}'));
  assert.ok(m, name + ' 함수를 찾을 수 없습니다');
  return m[0];
}

/* ⚠ 잘라 온 함수가 쓰는 **상수**도 함께 넣어야 한다 — 안 넣으면
   ReferenceError 로 터진다(2026-08-14 SHARE_TAG_OPTIONS 에서 같은 일을 겪었다). */
const WAGE_FLAG = html.match(/const WAGE_READ_ON = (?:true|false);/);
assert.ok(WAGE_FLAG, 'WAGE_READ_ON 상수를 찾을 수 없습니다');

function loadApp(appState) {
  const sandbox = { window: {}, console, Date, document: { getElementById: () => null } };
  sandbox.globalThis = sandbox;
  vm.createContext(sandbox);
  new vm.Script(store, { filename: 'store.js' }).runInContext(sandbox);
  new vm.Script([
    'const S = window.PuPaydataStore; S.init({uid:"U1"});',
    'const App = ' + JSON.stringify(Object.assign({
      kind: 'attend', viewerId: 'a1', viewingUid: '',
      readState: { status: 'idle', rows: [], err: '' }
    }, appState)) + ';',
    WAGE_FLAG[0],
    cut('esc'), cut('canWrite'), cut('readPanelHtml'),
    'window.App = App; window.readPanelHtml = readPanelHtml;'
  ].join('\n'), { filename: 'app.js' }).runInContext(sandbox);
  return sandbox.window;
}

test('★ 판독 층을 불러온다', () => {
  assert.match(html, /<script src="js\/pu-doc-read\.js">/);
  assert.match(html, /PuDocRead\.init\(/, '키를 어디서 얻는지 판독 층에 알려야 합니다');
});

test('★ 아직 안 읽었으면 「판독하기」 단추가 있다', () => {
  const W = loadApp({ kind: 'attend' });
  const h = W.readPanelHtml();
  assert.match(h, /판독하기/);
  assert.match(h, /doRead\(\)/);
});

test('★ 근로계약서·우리 산출물 탭에는 판독 단추가 없다', () => {
  ['contract', 'output'].forEach(k => {
    const h = loadApp({ kind: k }).readPanelHtml();
    assert.equal(/doRead\(\)/.test(h), false, k + ' 에 판독 단추가 보입니다');
  });
});

test('★ 남의 자리에서는 판독 단추가 없다 — 남의 값을 만들면 안 된다', () => {
  const h = loadApp({ kind: 'attend', viewingUid: 'U2', viewingDeputy: false }).readPanelHtml();
  assert.equal(/doRead\(\)/.test(h), false);
});

test('읽는 중이면 그렇다고 말한다', () => {
  const h = loadApp({ readState: { status: 'reading', rows: [], err: '' } }).readPanelHtml();
  assert.match(h, /읽는 중/);
  assert.equal(/doRead\(\)/.test(h), false, '읽는 중에 또 누르면 두 번 나갑니다');
});

test('실패하면 까닭을 보여주고 다시 누를 수 있다', () => {
  const h = loadApp({ readState: { status: 'err', rows: [], err: 'AI 키가 없습니다' } }).readPanelHtml();
  assert.match(h, /AI 키가 없습니다/);
  assert.match(h, /doRead\(\)/);
});

test('★ 급여대장 판독은 아직 꺼져 있다 — 처리위탁 근거 정리 전', () => {
  // 설계서 9장. 켤 때 이 검사를 함께 고친다.
  assert.match(html, /const WAGE_READ_ON = false/);
  const h = loadApp({ kind: 'ledger' }).readPanelHtml();
  assert.equal(/doRead\(\)/.test(h), false, '아직 켜면 안 됩니다');
  assert.match(h, /준비 중/);
});

test('★ 넓은 판은 판독하는 서류일 때만 — 명함처럼 좁은 것까지 반으로 가르지 않는다', () => {
  const rv = html.match(/function renderViewer\(\)[\s\S]*?\n\}/)[0];
  assert.match(rv, /readPanelHtml\(\)/, '함수만 있고 안 부르면 화면에 아무것도 없습니다');
  assert.match(html, /#readPanel\{[^}]*flex:0 0 50%/, '절반을 쓰는 꾸밈이 없습니다');
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --test tests/paydata-read-panel.test.js`
Expected: FAIL — `readPanelHtml 함수를 찾을 수 없습니다`

- [ ] **Step 3: 판독 층 불러오기**

`pu-paydata.html` 28번째 줄 `<script src="js/pu-appbar.js"></script>` **위**에 넣는다:

```html
<script src="js/pu-doc-read.js"></script>
```

`afterLogin()` 안, `S.signIn(` 을 부르는 줄 **위**에 넣는다:

```js
  /* 서류 판독 — AI 키가 어디 있는지도 판독 층이 안다(앱마다 복사하면 갈린다). */
  PuDocRead.init(Object.assign(
    { fetch: window.fetch.bind(window) },
    PuDocRead.keysFrom(db)
  ));
```

- [ ] **Step 4: 꾸밈(CSS) 넣기**

`@media(max-width:760px){.peoplebar{display:none}}` **아래**에 넣는다:

```css
/* ── 판독 패널 — 원본 옆 절반 (대표 지시 2026-08-13: "화면의 나머지 절반에
   인식된 내용을 정리해서 보여줄 수 있게, 그렇게 해야 정밀하게 찾을 수 있다") ── */
#viewerBody.split{align-items:stretch;justify-content:stretch;padding:0}
#viewerShot{flex:1;min-width:0;display:flex;align-items:center;justify-content:center;overflow:auto;padding:0 12px 16px}
#readPanel{flex:0 0 50%;background:#fff;display:flex;flex-direction:column;border-left:1px solid var(--line);color:var(--ink)}
#readPanel .ph{padding:11px 14px;border-bottom:1px solid var(--line);display:flex;align-items:center;gap:8px}
#readPanel .ph b{flex:1;font-size:14px}
#readPanel .pbody{flex:1;min-height:0;overflow-y:auto;padding:12px 14px}
#readPanel .pfoot{border-top:1px solid var(--line);padding:10px 14px;display:flex;gap:8px;align-items:center}
#readPanel .pfoot .sub{flex:1;margin:0}
.pre{text-align:center;padding:36px 18px;color:var(--sub)}
.pre .big{font-size:32px;margin-bottom:10px}
.pre p{font-size:12.5px;line-height:1.7;margin-bottom:16px}
@media(max-width:760px){#readPanel{flex:0 0 100%;border-left:none}#viewerShot{display:none}}
```

- [ ] **Step 5: 뷰어 HTML 을 두 칸으로**

`pu-paydata.html` 196번째 줄을 바꾼다.

바꾸기 전:
```html
  <div id="viewerBody" onclick="toggleViewerZoom(event)"></div>
```

바꾼 뒤:
```html
  <div id="viewerBody"><div id="viewerShot" onclick="toggleViewerZoom(event)"></div></div>
```

- [ ] **Step 6: `readPanelHtml` 과 `renderViewer` 배선**

`function renderViewer() {` **바로 위**에 넣는다:

```js
/* 급여대장 판독은 만들어 두되 켜지 않는다 — 근로자 성명·임금액이 구글로 나가는
   처리위탁 근거가 아직 정리되지 않았다(설계서 9장). 근태표는 임금액이 없어 켠다.
   ⚠ 켤 때는 tests/paydata-read-panel.test.js 의 짝 검사도 함께 고친다. */
const WAGE_READ_ON = false;

/* 오른쪽 절반 — 판독 전에는 단추, 판독 뒤에는 값 표. */
function readPanelHtml() {
  const rk = S.readKindFor(App.kind);
  const st = App.readState || { status: 'idle', rows: [], err: '' };
  let body;

  if (rk === 'wage' && !WAGE_READ_ON) {
    body = '<div class="pre"><div class="big">🔒</div><p><b>급여대장 판독은 준비 중입니다.</b><br>'
      + '근로자 성명·임금액이 밖으로 나가는 문제를 먼저 정리한 뒤 켭니다.</p></div>';
  } else if (!rk || !canWrite()) {
    body = '<div class="pre"><div class="big">📄</div><p>'
      + (!rk ? '이 종류는 값을 뽑지 않습니다.' : '남의 자리에서는 판독할 수 없습니다.')
      + '</p></div>';
  } else if (st.status === 'reading') {
    body = '<div class="pre"><div class="big">⏳</div><p>읽는 중입니다…</p></div>';
  } else if (st.status === 'err') {
    body = '<div class="pre"><div class="big">⚠</div><p>' + esc(st.err) + '</p>'
      + '<button class="btn pri" onclick="doRead()">🔍 다시 판독</button></div>';
  } else {
    body = '<div class="pre"><div class="big">🔍</div><p>아직 읽지 않았습니다.<br>'
      + '누르면 AI가 이 서류를 읽어 <b>사람별 표</b>로 만듭니다.<br>'
      + '읽은 뒤 확인하고 저장해야 값으로 남습니다.</p>'
      + '<button class="btn pri" onclick="doRead()">🔍 판독하기</button></div>';
  }
  return '<div id="readPanel"><div class="ph"><b>판독 결과</b></div>'
    + '<div class="pbody">' + body + '</div></div>';
}
```

`renderViewer()` 안을 바꾼다.

바꾸기 전:
```js
  const body = $('viewerBody');
  body.classList.toggle('zoom', App.viewerZoom);
```

바꾼 뒤:
```js
  const outer = $('viewerBody');
  const rk = S.readKindFor(App.kind);
  outer.classList.toggle('split', !!rk);
  const body = $('viewerShot');
  body.classList.toggle('zoom', App.viewerZoom);
```

⚠ **확대 꾸밈도 함께 옮겨야 한다.** `.zoom` 이 붙는 자리가 `#viewerBody` 에서
`#viewerShot` 으로 바뀌었으므로, 그것을 보는 규칙 두 줄도 바꾼다. 안 바꾸면
**사진을 눌러도 확대가 안 되는데 아무 표시도 안 난다**(상태와 App.render 는
그대로 돌아 겉으로는 멀쩡해 보인다).

바꾸기 전:
```css
#viewerBody.zoom{align-items:flex-start;justify-content:flex-start}
#viewerBody.zoom img{max-width:none;max-height:none;cursor:zoom-out}
```

바꾼 뒤:
```css
#viewerShot.zoom{align-items:flex-start;justify-content:flex-start}
#viewerShot.zoom img{max-width:none;max-height:none;cursor:zoom-out}
```

(`#viewerBody img`·`#viewerBody .doc` 는 자손 선택자라 그대로 둬도 걸린다 —
`#viewerShot` 이 `#viewerBody` 안에 있다. 바꿀 것은 `.zoom` 두 줄뿐이다.)

그리고 `renderViewer()` 의 **맨 끝**(닫는 `}` 바로 위)에 넣는다:

```js
  /* 판독 패널은 원본을 그린 뒤에 붙인다 — 위에서 innerHTML 을 갈아 끼우므로 */
  const old = document.getElementById('readPanel');
  if (old && old.parentNode) old.parentNode.removeChild(old);
  if (rk) outer.insertAdjacentHTML('beforeend', readPanelHtml());
```

`App` 상태에 `readState` 를 더한다. `viewerId: '', viewerUrl: '',` 이 있는 줄 뒤에 넣는다:

```js
  readState: { status: 'idle', rows: [], err: '' },
```

`openViewer(id)` 안 첫 줄 뒤에 판독 상태를 비우는 줄을 더한다:

```js
  App.readState = { status: 'idle', rows: [], err: '' };
```

- [ ] **Step 7: 통과 확인**

Run: `node --test tests/paydata-read-panel.test.js`
Expected: PASS 8/8

- [ ] **Step 8: 전체 검사**

Run: `node --test tests/*.test.js > /tmp_t.txt 2>&1; echo "EXIT:$?"; tail -8 /tmp_t.txt`
Expected: `EXIT:0`

- [ ] **Step 9: 커밋**

```bash
git add pu-paydata.html tests/paydata-read-panel.test.js
git commit -m "feat: 판독 패널 자리 — 원본 옆 절반"
```

---

### Task 4: 「판독하기」가 실제로 읽어 표를 그린다

**Files:**
- Modify: `pu-paydata.html`
- Test: `tests/paydata-read-panel.test.js` (이어 붙임)

**Interfaces:**
- Consumes: `S.fileToDataUrl(path, mime)`, `S.rowsFromRead(readKind, parsed)`, `PuDocRead.read/readWageTable/readChangeNotice`
- Produces: `doRead()` · `valueRowsHtml(rows)` · `editVal(i, key, v)` · `addValRow()` · `delValRow(i)`

- [ ] **Step 1: 검사를 이어 붙인다**

`tests/paydata-read-panel.test.js` 맨 뒤에 붙인다:

```js
/* ══════ 판독 실행 ══════ */
function loadRun(appState, opts) {
  opts = opts || {};
  const calls = { alerts: [], read: [] };
  const sandbox = {
    /* ⚠ Buffer 를 넣어야 한다 — fileToDataUrl 이 base64 로 바꿀 때
       window.btoa 가 없으면 Buffer.from 으로 떨어지는데, vm 안에는 없다.
       tests/paydata-file-dataurl.test.js 도 같은 이유로 넣어 두었다. */
    window: {}, console, Date, Buffer,
    document: { getElementById: () => null },
    alert: m => calls.alerts.push(m),
    PuDocRead: {
      read: p => { calls.read.push('read'); return Promise.resolve(opts.readOut || { kind: 'timesheet', fields: {} }); },
      readWageTable: p => { calls.read.push('wage'); return Promise.resolve(opts.wageOut || { ok: true, rows: [] }); },
      readChangeNotice: p => { calls.read.push('notice'); return Promise.resolve(opts.noticeOut || { ok: true, rows: [] }); }
    }
  };
  sandbox.globalThis = sandbox;
  vm.createContext(sandbox);
  new vm.Script(store, { filename: 'store.js' }).runInContext(sandbox);
  new vm.Script([
    'const S = window.PuPaydataStore;',
    'S.init({uid:"U1", storage:{ref:function(){return{getDownloadURL:function(){return Promise.resolve("https://x/f");}};}},'
      + ' fetch:function(){return Promise.resolve({ok:true,arrayBuffer:function(){return Promise.resolve(new ArrayBuffer(2));}});}});',
    'const PuDocRead = window.PuDocRead || globalThis.PuDocRead;',
    'const App = ' + JSON.stringify(Object.assign({
      kind: 'attend', viewerId: 'a1', viewingUid: '',
      itemsMonth: { a1: { companyId: 'co_1', kind: 'attend', month: '202608', filename: '근태.jpg', file: 'p/a1.jpg', mime: 'image/jpeg' } },
      itemsKeep: {}, companyId: 'co_1', companyName: '다온원', month: '2026-08',
      readState: { status: 'idle', rows: [], err: '' }
    }, appState)) + ';',
    'App.render = function(){};',
    WAGE_FLAG[0],                       // doRead 가 이 상수를 본다 — 안 넣으면 터진다
    cut('esc'), cut('canWrite'), cut('findRow'), cut('doRead'), cut('valueRowsHtml'),
    cut('editVal'), cut('addValRow'), cut('delValRow'),
    'window.App = App; window.doRead = doRead; window.valueRowsHtml = valueRowsHtml;',
    'window.editVal = editVal; window.addValRow = addValRow; window.delValRow = delValRow;'
  ].join('\n'), { filename: 'run.js' }).runInContext(sandbox);
  return { W: sandbox.window, calls };
}

test('★ 근태 탭이면 근태 판독기를 부른다', async () => {
  const { W, calls } = loadRun({ kind: 'attend' }, {
    readOut: { kind: 'timesheet', fields: { rows: [{ name: '배영승', paid: [1, 5], off: [], adj: '', note: '' }] } }
  });
  W.doRead();
  await new Promise(r => setTimeout(r, 10));
  assert.equal(calls.read[0], 'read');
  assert.equal(W.App.readState.status, 'done');
  assert.equal(W.App.readState.rows[0].name, '배영승');
});

test('★ 기타 탭이면 알림 판독기를 부른다', async () => {
  const { W, calls } = loadRun({ kind: 'etc' }, {
    noticeOut: { ok: true, rows: [{ name: '김신입', pairs: [{ item: '입사일', value: '2026-08-12' }] }] }
  });
  W.doRead();
  await new Promise(r => setTimeout(r, 10));
  assert.equal(calls.read[0], 'notice');
  assert.equal(W.App.readState.rows[0].pairs[0].item, '입사일');
});

test('★ 한 줄도 못 읽으면 그렇다고 말한다 — 빈 표를 띄우지 않는다', async () => {
  const { W } = loadRun({ kind: 'etc' }, { noticeOut: { ok: true, rows: [] } });
  W.doRead();
  await new Promise(r => setTimeout(r, 10));
  assert.equal(W.App.readState.status, 'err');
  assert.match(W.App.readState.err, /읽어내지 못했습니다/);
});

test('판독이 실패하면 까닭을 담는다', async () => {
  const { W } = loadRun({ kind: 'etc' }, { noticeOut: { ok: false, error: 'AI 키가 없습니다', rows: [] } });
  W.doRead();
  await new Promise(r => setTimeout(r, 10));
  assert.equal(W.App.readState.status, 'err');
  assert.match(W.App.readState.err, /키/);
});

test('★ 표에 근로자·항목·값이 고칠 수 있게 그려진다', () => {
  const { W } = loadRun({});
  const h = W.valueRowsHtml([{ name: '배영승', pairs: [{ item: '유급일수', value: '3일' }] }]);
  assert.match(h, /배영승/);
  assert.match(h, /유급일수/);
  assert.match(h, /editVal\(/, '칸을 고칠 수 없으면 확인 화면이 아닙니다');
  assert.match(h, /delValRow\(/);
});

test('★ 사람이 고치면 노란 표시가 걷힌다', () => {
  const { W } = loadRun({});
  W.App.readState = { status: 'done', rows: [{ name: '이옥자', pairs: [{ item: '휴무일수', value: '5일' }], iffy: true }], err: '' };
  W.editVal(0, 'name', '이옥자2');
  assert.equal(W.App.readState.rows[0].iffy, false, '확인이 끝난 줄이 계속 노랗게 뜨면 표시를 못 믿습니다');
});

test('줄 더하기·지우기', () => {
  const { W } = loadRun({});
  W.App.readState = { status: 'done', rows: [{ name: 'a', pairs: [{ item: 'x', value: '1' }] }], err: '' };
  W.addValRow();
  assert.equal(W.App.readState.rows.length, 2);
  W.delValRow(0);
  assert.equal(W.App.readState.rows.length, 1);
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --test tests/paydata-read-panel.test.js`
Expected: FAIL — `doRead 함수를 찾을 수 없습니다`

- [ ] **Step 3: 구현**

`function readPanelHtml() {` **바로 위**에 넣는다:

```js
/* 판독을 돌린다 — 사람이 「판독하기」를 눌렀을 때만(설계서 2장).
   창고 파일을 내려받아 base64 로 바꿔 판독 층에 싣는다. */
function doRead() {
  const rk = S.readKindFor(App.kind);
  if (!rk || !canWrite()) return;
  if (rk === 'wage' && !WAGE_READ_ON) return;
  const rec = findRow(App.viewerId);
  if (!rec) return;
  /* 판독 중에 다른 서류로 옮겨 갈 수 있다. 그때 늦게 온 답이 새 화면을 덮으면
     **엉뚱한 원본이 출처로 붙는다** — 값마다 출처를 붙이는 이 설계에서 가장
     나쁜 고장이다(없는 것보다 틀린 것이 나쁘다, 아무도 의심하지 않으므로).
     그래서 아래 두 갈래에서 자리가 그대로인지 먼저 본다. */
  const forId = App.viewerId;
  App.readState = { status: 'reading', rows: [], err: '' };
  App.render();

  S.fileToDataUrl(rec.file, rec.mime).then(dataUrl => {
    if (rk === 'timesheet') {
      return PuDocRead.read(dataUrl).then(r => ({
        ok: !r.error, error: r.error, parsed: (r && r.fields) || {}
      }));
    }
    const fn = (rk === 'wage') ? PuDocRead.readWageTable : PuDocRead.readChangeNotice;
    return fn(dataUrl).then(r => ({ ok: r.ok, error: r.error, parsed: r }));
  }).then(res => {
    if (App.viewerId !== forId) return;      // 다른 서류로 옮겨 갔다 — 덮지 않는다
    if (!res.ok) throw new Error(res.error || '판독하지 못했습니다');
    const rows = S.rowsFromRead(rk, res.parsed);
    if (!rows.length) throw new Error('사람별 값을 읽어내지 못했습니다 — 직접 적어 주세요.');
    App.readState = { status: 'done', rows: rows, err: '' };
    App.render();
  }).catch(e => {
    if (App.viewerId !== forId) return;      // 다른 서류로 옮겨 갔다 — 덮지 않는다
    App.readState = { status: 'err', rows: [], err: (e && e.message) || String(e) };
    App.render();
  });
}
window.doRead = doRead;

/* 읽어 온 값을 고칠 수 있는 표로. 한 줄 = 한 사람의 한 항목이다. */
function valueRowsHtml(rows) {
  let h = '<table class="vals"><tr><th style="width:28%">근로자</th>'
        + '<th style="width:32%">항목</th><th>값</th><th style="width:26px"></th></tr>';
  (rows || []).forEach((r, i) => {
    (r.pairs || []).forEach((p, j) => {
      h += '<tr class="' + (r.iffy ? 'iffy' : '') + '">'
        + '<td><input value="' + esc(r.name) + '" oninput="editVal(' + i + ',\'name\',this.value)"></td>'
        + '<td><input value="' + esc(p.item) + '" oninput="editVal(' + i + ',\'item\',this.value,' + j + ')"></td>'
        + '<td><input value="' + esc(p.value) + '" oninput="editVal(' + i + ',\'value\',this.value,' + j + ')"></td>'
        + '<td><span class="del" onclick="delValRow(' + i + ')">✕</span></td></tr>';
    });
  });
  return h + '</table>';
}

/* 고치면 그 줄의 노란 표시를 걷는다 — 확인이 끝났다는 뜻이다. */
function editVal(i, key, v, j) {
  const r = (App.readState.rows || [])[i];
  if (!r) return;
  if (key === 'name') r.name = v;
  else if (r.pairs && r.pairs[j]) r.pairs[j][key] = v;
  r.iffy = false;
}
function addValRow() {
  App.readState.rows.push({ name: '', pairs: [{ item: '', value: '' }], iffy: false });
  App.render();
}
function delValRow(i) { App.readState.rows.splice(i, 1); App.render(); }
window.editVal = editVal; window.addValRow = addValRow; window.delValRow = delValRow;
```

`readPanelHtml()` 안에서 마지막 `else` 앞에 판독이 끝난 경우를 더한다.
`} else if (st.status === 'err') {` **위**에 넣는다:

```js
  } else if (st.status === 'done') {
    const n = st.rows.length;
    const iffy = st.rows.filter(r => r.iffy).length;
    body = '<div class="okline">사람 <b>' + n + '명</b>의 값을 읽었습니다. 원본과 견주어 확인해 주세요.</div>'
      + (iffy ? '<div class="warnline">⚠ <b>' + iffy + '줄</b>은 확실하지 않습니다 — 노란 줄부터 봐 주세요.</div>' : '')
      + valueRowsHtml(st.rows)
      + '<div class="sub" style="margin-top:10px;line-height:1.7">저장하면 값마다 '
      + '<b>이 서류가 출처로</b> 붙습니다 — 나중에 값을 누르면 이 원본이 열립니다.</div>';
```

`readPanelHtml()` 의 돌려주는 줄을 바꿔 아래 단추 줄을 붙인다.

바꾸기 전:
```js
  return '<div id="readPanel"><div class="ph"><b>판독 결과</b></div>'
    + '<div class="pbody">' + body + '</div></div>';
```

바꾼 뒤:
```js
  const foot = (st.status === 'done')
    ? '<div class="pfoot"><span class="sub">' + st.rows.length + '명</span>'
      + '<button class="btn" onclick="addValRow()">＋ 줄</button>'
      + '<button class="btn pri" onclick="saveVals()">저장</button></div>'
    : '';
  return '<div id="readPanel"><div class="ph"><b>판독 결과</b></div>'
    + '<div class="pbody">' + body + '</div>' + foot + '</div>';
```

꾸밈을 CSS 맨 뒤에 더한다:

```css
table.vals{width:100%;border-collapse:collapse;font-size:12.5px}
table.vals th{background:var(--paper);border-bottom:1px solid var(--line);padding:7px 6px;text-align:left;font-size:11.5px;color:var(--sub)}
table.vals td{border-bottom:1px solid var(--line);padding:3px 4px}
table.vals input{width:100%;border:1px solid transparent;border-radius:6px;padding:5px 6px;font-size:12.5px;font-family:inherit;background:transparent;color:var(--ink)}
table.vals input:hover{border-color:var(--line)}
table.vals input:focus{outline:none;border-color:var(--navy);background:#fff}
table.vals tr.iffy{background:#FDF6E3}
table.vals .del{color:var(--bad);cursor:pointer;padding:2px 6px;border-radius:5px}
.okline{background:#EAF6EE;border:1px solid #B7E4C7;color:#1E5631;border-radius:9px;padding:8px 11px;font-size:12px;margin-bottom:10px}
.warnline{background:#FDF6E3;border:1px solid #F0D9A8;color:#7C4A03;border-radius:9px;padding:8px 11px;font-size:12px;margin-bottom:10px}
```

- [ ] **Step 4: 통과 확인**

Run: `node --test tests/paydata-read-panel.test.js`
Expected: PASS 15/15

- [ ] **Step 5: 전체 검사**

Run: `node --test tests/*.test.js > /tmp_t.txt 2>&1; echo "EXIT:$?"; tail -8 /tmp_t.txt`
Expected: `EXIT:0`

- [ ] **Step 6: 커밋**

```bash
git add pu-paydata.html tests/paydata-read-panel.test.js
git commit -m "feat: 판독하기 — 읽어서 고칠 수 있는 표로"
```

---

### Task 5: 저장과 중복 확인

**Files:**
- Modify: `pu-paydata.html`
- Test: `tests/paydata-read-panel.test.js` (이어 붙임)

**Interfaces:**
- Consumes: `S.buildValueRows(parsed, tag)`, `S.findDuplicateValue(existing, companyId, month, name)`, `S.listValues(slot, owner)`, `S.saveValues(slot, rows, owner)`
- Produces: `saveVals()`

- [ ] **Step 1: 검사를 이어 붙인다**

```js
/* ══════ 저장·중복 ══════ */
function loadSave(existing, confirmYes) {
  const calls = { alerts: [], confirms: [], saved: null };
  const sandbox = {
    window: {}, console, Date,
    document: { getElementById: () => null },
    alert: m => calls.alerts.push(m),
    confirm: m => { calls.confirms.push(m); return !!confirmYes; }
  };
  sandbox.globalThis = sandbox;
  vm.createContext(sandbox);
  new vm.Script(store, { filename: 'store.js' }).runInContext(sandbox);
  sandbox.__existing = existing || {};
  new vm.Script([
    'const S = window.PuPaydataStore;',
    'S.init({uid:"U1", db:{ref:function(p){ return {'
      + ' once:function(){ return Promise.resolve({val:function(){ return __existing; }}); },'
      + ' update:function(u){ __saved = u; return Promise.resolve(); } }; },'
      + ' }});',
    'var __saved = null;',
    // db.ref() 를 인자 없이 부르면 update 를 쓰는 자리다 — 위 ref 가 둘 다 준다
    'const App = ' + JSON.stringify({
      kind: 'attend', viewerId: 'a1', viewingUid: '', companyId: 'co_1',
      companyName: '다온원', month: '2026-08',
      itemsMonth: { a1: { companyId: 'co_1', kind: 'attend', month: '202608', file: 'p/a1.jpg' } },
      itemsKeep: {},
      readState: { status: 'done', err: '', rows: [{ name: '배영승', pairs: [{ item: '유급일수', value: '3일' }] }] }
    }) + ';',
    'App.render = function(){};',
    cut('esc'), cut('canWrite'), cut('findRow'), cut('saveVals'),
    'window.App = App; window.saveVals = saveVals; window.__got = function(){ return __saved; };'
  ].join('\n'), { filename: 'save.js' }).runInContext(sandbox);
  return { W: sandbox.window, calls };
}

test('★ 저장하면 값에 출처가 붙어 들어간다', async () => {
  const { W, calls } = loadSave({});
  W.saveVals();
  await new Promise(r => setTimeout(r, 10));
  const up = W.__got();
  assert.ok(up, '저장되지 않았습니다');
  const key = Object.keys(up)[0];
  assert.match(key, /\/values\/202608\//);
  assert.equal(up[key].sourceId, 'a1', '출처가 없으면 나중에 근거를 못 댑니다');
  assert.equal(up[key].companyId, 'co_1');
  assert.equal(up[key].name, '배영승');
  assert.match(calls.alerts[0], /저장/);
});

test('★ 같은 사람 값이 이미 있으면 묻는다', async () => {
  const { W, calls } = loadSave(
    { r1: { companyId: 'co_1', month: '202608', name: '배영승' } }, false);
  W.saveVals();
  await new Promise(r => setTimeout(r, 10));
  assert.equal(calls.confirms.length, 1, '묻지 않으면 근무일수가 두 배가 됩니다');
  assert.match(calls.confirms[0], /배영승/);
  assert.equal(W.__got(), null, '「그대로 두기」인데 저장됐습니다');
});

test('덮어쓰기를 고르면 저장한다', async () => {
  const { W } = loadSave(
    { r1: { companyId: 'co_1', month: '202608', name: '배영승' } }, true);
  W.saveVals();
  await new Promise(r => setTimeout(r, 10));
  assert.ok(W.__got());
});

test('★ 남의 자리에서는 저장하지 않는다', async () => {
  const { W, calls } = loadSave({});
  W.App.viewingUid = 'U2'; W.App.viewingDeputy = false;
  W.saveVals();
  await new Promise(r => setTimeout(r, 10));
  assert.equal(W.__got(), null);
  assert.equal(calls.confirms.length, 0);
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --test tests/paydata-read-panel.test.js`
Expected: FAIL — `saveVals 함수를 찾을 수 없습니다`

- [ ] **Step 3: 구현**

`function readPanelHtml() {` **바로 위**에 넣는다:

```js
/* 확인한 값을 저장한다. 값마다 출처(이 서류 번호)가 붙는다 —
   buildValueRows 가 sourceId 없이는 거절한다. */
function saveVals() {
  if (!canWrite()) return;
  const st = App.readState;
  if (!st || st.status !== 'done' || !st.rows.length) return;
  const slot = S.monthKey(App.month) || S.KEEP;
  const tag = {
    sourceId: App.viewerId, companyId: App.companyId,
    companyName: App.companyName, slot: slot
  };
  let rows;
  try { rows = S.buildValueRows({ rows: st.rows }, tag); }
  catch (e) { alert(e.message); return; }

  S.listValues(slot, App.viewingUid).then(existing => {
    /* 같은 사업장·월·근로자가 이미 있으면 묻는다 — 캡처는 실제로 두 번 올라온다. */
    const dupOf = {};                       // 새 줄 번호 → 이미 있는 자리 번호
    rows.forEach(r => {
      const at = S.findDuplicateValue(existing, r.companyId, r.month, r.name);
      if (at) dupOf[r.id] = at;
    });
    const who = rows.filter(r => dupOf[r.id]).map(r => r.name);
    if (who.length) {
      if (!confirm('이미 있습니다 — 덮을까요?\n\n' + who.join(', ') + '\n\n「취소」를 누르면 그대로 둡니다.')) return null;
      /* ⚠ 「덮는다」는 **그 자리에 다시 쓴다**는 뜻이다. 새 번호로 쓰면 옛 줄이
         그대로 남아 근무일수가 두 배가 된다 — 물어본 보람이 없어지고, 사람은
         덮인 줄 알고 있으므로 아무도 의심하지 않는다. */
      rows.forEach(r => { if (dupOf[r.id]) r.id = dupOf[r.id]; });
    }
    return S.saveValues(slot, rows, App.viewingUid);
  }).then(n => {
    if (n === null || n === undefined) return;
    App.readState = { status: 'idle', rows: [], err: '' };
    alert(n + '줄을 저장했습니다. 「이 달 값 보기」에서 볼 수 있습니다.');
    App.render();
  }).catch(e => alert('저장하지 못했습니다 — ' + (e.code || e.message)));
}
window.saveVals = saveVals;
```

- [ ] **Step 4: 통과 확인**

Run: `node --test tests/paydata-read-panel.test.js`
Expected: PASS 19/19

- [ ] **Step 5: 전체 검사**

Run: `node --test tests/*.test.js > /tmp_t.txt 2>&1; echo "EXIT:$?"; tail -8 /tmp_t.txt`
Expected: `EXIT:0`

- [ ] **Step 6: 커밋**

```bash
git add pu-paydata.html tests/paydata-read-panel.test.js
git commit -m "feat: 값 저장과 중복 확인"
```

---

### Task 6: 월별 값 표 — 사람이 한 줄, 항목이 가로

**Files:**
- Modify: `pu-paydata.html`
- Test: `tests/paydata-value-grid.test.js`

**Interfaces:**
- Consumes: `S.listValues(slot, owner)`
- Produces:
  - `valueGridModel(valueRows)` → `{items:[string], people:[{name, cells:{[item]:{value, sourceId, confirmed}}}]}`
  - `screenValues()` → 화면 HTML · `ensureValues()` · `App.values`

- [ ] **Step 1: 실패하는 검사를 쓴다**

`tests/paydata-value-grid.test.js`:

```js
'use strict';
// 월별 값 표 — 사람이 한 줄, 항목이 가로. 실행: node --test tests/*.test.js
const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');
const vm = require('node:vm');

const R = path.join(__dirname, '..');
const html = fs.readFileSync(path.join(R, 'pu-paydata.html'), 'utf8');
const store = fs.readFileSync(path.join(R, 'js', 'pu-paydata-store.js'), 'utf8');

function cut(name) {
  const m = html.match(new RegExp('function ' + name + '\\s*\\([\\s\\S]*?\\n\\}'));
  assert.ok(m, name + ' 함수를 찾을 수 없습니다');
  return m[0];
}

function loadApp(appState) {
  const sandbox = { window: {}, console, Date, document: { getElementById: () => null } };
  sandbox.globalThis = sandbox;
  vm.createContext(sandbox);
  new vm.Script(store, { filename: 'store.js' }).runInContext(sandbox);
  new vm.Script([
    'const S = window.PuPaydataStore; S.init({uid:"U1"});',
    'const App = ' + JSON.stringify(Object.assign({
      companyId: 'co_1', companyName: '다온원', month: '2026-08', values: {}
    }, appState)) + ';',
    cut('esc'), cut('valueGridModel'), cut('screenValues'),
    'window.App = App; window.valueGridModel = valueGridModel; window.screenValues = screenValues;'
  ].join('\n'), { filename: 'app.js' }).runInContext(sandbox);
  return sandbox.window;
}

const VALS = {
  v1: { companyId: 'co_1', month: '202608', name: '배영승', sourceId: 'a1', confirmed: false,
        pairs: [{ item: '유급일수', value: '3일' }, { item: '기본급', value: '3,200,000' }] },
  v2: { companyId: 'co_1', month: '202608', name: '이옥자', sourceId: 'a2', confirmed: true,
        pairs: [{ item: '유급일수', value: '3일' }] }
};

test('★ 사람이 한 줄로 모인다', () => {
  const W = loadApp({});
  const g = W.valueGridModel(VALS);
  assert.equal(g.people.length, 2);
  assert.equal(g.people[0].name, '배영승');
});

test('★ 항목이 가로 열이 된다 — 처음 나온 차례대로', () => {
  const W = loadApp({});
  const g = W.valueGridModel(VALS);
  assert.equal(g.items.join(','), '유급일수,기본급');
});

test('★ 값마다 출처가 남는다', () => {
  const W = loadApp({});
  const g = W.valueGridModel(VALS);
  assert.equal(g.people[0].cells['유급일수'].sourceId, 'a1');
});

test('★ 같은 사람의 값은 한 줄로 합쳐진다 — 서류가 달라도', () => {
  const W = loadApp({});
  const g = W.valueGridModel({
    v1: { name: '배영승', sourceId: 'a1', pairs: [{ item: '유급일수', value: '3일' }] },
    v2: { name: '배영승', sourceId: 'a2', pairs: [{ item: '기본급', value: '1' }] }
  });
  assert.equal(g.people.length, 1);
  assert.equal(g.items.length, 2);
});

test('자료가 없어도 터지지 않는다', () => {
  const W = loadApp({});
  assert.equal(W.valueGridModel(null).people.length, 0);
  assert.equal(W.valueGridModel({}).items.length, 0);
});

test('★ 없는 항목은 0 이 아니라 － 로 보인다', () => {
  const W = loadApp({ values: VALS });
  const h = W.screenValues();
  assert.match(h, /－/, '0 으로 두면 「0원」과 「안 왔음」이 구별되지 않습니다');
});

test('★ 확인 안 된 값은 노랗게', () => {
  const W = loadApp({ values: VALS });
  assert.match(W.screenValues(), /class="[^"]*iffy/);
});

test('★ 값을 누르면 출처 원본이 열린다', () => {
  const W = loadApp({ values: VALS });
  assert.match(W.screenValues(), /openViewer\('a1'\)/);
});

test('값이 없으면 빈 안내를 보여준다', () => {
  const W = loadApp({ values: {} });
  assert.match(W.screenValues(), /아직 정리된 값이 없습니다/);
});

test('★ 서랍에서 「이 달 값 보기」로 들어갈 수 있다', () => {
  assert.match(html, /App\.go\(\\?'values\\?'/, '들어갈 길이 없으면 만든 화면이 아닙니다');
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --test tests/paydata-value-grid.test.js`
Expected: FAIL — `valueGridModel 함수를 찾을 수 없습니다`

- [ ] **Step 3: 구현**

`function screenDrawer() {` **바로 위**에 넣는다:

```js
/* ══════ 월별 값 표 (설계서 4장) ══════
   값은 종류를 가로지른다 — 근태에서 나온 값과 급여대장에서 나온 값이 한 표에
   섞이므로 서랍 탭이 아니라 별도 화면이다.
   사람이 한 줄, 항목이 가로. 바깥 급여프로그램에 넣기 좋은 모양이다. */
function valueGridModel(valueRows) {
  const box = valueRows || {};
  const items = [], seenItem = {};
  const people = [], byName = {};
  Object.keys(box).forEach(id => {
    const r = box[id];
    if (!r || !r.name) return;
    let p = byName[r.name];
    if (!p) { p = byName[r.name] = { name: r.name, cells: {} }; people.push(p); }
    (r.pairs || []).forEach(pr => {
      if (!pr || !pr.item) return;
      if (!seenItem[pr.item]) { seenItem[pr.item] = 1; items.push(pr.item); }
      p.cells[pr.item] = { value: pr.value, sourceId: r.sourceId || '', confirmed: !!r.confirmed };
    });
  });
  return { items: items, people: people };
}

function ensureValues() {
  const slot = S.monthKey(App.month) || S.KEEP;
  return S.listValues(slot, App.viewingUid).then(v => {
    /* 이 사업장 것만 남긴다 — 값 칸은 그 달 전체가 한 자리에 있다. */
    const mine = {};
    Object.keys(v || {}).forEach(id => {
      if (v[id] && v[id].companyId === App.companyId) mine[id] = v[id];
    });
    App.values = mine; App.render();
  }).catch(e => { console.warn('[값]', e && e.code); App.values = {}; App.render(); });
}

function screenValues() {
  const g = valueGridModel(App.values);
  const iffy = g.people.reduce((n, p) =>
    n + Object.keys(p.cells).filter(k => !p.cells[k].confirmed).length, 0);

  let h = '<div style="margin-bottom:10px"><button class="btn" onclick="App.go(\'drawer\')">← 서랍으로</button></div>'
    + '<h1>' + esc(App.companyName || App.companyId) + ' — ' + esc(App.month) + ' 값</h1>'
    + '<div class="sub">근로자 ' + g.people.length + '명 · 항목 ' + g.items.length + '가지</div>';

  if (!g.people.length) {
    return h + '<div class="card"><div class="empty">아직 정리된 값이 없습니다.<br>'
      + '서랍에서 서류를 열고 「판독하기」를 눌러 주세요.</div></div>';
  }

  if (iffy) {
    h += '<div class="warnline" style="margin-bottom:12px">⚠ <b>확인 안 된 값이 ' + iffy
      + '개</b> 있습니다. 노란 칸을 눌러 원본과 견주어 주세요.</div>';
  }

  h += '<div class="bar" style="display:flex;gap:8px;margin-bottom:12px">'
    + '<button class="btn" onclick="valuesCsv()">⬇ 엑셀로 내려받기</button>'
    + '<button class="btn" onclick="valuesCopy()">📋 표 복사</button></div>';

  h += '<div class="card" style="overflow-x:auto"><table class="grid">'
    + '<tr><th>근로자</th>' + g.items.map(i => '<th>' + esc(i) + '</th>').join('') + '</tr>';
  g.people.forEach(p => {
    h += '<tr><td class="who">' + esc(p.name) + '</td>'
      + g.items.map(i => {
        const c = p.cells[i];
        if (!c) return '<td class="num">－</td>';
        return '<td class="num src' + (c.confirmed ? '' : ' iffy') + '"'
          + (c.sourceId ? ' onclick="openViewer(\'' + esc(c.sourceId) + '\')"' : '')
          + '>' + esc(c.value) + '</td>';
      }).join('') + '</tr>';
  });
  h += '</table></div>';
  return h;
}
```

`App.go` 의 화면 준비 줄에 값 화면을 더한다. `if (screen === 'trash') ensureTrash();` **아래**에 넣는다:

```js
    if (screen === 'values') ensureValues();
```

`App.render()` 의 화면 가르기에 더한다. `else if (App.screen === 'trash')` **위**에 넣는다:

```js
    else if (App.screen === 'values') { m.innerHTML = banner + screenValues(); }
```

`App` 상태에 `values: {},` 를 더한다(`trash: null,` 옆).

서랍 화면에 들어가는 길을 만든다. `screenDrawer()` 안 「이 달 값 급여관리로 넘기기」
단추를 그리는 자리를 아래로 바꾼다.

바꾸기 전:
```js
    if (S.canHandoffPayroll()) {
      h += '<div style="margin-bottom:12px"><button class="btn" onclick="handoffMonth()">'
        + '📤 이 달 값 급여관리로 넘기기</button></div>';
    }
```

바꾼 뒤:
```js
    h += '<div style="display:flex;gap:8px;margin-bottom:12px">'
      + '<button class="btn" onclick="App.go(\'values\')">📋 이 달 값 보기</button>'
      + (S.canHandoffPayroll()
        ? '<button class="btn" onclick="handoffMonth()">📤 급여관리에 알리기</button>' : '')
      + '</div>';
```

꾸밈을 CSS 맨 뒤에 더한다:

```css
table.grid{width:100%;border-collapse:collapse;font-size:12.5px}
table.grid th{background:var(--paper);border-bottom:1px solid var(--line);padding:9px 8px;text-align:left;font-size:11.5px;color:var(--sub);white-space:nowrap}
table.grid td{border-bottom:1px solid var(--line);padding:8px}
table.grid tr:last-child td{border-bottom:none}
table.grid td.num{text-align:right;font-variant-numeric:tabular-nums}
table.grid td.who{font-weight:700}
table.grid td.src{cursor:pointer;color:var(--navy);text-decoration:underline dotted;text-underline-offset:3px}
table.grid td.src:hover{background:#F5F7FF}
table.grid td.iffy{background:#FDF6E3}
```

- [ ] **Step 4: 통과 확인**

Run: `node --test tests/paydata-value-grid.test.js`
Expected: PASS 10/10

- [ ] **Step 5: 전체 검사**

Run: `node --test tests/*.test.js > /tmp_t.txt 2>&1; echo "EXIT:$?"; tail -8 /tmp_t.txt`
Expected: `EXIT:0` — 서랍 검사(`tests/paydata-seat.test.js`)가 「넘기기」 단추 문구를 보고 있으면 함께 고친다

- [ ] **Step 6: 커밋**

```bash
git add pu-paydata.html tests/paydata-value-grid.test.js
git commit -m "feat: 월별 값 표 — 사람이 한 줄, 항목이 가로"
```

---

### Task 7: 내보내기 — 엑셀 내려받기와 표 복사

**Files:**
- Modify: `pu-paydata.html`
- Test: `tests/paydata-value-grid.test.js` (이어 붙임)

**Interfaces:**
- Consumes: `valueGridModel(valueRows)` (Task 6)
- Produces: `csvEsc(v)` · `valuesTable()` → `{head:[], body:[[]], title:[]}` · `valuesCsv()` · `valuesCopy()`

- [ ] **Step 1: 검사를 이어 붙인다**

```js
/* ══════ 내보내기 ══════ */
function loadOut() {
  const saved = { blob: null, name: '', copied: '' };
  const sandbox = {
    window: {}, console, Date, Blob: function (parts, o) { this.parts = parts; this.type = o && o.type; },
    document: { getElementById: () => null },
    navigator: { clipboard: { writeText: t => { saved.copied = t; return Promise.resolve(); } } },
    alert: () => {}
  };
  sandbox.globalThis = sandbox;
  vm.createContext(sandbox);
  new vm.Script(store, { filename: 'store.js' }).runInContext(sandbox);
  new vm.Script([
    'const S = window.PuPaydataStore; S.init({uid:"U1"});',
    'const App = ' + JSON.stringify({
      companyId: 'co_1', companyName: '다온원', month: '2026-08',
      values: {
        v1: { companyId: 'co_1', name: '배영승', sourceId: 'a1',
              pairs: [{ item: '유급일수', value: '3일' }, { item: '비고', value: '가,나' }] }
      }
    }) + ';',
    'function saveBlob(b, n){ __b = b; __n = n; }',
    'var __b = null, __n = "";',
    cut('esc'), cut('valueGridModel'), cut('csvEsc'), cut('valuesTable'),
    cut('valuesCsv'), cut('valuesCopy'),
    'window.App = App; window.csvEsc = csvEsc; window.valuesTable = valuesTable;',
    'window.valuesCsv = valuesCsv; window.valuesCopy = valuesCopy;',
    'window.__blob = function(){ return __b; }; window.__name = function(){ return __n; };'
  ].join('\n'), { filename: 'out.js' }).runInContext(sandbox);
  return { W: sandbox.window, saved };
}

test('★ 쉼표 든 칸을 감싼다 — 안 감싸면 열이 밀린다', () => {
  const { W } = loadOut();
  assert.equal(W.csvEsc('가,나'), '"가,나"');
  assert.equal(W.csvEsc('그냥'), '그냥');
  assert.equal(W.csvEsc('말"표'), '"말""표"');
});

test('★ 표 머리에 근로자와 항목이 차례대로 온다', () => {
  const { W } = loadOut();
  const t = W.valuesTable();
  assert.equal(t.head.join(','), '근로자,유급일수,비고');
  assert.equal(t.body[0][0], '배영승');
});

test('★ 엑셀에서 한글이 안 깨진다 (BOM)', () => {
  const { W } = loadOut();
  W.valuesCsv();
  const b = W.__blob();
  assert.ok(b, '내려받지 않았습니다');
  assert.match(b.parts[0], /^﻿/, 'BOM 이 없으면 엑셀에서 한글이 깨집니다');
  assert.match(W.__name(), /다온원/);
  assert.match(W.__name(), /\.csv$/);
});

test('★ 복사는 탭으로 나눈다 — 엑셀에 바로 붙는다', () => {
  const { W, saved } = loadOut();
  W.valuesCopy();
  assert.match(saved.copied, /근로자\t유급일수/);
  assert.match(saved.copied, /배영승\t3일/);
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --test tests/paydata-value-grid.test.js`
Expected: FAIL — `csvEsc 함수를 찾을 수 없습니다`

- [ ] **Step 3: 구현**

`function screenValues() {` **바로 아래**(그 함수 끝 뒤)에 넣는다:

```js
/* 쉼표·따옴표·줄바꿈이 든 칸을 감싼다 — 안 감싸면 엑셀에서 열이 밀린다.
   (사진첩 근태표에서 쓰던 것과 같은 방식) */
function csvEsc(v) {
  const s = String(v == null ? '' : v);
  return /[",\n]/.test(s) ? '"' + s.replace(/"/g, '""') + '"' : s;
}

/* 내보낼 표 한 벌 — 내려받기와 복사가 같은 것을 쓴다(두 벌로 두면 어긋난다). */
function valuesTable() {
  const g = valueGridModel(App.values);
  return {
    title: [App.companyName + ' ' + App.month + ' 값'],
    head: ['근로자'].concat(g.items),
    body: g.people.map(p => [p.name].concat(g.items.map(i => (p.cells[i] ? p.cells[i].value : ''))))
  };
}

function valuesCsv() {
  const t = valuesTable();
  const lines = [t.title.join('')].concat([t.head.join(',')])
    .concat(t.body.map(r => r.map(csvEsc).join(',')));
  const name = ('값_' + App.companyName + '_' + App.month).replace(/[\\/:*?"<>|\s]+/g, '_');
  /* ﻿ 는 BOM — 없으면 엑셀이 한글을 깨뜨린다 */
  saveBlob(new Blob(['﻿' + lines.join('\r\n')], { type: 'text/csv;charset=utf-8' }), name + '.csv');
}

function valuesCopy() {
  const t = valuesTable();
  const tsv = [t.head.join('\t')].concat(t.body.map(r => r.join('\t'))).join('\n');
  navigator.clipboard.writeText(tsv).then(
    () => alert('표를 복사했습니다 — 엑셀이나 급여프로그램에 붙여넣으세요.'),
    () => alert(tsv));
}
window.valuesCsv = valuesCsv; window.valuesCopy = valuesCopy;
```

`saveBlob` 이 이 앱에는 없다. `function csvEsc(v) {` **바로 위**에 넣는다:

```js
/* 만든 파일을 내려받게 한다. 주소는 바로 되돌려 준다 — 안 하면 기억이 샌다. */
function saveBlob(blob, name) {
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url; a.download = name;
  document.body.appendChild(a); a.click();
  document.body.removeChild(a);
  setTimeout(() => URL.revokeObjectURL(url), 1000);
}
```

- [ ] **Step 4: 통과 확인**

Run: `node --test tests/paydata-value-grid.test.js`
Expected: PASS 14/14

- [ ] **Step 5: 전체 검사**

Run: `node --test tests/*.test.js > /tmp_t.txt 2>&1; echo "EXIT:$?"; tail -8 /tmp_t.txt`
Expected: `EXIT:0`

- [ ] **Step 6: 커밋**

```bash
git add pu-paydata.html tests/paydata-value-grid.test.js
git commit -m "feat: 값 표 내보내기 — 엑셀 내려받기와 표 복사"
```

---

### Task 8: 급여관리 알림 문구를 「값 N줄」로 바로잡는다

지금은 **서류 장수**를 세어 「자료 N건」이라 보낸다. 급여관리가 보는 것은
「이 사업장 이 달 자료가 정리됐나」이므로 **값 줄 수**가 맞다(설계서 4장).

**Files:**
- Modify: `pu-paydata.html` (`handoffMonth`)
- Modify: `js/pu-paydata-store.js` (`handoffToPayroll` 의 `종류`·`filename`)
- Test: `tests/paydata-payroll-handoff-screen.test.js` (고침)

**Interfaces:**
- Consumes: `valueGridModel(valueRows)` (Task 6), `S.listValues(slot, owner)`
- Produces: 없음(있는 것 고침)

- [ ] **Step 1: 있는 검사를 고친다**

`tests/paydata-payroll-handoff-screen.test.js` 에서 아래 검사를 바꾼다.

바꾸기 전:
```js
test('확인 문구에 업체명·월·건수가 들어간다', () => {
```
그 안의
```js
  assert.match(calls.confirms[0], /1건/);
```

바꾼 뒤:
```js
  assert.match(calls.confirms[0], /1줄/, '급여관리가 보는 것은 서류 장수가 아니라 값 줄 수입니다');
```

그리고 `loadApp` 의 `App` 에 값을 더한다(`itemsMonth` 옆):

⚠ **한 곳이 아니라 세 곳이다.** 같은 파일의 아래 두 검사도 옛 셈(서류 장수)에
기대고 있어서, 값으로 세게 바뀌면 0 줄이 되어 confirm 까지 가지 못한다.

- `확인을 취소하면 저장하지 않는다`
- `★ 확인하면 급여관리 수신함과 handoff_log 에 저장되고 성공을 알린다`

이 둘에도 **검사마다 따로** 같은 모양의 `values` 를 넣는다.
`loadApp` 의 공용 기본값으로 올리면 안 된다 — 그러면
`★ 이 달에 넘길 값이 없으면 알리고 묻지 않는다` 가 0 줄이 아니게 되어
그 검사가 지키려던 것이 무너진다(실제로 그렇게 해 보고 깨진 것을 확인했다).
```js
      values: { v1: { companyId: 'co_1', name: '배영승', pairs: [{ item: '유급일수', value: '3일' }] } },
```

`cut('valueGridModel'),` 을 `cut('screenDrawer'), cut('handoffMonth'),` **앞**에 더한다.

- [ ] **Step 2: 실패 확인**

Run: `node --test tests/paydata-payroll-handoff-screen.test.js`
Expected: FAIL — `1줄` 을 못 찾는다

- [ ] **Step 3: 구현**

`handoffMonth()` 를 바꾼다.

바꾸기 전:
```js
  const counts = drawerCounts(App.itemsKeep, App.itemsMonth, App.companyId);
  const total = S.KINDS.filter(k => !k.keep).reduce((n, k) => n + (counts[k.key] || 0), 0);
  if (!total) { alert('이 달에 넘길 값이 없습니다.'); return; }
  if (!confirm(App.companyName + ' ' + App.month + ' 자료(' + total + '건)를 급여관리 수신함으로 넘길까요?')) return;
  S.handoffToPayroll({ companyId: App.companyId, companyName: App.companyName, month: App.month })
```

바꾼 뒤:
```js
  /* 서류 장수가 아니라 **값 줄 수**를 센다 — 급여관리가 보는 것은
     「이 사업장 이 달 자료가 정리됐나」다(설계서 4장). */
  const g = valueGridModel(App.values);
  const total = g.people.reduce((n, p) => n + Object.keys(p.cells).length, 0);
  if (!total) { alert('이 달에 정리된 값이 없습니다.\n먼저 서류를 판독해 값을 만들어 주세요.'); return; }
  if (!confirm(App.companyName + ' ' + App.month + ' 값(' + total + '줄)이 정리됐다고 급여관리에 알릴까요?\n\n값 자체는 넘어가지 않습니다 — 계산은 바깥 프로그램에서 하십니다.')) return;
  S.handoffToPayroll({ companyId: App.companyId, companyName: App.companyName, month: App.month, rowCount: total })
```

`js/pu-paydata-store.js` 의 `handoffToPayroll` 안을 바꾼다.

바꾸기 전:
```js
      filename: o.companyName + ' ' + o.month + ' 값',
      사업장: o.companyName,
      월: o.month,
      종류: o.kindLabel || '급여데이터함 값',
```

바꾼 뒤:
```js
      filename: o.companyName + ' ' + o.month + ' 값 ' + Number(o.rowCount || 0) + '줄',
      사업장: o.companyName,
      월: o.month,
      종류: o.kindLabel || '급여데이터함 값',
      줄수: Number(o.rowCount || 0),
```

- [ ] **Step 4: 통과 확인**

Run: `node --test tests/paydata-payroll-handoff-screen.test.js tests/paydata-payroll-handoff.test.js`
Expected: PASS

- [ ] **Step 5: 전체 검사**

Run: `node --test tests/*.test.js > /tmp_t.txt 2>&1; echo "EXIT:$?"; tail -8 /tmp_t.txt`
Expected: `EXIT:0`

- [ ] **Step 6: 눈으로 확인**

`docs/mockups/paydata-read-value.html` 과 `docs/mockups/paydata-value-table.html` 을
브라우저로 열어 실제 화면과 견준다. 다르면 목업이 아니라 **화면을 고친다**(목업이 승인받은 것이다).

- [ ] **Step 7: 커밋**

```bash
git add pu-paydata.html js/pu-paydata-store.js tests/paydata-payroll-handoff-screen.test.js
git commit -m "fix: 급여관리 알림을 서류 장수가 아니라 값 줄 수로"
```

---

## 마무리

- [ ] `git fetch origin main && git rebase origin/main`
- [ ] `node --test tests/*.test.js > /tmp_t.txt 2>&1; echo "EXIT:$?"` → `EXIT:0`
- [ ] 새 가지로 push 하고 PR — 본문에 **급여대장 판독은 꺼 둔 상태**임을 적는다
- [ ] CI 통과 확인 후 병합
