# 자료함 격자 편집기 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 한글 서식을 세로 목록이 아니라 **원래 표 모양 그대로** 큰 팝업에 펼쳐 한 화면에서 고칠 수 있게 한다.

**Architecture:** `js/pu-hwp-edit.js` 에 순수 함수 `readGrid(doc)` 를 더한다 — 표의 행·열·합친 칸을 `getCellInfo` 에서 받아 격자를 되살리고, 고칠 수 있는 줄들은 기존 `readRows` 와 **같은 모양**으로 내준다. 그래야 이미 검사를 통과한 `changedRows()`/`applyRows()` 를 한 줄도 고치지 않고 그대로 쓴다. `pu-cards.html` 에는 큰 팝업(`.mged`)을 새로 만들어 그 격자를 HTML `<table>` 로 그린다.

**Tech Stack:** 빌드 없는 단일 HTML + 브라우저 표준 JS (ES2020). 검사는 `node:test` + `node:assert`. 한글 엔진은 `vendor/rhwp-core` (WASM) — 검사에서는 띄우지 않고 **가짜 문서**를 넣는다.

## Global Constraints

- **`readRows()` 를 지우거나 고치지 않는다.** 지금 화면(`heRenderRows`)이 쓰고 있고 `tests/hwp-edit.test.js` 가 검사한다. `readGrid()` 는 **옆에 새로 놓는다.**
- **`readGrid().units` 의 각 원소는 `readRows()` 가 내주던 줄과 같은 열쇠를 가진다** — `kind`(`'body'`|`'cell'`), `sec`, `para`, `ctrl`, `cell`, `cpara`, `text`, `len`, `no`. `changedRows()`/`applyRows()` 가 이 열쇠들을 쓴다.
- **행·열·합친 칸은 반드시 `getCellInfo` 에서 받는다.** `Math.floor(idx/cols)` 같은 짐작을 새로 쓰지 않는다.
- **`getStructure()` 를 부르지 않는다.** 실제로 `memory access out of bounds` 로 터진다.
- **`PureunHwp.renderPreview()` 에는 `_he.doc` 가 아니라 `bytes` 를 넘긴다.** 문서를 넘기면 그 문서가 죽는다.
- 파일은 UTF-8. 주석과 화면 글은 한국어. 이 저장소의 말투를 따른다 — 무엇을 하는지가 아니라 **왜 그렇게 했는지**를 적는다.
- 검사는 `node --test tests/*.test.js` 로 전부 돌려 **하나도 실패하지 않아야** 한다. 지금 1,607개가 통과한다.
- 커밋 메시지는 한국어. `feat(자료함): …` 꼴.

---

### Task 1: `readGrid()` — 표를 표로 읽기

**Files:**
- Modify: `js/pu-hwp-edit.js` (`CTRL_PROBE` 상수, 끝의 `api` 객체)
- Test: `tests/hwp-edit.test.js` (`fakeDoc` 넓히기 + 검사 더하기)

**Interfaces:**
- Consumes: 없음 (첫 과제)
- Produces:
  - `PuHwpEdit.readGrid(doc)` → `{ units, blocks, warn }`
    - `units`: `Array<{kind:'body'|'cell', sec:number, para:number, ctrl?:number, cell?:number, cpara?:number, text:string, len:number, no:number}>` — `no` 는 1 부터
    - `blocks`: `Array<{kind:'body', unit:number} | {kind:'table', sec, para, ctrl, rows:number, cols:number, cells:Array<{cell:number, row:number|null, col:number|null, rowSpan:number, colSpan:number, units:number[]}>}>`
    - `warn`: `{badCellInfo:number, textBoxes:number}`
  - `CTRL_PROBE` 가 `8` 이 된다

- [ ] **Step 1: 가짜 문서에 `getCellInfo` 와 글상자를 더한다**

`tests/hwp-edit.test.js` 의 `fakeDoc` 안, `getTextInCell` 바로 뒤에 넣는다. 기존 검사들은 이 두 함수를 안 쓰므로 그대로 통과한다.

```javascript
    /* 진짜 엔진은 행·열·합친 칸을 여기서 알려준다.
       spec.tables['0-0'].info[c] 로 칸마다 값을 정할 수 있고,
       'fail' 이라고 적으면 엔진이 못 읽는 경우를 흉내낸다. */
    getCellInfo: (s, p, ctrl, c) => {
      const t = tables[key(s, p)];
      const want = t.info && t.info[c];
      if (want === 'fail') throw new Error('칸 자리를 못 읽습니다');
      if (want) return JSON.stringify(want);
      const cols = t.colCount || 1;
      return JSON.stringify({ row: Math.floor(c / cols), col: c % cols, rowSpan: 1, colSpan: 1 });
    },
    /* 글상자가 있는 문단을 spec.textBoxes 에 '구역-줄' 로 적는다 */
    getTextBoxControlIndex: (s, p) => ((spec.textBoxes || []).indexOf(s + '-' + p) >= 0 ? 0 : -1),
```

- [ ] **Step 2: 실패하는 검사를 쓴다**

`tests/hwp-edit.test.js` 맨 끝에 붙인다.

```javascript
/* ══════════ readGrid — 표를 표로 읽기 ══════════ */

test('표가 blocks 에 표 하나로 들어오고 칸마다 행·열이 붙는다', () => {
  const doc = fakeDoc({
    body: [['급여대장']],
    tables: { '0-0': { ctrl: 0, rowCount: 2, colCount: 2, cells: [['이름'], ['급여'], ['윤성인'], ['700만원']] } }
  });
  const g = E.readGrid(doc);
  const tbl = g.blocks.filter(b => b.kind === 'table');
  assert.equal(tbl.length, 1);
  assert.equal(tbl[0].rows, 2);
  assert.equal(tbl[0].cols, 2);
  assert.deepEqual(tbl[0].cells.map(c => [c.row, c.col]), [[0, 0], [0, 1], [1, 0], [1, 1]]);
});

test('합친 칸은 getCellInfo 가 준 rowSpan·colSpan 을 그대로 쓴다', () => {
  /* 나눗셈으로 짐작하면 여기서 전부 어긋난다 — 서식은 거의 다 합친 칸이다 */
  const doc = fakeDoc({
    body: [['']],
    tables: { '0-0': { ctrl: 0, rowCount: 2, colCount: 4, cells: [['머리말'], ['왼쪽'], ['오른쪽']],
      info: [{ row: 0, col: 0, rowSpan: 1, colSpan: 4 },
             { row: 1, col: 0, rowSpan: 1, colSpan: 2 },
             { row: 1, col: 2, rowSpan: 1, colSpan: 2 }] } }
  });
  const t = E.readGrid(doc).blocks.find(b => b.kind === 'table');
  assert.deepEqual(t.cells.map(c => [c.row, c.col, c.rowSpan, c.colSpan]),
    [[0, 0, 1, 4], [1, 0, 1, 2], [1, 2, 1, 2]]);
});

test('자리를 못 읽은 칸도 사라지지 않고 row 가 null 로 남는다', () => {
  /* 조용히 빼면 그 칸은 영영 못 고친다 */
  const doc = fakeDoc({
    body: [['']],
    tables: { '0-0': { ctrl: 0, rowCount: 1, colCount: 2, cells: [['가'], ['나']], info: [null, 'fail'] } }
  });
  const g = E.readGrid(doc);
  const t = g.blocks.find(b => b.kind === 'table');
  assert.equal(t.cells.length, 2);
  assert.equal(t.cells[1].row, null);
  assert.equal(t.cells[1].colSpan, 1);
  assert.equal(g.warn.badCellInfo, 1);
  assert.equal(g.units.filter(u => u.text === '나').length, 1);
});

test('문서 전체가 표 한 칸이어도 문단마다 고칠 줄이 생긴다', () => {
  /* 실제 위임장이 이렇다 — 칸 2개에 문단이 9개·14개.
     칸을 한 줄로 그리면 두 줄짜리 문서가 되어 아무것도 못 고친다. */
  const doc = fakeDoc({
    body: [['']],
    tables: { '0-0': { ctrl: 0, rowCount: 2, colCount: 1,
      cells: [['위 임 장', '성 명 :', '주 소 :'], ['다 음', '1. 등기신청']] } }
  });
  const g = E.readGrid(doc);
  const t = g.blocks.find(b => b.kind === 'table');
  assert.equal(t.cells[0].units.length, 3);
  assert.equal(t.cells[1].units.length, 2);
  assert.deepEqual(t.cells[0].units.map(i => g.units[i].text), ['위 임 장', '성 명 :', '주 소 :']);
});

test('칸의 첫 줄은 비어 있어도 남고, 둘째 줄부터의 빈 줄은 뺀다', () => {
  /* 첫 줄이 빈 칸은 채울 자리다. 뒤의 빈 줄은 줄 간격용이라 번호만 늘어난다. */
  const doc = fakeDoc({
    body: [['']],
    tables: { '0-0': { ctrl: 0, rowCount: 1, colCount: 2, cells: [[''], ['값', '', '뒷줄']] } }
  });
  const g = E.readGrid(doc);
  const t = g.blocks.find(b => b.kind === 'table');
  assert.equal(t.cells[0].units.length, 1);
  assert.deepEqual(t.cells[1].units.map(i => g.units[i].text), ['값', '뒷줄']);
});

test('본문과 표가 문서에 나오는 순서대로 blocks 에 들어간다', () => {
  const doc = fakeDoc({
    body: [['머리말', '가운데말', '꼬리말']],
    tables: { '0-1': { ctrl: 0, rowCount: 1, colCount: 1, cells: [['표 안']] } }
  });
  const g = E.readGrid(doc);
  const seq = g.blocks.map(b => (b.kind === 'body' ? g.units[b.unit].text : '[표]'));
  assert.deepEqual(seq, ['머리말', '가운데말', '[표]', '꼬리말']);
});

test('units 는 readRows 와 같은 모양이라 changedRows·applyRows 가 그대로 먹는다', () => {
  /* 이것이 깨지면 격자로 읽어도 저장이 안 된다 */
  const doc = fakeDoc({
    body: [['계약서']],
    tables: { '0-0': { ctrl: 0, rowCount: 1, colCount: 2, cells: [['상 호'], ['']] } }
  });
  const g = E.readGrid(doc);
  const blank = g.units.findIndex(u => u.kind === 'cell' && u.text === '');
  const ch = E.changedRows(g.units, { [blank]: '천안가온신협' });
  assert.equal(ch.length, 1);
  const res = E.applyRows(doc, ch);
  assert.equal(res.ok, 1);
  assert.deepEqual(res.failed, []);
  assert.equal(E.readGrid(doc).units[blank].text, '천안가온신협');
});

test('아직 못 읽는 글상자는 세어서 알린다', () => {
  /* 조용히 빠지면 「고쳤는데 안 바뀌었다」가 된다 */
  const doc = fakeDoc({ body: [['본문', '글상자자리']], textBoxes: ['0-1'] });
  assert.equal(E.readGrid(doc).warn.textBoxes, 1);
});

test('표를 여덟 번째 자리까지 찾는다', () => {
  /* 그림이 앞에 끼면 표가 뒤로 밀린다. 넷까지만 보면 놓친다. */
  const doc = fakeDoc({
    body: [['']],
    tables: { '0-0': { ctrl: 6, rowCount: 1, colCount: 1, cells: [['늦게 나온 표']] } }
  });
  assert.equal(E.readGrid(doc).blocks.filter(b => b.kind === 'table').length, 1);
});
```

- [ ] **Step 3: 검사가 실패하는 것을 확인한다**

```bash
node --test tests/hwp-edit.test.js
```

Expected: FAIL — `TypeError: E.readGrid is not a function` (9개 검사가 깨진다)

- [ ] **Step 4: `readGrid` 를 만든다**

`js/pu-hwp-edit.js` 의 `CTRL_PROBE` 를 고친다.

```javascript
  /* 표 안을 들여다볼 때 몇 번째 물건까지 뒤져 볼지.
     표는 보통 첫 번째다. 그림·글상자가 앞에 끼면 뒤로 밀리므로 넉넉히 본다
     (실제 공문에서 표가 세 개 나왔다). 없는 자리는 던지고 지나가므로 값이 커도 손해가 없다. */
  const CTRL_PROBE = 8;
```

그리고 `cellPos` 함수 **바로 앞**에 넣는다.

```javascript
  /* ── 문서를 격자 그대로 읽기 ──
     readRows() 는 표 칸을 세로로 늘어놓는다. 서식은 거의 전부 표라서 그러면
     원래 모양이 사라진다 — 20행 × 3열 급여표가 60줄짜리 목록이 된다.
     여기서는 표를 **표로** 읽는다.

     units 는 readRows 가 내주던 줄과 **같은 모양**이다. 그래야 이미 검사를
     통과한 changedRows()/applyRows() 를 한 줄도 안 고치고 그대로 쓴다.
     blocks 는 화면에 그릴 차례와 모양만 담는다(글자는 units 에만 있다). */
  function readGrid(doc) {
    const units = [], blocks = [];
    const warn = { badCellInfo: 0, textBoxes: 0 };
    const secs = num(doc.getSectionCount());
    for (let s = 0; s < secs; s++) {
      const paras = num(doc.getParagraphCount(s));
      for (let p = 0; p < paras; p++) {
        let text = '';
        try { text = String(doc.getTextRange(s, p, 0, num(doc.getParagraphLength(s, p)))); } catch (_) { text = ''; }
        if (text.trim()) {
          blocks.push({ kind: 'body', unit: units.length });
          units.push({ kind: 'body', sec: s, para: p, text: text, len: text.length });
        }
        /* 글상자는 아직 못 읽는다. 세어서 화면에 알린다 —
           조용히 빠지면 「고쳤는데 안 바뀌었다」가 된다. */
        try {
          const tb = parseInt(doc.getTextBoxControlIndex(s, p), 10);
          if (isFinite(tb) && tb >= 0) warn.textBoxes++;
        } catch (_) {}
        pushTables(doc, units, blocks, warn, s, p);
      }
    }
    units.forEach(function (u, i) { u.no = i + 1; });
    return { units: units, blocks: blocks, warn: warn };
  }

  function pushTables(doc, units, blocks, warn, s, p) {
    for (let ctrl = 0; ctrl < CTRL_PROBE; ctrl++) {
      let dim = null;
      /* 표가 아니면 던진다 — 그림·글상자일 수 있으니 멈추지 말고 다음 것을 본다 */
      try { dim = JSON.parse(doc.getTableDimensions(s, p, ctrl)); } catch (_) { continue; }
      const total = num(dim && dim.cellCount);
      const cells = [];
      for (let c = 0; c < total; c++) {
        /* 행·열·합친 칸은 **반드시 엔진에서 받는다.** 칸 번호를 열 수로 나눠
           짐작하면 합친 칸이 하나만 있어도 그 뒤가 전부 어긋난다. */
        let inf = null;
        try { inf = JSON.parse(doc.getCellInfo(s, p, ctrl, c)); } catch (_) { inf = null; }
        if (!inf) warn.badCellInfo++;
        const idx = [];
        let cps = 0;
        try { cps = num(doc.getCellParagraphCount(s, p, ctrl, c)); } catch (_) { cps = 0; }
        for (let cp = 0; cp < cps; cp++) {
          let len = 0, text = '';
          try {
            len = num(doc.getCellParagraphLength(s, p, ctrl, c, cp));
            text = String(doc.getTextInCell(s, p, ctrl, c, cp, 0, len));
          } catch (_) { continue; }
          /* 칸의 첫 줄은 비어 있어도 남긴다 — 거기가 채울 자리다.
             둘째 줄부터의 빈 줄은 뺀다(줄 간격용이라 줄만 늘어난다). */
          if (cp > 0 && !text.trim()) continue;
          idx.push(units.length);
          units.push({ kind: 'cell', sec: s, para: p, ctrl: ctrl, cell: c, cpara: cp,
                       text: text, len: text.length });
        }
        cells.push({ cell: c,
                     row: inf ? int0(inf.row) : null,
                     col: inf ? int0(inf.col) : null,
                     rowSpan: num(inf && inf.rowSpan) || 1,
                     colSpan: num(inf && inf.colSpan) || 1,
                     units: idx });
      }
      blocks.push({ kind: 'table', sec: s, para: p, ctrl: ctrl,
                    rows: num(dim && dim.rowCount), cols: num(dim && dim.colCount),
                    cells: cells });
    }
  }

  /* num() 은 1 이상만 받는다(길이·개수용). 행·열은 0 부터라 따로 둔다. */
  function int0(v) { const n = parseInt(v, 10); return isFinite(n) ? n : 0; }
```

파일 끝의 `api` 객체에 더한다.

```javascript
  const api = { readRows: readRows, readGrid: readGrid, changedRows: changedRows, applyRows: applyRows,
                exportBytes: exportBytes, extOf: extOf, editedName: editedName, clean: clean,
                cellPos: cellPos, CTRL_PROBE: CTRL_PROBE };
```

- [ ] **Step 5: 검사가 통과하는 것을 확인한다**

```bash
node --test tests/hwp-edit.test.js
```

Expected: PASS — `fail 0`

- [ ] **Step 6: 검사를 전부 돌린다**

```bash
node --test tests/*.test.js
```

Expected: `fail 0` — 기존 1,607개가 그대로 통과해야 한다. `readRows` 검사가 깨지면 `readRows` 를 건드린 것이니 되돌린다.

- [ ] **Step 7: 커밋**

```bash
git add js/pu-hwp-edit.js tests/hwp-edit.test.js && git commit -m "feat(자료함): 표를 표로 읽는 readGrid — 행·열은 엔진에서 받는다"
```

---

### Task 2: 자료 바이트 읽기를 한 곳으로 모은다

**Files:**
- Modify: `pu-cards.html` (`matType` 상수 뒤에 함수 하나 새로 + 세 곳을 그것으로 바꿈)
- Test: `tests/cards-hwp-integration.test.js`

**Interfaces:**
- Consumes: `firebase.database()`, `DB_ROOT`, `_matMeta`
- Produces: `matBytes(id)` → `Promise<Uint8Array>` — 없으면 `throw`

**왜 먼저 하나:** 자료함 파일을 base64 에서 푸는 코드가 **똑같이 세 군데** 있다 (`downloadMaterial`, `fillMatPreview`, `previewMaterial`). 격자 편집기가 네 번째를 만들면 나중에 한 곳만 고쳐지는 일이 생긴다. 먼저 한 곳으로 모은다.

- [ ] **Step 1: 실패하는 검사를 쓴다**

```javascript
test('자료 바이트 읽기는 한 곳에만 있다', () => {
  /* 같은 코드가 여러 벌이면 한 곳만 고쳐져 어긋난다.
     읽기(once)는 한 곳이어야 한다 — 쓰기(set)·지우기(remove)는 그대로 둔다.
     charCodeAt 으로 세지 않는다: 자료함과 상관없는 _unb64 도 그것을 쓴다. */
  assert.match(source, /async function matBytes\(id\)/);
  const reads = (source.match(/materialFiles\/'\+id\)\.once\('value'\)/g) || []).length;
  assert.equal(reads, 1, '자료 파일을 읽는 곳이 아직 여러 곳입니다');
});

test('자료를 읽는 세 곳이 모두 matBytes 를 쓴다', () => {
  const each = ['async function downloadMaterial', 'async function fillMatPreview', 'async function previewMaterial'];
  each.forEach(head => {
    const at = source.indexOf(head);
    assert.ok(at > 0, head + ' 을 찾지 못했습니다');
    assert.match(source.slice(at, at + 2200), /await matBytes\(id\)/, head + ' 이 아직 직접 읽습니다');
  });
});

test('파일이 없으면 조용히 넘기지 않고 알린다', () => {
  const fn = source.slice(source.indexOf('async function matBytes'), source.indexOf('async function matBytes') + 600);
  assert.match(fn, /throw new Error/);
});
```

- [ ] **Step 2: 검사가 실패하는 것을 확인한다**

```bash
node --test tests/cards-hwp-integration.test.js
```

Expected: FAIL — `matBytes` 가 없고 `charCodeAt(i)` 가 3벌이다

- [ ] **Step 3: 함수를 만든다**

`const matType = n => MAT_TYPES[matExt(n)];` 줄 **바로 뒤**에 넣는다.

```javascript
/* 자료함에 올린 파일을 바이트로 읽는다.
   같은 코드가 세 군데(내려받기·미리보기·보기) 있어 한 곳으로 모았다 —
   여러 벌이면 한 곳만 고쳐져 어긋난다. */
async function matBytes(id){
  const s = await firebase.database().ref(DB_ROOT+'/materialFiles/'+id).once('value');
  const dataUrl = s.val();
  if(!dataUrl) throw new Error('파일 데이터가 없습니다 — 자료함에서 파일을 다시 올려주세요');
  const bin = atob(String(dataUrl).split(',')[1]||'');
  const arr = new Uint8Array(bin.length);
  for(let i=0;i<bin.length;i++) arr[i]=bin.charCodeAt(i);
  return arr;
}
```

- [ ] **Step 4: 세 곳을 이 함수로 바꾼다**

`downloadMaterial` 안 (`pu-cards.html:2536` 언저리) — 아래 네 줄을 지우고

```javascript
    const dataUrl = s.val();
    const bin = atob(dataUrl.split(',')[1]);
    const arr = new Uint8Array(bin.length);
    for(let i=0;i<bin.length;i++) arr[i]=bin.charCodeAt(i);
```

그 자리와 바로 위의 `firebase.database().ref(...).once('value')` 줄까지 함께 이것으로 바꾼다.

```javascript
    const arr = await matBytes(id);
```

`previewMaterial` 안 (`pu-cards.html:4270` 언저리) 도 똑같이 바꾼다.

```javascript
    const arr = await matBytes(id);
```

`fillMatPreview` 안 (`pu-cards.html:2805` 언저리) 은 **기다리는 사이 다른 것을 눌렀는지 보는 줄을 지키면서** 바꾼다.

```javascript
    const arr = await matBytes(id);
    if(state.matPrevId!==id) return;              /* 기다리는 사이 다른 것을 눌렀다 */
    if(_mpUrl){ try{URL.revokeObjectURL(_mpUrl)}catch(_){} _mpUrl=null; }
```

세 곳 모두 없어진 `dataUrl`·`bin` 변수를 뒤에서 쓰지 않는지 확인한다.

```bash
grep -n "dataUrl\|const bin" pu-cards.html
```

Expected: `matBytes` 안의 것만 남는다

- [ ] **Step 5: 문법과 검사를 확인한다**

```bash
node "$SCRATCH/jscheck.js" pu-cards.html && node --test tests/*.test.js
```

Expected: `ALL OK` 그리고 `fail 0`

- [ ] **Step 6: 커밋**

```bash
git add pu-cards.html tests/cards-hwp-integration.test.js && git commit -m "refactor(자료함): 파일 바이트 읽기를 matBytes 한 곳으로"
```

---

### Task 3: 큰 팝업에 격자를 그린다

**Files:**
- Modify: `pu-cards.html` (CSS 는 `.mat-hwp-back` 규칙 **뒤**, JS 는 `heBig()` 함수 **뒤**)
- Test: `tests/cards-hwp-integration.test.js`

**Interfaces:**
- Consumes: `PuHwpEdit.readGrid(doc)` → `{units, blocks, warn}` (Task 1) · `matBytes(id)` → `Promise<Uint8Array>` (Task 2) · `PureunHwp.openDoc(bytes, name)` → `Promise<doc>` · `matExt(name)`, `_matMeta`, `esc()`, `toast()`, `$()`
- Produces:
  - `window._mg` — `{id, name, bytes, doc, grid, edited, busy}` 또는 `null`
  - `openMatEditor(id)` — 자료 번호로 큰 팝업을 연다
  - `closeMatEditor()`
  - `mgRender()` / `mgGridHtml()` / `mgTableHtml(block)` / `mgInput(i, el)`
  - `mgChanged()` → `number`

- [ ] **Step 1: 실패하는 검사를 쓴다**

`tests/cards-hwp-integration.test.js` 끝에 붙인다. 이 저장소는 단일 HTML 이라 **글월이 있는지**로 검사한다(기존 검사와 같은 방식).

```javascript
test('큰 팝업이 표를 세로 목록이 아니라 표로 그린다', () => {
  assert.match(source, /function openMatEditor/);
  assert.match(source, /PuHwpEdit\.readGrid\(/);
  assert.match(source, /rowspan="\$\{/);
  assert.match(source, /colspan="\$\{/);
});

test('격자를 그릴 때 자리를 짐작하지 않는다', () => {
  /* Math.floor(i/cols) 를 새로 쓰면 합친 칸이 있는 서식이 전부 어긋난다 */
  const mg = source.slice(source.indexOf('function mgTableHtml'), source.indexOf('function mgInput'));
  assert.ok(mg.length > 200, 'mgTableHtml 을 찾지 못했습니다');
  assert.doesNotMatch(mg, /Math\.floor\([^)]*cols/);
});

test('자리를 못 읽은 칸은 표 아래에 따로 내놓는다', () => {
  assert.match(source, /자리를 모르는 칸/);
});

test('큰 팝업이 화면을 꽉 채운다', () => {
  assert.match(source, /\.mged\{position:fixed;inset:0/);
  assert.match(source, /\.mged-box\{width:min\(1240px,97vw\)/);
});
```

- [ ] **Step 2: 검사가 실패하는 것을 확인한다**

```bash
node --test tests/cards-hwp-integration.test.js
```

Expected: FAIL — 4개 모두 실패

- [ ] **Step 3: CSS 를 더한다**

`pu-cards.html` 에서 `@media(max-width:760px){.mat-hwp-back…` 줄 **바로 뒤**에 넣는다.

```css
/* 격자 편집기 — 서식을 원래 표 모양으로 펼쳐 한 화면에서 고친다.
   작은 서랍 칸에서는 표가 접혀 보이지 않아 큰 팝업으로 뺐다(대표 지시 2026-08-10). */
.mged{position:fixed;inset:0;z-index:140;background:rgba(0,0,0,.72);display:flex;align-items:center;justify-content:center;padding:18px}
.mged-box{width:min(1240px,97vw);height:min(900px,94vh);background:#fff;border-radius:14px;overflow:hidden;display:flex;flex-direction:column}
.mged-head{display:flex;align-items:center;gap:10px;padding:12px 16px;border-bottom:1px solid #e5e9f2;font-size:14.5px;font-weight:800;color:#1a2540}
.mged-head .x{margin-left:auto;border:0;background:transparent;font-size:20px;cursor:pointer;color:#64748b;line-height:1}
.mged-bar{display:flex;gap:6px;align-items:center;flex-wrap:wrap;padding:9px 16px;border-bottom:1px solid #eef1f7;background:#fafbfe}
.mged-bar button{border:1px solid #cbd5e6;background:#fff;border-radius:8px;padding:5px 11px;font-size:12.5px;font-weight:700;cursor:pointer;font-family:inherit;color:#28324a}
.mged-bar button:hover{background:#eef3fc}
.mged-bar button.go{background:#1d4fc4;border-color:#1d4fc4;color:#fff}
.mged-bar button:disabled{opacity:.45;cursor:default}
.mged-bar .cnt{margin-left:auto;font-size:12px;color:#5b6784}
.mged-body{flex:1;overflow:auto;padding:16px 18px;background:#f4f6fa}
.mged-note{padding:9px 16px;border-top:1px solid #eef1f7;background:#fffbeb;font-size:12px;color:#92400e;line-height:1.6}
/* 표 — table-layout:fixed 가 있어야 긴 글이 칸을 밀어내지 않는다 */
.mgtbl{border-collapse:collapse;width:100%;table-layout:fixed;background:#fff;margin:0 0 16px}
.mgtbl td{border:1px solid #c3cddf;padding:3px 4px;vertical-align:top}
.mgbody{background:#fff;border:1px solid #e2e8f4;border-radius:8px;padding:3px 5px;margin:0 0 10px}
.mgline{display:flex;align-items:flex-start;gap:5px}
.mgline+.mgline{margin-top:2px}
.mgline .no{flex:0 0 auto;min-width:26px;font-size:10.5px;color:#94a3b8;text-align:right;padding-top:6px;user-select:none}
.mgline textarea{flex:1;min-width:0;border:1px solid transparent;border-radius:5px;padding:4px 6px;font:inherit;font-size:12.5px;line-height:1.65;resize:none;overflow:hidden;background:transparent;color:#1a2540}
.mgline textarea:hover{border-color:#dbe3f0}
.mgline textarea:focus{outline:0;border-color:#1d4fc4;background:#f7faff}
.mgline.on textarea{background:#fff7e6;border-color:#f0b429}
.mgorph{background:#fff7e6;border:1px solid #f0b429;border-radius:8px;padding:8px 10px;margin:0 0 16px;font-size:12px;color:#92400e}
```

- [ ] **Step 4: 팝업을 만들고 격자를 그린다**

`function heBig(){ … }` 줄 **바로 뒤**에 넣는다.

```javascript
/* ══════════════ 격자 편집기 — 서식을 표 모양 그대로 ══════════════
   서랍의 세로 목록으로는 서식을 못 고친다. 서식은 거의 전부 표라서
   칸을 한 줄씩 늘어놓으면 원래 모양이 사라진다(20행×3열 급여표 → 60줄).
   여기서는 표를 표로 그려 한 화면에서 훑고 고친다.

   ⚠ 문서(_mg.doc)는 WASM 쪽 기억을 쓴다. 닫을 때 반드시 free() 한다.
   ⚠ 원본 모습은 renderPreview(el, _mg.bytes, …) 로 그린다. bytes 다.
     doc 을 넘기면 그 문서가 죽어 편집이 통째로 멈춘다. */
let _mg = null;   /* {id,name,bytes,doc,grid,edited,busy} */

function mgChanged(){
  return _mg ? PuHwpEdit.changedRows(_mg.grid.units, _mg.edited).length : 0;
}
function closeMatEditor(){
  if(_mg && mgChanged() && !confirm('고친 내용을 아직 저장하지 않았습니다.\n버리고 닫을까요?')) return;
  if(_mg){ try{ if(_mg.doc && _mg.doc.free) _mg.doc.free(); }catch(_){} }
  _mg=null;
  const el=$('mgedBack'); if(el) el.remove();
}

async function openMatEditor(id){
  const m = _matMeta[id]; if(!m) return toast('자료를 찾지 못했습니다');
  const name = m.fileName || m.name || '한글자료.hwp';
  if(!/^(hwp|hwpx)$/.test(matExt(name)))
    return toast('한글 문서(.hwp·.hwpx)만 고칠 수 있습니다 — PDF 는 글자를 바꿀 수 없습니다', 7000);
  if(_mg) closeMatEditor();
  if(_mg) return;                                  /* 닫기를 취소했다 */

  const back=document.createElement('div');
  back.className='mged'; back.id='mgedBack';
  back.innerHTML='<div class="mged-box" role="dialog" aria-modal="true">'
    +'<div class="mged-head">📄 '+esc(m.name||name)+'<button class="x" onclick="closeMatEditor()">✕</button></div>'
    +'<div class="mged-body" id="mgedBody"><div class="mpmsg">📕 한글 문서를 여는 중…<br>'
    +'<span style="font-size:11.5px">처음 한 번은 조금 걸립니다</span></div></div></div>';
  back.addEventListener('click', e=>{ if(e.target===back) closeMatEditor(); });
  document.body.appendChild(back);

  try{
    const arr = await matBytes(id);
    const doc = await PureunHwp.openDoc(arr, name);
    if(!$('mgedBack')){ try{doc.free()}catch(_){} return; }   /* 기다리는 사이 닫았다 */
    _mg={id:id, name:name, bytes:arr, doc:doc, grid:{units:[],blocks:[],warn:{badCellInfo:0,textBoxes:0}},
         edited:{}, busy:false};
    try{ _mg.grid = PuHwpEdit.readGrid(doc); }catch(e){
      return mgFail('글을 읽지 못했습니다 — '+((e&&e.message)||''));
    }
    mgRender();
  }catch(e){
    mgFail('한글 문서를 열지 못했습니다 — '+((e&&e.message)||''));
  }
}
function mgFail(msg){
  const b=$('mgedBody'); if(b) b.innerHTML='<div class="mpmsg">'+esc(msg)+'</div>';
}

function mgRender(){
  const box=$('mgedBack'); if(!box || !_mg) return;
  const n=mgChanged(), w=_mg.grid.warn;
  const miss=[]; 
  if(w.textBoxes) miss.push('글상자 '+w.textBoxes+'개');
  if(w.badCellInfo) miss.push('자리를 모르는 칸 '+w.badCellInfo+'개');
  box.querySelector('.mged-box').innerHTML =
    '<div class="mged-head">📄 '+esc((_matMeta[_mg.id]||{}).name||_mg.name)
      +'<button class="x" onclick="closeMatEditor()">✕</button></div>'
    +'<div class="mged-bar">'
      +'<button onclick="mgPreview()">👁 원본 모습</button>'
      +'<button class="go" id="mgSaveB" onclick="mgSave()"'+(n?'':' disabled')+'>💾 자료함에 저장</button>'
      +'<button id="mgDownB" onclick="mgDownload()"'+(n?'':' disabled')+'>📥 내려받기</button>'
      +'<span class="cnt" id="mgCnt">'+(n?'고친 곳 <b>'+n+'</b>개':'고친 곳 없음')+'</span>'
    +'</div>'
    +'<div class="mged-body" id="mgedBody">'+mgGridHtml()+'</div>'
    +(miss.length
      ? '<div class="mged-note">⚠ 아직 못 읽는 것이 있습니다 — '+esc(miss.join(', '))
        +'. 이 부분은 한글에서 고쳐 주십시오.</div>'
      : '');
  mgFitAll();
}

function mgGridHtml(){
  const g=_mg.grid;
  if(!g.blocks.length) return '<div class="mpmsg">고칠 글을 찾지 못했습니다.<br>글이 그림 안에 있는 문서일 수 있습니다.</div>';
  return g.blocks.map(b=> b.kind==='body'
    ? '<div class="mgbody">'+mgLineHtml(b.unit)+'</div>'
    : mgTableHtml(b)).join('');
}

/* 한 줄 — 번호와 고칠 칸. 번호는 문서 안 차례이고 저장할 때 쓰는 열쇠이기도 하다. */
function mgLineHtml(i){
  const u=_mg.grid.units[i];
  const v=_mg.edited[i]==null ? u.text : _mg.edited[i];
  const on=PuHwpEdit.clean(v)!==u.text;
  return '<div class="mgline'+(on?' on':'')+'" id="mgL'+i+'">'
    +'<span class="no">'+u.no+'</span>'
    +'<textarea rows="1" oninput="mgInput('+i+',this)">'+esc(v)+'</textarea></div>';
}

/* 표를 표로 그린다.
   ⚠ 자리는 getCellInfo 가 준 row·col 만 쓴다. 칸 번호를 열 수로 나눠 짐작하면
     합친 칸이 하나만 있어도 그 뒤가 전부 어긋난다(실제 공문에 55개 있었다).
   ⚠ 합친 칸이 덮는 자리는 미리 막아 둔다. 안 막으면 칸이 오른쪽으로 밀린다. */
function mgTableHtml(b){
  const rows=Math.max(1,b.rows), cols=Math.max(1,b.cols);
  const taken={};                                  /* 'r-c' → 이미 다른 칸이 덮었다 */
  const at={};                                     /* 'r-c' → 거기서 시작하는 칸 */
  const orphan=[];                                 /* 자리를 모르는 칸 */
  b.cells.forEach(c=>{
    if(c.row==null || c.col==null){ orphan.push(c); return; }
    at[c.row+'-'+c.col]=c;
  });
  let h='<table class="mgtbl">';
  for(let r=0;r<rows;r++){
    h+='<tr>';
    for(let c=0;c<cols;c++){
      if(taken[r+'-'+c]) continue;
      const cell=at[r+'-'+c];
      if(!cell){ h+='<td></td>'; continue; }
      const rs=Math.max(1,cell.rowSpan), cs=Math.max(1,cell.colSpan);
      for(let dr=0;dr<rs;dr++) for(let dc=0;dc<cs;dc++)
        if(dr||dc) taken[(r+dr)+'-'+(c+dc)]=1;
      h+='<td rowspan="'+rs+'" colspan="'+cs+'">'+cell.units.map(mgLineHtml).join('')+'</td>';
    }
    h+='</tr>';
  }
  h+='</table>';
  /* 자리를 못 읽은 칸도 고칠 수 있어야 한다. 조용히 빼면 영영 못 고친다. */
  if(orphan.length){
    h+='<div class="mgorph">⚠ 자리를 모르는 칸 '+orphan.length+'개 — 표 어디인지 알 수 없지만 고칠 수는 있습니다.'
      +orphan.map(c=>c.units.map(mgLineHtml).join('')).join('')+'</div>';
  }
  return h;
}

function mgFit(el){ el.style.height='auto'; el.style.height=(el.scrollHeight+2)+'px'; }
function mgFitAll(){ const b=$('mgedBody'); if(b) b.querySelectorAll('textarea').forEach(mgFit); }

/* 화면을 다시 그리지 않는다 — 그리면 글 쓰던 자리를 잃는다 */
function mgInput(i, el){
  if(!_mg) return;
  _mg.edited[i]=el.value;
  mgFit(el);
  const row=$('mgL'+i);
  if(row) row.classList.toggle('on', PuHwpEdit.clean(el.value)!==_mg.grid.units[i].text);
  const n=mgChanged();
  if($('mgCnt')) $('mgCnt').innerHTML = n?'고친 곳 <b>'+n+'</b>개':'고친 곳 없음';
  if($('mgSaveB')) $('mgSaveB').disabled=!n;
  if($('mgDownB')) $('mgDownB').disabled=!n;
}
```

- [ ] **Step 5: 문법을 확인한다**

`<scratchpad>/jscheck.js` 로 인라인 `<script>` 를 뽑아 검사한다. 없으면 만든다.

```bash
node "$SCRATCH/jscheck.js" pu-cards.html
```

Expected: `검사한 블록 N개 — ALL OK`

- [ ] **Step 6: 검사가 통과하는 것을 확인한다**

```bash
node --test tests/cards-hwp-integration.test.js
```

Expected: PASS

- [ ] **Step 7: 커밋**

```bash
git add pu-cards.html tests/cards-hwp-integration.test.js && git commit -m "feat(자료함): 큰 팝업에 서식을 표 모양 그대로 펼친다"
```

---

### Task 4: 저장·내려받기·원본 모습

**Files:**
- Modify: `pu-cards.html` (Task 3 에서 넣은 `mgInput` 함수 **뒤**)
- Test: `tests/cards-hwp-integration.test.js`

**Interfaces:**
- Consumes: `_mg`, `mgChanged()`, `mgRender()` (Task 3) · `PuHwpEdit.changedRows/applyRows/exportBytes/editedName/extOf` · `putMaterial(id, name, file)` · `matType(name)` · `PureunHwp.renderPreview(el, bytes, name)` · `PureunHwp.download(bytes, name, ext)`
- Produces: `mgApply()` → `{ok:number, failed:Array}` · `mgSave()` · `mgDownload()` · `mgPreview()`

- [ ] **Step 1: 실패하는 검사를 쓴다**

```javascript
test('저장은 되돌릴 수 없으니 먼저 물어본다', () => {
  const fn = source.slice(source.indexOf('async function mgSave'), source.indexOf('function mgDownload'));
  assert.ok(fn.length > 200, 'mgSave 를 찾지 못했습니다');
  assert.match(fn, /confirm\(/);
  assert.match(fn, /putMaterial\(/);
});

test('넣지 못한 줄을 조용히 넘기지 않는다', () => {
  assert.match(source, /function mgTellFailed/);
  assert.match(source, /넣지 못한 곳/);
});

test('문서에 넣은 뒤 반드시 다시 읽는다', () => {
  /* 넣고 나면 줄 길이가 달라진다 — 안 읽으면 다음 저장이 어긋난다 */
  const fn = source.slice(source.indexOf('function mgApply'), source.indexOf('function mgTellFailed'));
  assert.ok(fn.length > 120, 'mgApply 를 찾지 못했습니다');
  assert.match(fn, /PuHwpEdit\.readGrid\(_mg\.doc\)/);
  assert.match(fn, /_mg\.edited\s*=\s*\{\}/);
});

test('원본 모습은 문서가 아니라 바이트로 그린다', () => {
  /* 문서를 넘기면 엔진이 가져가 편집이 죽는다 */
  const fn = source.slice(source.indexOf('function mgPreview'), source.indexOf('function mgPreview') + 700);
  assert.match(fn, /renderPreview\([^)]*_mg\.bytes/);
  assert.doesNotMatch(fn, /renderPreview\([^)]*_mg\.doc/);
});
```

- [ ] **Step 2: 검사가 실패하는 것을 확인한다**

```bash
node --test tests/cards-hwp-integration.test.js
```

Expected: FAIL — 4개 모두 실패

- [ ] **Step 3: 만든다**

`function mgInput(i, el){ … }` **뒤**에 넣는다.

```javascript
/* 고친 것을 문서에 넣고 새 바이트를 만든다.
   ⚠ 넣은 뒤에는 줄 길이가 달라졌다 — 반드시 다시 읽어야 다음 저장이 어긋나지 않는다. */
function mgApply(){
  const ch = PuHwpEdit.changedRows(_mg.grid.units, _mg.edited);
  if(!ch.length) return {ok:0, failed:[]};
  const res = PuHwpEdit.applyRows(_mg.doc, ch);
  _mg.bytes = PuHwpEdit.exportBytes(_mg.doc, _mg.name);
  _mg.grid = PuHwpEdit.readGrid(_mg.doc);
  _mg.edited = {};
  return res;
}
/* 실패한 곳을 조용히 넘기면 「저장했다는데 안 바뀌었다」가 된다 */
function mgTellFailed(res){
  if(!res.failed.length) return;
  toast('넣지 못한 곳: '+res.failed.map(f=>f.no+'번('+f.why+')').join(', '), 9000);
}

async function mgSave(){
  if(!_mg || _mg.busy) return;
  const n = mgChanged();
  if(!n) return toast('고친 곳이 없습니다');
  if(!confirm('고친 '+n+'곳을 자료함의 「'+_mg.name+'」에 저장합니다.\n예전 내용은 남지 않습니다. 진행할까요?')) return;
  _mg.busy=true;
  try{
    const res = mgApply();
    mgTellFailed(res);
    const t = matType(_mg.name) || {mime:'application/octet-stream'};
    const file = new File([_mg.bytes], _mg.name, {type:t.mime});
    const cur = _matMeta[_mg.id] || {};
    await putMaterial(_mg.id, cur.name || _mg.name, file);   /* 갈래·설명·순서는 그대로 얹힌다 */
    toast('✅ 자료함에 저장했습니다');
    mgRender();
  }catch(e){
    toast('저장하지 못했습니다 — '+((e&&e.message)||''), 8000);
  }finally{ _mg.busy=false; }
}

function mgDownload(){
  if(!_mg || _mg.busy) return;
  try{
    const res = mgApply();
    mgTellFailed(res);
    PureunHwp.download(_mg.bytes, PuHwpEdit.editedName(_mg.name), PuHwpEdit.extOf(_mg.name));
    toast('📥 고친 문서를 내려받았습니다 — 자료함의 원본은 그대로입니다');
    mgRender();
  }catch(e){ toast('만들지 못했습니다 — '+((e&&e.message)||''), 8000); }
}

/* 원본 모습 — 한글로 인쇄한 그대로 그린다.
   ⚠ 반드시 _mg.bytes 를 넘긴다. renderPreview 는 받은 바이트로 제 문서를 열고
     끝나면 닫는다(pu-hwp-engine.js). _mg.doc 를 넘기면 그 문서가 죽어
     편집이 통째로 멈춘다. */
function mgPreview(){
  if(!_mg) return;
  const b=$('mgedBody'); if(!b) return;
  if(mgChanged() && !confirm('고친 내용을 아직 저장하지 않았습니다.\n버리고 원본 모습을 볼까요?')) return;
  _mg.edited={};
  b.innerHTML='<div class="mpmsg">모습을 그리는 중…</div>';
  PureunHwp.renderPreview(b, _mg.bytes, _mg.name).then(()=>{
    const back=document.createElement('div');
    back.style.cssText='text-align:center;padding:10px 0 18px';
    back.innerHTML='<button onclick="mgRender()" style="border:1px solid #cbd5e6;background:#fff;border-radius:8px;padding:7px 14px;font-size:12.5px;font-weight:700;cursor:pointer;font-family:inherit">← 고치기로 돌아가기</button>';
    b.appendChild(back);
  }).catch(e=>{
    b.innerHTML='<div class="mpmsg">모습을 그리지 못했습니다 — '+esc((e&&e.message)||'')
      +'<br><button onclick="mgRender()" style="margin-top:10px;border:1px solid #cbd5e6;background:#fff;border-radius:8px;padding:7px 14px;font-size:12.5px;cursor:pointer;font-family:inherit">← 고치기로 돌아가기</button></div>';
  });
}
```

- [ ] **Step 4: 문법을 확인한다**

```bash
node "$SCRATCH/jscheck.js" pu-cards.html
```

Expected: `ALL OK`

- [ ] **Step 5: 검사가 통과하는 것을 확인한다**

```bash
node --test tests/cards-hwp-integration.test.js
```

Expected: PASS

- [ ] **Step 6: 커밋**

```bash
git add pu-cards.html tests/cards-hwp-integration.test.js && git commit -m "feat(자료함): 격자 편집기에서 저장·내려받기·원본 모습"
```

---

### Task 5: 자료를 누르면 팝업이 열리게 잇고, 실제 서류로 확인한다

**Files:**
- Modify: `pu-cards.html:4191` 언저리 (자료함 관리 목록의 `👁` 단추 옆)
- Test: `tests/cards-hwp-integration.test.js`

**Interfaces:**
- Consumes: `openMatEditor(id)` (Task 3)
- Produces: 화면에서 자료를 눌러 격자 편집기를 여는 길

- [ ] **Step 1: 실패하는 검사를 쓴다**

```javascript
test('자료를 눌러 격자 편집기를 열 수 있다', () => {
  assert.match(source, /onclick="openMatEditor\(/);
});

test('한글이 아닌 자료는 고치기를 막고 까닭을 말한다', () => {
  const fn = source.slice(source.indexOf('async function openMatEditor'), source.indexOf('function mgFail'));
  assert.match(fn, /PDF 는 글자를 바꿀 수 없습니다/);
});
```

- [ ] **Step 2: 검사가 실패하는 것을 확인한다**

```bash
node --test tests/cards-hwp-integration.test.js
```

Expected: FAIL — `자료를 눌러 격자 편집기를 열 수 있다` 가 실패

- [ ] **Step 3: 단추를 잇는다**

`pu-cards.html:4191` 의 이 줄을 찾는다.

```html
<button title="미리보기" onclick="previewMaterial('${m.id}')">👁</button>
```

**바로 뒤**에 한 줄 더 놓는다. `👁`(원본 모습)는 그대로 둔다 — 보는 일과 고치는 일은 다르고, 하나로 합치면 무엇이 열릴지 모르게 된다.

```html
<button title="서식을 표 모양 그대로 펼쳐 한 화면에서 고칩니다" onclick="openMatEditor('${m.id}')">✏</button>
```

- [ ] **Step 4: 검사를 전부 돌린다**

```bash
node --test tests/*.test.js
```

Expected: `fail 0`

- [ ] **Step 5: 진짜 한글 파일로 확인한다**

로컬 서버를 띄우고 브라우저로 연다. **실제 서식으로 확인하는 것이 이 과제의 핵심이다** — 가짜 문서 검사만으로는 격자가 눈에 맞게 나오는지 알 수 없다.

```bash
python -m http.server 8825
```

자료함에 올려 두고 `✏ 고치기` 를 눌러 확인한다.

| 파일 | 봐야 할 것 |
|---|---|
| `16년10월급여.hwp` | 20행 × 3열 표로 나오는가 (60줄짜리 목록이 아니라) |
| `7. 위임장.hwp` | 표 한 칸 안의 문단들이 **각각 고칠 수 있는 줄**로 나오는가 |
| 합친 칸이 많은 공문 | 칸이 오른쪽으로 밀리지 않고 원본과 같은 모양인가 |

그리고 반드시:

- 한 칸을 고쳐 `💾 자료함에 저장` → **그 칸만 바뀌고 나머지는 그대로인가**
- `👁 원본 모습` 을 켰다가 `← 고치기로 돌아가기` → **편집이 살아 있는가** (여기서 죽으면 `renderPreview` 에 `doc` 를 넘긴 것이다)
- PDF 자료에서 `✏ 고치기` → 「PDF 는 글자를 바꿀 수 없습니다」가 뜨는가

- [ ] **Step 6: 커밋**

```bash
git add pu-cards.html tests/cards-hwp-integration.test.js && git commit -m "feat(자료함): 자료를 눌러 격자 편집기를 연다"
```

---

## 이 계획이 다루지 않는 것

설계서의 나머지는 다음 계획으로 넘긴다. 여기서 손대면 과제가 커져 검사가 헐거워진다.

- **`[빈칸만]` 거르개** — 설계서 § 3 의 도구줄에 있다. 세로 목록에서는 거르기가 쉽지만 **표에서는 줄을 걸러 내면 표가 무너진다.** 표 모양을 지키면서 빈칸만 보이게 하려면 거르기가 아니라 **눈에 띄게 칠하기**로 가야 한다. 격자가 실제 서식에서 어떻게 보이는지 확인한 뒤 정한다.
- **채울 자리 지정과 명함첩 당겨오기** (`slots`, `mode`, `keep`) — 설계서 § 4
- **묶음에 채우기 잇기** — 설계서 § 6
- **`sendAs`(한글/PDF 고르기)** — 설계서 § 6
- **원본 덮어쓰기 전 이전본 남기기** — 설계서 § 5. `putMaterial` 을 건드려야 해 따로 뗀다
- **중첩표·글상자 읽기** — 이번엔 세어서 알리기만 한다
- **표 줄 추가·삭제**
