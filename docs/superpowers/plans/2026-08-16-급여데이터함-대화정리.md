# 급여데이터함 카톡 대화 정리 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 카톡에서 내보낸 대화 `.txt` 를 사업장 서랍에 담아 말풍선으로 읽고, 달을 골라 판독해 「요약 · 할 일 · 값」으로 정리한다.

**Architecture:** 담기는 이미 된다(`acceptFile` 이 `.txt` 를 막지 않는다 — 코드로 확인). 새로 만드는 것은 ① 내보내기 꼴을 가르는 **순수 계산 파일** 하나 ② 공용 판독 층에 **글을 보내는 새 함수** 하나 ③ 화면. 값은 이미 있는 값 층(`buildValueRows`·`saveValues`)으로 그대로 흘려보낸다.

**Tech Stack:** 빌드 없는 단일 파일 HTML · Firebase RTDB/Storage · `node --test` · 공용 판독 층 `js/pu-doc-read.js`(Gemini)

설계서: `docs/superpowers/specs/2026-08-16-급여데이터함-대화정리-design.md`
목업: `docs/mockups/paydata-chat.html`

## Global Constraints

- **공용 판독 층(`js/pu-doc-read.js`)에서 다른 앱이 기대는 동작을 바꾸지 않는다.** 사진첩·명함첩·업체관리가 함께 쓴다. `read()` 와 `PROMPT_ALL` 은 **손대지 않는다.** 새 함수만 더한다.
- **한국어 주석·화면 문구.** 파일의 기존 목소리(왜 그런지를 적는다)를 따른다.
- **검사 판정은 종료코드로.** `node --test tests/*.test.js > /tmp_t.txt 2>&1; echo "EXIT:$?"` 가 `EXIT:0` 이어야 한다. 출력만 훑지 않는다.
- **커밋은 경로를 찍어서.** `git add -A` / `git add .` 금지 — 여러 세션이 같은 저장소를 쓴다.
- **빌드 없음.** 인라인 `<script>` 가 파싱되는지 확인한다.
- **CRSF 덫:** `core.autocrlf=true` 라 작업트리가 CRLF 다. 글자 바로 뒤에 `\n` 을 붙인 정규식은 안 걸린다. `[\s\S]*?` 를 앞에 두거나 `\r?\n` 을 쓴다.
- **잘라 쓰는 덫:** 검사가 HTML 에서 함수를 잘라 `vm` 에서 돌린다. 잘린 함수가 최상위 `const`(예: `WAGE_READ_ON`)를 보면 그 상수도 샌드박스에 넣어야 한다.
- **주민등록번호는 이번에 막지 않는다**(대표 결정 2026-08-16). 대신 판독 화면에 경고를 띄운다. 글을 `●` 로 바꾸는 것은 이 계획 범위 밖이다.
- **새 콘솔 규칙 없음.**

---

## File Structure

| 파일 | 맡는 것 | 새로/고침 |
|---|---|---|
| `js/pu-chat-parse.js` | 카톡 내보내기 글 → 말줄 배열. 순수 계산, 저장·화면 모름 | **새로** |
| `js/pu-doc-read.js` | 글을 보내는 판독 함수 `readChatText` | 고침(**더하기만**) |
| `js/pu-paydata-store.js` | 대화 탭(`KINDS`), 요약·할 일 저장 | 고침 |
| `pu-paydata.html` | 글 뷰어 · 달 고르기 · 정리 화면 · 값 저장 · 자리 찾아가기 | 고침 |
| `tests/paydata-chat-parse.test.js` | 꼴 가르기 검사 | **새로** |
| `tests/paydata-chat-read.test.js` | 판독기 검사 | **새로** |
| `tests/paydata-chat-screen.test.js` | 화면 배선 검사 | **새로** |

파서를 저장 층에 안 넣는 까닭: `js/pu-paydata-store.js` 는 「어디에 담는지 아는 유일한 파일」이다. 글 가르기는 담는 것과 무관하고, 섞으면 그 파일이 두 가지 일을 하게 된다.

---

### Task 1: 카톡 내보내기 꼴 가르기

**Files:**
- Create: `js/pu-chat-parse.js`
- Test: `tests/paydata-chat-parse.test.js`

**Interfaces:**
- Produces `window.PuChatParse`:
  - `parseKakao(text)` → `{ ok, lines: [{at, atText, who, text}], months: ['2026-08', …] }`
    `at` = epoch ms · `atText` = `'2026-08-12 14:05'` · `who` = 보낸 사람 · `text` = 말
  - `sliceMonth(lines, 'YYYY-MM')` → 그 달 말줄만
  - `toPlain(lines)` → 판독기에 보낼 글 (`'[2026-08-12 14:05] 임대순 대표: 메시지'` 한 줄씩)

- [ ] **Step 1: 실패하는 검사를 쓴다**

`tests/paydata-chat-parse.test.js`:

```js
'use strict';
// 카톡 내보내기 꼴 가르기 — 실행: node --test tests/*.test.js
//   ⚠ 안드로이드와 아이폰의 꼴이 다르다. 실물을 못 받아 알려진 두 꼴을 다 받게
//   만들었고, **못 알아보면 글 그대로 돌려준다**(앱이 멈추면 안 된다).
const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');
const vm = require('node:vm');

function load() {
  const src = fs.readFileSync(path.join(__dirname, '..', 'js', 'pu-chat-parse.js'), 'utf8');
  const sandbox = { window: {}, console, Date };
  sandbox.globalThis = sandbox;
  vm.createContext(sandbox);
  new vm.Script(src, { filename: 'pu-chat-parse.js' }).runInContext(sandbox);
  return sandbox.window.PuChatParse;
}

const 안드로이드 = [
  '다온원 님과 카카오톡 대화',
  '저장한 날짜 : 2026년 8월 14일 오전 11:20',
  '',
  '--------------- 2026년 8월 12일 화요일 ---------------',
  '2026년 8월 12일 오후 2:05, 임대순 대표 : 김신입씨 8월 12일자로 입사했습니다',
  '2026년 8월 12일 오후 2:20, 권형하 : 네 취득신고 진행하겠습니다',
  '--------------- 2026년 8월 14일 목요일 ---------------',
  '2026년 8월 14일 오전 11:02, 임대순 대표 : 배영승씨 식대를 20만원으로'
].join('\n');

const 아이폰 = [
  '2026. 8. 12. 오후 2:05, 임대순 대표 : 김신입씨 입사',
  '2026. 8. 12. 오전 9:00, 권형하 : 확인했습니다'
].join('\n');

test('★ 안드로이드 꼴을 가른다', () => {
  const P = load();
  const r = P.parseKakao(안드로이드);
  assert.equal(r.ok, true);
  assert.equal(r.lines.length, 3, '말줄만 세 개여야 합니다(머리말·날짜 구분선은 말이 아닙니다)');
  assert.equal(r.lines[0].who, '임대순 대표');
  assert.equal(r.lines[0].text, '김신입씨 8월 12일자로 입사했습니다');
  assert.equal(r.lines[0].atText, '2026-08-12 14:05', '오후 2시는 14시입니다');
});

test('★ 아이폰 꼴도 가른다', () => {
  const P = load();
  const r = P.parseKakao(아이폰);
  assert.equal(r.lines.length, 2);
  assert.equal(r.lines[0].atText, '2026-08-12 14:05');
  assert.equal(r.lines[1].atText, '2026-08-12 09:00', '오전 9시는 09시입니다');
});

test('★ 오전 12시는 0시, 오후 12시는 12시다 — 뒤집히면 하루가 밀린다', () => {
  const P = load();
  const r = P.parseKakao([
    '2026년 8월 12일 오전 12:30, 갑 : 자정',
    '2026년 8월 12일 오후 12:30, 갑 : 정오'
  ].join('\n'));
  assert.equal(r.lines[0].atText, '2026-08-12 00:30');
  assert.equal(r.lines[1].atText, '2026-08-12 12:30');
});

test('★ 엔터가 든 말은 앞 말에 붙는다 — 따로 세면 줄 수가 틀린다', () => {
  const P = load();
  const r = P.parseKakao([
    '2026년 8월 12일 오후 2:05, 갑 : 첫째 줄',
    '둘째 줄',
    '셋째 줄'
  ].join('\n'));
  assert.equal(r.lines.length, 1);
  assert.equal(r.lines[0].text, '첫째 줄\n둘째 줄\n셋째 줄');
});

test('★ 못 알아보면 글 그대로 준다 — 앱이 멈추지 않는다', () => {
  const P = load();
  const r = P.parseKakao('이건 카톡 내보내기가 아닙니다\n그냥 글입니다');
  assert.equal(r.ok, false);
  assert.equal(r.lines.length, 0);
  assert.equal(r.raw, '이건 카톡 내보내기가 아닙니다\n그냥 글입니다', '원문을 잃으면 안 됩니다');
});

test('나온 달을 최근 순으로 알려준다', () => {
  const P = load();
  const r = P.parseKakao(안드로이드);
  assert.equal(r.months.join(','), '2026-08');
});

test('★ 달로 잘라 낸다', () => {
  const P = load();
  const lines = P.parseKakao([
    '2026년 7월 30일 오후 2:05, 갑 : 칠월',
    '2026년 8월 12일 오후 2:05, 갑 : 팔월'
  ].join('\n')).lines;
  assert.equal(P.sliceMonth(lines, '2026-08').length, 1);
  assert.equal(P.sliceMonth(lines, '2026-08')[0].text, '팔월');
  assert.equal(P.sliceMonth(lines, '').length, 2, '달을 안 고르면 전부입니다');
});

test('★ 판독기에 보낼 글은 시각과 이름이 붙는다 — AI 가 누가 언제 한 말인지 알아야 한다', () => {
  const P = load();
  const lines = P.parseKakao(안드로이드).lines;
  const plain = P.toPlain(lines.slice(0, 1));
  assert.equal(plain, '[2026-08-12 14:05] 임대순 대표: 김신입씨 8월 12일자로 입사했습니다');
});

test('빈 글·이상한 값에도 터지지 않는다', () => {
  const P = load();
  assert.equal(P.parseKakao('').lines.length, 0);
  assert.equal(P.parseKakao(null).lines.length, 0);
  assert.equal(P.sliceMonth(null, '2026-08').length, 0);
  assert.equal(P.toPlain(null), '');
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --test tests/paydata-chat-parse.test.js`
Expected: FAIL — 파일이 없다

- [ ] **Step 3: 구현**

`js/pu-chat-parse.js` 를 만든다:

```js
/* 카톡 내보내기 글 가르기 — 순수 계산
   ═══════════════════════════════════════════════════════════════════
   「대화 내용 내보내기」가 만든 .txt 를 말줄 배열로 바꾼다.
   담지도 그리지도 않는다 — 값만 다룬다. 그래서 실물 없이도 검사할 수 있다.

   ⚠ 안드로이드와 아이폰의 꼴이 다르고 판이 올라가며 바뀐다. 알려진 두 꼴을
   모두 받고, **못 알아보면 글 그대로 돌려준다** — 알아보기 실패가 「대화를
   못 본다」가 되면 안 된다. 화면은 raw 를 그대로 보여주면 된다. */
(function (global) {
  'use strict';

  /* 안드로이드: 2026년 8월 12일 오후 2:05, 임대순 대표 : 메시지
     아이폰:     2026. 8. 12. 오후 2:05, 임대순 대표 : 메시지
     보낸 사람 이름에 콜론이 들어갈 수 있어 **처음 나오는 ' : ' 로만** 가른다. */
  var RE_AND = /^(\d{4})년 (\d{1,2})월 (\d{1,2})일 (오전|오후) (\d{1,2}):(\d{2}), (.*?) : ([\s\S]*)$/;
  var RE_IOS = /^(\d{4})\. (\d{1,2})\. (\d{1,2})\. (오전|오후) (\d{1,2}):(\d{2}), (.*?) : ([\s\S]*)$/;

  function pad2(n) { return (n < 10 ? '0' : '') + n; }

  /* 오전 12시는 0시, 오후 12시는 12시다. 이걸 뒤집으면 자정 말이 정오로 가고
     날짜 경계에서 하루가 밀린다. */
  function hour24(ampm, h) {
    h = Number(h);
    if (ampm === '오전') return h === 12 ? 0 : h;
    return h === 12 ? 12 : h + 12;
  }

  function oneLine(s) {
    var m = RE_AND.exec(s) || RE_IOS.exec(s);
    if (!m) return null;
    var y = Number(m[1]), mo = Number(m[2]), d = Number(m[3]);
    var hh = hour24(m[4], m[5]), mi = Number(m[6]);
    return {
      at: new Date(y, mo - 1, d, hh, mi, 0).getTime(),
      atText: y + '-' + pad2(mo) + '-' + pad2(d) + ' ' + pad2(hh) + ':' + pad2(mi),
      who: String(m[7] || '').trim(),
      text: String(m[8] || '')
    };
  }

  function parseKakao(text) {
    var raw = String(text == null ? '' : text);
    var rows = raw.split(/\r?\n/);
    var out = [], last = null;
    for (var i = 0; i < rows.length; i++) {
      var got = oneLine(rows[i]);
      if (got) { out.push(got); last = got; continue; }
      /* 시각이 없는 줄 — 엔터가 든 말의 이어진 부분이다. 앞 말에 붙인다.
         앞 말이 없으면 머리말·날짜 구분선이므로 버린다. */
      if (last && rows[i] !== '') last.text += '\n' + rows[i];
    }
    var seen = {}, months = [];
    out.forEach(function (l) {
      var mk = l.atText.slice(0, 7);
      if (!seen[mk]) { seen[mk] = 1; months.push(mk); }
    });
    months.sort().reverse();           // 최근 달이 앞
    return { ok: out.length > 0, lines: out, months: months, raw: raw };
  }

  /* 달을 안 주면 전부 — 화면의 「전체」가 이 길로 온다. */
  function sliceMonth(lines, ym) {
    var arr = lines || [];
    if (!ym) return arr.slice();
    return arr.filter(function (l) { return l && l.atText.slice(0, 7) === ym; });
  }

  /* 판독기에 보낼 꼴. 시각과 이름을 함께 보내야 AI 가 「누가 언제 한 말인지」를
     알고, 값의 출처를 그 시각으로 돌려줄 수 있다. */
  function toPlain(lines) {
    return (lines || []).map(function (l) {
      return '[' + l.atText + '] ' + l.who + ': ' + l.text;
    }).join('\n');
  }

  global.PuChatParse = { parseKakao: parseKakao, sliceMonth: sliceMonth, toPlain: toPlain };
})(typeof window !== 'undefined' ? window : globalThis);
```

- [ ] **Step 4: 통과 확인**

Run: `node --test tests/paydata-chat-parse.test.js`
Expected: PASS 9/9

- [ ] **Step 5: 전체 검사**

Run: `node --test tests/*.test.js > /tmp_t.txt 2>&1; echo "EXIT:$?"; tail -8 /tmp_t.txt`
Expected: `EXIT:0`

- [ ] **Step 6: 커밋**

```bash
git add js/pu-chat-parse.js tests/paydata-chat-parse.test.js
git commit -m "feat(대화): 카톡 내보내기 꼴 가르기 — 못 알아보면 글 그대로"
```

---

### Task 2: 대화 탭을 만든다

**Files:**
- Modify: `js/pu-paydata-store.js` (`KINDS`)
- Modify: `pu-paydata.html` (`<script src>` 로 파서 싣기)
- Test: `tests/paydata-chat-screen.test.js`

**Interfaces:**
- Consumes: `window.PuChatParse` (Task 1)
- Produces: `S.KINDS` 에 `{ key: 'chat', label: '대화' }` · `S.readKindFor('chat')` → `'chattext'`

- [ ] **Step 1: 실패하는 검사를 쓴다**

`tests/paydata-chat-screen.test.js`:

```js
'use strict';
// 대화 탭·화면 배선 — 실행: node --test tests/*.test.js
const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');
const vm = require('node:vm');

const R = path.join(__dirname, '..');
const html = fs.readFileSync(path.join(R, 'pu-paydata.html'), 'utf8');
const store = fs.readFileSync(path.join(R, 'js', 'pu-paydata-store.js'), 'utf8');

function loadStore() {
  const sandbox = { window: {}, console };
  sandbox.globalThis = sandbox;
  vm.createContext(sandbox);
  new vm.Script(store, { filename: 'store.js' }).runInContext(sandbox);
  return sandbox.window.PuPaydataStore;
}

test('★ 서랍에 대화 탭이 있다', () => {
  const S = loadStore();
  const keys = S.KINDS.map(k => k.key);
  assert.ok(keys.indexOf('chat') >= 0, '대화 탭이 없습니다: ' + keys.join(','));
  const chat = S.KINDS.find(k => k.key === 'chat');
  assert.equal(chat.label, '대화');
  assert.ok(!chat.keep, '대화는 월과 무관한 keep 칸이 아닙니다');
});

test('기존 다섯 탭이 그대로 있다 — 늘리는 것이지 바꾸는 것이 아니다', () => {
  const S = loadStore();
  const keys = S.KINDS.map(k => k.key);
  ['contract', 'attend', 'ledger', 'output', 'etc'].forEach(k => {
    assert.ok(keys.indexOf(k) >= 0, k + ' 이 사라졌습니다');
  });
});

test('★ 대화 탭은 글 판독기로 간다', () => {
  const S = loadStore();
  assert.equal(S.readKindFor('chat'), 'chattext');
});

test('★ 대화 가르는 층을 불러온다', () => {
  assert.match(html, /<script src="js\/pu-chat-parse\.js">/);
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --test tests/paydata-chat-screen.test.js`
Expected: FAIL — 대화 탭이 없습니다

- [ ] **Step 3: 구현**

`js/pu-paydata-store.js` 의 `KINDS` 를 바꾼다.

바꾸기 전:
```js
    { key: 'etc',      label: '기타' }
  ];
```

바꾼 뒤:
```js
    { key: 'etc',      label: '기타' },
    /* 대화 — 카톡에서 내보낸 .txt. 기타에 섞지 않는 까닭: 한 사업장에 보통
       한 덩어리이고 계속 갱신되는데, 기타에 다른 서류와 섞이면 「이 업체 대화
       어디 있지」가 된다. */
    { key: 'chat',     label: '대화' }
  ];
```

`readKindFor` 에 한 줄 더한다.

바꾸기 전:
```js
    if (kind === 'etc') return 'notice';
```

바꾼 뒤:
```js
    if (kind === 'etc') return 'notice';
    if (kind === 'chat') return 'chattext';
```

`pu-paydata.html` 에서 `<script src="js/pu-doc-read.js"></script>` **위**에 넣는다:

```html
<script src="js/pu-chat-parse.js"></script>
```

- [ ] **Step 4: 통과 확인 + 탭이 늘어 깨진 검사가 없는지**

Run: `node --test tests/*.test.js > /tmp_t.txt 2>&1; echo "EXIT:$?"; tail -8 /tmp_t.txt`
Expected: `EXIT:0` — 탭 수를 못 박은 검사는 없는 것으로 확인했지만, 깨지면 **검사를 느슨하게 고치지 말고** 보고한다

- [ ] **Step 5: 커밋**

```bash
git add js/pu-paydata-store.js pu-paydata.html tests/paydata-chat-screen.test.js
git commit -m "feat(대화): 서랍에 대화 탭을 만든다"
```

---

### Task 3: 글 파일을 말풍선으로 보여준다

**Files:**
- Modify: `pu-paydata.html` (`renderViewer` · CSS · `App` 상태)
- Test: `tests/paydata-chat-screen.test.js` (이어 붙임)

**Interfaces:**
- Consumes: `PuChatParse.parseKakao`
- Produces: `chatBodyHtml(parsed)` · `App.chatText` · `App.chatParsed` · `loadChatText(rec)`

- [ ] **Step 1: 검사를 이어 붙인다**

```js
/* ══════ 글 뷰어 ══════ */
function cut(name) {
  const m = html.match(new RegExp('function ' + name + '\\s*\\([\\s\\S]*?\\n\\}'));
  assert.ok(m, name + ' 함수를 찾을 수 없습니다');
  return m[0];
}

function loadChat() {
  const sandbox = { window: {}, console, Date };
  sandbox.globalThis = sandbox;
  vm.createContext(sandbox);
  new vm.Script(fs.readFileSync(path.join(R, 'js', 'pu-chat-parse.js'), 'utf8')).runInContext(sandbox);
  new vm.Script([
    'const PuChatParse = window.PuChatParse;',
    cut('esc'), cut('chatBodyHtml'),
    'window.chatBodyHtml = chatBodyHtml;'
  ].join('\n'), { filename: 'chat.js' }).runInContext(sandbox);
  return sandbox.window;
}

const SAMPLE = [
  '--------------- 2026년 8월 12일 화요일 ---------------',
  '2026년 8월 12일 오후 2:05, 임대순 대표 : 김신입씨 입사했습니다',
  '2026년 8월 14일 오전 11:02, 임대순 대표 : 식대 20만원으로'
].join('\n');

test('★ 말풍선으로 그린다 — 누가·언제·무슨 말', () => {
  const W = loadChat();
  const h = W.chatBodyHtml(W.window ? null : require('node:vm'), SAMPLE) || W.chatBodyHtml(SAMPLE);
  assert.match(h, /임대순 대표/);
  assert.match(h, /김신입씨 입사했습니다/);
  assert.match(h, /14:05|오후 2:05/, '시각이 보여야 어느 말인지 짚습니다');
});

test('★ 날짜가 바뀌면 구분선이 들어간다', () => {
  const W = loadChat();
  const h = W.chatBodyHtml(SAMPLE);
  assert.match(h, /2026-08-12/);
  assert.match(h, /2026-08-14/);
  assert.equal((h.match(/class="daysep"/g) || []).length, 2, '날짜마다 하나씩이어야 합니다');
});

test('★ 못 알아보면 글 그대로 보여준다 — 빈 화면이 되면 안 된다', () => {
  const W = loadChat();
  const h = W.chatBodyHtml('카톡 꼴이 아닌 그냥 글\n둘째 줄');
  assert.match(h, /카톡 꼴이 아닌 그냥 글/);
  assert.match(h, /둘째 줄/);
});

test('★ 글에 든 꺾쇠를 그대로 그리지 않는다', () => {
  const W = loadChat();
  const h = W.chatBodyHtml('2026년 8월 12일 오후 2:05, 갑 : <script>나쁜것</script>');
  assert.equal(/<script>나쁜것/.test(h), false, '남이 보낸 글이 화면에서 실행되면 안 됩니다');
  assert.match(h, /&lt;script&gt;/);
});

test('★ 글 파일이면 뷰어가 글을 불러온다', () => {
  const rv = html.match(/function renderViewer\(\)[\s\S]*?\n\}/)[0];
  assert.match(rv, /chatBodyHtml\(/, '함수만 있고 안 부르면 화면에 아무것도 없습니다');
  assert.match(html, /function loadChatText/, '창고에서 글을 받아 오는 길이 있어야 합니다');
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --test tests/paydata-chat-screen.test.js`
Expected: FAIL — `chatBodyHtml 함수를 찾을 수 없습니다`

- [ ] **Step 3: 구현 — 꾸밈**

CSS 맨 뒤에 넣는다:

```css
/* ── 대화 보기 ── */
.chatwrap{font-size:12.5px;line-height:1.6;color:var(--ink)}
.chatwrap .daysep{text-align:center;color:var(--sub);font-size:11px;margin:14px 0 10px;position:relative}
.chatwrap .daysep span{background:#fff;padding:0 10px;position:relative;z-index:1}
.chatwrap .daysep::before{content:'';position:absolute;left:0;right:0;top:50%;border-top:1px solid var(--line)}
.chatwrap .msg{margin-bottom:9px}
.chatwrap .msg .who{font-weight:700;color:var(--navy)}
.chatwrap .msg .at{color:var(--sub);font-size:11px;margin-left:6px}
.chatwrap .msg .say{white-space:pre-wrap;word-break:break-word}
.chatwrap .msg.hit{background:#FDF6E3;border-radius:8px;padding:6px 8px;margin-left:-8px;margin-right:-8px}
.chatwrap .plain{white-space:pre-wrap;word-break:break-word;font-family:ui-monospace,Consolas,monospace;font-size:12px}
.chatwrap .oops{background:#FDF6E3;border:1px solid #F0D9A8;color:#7C4A03;border-radius:9px;padding:8px 11px;font-size:12px;margin-bottom:10px}
#viewerShot.textdoc{align-items:flex-start;justify-content:stretch;background:#fff;padding:14px 16px}
```

- [ ] **Step 4: 구현 — 그리기**

`function renderViewer() {` **위**에 넣는다:

```js
/* 대화 글을 말풍선으로 그린다. 못 알아보면 글 그대로 — 알아보기 실패가
   「대화를 못 본다」가 되면 안 된다(설계서 3장).
   hitAt 을 주면 그 시각의 말을 노랗게 짚는다(값에서 찾아올 때 쓴다). */
function chatBodyHtml(text, hitAt) {
  const p = PuChatParse.parseKakao(text);
  if (!p.ok) {
    return '<div class="chatwrap">'
      + '<div class="oops">카톡 내보내기 꼴로 보이지 않아 글 그대로 보여드립니다.</div>'
      + '<div class="plain">' + esc(p.raw) + '</div></div>';
  }
  let h = '<div class="chatwrap">', day = '';
  p.lines.forEach(l => {
    const d = l.atText.slice(0, 10);
    if (d !== day) { day = d; h += '<div class="daysep"><span>' + esc(d) + '</span></div>'; }
    const hit = (hitAt && l.atText === hitAt) ? ' hit' : '';
    h += '<div class="msg' + hit + '" id="cm_' + esc(l.atText.replace(/[^0-9]/g, '')) + '">'
      + '<span class="who">' + esc(l.who) + '</span>'
      + '<span class="at">' + esc(l.atText.slice(11)) + '</span>'
      + '<div class="say">' + esc(l.text) + '</div></div>';
  });
  return h + '</div>';
}

/* 창고에서 글을 받아 온다. 사진과 달리 주소만으로는 못 그리므로 내용을 가져온다. */
function loadChatText(rec) {
  if (!rec || !rec.file) return;
  App.chatText = null;
  S.fileDownloadUrl(rec.file)
    .then(url => fetch(url))
    .then(r => { if (!r.ok) throw new Error('불러오지 못했습니다'); return r.text(); })
    .then(t => { App.chatText = t; App.render(); })
    .catch(e => { App.chatText = '(불러오지 못했습니다 — ' + (e.message || e) + ')'; App.render(); });
}
```

`App` 상태에 더한다(`viewerErr: '',` 옆):

```js
  chatText: null, chatHitAt: '',
```

`openViewer(id)` 를 고친다.

바꾸기 전:
```js
  App.readState = { status: 'idle', rows: [], err: '' };
  App.render();
  const rec = findRow(id);
  if (!rec) { App.viewerErr = '자료를 찾을 수 없습니다.'; App.render(); return; }
  S.fileDownloadUrl(rec.file).then(url => { App.viewerUrl = url; App.render(); })
    .catch(e => { App.viewerErr = e.message; App.render(); });
```

바꾼 뒤:
```js
  App.readState = { status: 'idle', rows: [], err: '' };
  App.chatText = null; App.chatOut = null;
  App.render();
  const rec = findRow(id);
  if (!rec) { App.viewerErr = '자료를 찾을 수 없습니다.'; App.render(); return; }
  /* 글 파일은 주소만으로 못 그린다 — 내용을 받아 와야 한다. */
  if (isTextRec(rec)) loadChatText(rec);
  S.fileDownloadUrl(rec.file).then(url => { App.viewerUrl = url; App.render(); })
    .catch(e => { App.viewerErr = e.message; App.render(); });
```

`renderViewer()` 안, 사진인지 가르는 자리를 바꾼다.

바꾸기 전:
```js
  } else if (isImage) {
    body.innerHTML = '<img src="' + esc(App.viewerUrl) + '" alt="">';
  } else {
```

바꾼 뒤:
```js
  } else if (isImage) {
    body.innerHTML = '<img src="' + esc(App.viewerUrl) + '" alt="">';
  } else if (isTextRec(rec)) {
    body.innerHTML = (App.chatText === null)
      ? '<div class="doc">글을 불러오는 중…</div>'
      : chatBodyHtml(App.chatText, App.chatHitAt);
  } else {
```

그리고 `body.classList.toggle('zoom', …)` 옆에 한 줄 더한다:

```js
  body.classList.toggle('textdoc', isTextRec(rec));
```

`isTextRec` 은 `chatBodyHtml` 위에 둔다:

```js
/* 글 파일인가 — 카톡 내보내기는 text/plain 이지만 기기에 따라 빈 mime 로 오기도
   해서 확장자도 함께 본다. */
function isTextRec(rec) {
  if (!rec) return false;
  if (/^text\//.test(rec.mime || '')) return true;
  return /\.txt$/i.test(rec.filename || '');
}
```

- [ ] **Step 5: 통과 확인**

Run: `node --test tests/paydata-chat-screen.test.js`
Expected: PASS

- [ ] **Step 6: 전체 검사 + 커밋**

```bash
node --test tests/*.test.js > /tmp_t.txt 2>&1; echo "EXIT:$?"; tail -8 /tmp_t.txt
git add pu-paydata.html tests/paydata-chat-screen.test.js
git commit -m "feat(대화): 글 파일을 말풍선으로 — 못 알아보면 글 그대로"
```

---

### Task 4: 달을 골라 그 구간만

**Files:**
- Modify: `pu-paydata.html` (`readPanelHtml` · CSS · `App` 상태)
- Test: `tests/paydata-chat-screen.test.js` (이어 붙임)

**Interfaces:**
- Consumes: `PuChatParse.parseKakao`/`sliceMonth`
- Produces: `chatMonthPickHtml()` · `App.chatMonth`

> ⚠ `readPanelHtml` 은 이미 갈래가 여럿이다(급여대장 잠금 · 알림 잠금 · 종류 없음 ·
> **사진 아님** · 읽는 중 · 다 읽음 · 실패 · 아직). 대화는 **글 파일**이라
> 「사진 아님」 갈래에 먼저 걸려 단추가 아예 안 나온다. 그래서 대화 갈래를
> **「사진 아님」보다 앞에** 둬야 한다.

- [ ] **Step 1: 검사를 이어 붙인다**

`tests/paydata-chat-screen.test.js` 맨 뒤에 붙인다:

```js
/* ══════ 달 고르기 ══════ */
test('★ 대화에 나온 달을 골라 그 구간 줄 수를 보여준다', () => {
  assert.match(html, /function chatMonthPickHtml/, '달 고르개가 없습니다');
  const fn = html.match(/function chatMonthPickHtml\(\)[\s\S]*?\n\}/)[0];
  assert.match(fn, /sliceMonth/, '고른 달의 줄 수를 세어야 합니다');
  assert.match(fn, /전체/, '「전체」도 고를 수 있어야 합니다');
});

test('★ 대화 갈래가 「사진 아님」보다 앞에 온다 — 뒤에 두면 단추가 안 나온다', () => {
  const fn = html.match(/function readPanelHtml\(\)[\s\S]*?\n\}/)[0];
  const chatAt = fn.indexOf("'chattext'");
  const imgAt = fn.indexOf('isImageRec(rec)');
  assert.ok(chatAt >= 0, '대화 갈래가 없습니다');
  assert.ok(imgAt >= 0, '사진 아님 갈래가 없습니다');
  assert.ok(chatAt < imgAt, '대화가 「사진 아님」 뒤에 있어 걸러집니다');
});

test('★ 판독 화면에 주민등록번호 경고가 있다', () => {
  // 대표 결정(2026-08-16): 막지는 않되 모르고 넣는 일은 없게 한다
  assert.match(html, /주민등록번호가 있으면 그대로/, '경고 문구가 없습니다');
  assert.match(html, /사진 가림은 글에 걸리지 않습니다/);
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --test tests/paydata-chat-screen.test.js`
Expected: FAIL — 달 고르개가 없습니다

- [ ] **Step 3: 달 고르개를 만든다**

`function readPanelHtml() {` **위**에 넣는다:

```js
/* 대화가 길면 달로 갈라 그 구간만 읽는다 — 통째로 보내면 비싸고, 잡담에 묻혀
   정확도가 떨어지고, 값 층이 어차피 귀속월 단위다(설계서 4장). */
function chatMonthPickHtml() {
  const p = PuChatParse.parseKakao(App.chatText || '');
  const now = App.chatMonth || (p.months[0] || '');
  const opt = (v, label) => '<option value="' + esc(v) + '"'
    + (now === v ? ' selected' : '') + '>' + esc(label) + '</option>';
  let h = '<div class="mrange"><span>읽을 구간</span>'
    + '<select onchange="App.chatMonth=this.value;App.render()">';
  p.months.forEach(m => {
    h += opt(m, m + ' (' + PuChatParse.sliceMonth(p.lines, m).length + '줄)');
  });
  h += opt('', '전체 ' + p.lines.length + '줄');
  return h + '</select></div>';
}
```

`App` 상태에 `chatMonth: '',` 를 더한다.

- [ ] **Step 4: 대화 갈래를 「사진 아님」 앞에 끼운다**

`readPanelHtml()` 안에서 아래를 찾는다:

```js
  } else if (!isImageRec(rec)) {
```

그 **바로 앞**에 대화 갈래를 넣는다:

```js
  } else if (rk === 'chattext') {
    /* 대화는 글 파일이라 아래 「사진 아님」 갈래에 걸리면 단추가 아예 안 나온다.
       그래서 여기서 먼저 가른다. 읽는 중·다 읽음·실패는 아래 갈래를 그대로 쓴다. */
    if (st.status === 'reading') {
      body = '<div class="pre"><div class="big">⏳</div><p>읽는 중입니다…</p></div>';
    } else if (st.status === 'done') {
      body = chatOutHtml();
    } else if (st.status === 'err') {
      body = '<div class="pre"><div class="big">⚠</div><p>' + esc(st.err) + '</p>'
        + '<button class="btn pri" onclick="startMask()">💬 다시 정리</button></div>';
    } else {
      body = '<div class="pre"><div class="big">💬</div><p>아직 정리하지 않았습니다.<br>'
        + '대화가 길면 <b>달을 골라</b> 그 구간만 읽습니다.<br>'
        + '읽은 뒤 확인하고 저장해야 값으로 남습니다.</p>'
        + chatMonthPickHtml()
        + '<div class="warnline" style="text-align:left">⚠ 대화에 <b>주민등록번호</b>가 '
        + '있으면 그대로 밖으로 나갑니다 — 사진 가림은 글에 걸리지 않습니다.</div>'
        + '<button class="btn pri" onclick="startMask()">💬 대화 정리하기</button></div>';
    }
```

> `chatOutHtml()` 은 Task 6 에서 만든다. 이 단계에서는 아직 없으므로 **Task 6 까지
> 마친 뒤에 전체 검사가 통과**한다. 이 단계의 짝 검사는 갈래 차례와 달 고르개만 본다.

꾸밈을 CSS 맨 뒤에 더한다:

```css
.mrange{display:flex;gap:6px;align-items:center;justify-content:center;flex-wrap:wrap;margin-bottom:10px;font-size:12px}
.mrange select{border:1px solid var(--line);border-radius:8px;padding:5px 8px;font-size:12px;font-family:inherit}
```

- [ ] **Step 5: 짝 검사 통과 확인**

Run: `node --test tests/paydata-chat-screen.test.js`
Expected: 이 파일의 검사는 통과. 전체 검사는 Task 6 뒤에 맞춘다.

- [ ] **Step 6: 커밋**

```bash
git add pu-paydata.html tests/paydata-chat-screen.test.js
git commit -m "feat(대화): 달을 골라 그 구간만 + 주민번호 경고"
```

---

### Task 5: 대화 판독기 (글을 보낸다)
 대화 판독기 (글을 보낸다)

**Files:**
- Modify: `js/pu-doc-read.js` (`readChangeNotice` 아래, `function read(` 위)
- Test: `tests/paydata-chat-read.test.js`

**Interfaces:**
- Produces: `PuDocRead.readChatText(plainText)` → `Promise<{ok, error, summary, todos, rows}>`
  `todos` = `[{t, done, ours}]` · `rows` = `[{name, pairs:[{item,value,at}]}]`

- [ ] **Step 1: 실패하는 검사를 쓴다**

`tests/paydata-chat-read.test.js`:

```js
'use strict';
// 대화 판독기 — 글을 보낸다. 실행: node --test tests/*.test.js
//   기존 판독기는 사진(base64)만 보낸다. 이것은 **글**을 보내므로 배관이 다르다.
const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');
const vm = require('node:vm');

const SRC = path.join(__dirname, '..', 'js', 'pu-doc-read.js');

function loadRead() {
  const sandbox = { window: {}, console };
  sandbox.globalThis = sandbox;
  vm.createContext(sandbox);
  new vm.Script(fs.readFileSync(SRC, 'utf8'), { filename: 'pu-doc-read.js' }).runInContext(sandbox);
  return sandbox.window.PuDocRead;
}

function fakeFetch(reply) {
  const calls = [];
  const fn = function (url, init) {
    calls.push({ url: url, body: JSON.parse(init.body) });
    return Promise.resolve({ ok: true, status: 200,
      json: () => Promise.resolve({ candidates: [{ content: { parts: [{ text: reply }] } }] }) });
  };
  fn.calls = calls;
  return fn;
}

const PLAIN = '[2026-08-12 14:05] 임대순 대표: 김신입씨 8월 12일자로 입사했습니다';

test('판독 층에 readChatText 가 붙는다', () => {
  assert.equal(typeof loadRead().readChatText, 'function');
});

test('★ 글을 보낸다 — 사진이 아니다', async () => {
  const R = loadRead();
  const f = fakeFetch(JSON.stringify({ summary: '요약', todos: [], rows: [] }));
  R.init({ fetch: f, getKey: () => Promise.resolve('KEY') });
  await R.readChatText(PLAIN);
  const parts = f.calls[0].body.contents[0].parts;
  assert.equal(parts.length, 1, '글 하나만 보내야 합니다');
  assert.ok(!parts[0].inline_data, '사진을 실으면 안 됩니다');
  assert.match(parts[0].text, /김신입씨/, '대화 글이 실려야 합니다');
});

test('★ 요약·할 일·값을 함께 뽑는다', async () => {
  const R = loadRead();
  R.init({ fetch: fakeFetch(JSON.stringify({
    summary: '입사와 식대 이야기가 오갔습니다.',
    todos: [{ t: '김신입 취득신고', done: false, ours: true }],
    rows: [{ name: '김신입', pairs: [{ item: '입사일', value: '2026-08-12', at: '2026-08-12 14:05' }] }]
  })), getKey: () => Promise.resolve('KEY') });
  const r = await R.readChatText(PLAIN);
  assert.equal(r.ok, true);
  assert.match(r.summary, /입사/);
  assert.equal(r.todos[0].t, '김신입 취득신고');
  assert.equal(r.rows[0].pairs[0].at, '2026-08-12 14:05', '값의 출처 시각이 있어야 합니다');
});

test('★ 값의 출처 시각이 없으면 그 짝을 버린다 — 어디서 나왔는지 모르는 값은 안 받는다', async () => {
  const R = loadRead();
  R.init({ fetch: fakeFetch(JSON.stringify({
    rows: [{ name: '갑', pairs: [{ item: '식대', value: '1' }, { item: '기본급', value: '2', at: '2026-08-12 14:05' }] }]
  })), getKey: () => Promise.resolve('KEY') });
  const r = await R.readChatText(PLAIN);
  assert.equal(r.rows[0].pairs.length, 1);
  assert.equal(r.rows[0].pairs[0].item, '기본급');
});

test('이름 없는 줄·인사말 할 일은 버린다', async () => {
  const R = loadRead();
  R.init({ fetch: fakeFetch(JSON.stringify({
    todos: [{ t: '', done: false, ours: true }, { t: '취득신고', done: false, ours: true }],
    rows: [{ name: '', pairs: [{ item: '식대', value: '1', at: 'x' }] }]
  })), getKey: () => Promise.resolve('KEY') });
  const r = await R.readChatText(PLAIN);
  assert.equal(r.todos.length, 1);
  assert.equal(r.rows.length, 0);
});

test('AI 키가 없으면 부르지 않고 한국어로 알린다', async () => {
  const R = loadRead();
  const f = fakeFetch('{}');
  R.init({ fetch: f, getKey: () => Promise.resolve('') });
  const r = await R.readChatText(PLAIN);
  assert.equal(r.ok, false);
  assert.match(r.error, /키/);
  assert.equal(f.calls.length, 0);
});

test('빈 글이면 부르지 않는다 — 헛돈이 나간다', async () => {
  const R = loadRead();
  const f = fakeFetch('{}');
  R.init({ fetch: f, getKey: () => Promise.resolve('KEY') });
  const r = await R.readChatText('   ');
  assert.equal(r.ok, false);
  assert.equal(f.calls.length, 0);
});

test('AI 가 JSON 이 아닌 말을 해도 터지지 않는다', async () => {
  const R = loadRead();
  R.init({ fetch: fakeFetch('죄송합니다'), getKey: () => Promise.resolve('KEY') });
  assert.equal((await loadRead().readChatText(PLAIN)).ok, false);
});

test('★ 지어내지 말라고 못 박는다', () => {
  const src = fs.readFileSync(SRC, 'utf8');
  const m = src.match(/var CHAT_PROMPT =[\s\S]*?;\r?\n/);
  assert.ok(m, 'CHAT_PROMPT 를 찾을 수 없습니다');
  assert.match(m[0], /지어내지/);
  assert.match(m[0], /주민등록번호/, '주민번호는 담지 말라고 해야 합니다');
  assert.match(m[0], /인사말/, '잡담을 걸러야 합니다');
});

test('★ 기존 함수·프롬프트가 그대로다 — 다른 앱이 함께 씁니다', () => {
  const R = loadRead();
  assert.equal(typeof R.read, 'function');
  assert.match(R.PROMPTS.all, /금액과 사람 이름은 담지 마세요/);
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --test tests/paydata-chat-read.test.js`
Expected: FAIL — `readChatText is not a function`

- [ ] **Step 3: 구현**

`js/pu-doc-read.js` 에서 `function read(dataUrl) {` **위**에 넣는다:

```js
  /* ── 대화 판독 (급여데이터함 전용, 2026-08-16) ──
     지금까지의 판독기는 모두 **사진**을 보낸다. 이것은 카톡에서 내보낸 **글**을
     보내므로 배관이 다르다(readPairsWith 는 이미지 전용).
     뽑는 것 셋: 요약 · 할 일 · 값. 할 일 규칙은 공용 chat 갈래에서 옮겨 왔다. */
  var CHAT_PROMPT =
    '아래는 노무법인과 고객사가 카카오톡으로 주고받은 대화입니다. 각 줄은 [시각] 보낸사람: 말 꼴입니다.' +
    ' 급여·인사 업무에 필요한 것만 골라 JSON으로만 답하세요.' +
    '\n키: summary(이 구간에 무슨 얘기가 오갔는지 두세 문장), todos(할 일 — 아래 규칙), rows(값 — 아래 규칙).' +
    '\ntodos 규칙: [{"t":"할 일 한 줄","done":true/false,"ours":true/false}] 배열입니다.' +
    ' **상대가 요청한 것을 먼저** 담고(ours=false), 우리가 하기로 한 것을 다음에 담으세요(ours=true).' +
    ' 대화에서 이미 처리된 것으로 보이면 done=true.' +
    ' **인사말·잡담은 담지 마세요.**' +
    '\nrows 규칙: [{"name":"근로자 이름","pairs":[{"item":"항목","value":"값","at":"2026-08-12 14:05"}]}] 배열입니다.' +
    ' 급여에 반영할 것만 담으세요 — 예 입사일·퇴사일·기본급·식대·직책수당·근무일수.' +
    ' **at 은 그 말이 나온 줄의 시각을 그대로** 적으세요. 시각을 모르면 그 짝을 담지 마세요.' +
    ' value 는 적힌 그대로(날짜는 2026-08-12 꼴, 금액은 적힌 표기 그대로).' +
    ' **확실하지 않으면 지어내지 말고 담지 마세요.** **주민등록번호는 담지 마세요.**' +
    ' 사람 이름이 없으면 그 줄을 담지 마세요. JSON 외 텍스트 금지.';

  function chatFail(message) {
    return { ok: false, error: message, summary: '', todos: [], rows: [] };
  }

  function afterChatRead(parsed) {
    var todos = (Array.isArray(parsed.todos) ? parsed.todos : []).map(function (t) {
      return { t: String((t && t.t) || '').trim(), done: !!(t && t.done), ours: !!(t && t.ours) };
    }).filter(function (t) { return t.t; });
    var rows = (Array.isArray(parsed.rows) ? parsed.rows : []).map(function (p) {
      var pairs = (Array.isArray(p && p.pairs) ? p.pairs : []).map(function (pr) {
        return {
          item: String((pr && pr.item) || '').trim(),
          value: String((pr && pr.value) || '').trim(),
          at: String((pr && pr.at) || '').trim()
        };
      /* 출처 시각이 없는 값은 버린다 — 어디서 나왔는지 못 짚는 값은
         이 설계에서 값이 아니다(설계서 5장). */
      }).filter(function (pr) { return pr.item && pr.at; });
      return { name: String((p && p.name) || '').trim(), pairs: pairs };
    }).filter(function (r) { return r.name && r.pairs.length; });
    return {
      ok: true, error: null,
      summary: String(parsed.summary || '').trim(),
      todos: todos, rows: rows
    };
  }

  function readChatText(plainText) {
    if (!deps.fetch) return Promise.resolve(chatFail('판독 준비가 되지 않았습니다'));
    var body = String(plainText == null ? '' : plainText).trim();
    if (!body) return Promise.resolve(chatFail('읽을 대화가 없습니다'));

    var keyP = deps.getKey ? Promise.resolve().then(deps.getKey) : Promise.resolve('');
    return keyP.catch(function () { return ''; }).then(function (key) {
      if (!key) return chatFail('AI 키가 없습니다 — 포털 설정에서 등록해 주세요');
      return askAny(key, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          contents: [{ parts: [{ text: CHAT_PROMPT + '\n\n' + body }] }],
          generationConfig: { temperature: 0 }
        })
      }).then(function (j) {
        var parsed = parseReply(j);
        if (!parsed) throw new Error('AI가 알아볼 수 없는 답을 보냈습니다');
        return afterChatRead(parsed);
      }).catch(function (e) {
        return chatFail((e && e.message) || String(e));
      });
    });
  }
```

내보내는 목록의 `readChangeNotice: readChangeNotice,` 아래에 넣는다:

```js
    readChatText: readChatText,
```

- [ ] **Step 4: 통과 확인 + 다른 앱 회귀**

```bash
node --test tests/paydata-chat-read.test.js tests/pu-doc-read.test.js tests/paydata-wage-read.test.js tests/paydata-change-notice.test.js
node --test tests/*.test.js > /tmp_t.txt 2>&1; echo "EXIT:$?"; tail -8 /tmp_t.txt
```

- [ ] **Step 5: 커밋**

```bash
git add js/pu-doc-read.js tests/paydata-chat-read.test.js
git commit -m "feat(대화): 글을 보내는 판독기 — 요약·할 일·값"
```

---

### Task 6: 정리 결과 화면 — 글 판독을 실제로 돌린다

**Files:**
- Modify: `pu-paydata.html` (`startMask` · 새 `runChatRead` · `chatOutHtml`)
- Modify: `tests/paydata-mask-screen.test.js` (가림 울타리를 대화까지 넓힌다)
- Test: `tests/paydata-chat-screen.test.js` (이어 붙임)

**Interfaces:**
- Consumes: `PuDocRead.readChatText` (Task 5) · `PuChatParse.sliceMonth`/`toPlain` · `S.rowsFromRead`
- Produces: `runChatRead()` · `chatOutHtml()` · `App.chatOut = {summary, todos, rows}`

> ⚠ **가림 울타리를 건드리는 과제다. 여기가 이 계획에서 가장 조심할 곳이다.**
> 지금 코드는 「판독기를 부르는 곳은 `runRead` 하나, `runRead` 를 부르는 곳은
> `maskConfirm` 하나」를 **검사로 못 박아** 두었다(`tests/paydata-mask-screen.test.js`).
> 사진에서 주민등록번호를 가리지 않고 나가는 길을 막으려고 만든 울타리다.
>
> 대화는 **글**이라 사진 가림을 걸 수 없다. 그래서 가림 화면을 거치지 않는
> 새 길(`runChatRead`)이 생긴다 — 이것은 **대표 결정(2026-08-16, 설계서 6장)**
> 이지 실수가 아니다.
>
> **그런데 지금 울타리 검사는 이 새 길을 못 잡는다.** 검사의 정규식이
> `read|readWageTable|readChangeNotice` 세 이름만 훑기 때문에 `readChatText` 는
> 그냥 지나간다. 그대로 두면 「가림을 거치지 않는 길이 생기면 검사가 깨진다」는
> 말이 **거짓말**이 된다. 다음 사람이 그 말을 믿는다.
> 그래서 **울타리를 넓혀** `readChatText` 도 세고, 그것이 `runChatRead` 하나에서만
> 불리며 왜 가림을 안 거치는지 검사에 적어 둔다.

- [ ] **Step 1: 울타리 검사를 넓힌다 (먼저 한다)**

`tests/paydata-mask-screen.test.js` 의 `★ 판독기를 부르는 곳은 runRead 하나뿐이다`
**아래**에 붙인다:

```js
/* ⚠ 대화(글)는 사진 가림을 걸 수 없어 가림 화면을 거치지 않는다 —
   대표 결정 2026-08-16(설계서 6장). 실수로 생긴 길이 아니라는 것을 여기 못 박고,
   **그 길이 하나뿐인 것**을 지킨다. 위 검사는 이름 셋만 훑어 이 판독기를 놓친다. */
test('★ 글 판독기를 부르는 곳은 runChatRead 하나뿐이다', () => {
  const one = cut('runChatRead');
  assert.ok(one.indexOf('PuDocRead.readChatText(') >= 0, 'runChatRead 가 글 판독기를 안 부릅니다');
  const rest = html.replace(one, '');
  assert.equal(/PuDocRead\.readChatText\(/.test(rest), false,
    '★ runChatRead 말고 다른 곳에서 글 판독기를 부르고 있습니다');
});

test('★ runChatRead 를 부르는 곳은 startMask 하나뿐이다', () => {
  const calls = html.match(/runChatRead\(/g) || [];
  assert.equal(calls.length, 2, 'runChatRead 는 정의 한 번 + startMask 에서 한 번만 나와야 합니다');
  assert.match(cut('startMask'), /runChatRead\(/);
});

test('★ 대화가 아닌 것은 여전히 가림을 거친다 — 울타리가 헐렁해지지 않았다', () => {
  const sm = cut('startMask');
  assert.match(sm, /chattext/, '대화 갈래가 없습니다');
  /* 대화 갈래는 곧바로 돌려보내야 한다 — 아래 가림 여는 코드로 흘러가면
     글 파일에 가림 화면이 뜬다. */
  assert.match(sm, /runChatRead\(\);\s*\r?\n\s*return;/, '대화 갈래가 곧바로 끝나지 않습니다');
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --test tests/paydata-mask-screen.test.js`
Expected: FAIL — `runChatRead 함수를 찾을 수 없습니다`

- [ ] **Step 3: `startMask` 에서 대화를 갈라 보낸다**

`startMask()` 안에서 아래를 찾는다:

```js
  if (rk === 'notice' && !NOTICE_READ_ON) return;
```

그 **바로 뒤**에 넣는다:

```js
  /* 대화는 **글**이라 사진 가림을 걸 수 없다. 가림 화면을 거치지 않고 곧바로
     읽는다 — 대표 결정 2026-08-16(설계서 6장). 글 속 주민등록번호는 지금
     막지 않고, 판독 화면에 경고를 띄우는 것으로 갈음한다.
     ⚠ 이 길이 하나뿐인 것을 tests/paydata-mask-screen.test.js 가 지킨다. */
  if (rk === 'chattext') { runChatRead(); return; }
```

- [ ] **Step 4: `runChatRead` 를 만든다**

`function startMask() {` **위**에 넣는다:

```js
/* 대화 정리 — 고른 달 구간만 글로 보낸다.
   창고 파일은 뷰어가 이미 받아 두었으므로(App.chatText) 다시 받지 않는다. */
function runChatRead() {
  const p = PuChatParse.parseKakao(App.chatText || '');
  const part = PuChatParse.sliceMonth(p.lines, App.chatMonth || (p.months[0] || ''));
  if (!part.length) {
    App.readState = { status: 'err', rows: [], err: '고른 구간에 대화가 없습니다.' };
    App.render();
    return;
  }
  App.readState = { status: 'reading', rows: [], err: '' };
  App.render();

  /* 판독 중에 다른 서류로 옮겨 갈 수 있다 — 늦게 온 답이 새 화면을 덮으면
     엉뚱한 원본이 출처로 붙는다. 사진 판독과 같은 보호를 건다. */
  const forId = App.viewerId;
  PuDocRead.readChatText(PuChatParse.toPlain(part)).then(r => {
    if (App.viewerId !== forId) return;
    if (!r.ok) throw new Error(r.error || '정리하지 못했습니다');
    App.chatOut = { summary: r.summary, todos: r.todos, rows: r.rows };
    App.readState = { status: 'done', rows: S.rowsFromRead('notice', r), err: '' };
    App.render();
  }).catch(e => {
    if (App.viewerId !== forId) return;
    App.chatOut = null;
    App.readState = { status: 'err', rows: [], err: (e && e.message) || String(e) };
    App.render();
  });
}
```

`App` 상태에 `chatOut: null,` 을 더한다(`chatText` 옆).

- [ ] **Step 5: 결과 그리기**

`function readPanelHtml() {` **위**에 넣는다:

```js
/* 대화 정리 결과 — 요약·할 일·값. 값 표는 서류 판독과 같은 것을 쓴다
   (고치면 노란 표시가 걷히는 것도 그대로 따라온다). */
function chatOutHtml() {
  const o = App.chatOut || { summary: '', todos: [], rows: [] };
  const st = App.readState || { rows: [] };
  let h = '<div class="okline">' + esc(App.chatMonth || '전체')
    + ' 구간을 읽었습니다. 원문과 견주어 확인해 주세요.</div>';
  h += '<div class="warnline">⚠ 대화에 <b>주민등록번호</b>가 있으면 그대로 밖으로 나갑니다.</div>';

  h += '<div class="sec">이 달 무슨 얘기가 오갔나</div>';
  h += '<div style="font-size:12.5px;line-height:1.7;margin-bottom:4px">'
    + (o.summary ? esc(o.summary) : '<span class="sub">뽑아내지 못했습니다.</span>') + '</div>';

  h += '<div class="sec">할 일</div>';
  h += o.todos.length ? o.todos.map(t =>
    '<div class="todo' + (t.done ? ' done' : '') + '">'
    + '<span class="tag ' + (t.ours ? 'us' : 'them') + '">'
    + (t.ours ? '우리가 약속' : '상대 요청') + '</span>'
    + '<div>' + esc(t.t) + '</div></div>').join('')
    : '<div class="sub">뽑아낸 할 일이 없습니다.</div>';

  h += '<div class="sec">값으로 남길 것</div>';
  h += st.rows.length ? valueRowsHtml(st.rows)
    : '<div class="sub">급여에 반영할 값은 없습니다.</div>';
  return h;
}
```

꾸밈을 CSS 맨 뒤에 더한다:

```css
.sec{font-size:11.5px;color:var(--sub);font-weight:700;margin:14px 0 7px}
.todo{display:flex;gap:8px;align-items:flex-start;padding:7px 9px;border:1px solid var(--line);border-radius:9px;margin-bottom:6px;font-size:12.5px}
.todo .tag{flex:0 0 auto;font-size:10.5px;border-radius:99px;padding:1px 7px;font-weight:700}
.todo .tag.them{background:#FDF6E3;color:#7C4A03}
.todo .tag.us{background:#EEF2FF;color:#3730A3}
.todo.done{opacity:.55;text-decoration:line-through}
```

- [ ] **Step 6: 검사를 이어 붙인다**

`tests/paydata-chat-screen.test.js` 맨 뒤에 붙인다:

```js
test('★ 요약·할 일·값이 함께 그려진다', () => {
  const fn = html.match(/function chatOutHtml\(\)[\s\S]*?\n\}/);
  assert.ok(fn, 'chatOutHtml 이 없습니다');
  assert.match(fn[0], /무슨 얘기가 오갔나/);
  assert.match(fn[0], /할 일/);
  assert.match(fn[0], /값으로 남길 것/);
  assert.match(fn[0], /상대 요청/, '누가 요청한 것인지 갈라야 합니다');
  assert.match(fn[0], /valueRowsHtml/, '값 표는 서류 판독과 같은 것을 씁니다');
});

test('★ 고른 구간에 대화가 없으면 부르지 않고 알린다 — 헛돈이 나간다', () => {
  const fn = html.match(/function runChatRead\(\)[\s\S]*?\n\}/)[0];
  assert.match(fn, /고른 구간에 대화가 없습니다/);
  const before = fn.indexOf('고른 구간에 대화가 없습니다');
  const call = fn.indexOf('PuDocRead.readChatText');
  assert.ok(before < call, '판독기를 먼저 부르면 안 됩니다');
});

test('★ 늦게 온 답이 남의 화면을 덮지 않는다', () => {
  const fn = html.match(/function runChatRead\(\)[\s\S]*?\n\}/)[0];
  assert.equal((fn.match(/App\.viewerId !== forId/g) || []).length, 2,
    '성공·실패 두 갈래 모두 자리를 확인해야 합니다');
});
```

- [ ] **Step 7: 통과 확인**

```bash
node --test tests/paydata-chat-screen.test.js tests/paydata-mask-screen.test.js
node --test tests/*.test.js > /tmp_t.txt 2>&1; echo "EXIT:$?"; tail -8 /tmp_t.txt
```
Expected: `EXIT:0` — 여기서 Task 4 가 남겨 둔 `chatOutHtml` 자리가 채워져 전체가 맞는다

- [ ] **Step 8: 커밋**

```bash
git add pu-paydata.html tests/paydata-chat-screen.test.js tests/paydata-mask-screen.test.js
git commit -m "feat(대화): 글 판독을 돌린다 — 가림 울타리를 대화까지 넓힌다"
```

---

### Task 7: 값에 출처 시각을 붙여 저장한다
 값에 출처 시각을 붙여 저장한다

**Files:**
- Modify: `js/pu-paydata-store.js` (`rowsFromRead` · `buildValueRows`)
- Modify: `pu-paydata.html` (`saveVals`)
- Test: `tests/paydata-chat-read.test.js` (이어 붙임)

**Interfaces:**
- Produces: 값 줄에 `sourceAt` 칸(선택). 없으면 지금과 같다

- [ ] **Step 1: 검사를 이어 붙인다**

```js
/* ══════ 출처 시각 ══════ */
function loadStore2() {
  const sandbox = { window: {}, console };
  sandbox.globalThis = sandbox;
  vm.createContext(sandbox);
  new vm.Script(fs.readFileSync(path.join(__dirname, '..', 'js', 'pu-paydata-store.js'), 'utf8')).runInContext(sandbox);
  const S = sandbox.window.PuPaydataStore; S.init({ uid: 'U1' }); return S;
}

test('★ 대화에서 온 값은 출처 시각을 지닌다', () => {
  const S = loadStore2();
  const rows = S.rowsFromRead('notice', {
    rows: [{ name: '김신입', pairs: [{ item: '입사일', value: '2026-08-12', at: '2026-08-12 14:05' }] }]
  });
  assert.equal(rows[0].pairs[0].at, '2026-08-12 14:05', '시각을 잃으면 대화 자리로 못 갑니다');
});

test('★ 저장할 때 출처 시각이 값 줄에 남는다', () => {
  const S = loadStore2();
  const out = S.buildValueRows({
    rows: [{ name: '김신입', pairs: [{ item: '입사일', value: '2026-08-12', at: '2026-08-12 14:05' }] }]
  }, { sourceId: 'c1', companyId: 'co_1', slot: '202608', at: 1 });
  assert.equal(out[0].pairs[0].at, '2026-08-12 14:05');
});

test('서류에서 온 값은 시각이 없어도 그대로다', () => {
  const S = loadStore2();
  const out = S.buildValueRows({ rows: [{ name: '갑', pairs: [{ item: '기본급', value: '1' }] }] },
    { sourceId: 'a1', companyId: 'co_1', slot: '202608', at: 1 });
  assert.equal(out[0].pairs[0].at, undefined, '없던 칸을 만들지 않습니다');
});
```

- [ ] **Step 2: 실패 확인 → Step 3: 구현**

`js/pu-paydata-store.js` 의 `rowsFromRead` 안, `wage`/`notice` 갈래에서 `at` 을 함께 옮긴다.

바꾸기 전:
```js
        (Array.isArray(r.pairs) ? r.pairs : []).forEach(function (p) {
          var item = String((p && p.item) || '').trim();
          if (item) pushPair(pairs, item, p && p.value);
        });
```

바꾼 뒤:
```js
        (Array.isArray(r.pairs) ? r.pairs : []).forEach(function (p) {
          var item = String((p && p.item) || '').trim();
          if (!item) return;
          var before = pairs.length;
          pushPair(pairs, item, p && p.value);
          /* 대화에서 온 값은 「그 말이 나온 시각」을 지닌다 — 값에서 대화 자리로
             찾아가는 데 쓴다. 서류에서 온 값에는 없으므로 있을 때만 붙인다. */
          if (pairs.length > before && p && p.at) pairs[pairs.length - 1].at = String(p.at);
        });
```

`buildValueRows` 의 `pairs` 만드는 자리도 `at` 을 살린다.

바꾸기 전:
```js
          pairs: ((p && p.pairs) || []).map(function (pr) {
            return { item: String((pr && pr.item) || ''), value: String((pr && pr.value) || '') };
          }),
```

바꾼 뒤:
```js
          pairs: ((p && p.pairs) || []).map(function (pr) {
            var one = { item: String((pr && pr.item) || ''), value: String((pr && pr.value) || '') };
            if (pr && pr.at) one.at = String(pr.at);   // 대화에서 온 값만 지닌다
            return one;
          }),
```

- [ ] **Step 4: 통과 확인 + 전체 검사 + 커밋**

```bash
node --test tests/*.test.js > /tmp_t.txt 2>&1; echo "EXIT:$?"; tail -8 /tmp_t.txt
git add js/pu-paydata-store.js pu-paydata.html tests/paydata-chat-read.test.js
git commit -m "feat(대화): 값에 출처 시각을 붙인다"
```

---

### Task 8: 값에서 대화 그 자리로 간다

**Files:**
- Modify: `pu-paydata.html` (`openViewerAt` · `closeViewer` · `valueGridModel` · `screenValues`)
- Test: `tests/paydata-chat-screen.test.js` (이어 붙임)

**Interfaces:**
- Consumes: `App.chatHitAt`(Task 3) · 값 줄의 `pairs[].at`(Task 7)
- Produces: `openViewerAt(sourceId, atText)`

- [ ] **Step 1: 검사를 이어 붙인다**

```js
/* ══════ 대화 자리로 가기 ══════ */
test('★ 대화에서 온 값은 그 자리로 가는 길이 있다', () => {
  assert.match(html, /function openViewerAt/, 'openViewerAt 이 없습니다');
  const sv = html.match(/function screenValues\(\)[\s\S]*?\n\}/)[0];
  assert.match(sv, /openViewerAt\(/, '값 표에서 그 자리로 갈 수 있어야 합니다');
});

test('★ 그 자리를 못 찾으면 짚지 않는다 — 엉뚱한 데를 조용히 보여주지 않는다', () => {
  const W = loadChat();
  const h = W.chatBodyHtml('2026년 8월 12일 오후 2:05, 갑 : 있음', '2026-08-99 99:99');
  assert.equal(/class="msg hit"/.test(h), false, '없는 자리를 짚으면 안 됩니다');
});

test('그 자리가 있으면 노랗게 짚는다', () => {
  const W = loadChat();
  const h = W.chatBodyHtml('2026년 8월 12일 오후 2:05, 갑 : 있음', '2026-08-12 14:05');
  assert.match(h, /class="msg hit"/);
});

test('★ 뷰어를 닫으면 짚어 둔 자리도 지운다 — 다음에 열 때 엉뚱한 줄이 노랗다', () => {
  assert.match(cut('closeViewer'), /chatHitAt/);
});

test('★ 서류에서 온 값은 예전처럼 그냥 연다 — 시각이 없다', () => {
  const sv = html.match(/function screenValues\(\)[\s\S]*?\n\}/)[0];
  assert.match(sv, /openViewer\(/, '시각 없는 값은 그냥 열어야 합니다');
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --test tests/paydata-chat-screen.test.js`
Expected: FAIL — `openViewerAt 이 없습니다`

- [ ] **Step 3: 찾아가는 길을 만든다**

`function closeViewer()` **위**에 넣는다:

```js
/* 값에서 대화 그 자리로. **시각**으로 찾는다 — 대화방을 다시 내보내면 줄 번호는
   밀리지만 시각은 그대로다(설계서 5장). 못 찾으면 아무 줄도 짚지 않는다 —
   엉뚱한 줄을 노랗게 하면 그 표시를 못 믿게 된다. */
function openViewerAt(id, atText) {
  App.chatHitAt = String(atText || '');
  openViewer(id);
}
window.openViewerAt = openViewerAt;
```

`closeViewer` 를 바꾼다.

바꾸기 전:
```js
function closeViewer() { App.viewerId = ''; App.render(); }
```

바꾼 뒤:
```js
function closeViewer() { App.viewerId = ''; App.chatHitAt = ''; App.render(); }
```

> `openViewer` 에서는 `chatHitAt` 을 **비우지 않는다** — `openViewerAt` 이 먼저
> 세워 두고 부르기 때문이다. 닫을 때 비우는 것으로 충분하다.

- [ ] **Step 4: 값 칸이 시각을 지니게 한다**

`valueGridModel` 안에서 아래를 찾는다:

```js
      p.cells[pr.item] = { value: pr.value, sourceId: r.sourceId || '', confirmed: !!r.confirmed };
```

바꾼 뒤:
```js
      /* at 은 대화에서 온 값만 지닌다 — 그 말이 나온 시각. 서류에서 온 값은 없다. */
      p.cells[pr.item] = { value: pr.value, sourceId: r.sourceId || '',
        confirmed: !!r.confirmed, at: pr.at || '' };
```

- [ ] **Step 5: 값 표에서 눌러 가게 한다**

`screenValues()` 안, 칸을 그리는 자리를 찾는다(`openViewer(` 를 onclick 에 넣는 곳).
그 칸 만드는 식을 아래처럼 바꾼다 — **시각이 있으면 그 자리로, 없으면 예전처럼**:

```js
        const go = !c.sourceId ? ''
          : (c.at ? 'openViewerAt(\'' + esc(c.sourceId) + '\',\'' + esc(c.at) + '\')'
                  : 'openViewer(\'' + esc(c.sourceId) + '\')');
```

그리고 그 아래 `onclick` 을 붙이던 자리를 `go` 로 바꾼다:

```js
          + (go ? ' onclick="' + go + '"' : '')
```

> 지금 코드가 `onclick="openViewer(...)"` 를 어떻게 짜 넣는지 **먼저 읽고**
> 그 모양에 맞춘다. 따옴표 이스케이프가 그 자리마다 다르다(이 저장소에서 실제로
> 여러 번 어긋난 자리다).

- [ ] **Step 6: 통과 확인 + 전체 검사**

```bash
node --test tests/paydata-chat-screen.test.js
node --test tests/*.test.js > /tmp_t.txt 2>&1; echo "EXIT:$?"; tail -8 /tmp_t.txt
```
Expected: `EXIT:0`

- [ ] **Step 7: 커밋**

```bash
git add pu-paydata.html tests/paydata-chat-screen.test.js
git commit -m "feat(대화): 값에서 대화 그 자리로 — 못 찾으면 짚지 않는다"
```

---

## 마무리


- [ ] `git fetch origin main && git rebase origin/main`
- [ ] `node --test tests/*.test.js > /tmp_t.txt 2>&1; echo "EXIT:$?"` → `EXIT:0`
- [ ] 목업(`docs/mockups/paydata-chat.html`)과 실제 화면을 견준다. 다르면 **화면을 고친다**(목업이 승인받은 것이다)
- [ ] 새 가지로 push → PR — 본문에 **주민번호는 글에서 안 가려진다**는 것을 적는다
- [ ] CI 통과 확인 후 병합
- [ ] 대표님께 **실물 내보내기 앞 20줄**(내용 가리고 꼴만)을 청해 말풍선 가르기가 맞는지 맞춰 본다
