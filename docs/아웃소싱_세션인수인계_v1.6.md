# 급여 아웃소싱 자동화 — 세션 인수인계 (v1.6 · 2026-07-15 기준)

> **v1.6 변경 요약**: 대기 질문 3개 확정(①알림 채널 ②원천세 경계 ③이체 범위) + **소급 정정 방식 확정(③혼용, 엔진 데이터 모델 확정)** + **능동 검토 예외·운영 구멍 8건 전부 해결** + **실무·법령 준수 구멍 6건 전부 해결**(중도정산·일용 매월신고·반기납·개인정보 재위탁·급여일 병목·간이지급명세서).
> D3·D4·D6·D7 확정 반영. 이체 실행 연동은 "확정은 사람" 원칙에 따라 로드맵 후순위 + 실행 전 사람 승인 강제로 명시. 하네스 v2.1에 급여일 분포 조사 추가.
> 미결 잔여: 일용 분해 정책, 세후 부담주체, 파일럿 지정, 노무사 확정 영역(명세서 법정항목·연차촉진 서면요건·위수탁 재위탁 조항 등).

> **사용법**: 새 Claude 창을 열고 이 파일을 업로드(또는 붙여넣기)하면, 이어서 진행할 수 있다.
> 함께 다시 올려야 할 파일: `payroll_mvp.html`, `pu-erp-인사관리-추출.zip`, 급여대장/근태 zip들(필요 시).

---

## 0. 프로젝트 한 줄

푸른노무법인(권형하 대표, 공인노무사)의 **약 110개 사업장(목표 500) 급여·노동법 사무 아웃소싱 자동화 시스템**.
사업장은 하던 대로 자료를 보내고(엑셀·HWP·사진·카톡), 시스템이 읽고·계산하고, 담당자 5명은 확인 클릭만.
급여에서 4대보험 신고·연차·퇴직금·지원금·서식까지 자동으로 딸려 나오는 구조.

## 1. 절대 규칙 (반드시 지킬 것)

1. **코딩 보류**: 설계는 완료 단계. 실제 코딩은 "자료 준비 + 하네스 지시문 투입"으로 시작하며, Claude Code에서 진행 예정. 이 채팅에서는 설계·문서·목업만.
2. **설계·구조 설명 시 반드시 플로우차트(다이어그램) 포함** (사용자 지정 규칙).
3. 가정 금지 — 가정은 명시, 갈리면 질문. 최소·단순 우선. 단계 계획 제시 → 승인 후 진행.
4. UI는 폰·PC 모두 **항상 단순하게**: 한 화면 주행동 1개, 버튼 위치 고정(우하단), 3색 신호(초록/주황/빨강), 전문용어 금지, 되돌리기 가능, "3초 안에 다음 행동을 안다".
5. 자동화 한계: **자동화는 "가져와 알려주기"까지, 반영·확정은 사람 승인** (급여는 법적 책임 행위).

## 2. 아키텍처 (확정)

- **하나의 앱, 하나의 창고(DB)**: 사업장 110개 = 설정 카드 110장(데이터). 앱·DB를 사업장별로 쪼개지 않음. DB 안에서 사업장별 사물함(칸막이)+보안규칙 자물쇠.
- **모듈 먼저**: 공통 부품(수당 블록·규칙 블록·계산 코어)을 먼저 만들고, 사업장은 설정 카드에 값만 채움. 비슷한 곳은 카드 복제(다온케이 10지점=1장).
- **흐름**: 수신함(자동태깅·D-7 미도착 독촉) → 추출(엑셀 직접/HWP/사진 AI 2단) → 입력 어댑터(양식 패밀리 6종→표준) → **표준 엔진 1개** → 검토(오버라이드 기록) → 확정(담당자 종결 기본+사업장별 승인 노무사 지정형) → 출력 어댑터(사업장 기존 양식 재생성+엑셀 사본) → 사업주 전달(회신기한 D-N) → 명세서(사업주 경유 배포, 화이트라벨, 교부대장).
- **파생 자동**: 엔진이 입사/퇴사/휴직/경계초과 감지 → 4대보험 신고(서식 자동, 서명만 사람)·연차(촉진 통보서)·퇴직정산(퇴직소득세·IRP 이연·중도정산까지)·지원금 스캐너·휴직 처리 자동 개시. **[v1.6] 일용직 매월 신고 2종 편입**: 일용직 급여 확정 시 근로내용확인신고(고용보험·매월) + 일용근로소득 지급명세서(매월) 서식 데이터 자동 생성 — 다른 신고와 동일하게 서식 자동·제출(서명)만 사람. 마감 캘린더에 제출 기한 알림 포함(누락=과태료·가산세).
- **기술**: 단일 HTML + Preact(CDN, 빌드 없음 — pu-erp 방식 그대로) + Firebase(Functions·Storage). GitHub Pages 배포. 정식 React 프로젝트는 하지 않기로 확정(필요 신호가 오면 그때 이전 — Preact라 이전 쉬움). 백엔드는 Firebase 기본, 3단계 전 Supabase 재검토(급여만 분리 옵션, 마스터 단방향 동기화). 장기: 급여로 검증 후 pu-erp 등 순차 이전 검토.
- **브랜드**: 네이비 #2E3A8C + 바이올렛 #7C6CE0.

## 3. 실측으로 확인된 사실 (설계의 뼈)

- **국민연금·건강보험 = 매월 계산이 아니라 공단 고지액 반영** (천안상용 3·4·5월 역산: 급여 출렁여도 연금 3개월 고정, 건보는 4월 정산 때만 일괄 변동). → 엔진은 "고지액 모드"(이월 기본+변동달만 갱신).
- **고지액 확보 = 토탈서비스/EDI 조회** (파일 수신 안 함 — 사용자 확인). 이월 + 변동달(7월 연금·4월 건보·입퇴사)만 조회·수기 입력. 고지서 파싱 불필요. 장기: Claude in Chrome 자동 조회.
- **사업장들은 급여 프로그램이 아니라 수기 엑셀로 계산** (사용자 확인). → 고용보험 단수처리가 절사/올림/반올림 어느 것도 전원 불일치(실측: 33명 중 절사22·올림19·반올림28). 요율도 0.9%/0.899%/면제 혼재.
- → **하네스 채점 기준 = "0원 차이" 아님**: 큰 항목(기본급·수당·과세총액) 정확 일치 + 원 단위 허용오차 + 차이 기록. 사업장 엑셀은 정답이 아니라 참고(골든 오류 실재 가능). 불일치는 엔진오류/골든오류/정책차이 3분류로 노무사 판정.
- 지방소득세 = 소득세×10% 절사 (31명 전원 일치 — 이 규칙은 확실).
- MVP 계산 치명 오류 8건 확인(10원 올림, 비과세 초과분 증발, 세액표 근사 등) — pu-erp 코어가 더 성숙(비과세 한도·통상/평균임금·일할 정확).

## 4. 핵심 결정 목록 (전부)

**범위·조직**
- 규모: 약 110곳(담당자 5명×20~30), 목표 500.
- 업무 유형 사업장별: 계산대행/신고대행/검증대행(검증대행=사업장 값 기본+차이 리포트 통지).
- 연말정산 안 함(세무사 안내). 세무사 협업 없음. **단 중도퇴사자 약식 중도정산은 법정 의무라 시스템이 자동 수행(v1.6 확정 — 엔진 퇴직 항목 참조)**. **원천세 경계 확정(v1.6)**: 자동화는 급여대장 + 원천세 집계표(원천징수이행상황신고서 작성용 데이터)까지만 생성. **실제 신고는 사람이 홈택스에서 수행** — "확정은 사람" 원칙과 일관. **[v1.6] 반기납 구분**: 설정 카드에 원천세 납부 주기 필드(월납/반기납) + 집계표 반기 합산 출력 지원. **[v1.6] 간이지급명세서 경계**: 상용직 간이지급명세서(매월 제출 의무 — 지식 기준, 재확인 대상)도 원천세와 동일 경계 — 시스템은 제출용 데이터 생성까지, 제출은 사람이 홈택스.
- 멀티유저: 구글 로그인, 담당자/관리자 2단계. 이관 = 안1(직원 요청→관리자 승인, 인계 패키지 자동). 부재 = 기간 위임+대행 표시(대행자 확정 가능).
- 급여 확정 권한: 담당자 종결 기본 + **사업장별 "검토 승인 노무사" 지정 시 그 승인 필요**.
- 간이세액표: 담당자 누구나 등록 가능, 개정 알림 전원, 이력 기록.

**엔진**
- 코어 = pu-erp 인사관리 추출본(00~06) 이식 후 하네스 기준 교정·확장. 멀티테넌트화(clients/{사업장}/…), 의존성 얇게(dbGet/dbSet), ERP 전용부 제거. MVP에선 검토 워크플로(확신도·원본대조)만.
- 3층: 코어(법정) / 블록(수당·공수·포괄일당 분해기 ON/OFF) / 업종 프리셋(식당·제조·병원·건설+검증규칙).
- 근로자 유형 판정 층: 4주평균→통상/단시간/초단시간 자동, 자격(주휴·연차·퇴직금·보험) 자동 배정, 15h·월60h 경계 감시(서비스 상품), 수동 고정 가능.
- 일용직: 공수 정의표(사업장별) 그대로 입력 + 법정 가산(휴일 최대 2.5배, 공휴일 캘린더) 병행 검증→미달 경고. 포괄일당 분해기(기본·연장·주휴 역산, 최저임금 검증, 계약서·명세서 동일 분해). 연차 포함 분해는 "리스크 검토" 플래그→대표 승인 정책만 자동.
- **최저임금 위반 처리 확정(v1.6)**: 최저임금 미달 감지 시 **기본 = 확정 차단**(경고만으로 통과 불가 — 노무법인 책임 직결). **단 법정 예외는 사유·근거 등록 시 해제**: 수습 감액(최저임금 90%, 1년 이상 계약·단순노무 아닌 경우) / 장애인 적용제외 인가 / 감시·단속적 근로 승인 등 — 사유 + 근거(인가번호·계약서 등) 기록 + 노무사 승인. 예외 없이 미달이면 "최저임금 미달 — 재확인 필요" 표시하고 확정 불가. 이 판정은 **상식 범위 하드 가드레일(2차 그물 ②)의 한 규칙(시급 < 최저임금)으로 편입**.
- 세후 역산(병원 봉직의): 목표 세후→세전 반복 수렴(고지액 고정 제외). 부담주체(일반/네트제/순액) 계약별 설정. 신규 입사 첫 달=계산 모드로 보험료 포함 수렴→취득신고→익월 고지액 전환. 사업소득 3.3%는 수동 지정만.
- 퇴직: 평균임금 자동(직전 3개월+상여·연차 3/12, 체불계산기 모듈 이식 예정)→통상임금 비교→퇴직금. 퇴직소득세 전자동(근속공제→환산→세율→재환산), IRP 과세이연/현금 원천징수영수증. DC는 적립 확인 모드. 홈택스 모의계산 대조 검증.
- **중도퇴사자 근로소득 중도정산 확정(v1.6) = 약식 정산 자동(안2)**: 퇴직 시 근로소득 중도정산은 원천징수의무자의 법정 의무 — "연말정산 안 함"·"세무사 협업 없음"과의 공백 해소. **실무 표준인 약식 정산**: 급여 데이터만으로 계산 가능한 범위(총급여→근로소득공제→기본공제→산출세액→기납부세액 비교→환급/추징)를 퇴직월 급여에 자동 반영. 공제자료 수집 불요 → "연말정산 안 함" 방침과 양립(연말 계속근로자 정산은 계속 안 함). **원천징수영수증 자동 발급 + "추가 공제는 이듬해 5월 종합소득세 신고" 안내문 자동 첨부**(근로자 민원 예방). 퇴직 자동화 파이프라인에 편입. ※ 세부 산식(공제 구간·표준세액공제 범위)은 웹검색 재확인 체크리스트에 추가, 약식 범위 최종 확정은 노무사(대표).
- 소급 정정: **자주 발생 → 일상 기능으로 설계**(마감 후 수정은 오직 '소급 정정 절차'로만). **반영 방식 확정(v1.6) = ③ 혼용(사유 기반)**: (1) 관리자만 정정 절차 개시, (2) 정정 사유 입력(수당누락/오분류/소급인상 등), (3) 시스템이 과세소득·4대보험 변동 여부를 계산해 **방식 자동 제안** — 세금·보험 영향 없는 소액은 '다음달 합산' 제안, 과세·보험 변동 시 '재발행(해당 월 재계산 + 정정 명세서 + 원천세·보험 정정신고 안내)' 제안, (4) 담당자/노무사가 제안 수정 가능·**사람 승인 후 확정**, 사유·근거·감사로그 기록, **원본 월 데이터는 절대 보존**. "확정은 사람" 원칙 준수.
  - **엔진 데이터 모델 함의(확정)**: 급여 레코드는 월별 **불변(immutable) + 버전 체인** 구조 — 마감 시 스냅샷 잠금, 정정은 원본을 덮어쓰지 않고 correction 레코드(원본월 참조 + 방식 + 사유 + 차액 + 승인자)를 별도 생성. 월 마감·잠금(D4)은 이 스냅샷 위에 성립. 신고 데이터(원천세 집계표·4대보험)는 correction 방식이 '재발행'일 때만 해당 월 재집계 대상으로 표시.
- **4대보험 고지액 타이밍 갭 처리 확정(v1.6)**: 국민연금·건보는 공단 고지액 반영인데, 입퇴사 많은 달엔 고지가 급여 마감보다 늦게 나옴(취득/상실→공단 반영→고지 시차). 급여는 못 늦춤. → **새 규칙 불필요, 기존 두 모델 조합으로 해결**: (1) 마감 시점에 고지 없으면 **추정 가마감**(연금은 월 고정이라 대부분 전월 고지액 그대로 맞음, 신규 입사자만 첫 달 계산 모드 — 이미 세후역산 항목에 규칙 존재) + '추정' 플래그, (2) 고지 확정 후 차이나면 **소급 정정(③혼용)으로 정산**. 즉 미수신 처리 + 소급 정정과 한 데이터 모델.
  - **[v1.6] 소급 허용 기간 상한**: 무한 소급 방지 — 원천세 경정청구·4대보험 소멸시효(통상 3년)와 맞춰 소급 정정 허용 기간 상한 설정. 상한 초과 건은 자동 처리 불가·노무사 수동 판단 표시.
- 오버라이드: 모든 직접 수정 기록(누가·언제·원값→새값·사유)+셀 표시+근태 불일치 플래그+하네스 분리 채점.
- **급여 이체 확정(v1.6)**: 둘 다 지원. **기본 = 은행 업로드용 이체 파일 생성**(계좌·금액 확정 후 은행 대량이체 포맷 산출, 사람이 은행에서 실행). **옵션 = 원하는 사업장만 펌뱅킹/오픈뱅킹 연동으로 이체 실행**. ⚠️ 이체는 되돌릴 수 없는 확정 행위 — "확정은 사람" 원칙과 유일하게 부딪히는 지점이므로: (1) 연동 사업장이라도 **이체 실행 직전 사람의 최종 승인 + 2차 인증 강제**, (2) **MVP 범위는 파일 생성만 구현**, 실행 연동은 **로드맵 후순위로 분리**(은행별 계약·보안 심사·전용 인증서 필요 → 초기 개발에 넣으면 출시 지연).

**수집·OCR**
- **자료 미수신·지연·오류 실패 경로 확정(v1.6)** — 설계 전제를 "자료가 온다"에서 **"안 와도 급여는 나간다"(법정 지급일 존재)**로 전환. 마감일 도달 시 자료 상태 4분기: (1) 정상→정규 처리, (2) 지연→다채널 독촉 강화 후 재판정, (3) 일부/오류→검증 실패 반려·사유 명시 재요청, (4) 끝내 미수신→담당자 강제 개입.
  - **미수신 시 기본 처리 = 사업장별 기본값 설정**: 설정 카드에 "자료 미수신 시 기본 처리" 필드(전월값 가마감 / 담당자 수기 입력). 초기 구축 시 과거 대장 변동성 보고 **기본값 자동 초안**(변동 적으면 전월값 추천). **'지급 보류'는 기본값 아님** — 임금체불 위험 때문에 담당자 수동 선택 옵션(체불 경고 표시).
  - **공통 원칙**: 가마감·추정값은 반드시 **'추정' 플래그** + 자료 도착 시 **소급 정정 절차(③혼용)로 자동 연결**(원본월 보존). 즉 미수신 처리와 소급 정정이 한 데이터 모델로 연결됨.
- 수신 메일 = 법인 도메인 신설(예: 자료@fairrunlabor.com, 서버 웹훅). 다음메일은 자동전달 유입구로 존속(사업장 재안내 불필요). 발신은 기존 payroll@fairrunlabor.com(Resend 인증 완료).
- 카톡·문자: 자동 수집 불가/금지 → 직원 "공유하기→앱" 1탭 업로드(지금) + 카톡 채널·문자 가상번호(중기 유도, 강제 없음).
- OCR = LLM 비전: Gemini Flash 1차(장당 1~5원) + Claude 재판독(저확신만). 텍스트 PDF는 직접 추출. 3중 방어(행합계 검산→2모델→사람). 예산 월 3만 + 백필 일회성 10만. 사진 주민번호는 전송 전 자동 마스킹.
- **2차 그물 = '조용한 오염' 방어 확정(v1.6)** — 기존 3중 방어는 "숫자가 서로 맞나"(파싱 실패)를 잡고, 2차 그물은 "이 값이 애초에 말이 되나"(그럴듯한 오독)를 잡음. 성격이 달라 둘 다 필요. **엔진 핵심 안전장치로 처음부터 탑재**:
  - **① 전월 대비 급변 감지**: 직원별 항목값이 전월 대비 ±X% 초과 시 사람 확인 플래그. 정당한 급변(신규입사·상여달·소급인상)은 사유 달면 통과. 임계값은 튜닝 가능(초기 넉넉히→파일럿 오탐률 보고 조정).
  - **② 상식 범위 체크(하드 가드레일)**: 물리적 불가능·법정 한도 초과 차단 — 월 연장근로 한도 초과, 근무일수 > 그 달 일수, 시급 < 최저임금, 공제액 > 지급액 등. 절대 규칙이라 오탐 적음 → 강하게 적용.
  - **③ 교차 검증**: 서류 간 대조 — 근태 근무일수 ↔ 대장 지급일수, 4대보험 대상인데 공제 0 등.
  - 세 그물 모두 "차단"이 아니라 **사람 앞으로 끌어올리기**("확정은 사람" 준수). 오탐 피로 방지를 위해 급변 임계값은 파일럿에서 조정 전제.

**고객 접점**
- 로그인 = 관리자 5명 + 사업주 소수만. **근로자는 로그인·연락처·직접발송 없음**(현재 연락처 미보유, 모든 자료 사업주 경유 배포 — 사용자 확인). 근로자 앱 만들지 않음(확정, 메모리 저장됨). **[v1.6] 퇴사자 서류**: 근로자 로그인 없으므로 퇴사자 명세서·원천징수영수증 요구 시 **사업주 경유 재발급 경로**만(휴지통·보존 기간 내 재출력).
- 사업주 포털(7단계, PWA): 자료 제출·검토본 승인·명세서/지원금 열람. 화이트라벨(사업장 로고·상호·색 — 필드 3개, 명세서 PDF에도).
- 앱 형태: PWA 먼저 → 검증 후 필요 시 앱스토어 래핑(Capacitor). 처음부터 앱스토어 안 감.
- **알림 채널 확정(v1.6)**: 담당자 5명 기한 알림(D-7 미도착·회신기한 등)은 **앱 내 알림센터(기본) + 이메일 병행 + 카카오 병행**. 문자(SMS)는 채택 안 함. 중요 기한은 다중 채널 동시 발송으로 누락 방지. (카카오는 알림톡 채널 발신 — 발신 프로필·템플릿 사전 심사 필요, 로드맵에 준비 항목으로 반영.)

**보안 (7겹)**
①진짜 인증(익명 폐지·2FA) ②전송(HTTPS+마스킹) ③저장(주민번호·계좌 AES-GCM, **키=서버 보관 A안 확정**, 최소수집) ④DB규칙 서버 강제(담당자∨부담당자∨위임기간 대행자∨관리자) ⑤감사로그·이상탐지 ⑥퇴사자 즉시 차단·다운로드 제한·자동 로그아웃 ⑦스냅샷 백업·통지 절차 + 위수탁 계약(노무사 작성).
- **[v1.6] 개인정보 재위탁·국외이전 대응**: 데이터 흐름 = 사업장(위탁)→노무법인(수탁)→Gemini·Firebase(재수탁, 국외 가능). 주민번호를 마스킹해도 **이름+급여액도 개인정보** → 조치: (1) 위수탁 계약서에 **재위탁 동의·국외이전 조항 명시**(노무사 확정 영역), (2) **Firebase 서울(asia-northeast3) 리전 사용 확정**, (3) **GitHub 저장소 비공개(private) 확정**(코드·설정 공개 방지), (4) OCR 외부 전송분은 최소화(이름 마스킹 가능 여부 구현 시 검토).

**운영 (2차 검토 8영역 결론)**
- 초기 구축: 과거 대장에서 **설정 카드 자동 초안**→확인 10~20분/곳→복제, 물결식(파일럿→패밀리 대표→확장), 신·구 병행 계산 일치 시 전환. 총 ≈16시간(5명 분산).
- 실패 대응: 원본 절대 보존·정정본 재발행·확정본 엑셀 사본 상시(시스템 죽어도 급여 나감)·D-2~3 여유 확정·휴지통 30일·확정 취소. **[v1.6 추가] 복구 리허설**: "확정본 엑셀 사본만으로 급여 나감"은 훈련 안 하면 위기 때 안 돌아감 → 파일럿 중 1회 "시스템 없다 치고 엑셀 사본만으로 급여 지급" 훈련 필수(파일럿 계획에 편입).
- 법·제도(노무사 확정 영역): 명세서 법정항목 체크리스트 내장, 전자교부 요건, **연차촉진 서면 요건**(전자+열람기록/서면 병행 — 조합은 대표 확정), 위수탁 계약서·처리방침, 신고 위임.
- 사업·운영: 파일럿=단순·협조 2~3곳(나루트·피앤피·공주 등 근태↔대장 짝 맞는 곳 후보). 변화관리=병행+챔피언 1명+대표 주도. 기간은 하네스 후 추정(하네스 수일~2주/엔진 2~4주/서버 3~6주 감).
- **숙련자 이탈 방지 전략 확정(v1.6)** — 최대 실패 원인(사람 문제). 악순환 구조: 초기엔 시스템이 암묵지를 몰라 숙련자가 더 정확 → "내가 하는 게 빠름" → 시스템 안 씀 → 데이터·피드백 안 쌓임 → 영영 못 따라잡음. **끊는 3개입 + 강제**:
  - **① 병행 기간 시스템 우선 확인 의무(강제)**: 손값 입력 전 시스템 결과를 먼저 확인. 단 프레이밍은 "믿어라"가 아니라 **"감독하라"**(숙련자=검수자, 결과 보고 틀리면 고침=학습 데이터). **종료 조건 명시**: 그 사업장 신·구 값 N개월 연속 일치 시 강제 해제(끝이 보이게).
  - **② 암묵지 선주입**: 직원 스터디(16영역 자료 완료) + 암묵지 관리(설정 카드 승격/사업장 노트)를 **도입 전** 시스템 초기 설정에 미리 반영.
  - **③ 불일치=학습 기회**: 병행 중 손값≠시스템값이면 오버라이드 기록 + 승격 루프(반복→규칙 제안, 분기별 대표 리뷰)로 암묵지를 시스템 규칙으로 승격. 병행 기간 = 격차 해소의 시간.
- 테스트: 계산=하네스(배포 관문), 안전 로직 5개만 자동 테스트(권한격리·마스킹·오버라이드 기록·확정잠금·발송대상), 화면=파일럿 실사용, 회귀=하네스 전수 재채점.
- 기준값: 연도별 이력 구조(값은 코드에 안 박음). 갱신 캘린더 내장(1월 최저임금·7월 연금·4월 건보·세액표). **자동 수급**(공공데이터 API: 최저임금·요율·공휴일 / 파일 파싱: 간이세액표 / 공고 감지+AI: 지원금) → 변경 알림 → **노무사 승인 게이트** → 반영. 법 해석 필요한 것(퇴직세·휴가휴직)은 사람 확인. 완전 무인 반영 금지.
- 내 지식 시점 주의: 설계 속 수치(요율 4.5%·건보 3.545%·비과세 20만 등)는 2026.1 기준 예시 — 구현 시 웹검색 재확인 12항목 체크리스트 있음.

**추가 모듈 (v1.5 이후 확정)**
- **서식함**: 3층(법인 표준/사업장 커스텀/발급본). 토큰 치환({{성명}} 등) 자동 작성, 입사 패키지(유형별 계약서 자동 분기), 증명서 클릭 발급+도장 자동 날인(kcareer 파이프라인 이식 — kcareer 작업이 선행 개발), 미서명 경고(과태료 방지), 서식 버전 관리(법 개정 플래그). 6단계 묶음.
- **암묵지 관리**: 규칙형→설정 카드 필드 승격(시스템 실행) / 서사형→사업장 노트(고정 항목: 연락규칙·키맨 성향·자료 특성·과거 분쟁·돈 주의 + 자유 메모)+근로자 메모+타임라인(자동+수동). "그 자리에서 3초" 원칙. 승격 루프(반복 메모→규칙 제안, 분기별 대표 리뷰). 개인 수첩 금지, 사실 위주 작성, 전체 검색, 담당자+관리자만 접근.
- **실시간 기록**: 플로팅 ✍️ 버튼(모든 화면 우하단, 보던 사업장 자동 지정), 분류 칩 1탭+한 줄, 카톡 "공유하기→앱" 1탭(원문이 근거로), **음성 메모**("사업장이름, 내용" 말하면 STT→맨 앞 이름으로 사업장 자동 인식, 직전 통화번호 보조, 애매하면 후보 1탭, 인식 실패해도 원음성 보존, 운전 모드 큰 버튼). PC는 셀 우클릭 메모.
- 첫 암묵지 수집 = 직원 스터디(자료 이미 제작: 16영역 쉬운 설명+확정사항+담당자 기입란, 중복 방지 영역 구분).

## 5. 3차 정밀 검토 결과 (최신 — 반영 대기)

월 사이클 시뮬레이션으로 12건 발견. **높음 3건은 설계 반영 확정**:
- **D1 임금 산정 기간**: "1일~말일" 암묵 전제 깨짐(26일~25일 마감 사업장 흔함) → 설정 카드에 "산정 기간" 필드 + 하네스가 대장 날짜 범위 역산으로 자동 감지.
- **D2 동명이인**: 근태표엔 이름만(주민번호 없음) → 이름 정규화+구분 별칭 필드+애매하면 자동 매칭 금지(사람 확인 플래그) + 하네스 동명이인 실태 조사.
- **D3 알림 수단 [확정 v1.6]**: 앱 내 알림센터(기본) + PWA 푸시 + **이메일 병행 + 카카오(알림톡) 병행**. 문자(SMS) 미채택.

중간 4: D4 월 마감·잠금(마감 후엔 소급 정정 절차로만, 관리자만 해제 — **소급 정정 혼용 방식 확정으로 데이터 모델 확정: 불변 스냅샷 + correction 버전체인**) / D5 **개발·운영 데이터 분리**(경로 분리+테스트 띠 표시 — 3단계 첫 규칙) / **D6 원천세 경계 [확정 v1.6]**: 집계표까지만 생성, 신고는 사람이 홈택스 / **D7 이체 [확정 v1.6]**: 기본 파일 생성 + 옵션 연동(실행 전 사람 승인·2FA 강제, MVP는 파일만).
낮음 5: D8 대행자 확정 가능(대행 표시) / D9 투잡 주민번호 중복 감지→노무사 판단 / D10 결근·조퇴 공제 방식 필드 / D11 폐업·휴업 이벤트 / D12 전역 검색·배포 세션 안내.

### ✅ 질문 3개 + 소급 정정 확정 완료 (v1.6)
1. **알림 채널** → 앱 알림센터(기본) + PWA 푸시 + 이메일 병행 + 카카오 병행. SMS 미채택.
2. **원천세** → 집계표(이행상황신고서 작성용 데이터)까지만 생성. 신고는 사람이 홈택스.
3. **급여 이체** → 기본 파일 생성 + 옵션 사업장만 펌뱅킹/오픈뱅킹 연동. 실행 전 사람 승인·2FA 강제, MVP는 파일만.
4. **소급 정정 [확정 v1.6]** → ③ 혼용(사유 기반): 사유 입력 → 시스템이 합산/재발행 자동 제안 → 사람 승인. 엔진은 불변+버전체인, correction 레코드 별도 생성(원본월 보존). D4 마감·잠금이 이 스냅샷 위에 성립.

### ★ 잔여 미결 (우선순위 순 — 다음에 다룰 것)
- 일용 분해 정책(연차 포함) 대표 승인 / 세후 부담주체(병원별) / 파일럿 2~3곳 지정 / 명세서 법정항목·연차촉진 서면요건 확정(노무사) / 백엔드 최종(3단계 전).

### ⚠ 검토 중 발견 — 반영·논의 대기 (검토자 지적)
- **성공 기준 92% 모수 근거**: 모수 49의 출처 불명. 파싱 실패가 특정 패밀리(예: DB형)에 몰리면 92% 넘겨도 못 쓰는 사업장 발생 → **채점을 패밀리별로도 분리** 권장(하네스 v2.1 커버리지 리포트에 반영).
- **근태↔대장 매칭 키 부재**: 근태표엔 이름만·주민번호 추출 금지 → 동명이인(D2)·투잡 중복(D9)에서 이름 매칭 필연 실패. **매칭 키 후보(사번·부서·입사일 조합) 실태를 하네스 1차 조사 항목에 추가** 권장.
- **암호화 키 서버 보관(A안) 리스크**: 서버 침해 시 키·데이터 동일 위치 → 위수탁 계약·보안 심사 지적 가능. 대표(노무사)에게 리스크 명시 필요.

### 🔍 능동 검토로 발견한 예외·운영 구멍 8건 (v1.6 추가)
설계가 해피패스는 촘촘하나 예외·경계·운영 현실에 공백. 심각도순. **진행: 8건 전부 해결·반영 완료(🔴 1~3 설계직결 / 🟠 4~6 파일럿전 / 🟡 7~8 기록)**:
- **[🔴 해결 v1.6] 1. 자료 미수신 실패 경로** → 사업장별 기본값(전월값/수기) + 추정 플래그 + 소급정정 자동연결로 확정(4절 수집·OCR 반영).
- **[🔴 해결 v1.6] 2. OCR '조용한 오염'** → 2차 그물 3종(전월 급변 감지 + 상식 범위 하드가드레일 + 서류 교차검증) 엔진 핵심에 탑재 확정(4절 OCR 반영). 사람 앞으로 끌어올리기 방식, 급변 임계값은 파일럿 튜닝.
- **[🔴 해결 v1.6] 3. 최저임금 위반** → 기본 확정 차단 + 법정 예외(수습감액·장애인 적용제외·감단직)는 사유·근거 등록 + 노무사 승인 시 해제. 하드 가드레일 규칙으로 편입(4절 일용직 반영).
- **[🟠 해결 v1.6] 4. 숙련자 이탈 리스크** → 병행기간 시스템 우선 확인 의무(강제, "감독하라" 프레이밍 + N개월 일치 시 해제) + 암묵지 선주입 + 불일치=승격. 흩어진 재료(직원스터디·승격루프·오버라이드)를 한 전략으로 묶음(운영 사업·운영 반영).
- **[🟠 해결 v1.6] 5. 4대보험 고지액 타이밍 갭** → 새 규칙 불필요. 추정 가마감(연금 월고정+신규만 계산모드)+플래그 → 고지 후 소급정정 정산. 미수신+소급 모델 조합(4절 엔진 반영).
- **[🟠 해결 v1.6] 6. 백업·복구 리허설** → 파일럿 중 1회 "시스템 없다 치고 엑셀 사본만으로 급여 지급" 훈련 필수(운영 실패대응 반영).
- **[🟡 반영 v1.6] 7. 퇴사자 서류 접근** → 사업주 경유 재발급 경로만(고객 접점 반영).
- **[🟡 반영 v1.6] 8. 소급 정정 상한** → 경정청구·소멸시효(통상 3년)와 맞춰 상한 설정, 초과 건 노무사 수동 판단(4절 엔진 반영).

### ⚖️ 실무·법령 준수 재검토로 발견한 구멍 6건 (v1.6 2차 검토 — 전건 반영 완료)
노무법인이 대행하며 매달 지는 법정 의무 관점 재검토. ※ A~C·F는 세법·사회보험 실무로 2026.1 지식 기준 — 세부 요건은 구현 시 웹검색 재확인 + 노무사 최종 판단:
- **[🔴 해결 v1.6] A. 중도퇴사자 중도정산** → 약식 정산 자동(안2) 확정: 기본공제 수준까지 퇴직월 급여에 자동 반영 + 원천징수영수증·5월 종소세 안내문 자동 발급(4절 엔진 반영). "연말정산 안 함"과 양립.
- **[🔴 해결 v1.6] B. 일용직 매월 신고 2종** → 파생 자동에 편입: 근로내용확인신고 + 일용근로소득 지급명세서 서식 데이터 자동 생성, 제출(서명)만 사람 + 기한 알림(2절 파생 자동 반영).
- **[🟠 해결 v1.6] C. 원천세 반기납 구분** → 설정 카드에 납부 주기 필드(월납/반기납) + 집계표 반기 합산 출력(4절 범위·조직 반영).
- **[🟠 해결 v1.6] D. 개인정보 재위탁·국외이전** → 위수탁 계약서에 재위탁·국외이전 조항(노무사 확정) + Firebase 서울 리전 확정 + GitHub 비공개 확정 + OCR 전송 최소화 검토(보안 반영).
- **[🟡 반영 v1.6] E. 급여일 집중 병목** → 하네스 v2.1에 "급여일 분포 조사" 목표 추가(지시문 6번) → 5명 처리 캘린더 설계 근거.
- **[🟡 반영 v1.6] F. 상용직 간이지급명세서** → 원천세와 동일 경계 명시: 데이터 생성까지 시스템, 제출은 사람(4절 범위·조직 반영).

→ v1.6 설계서 갱신 완료. **하네스 지시문 v2.1 확정 완료**(7절 + 별도 파일). 다음: 자료 준비 → Claude Code 착수.

## 6. 착수 조건 & 자료

- [x] 자료: 급여대장 4명분 전부 확보(신욱임·주민정 + **박은비·김보람 v1.6에서 수신·검증 완료**) / 근태 3명분(주민정·박은비·김보람). **신욱임 근태만 미제출 — 착수 비차단**(매칭 조사에서 해당 사업장만 제외, 추후 증분). 옛 파일 표본은 박은비 zip 내 2021~22년 자료로 일부 대체 가능.
- [ ] PC 작업 폴더: `samples/`(급여대장) + `raw/`(근태) + `payroll_mvp.html` + `puerp/`(pu-erp 인사관리 추출)
- [ ] Claude Code에 아래 지시문 투입 → 하네스 1차 시작

## 7. 하네스 1차 지시문 (v2.1 — 확정)

> 별도 파일 `하네스1차_지시문_v2.1.md`로 분리 관리. Claude Code에 이 코드블록을 그대로 투입.

```
# 급여 아웃소싱 자동화 — 하네스 1차 구축 지시문 (v2.1)

> **투입 방법**: 이 파일을 Claude Code 세션에 올리고 "이 지시문대로 하네스 1차 시작"이라고 요청.
> **v2.1 변경점**: D1(산정기간)·D2(동명이인) 조사 정식 편입 + 패밀리별 채점 분리 + 근태↔대장 매칭 키 실태 조사 추가.

## 배경
나는 노무법인 대표다. 약 110개 사업장의 급여 아웃소싱 자동화 시스템의 첫 단계로,
"골든(완성 급여대장) 파서 + 채점 기반"인 하네스를 만든다.
**이번 세션 목표는 하네스 1차뿐. 그 이상 만들지 마라.** (엔진·어댑터·서버·계산교정은 다음 세션.)

## 폴더
- `./samples/{담당자}/…/{월}/` = 완성 급여대장(골든)
- ※ 원본 zip을 리눅스에서 풀 경우 한글 파일명 CP949 주의(`unzip -O cp949` 또는 파이썬 cp437→cp949 재해석). 폴더 구조 없는 낱개 급여대장(담당자 루트에 존재)도 정상 입력으로 취급.
- ※ 실측: .xls 구형이 담당자 2명 합계 142개로 많음 — 1차는 규칙대로 건너뛰되 리포트에서 비중 확인, 크면 2차에서 .xls 판독(xlrd) 추가 결정.
- `./raw/` = 근태 원천
- `./payroll_mvp.html`, `./puerp/` = 참고(수정 금지)
- 결과 → `./harness/`

## 반드시 알아야 할 사실 (실측 확인)
1. 사업장은 급여 프로그램이 아니라 **수기 엑셀** — 고용보험 단수처리(절사/올림/반올림)가 뒤섞여 단일 규칙으로 전원 안 맞는다. 요율도 0.9%/0.899%/면제 혼재.
2. **국민연금·건강보험은 공단 고지액 그대로**(연금 월 고정, 건보 4월 변동) — 계산 재현 시도 금지.
3. **골든 자체에 오류 있을 수 있음** — 맹신 금지. 골든은 정답이 아니라 참고.
4. 지방소득세 = 소득세×10% 절사 (이 규칙만 확실).

## 목표 (이것만)

### 1) 골든 파서: 전 급여대장 엑셀 → 직원별 JSON
필드: `{성명, 입퇴사일, 유형추정, 기본급, 수당들, 과세총액, 소득세, 지방세, 연금, 건보, 장기, 고용, 공제총액, 실수령}`
- 2단 병합 헤더 평탄화 / 급여대장 시트 자동 식별(시트 혼재)
- **파일명 불신** — 내용으로 귀속월 판별(5월 폴더에 6월 파일 실재)
- 양식 패밀리 6종 태깅: 표준형 / 단순형 / ★DB형 / 직군분리형 / 근태기반형 / 연간누적형
- `.xls` 구형·손상·PDF는 건너뛰고 **사유 기록**
- **주민등록번호 추출 금지(성명만). 외부 API 전송 금지 — 전부 로컬.**

### 2) 단수처리 자동 판정
사업장별 고용보험 절사/올림/반올림 중 **최다 일치 방식** 리포트 (+요율 0.9%/0.899%/면제 편차 감지).

### 3) 산정 기간 자동 감지 (D1)
각 사업장 대장의 날짜 범위를 역산 → "1일~말일 / 26일~25일" 등 판별. 감지 실패 시 사유 기록.

### 4) 동명이인 실태 조사 (D2)
사업장 내 동일·유사 이름 목록화. 유사 판정은 이름 정규화(공백·한자·오타) 후 비교.

### 5) [신규] 근태↔대장 매칭 키 실태 조사
근태표엔 이름만 있고 주민번호 추출은 금지 → 이름만으로는 동명이인·투잡에서 매칭 필연 실패.
※ 근태 확보 현황: 주민정·박은비·김보람 3명분 있음 / 신욱임 근태 미제출 — 신욱임 담당 사업장은 "근태 미제출"로 기록하고 조사에서 제외, 추후 도착 시 증분 조사.
**대장·근태 양쪽에 어떤 매칭 키 후보가 실재하는지 조사**: 사번, 부서, 입사일, 직급, 연락처 뒷자리 등.
- 각 사업장별로 "이름 단독 매칭 가능 / 보조키 필요 / 매칭 불가" 3분류
- 근태·대장 이름 대조 시 한쪽에만 있는 이름(unmatched) 목록

### 6) [신규] 급여일 분포 조사
사업장별 급여 지급일(대장·파일명·내용에서 추정) 분포표 — 10일·25일·말일 집중도 파악(담당자 5명 처리 캘린더 설계용). 추정 불가 시 사유 기록.

### 7) 커버리지 리포트 (HTML 1장)
- 사업장 × 월 성공/부분/실패 표
- 사업장별 실수령 합계(눈대조용)
- 실패 사유
- 단수 판정 / 산정기간 / 동명이인 / 매칭 키 실태 / 급여일 분포

## 성공 기준 (v2.1 — 패밀리별 분리)
파싱 가능 엑셀(구형·손상·PDF 제외 = 모수 산출) 중 **전체 92%+**에서 직원별 실수령 추출.
**단, 전체 92%만 보지 말고 패밀리 6종 각각의 성공률을 별도 표기**한다.
- 특정 패밀리(특히 DB형)에 실패가 몰리면 전체 92%를 넘겨도 "그 패밀리는 실사용 불가"로 판정 → 리포트에 경고.
- 패밀리별 모수가 너무 작아(예: 3건 이하) 통계가 무의미하면 그렇게 명시.
- **모수 산출 근거를 리포트에 명시**: 전체 파일 N개 중 제외 M개(구형/손상/PDF 각각 몇 개), 파싱 대상 모수 = N−M.

## 다음 세션 예고 (이번엔 금지)
근태→대장 변환 추적·어댑터 / 엔진 골든 대조 채점(큰 항목 일치 + 원단위 허용 + 3분류) / 계산 교정 / HWP 처리.

## 작업 방식 (엄수)
- 가정 금지 · 해석 갈리면 질문 / 최소 코드
- **단계 계획 먼저 제시 → 승인 후 진행**
- 가장 단순한 패밀리 1종부터 검증 후 확장
- Python + openpyxl, 의존성 최소
- HWP는 다음 세션
- 보고는 "했다 · 안 됐다 · 다음" 명확히

```

## 8. 로드맵 (확정)

1.하네스 → 2.엔진 이식·교정(주민정 1곳 3개월 큰항목 일치) → 3.서버·멀티유저·수신함·보안·개발/운영분리·백엔드 최종 → 4.어댑터 확장 → 4.5.백필(10년, Tier0/1/2, 합계대사) → 5.파일럿 2~3곳 병행 전환 → 6.발송·신고·사무대행·퇴직·휴직·지원금·서식함 → 7.사업주 포털(PWA·화이트라벨). (구 8단계 근로자 앱 = 삭제 확정)

## 9. 이번 세션 산출물 (참고 — 새 창에선 안 보임, 다운로드해 둘 것)

종합설계서_v1.5.html(마스터), 3차정밀검토_디테일12건.html, 직원스터디_전체정리.html, 서식함_설계, 암묵지_관리설계, 암묵지_실시간캡처, 실시간기록_화면, 음성메모_설계, UI단순함_원칙, 기술스택_선택, 전체연결지도, ERP대시보드_목업, 담당자로그인_화면, 급여관리_대시보드, 급여대장_편집화면, 화면디자인_초안(모바일 6장), 계산규칙_역산결과, 과거10년_백필설계, 퇴직금_자동화설계, 초단시간_단시간_설계, 일용직_설계, 세후역산_설계, OCR_검토, 수집채널_통합설계, 수신메일_결정, 정부지원금_설계, 기성시스템_검토, Supabase_검토, 데이터보안_설계, 고객앱_설계, 고객접점_단순화, 화이트라벨_설계, 창고구조_모듈우선, 설계_교차감사보고서, 영역1~8 검토, 기준값_자동수급, 하네스1차_지시문_v2.md, 최종통합설계서 v1~v1.4.

## 10. 새 세션 첫 액션 (v1.6 이후 · 권장 순서)

1. 이 문서(v1.6) 업로드 → "인수인계 확인" 요청
2. ✅ 완료: 질문 3개(알림·원천세·이체) + 소급 정정 방식(③혼용) 확정 → 2·4·5절 반영, 엔진 데이터 모델 확정
3. ✅ 완료: 하네스 지시문 v2.1 확정 (7절 + 별도 파일 `하네스1차_지시문_v2.1.md`)
4. **[다음] 착수 조건 충족**: 미제출 자료(박은비·김보람 급여대장 zip / 신욱임 근태표 zip) 수집 + PC 작업 폴더 구성
5. 자료 준비 완료 시 → Claude Code에서 v2.1 지시문 투입 → 하네스 1차 착수
