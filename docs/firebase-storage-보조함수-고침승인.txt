# ═══════════════════════════════════════════════════════════════════
#  창고(Storage) 규칙 — 보조 함수 «고침 승인»
#
#  ★ 무엇인가 — scripts/storage-rules-deploy.js 의 안전장치는 기준
#    (docs/firebase-storage-콘솔원문-<날짜>.txt)에 있던 보조 함수가 한 글자라도
#    달라지면 멈춘다. 그것이 맞다 — 보조 함수 한 줄이 창고 전체의 문이다.
#    다만 그 때문에 «조이는 고침»조차 할 수가 없어, 여기에 옛 몸과 새 몸을
#    그대로 적어 두면 그 «하나»만 지나간다.
#
#  ⚠ 이것은 --force 가 아니다. 옛 몸도 새 몸도 글자까지 맞아야 지나가므로,
#    승인한 그 고침 말고는 어떤 흔들림도 다시 멈춘다.
#
#  ▶ 올린 뒤에 할 일: 콘솔 원문을 새 날짜 파일로 남기고 «이 승인 줄을 지운다».
#    그때부터는 새 몸이 기준이라, 남겨 두면 다음 사람이 헷갈린다.
# ═══════════════════════════════════════════════════════════════════

[isStaff]
왜: 「로그인했고 이메일이 있으면 직원」이었다. 파이어베이스 「새 가입 막기」가 꺼져 있어, 아무 이메일로나 가입한 사람이 명함첩·급여데이터함·자문 증빙·서고 원본을 읽었다. 실시간DB 는 PR #1225 로 이미 갈랐는데 창고만 안 갈려 있었다(창고 규칙은 실시간DB 를 못 읽어 uid_roles 를 볼 수 없다). 계정 450개를 재어 「이메일 계정 12개 전부 @pureun.kr · 바깥 도메인 0개」를 확인했으므로, 도메인으로 잠가도 지금 쓰는 분 중 막히는 사람이 없다. (대표 결정 2026-09-13 「도메인 잠그고 올린다」)
옛: return request.auth != null && request.auth.token.email != null;
새: return request.auth != null && request.auth.token.email != null && request.auth.token.email.matches('^[^@]+@pureun[.]kr$');
