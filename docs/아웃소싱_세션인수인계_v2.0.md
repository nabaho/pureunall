# 급여 아웃소싱 자동화 — 세션 인수인계 v2.0 (개발 착수 이후 · 2026-07-20 기준)

> **사용법**: 새 Claude Code / Cowork 세션에 이 파일을 올리고 "이 인수인계대로 이어서 진행"이라고 요청.
> v1.6(설계 완료본) 이후 **실제 개발이 상당히 진행된 상태**를 담는다.

---

## 0. 프로젝트 한 줄
푸른노무법인(권형하 대표·공인노무사)의 **약 110개 외부 고객 사업장 급여·노동법 아웃소싱 자동화 시스템**.
사업장은 하던 대로 자료 보내고(엑셀·PDF·사진), 시스템이 읽고·계산하고, 담당자는 확인·확정만.

## 1. 아키텍처 확정 — "A안" (중요)
- **기존 푸른통합시스템에 통합**: `nabaho.github.io/pureunall/enter.html` 런처(GitHub Pages)에 **급여관리 타일 추가 완료**(라이브).
- **같은 Firebase 프로젝트 `pureun-erp`** 사용 → pu-erp 직원이 **같은 이메일/비번으로 로그인(SSO)**, **권한(uid_roles) 공유**.
- **데이터는 `payroll_os` 노드에만** 저장(pu-erp 자체 data는 안 건드림 — kcareer·ieum과 동일한 앱별 칸 분리 패턴).
- 계산 코어·기준값은 pu-erp에서 이식(공유), 데이터는 분리(설계 "마스터 단방향 동기화").

## 2. 완성된 토대
- **하네스(1차) 목표 0~7 전부 완료**: 파일분류·골든파서(3종 양식)·단수처리·산정기간·동명이인·근태매칭·급여일·커버리지 리포트.
  - 직원 **27,671명** 추출(엑셀분), 성명·기본급·과세·소득세·4대보험·공제·실수령. **주민번호 미저장**(성만).
- **엔진(2차) 검증 완료**: pu-erp calcPayroll 이식 + 실측 교정. 3개 사업장 교차검증 **지방세 100%·고용보험 99~100%·실수령 100%**.
  - 실측 교정: 지방세=소득세×10% **10원 절사** / 연금·건보·장기=**공단 고지액 모드**(재계산X) / 고용보험=과세×**연도별 요율**×사업장 단수처리.
- **설정 카드 자동생성**: 69개 사업장 카드(급여일·산정기간·단수처리·동명이인·확인필요 플래그).
- **통합 앱 라이브**: `payroll-os.html` — SSO 로그인·서버연결·App Check 통과 확인(p001@pureun.kr 관리자). 설정카드·급여처리(사업장→월→직원 드릴다운·3색신호·확정잠금) 작동.

## 3. ★ 파일 위치 지도 (가장 중요)
```
[코드 저장소] C:\Users\fair0\Documents\pureunall   (git, GitHub nabaho/pureunall, 라이브)
  ├─ enter.html            런처 (급여관리 타일 추가됨) — main 브랜치, 배포됨
  ├─ payroll-os.html       ★급여 아웃소싱 앱 (라이브) — main, 배포됨
  ├─ pu-erp.html           기존 ERP(참고·수정금지)
  └─ (main 브랜치 = 라이브 배포. push 시 즉시 nabaho.github.io 반영. 타 세션 동시작업 주의)

[하네스 작업공간·워크트리] C:\Users\fair0\Documents\pureunall-harness  (브랜치: harness-payroll)
  ├─ harness/   classify.py·profile.py·parser_v1.py·analyze.py·period.py·matching.py·report.py·README.md
  ├─ engine/    payroll_core.js·check_golden.js·validate_multi.js·build_site_cards.py·cards_html.py·
  │             cards_edit_html.py·audit_sites.py·ocr_triage.py·ocr_pipeline.py·build_pilot.py·rates.json 등
  ├─ reference/ puerp/(pu-erp 추출본 00~06)·payroll_mvp.html·급여대시보드_목업.html
  └─ docs/      인수인계 v1.6·지시문 v2.1/v2.2

[실제 자료 + 결과물] C:\Users\fair0\OneDrive\바탕 화면\급여아웃소싱 서류들  (git 아님, 주민번호 O)
  ├─ 주민정/ 박은비/ 김보람/   실제 급여자료(약 6,700개)
  ├─ 2.급여관리/               과거 아카이브(하네스 대상 제외)
  └─ _harness_out/            결과물(file_classification·parser_output·site_cards·pilot_payroll·coverage_report 등)
     ※ 개인정보 포함 → git 커밋 금지. .gitignore로 차단됨.
```
- **워크트리 이유**: pureunall이 main으로 전환돼(타 세션 명함첩/달력 작업) 하네스 코드가 워크트리로 격리됨. **매 단계 즉시 커밋**(미커밋 시 브랜치전환에 유실 이력 있음).
- 엔진/파이썬은 `PAYROLL_DATA_ROOT` 환경변수로 자료폴더 지정(기본=위 자료폴더).

## 4. Firebase 연결 정보
- 프로젝트 `pureun-erp` (asia-southeast1), SDK **10.13.2 compat**, **App Check reCaptcha** 키 `6Ldu1hEtAAAAABB9lH9EMEyd7uBLEy9H5HxJoua_`.
- 인증: **이메일/비밀번호**(pu-erp와 공유), 로그인 후 `uid_roles/{uid}`에 권한(isAdmin/fin).
- **규칙에 `payroll_os` 블록 추가·게시 완료**(fin 또는 isAdmin 접근). 그 외 pu-erp 규칙 불변.
- 데이터 구조: `payroll_os/site_cards`(설정카드 배열), `payroll_os/payroll`(급여), `payroll_os/payroll_locked`(확정 잠금맵).
- 앱이 파일 업로드(가져오기)로 site_cards.json / pilot_payroll.json을 payroll_os에 저장.

## 5. 실측 사실 (설계 뼈대)
- 사업장은 급여프로그램 아닌 **수기 엑셀** → 고용보험 단수처리(절사/올림/반올림) 혼재, 요율 0.9/0.899/면제 혼재.
- **연금·건보=공단 고지액**(연금 월고정·건보 4월변동) → 재계산 금지, 고지액 모드.
- 지방세=소득세×10% 절사(확실). 고용보험 요율 **연도별**(2021이하 0.8%, 2022+ 0.9%).
- 급여대장 매체: **xlsx 678(파싱 85%) / PDF 519 / 이미지 467 / hwp 106** → 절반 이상이 PDF·이미지(**OCR 필요**).
- 산정기간: 대부분 1일~말일이나 **비표준 6곳**(나루오엠 11~10일 등). 급여일: 5일·말일 각 7곳 등.

## 6. 확정 결정 목록
- **파일럿 3곳**: 다온원 · 나라앤드씨 · 새별.
- 일용직 분해 **포함** / 세후부담 **사업장별 설정** / 명세서 법정항목·연차촉진 **우선 모두 포함** / OCR 외부전송 **동의(유료 티어)**.
- 개발순서: **①핵심루프(수신함→엔진→검토→확정→명세서) 한바퀴 → ②OCR·어댑터 → ③파생신고 → ④부가모듈**.
- 유지보수: 담당자는 **코드 아닌 설정카드만** 화면에서 수정(사업장별 담당자 잠금+수정기록+승격루프). 충돌 구조적 차단.

## 7. 현재 진행 중 — 핵심루프
- 급여 처리 화면 **작동**: 사업장→월→직원별 표(10개 항목)·3색 신호·확정/되돌리기. red(이상)는 확정 차단.
- 파일럿 데이터 `pilot_payroll.json` 생성(초안"(안)"파일 제외 + 사람당 1줄 중복제거 완료).
- **나라앤드씨=완전**(모든 항목+실수령 검산 일치). **새별=공제항목 결측**(orange). **다온원=일용 양식이라 기본급/과세/공제총액 "-"**.

## 8. 다음 할 일 (우선순위)
1. **다온원 일용 양식 지급항목 파싱 보완** (기본급/과세/공제총액 "-" 채우기).
2. **명세서 출력** 화면 (확정 급여 → 화이트라벨 명세서 PDF) — 핵심루프 마지막 조각.
3. **수신함** 실구현 (메일 웹훅·자동태깅·D-7 독촉) — 법인 메일 도메인 필요.
4. **OCR** (사진·PDF 급여대장) — Gemini 키를 `engine/.secrets/gemini.key`에 넣고 `ocr_pipeline.py` call_vision/이미지마스킹 구현. 3중 방어(행합계·2모델·확신도) 골격 있음.
5. **파생 자동**: 4대보험 신고·일용 매월신고·퇴직정산·연차/휴직·원천세/지원금.
6. **부가**: 서식함·암묵지 관리·실시간기록·사업주 포털.
7. 앱 개선: 급여처리에 "데이터 다시 올리기" 버튼 상시노출(현재 목록화면엔 없음).

## 9. 알려진 이슈
- 다온원 일용: 지급항목 결측(위 8-1).
- 새별: 공제항목 결측(양식 보완 필요).
- 사업장 양식이 제각각(패밀리 6종) → 사업장별 파서 정밀화가 지속 작업.
- 인앱 브라우저 프리뷰는 file:// 대화형 HTML에서 멈춤 → 실제 확인은 크롬 직접/배포본으로.
- 콘솔(Windows cp949)에서 파이썬 print의 —·⚠️ 등 특수문자 시 UnicodeEncodeError(파일 출력은 정상).

## 10. 새 세션 첫 액션 (권장 순서)
1. 이 문서 업로드 → "인수인계 v2.0 확인, 이어서 진행" 요청.
2. 자료폴더(`급여아웃소싱 서류들`)와 코드(`pureunall`, `pureunall-harness`) 접근 확인.
3. 라이브 앱 확인: `nabaho.github.io/pureunall/enter.html` → 급여관리 타일 → 로그인 → 급여처리.
4. 다음 작업 선택: 위 8번 우선순위 중 (권장: 명세서 출력 또는 다온원 파싱 보완).
5. **원칙**: 코드=워크트리 harness-payroll에 커밋 / 라이브(enter.html·payroll-os.html)=main 수정은 신중히·즉시 배포됨 / 주민번호·개인정보 커밋 금지.

## 부록 — 사용자 선호
- 항상 **쉬운 설명** + 답변 끝에 **"최적 방법"** 요약. 전문용어 최소화, 비유 활용.
- 설계·구조 설명 시 **플로우차트/다이어그램 포함**.
- 단계 계획 먼저 제시 → 승인 후 진행. 가정 금지·갈리면 질문.
