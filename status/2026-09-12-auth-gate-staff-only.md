# 2026-09-12 · 보안 — 「로그인했다」와 「우리 직원이다」를 갈랐다

- PR: (이 PR)
- 대표 지시: 「보안관련 시스템 확인해달라」
  (유튜브 「바이브코딩 앱이 해킹될 수 밖에 없는 이유」를 보시고)
- 손댄 곳: `scripts/make-firebase-rules.js` · 검사 넷 · 안내문 하나
- **규칙은 콘솔에 올렸다** — 새 기준 `docs/firebase-rules-콘솔원문-2026-09-12.json`

## 찾은 구멍 둘 — 「함께」 막아야 뜻이 있다

**㉠ 「로그인했다」를 「우리 직원이다」로 쓰고 있었다**

파이어베이스 설정을 직접 읽어 확인했다.

```
이메일+비번 로그인   켜짐
새 가입 막기        ★꺼짐 — 아무나 계정을 만들 수 있음★
```

그런데 옛 `LOGIN` 은 「비번 계정인가」만 봤다.

```js
const LOGIN = "auth != null && (sign_in_provider === 'password' || passkey === true)";
//                              ↑ 「우리 직원」이라는 뜻으로 232곳에 걸려 있었지만
//                                실제 뜻은 「아무 이메일로나 가입한 사람」이었다
```

웹 열쇠(`AIzaSy…`)는 HTML 에 그대로 있다 — **그건 원래 공개되는 것이라 잘못이 아니다.**
문제는 그 열쇠로 **가입까지 되는데**, 가입만 하면 규칙이 열렸다는 점이다.

가입 한 번이면 읽히던 것: `data/companies`(고객사) · `data/contracts`(계약) ·
`data/cases`(사건) · `pucards`(명함첩) · `data/user_dir`(직원명부) · `uid_roles`,
그리고 **`data/$other`** — 앞으로 `/data` 에 만드는 칸이 «아무 설정 없이 열린 채로» 태어났다.

**㉡ 스스로 직원이 될 수 있었다 — ㉠만 막으면 이 길로 뚫린다**

`pu-erp.html:83299` 가 로그인할 때마다 `uid_roles/<내uid>` 를 «자기 손으로» 쓴다.
그 자체는 새 직원 등록 길이라 나쁘지 않은데, 지켜지는 칸이
`fin`·`isAdmin`·`isSubAdmin` 셋뿐이라 **`sid` 와 `status` 는 아무 값이나** 쓸 수 있었다.

```
① 아무 이메일로 가입
② uid_roles/<내uid> 에 { sid:'X-999', status:'active' } 를 스스로 씀
③ 「재직 직원」이 됨 → 메일함(고객사 임직원 신상)까지 열림
```

★ 그래서 「LOGIN 을 재직자로 바꾸기」만으로는 **아무것도 못 막는다.** 둘은 한 벌이다.

## ⚠ 아직 뚫리지 않았다 — 재서 확인했다

계정 450개를 전부 갈랐다(이메일·이름은 안 찍었다).

```
익명 438  ← 전자서명 링크로 들어온 근로자. 정상
이메일 12 ← 전부 @pureun.kr.  바깥 도메인 0개
uid_roles 등록 10명 (전원 재직)
```

## 고침

```js
const 비번로그인 = "auth != null && (sign_in_provider === 'password' || passkey === true)";
const 재직자 = "root.child('uid_roles').child(auth.uid).child('status').val() === 'active'";
const LOGIN = `${비번로그인} && ${재직자}`;
```

그리고 `uid_roles/$uid` 에 **명단맞춤**(`.validate`) 을 걸었다 —
`sid` 와 `status` 를 **`sid_roles`** 와 견준다. 그 자리는 «관리자만» 쓰므로 믿을 수 있다.

### 일부러 그렇게 한 것 셋

**① 쓰기 문은 «옛 조건»(비번로그인)을 그대로 쓴다.**
새 `LOGIN` 을 쓰면 아직 등록 안 된 새 직원이 자기 칸을 못 만들어 **영영 못 들어온다**(닭과 달걀).
대신 내용을 명단과 견주므로 뚫리지는 않는다.

**② uid 가 아니라 «이메일»로 묶었다.**
uid 는 그 사람이 처음 로그인해야 생기는데, 관리자는 그 전에 명단을 적어 둬야 한다.
이메일은 미리 안다 — 그래서 **관리자가 사번+이메일만 넣어 두면 첫 로그인에 저절로 등록된다.**

**③ 익명 로그인은 «끄지 않았다».**
전자서명(`sign.html`)·이음(`ieum-view.html`)이 쓴다 — 끄면 근로자가 서명을 못 한다.
대신 익명으로 열리는 자리를 검사로 **둘로 못 박았다**(`ieum_public` · `esign/…/meta`).

## 검사

`tests/firebase-auth-gate.test.js` **9건** — 조건문을 글자로 안 박고 «성질»을 본다.

- 「비번 로그인만으로 열리는 자리가 없다」 ← 봐줄 곳은 `uid_roles/$uid` 하나뿐
- 「명단맞춤이 믿을 수 있는 자리만 본다」 · 「이메일·status 를 견준다」
- 「익명으로 열리는 자리는 정해진 둘뿐」 · 「로그인 없이 읽히는 곳은 판번호 하나」
- 「LOGIN 이 걸린 자리가 여전히 많다」 ← 조건을 통째로 지워도 통과하는 것을 막는 반대쪽 잣대

★ **고장 11개 전부 물린다.**
처음엔 **10/11 이었다** — `sid_roles` 가 두 번 나오는데 «한 곳만» 딴 데를 가리켜도 통과했다.
「뿌리에서 보는 자리 전부」를 보게 고쳤다.

### 검사 모형 셋을 사실에 맞췄다

`firebase-access-matrix` · `firebase-rules-apply` · `rules-narrow-three` 는 규칙식을
**실제로 계산하는** 좋은 검사인데, 「직원」 모형에 `status` 가 없어 새 잣대에 걸렸다.

⚠ **규칙을 늦추지 않고 «모형»을 고쳤다.** 모형이 옛 시절을 흉내내면 검사가 헛돈다.

★ 그 모형으로 새 잠금이 정말 먹는지 **두 건을 더했다** —
「스스로 가입한 바깥 사람은 고객사·계약·사건을 못 읽는다」 ·
「퇴사자는 계정이 살아 있어도 못 읽는다」.

전체 **15,631 통과 · 0 실패**.

## 자료 쪽에서 함께 한 것

| | |
|---|---|
| `sid_roles` 10칸에 `loginEmail` | 실제 계정에서 옮겨 적음. 겹친 사번 0, 빈 곳 0. **다른 칸은 안 건드림**(되읽어 확인) |
| 김석우(A-006) 명부 | 휴직 → 퇴사 (대표 지시). 잎사귀 하나만 고침, 명부 32명 그대로 |
| 박성수(P-002)·김석우 계정 | **사용중지**. 재직 10명 중 잘못 꺼진 사람 0 |

두 분은 출퇴근·사건·계약·컨설팅 **네 곳을 사번과 이름으로 훑어 흔적 0건**,
로그인 기록도 0이었다 — 그것을 대표께 보이고 확인받은 뒤 처리했다.

## 남은 것 — 다음 방에

- ⚠ **구글 캘린더 API 열쇠**가 `pu-erp.html` 에 박혀 있다(`GCAL_API_KEY`).
  파이어베이스 열쇠와 달리 **제한을 안 걸면 남이 써서 할당량을 태울 수 있다.**
  구글 클라우드 콘솔에서 「HTTP 리퍼러 제한」을 걸면 된다. 급하지 않다.
- ⚠ 파이어베이스 **「새 가입 막기」는 여전히 꺼져 있다.** 켜면 익명 로그인까지
  함께 막힐 가능성이 커서(전자서명이 쓴다) **일부러 안 건드렸다.**
  이제 규칙 쪽이 막혔으므로 가입해도 아무것도 못 읽는다 — 그 스위치는 덤이다.

## 2026-09-13 — main 을 병합해 다시 열었다 (규정관리 방)

이 가지가 **이틀째 붉은 채 멎어 있었다**. 그 사이에 실시간DB 규칙 배포가
통째로 막혀 서면함(`erp_docs`)·뉴스레터 전문 옮기기가 함께 기다리고 있었다.

- **붉었던 까닭은 이 가지의 잘못이 아니었다.** `test-cut-truncation` 이 잡은
  `kcareer-reocr-run.test.js:173` 의 「고정 폭 2600자 창」 하나였고,
  **main 이 이미 `cutFn` 으로 고쳐 두었다**. 101 개 뒤에 있어서 옛 모습으로 돌았을 뿐이다.
  코드는 한 줄도 안 건드리고 `git merge origin/main` 만 얹었다(충돌 0).

- ★★ **병합만으로는 안 됐다 — 규칙 파일을 다시 내놓아야 했다.**
  글자로는 깨끗이 붙었는데 **뜻이 어긋났다**: 서면함 방이 새 뿌리 셋
  (`erp_docs`·`erp_doc_idx`·`erp_doc_text`)을 **옛 `LOGIN`** 으로 써 두었고,
  글자 병합은 그것을 그대로 살렸다. 만들개로 다시 내놓으니 그 셋의 12줄에
  `status === 'active'` 가 붙었다 — **새 뿌리 셋이 「가입만 한 사람」에게 열린 채
  콘솔로 나갈 뻔한 것**을 여기서 막았다. 서면함에는 근로자 서면 원본과 본문이 든다.
  ⚠ 앞으로도 이 가지를 main 과 합칠 때는 **반드시 만들개로 다시 내놓을 것.**
  `node scripts/make-firebase-rules.js > docs/firebase-rules-전체-적용본.json`
  (그리고 `docs/rules-paste.json` 에 같은 내용을 복사).

- **살아 있는 콘솔에 대고 견줬다** (`node scripts/rules-deploy.js`, 읽기만):

  | | main 단독 | main + 이 가지 |
  |---|---|---|
  | 값이 바뀌는 규칙 | **166** (콘솔의 조임이 통째로 풀림) | **0** |
  | 사라지는 규칙 | 1 (`/uid_roles/$uid/.validate`) | **0** |
  | 새로 생기는 규칙 | — | **3** (서면함 뿌리 셋) |

  → 이 가지가 들어오면 규칙 배포가 **순수한 «더하기»** 가 된다.

- 전체 검사 **16,333건 / fail 0** (윈도우 전용 기존 결함 `rc-open` 하나만 예외 —
  main 에서도 같은 모양으로 나고 리눅스 CI 는 통과한다).
