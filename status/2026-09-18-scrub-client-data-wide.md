# 2026-09-18 · 저장소 전체에서 «진짜 의뢰인 자료»를 걷어냈다 · 문지기를 세웠다

- PR: (아래에 채움)
- 앞 PR: #1419 (한 의뢰인 한 곳만 정리)
- 대표 결정: 「문지기 검사부터 · 목업 정리 · 검사 정리 · 저장소 비공개」
  → 목업은 「폴더를 통째로 지우기」

## ★★★ 처음 센 것보다 훨씬 컸다

#1419 는 대표께서 짚어 주신 **한 곳**을 치웠다. 그 뒤 저장소 전체를 훑으니 —

| 무엇 | 얼마나 |
|---|---|
| 사업자등록번호(가짜가 아닌 듯한 것) | 61가지 · 107파일 |
| 의뢰인 회사 이름 | 28곳 |
| 대표자·담당자 실명 | 20여 명 |
| **직원 이름과 짝지어진 주민등록번호** | 1가지(여러 파일) |
| **사건 당사자 실명 + 사건번호** | 목업 여러 장 |

## ⚠⚠ 제일 무거웠던 둘

**① 주민등록번호** — `tests/kcareer-hwptext*.test.js` 에 「성명 ○○○ / 주민등록번호 열세 자리」가
한 문단으로 들어 있었다. **가리는 기능을 검사하려고** 넣은 값인데, 가려야 할 것을 검사 파일에
박아 둔 셈이다. 다른 주민번호는 모두 `1234567` 같은 뻔한 가짜인데 그것만 달랐다.
⚠ 고칠 때 **앞 여섯 자리도 함께** 바꿔야 했다 — 뒷자리만 바꾸면
`indexOf('앞여섯자리') < 0` 을 보던 검사 둘이 **아무것도 안 보는 검사**가 된다(실제로 그랬다).
바꾼 뒤 가리기 함수를 일부러 망가뜨려 **4개가 걸리는 것**까지 봤다.

**② 사건 당사자 명단** — `docs/mockups/worker-box-built.html` 은 근로자 정보함 화면을
통째로 떠 온 것이었다. **이름 + 사업장 + 무슨 사건인지 + 사건번호**가 한 줄에 있었다.
부당해고·산업재해·임금체불은 당사자에게 민감한 일이다.

## 목업 폴더는 통째로 지웠다 (317장)

- 목업 **319장 중 83장**에 번호·전화·메일·주민번호 꼴이 있었고, 이름은 더 넓었다
  (뉴스레터 **받는 분 실명 명단**, 명함 연락처, 업체 대표자 …).
- 한 장씩 고치면 **반드시 몇 장을 빠뜨린다.** 빠뜨린 한 줄이 사건 당사자 이름이면 그것으로 끝이다.
- **검사는 목업을 한 장도 읽지 않는다**(53개가 주석에 경로만 적고 있었다). 지워도 안 깨진다.
- 남긴 것은 `art-news-banner.html` · `art-news-side.html` 둘 —
  화면을 뜬 목업이 아니라 **뉴스레터 그림의 원본 코드**다. 지우면 `img/` 를 다시 만들 길이 없다.
- 왜 지웠는지는 `docs/mockups/README.md` 에 남겼다.

## ★★ 값의 «성질»을 지켜서 바꿨다

바꾼 값이 성질을 잃으면 **검사가 조용히 뜻을 잃는다.**

- **사업자번호 51개** — 검산을 통과하던 것은 통과하게, 못 하던 것은 못 하게 새로 지었다.
  가운데 두 자리(개체 갈래)도 그대로 두고 앞을 `123-` 으로 모았다.
- **「한 글자 오독」 짝** — 모음 ㅗ/ㅜ 한 획 차이라는 성질이 기능의 전부라 같은 결로 바꿨다.
- **이름 길이** — `해찬솔F3`(한글 3자)을 2자로 바꿨더니 메일 제안 잣대(`MLK_MIN_MATCH`)에
  못 미쳐 검사가 깨졌다. 3자로 되돌렸다. ⚠ 「가나다라마바사」가 들어간 검사가 있어
  그 글자를 피해 지었다.

## ⚠ 고치다 밟은 것 — 다음 사람에게

1. **띄어쓴 꼴** — `「김 도경」`·`'최 상 윤'`·`'가야 엔지니어링'` 처럼 **사이가 벌어진 이름**이
   그대로 남아 검사 셋이 깨졌다. 붙여 쓴 것만 찾으면 안 된다.
2. **정규식 안의 꼴** — `/texas@daum\.net/` 은 점이 `\.` 이라 글자 찾기로 안 잡힌다.
3. **대문자 꼴** — `PALCHUN@NAVER.COM`.
4. **부분 문자열로 찾는 검사** — `ids('가야')`·`b.set('41281')`·`assert.match(shown, /312/)` 처럼
   **번호·이름의 일부**를 치는 검사가 있다. 값을 바꾸면 그 «일부»도 함께 바꿔야 한다.
   ⚠ 새 번호를 전부 `123-` 으로 모았더니 `12381` 로는 두 줄이 걸렸다 — 전체 자릿수로 바꿨다.
5. ★★★ **`git ls-files` 는 한글 이름을 따옴표로 감싼다.** 그대로 읽으면 한글 이름 파일이
   **아무 소리 없이 빠진다** — 첫 훑개가 그래서 두 파일을 놓쳤다.
   `git -c core.quotepath=false ls-files` 를 쓸 것. 이것은 새 문지기 검사가 기계로 지킨다.

(2026-09-08 정리 기록 `status/2026-09-08-scrub-pii-2.md` 도 같은 다섯 꼴을 적어 두었다.
 그때 「사업자번호 무리 60개는 다음에」라고 미뤄 둔 것이 이번 일이다.)

## 문지기 — `tests/no-real-client-data.test.js`

지운 값을 목록으로 적으면 **그 목록이 곧 유출**이다. 그래서 «가짜가 갖춰야 할 모양»만 못 박는다.

| 무엇 | 규칙 |
|---|---|
| 사업자등록번호 | `123-` 으로 시작(우리 법인 번호와 뻔한 가짜만 예외) |
| 주민등록번호 | 뒷자리가 `1234567`·`2000000` 같은 뻔한 가짜 |
| 휴대전화 | 가운데 네 자리가 `1111`·`1200`·`9999` 같은 뻔한 꼴 |
| `docs/mockups` | 안내문과 그림 원본 말고는 두지 않는다 |
| 훑기 자체 | 한 파일도 조용히 안 빠진다(못 읽으면 실패) |

실패 글에 **고치는 법을 함께 적었다**(검사자리 셈하는 법까지). 안 적으면 다음 사람은
검사를 고치는 대신 **지우는 쪽**을 고른다.

## 검사

- 전체 **17,847개 중 실패 0** (종료코드 0)
- **이빨 확인** — 네 규칙에 각각 진짜 같은 값을 심어 **넷 다 걸리는 것**을 봤다.
  가리기 함수를 망가뜨려 주민번호 검사 4개가 걸리는 것도 봤다.

## ⚠⚠ 남은 일

1. **과거 커밋에는 그대로 남는다.** 지운 것은 지금 판뿐이다.
   역사 되쓰기는 열린 PR 과 다른 방 작업트리를 전부 깨뜨린다 — 대표 판단이 필요하다.
2. **저장소 비공개는 «멈춰 두었다».** `https://nabaho.github.io/pureunall/` 가 `main` 에서
   그대로 서비스되는데, 깃허브는 **무료 계정에서 비공개 저장소의 Pages 를 안 띄운다.**
   돌리는 순간 사이트가 내려갈 수 있다. 계정 등급부터 확인해야 한다.
3. **이메일 449가지는 안 봤다** — 우리 법인 주소와 이미 가려 둔 `cust NN` 이 대부분이라
   문지기에 안 넣었다. 헛걸림이 크면 아무도 안 본다.
4. ★★ **주석 속 업체 이름은 «긴 꼬리»로 남았다.** 이번에는 번호를 실마리로 찾았기 때문에
   **번호 없이 이름만 적힌 주석**은 못 걸렀다. `㈜`·`(주)`·`주식회사` 표를 단 것만 세어도
   **117가지**이고(㈜정일제지 · (주)케이이알 · 유원에프앤비 · 토탈방재 · 화담원 …),
   표 없이 적힌 것은 더 많다.
   ⚠ **이것은 모양으로 못 막는다** — 회사 이름에는 정해진 꼴이 없어 문지기가 손을 못 댄다.
   사람이 한 번 훑어야 하는 일이고, 주석은 「왜 그렇게 고쳤나」를 적은 자리라
   **이름만 갈아 끼우면 뜻은 그대로 남는다.** 다음 판에서 이어서 할 일이다.
