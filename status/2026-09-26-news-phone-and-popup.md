# 폰에서 글자가 작던 것 · 팝업에서 오른쪽이 잘리던 것

대표 지시 2026-09-26
「폰에서 글자가 작다. 그리고 팝업에는 오른쪽까지 확장해서 전체가 다 보이게 해라.」

편지를 700 → 980 으로 넓히면서 **두 가지가 따라오지 못했다.**

## ① 폰 — 980 고정이라 통째로 «줄여» 보여 줬다

폰 메일 앱은 폭이 넘치는 편지를 화면에 맞게 줄인다. 980 짜리가 375 폰에서는
**38% 로 줄어** 글자가 그만큼 작아진다. 넓혀서 얻은 것을 폰에서 잃고 있었다.

**고친 길 — 화면 크기별 규칙(@media).** 폰·웹메일에서만 칸을 쌓아 폭에 맞춘다.
★ **아웃룩은 @media 를 모른다** — 그래서 데스크톱 모습은 한 글자도 안 바뀐다.
★ **글자 크기는 안 건드렸다** — 줄이지만 않으면 14px 그대로가 폰에서 읽기 좋다.

실측(375px 폰):

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| 편지 폭 | 980 (38% 로 줄여 보임) | **375 (안 줄인다)** |
| 본문 글자 | 14px × 0.38 ≈ 5px | **14px 그대로** |
| 자료 카드 | 세 칸이 짓눌림 | **한 장씩 쌓임 (361px)** |
| 가로 스크롤 | — | **없음** |

### ⚠⚠ 발송기에 «좁은 문»을 냈다 — 넓히지 말 것

발송기는 `<style>` 을 **속 글자까지 통째로 버린다**(`functions/mail-send.js`).
그래서 지금까지 화면 크기별 규칙을 쓸 수가 없었다. 이번에 **셋을 다 지킬 때만**
통과하는 문을 냈다:

1. **`@media` 블록 «안»**에 있는 규칙만 — 밖에 있으면 데스크톱까지 덮는다
2. 고르개가 **우리 반 이름(`.pu-…`)**일 때만
3. 값은 inline 과 **같은 잣대**(`STYLE_OK`) — `url()`·`expression()` 은 그대로 막는다

또 조건도 좁혔다: **화면 폭을 보는 것만**(`max-width`) 받는다. `@media print`·
`@media screen`·`min-width` 는 안 받는다.

⚠ **이 문을 넓히지 말 것.** 「받은 것 그대로 전달」로 **남의 편지 HTML 이 이 길로 들어온다.**
넓히는 순간 남의 CSS 가 우리 편지를 덮는다(글을 숨기거나 남의 서버 그림을 부른다).
검사가 남의 `<style>`·남의 반 이름·인쇄 규칙·`url()`을 모두 막는지 본다.

`class` 속성도 함께 열었다 — 규칙이 붙잡을 손잡이다. 값은 **`pu-` 로 시작하는 것만** 남긴다.

### ⚠ 하마터면 CSS 가 «글자로» 나갈 뻔했다

되살린 꾸밈을 **태그 거르개 «앞»**에 붙였더니, 거르개가 `<style>` 태그만 지우고
**속 CSS 를 글자로 남겼다** — 편지 맨 위에 `@media only screen…` 이 그대로 보인다.
지금은 거르개를 **지난 뒤**에 붙인다. 검사가 그 자리를 지킨다.

## ② 팝업 — 미리보기 틀이 700 에 굳어 있었다

편지는 980 인데 틀은 700 이라 **오른쪽이 잘렸다**(대표 화면: 팝업 아래 가로 스크롤).

⚠ **2026-09-12 에도 똑같은 일이 있었다**(680 → 780). 숫자를 두 곳에 적으면 되풀이된다.
그래서 이번엔 **숫자를 안 적는다** — 편지 짓는 층에서 읽는다
(`const 편지폭 = window.PuNewsTpl.전문넓이`). 팝업 창은 780 → **1060**.

## 검사

`tests/newsletter-phone-and-popup.test.js` 10개.
★ 둘은 **발송기를 실제로 지난 뒤**를 본다(규칙이 지워지면 폰은 예전 그대로다).
★ 셋은 **남의 CSS 가 여전히 하나도 안 통과하는지**를 본다 — 문을 넓힌 것이 아님을 지킨다.
★ 하나는 **CSS 가 글자로 새지 않는지**를 본다(위의 하마터면).

옛 검사 하나를 고쳤다 — `<td align="center" width=…>` 를 글자 그대로 찾고 있어
반 이름(class)이 끼자 깨졌다. 기능은 멀쩡한데 검사만 깨지는 자리라 덜 까다롭게 고쳤다.

뉴스레터 1,047 · 메일 1,517 통과 / 0 실패. 되돌림은 아래 「남은 일」 참고.

## ⚠⚠ 되돌림 검사가 «본 파일에 남긴 것»을 그대로 커밋했다 — 되풀이하지 말 것

이번에 크게 한 번 밟았다. 순서대로 적어 둔다.

1. 되돌림 검사를 **작업트리에서 바로** 돌렸다(`mutate-phone.js`).
2. 기계가 붐벼 그 작업이 **⑨번 되돌림을 적용한 채 멈췄다** — `finally` 가 못 돌아
   `functions/mail-send.js` 에 `const 미디어 = /()([\s\S]*)/g;` 가 **남았다**.
3. `git status` 는 ` M functions/mail-send.js` 만 보여 줬다 — 내가 고치던 파일이라
   **오염이 내 고침 속에 숨었다.**
4. 그대로 커밋·푸시했고, 그 정규식이 **빈 글자에 맞아 영영 도는** 바람에
   CI 가 10분 제한에 걸려 취소됐다(다른 PR 은 1~2분).
5. 이어서 돌린 둘째 되돌림 스크립트는 **이미 오염된 파일을 «원본»으로 찍어** 두었다가
   끝나고 그대로 되돌려 놓았다 — 오염이 한 번 더 굳었다.

**그래서 바꾼 것 셋:**
- 되돌림은 이제 **사본에서만** 돈다(`scratchpad/mutate-safe.js` — `js/`·`functions/`·
  `tests/`·`pu-news.html` 을 임시 자리에 복사해 거기서 고친다). 본은 한 글자도 안 건드린다.
- 멎는 것도 **「울었다」로 친다** — 검사가 끝까지 못 가는 것이 곧 잡힌 것이다.
  (그 전에는 멎음을 「통과」로 세어, 오염된 파일에서 ⑧이 «울었다»고 잘못 읽었다.)
- `안전한스타일` 의 되풀이에 **안전 고리**를 달았다(50번·앞으로 안 나가면 끊음).
  지금 꼴로는 닿지 않지만, 이번 같은 일이 또 나면 그 자리에서 끝난다.

⚠ **`git status` 가 깨끗해 보여도 안심하지 말 것** — 내가 고치던 파일이면 오염이 숨는다.
되돌림을 돌린 뒤에는 **`git diff` 를 눈으로 읽는다.**

## 되돌림 검사에서 드러난 «헐거운 검사» 둘

- 남의 CSS 막는 것을 **한 덩이로** 넣었더니, 맨 앞의 `url( )` 하나가 `<style>` 을 통째로
  버려 **뒤의 셋은 재 보지도 못했다** — 고르개·값 가리기를 꺼도 통과했다. 넷으로 갈랐다.
- 반 이름을 `<p>` 로 봤다. `<p>` 는 애초에 `class` 를 안 받아(ATTR_OK) 늘 통과한다.
  `<div>` 로 바꿨다.
- 되돌림 ⑪은 찾는 글자가 `cleanStyle` 과 **겹쳐** 엉뚱한 곳을 집고 있었다. 앞줄을 붙여 하나만 집는다.

## 남은 일

- [ ] 대표: 「✉ 시험 발송」으로 **폰에서** 받아 보시기 — 이번엔 글자가 안 작아야 한다
- [ ] 아웃룩으로 받으시는 자문사는 예전 그대로 보인다(@media 를 모른다) — 그것이 맞다
