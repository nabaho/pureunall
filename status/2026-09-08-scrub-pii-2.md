# 2026-09-08 · ㉮ 검사에 남은 «사람을 가리키는» 값

- 대표 결정 2026-09-08 「가」 (㉮) — `status/2026-09-08-scrub-test-pii.md` 의 다음 걸음

## 바꾼 것 — 45개 파일 · 233군데

| 갈래 | 개수 | 어떻게 |
|---|---|---|
| 휴대폰 | 19개 | `010-1200-0001` … `010-1200-0019` |
| 개인 메일 계정 | 22개 | `cust01@…` … `cust22@…` — **집 이름(도메인)은 그대로** |
| 그 사람들과 한 줄에 있던 것 | 이름 2 · 상호 3 · 도메인 3 · 법인등록번호 1 · 유선 1 | 가짜 값 |

**도메인을 왜 남기나** — 검사가 「다음·네이버·한메일」을 갈래로 견준다(주소 문지기·
메일 주인 가리기·다음메일 통로). 도메인을 바꾸면 그 검사의 «뜻»이 사라진다.
지우려는 것은 **누구인지**이고, 그것은 계정 이름에 있다.

## ★ 배운 것 — 한 값이 여러 «꼴»로 적혀 있다

한 꼴만 바꾸면 검사가 깨진다. 실제로 다섯 군데가 그랬다:

| 꼴 | 어디서 | 무슨 일이 났나 |
|---|---|---|
| 숫자만 `01012000003` | `cards-bykey` 의 `bykey` 열쇠 | 열쇠만 옛 번호로 남는다 |
| **빈칸** `010 1200 0003` | `cards-lockkey` | 「같은 번호인데 지문이 다르다」로 깨졌다 |
| **앞머리만** `010-1200` | `pucards-pull` 의 찾기말 | 찾기 결과가 0건이 되었다 |
| **주소창꼴** `cust11%40naver.com` | `cards-mail-popup` (정규식 안, 점이 `\.`) | 열리는 주소와 기대가 갈렸다 |
| **끝 글자가 잘린 것** `@hanmail.ne` | `cards-mail-addr-guard` · `pu-cards.html` 주석 | 문지기가 고쳐 내놓는 값과 기대가 갈렸다 |

그리고 **값이 뜻을 지고 있던 자리** 하나 — `mail-daum-box-screen` 의 얼굴 글자.
계정 이름 첫 글자라서 `b…` → `c…` 로 바뀌며 기대값도 `B` → `C` 로 고쳐야 했다.

⚠ **다음에 또 지울 때는 이 다섯 꼴을 함께 찾을 것.** 줄표만 찾으면 안 된다.

## 손대지 않은 것 — 왜

**① 우리 법인 «자기» 주소** — `370-6@daum.net`·`370-6@hanmail.net`·`pureun@daum.net`·
`kwon.hyung@daum.net`·`cheonan@daum.net`·`seosan@daum.net`·`@pureun.kr`·
`fairrunlabor.com`·`mailer-daemon@daum.net`.
우리 것이라 「남의 개인정보」가 아니고, **앱에서 메일 주인·보낸이를 가리는 데 쓴다** —
바꾸면 메일이 엉뚱한 사람 함으로 갈라진다. 지우고 싶으시면 «기능부터» 봐야 한다.

**② 앱 파일에 값으로 든 것** — `cust45@naver.com`(pu-cards) · `cust27@naver.com`(work) ·
`cust39@gmail.com`(pu-cards) · `010-1200-0026`(work).
검사와 앱이 «같은 값»을 보고 있어, 한쪽만 바꾸면 조용히 어긋난다.
하나씩 「기능인가 붙임자료인가」를 봐야 한다.

**③ 사업자번호·법인등록번호 무리** — 60개 남짓, 앱 파일에도 있다.
그 가운데 `134-86-05772` 하나가 **22개 파일**에 있고 `pu-cards.html` 에도 있다
(상호는 이미 「가나테크」로 바뀌었는데 **번호만 실제로 남아 있다**).
사업자번호는 공개 자료라 개인정보와 무게가 다르지만, **어느 회사인지 바로 조회된다.**

**④ 유선전화 무리** — 40개 남짓. 대표번호는 공개된 것이 많다.

**⑤ 대표 지시를 옮긴 주석의 업체 이름** — 그 기록의 뜻이라 남긴다(1차와 같은 잣대).

## 다음 걸음

- ㉯ **깃 기록에서 지우기** — 여전히 남아 있다. 다른 방 작업이 다 합쳐진 뒤 한 번에.
- 자동 검사는 ② 를 정리한 뒤에 둔다(지금 두면 늘 빨갛다).
