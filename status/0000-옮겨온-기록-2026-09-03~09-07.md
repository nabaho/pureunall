# 옮겨 온 기록 — 2026-09-03 ~ 2026-09-07

> STATUS.md 의 「6. 마지막 손댄 기록」 표를 **그대로** 옮긴 것이다(2026-09-07).
> 여러 방이 이 표 한 자리에 덧붙이다 부딪혀, 앞으로는 **방마다 딴 파일**에 적는다.
> ⚠ 이 파일은 «역사»다 — 새 기록을 여기 붙이지 말 것. `status/날짜-가지이름.md` 로 새로 만든다.

| 날짜 | 무엇 |
|---|---|
| 2026-09-03 | 이 파일을 만듦. 열린 PR 정리(#879 합침 · #326 닫음 — 기능이 이미 main 에 있었다) |
| 2026-09-03 | ①번(사업자번호)이 **틀린 진단**임을 확인하고 닫음. 대신 급여데이터함의 이름 짐작 결함을 고침(그대로 같은 것 먼저) |
| 2026-09-03 | 「사업장에 보낸 서류」 설계안을 올림(대표 지시) — 만들기 전 검토 단계 |
| 2026-09-03 | 그 1걸음을 만듦 — 기업 상세 「📤 보낸 서류」 칸(새 쓰기 0). 받은 것에 📥 |
| 2026-09-03 | 상호 칸 정리 — 서식을 **제 열**로(90px), 짧은 이름표(등록증·고유번호·업태·종목). 표 폭 한도 1010→1100 은 **대표께 물어보고** 정함 |
| 2026-09-03 | ⚠ #903 이 `<td>` 에 `display:flex` 를 주어 **열이 하나씩 밀렸다** — 안쪽 상자로 고치고 검사로 못 박음(#910) |
| 2026-09-03 | 2걸음 — 1:1 보내기가 `sentDocs/{회사열쇠}` 에도 한 줄 남긴다(첫 새 쓰기) |
| 2026-09-03 | 기업 상세 조용하게 — 겹치는 요약 줄·같은 이름 서류 접기·설명은 한 줄+말풍선 |
| 2026-09-03 | 3걸음 — 전 사업장 «배포 기록»(묶음·모아 쓰기·뺀 곳 세기·되돌리기) |
| 2026-09-03 | 4걸음 — 「이 갈래를 못 받은 곳만」 거르개. 묶음만 읽어 센다(회사 기록을 통째로 안 읽는다) |
| 2026-09-03 | 덧 — 단체 메일도 «걸린 뒤에» 묶음 기록을 남긴다(3걸음의 길 그대로) |
| 2026-09-03 | 덧 — 같은 서류 정리 → **휴지통**(지우지 않는다 · 사진첩 원본은 그대로 · 되돌리기) |
| 2026-09-05 | 환경설정 여섯 탭 결 맞춤(#956) · **검토안**: docs/mockups/2026-09-05-settings-review.html — 대표 결정 **「나」·「다」 둘 다 채택** |
| 2026-09-05 | 환경설정 **🔔 지금 손볼 것** 띠 + 정리 탭 배지(대표 결정 「나」= 목업 ㉯). 휴지통은 총계 칩에서 띠로 옮김 |
| 2026-09-05 | 환경설정 **탭을 없애고 한 화면**(대표 결정 「다」= 목업 ㉰). 30개가 스크롤 한 번에 다 보인다 — 아래 「4-1」 |
| 2026-09-05 | 👷 근로자 **사건별 명단 탭**(대표 지시). 남아 있던 명함 띠를 걷어내고, 끝난 사건은 🚪 로 접는다 — 아래 「4-2」 |
| 2026-09-05 | 반출 딱지 기준을 **1,000건**으로(대표 결정). 사유 묻는 기준(200)과 «수를 갈랐다» — 아래 「4-3」 |
| 2026-09-06 | 업체관리 **「폐업」 탭·칸 삭제**(대표 지시 「폐업은 필요없다. 삭제해라」). 373곳 중 켜진 곳 0곳 — 한 번도 안 쓰였다. 폐업은 「종료 처리」 사유 「업체 폐업」으로 간다. 되살리기 막는 검사: `tests/erp-closed-biz.test.js` |
| 2026-09-06 | 경력관리 칸 지도 — **목록은 «표»가 아니라 «구역»**(#1030). 인적사항+학력이 한 표인 서식에서 채울 자리가 **0개**였다(성명·생년월일·주소가 통째로 안 잡힘). 구역 밖은 낱개 자리로, 구역 끝은 «이어지는 빈 줄»까지. `tests/kcareer-list-swallow.test.js` |
| 2026-09-06 | 경력관리 **📋 붙여넣어 여러 건 넣기**(대표 승인 「이대로」). 여섯 화면(자격증·학력·위촉장·표창·수료증·기업경력) 더보기에. 미리보기·중복 갈라내기(dupKey 재사용)·칸 고치기·되돌리기. 대표 물음 「MCP 로 직접 넣기」는 **안 함** — 자료가 브라우저 안에 있어 MCP 가 못 만지고, 클라우드 사본을 고치는 길은 삭제 기억(자리표)을 건드려 위험하다. `tests/kcareer-paste-in.test.js` |
| 2026-09-06 | 메일 — 바깥 그림을 **늘 보여준다**(대표 결정). 「막았습니다 · 그림 보기」 띠를 없앰. 실측: 받은메일함 438통이 **보낸이 185명**이라 사람마다 허락하는 길로는 안 끝난다(아는 곳만 풀어도 73%가 그대로 물었다). ⚠ 안 뜨는 그림은 조용한 빈 자리로 — 2026-09-05 의 「찢어진 그림표」가 되살아나지 않게 |
| 2026-09-06 | 업체관리 표 **한 줄 규칙** — 대표자·계약시작·월자문료가 두 줄로 흐르던 것. 폭을 정할 때 «안쪽 여백 8px»을 빼먹고 있었다(칸 폭 − 24 가 글자 자리다). `.co-1line` 이 줄바꿈 자체를 막는다 |
| 2026-09-06 | 증명서 **생년월일·담당업무 자동 채움**(대표 지시). 주민번호에서 생년월일을 뽑아 32명 중 25명이 저절로 채워진다(남는 7명은 화면 입력칸). 담당업무 자리에 «직위»를 옮겨 적던 것(「담당업무: 직원」)을 직위별 문구로 고침 — `tests/cert-birth-and-duty.test.js` |
| 2026-09-06 | 급여대장 가져오기가 **직위를 지어내던 것**(대표 지시 「고쳐라」). 새 퇴사자에게 무조건 `title:'직원'` 을 박아 노무사가 경력증명서에 직원으로 찍혔다(이 길로 들어온 15명 중 10명은 사람이 뒤늦게 고쳐 둔 상태였다). 이제 명부의 **직위·직급·직책 칸을 읽고**, 없으면 **빈칸으로 둔다**(메모에 「직위 미확인」·알림에 인원수). 빈 직위는 고르는 칸에서 「직위 미입력」으로 보인다(`staffPickLabel` 한 곳). ⚠ **이미 있는 기록은 안 건드렸다** — 남은 「직원」 5명은 사람이 확인한 값일 수 있다. `tests/import-no-fake-title.test.js` |
| 2026-09-06 | **회의·비용관리도 함께**(대표 상시 지시 「항상 똑같이」). 비용 두 화면에 붙여넣기 + 금액「45,000원」→45000 + 구분을 고르개 항목으로. 겸사겸사 **같음 판정에 비용 라벨(content)이 빠져 있던 것**을 고쳤다 — 같은 날 같은 지출처의 다른 영수증이 한 건으로 묻혀 OCR 로 둘째가 기록조차 안 만들어졌다(실측). `tests/kcareer-paste-in.test.js` |
| 2026-09-06 | 서류 만들기 **📂 작성 중 보관함**(대표 승인 「목업대로」). 담기는 자리가 **한 자리뿐**이어서 새 양식이 앞서 하던 이력서를 덮어썼다 — 자리마다 따로 담고, **친 값·짝 지정까지** 함께 담는다(전엔 문서만 담아 이어서 열면 친 것이 사라졌다). 버리기는 30일 휴지통 · 완성본은 「지난 작성」으로 남김 · 자리 10개 한도. ⚠ 색인을 **FB_SKIP** 에 넣었다 — 전에는 색인만 클라우드로 오가고 파일은 안 가서, 폰에서 열면 딱지를 지워 **PC 의 작업까지 못 꺼내게** 됐다. `tests/kcareer-draft-box.test.js` |
| 2026-09-07 | 사진첩 **공유자 목록에 「사진첩을 안 연 직원」이 아예 없었다**(건의 김동현 노무사 「김혜민 노무사님이 없습니다」). 고를 사람을 `puphotos/owners` 에서 가져왔는데 그 자리는 «사진을 올린 적 있는 사람»만이다 — 사진을 **받을** 사람이 왜 사진첩을 먼저 열었어야 하나, 뒤집힌 문이었다. 화면 주석은 「골라 줄 uid 자체가 없다」고 적어 뒀는데 그 전제가 틀렸다(`uid_roles` 에 있다). `listStaff()` 로 재직 사번+`user_dir` 이름을 합쳐 낸다 — 퇴직자는 뺀다. `tests/photos-share-all-staff.test.js` |
| 2026-09-07 | 사진첩 **폴더에 담긴 사진을 통째로 열어 주기**(같은 건의의 둘째 대목). 폴더 ⋯ 메뉴 → 늘 쓰던 그 고르개가 열린다. ⚠ 이름을 「폴더 공유」로 **안 달았다** — 가는 것은 사진이고 **나중에 담는 사진은 안 따라간다.** 그 한 줄을 고르개에 함께 적는다(조용히 안 가는 것이 가장 아프다). 지우기와 열어 주기가 `folderItems` 한 셈을 쓴다 |
| 2026-09-07 | 메일 — 본문이 통째로 깨져 보이던 것(대표 제보). **보낸 쪽이 글자표를 틀리게 적었다** — `text/html` 조각에 `charset=euc-kr` 이라 써 놓고 바이트는 UTF-8. 목록 미리보기는 `text/plain`(맞는 글자표)을 골라서 «멀쩡했고», 열 때만 깨졌다. 이제 `toText` 가 **바이트를 먼저 믿는다**(올바른 UTF-8 이면 머리글 무시). 반대 잘못도 함께 고침 |
| 2026-09-07 | 메일 **📬 열람 확인** — 보낸메일함 줄 끝에 «상대가 언제 열었나»가 붙는다(대표 지시 「보낸 시간과 확인한 시간을 같이 보면 편하다」). 다음메일의 수신확인 값은 **웹 화면 전용이라 우리 쪽으로 안 온다** — 그래서 본문 끝에 보이지 않는 1×1 그림을 넣어 우리가 잰다(대표께서 두 길 중 「상대 모르게」를 고르심). ⚠★ 적는 것은 **시각과 횟수뿐** — IP·기기·있는 곳은 일부러 안 적고 여는 함수도 그 값을 안 읽는다. ⚠ 「안 열었다」는 안 열었다는 뜻이 아니다(지메일이 대신 받아 두면 열지 않아도 찍히고, 그림을 막아 두면 열어도 안 찍힌다) — 그 말을 화면에 적었다. `tests/mail-open-track.test.js` |
| 2026-09-07 | 메일 열람 확인 **끄고 켜는 스위치**(설정 ②보내기 · 대표 전용). 스위치를 **서버가 읽는다**(`pucards/config/matMail/track`) — 그림을 붙이는 것이 서버라, 화면에만 두면 «끈 것처럼 보이는데 안 꺼진» 상태가 된다. 안 적혀 있으면 켠 것으로 본다. ⚠ 겸사겸사 **「제목·본문 되돌리기」가 config/matMail 을 통째로 `set`** 하던 것을 `update` 로 고쳤다 — 그 한 번에 전 직원 **보안문구·서명·명함(sec·sign·perUser)** 이 함께 지워졌다(묻는 말은 「제목·본문」이라고만 했다) |
| 2026-09-06 | 규정관리 **화면 정리 네 단계**(#1004, 대표 지시 「정신 사납다」). 배너 세 겹 → 상태 한 줄 · 참고 도구 넷 → `⋯ 참고` 서랍 · 흐름을 넷으로(넣기·비교·고치기·내보내기) · 편집 툴바 열세 단추를 보기/고치기/문서로 묶음. ⚠ **단추를 하나도 없애지 않았다** — 검사가 그것을 지킨다. 설계서 `docs/superpowers/specs/2026-09-05-규정관리-화면-정리-design.md`. 새 검사 52건 |
| 2026-09-06 | 규정관리 ERP 업체가 「2건」으로 읽히던 것(#1005) — `pu-erp` 가 값을 `{v,u}` 로 «싸서» 저장하는데 그 포장을 안 벗겼다. 연결이 끊긴 게 아니라 **처음부터 한 번도 읽힌 적이 없었다**. `tests/rules-erp-companies.test.js` (11) |
| 2026-09-06 | 규정관리 「📄 한글 모양」(#1006) — 신구대조표를 진짜 한글 파일로 본다. 이미 있는 미리보기를 그대로 쓰고 단추만 붙였다 |
| 2026-09-06 | 문서 엔진 rhwp **0.7.19 → 0.8.4**(#1007) — 0.7.19 가 가운뎃점(·)을 떨어뜨렸다(A/B 실측 0개 대 12개). ⚠ 기본판만 올려선 안 됐다 — 브라우저에 저장된 옛 판이 기본값을 이긴다(`pickVer`). 여덟 화면의 `?v=` 를 함께 올림 |
| 2026-09-06 | 규정관리 **왼쪽으로 몰기 + 위로 40px**(#1017) — 오른쪽 끝으로 밀던 «보이지 않는 여백» 셋(상태 줄·사업장 줄·결과 바닥 줄)을 걷었다. ⚠ 빈 여백으로 가르면 2px 모자라 맨 끝 단추가 접힌다(flex 는 줄을 «먼저» 나누고 그다음 줄인다) — 1px 금으로 갈랐다 |
| 2026-09-06 | 규정관리 **두 판이 창을 채운다**(#1027) — `.preview` 560px·`.findings` 470px 로 높이가 못박혀, 낮은 창에서는 〔보관함에 저장〕〔검토 완료〕가 화면 밖으로 밀리고(832px 창에서 127px 넘침) 높은 창에서는 아래가 텅 빘다. ⚠ **JS 로 재서 맞추지 말 것** — `resize`·`ResizeObserver` 가 «안 오는» 자리가 있어 한 번 어긋나면 그대로 남는다. CSS 로 흐름만 정했다(`body`→`.wrap`→`.grid` 세로 flex). ⚠ `min-height` 가 아니라 `height` — min 만 주면 높이가 «내용대로» 정해져 페이지가 5014px 로 늘어났다 |
| 2026-09-06 | 규정관리 **가로를 끝까지**(#1056) — #1027 로 `body` 를 flex 로 만든 순간 `.wrap` 의 `margin:0 auto` 가 «가운데 정렬»로 뜻이 바뀌어, 1900px 창에서 폭이 1413px 로 줄고 좌우가 244px 씩 비었다. ⚠ **내용이 넓으면 멀쩡해 보인다**(표본 문서 최대폭 5111px) — 사업장을 안 고른 «빈 화면»에서 확인해야 잡힌다. `width:100%` 한 줄 |
| 2026-09-07 | **윈도우에서 8건이 빨갛던 것을 0으로**(CI 는 초록이었다). CRLF 정규식 4건 · `dead-code.js` 경로 구분자 1건 · `bash` 없음 3건. 위 「CI 는 초록인데 내 컴퓨터는 빨갛다」 참고. `tests/windows-checkout.test.js` (4) |
| 2026-09-07 | 계약 입력의 **업체 온톨로지 확인을 자동화**. 직접 입력·기업정보함·OCR의 정확한 10자리 사업자번호가 업체 명부의 한 곳과만 일치하면 업체 ID를 바로 연결하고 긴 검증 상자는 `✓ 업체 자동 확인` 한 줄로 접는다. 이름만 같거나 사업자번호가 중복되면 종전처럼 사람이 고른다. 입력한 회사명은 자동으로 덮지 않는다. `tests/ontology-company-write.test.js` (13) |
| 2026-09-07 | 규정관리 **발밑 안내 세 줄을 걷었다**(대표 지시 「캡쳐2 내용 없애라 … 박스 더 아래로 내려라」). 1900px 에서 59px 를 늘 차지하던 것 + 아래 여백 22→10px → 두 판이 **85px 더 내려왔다**(판 바닥 855→940, 페이지 넘침 0). ⚠ **지우지 않았다** — 전문은 `⋯ 참고 › ⓘ 이 화면 안내` 로(서랍은 이미 다섯 도구를 담아 세로 비용 0), 그리고 「최종 판단은 공인노무사」 한 마디는 **검토 결과 판 머리**(`규칙집 v3 · 89항목` 옆)에 붙였다 — 발밑은 아무도 안 읽지만 결과를 «읽는» 자리는 본다. `tests/rules-note-fold.test.js` (7) |
| 2026-09-07 | 서고 **제출 정보 규칙이 콘솔에 올라갔다** (대표가 손수). 되받아 기계로 견줘 확인 — 적용본 433개 대 09-07 아침 콘솔 427개, **차이는 정확히 6개**(전부 `…/docs/$role/sub`)였고 콘솔에만 있던 것·값이 다른 것 **0개**. 이제 콘솔 = 적용본(433/433) 이므로 서고 제출 정보 입력이 열렸다. ⚠ 새 snapshot 파일은 **안 만들었다** — 사람이 옮겨 적는 자리를 또 만들지 않는다 |
| 2026-09-07 | 사진첩 **단순 사진을 구글 판독에 안 보낸다**(대표 지시 「단순 사진은 구글 판독에 안 들어가는게 좋은데」). 「사진인지 서류인지는 AI 가 가린다」로 만들어 두었기에 **가리려면 먼저 한 번 읽어야** 했다 — 그래서 현장 사진도 예외 없이 한 번은 갔고 그 한 번이 하루 몫을 먹었다. 그런데 **물어볼 필요가 없었다**: 카메라의 「📷 일반사진」이 저장될 때 버려지고(올리는 길 여섯이 전부 `doc` 으로 못 박아 넘겼다), 읽는 쪽은 `kind` 칸을 아예 안 봤다(`photo` 로 적힌 59장도 다 읽혔고 그중 57장은 정말 사진이었다). 실측 844장 — 9월 판독 166번 중 **124번(75%)이 사진**, 9/7 한 뭉치 164장에 **서류가 0장**인 채 하루 몫을 다 써서 뒤에 올린 서류가 못 읽혔다. ㉰고른 갈래를 싣는다(`metaKind` — 화질은 서류 기준 2560px 그대로, 뒤에 「서류였다」고 눌러야 하므로) ㉮열 장 넘는 뭉치는 **한 번 묻고 답할 때까지 안 보낸다**(30장씩 쪼개진 여섯 뭉치도 물음은 하나 · 스캔 문서·글자 파일은 안 묻는다 · 답은 저장 안 한다) ㉯「그냥 사진」은 `read.noRead` 로 **사람이 그랬다고** 적는다(꾸민 AI 판정으로 안 남긴다 · 딱지는 「판독 안 함」) ㉱한도에 걸리면 그 자리에서 자동을 멈추고, 서버는 「하루 몫 429」를 다시 안 부른다(한 장에 모델 둘 × 세 번을 두드리던 것). 문지기는 `readSkipWhy` **한 곳**이고 사람이 누르는 길은 지나지 않는다. 서버 규칙은 안 건드렸다(items 아래는 이미 주인·총괄관리자 쓰기). `tests/photos-read-gate.test.js`(19개 · 되돌림 23/23) · 옛 검사 여섯이 「지금 값」(거르개 차례·`kind` 글자)을 박아 두어 규칙으로 다시 겨눴다
| 2026-09-07 | 서고 **4단계 — 사례집 검색 + 문안 은행 적립**(대표 지시 「4」). 새 화면 없이 `🔎 조문 검색`이 서고까지 본다. ★★ 색인 낱말과 검색어의 **길이 관계가 양쪽**이라 앞머리 훑기 1회 + 앞토막 정확히 찾기(최대 9회) **둘 다** 해야 한다 — 한 방향만 하면 「되는 것 같은데 안 나온다」. 본문은 후보 40회차만 읽고, **판정은 본문이** 한다(보관함과 똑같은 셈). 한계(2번 이상·상위 60)를 화면에 늘 적는다. 은행 적립은 **개정본만**, 실패해도 올리기는 계속. 검사 31개 |
| 2026-09-07 | 서고 **⑤ 원본 파일**(대표 지시 「배포되면 파일스토리지 진행」). 기존 창고 `pureun-erp-hrphotos` 에 `casebook/{사업장}/{회차}/{역할}.{확장자}` 로 담고, 사업장 이력 칩에 `📎 원본`. ★★ 막혀 있던 것은 **파일 손잡이** — `cbRead` 가 이름·해시·글만 담고 `File` 을 버려 올릴 때 바이트가 없었다(`File` 은 디스크 참조라 수백 개도 가볍다). ⚠ 쓰는 차례는 **무거운 것부터**(원본→글→회차→색인) — 거꾸로면 「색인은 있고 파일은 없는」 거짓말이 남는다. ⚠ 원본 실패가 회차 실패가 아니다(규칙 게시 전에도 글·색인은 올라간다). ★ 대표님 손: `docs/firebase-storage-전체(붙여넣기용).txt` 를 hrphotos 창고 규칙에 게시. 검사 20개 |
| 2026-09-07 | 사진첩 → 기업 상세 **구멍 둘** 막음 — 못 보낸 까닭을 화면에 내고, 서식도 «상호로» 받는다. 상호로 가는 길은 사람이 눌러야 열린다(`autoSendCoInfo` 가 번호를 직접 본다) |
| 2026-09-07 | 업체관리 **이관 막이 + 이관 되돌리기**(대표 지시 「관리에서 계약관리로 이관시킬수 있어야」). 계약-2026-121 「주식회사 새롬산업·자문」이 「새롬사내근로복지기금·기금」 안으로 들어갔다 — **계약서는 맞았고 고른 업체가 틀렸다**. 그 기금은 이미 있던 업체라 지우지 않고 «빈 칸 14개»를 채웠다(사업자번호·대표자·전화·담당자 박지윤·자문료 30만원). 이름과 유형만 기금 그대로라 **한 칸에 두 회사가 섞였다**. ★★ 왜 아무 검사도 안 걸렸나 — `validateCompanyLink` 는 이름이 달라도 **사업자번호가 같으면** 통과시킨다(`!(biz&&targetBiz&&biz===targetBiz)` 예외). 두 기록이 번호를 나눠 갖고 있어 그 예외에 딱 걸렸고, **유형(자문/기금)은 지금껏 아무 데서도 안 봤다**. ⓒ `erpCoTransferGap` — 이관 전에 계약서와 고른 업체의 **이름·유형**을 견주어 다르면 멈춘다(기본 단추는 「그만두기」 · 괄호 표기 차이로는 안 멈춘다 — 매번 뜨는 경고는 눈에서 지워진다). ⓑ `undoCompanyTransfer` — 이관할 때 `xferUndo`(mode·before·after)를 남기고, 「새로 만든 업체」는 휴지통으로·「얹은 업체」는 **채운 칸만 도로 비운다**. ⚠ `returnToContract` 를 베끼지 말 것 — 그것은 ①업체를 **지우고** ②**새 계약을 만든다**(계약-2026-080·081 이 실제로 번호가 두 벌이다). 여기서는 원래 계약의 status 만 `signed` 로 되돌린다. ⚠ 이관 뒤 사람이 고친 칸은 안 건드린다. 덧 — 이관된 것이 **그 유형 탭**에서 보이게(대표 지시): 맨 위로 올리는 일은 있었으나 «기금 깔때기가 걸린 화면에 자문이 오면» 목록에 아예 안 떴다. `tests/co-transfer-undo.test.js`(18 · 되돌림 21/21) · `company-arrival-tax` 가 두 줄의 «붙어 있음»을 박아 둬 차례 검사로 고침 |
| 2026-09-07 | **공용 백업이 주민번호를 안 잠그고 있었다.** 대표 지시(2026-08-29 「백업 시 주번 암호화해야 된다」)가 `pu-erp.html` 자기 백업에만 붙어 있었고, 공용 부품 `js/pu-backup.js` 는 **기금**(임원 주민등록번호 — 2026-09-07 새로 들어옴)·**경력관리**·취업규칙·명함·급여OS·업무를 서른 시점씩 뜨면서 맨몸으로 담았다. 열린 문은 아니다(`systemBackups` 는 관리자·위임관리인만 읽는다) — 그러나 잠금을 만든 까닭이 「백업은 오래 남고, 옮겨 다니고, 아무도 안 본다」였다. ⚠ 못 본 까닭: `tests/rrn-seal.test.js` 가 **`pu-erp.html` 하나만** 보고 있었다. ⚠★ 크기를 «재기 전에» 잠근다(잠근 값이 열세 자리→백 자리라 재고 나서 잠그면 16MB 한도를 넘겨 통째로 거부된다). ⚠ 잠글 것이 «없으면» 열쇠를 안 가져온다 — 주민번호 없는 앱의 백업이 열쇠 칸 권한 때문에 멎지 않게. ⚠ 되돌릴 때 «반드시» 푼다(안 풀면 `enc:v1:…` 이 번호 자리에 들어가고 그 화면을 저장하면 진짜 번호가 사라진다). 옛 백업은 잠긴 자리가 없어 그대로 지나간다. 검사 25건 · 되돌림 9/9 |
