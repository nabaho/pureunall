# 2026-09-13 · 경력관리 — 학력이 «자격·상벌 표»에 박히던 것

- PR: #1253 (커밋 `6d555447`)
- 무엇: 「내 정보로 채우기」가 학력을 자격·상벌 표에 박던 것(대표 제보).
  뿌리는 «세로로 합친 이름 칸»(학력사항)이다 — 머리줄 6칸, 자료 줄 5칸이라
  열 이름을 칸 순서로 세면 한 칸씩 밀리고, 급을 못 찾아 학력 줄을 통째로 건너뛴 뒤
  아래 빈 줄에 박혔다. 열 번호(`hp:cellAddr`)가 «있는» 서식은 이미 고쳐져 있었고,
  **없는 서식**(편집기를 거쳐 다시 내보낸 문서 등)에 빗장이 없었다.
  세로 라벨 수만큼 밀어 맞추고, 칸 수가 달라지면 구역을 끊는다.
- 검사: `tests/kcareer-colshift-noaddr.test.js`

⚠ 이 기록은 원래 `STATUS.md` 6절 «맨 끝»에 표 한 줄로 붙어 있었다.
그 자리는 2026-09-07 에 없앤 자리다(여러 방이 같은 끝줄에서 부딪혔다) —
`tests/status-log-noconflict.test.js` 가 표 줄을 0줄로 못 박고 있어 **main 이 빨강이 되었고,
저장소의 모든 배포가 막혀 있었다.** 글자 그대로 옮겨 왔고 내용은 하나도 바꾸지 않았다.
