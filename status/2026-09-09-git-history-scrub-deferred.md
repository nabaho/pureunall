# 2026-09-09 · ㉯ 「깃 기록에서 지우기」 — 지금은 미룬다

- 대표 결정: 「나」(㉯ 진행) → 지금 상태를 재어 보고 **「지금은 미루고 상태만 기록」** 으로 정정
- 이 파일의 자리: `status/2026-09-08-scrub-test-pii.md`·`status/2026-09-08-scrub-pii-2.md`
  가 파일에서는 이미 지웠지만(㉮), **깃 «기록»에는 여전히 남아 있다**고 적어 둔 그 ㉯다.

## 왜 지금 안 하나 — 재어 본 숫자

| | |
|---|---|
| 원격에 병합 안 된 브랜치 | **44개** |
| 열려 있는 워크트리 | **31개** |
| 열려 있는 PR | 0개 |

㉯(예: `git filter-repo` 로 과거 커밋의 실제 개인정보를 지우고 강제 푸시)를 하면:

- **44개 브랜치가 전부 옛 기록과 갈라진다** — 재작성된 `main`과 조상이 안 맞아
  **다시는 깨끗이 합칠 수 없다.** 그 방들은 브랜치를 새로 따서 자기 변경만 옮겨 담아야 한다.
- **31개 워크트리가 전부 못 쓰게 된다** — 이 기기의 것이든 다른 기기의 것이든,
  다시 만들어야 한다.
- 열린 PR은 0개라 그 자체는 안전하지만, **44개 브랜치 뒤에 있는 "곧 PR이 될" 작업**이
  전부 영향을 받는다.

## 지난 판단이 맞았다

㉮ 2차(`status/2026-09-08-scrub-pii-2.md`)에서 이미 이렇게 적어 두었다:

> ㉯ 깃 «기록»에서 지우기 — 되돌릴 수 없고 열려 있는 남의 브랜치 30여 개를 깨뜨린다.
> 다른 방 작업이 모두 합쳐진 뒤 한 번에 해야 한다.

오늘 다시 재어 보니 그 숫자(30여 개)가 그대로다(44개로 오히려 늘었다) — **재개 조건이
아직 하나도 안 갖춰졌다.**

## 재개 조건

**원격의 미병합 브랜치가 뚜렷이 줄어든 때**(이상적으로는 한 자리 수) — 그때 다시
「나」를 물어보면 된다. 그 전에는 이 작업을 시작하지 않는다.

## 참고 — ㉯ 를 실제로 할 때 필요한 것 (지금은 준비만, 착수 안 함)

- 지울 값 목록은 이미 있다 — `status/2026-09-08-scrub-test-pii.md` ·
  `status/2026-09-08-scrub-pii-2.md` 의 이름표(가나테크·가나솔루션·cust01~62·
  010-1200-0001~0028 등, 원래 값은 그 문서에는 이미 안 남아 있으니 **깃 로그**에서
  다시 찾아야 한다 — `git log -S<원래값>` 로 커밋을 짚는다).
- `git filter-repo --replace-text` 로 문자열을 통째로 바꾸는 방식이 `filter-branch` 보다
  빠르고 안전하다(공식 권장).
- 강제 푸시 뒤 **모든 열린 브랜치를 새 `main` 위로 다시 만들어야** 한다 — 리베이스가
  아니라 **새로 따는 것**에 가깝다(조상이 아예 다른 나무가 된다).
- GitHub 쪽 캐시(포크·PR 코멘트에 인용된 diff 등)는 강제 푸시로도 안 지워질 수 있다 —
  완전한 삭제를 원하면 GitHub 지원팀에 별도 요청이 필요할 수 있다.
