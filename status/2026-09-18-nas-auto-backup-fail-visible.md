# 2026-09-18 · 나스 자동 백업이 «조용히» 실패하던 것 — 화면은 초록으로 「활성화됨」만 말했다

가지 `claude/login-start-screen-386qu2`

## 무엇이 있었나

대표님이 나스를 막 연결하시고 「다음 뭐해야되나 나스」라고 물으셨다. 다음 할 일을 짚다가
자동 백업 코드에서 이것을 봤다 —

```js
.catch(function(){ /* 자동 백업 실패는 조용히 무시 … — ⚠ 남은 일: 실패를 설정 화면 기록에라도 남길 것 */ })
```

7일 자동 백업은 **아무도 안 볼 때** 돈다. 그 실패를 통째로 삼키고 있었다.
그 사이 설정 화면은 초록 띠로 「주 1회 자동 백업 활성화됨」이라고만 말한다.
**한 장도 안 올라갔어도 사람은 백업이 있는 줄 안다.**
백업은 «없는 것»보다 **«없는 줄 모르는 것»**이 나쁘다 — 정작 필요할 때 없다.

하나 더: 실패하면 성공 시각을 안 남기므로 **앱을 열 때마다** 저장소 4MB 를 풀어 글자로 만들고
나스에 붙어 보고 또 실패했다. 사무실 밖에서는 그것이 «늘»이다(화면이 그때마다 몇 백 ms 멈춘다).

## 고친 것

| | 전 | 후 |
|---|---|---|
| 실패 | `catch(){}` 가 삼킴 | `nasAutoNote(false, 까닭)` 로 남김 |
| 되풀이 | 앱 열 때마다 4MB 풀고 재시도 | 실패 뒤 **하루** 쉼 (`NAS_AUTO_RETRY_MS`) |
| 설정 화면 | 언제나 초록 「활성화됨」 | 실패면 **빨강** + 까닭 + 「마지막 성공은 언제」 |
| 까닭 | — | 사람 말로 (`Failed to fetch` → 「사무실 밖이거나, 인증서를 안 믿거나, CORS 가 빠졌습니다」) |
| 「지금 초기화」 | 성공 열쇠만 지움 | 물러서기 기록도 함께 품 (안 그러면 눌러도 하루를 쉰다) |

새 도우미는 `nasInfoLine` 뒤 한자리에 모았다 —
`NAS_AUTO_KEY` · `NAS_AUTO_TRY_KEY` · `NAS_AUTO_RETRY_MS` · `nasAutoWhy` · `nasAutoNote` ·
`nasAutoLastTry` · `nasAutoClear`.

## 곁들여 고친 것 — 검사가 «빈 글자»를 보고 있었다

`tests/erp-boot-nameplate.test.js` 의 두 검사가 `var AUTO_KEY = 'pureun_v6_nas_auto_backup'`
글자로 자리를 잡고 **3,400자를 잘라** 보고 있었다. 그 한 줄을 공용 상수로 바꾸자
`indexOf` 가 `-1` 이 되어 빈 글자를 검사했고, 오류 메시지는 「''가 정규식에 안 맞는다」뿐이었다.
`CLAUDE.md` 와 `tests/test-cut-truncation.test.js` 가 경고하는 **고정 폭 자르기**가 이것이다.
자리는 주석으로 잡고 끝은 `}, []);` 까지 가게, 못 찾으면 **그 자리에서** 말하게 고쳤다.

## 손댄 곳

- `pu-erp.html` — 도우미 7, 자동 백업 갈래(물러서기·기록), 설정 화면 상태 띠
- `tests/nas-auto-backup-fail-visible.test.js` 새로 7개
- `tests/erp-boot-nameplate.test.js` — 무른 자리잡기 고침

전체 검사 17,689개 통과. 돌연변이 셋(다시 삼키기 · 물러서기 빼기 · 초기화가 열쇠 하나만) 모두 걸린다.

## 남은 일 — 나스 다음 순서

1. **(대표님)** 이알피 → 설정 → 🖥️ NAS 자동 백업 → **🔌 연결 테스트** →
   기록에 뜨는 `🖥 모델 · RAM · DSM · Container Manager(도커)` 한 줄.
   → AI 색인 설계(`docs/2026-09-10-NAS-AI-색인-설계.md`) **0단계**가 이것이다. 이것 없이는 1단계를 못 정한다.
2. **(대표님)** 같은 화면에서 **💾 지금 백업** 한 번 — 나스에 `pureun_erp_backup_2026-09-18.json` 이
   실제로 생기는지. 연결은 됐지만 «파일이 올라가는지»는 아직 한 번도 확인 못 했다.
3. 🖥 줄을 받으면 1단계(계약서 100장 OCR)를 **어디서** 돌릴지 정한다 — 도커 있으면 NAS, 없으면 사무실 PC.
