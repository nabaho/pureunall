# 푸른통합 — 공용 상태 (STATUS)

> **모든 방(세션)이 지키는 규칙**
> 1. **시작할 때**: 이 파일을 읽고, `gh pr list --state open` 으로 지금 열린 것을 본다.
> 2. **끝낼 때**: 손댄 앱의 줄과 「남은 일」을 고쳐서 **같은 PR 에 함께** 넣는다.
>    따로 커밋하면 다음 사람이 옛 줄을 읽는다.
> 3. **AI 개인 메모리에 기대지 말 것** — 계정마다·방마다 다르다. **이 파일이 유일한 공용 뇌.**
> 4. ⚠ **가지 목록으로 「남은 일」을 세지 말 것.** 쥐어짜 합치기(squash) 때문에 이미
>    합쳐진 가지도 `--no-merged` 에 그대로 뜬다 — 2026-09-03 에 34개로 보였으나
>    실제로 남은 것은 **둘**이었다. 남은 일은 **열린 PR** 과 **PR 이 한 번도 없던 가지**로 본다.

*(이 파일을 만든 까닭: 2026-09-03 대표 지시 「지금까지 코딩하던 것과 다른 채팅방에서 하던 것
모두 검토하고 앞으로 필요한 부분 정리해달라」. 사흘간 139건이 합쳐지고 작업방 20개가 동시에
도는데, 「무엇이 남았나」를 담은 곳이 기금 하나뿐이었다.)*

---


## 되풀이하지 말 것 — 「CI 는 초록인데 내 컴퓨터는 빨갛다」 (2026-09-07)

현재 main 을 **윈도우에서** 돌리면 8건이 빨갰다. CI(리눅스)는 초록이다.
**여덟 건 다 「윈도우에서만 새는」 자리였고, 환경이 틀린 것이 아니라 코드가 틀린 것이었다.**

| 몇 건 | 어디 | 까닭 |
|---|---|---|
| **4** | `rules-casebook-since`·`review` | 이 저장소는 윈도우에서 **CRLF** 로 내려온다. `];\n` 처럼 «글자 뒤에 곧바로 줄바꿈» 을 찾는 정규식은 `];\r\n` 을 못 맞춘다 → 「규칙집을 못 찾았습니다」로 죽었다(규칙집은 멀쩡했다) |
| **1** | `tools/dead-code.js:133` | `path.relative` 결과를 그대로 열쇠로 써서 `reference\payroll_mvp.html` 이 됐다. `!f.startsWith('reference/')` 를 **그대로 통과**하고, `^[^/]+\.html$` 도 역슬래시를 막지 않아 «최상위 html» 로 세어졌다 → 참고본 함수 셋이 「죽은 코드」로 올라왔다 |
| **3** | `dev-tools-not-published` | PowerShell 창에는 `bash` 가 없어 `spawnSync bash ENOENT` 로 죽었다 |

**고친 방법**
- 줄끝은 **한 곳에서** 맞춘다 — 읽는 자리를 함수 하나로 모으고 그 정규식을 `\r?\n` 으로 둔다(`rules-casebook-review` 의 `규칙집()`). 흩뿌리지 않는 것이 핵심이고, 방법은 «읽을 때 `.replace(/\r\n/g,'\n')`» 든 «`\r?\n`» 이든 좋다.
- ⚠ **못 찾으면 «까닭»을 적는다**(`assert.ok(m, '…')`). 안 적으면 `TypeError: Cannot read properties of null (reading '1')` 로 죽어, 다음 사람이 검사가 아니라 **규칙집을 의심한다** — 2026-09-07 에 그 물음이 실제로 왔다.
- 경로는 **열쇠를 만드는 한 곳**에서 고른다(`.split(path.sep).join('/')`).
- `bash` 없음은 V3 가 `rm` 을 막는 경우와 **같은 문**으로 건너뛴다. ⚠ `process.env.CI` 면 **절대 안 건너뛴다** — 리눅스에서 bash 가 없으면 그건 진짜 고장이다.

★ **왜 이걸 고쳤나** — 로컬에서 늘 빨간 검사를 보면 사람이 **빨강을 무시하게 된다.**
  팀은 윈도우에서 일한다. 「CI 만 초록」인 검사는 절반만 일하는 검사다.
★ **`\n}` 은 괜찮다** — 줄바꿈이 «앞»에 오면 `\r\n}` 안에 `\n}` 이 있어 그대로 맞는다.
  깨지는 것은 앞에 글자가 붙은 경우뿐이다(`;\n` · `);\n`).

⚠ **아직 남은 넓은 쪽 — 각 방이 자기 앱에서 고를 것.**
  `;\n` 을 정규식에 쓰면서 줄끝을 안 고르는 검사가 **31개 더 있다**(2026-09-07 실측):
  `billing-who` · `cards-co-*` · `contract-co-group` · `erp-*` · `gov-*` · `ledger-clinic-alias` ·
  `paydata-*` · `photo*` · `work-*`.
  지금은 그 자리가 판정을 좌우하지 않아 «우연히» 지나가고 있다 — 그 파일의 정규식이
  조금만 바뀌면 곧 빨개진다. **한 PR 이 남의 앱 검사를 뭉텅이로 고치면 부딪힘만 커지므로**
  여기서 안 건드렸다. 자기 앱을 만질 때 함께 고쳐 달라.
  규정관리 범위는 `tests/windows-checkout.test.js` (4) 가 지킨다.

---

## 되풀이하지 말 것 — 「열려 있는데 안 보인다」 (2026-09-07)

대표 제보 「참고가 안 열린다」. 서랍은 **열리고 있었다** — `display:flex` 였고 자리도
잡혀 있었는데 화면에 **한 점도 안 그려졌다**(411px 실측: 좌281~우431, 화면 411).

까닭이 둘 겹쳤다:

| | |
|---|---|
| ㉠ | 폰 구간에서 `header{overflow:hidden}` · `.hdr-nav{overflow-x:auto}` — `position:absolute` 인 서랍을 **통째로 잘라 냈다** |
| ㉡ | `header` 에 `backdrop-filter` 가 있어 **`position:fixed` 로 바꿔도 소용없었다** — backdrop-filter 는 fixed 자손의 «담는 상자»가 되어 그 안에 가둔다 |

**그래서 서랍을 머리줄 «밖»(body 직계)으로 꺼냈다.** 자리는 JS 가 단추 밑에 잡아 준다.

★★ **서고만의 일이 아니었다.** 표준규칙·문안 은행·보관함·조문 검색까지
**다섯 개 전부** 폰에서 못 쓰는 상태였다 — 서랍을 접은 날부터 줄곧.
대표는 폰을 주로 쓰시는데 아무도 몰랐다.

★ **배운 것**: 「눌러도 안 열린다」는 대개 «안 열리는» 것이 아니라 «안 보이는» 것이다.
`display` 만 재면 열린 걸로 나온다. **`elementFromPoint` 로 그 자리에 진짜 그 요소가
있는지**까지 봐야 한다. 이번에도 그것으로 잡았다.
`tests/ref-drawer-visible.test.js` (6) 가 지킨다.

## 대표 확인이 필요한 것 (2026-09-04)

- [x] **퇴직정산 평균임금 0원 — 화면이 스스로 짚는다** (2026-09-05)
  급여를 `p.sid` 로 찾던 흠(고침 `f85c123`)으로 평균임금이 0 이 된 건이 있을 수 있다.
  **고친 것만으로는 부족하다** — 이미 저장된 건은 열어도 다시 계산하지 않는다.
  그래서 퇴직정산 창이 그런 건을 **먼저 말한다**: 「평균임금이 0원으로 산정된 건입니다 ·
  이 사람 급여 기록은 N개월치 있습니다」 + `[🔄 평균임금 다시 계산]`.
  ⚠ **자동으로 안 고친다** — 이미 지급한 건일 수 있어 사람이 누른다. 화면이
    「이미 지급한 건이면 차액을 어떻게 할지 먼저 정하십시오」라고 적는다.
  ⚠ 급여 기록이 아예 없어 0 인 건은 **안 짚는다**(그때는 0 이 맞는 값이다).
  ⚠ 사람이 손으로 적어 둔 값이 **이긴다** — 다시 셀 때도 안 건드린다.
  `tests/retire-zero-wage.test.js` (8) · 고장넣기 셋 모두 걸림.
  **★ 대표 확인**: 그래도 «이미 지급한 건이 있었는지»는 화면에서 직접 보셔야 한다.
- [x] **파이어베이스 규칙 세 자리** (2026-09-05 · 올라갔다)
  `/ontology` · `/data/co_no_seq` · `/kcareer_inbox`. `/ilabor` 를 올리는 김에 함께 나갔다
  (규칙은 «통째로» 갈아 끼우는 것이라 기다리던 것이 같이 올라간다).
  ⚠ **이 줄이 「아직 안 올라갔다」로 오래 남아 있었다.** 올라간 뒤에 지우지 않으면
    다음 사람이 안 해도 될 일을 한다 — 어긋난 안내는 없는 것보다 나쁘다.
- [x] **`/rules_mgmt/casebook` — 올라갔고 대조까지 끝났다** (2026-09-07)
  대표가 **폰으로 손수** 붙여넣어 게시하셨다. 되받아 기계로 견줘 셋 다 깨끗했다 —
  글자 그대로 · 뜻(최상위 58칸) · 규칙 문장 낱개(**427개**, 어긋난 것 0).
  `docs/firebase-rules-콘솔원문-2026-09-07.json` 이 그 판이다.
  ⚠★ **이 길은 임시다 — 되풀이하지 말 것.** 이 방의 통신 정책이
    `auth.firebase.tools` 와 `*.firebaseio.com` 을 막아 firebase CLI 가 못 돌았고,
    그래서 «붙여넣을 글을 사람이 옮기는» 옛 방식으로 되돌아갔다.
    사람이 옮기는 자리가 생기면 **한 글자가 조용히 바뀌어도 게시는 그냥 된다** —
    하필 그것이 `.read` 안이면 급여·주민번호가 열린다. 그래서 되받아 견뎠다.
  ★ **고칠 자리는 방(환경)의 통신 허용 목록이다.** 아래 셋을 넣으면
    `scripts/rules-deploy.js` 가 살아 있는 콘솔을 직접 읽어 견주고 올린다 —
    사람이 옮겨 적는 자리가 아예 없어진다(CLAUDE.md 가 정한 본래 방식).
    | 넣을 주소 | 무엇에 쓰나 |
    |---|---|
    | `auth.firebase.tools` | firebase CLI 로그인 |
    | `pureun-erp-default-rtdb.firebaseio.com` | 규칙 읽기·올리기 |
    | `pureun-erp-default-rtdb.asia-southeast1.firebasedatabase.app` | 우리 DB 의 실제 주소(**서울이 아니라 싱가포르**라 이름이 다르다 — `*.firebaseio.com` 만으로는 안 걸린다) |
    ⚠ 넣으신 뒤에는 **새 방을 여셔야 한다** — 방은 시작할 때의 정책을 그대로 문다.
- [x] **얇은 거울** (2026-09-05 · 끝) — **「읽는 쪽」이 만든다.**
  두 길(기업정보함이 쓸 때마다 / 함수 트리거) 다 안 골랐다. 앞엣것은 쓰는 자리가 두 파일에
  흩어져 **하나만 놓쳐도 조용히 낡고**, 뒤엣것은 함수 배포가 아직 안 돌았다.
  대신 **무거운 자리를 한 번 읽은 김에** 떠 둔다 — 쓰는 자리를 안 건드리니 놓칠 자리가 없다.
  ⚠ 거울은 낡을 수 있다. **숨기지 않는다** — 화면이 「마지막으로 뜬 때」를 말한다.
  ⚠ 규칙은 손댈 것이 없었다(`pucards/$k` 가 이미 열려 있다).
  자세한 것은 `docs/푸른통합온톨로지-6단계.md` 4-D.

## 0. 한 줄

푸른노무법인 사내 통합 포털. 정적 HTML + Firebase RTDB(서버 없음), GitHub Pages 배포.
화면 13개가 한 저장소에 있고, 여러 방이 **동시에** 고친다.

- 포털 입구: `enter.html` · 배포: https://nabaho.github.io/pureunall/
- 검사: `node --test tests/*.test.js` (2026-09-03 기준 약 **10,950건**)
- **고장넣기(mutation)는 필수 절차다.** 검사가 정말 무는지 확인하지 않은 PR 은 반쪽이다.

---

## 1. 화면 한눈에 (2026-09-03 실측)

| 화면 | 무엇 | 크기 | 사흘 | 한달 |
|---|---|---:|---:|---:|
| `pu-erp.html` | 푸른이알피 — 업체·계약·사건·직원. **모든 것의 원본** | 5,200KB | 27 | 345 |
| `pu-cards.html` | 기업정보함 — 사업자·명함·기업 상세 | 1,707KB | 30 | 426 |
| `pu-photos.html` | 사진첩 — 서류를 찍어 판독하고 각 앱으로 보낸다 | 987KB | 21 | 293 |
| `fund.html` | 기금관리 (자체 기록부 있음 → `fund-erp/STATUS.md`) | 842KB | 15 | 132 |
| `kcareer.html` | 경력관리 | 761KB | 20 | 117 |
| `gov-consulting.html` | 정부컨설팅 — 사업·회차·일정 | 755KB | 25 | 129 |
| `work.html` | 업무관리 | 631KB | 7 | 91 |
| `rules.html` | 규정관리 (취업규칙) | 501KB | 3 | 35 |
| `pu-paydata.html` | 급여데이터함 | 301KB | 8 | 119 |
| `pu-home.html` | 홈페이지 관리 — 요즘 가장 뜨겁다 | 276KB | 15 | 52 |
| `enter.html` | 포털 입구 · 사용액 | 271KB | 2 | 95 |
| `payroll-os.html` | 급여관리 | 165KB | 5 | 32 |
| `pu-news.html` | 뉴스레터 — **8월 말에 새로 만든 것** | 59KB | 12 | 12 |
| `pu-camera.html` | 푸른카메라 — **사진만 찍는 별개 앱**(2026-09-12). 담기는 곳은 사진첩 한 곳 | 25KB | — | — |

*사흘 = 08-31~09-03 · 한달 = 08-04~ · 그 화면을 건드린 커밋 수(크기가 아니라 손댄 횟수).*

---

## 2. 지금 손이 필요한 것

### ~~① 업무관리·급여데이터함에 사업자번호가 없다~~ → **틀린 진단이었다 (2026-09-03 확인)**
들여다보니 두 앱은 **이름으로만 맞추지 않는다** —
- `pu-paydata.html` 은 저장을 **`companyId`**(푸른이알피 업체 번호)로 한다.
- `work.html` 도 **`co_id` 우선**, 없을 때만 정규화한 이름 «정확 일치»로 물러난다
  (`coFind` · `_normCo` 가 `(주)·주식회사·빈칸`을 뗀다 → 2026-08-28 의
  「주식회사 행복한단홍갈비」/「행복한 단홍갈비」 쌍은 **이미 맞는다**).

**사업자번호는 이 자리의 열쇠가 아니다** — 업체 번호가 이미 그 일을 하고 있다.
넣어도 얻는 것이 없으므로 **하지 않는다.** (기업정보함 점검의 C2 는 이로써 닫는다.)

⚠ 대신 그 자리를 살펴보다 **진짜 결함 하나**를 찾아 고쳤다 —
급여데이터함은 올린 파일의 «이름»으로 사업장을 짐작한다(사업자번호가 파일 이름에
있을 수 없으니 이 자리는 이름으로 할 수밖에 없다). 그런데 긴 이름부터 훑으며
「담고 있나」만 보아서, 파일 「천성」이 목록에 **「천성」이 버젓이 있는데도**
「천성가축약품」으로 갔다(PR #837 이 「다른 곳」이라고 못 박은 쌍).
→ **이름이 그대로 같은 곳을 한 바퀴 먼저 찾게** 고쳤다.
⚠ 「천성전자」가 「천성」으로 걸리는 것은 **못 막는다** — 「화담원 아산점」이 화담원의
지점이라, 「이름 뒤에 글자가 붙으면 다른 곳」으로 볼 수가 없다. 짐작일 뿐이므로
사람이 고른 이름표가 이기게 두었다. 그 사실을 검사에 적어 두었다
(`tests/paydata-name-exact.test.js`).

### ★ 만드는 중 — 「사업장에 보낸 서류」를 기업정보함에 (대표 지시 2026-09-03)
「취업규칙 이후에 근로계약서와 연차 관련 기타 서류 등 **모든 사업장에 송부되었던 서류**를
한번에 데이터함에 보관하려고 한다.」

실측해 보니 조각은 여럿 있는데 **「사업장별로 보낸 서류」 한자리가 없다** —
- 보낸 기록 `pucards/sendLog/{명함번호}` 는 **사람에 붙는다**(담당자가 바뀌면 흩어진다).
  기업 상세에는 아예 안 나온다.
- **단체 메일은 기록을 하나도 안 남긴다.**
- 취업규칙만 `PuRulesHistory` 로 **회사에 붙는다** — 이 방식이 옳고, 여기를 넓히면 된다.
- 근로계약서·연차는 사진첩에 «받은 것»만 있고 «보낸 것»은 없다.

**검토안(목업 3장 포함): `docs/mockups/2026-09-03-sent-docs-design.html`**
설계 넷 — ①기록의 임자를 사람→사업장 ②갈래를 한 곳에(SENT_KINDS) ③배포를 «묶음»으로
(312곳에 통째로 베끼면 2026-08-16 규모) ④기업 상세를 「받은 것/보낸 것」 두 축으로.
만드는 순서 네 걸음을 적어 두었고, 대표 지시 「1걸음씩」으로 **한 걸음씩** 만든다.

- [x] **1걸음 — 기업 상세에 「📤 보낸 서류」 칸** (2026-09-03 · 새 쓰기 0)
      그 회사 사람들의 `sendLog` 를 모아 «회사 눈»으로 보여 준다. 받은 쪽에는 📥 를 붙여
      두 축이 되게 했다. `coSentList`·`coSentHtml`·`loadCoSent` · 검사 16개 · 고장넣기 19개.
      ⚠ 취업규칙 회차는 **여기 안 넣었다** — 이미 위 업무 이력 표에 갈래로 있다
        (`coRulesRecs`). 넣었으면 한 화면에 같은 회차가 두 번 나올 뻔했다.
      ⚠ 사람이 30명 넘는 회사는 **최근 30명분만** 읽고, 그 사실을 화면에 적는다
        (명함 한 장이 서버 조회 한 번이다).
- [x] **2걸음 — 1:1 보내기가 «회사에도» 한 줄 남긴다** (2026-09-03 · 첫 새 쓰기)
      `pucards/sentDocs/{회사열쇠}` 에 한 줄. 회사 열쇠는 기업 상세가 쓰는 «그 열쇠»다
      (`coKeyOfCard` → `coList`). 새 잣대를 만들지 않았다.
      ⚠ 명함 기록(`sendLog`)은 **그대로 둔다** — 읽을 때 두 곳을 합치고, 같은 명함·같은
        시각이면 한 번만 센다. 옮기다 잃는 것이 더 크다.
      ⚠ 받는 **메일 주소는 회사 쪽에 안 남긴다** — 전 직원이 보는 자리다. 이름이면 된다.
      ⚠ 서버 규칙은 «안 고쳤다» — `pucards/$k/$k2` 가 이미 열려 있다(sendLog 와 같은 자리).
      갈래 목록 `SENT_KINDS` 를 지금부터 적어 둔다 — 나중에 붙이면 기록마다 다시 매겨야 한다.
- [x] **3걸음 — 전 사업장 배포 기록** (2026-09-03)
      ★ **보내는 도구가 아니라 «기록하는» 도구다.** 대표 지시의 말이 「보관」이었다 —
        메일은 지금 길로 보내고, 「어느 서식을 어느 사업장에 보냈다」를 한 번에 남긴다.
        없는 발송 사실을 만들어 내지 않는다.
      `sentBatch/{묶음}` 한 줄 + 회사별 `sentDocs/{열쇠}/{묶음}` 한 줄.
      ⚠ **묶음 줄을 먼저** 쓴다 — 중간에 끊겨도 되돌릴 수 있어야 한다.
      ⚠ **회사별 줄의 열쇠가 묶음 번호다** — 두 번 눌러도 줄이 두 개가 안 된다.
      ⚠ **한 통에 200곳씩** 모아 쓴다(2026-08-16 오류 5,000건이 한 장씩 쓴 값이다).
      ⚠ **계약해지(🚪)는 기본으로 뺀다** — 끝난 곳이 「받은 곳」으로 잡히면 4걸음의
        「안 받은 곳」 세기가 틀린다. 뺀 곳은 «세어서» 화면에 적는다.
      ⚠ **되돌리기**가 있다(회사별 줄 먼저, 묶음 마지막). 묶음에 열쇠 목록을 담아 둔다.
      켜는 길 둘: PC 설정 › 탭·자료 · 폰 메뉴.
- [x] **4걸음 — 「이 갈래를 못 받은 사업장만 보기」 거르개** (2026-09-03)
      설계안이 말한 「이 구조가 공짜로 주는 것」이 이것이다 — 배포를 «묶음»으로
      남겨 두었으니 지금 목록에서 그 묶음에 안 든 곳을 빼면 된다.
      🔎 거르개 ▾ 안에 「📤 못 받은 곳」 › 배포한 갈래마다 한 줄(못 받은 곳 수와 함께).
      ⚠ 회사 4,159곳의 기록(`sentDocs`)을 **통째로 안 읽는다** — 묶음(`sentBatch`)
        몇 줄에 받은 곳 열쇠가 다 들어 있다. 1:1 발송은 갈래가 늘 「자료」라,
        취업규칙·근로계약서 같은 갈래는 **묶음이 유일한 자리**이고 이 셈이 온전하다.
      ⚠ **계약해지(🚪)는 여기서도 뺀다** — 3걸음이 애초에 안 보낸 곳이다. 안 빼면
        끝난 곳이 죄다 「안 받은 곳」으로 올라와 목록이 못 쓰게 된다.
      ⚠ **묶음을 아직 못 읽었으면 안 거른다** — 빈 것으로 거르면 4,159곳이 통째로
        「안 받은 곳」이 되어 화면이 거짓말을 한다.
      ⚠ 켜짐 띠에 **갈래 이름이 들어간다**(「📤 근로계약서 못 받은 곳」) — 어느 서식인지
        모르면 볼 값이 없다. 같은 갈래를 다시 누르면 풀린다.

- [x] **덧 — 단체 메일도 기록을 남긴다** (2026-09-03 · 대표 지시 「단체메일로 기록 남게」)
      `bulkSendAll` 이 «걸린 뒤에» 3걸음의 묶음을 하나 남긴다 — 새 자리를 안 만든다.
      갈래는 「자료」, 이름은 제목({회사}·{이름} 자리는 뺀다), 대상은 «실제로 나간»
      명함의 회사들.
      ⚠ `mailTargets` 가 돌려주는 줄에는 **명함 번호가 없다**(그 값이 그대로 서버로
        나가서 일부러 적게 담는다) — 그래서 묶음을 «만들 때» 회사 열쇠를 적어 둔다.
      ⚠ 기록에 실패하면 **그 자리에서 말한다** — 메일은 나갔는데 기록이 없으면
        나중에 「안 보냈다」로 읽힌다.

- [x] **덧 — 같은 서류 정리 → 휴지통** (2026-09-03 · 대표 지시 「중복이면 제거」·
      「중복자료 삭제 휴지통에 넣어달라」)
      ★ **지우지 않는다.** `trashDocs` 로 옮기고 휴지통에서 되돌린다.
      ★ **사진첩 원본은 안 건드린다** — 옮기는 것은 기업정보함이 든 «기록»뿐이다.
      잣대 둘(둘 다 «좁게»):
      ① 사진첩의 «같은 서류»(주인·해·번호가 같다)가 두 번 들어온 것
      ② 사진은 달라도 «이름도 적힌 것도 한 글자도 안 다른» 것
      ⚠ 적힌 것이 **셋도 안 되면 안 묶는다** — 우연히 같은 남의 서류를 지운다.
      ⚠ **회사마다 따로** 본다. 남기는 것은 **최근** 것.
      ⚠ 넣기와 빼기를 **한 통에** — 나눠 쓰면 잃거나 두 곳에 남는다.
      ⚠ 휴지통 열쇠가 «회사+서류»라 두 번 정리해도 한 줄이다.
      🗑 휴지통 화면에 「📄 서류 기록」 칸이 붙었다(복원·영구삭제). 열 때 한 번만 읽는다.

**네 걸음과 덧 둘이 다 끝났다.** 「보낸 서류」 줄기는 여기서 닫는다.

*(2026-09-03 에 적었던 「단체 메일은 아직 기록을 안 남긴다」 줄은 지웠다 — 그날 저녁
덧으로 남게 만들었고, 기업 상세의 각주도 함께 고쳤다.)*

### ② 죽은 가지 치우기
`feat/homepage-manage` 는 2026-08-27 에 멈춘 뒤 본줄기가 **2,171 커밋** 앞서 갔다.
되살릴 가지가 아니다. 그 안의 메일 목업 여섯 개가 PR #879(2026-09-03 합침)로
건져졌는지 확인한 뒤 치운다.

### ③ 취업규칙 서고 — **1단계 만들었다** (2026-09-05, 대표 지시 「모두 진행」)
설계서 `docs/superpowers/specs/2026-09-02-취업규칙-서고-design.md` ·
계획서 `docs/superpowers/plans/2026-09-02-취업규칙-서고-1단계.md`
(⚠ STATUS 가 가리키던 `docs/chwieop-seogo-*` 는 **파일이 아니라 가지 이름**이었다.
 문서는 이미 `main` 에 들어와 있었다 — 그래서 「문서만 있고 PR 이 없다」로 읽혔다.)

- [x] **1단계 — 순수 판정 모듈** `js/pu-rules-casebook.js` (화면·Firebase 없음)
      사업장 키 · 서류종류 판정 · 회차 번호 · ERP 대조 · 파일 해시 · 경로 조립 · 골든셋.
      검사 **51개**(`tests/rules-casebook-*.test.js`). `rules.html` 에 한 줄로 실었다.
      ⚠ **브라우저에서 실제로 돌려 확인했다** — 계획서가 「`shaOf` 가 브라우저에서도
        도는지 여기서 처음 확인된다」고 못 박은 자리라 건너뛰지 않았다. 네 값 모두 일치.
      ★ **계획서의 검사와 구현이 한 곳에서 어긋났다** — 회차 번호에서 구현은 빈자리를
        메우고(`2022-2` 되쓰기), 같은 계획서의 검사는 「번호는 뒤로만 간다」를 막고 있었다.
        **검사 쪽이 맞다** — 되쓰면 지운 회차의 창고 파일과 뒤섞인다. 구현을 고쳤다.
      ★ `nameFromFile` 이 Produces 목록에서 빠져 있었다(검사는 부른다). 내보내게 했다.

- [x] **2단계-A — 분류 «심장»** (2026-09-05 · 화면 없이 판정만)
      폴더를 통째로 떨어뜨렸을 때 파일마다 «어느 사업장 / 몇 년 / 무슨 서류»를 가리는 부분.
      `classify` · `yearOf` · `effDateOf` · `markBeforeAfter` · `applyFolderSite` ·
      `splitReady` · `WRITE_ORDER`. 검사 **13개**(`tests/rules-casebook-classify.test.js`).
      ★ **원칙: 못 가리면 추측하지 않는다.** 애매한 것은 `etc` 로 밀지 않고 「확인 필요」로
        올리고, **무엇을 못 가렸는지**까지 말한다(「확인 필요」만으로는 고칠 수가 없다).
      ⚠ 연도를 못 가려도 **올해로 안 바꾼다** · 시행일을 모르면 신·구를 **안 가린다**
        (차례로 정하면 폴더를 다시 읽을 때 답이 달라진다).
      ★ 만들며 잡은 것 — 폴더 이름(「2022개정」)이 사업장으로 들어앉아, 사람이
        「전부 이 사업장으로」를 눌러도 **안 바뀌었다**. 짐작의 «단단함»을 갈랐다
        (ERP 로 맞은 것만 단단하다 · `firmSite`).
- [x] **2단계-B — 화면** (2026-09-07 · 끝) — `rules.html` › ⋯ 참고 › **🗄 서고**
      폴더 떨어뜨리기(안쪽 폴더까지 걸어 들어간다) · ⚠ 확인 필요분을 **맨 위**에 ·
      가려진 것은 접어 둠 · 「이 폴더 전부 ○○로」 · 올리기 · 「실패한 것만 다시 시도」.
      ★ **올리는 화면 첫 줄에 «서고에 올리면 직원 전체가 볼 수 있습니다»를 적었다**
        (설계서 §6 — 보관함과 갈리는 지점이다. 모르고 올렸다가 나중에 알면 사고다).
      ★ **끊긴 것을 «이어» 올린다** — 같은 해 회차가 이 묶음의 파일을 하나라도 들고
        있으면 그 회차에 이어 붙인다. 안 그러면 넷 중 셋을 올리고 끊겼을 때 나머지
        하나가 「2022-2」라는 **유령 회차로 혼자 앉는다**(설계서 §7 이 말한 고아).
      ⚠ 확인 필요분은 **빼고 먼저 올린다** — 17건 때문에 325건이 막히면 안 된다.
      ⚠ 색인은 **개정본(after)만** — before 까지 넣으면 옛 문구가 검색에 섞인다
        (문안 은행이 「⚠ 개정 전 문구 의심」으로 이미 겪은 일이다).
      ⚠ 파일 바이트는 **안 들고 있는다**(해시와 글만 남기고 버린다) · 색인 쓰기는
        200칸씩 나눈다(2026-08-16 대량 쓰기 사고).
      `uploadPlan`·`idxKeysOf`·`paths.revs` + 검사 **12개**(`tests/rules-casebook-upload.test.js`).
- [x] **3단계 — 서고 목록 + 사업장 이력** (2026-09-07 · 끝) — 같은 창의 두 갈래
      **📚 목록** — `casebook/index` **한 층만** 읽는다. 회차·본문을 함께 읽으면 서고를
        여는 순간 수십 MB 가 딸려 온다(§3 이 층을 가른 까닭).
        찾기(사업장·사업자번호·업종·담당) · 연도 · 규모로 거르고, **📊 실적표**로 내보낸다.
        ⚠ 내보내는 것은 «보고 있는 줄»이다 — 거른 것과 내보낸 것이 다르면 실적표가 거짓말을 한다.
      **🏢 사업장 이력** — 줄을 누르면 그 사업장의 회차만 읽는다. 회차마다 어떤 서류가
        있는지 칩으로. 핵심 단추는 **「▶ 이 회차로 검토 시작」** — 개정본 **본문만** 읽어
        `applyWorkObject` 로 검토 화면에 싣는다(임시저장 복원과 같은 길).
      ★ **못 하는 것은 «왜 못 하는지»를 적는다** — 스캔 PDF 라 글이 없으면 단추를 흐리게만
        두지 않고 「개정본에 글이 없습니다 (스캔 파일)」를 붉게 적는다. 단추만 흐리면
        왜 못 누르는지 아무도 모르고, 몇 번 겪으면 화면을 안 믿게 된다.
      ★ **모르는 값을 지어내지 않는다** — 규모를 모르면 `—` 이지 「0명」이 아니다.
        규모로 거를 때도 모르는 곳은 «빼 놓는다»(0명으로 치면 「9명 이하」에 끼어든다).
      ⚠ **폰에서 여섯 칸이 죄다 접혔다**(411px 실측 — 「도소/매」「42/명」「김노/무」).
        곁칸(업종·규모·담당)을 감추고 사업장 이름 밑 «이미 있는» 작은 줄에 붙였다.
        ★ 사업자번호·담당까지 넣었더니 그 줄이 또 접혀 **세 줄**이 됐다 —
          곁줄을 두 줄로 만들 바에는 덜 중요한 것을 뺀다(둘은 사업장을 열면 머리줄에 있다).
          표 높이 184 → **139px**.
      검사 **16개**(`tests/rules-casebook-view.test.js`).
      ★ 만들며 잡은 것 — 배열도 `typeof` 로는 `'object'` 라 «업체 한 줄»로 세어졌다.
        pu-erp 의 `erpCompaniesFrom` 이 같은 자리에서 겪은 일이다(2026-09-05
        「ERP 업체 2건 로드」 — 실은 지도 통째와 시각 숫자였다). `isRow` 로 막았다.
- [x] **㉠ 「글 없음」 세기 · ㉡ 제출 서류에 몇 줄 적기** (2026-09-07 · 대표 지시 「ㄱ ㄴ」)
      ★ 대표 물음: 「보관함 저장할 때 OCR 하는 게 좋은 건가, 단순 보관은 의미가 없을 것 같은데」
      ★ **답: 서류를 두 종류로 갈라야 답이 나온다.**
        | | 본문류(개정본·개정 전·대조표) | 제출류(신고서·의견청취·동의서) |
        |---|---|---|
        | 실제 파일 | 대개 `.hwp` — 글이 이미 있다 | 대개 스캔 — 도장·서명 |
        | 무엇에 쓰나 | 검색·문안 재사용·재개정 | **증빙** |
        | OCR | 거의 필요 없다 | **해도 소용없다** |
        제출류에서 정작 필요한 「언제·어느 노동청에·몇 명 동의로」는 도장과 손글씨라
        기계가 못 읽는다. 사람이 3초면 적는다. 그래서 OCR 대신 «몇 줄 적기»를 붙였다.
      - **㉠ 🔍 글 없음 세기** — 목록에서 한 번 누르면 본문류/제출류로 갈라 셈을 보인다.
        ⚠ 이 셈만 회차 층을 «통째로» 읽는다(목록에 없는 값이라). 그래서 저절로 안 하고
          사람이 누를 때만 돈다. 본문은 딴 층이라 안 딸려 온다.
        ★ 숫자만 던지지 않고 «이 숫자로 무엇을 정하는지»까지 화면에 적는다.
      - **㉡ 제출 정보** — 회차의 신고서·의견청취·동의서에 다섯 칸.
        `at`(언제) · `no`(접수번호) · `office`(어디에) · `n`(몇 명) · `nAll`(전체 몇 중).
        ★ **서류마다 묻는 말이 다르다** — 신고서에 「동의 인원」을 묻고 동의서에 「접수번호」를
          물으면 안 쓰는 칸이 늘 비어 있고, 사람은 곧 칸 전체를 무시한다. 안 묻는 칸은 감춘다.
        ★ **규칙에 막히기 «전에» 말해 준다** — 「동의 인원이 전체보다 많습니다」.
          permission_denied 는 무엇이 잘못인지 안 알려 준다.
        ⚠ **빈 칸은 안 담는다** — 「모른다」와 「0」은 다른 말이다(0 은 담는다).
      ✅ **`…/docs/$role/sub` 규칙이 올라갔다 — 제출 정보 입력이 이제 열렸다** (2026-09-07 저녁)
        대표가 콘솔에 손수 넣으셨다. 되받아 «기계로» 견줘 확인했다:
        | | |
        |---|---|
        | 적용본 `docs/firebase-rules-전체-적용본.json` | 규칙 낱개 **433개** |
        | 09-07 아침 콘솔 snapshot | **427개** |
        | 차이 | **정확히 6개** — 전부 `…/docs/$role/sub` 의 `at`·`no`·`office`·`n`·`nAll`·`$other` |
        | 콘솔에만 있던 것 · 값이 다른 것 | **0개 · 0개** |
        지금 콘솔에 그 여섯이 다 있으므로 **콘솔 = 적용본(433/433)** 이다.
        ⚠ **새 snapshot 파일은 안 만들었다.** 콘솔 글을 사람이(또는 AI가) 옮겨 적는 순간
          한 글자가 조용히 바뀌어도 아무도 모른다 — 하필 그것이 `.read` 안이면 급여·주민번호가
          열린다. 「같다」는 사실만 여기 적는다. 진짜 snapshot 은 통신 허용 목록이 풀려
          `scripts/rules-deploy.js` 가 콘솔을 직접 읽을 때 남긴다.
      검사 **17개**(`tests/rules-casebook-sub.test.js`).
- [x] **「그 뒤 시행 N」 — 서고를 «명단»으로** (2026-09-07 · 대표 물음 「취규 내용검토 필요한가?」)
      ★ **답: 필요하다. 그리고 그것이 서고의 값어치다.** 담아 두는 것만으로는 값어치가 없고,
        «지금 기준으로 다시 훑을 때» 생긴다.
      ★ **왜** — 법이 계속 바뀐다. 규칙집 92개 중 **18개에 시행일**이 붙어 있다(2019~2027).
        | 마지막 개정이 | 그 뒤 시행돼 «이미 적용 중»인 규칙 |
        |---|---|
        | 2021년 | **11개** |
        | 2022~2024년 | **10개** |
        | 2025년 | 2개 |
        출산전후휴가·육아휴직·배우자 출산휴가·임금명세서 교부 같은 것들이다.
        마지막 개정이 오래된 사업장은 그것들을 못 반영했을 수 있다 — 곧 **연락할 명단**이다.
      ★★ **「위반」이라 말하지 않는다.** 서고의 회차는 「그때 우리가 낸 것」이지
        「지금 그 회사가 쓰는 것」이 아니다 — 우리를 안 거치고 자체 개정했을 수 있다.
        그래서 셈만 보이고, **그 까닭을 화면 발밑에 적어 두었다**(숫자만 보면 위반으로 읽힌다).
      ★ **본문을 안 읽는다** — 목록에 이미 있는 `lastYear` 와 규칙집 시행일만 견준다.
        그래서 목록 화면에서 곧바로 나온다(층을 안 건드린다).
        어느 조문이 실제로 걸리는지는 사업장을 «열 때» 본문에 규칙을 돌리는 딴 일이다.
      ⚠ **연도를 모르면 0 이 아니라 `?`** — 0 은 「다 반영됐다」로 읽힌다.
      ⚠ 화면에 보이면 **실적표에도 넣는다** — 보는 것과 내보낸 것이 달라지면 안 된다.
      「손봐야 할 곳만」 거르개. 검사 **10개**(`tests/rules-casebook-since.test.js`) —
      진짜 규칙집으로 한 번 돌려 「2022년 뒤로 시행된 것이 실제로 있다」까지 못 박았다
      (없으면 이 화면을 만들 까닭이 없다).
- [x] **깊은 검토 — 회차 본문에 규칙 92개를 실제로 돌린다** (2026-09-07 · 끝)
      사업장 이력의 회차마다 **🔎 지금 기준으로 훑기**. 본문을 읽어 `parseArticles` →
      `evaluate` 로 돌리고, 위반의심·누락만 추려 보인다(적합 56줄을 실으면 봐야 할 5줄이 묻힌다).
      ★★ **가장 조심한 것 — 「위반의심 0」을 «깨끗하다»로 읽게 두면 안 된다.**
        규칙 92개 중 **27개가 「수동확인」**이라 기계가 아예 판단하지 않는다.
        실측에서도 위반의심 0 · 수동확인 28 이 나왔다 — 단서를 안 적었으면
        그 화면은 「이 사업장은 문제없다」로 읽혔을 것이다. 그래서 늘 함께 적는다.
      ★ **규모가 결과를 크게 바꾼다**(실측: 5인미만 52개 · 10인이상 87개 · 30인이상 89개).
        업체 자료에서 가져오되 **어디서 온 값인지 말하고**, 그 자리에서 고쳐 다시 훑을 수 있다.
      ⚠ 조건부 규칙은 실제로 **0개**다(전부 「공통」) — 빈 Set 이라 놓치는 것이 없다.
        (`category` 의 「조건부 5」와 `condition` 은 다른 칸이다. 헷갈리지 말 것.)
      실측(샘플 5,320자·조문 54개): 누락 5건 — 장애인 인식개선 교육 · 예비군·민방위 훈련 ·
        사회보험 가입 · 채용서류 반환 · 근로자 개인정보 보호. 전부 진짜 빠진 것이다.
      검사 **11개**(`tests/rules-casebook-review.test.js`) — 규칙집에 새 갈래가 생기면
      `CAT_ORDER` 에 안 넣은 것이 그 자리에서 걸린다(안 넣으면 목록 맨 뒤로 조용히 밀린다).
- [x] **㉢ 본문이 스캔뿐인 옛 회차에만 OCR** — **끝났다** (PR #1116 · 대표 결정 2026-09-07 「읽혀 검색에 걸리게」)
      ⚠⚠ **이 줄이 2026-09-13 까지 `- [ ] 아직 안 만들었다` 로 남아 있었다.** 기능은
        이미 `main` 에 있었는데(`2233427b`) 체크만 안 됐다 — 그 한 글자 때문에 기능 검토가
        「아직 안 만든 것」으로 잘못 꼽았다. **기록부가 틀리면 다음 사람이 같은 일을 또 판다.**
      사업장 이력의 서류 칩에 `[🔍 글자 읽기]`(다시 읽으면 `[🔄 다시 읽기]`).
      검사 **38개**(`tests/rules-casebook-ocr.test.js`) — 아래 셋을 다 지킨다.
      - **브라우저 안에서만**(Tesseract.js wasm). `pu-erp` 의 `ocrExtract` 는 Google Vision 을
        «먼저» 부르므로 **안 가져다 썼다** — 신고서·동의서에는 근로자 이름·서명이 있다.
        (검사가 「readDoc 으로 갈아타는 것」을 그 자리에서 막는다.)
      - **원문 층과 딴 자리**(`paths.ocr`) · **문안 은행에 안 적립** · **「검토 시작」도 안 열어 준다.**
      - 글이라 할 만한 것이 없으면 **안 담는다**(잡티 수천 자도 막는다) · 제출류에는 **안 붙는다**
        (도장·손글씨는 기계가 못 읽는다) · 쪽 수 한도 30쪽 · 첫 1회 15MB 사전 받는 것을 «미리» 말한다.
- [x] **4단계 — 사례집 검색 + 문안 은행 적립** (2026-09-07 · 대표 지시 「4」)
      **새 화면을 안 만들었다**(설계서 §5-③). `🔎 조문 검색`이 지금 스스로 밝히던 한계
      (「※ N곳은 보관된 원문이 없어 검색되지 않습니다」)를 서고가 채운다.
      ★ **왜 색인인가** — `artsBuild` 는 보관함 건마다 읽기 1회로 전문을 다 쌓는다.
        수백 건이면 무너진다. 색인으로 «후보를 좁히고» 본문은 고른 것만 읽는다(상한 40회차).
      ★★ **색인 낱말과 검색어의 길이 관계가 «양쪽»이다** — 여기서 한 번 헤맬 자리다.
        `idxKeysOf` 는 본문 낱말을 «그대로» 열쇠로 쓴다.
        | 어느 쪽이 긴가 | 예 | 어떻게 |
        |---|---|---|
        | 색인 낱말이 길다 | 「연차」로 「연차유급휴가」 | 앞머리 훑기 1회(`orderByKey().startAt(q).endAt(q+\uf8ff)`) |
        | 색인 낱말이 짧다 | 「연차유급휴가」로 「연차」 | 앞토막 정확히 찾기(2글자부터, 최대 9회) |
        한 방향만 하면 **「되는 것 같은데 안 나온다」**가 된다. 합쳐 읽기 10회 이하.
      ⚠ **색인은 «좁히는 도구»다. 판정은 본문이 한다** — 읽어 온 글을 보관함 검색과
        «똑같은 셈»(`stripWs(title)+bodyNs`)으로 다시 견준다. 검사가 두 곳이 같은지 지킨다.
      ⚠ **색인의 한계를 화면에 «늘» 적는다** — 색인은 한 회차에서 2번 이상 나온 낱말
        상위 60개만 담는다. 한 번만 나온 말은 못 찾는다. 안 적으면 「검색했는데 없네」가 된다.
      ★ 대표 결정: **보관함과 서고를 한 목록에 섞고 `🗄 서고` 딱지로 가른다**
        (같은 주제 조문이 사업장별로 나란히 읽히는 것이 이 화면의 값어치다).
      ⚠ 글자마다 Firebase 를 읽지 않는다(260ms 모아서 한 번) · 늦게 온 답이 지금 검색어를
        덮지 않는다 · 한 번 읽은 본문은 캐시.
      **문안 은행 적립** — 올릴 때 이미 있는 길(`bankAccum`→`bankMergeUpload`)을 그대로 탄다.
      ⚠ **개정본(after)만.** before 를 넣으면 옛 문구가 은행에 섞이고 다음 사람이 그것을
        복사해 쓴다(은행이 「⚠ 개정 전 문구 의심」으로 이미 겪은 일).
      ⚠ 적립이 실패해도 **올리기는 계속된다** — 은행이 서고를 막으면 안 된다.
      ⚠ 적립한 셈을 마무리 알림에 적는다 — 딴 화면이라 조용히 늘면 확인할 길이 없다.
      검사 **31개**(`rules-casebook-search.test.js` 16 · `-wired.test.js` 15).
- [x] **원본 파일(⑤) — Firebase Storage** (2026-09-07 · 대표 지시 「배포되면 파일스토리지 진행」)
      창고는 **새로 만들지 않았다** — 기존 `pureun-erp-hrphotos`(기금관리가 인가증·등기부
      원본을 이미 넣는 국내 창고). 자리는 `casebook/{siteKey}/{revId}/{role}.{ext}`.
      ★★ **막혀 있던 것은 「파일 손잡이」였다** — `cbRead` 가 이름·해시·글만 담고
        `File` 을 버려서, 올릴 때 «바이트가 없었다». `File` 은 바이트가 아니라 디스크
        참조라 수백 개를 들고 있어도 가볍다. `entries` 에 `f` 를 담아 이어 줬다.
        `uploadPlan` 이 행 객체를 «그대로» `회.docs` 에 넣으므로 그 뒤는 저절로 따라간다.
      ⚠ **쓰는 차례는 무거운 것부터**(설계서 §7): ⓞ 원본 → ② 글 → ③ 회차 → ④ 색인.
        끊기면 «파일은 있고 색인이 없는» 고아가 남는데 그건 `sha` 로 찾아 색인만 붙이면
        된다. 거꾸로 하면 «색인은 있고 파일은 없는» 거짓말이 남는다. 검사가 차례를 지킨다.
      ⚠ **원본 실패가 회차 실패가 아니다.** Storage 규칙이 콘솔에 아직 없을 수 있어
        (아래 ★) 그때도 글·색인은 그대로 올라간다(설계서 §3 차선). 못 담으면 `path` 를
        **적지 않는다** — 없는 자리를 적으면 화면이 헛것을 열려 한다.
      ⚠ **몇 건 못 담았는지·왜인지 알린다.** `storage/unauthorized` 면 「창고 규칙이 아직
        올라가지 않아…」라고 이름을 대고 말한다. 조용히 빠지면 나중에 원본을 찾다가 없다.
      **여는 쪽** — 사업장 이력의 서류 칩에 `📎 원본`. 없으면 「원본 없음」이라 적는다
        (흐린 단추만 두면 왜 못 누르는지 모른다). 내려받기 주소는 «누를 때» 얻는다 —
        미리 얻으면 시한 있는 주소를 안 쓸 만큼 수십 개 만든다.
      검사 **20개**(`rules-casebook-orig.test.js` 11 · `casebook-storage-rule.test.js` 9).

      ★★ **대표님 손이 한 번 필요하다 — 이것만 하면 원본이 담깁니다.**
        `docs/firebase-storage-전체(붙여넣기용).txt` 를 콘솔에 게시.
        콘솔 → Storage → **pureun-erp-hrphotos** → 규칙 → 붙여넣기 → 게시.
        이번에 `casebook/` 한 칸을 그 파일에 넣어 뒀다(검사가 그 칸의 권한·크기·자리를 지킨다).
        ⚠ 게시 전에도 **아무것도 안 깨진다** — 글·색인·검색·사례집·이력은 그대로 되고
          원본만 안 담기며, 앱이 그 사실을 이름을 대고 말한다.

### ★ 대표 결정 둘 대기 — 규정관리 «사업장 줄» 정리 (2026-09-07)

대표 지시 「사업장이 여러 개가 계속 이렇게 나오면 너무 많이 개수가 발생해서 관리가 어렵다.
어떻게 하는 게 가장 좋은지 모든 방법 다 찾아봐라」 → 방법 다섯 갈래를 훑고 목업까지 냈다.

**목업**: `docs/mockups/2026-09-07-사업장줄-검토안.html` (자바스크립트 없이 세 장면)

**실측** — 1900px 화면에서 칩은 **12개까지만** 보인다(칩 평균 124px · 줄 폭 1583px).
20곳이면 8곳, 50곳이면 38곳이 화면 밖이다. 가로 스크롤은 «높이»는 지켜도 «훑기»는 못 지킨다.

**구조상 원인 셋**
| | |
|---|---|
| ㉠ | **아무것도 안 빠진다** — `신고완료` 가 되어도 줄에 그대로 남는다 |
| ㉡ | **남의 끝난 일이 앞자리를 먹는다** — `canOpen = 내 것 \|\| 완료본` 이라 남의 것도 들어오는데, 신고(`filedAt`)가 없으면 `dashStage` 가 **검토완료**로 분류해 앞쪽에 섞인다 |
| ㉢ | 정렬은 이미 급한 순이라(`개정중→검토완료→작성중→신고완료→미작성`) **뒤쪽 두 무리만 접어도** 대부분 12칸에 들어온다 |

**대표가 「이어서 할 곳 고르기」로 답하셨다** → 그에 맞춘 설계:
- 줄에는 **내가 이어서 할 곳만**(개정중·검토완료·작성중). 신고완료·미작성·남의 완료본은
  **`＋N곳 더` 단추 하나**로 접고, 누르면 이미 있는 「전체 보기」가 열린다.
- **📌 핀** — 자주 보는 곳은 상태와 무관하게 맨 앞(전체 보기 목록의 줄마다 📌).
- 집계는 전체 곳수를 **그대로** 말한다(접었다고 개수까지 감추면 접기 전보다 나쁘다).

⚠ **목업을 그리며 셋이 바뀌었다** — 그림이 아니었으면 못 봤다.
  ㉠ 접기 단추를 상태별 셋으로 두니 **330px 을 먹어 정작 사업장 칩을 밀어냈다** → 하나로.
  ㉡ 접기 단추를 «구르는 줄 안»에 두니 칩과 함께 밀려 **「나머지가 여기 있다」는 표시가
     화면 밖으로 사라졌다** → 줄 밖으로.
  ㉢ 집계 `검토 15` 의 **12곳이 남의 완료본**이라 오히려 헷갈렸다 → `34곳 · 할 일 6`.

- [x] **㉠ 남의 완료본 12곳을 줄에서 뺐다** (2026-09-07 대표 결정 「내 할 일만 보이게」)
      줄에는 개정중·검토완료·작성중 «내 것»만. 나머지는 「＋N곳 더」 하나로 접고 전체 보기로 연다.
- [x] **㉡ 핀은 안 만들었다** (2026-09-07 대표 결정 「안 쓴다. 버리고 단순하게」)

### ④ 기업정보함 점검 — A1·죽은 함수는 끝났다 (남은 것은 아래 ⑤ 의 ㉢ E3)
- [x] **A1 — 죽은 코드 정리** (2026-09-05 · 끝)
      결과는 싱겁다 — **진짜 죽은 것은 둘뿐**이었다(`mergeOne`·`memberKindLabel`). 걷어냈다.
      ★ **이 자리가 막혀 있던 까닭은 「코드」가 아니라 「세는 법」이었다.**
      순진하게 세면 네 번 틀리는데, **네 번 다 «산 것을 죽었다»고 말한다** —
      그대로 지웠으면 화면이 멎었을 자리다.
      | 함정 | 어떻게 틀리나 | 실제로 잡힌 것 |
      |---|---|---|
      | 중괄호 세기 | `return out+"}"` 의 `}` 에 몸통 끝이 어긋난다 | rules.html `dlDoc` 이 「146KB 죽은 코드」로 |
      | 글자 걷어내기 | 틀글(`` ` ``) 닫는 백틱을 놓치면 그 뒤가 통째로 글자 | pu-cards 함수 1,406개 → 55개 |
      | 앞의 점(`.`) 빼기 | 검사는 `ctx.fn(...)` 로 부른다 | `pickEmail` 이 죽은 것으로 |
      | 즉시 실행 함수 | `(function 이름(){…})()` 는 부르는 자리가 없어도 산다 | `cleanupLS` 등 여섯이 죽은 것으로 |
      | 주석 속 선언 | 「자리가 생기면 이렇게」를 진짜 선언으로 읽는다 | `fromSms`·`fromKakao` 가 죽은 것으로 |
      연장 `tools/dead-code.js` (배포에서 걷힌다) · 검사 `tests/dead-code.test.js` 8개.
      ⚠ 검사가 «죽은 코드 0개»만 보지 않는다 — **연장이 다섯 함정에 다시 빠지는지도 잰다.**
        그것이 없으면 연장이 고장 나 「0개」라고 말할 때 아무도 모른다.
      ⚠ 이 연장은 **넉넉한 쪽으로 틀린다**(재귀는 산 것으로 본다) — 산 것을 지우느니 남긴다.
- [x] **검사·문서만 부르던 함수 정리** (2026-09-05 · 끝) — **지금 0개**
  여덟을 걷어내고, 다섯은 **까닭을 붙여 남겼다**. 검사 아홉을 산 코드로 되겨눴다.
  | 걷어낸 것 | 왜 |
  |---|---|
  | `onSelectPastCompany`(93줄) | `fillFromPast`·`fillCompanyImagesFromPucards` 와 나란한 쌍둥이 |
  | `savePortalCameraFile`+`portalCameraJpeg` | 포털이 사진첩으로 «바로» 가게 바뀌며 없어진 넘김길 |
  | `_mailDom`·`undoConfirm`·`pickEmail`·`mbCoIdOf`·`staffOf` | 같은 일을 하는 산 코드가 따로 있다 |

  | 남긴 것 | 왜 |
  |---|---|
  | `erpBuildSug` | 검사가 「조각내 돌린 것 = 한 번에 돌린 것」을 견주는 **기준자** |
  | `setNavState` | `nav_state` 는 다른 기기에 남아 있다 — **되돌릴 일이 생기면 이 손이 필요하다** |
  | `erpConsTypeName` | 검사 넷이 **자리 표지**로 쓴다. 88자 지우자고 넷을 손볼 값이 아니다 |
  | `cmHas`·`normCat` | 주석이 「남겨 둔다」고 적어 두었다 |

  ⚠ **까닭은 그 함수 «바로 위»에 적는다.** `setNavState` 의 까닭이 130줄 떨어져 있어
    한 번 지웠다가 되살렸다. 떨어져 있으면 다음 사람도 똑같이 지운다.
  ★ **검사 셋이 「안 도는 코드」를 보고 초록불을 켜고 있었다** —
    특히 `biztype-from-card` 는 「과거 회사 불러오기가 업태를 서버에서 보충한다」를
    **죽은 쌍둥이 안의 배선**으로 확인하고 있었다(산 자리는 `applyCoGroup`).
    다행히 기능은 산 쪽에 그대로 있었다 — **없어진 기능은 하나도 없었다.**

### ★ 뉴스레터 — **아직 한 통도 «진짜로» 안 나갔다** (2026-09-07 서버 실측)

회차 둘(`2026-08-w5`·`2026-09-w1`)이 다 **초안·시험**이다. 화면·편지·검사는 다 되어 있고
자문사로 나가는 길만 안 밟았다. 서버를 열어 재 본 값:

| 무엇 | 지금 |
|---|---|
| 나갈 곳 | 자문중 206곳 → **116곳** (겹친 주소 13 걸러짐 · 메일 없는 곳 101) |
| 보내는 주소 | `370-6@hanmail.net` (대표 결정 — 다른 곳으로 안 보낸다) |
| 첨부 | **0개였다** → 이 PR 로 고쳤다(아래) |
| 인사·노무관리 「우리 글」 | 비어 있다 — 대표가 한마디 적으실 자리 |

**이 PR 이 고친 것 — 「첨부도 붙인다」가 한 번도 안 지켜지고 있었다**
- 이름이 둘이었다: 노무사회 자료는 `파일자리`, 아침에 모아 온 자료는 `사본`.
  `자료다듬기` 한 곳에서 둘 다 받는다.
- 서버 허용 목록에 `newsdocs/` 가 없어 **조용히 건너뛰었다**(`첨부자리허용`).
  ⚠ «치울 것»에는 안 넣는다 — 넣으면 한 번 보내는 순간 사본이 사라진다.
- 사본 없이 한 번 담긴 줄은 다시 모아도 **건너뛰어** 영원히 못 채웠다(`모으기`).
- **아침 모으개가 열리지 않는 곳에서 돌고 있었다** — 2026-09-07 07:10 첫 자동 모으기가
  일곱 번 다 `fetch failed`(고용노동부·법제처). 십 분 전 매일노동뉴스 모으기는 멀쩡했다.
  「지금 가져오기」는 처음부터 서울이라 **손으로 누르면 됐다 — 그래서 아무도 몰랐다.**
  `dailyDocsCollect` 를 서울로 옮겼다. ⚠ 지역이 바뀌면 예전 것(us-central1)은 지워진다.

**그 뒤에 이어진 것 (2026-09-07~08)**
- **받는 곳을 「전부」로** — 자문 27곳 → **116곳** (대표 결정 2026-09-07).
  2026-09-03 「자문 기본값으로」를 뒤집은 것이다. ⚠ 옛 메모를 읽고 되돌리지 말 것 —
  까닭은 `pu-news.html` 의 `유형: ''` 바로 위와 `tests/newsletter-list-by-type.test.js` 에 있다.
- **굳어 버린 줄을 되짚어 채운다** — 회차에 든 것은 담을 때 «모양 그대로» 굳어서,
  창고 자리를 뒤늦게 얻어도 안 바뀌었다(첨부 0개). `Core.칸자리채우기` 가 열 때 채운다.
- **꼭지를 「탭」으로 · 줄마다 ㅁ 로 고르기** (대표 지시 2026-09-08).
  ⚠ ㅁ 는 ✕ 와 «뜻이 다르다» — ✕ 는 목록에서 없애고, ㅁ 는 목록에 두고 편지에서만 뺀다.
  ⚠ 걸러내는 자리는 `Core.실릴것만` **한 곳**이다. 편지·평문·첨부·셈이 저마다 거르면
  「화면에서는 빠졌는데 첨부는 붙는」 일이 난다. `tests/newsletter-pick.test.js` 가 지킨다.
- **머리 두 줄 · 오른쪽은 「핵심만」** (대표 지시 2026-09-08 「간략히 핵심만 … 2줄로」).
  셋째 줄의 「⟳ 다시 채우기」는 위 띠와 **같은 일**을 했다(길바꾸기가 누를 때마다 담는다).
  오른쪽 편지 전문(3,000px)은 **요약 한 장**으로 바꾸고 전문은 「크게 보기」 팝업으로.
  ⚠ 요약 안에서 편지를 다시 그리지 말 것 — 굴려야 하는 자리가 도로 생긴다.
- ⚠★ **지난 회차 미리보기가 「추적을 걸어」 짓고 있었다** (2026-09-08, 검사를 넓히다 나왔다).
  지난 회차를 열어 링크를 누르면 **우리 클릭이 그 회차 셈에 찍혔다** — 이미 보낸 회차의
  열람·클릭이 더럽혀진다. `{미리보기:true}` 를 넣었다.
  ★ 검사에서 «자리 이름을 손으로 적어 두지 않는» 쪽으로 바꾸자 드러난 것이다
    (`newsletter-preview-link.test.js` 의 `미리보기자리들()`).

- ★★ **대표자와 담당자를 갈라 «각자» 보낸다** (대표 지시 2026-09-09 「각자보내야되서
  각자 이메일등을 정리해야한다 … 분리해라」). 목업 `docs/mockups/news-ceo-staff-split.html`.
  실측으로 드러난 것이 컸다 — 화면이 `ceoName`·`ceoPhone` 을 읽는데 **그 칸은 사업장
  375곳 어디에도 없다.** 그래서 대표자 열 119줄이 «모두» 「—」였다(있는 자료 `ceo` 204곳을
  못 읽었다). 「대표자 이메일」 칸도 아예 없어, 대표자로 나가던 6줄은 사실 **회사 메일**
  (`email` 12곳)이고 그 12곳 중 6곳은 담당자 주소와 똑같다.
  - **업체관리에 `ceoEmail`(대표자 이메일) 칸을 새로 만들었다** (대표 결정). 본인 주소가
    회사 메일을 이기고, 없으면 회사 메일로 물러서되 화면에 **「회사 메일」이라 밝힌다.**
    ⚠ 곁의 `email` 이름표를 「대표 이메일」 → **「회사 이메일」**로 고쳤다 — 둘이 나란한데
      이름이 거의 같으면 어디에 넣을지 알 수 없다.
  - **갈래(누구) 축**을 새로 두었다(담당자·대표자·더한분). 유형과 **다른 축**이라 칩줄이
    두 줄이고, 표도 셋으로 갈랐다 — 갈래마다 열이 다르다.
    ⚠ 대표자 **전화 열은 안 만들었다** — `ceoPhone` 이 어디에도 없어 늘 「—」다.
      칸이 채워지면 그때 넣는다(`tests/newsletter-who-split.test.js` 에 지울 조건을 적어 뒀다).
  - **기업정보함 명함에서 «골라» 담는 창** — 자문중 208곳에 대해 명단에 없는 주소가
    대표자 30건·담당자 144건. ⚠ **저절로 안 담는다**(회사 이름으로 맞춘 것이고, 이름만
    보고 옮겨 자문 계약이 남의 업체에 들어간 적이 있다). 담으면 업체관리에 들어간다.
  - **➕ 「따로 더한 분」 — `newsletter/더한분들`** (대표 지시 「기업정보함에서 추가적으로
    더 관리해야될 사람들 … 어디에 별도로 추가할 것인지」). 기업정보함 ⋯ 메뉴의
    **「📰 뉴스레터 명단에 넣기」**(대표 전용)로 담는다.
    ⚠ **이 자리«만» 사본이다** — 계약이 끝나도 저절로 안 빠진다. 담은 날을 함께 적는다.
    ★ 사업장 명단과 **같은** `명단다듬기` 를 지난다 — 잣대를 둘로 두면 한 곳에서 뺀
      사람이 다른 곳에서 받는다.
    ★ 「동의 아직」이 한 줄이라도 있으면 **(광고) 표기가 저절로 켜진다**(정보통신망법 50조).
  - 실측: 대표자 이름 0줄 → **117줄** · 명함 대표자를 다 담으면 119곳 → **146곳**.
    되돌림 검사 20가지 전부 울었다.

- ★★ **편지는 「요약」만 나간다 · 자세한 것은 웹 쪽에서** (대표 지시 2026-09-12
  「한화면에 모든내용의 요약만 … 클릭하면 확인할 수 있게」, PR #1215·#1216).
  꼭지마다 한 줄 요약. 실측 **4,034px → 1,613px**.
  ⚠⚠ **같은 날 한 번 뒤집혔다** — 처음에는 꼭지 끝에 「N건 자세히 보기」 **하나**를
    두었다(링크가 많으면 눈이 쉴 곳이 없다고 보아서). 대표께서 바로
    「각각의 건에 대해 클릭하면 팝업으로 … 몇건 자세히보기 이렇게 안본다」고 하셨다.
    **줄마다 제 건으로 간다**(PR #1219). 옛 메모를 읽고 되돌리지 말 것.
  - 건마다 `id="n-{꼭지}-{차례}" data-pop` 을 달고, 웹 쪽에서 그 칸을 **통째로 베껴**
    창에 띄운다 — 편지와 창이 다를 수가 없다. 요약에서 `#n-…` 로 오면 **창까지** 연다.
    ⚠ 번호는 «꼭지 안의 차례»다. 전문은 갈래(자료·판례·기사)로 갈라 그리므로
      그린 차례로 매기면 요약의 셋째 줄이 전문의 첫 칸을 가리킨다.
    ⚠ 자리표를 **원본에 붙이지 말 것** — 얕은 사본에 붙인다. 원본에 붙이면 그 값이
      회차와 함께 서버에 저장되어 자료에 없던 칸이 슬그머니 생긴다.
    ⚠ 칸 **안의** 링크(원문·내려받기)는 그대로 통하게 둔다.
  ⚠⚠ **메일에는 팝업이 없다** — 메일 프로그램이 자바스크립트를 지운다. 그래서
    「눌러서 자세히」는 우리 서버의 그 회차 쪽(`newsView?i=회차`)을 여는 것으로 만들었다.
  - ★ **서버는 편지를 다시 짓지 않는다.** 앱이 보낼 때 지은 전문을
    `newsletter/issues/{회차}/전문` 에 담아 두고 그것을 꺼내 줄 뿐이다.
    편지 짓는 층(`js/pu-news-tpl.js`)은 화면 쪽이라 `functions/` 에 안 올라간다 —
    **서버로 베끼자는 말이 나오면 막을 것.** 두 벌이 되면 반드시 어긋난다.
  - ⚠⚠ **회차를 통째로 읽지 말 것** — 안에 `받는이`(주소 대장)가 있고 그 쪽은
    로그인이 없다. `상태`·`전문`·`제목` 칸만 읽는다. 초안도 안 내준다.
  - ⚠ 전문은 **보내기 전**에 담고, **못 담으면 안 보낸다**. 회차에 `꼴:'요약'` 을
    적어 두어 지난 회차가 그때 나간 그대로 그려지게 한다.
  - ⚠ 폰에서 안 잘리게 `viewport width=700`. `transform:scale(calc(100vw/716))` 은
    통째로 무시된다 — `scale()` 은 «수»를 받는데 `calc(길이/수)` 는 길이다.
  - ⚠ 「못 준다」는 답은 **캐시하지 않는다**(#1216). 전문을 담기 전에 한 번 누른 사람이
    있으면 그 404 가 5분간 굳어, 담은 뒤에도 계속 「없다」가 나왔다(실제로 겪었다).
  - **크게보기 창이 편지를 자르고 있었다** — 창 680px 에 편지 700px, 오른쪽 76px 이
    통째로 잘렸다. 창을 780px 로 넓히고 그보다 좁으면 줄인다. 세 곳이 따로 하던 일을
    `편지창채우기` 하나로 모았다.
  - ⚠ `clientWidth` 는 «안쪽 여백을 품는다» — 그대로 쓰면 그만큼 가로로 넘친다.

**남은 순서** ① 우리 글 한마디 **(2026-09-12 넣었다)** ② 시험 한 통(첨부 5개 6.8MB)
③ **첫 실제 발송** (지금 119곳 · 명함에서 대표자를 담으시면 최대 146곳)
④ 나간 뒤 nomu.kr 유료 구독 끊기
(**운영 주체를 아직 모른다 — 대표 확인 필요**) ⑤ 미열람 3회 화면(보낸 회차가 생긴 뒤)

- ★★ **같은 회차가 «두 번» 나가지 않게 — 잠금은 서버에 하나뿐이다** (2026-09-13).
  `App.보내는중` 은 «그 PC 안»에서만 산다 — 관리자가 둘이면 서로를 못 봐서 같은 순간에
  누르면 119곳이 두 통을 받는다. 되돌릴 수 없다. 그래서 두 사람이 반드시 만나는 곳인
  서버(`sendBulkMail`)가 회차를 찜하고, 대기열·발송 상태·완료 잠금을 **root update 한 번**에
  확정한다. 자동 발송(`weeklyNewsletterSend`)도 **같은 잠금**을 지난다.
  판단은 `functions/news-lock.js` 한 곳에 있다 — 그래야 「두 사람이 같은 순간에」를
  검사가 실제로 만들어 볼 수 있다(`tests/newsletter-send-lock.test.js`).
  - ⚠⚠ **거래(transaction)는 «찬 자리»에서 접힌다.** 파이어베이스는 지금 손안에 있는
    값으로 «먼저 한 번» 부른다 — 듣고 있지 않은 자리면 그 값이 `null` 이다. 거기서
    `undefined` 를 돌려주면 **서버에 물어보지도 않고** 끝난다. 그래서 `잠글까(null,…)`
    는 «잠근다»고 답한다(진짜 값이 다르면 파이어베이스가 그 값으로 한 번 더 부른다).
    같은 덫: 지역뉴스 등록이 거래 앞에 `once('value')` 를 붙여 둔 까닭이 이것이다.
  - ⚠ **잠금은 회차 «한 칸»(`발송잠금`)에만 건다.** 회차 통째에 걸면 25,000자 전문과
    119곳 주소를 통째로 읽어 통째로 다시 쓴다 — 잠금 한 칸 때문에.
  - ⚠⚠ **자리 이름은 손으로 다시 씻지 않는다.** 「보냄」 표는 `NT.보냄표`(=`NT.주소열쇠`)
    를 지나야 한다. 손으로 씻다 `toLowerCase` 를 빠뜨리면 **대문자 한 글자에 그분이
    미열람 셈에서 영영 빠진다** — 아무도 모르게.
  - ⚠ **「번호 → 주소」 대장(`받는이`)은 서버가 안 쓴다.** 화면이 걸기 «전»에 적는다.
    나간 뒤에 적으면 먼저 열어 본 분의 표가 「모르는 번호」로 버려진다.
  - ⚠ **링크 목록은 «자리 번호»로 찾는다** — 거르면 번호가 밀려 엉뚱한 곳으로 가고,
    앞의 몇 개만 옮기면 나머지는 말없이 튕긴다.
  - ★ 화면과 서버가 **함께** 나가야 한다 — 새 화면 + 옛 서버면 상태·링크·보냄 표를
    아무도 안 적는다(화면이 그 일을 서버에 넘겼다). 서버를 «먼저» 올린다(옛 화면에는
    아무 영향이 없다).
  - 아직 안 한 것 → **예약본 무효화**(확정 뒤 회차를 고쳐도 예약본이 그대로다) ·
    기사 저장 동시 덮어쓰기 · 온톨로지 `observe` · 지역뉴스 배열 구조.

⚠ 곁에서 본 딴 탈 둘 — 뉴스레터와 다른 길이라 여기서는 안 건드렸다
- `weeklyNewsBrief` 가 **`GitHub 401: Bad credentials`** 로 죽는다 (2026-09-06 22:30).
- `tests/rc-open.test.js` 의 「묻지 않은 채로는 창을 열지 않는다」가 **이 PC 에서 깨진다**
  (2026-09-08 · 깨끗한 main 에서도 같다 · CI 는 초록). 창을 여는 도구라
  이 컴퓨터에서만 실제로 열리는 것으로 보인다 — 만든 방이 보아야 한다.

---

### ⑤ 기업정보함 점검 — 2026-09-07 (대표 지시 「지금 해야 될 부분 모두 검토해라」)

> 세어 본 것만 적는다. 「그럴 것 같다」는 안 적었다.

#### ㉠ 사진첩 → 기업정보함 다리에 구멍 둘 — **끝났다** (2026-09-07)

**① 못 보낸 이유가 화면에 안 나온다.** `read.filedInfoError` 를 **쓰는 자리는 둘**
(`pu-photos.html:8021`·`8103`)인데 **그리는 자리가 0곳**이다. 나머지 언급은 사본 만들 때
떼는 목록(`:15939`)과 그걸 확인하는 검사뿐이다 — 실패가 조용히 삼켜진다.
⚠ 남의 실패는 다 보인다(`filedError`·`filedCoError`·`filedWkError` 는 판에 나온다).
  **기업 상세 것만** 안 나온다. 넷 중 하나만 빠진 것이라 실수로 빠진 것이 거의 확실하다.

**② 서식(form)은 「상호를 적어 주세요」해 놓고 적어도 안 받는다.**
`CO_FIX_KINDS = { bankbook, cms, form }` (`pu-photos.html:13758`) 이라 서식에도 상호 칸이
나오고, `coWhyNot` 도 「상호를 적어 주세요」라고 말한다(`:13774`). 그런데 보내는 문
`canSendCoInfo` 의 상호 허용은 **`cms`·`bankbook` 둘뿐**이다(`:7983`) — 적어도 계속 안 간다.
★ 하필 그 함수 «바로 위 주석»이 이렇게 적어 두었다: 「적는 칸을 내주고 그 값을 안 보는
  것은 적으라고 해 놓고 안 받는 것이다」(`:7969`). 통장에서 한 번 겪고 적어 둔 말인데,
  서식에서 같은 일이 그대로 남아 있다.
⚠ 고칠 때 **`coInfoFields` 도 같은 값을 봐야 한다** — 한쪽만 고치면 「단추는 떴는데
  옛 이름으로 간다」가 된다(그 주석이 이미 경고해 둔 자리다).

**고친 결과 (2026-09-07)**

| | |
|---|---|
| ① | `renderReadPanel` 에 `filedInfoError` 갈래를 넣었다. 네 실패가 이제 **같은 결**(`.cause`)로 나오고, **다시 보낼 단추**가 함께 붙는다(조건은 `canSendCoInfo` 를 그대로 부른다 — 따로 적으면 죽은 단추가 생긴다) |
| ② | `canSendCoInfo` 의 상호 허용에 `form` 을 넣었다. 안내 문구도 「사업자번호**나 상호**를 채워 주세요」로 바로잡았다 — 번호만 말하면 적을 수 있는 것을 안 적게 만든다 |
| ②-덧 | `autoSendCoInfo` 가 **번호를 직접** 본다. 상호로 넓히고 나면 `bizNoOk` 하나만 보는 것은 위험하다 — 상호만 있는 것이 **스스로** 갈 수 있다. 지금 판독기가 막아 주는 것은 «우연»이라 구조로 바꿨다. 상호로 찾은 것은 **사람이 눌러야** 간다 |

⚠ **상수를 함께 쓰지 못했다.** `CO_FIX_KINDS` 를 `canSendCoInfo` 안에서 부르니
  검사 **일곱 벌**이 `ReferenceError` 로 죽었다 — 그 함수를 통째로 떠서 «돌리는»
  검사들이라 5,800줄 떨어진 상수를 못 본다. 목록은 제자리에 두고
  **두 목록이 같은지를 검사가 지킨다**(`tests/photos-coinfo-name-kinds.test.js` 12건).

⚠ 옛 사실을 못 박아 둔 검사 둘을 옮겼다 — `photos-cms-autopay` 의
  「서식은 예전대로 사업자번호가 있어야 한다」와 `photos-to-coinfo` 의 문구 검사.
  전자는 **위험해서 막아 둔 것이 아니라** 그때 범위가 CMS 뿐이었던 것이다
  (화면은 그때도 서식에 상호를 묻고 있었다).

★ **고장넣기에서 하나가 샜다** — `} else if (false && read.filedInfoError)` 로 꺼도
  「글자가 있나」만 보는 검사는 초록이었다. 이 저장소 「되풀이된 실수 ②」 그대로다.
  지금은 **조건을 통째로** 못 박는다(`/\} else if \(read\.filedInfoError\) \{/`).
  고장 13개를 넣어 모두 잡히는 것을 확인했다.

#### ㉡ 기업정보함 «쪽»에서 사진첩 서류를 붙이기 — **1걸음 끝났다** (2026-09-07)

`coInfo/{회사}/docs` 에 **새 서류를 만드는 코드가 pu-cards.html 에 하나도 없다.**
있는 것은 지우기(`:25762`)·되살리기·사업에 배정하기(`docPin`)뿐이다.
다리는 **사업자번호 숫자 하나**다 — 번호를 못 읽으면 아무 데도 못 간다.
⚠ **미리 알아야 할 제약**: 사진첩 자료는 `puphotos/u/{uid}` 아래 **사람마다 따로**다.
  그래서 기업정보함에서 고를 수 있는 것은 **내가 찍은 것**뿐이다 — 남이 찍은 것까지
  붙이려면 서버 규칙부터 손대야 하는 «다른 일»이다.
⚠ 사업자번호 «없는» 회사(열쇠가 `n+상호`)는 지금 구조로는 애초에 못 받는다.
  다만 `coEffectiveExtra` 가 `coInfo['n'+_norm(이름)]` 도 합쳐 주므로, 이름 열쇠로
  쓰면 붙는다 — 새 자리를 만들 필요는 없다.

**만든 것 (2026-09-07 · 1걸음)** — 기업 상세의 「📥 읽어 온 서류」 밑에 띠 하나

| | |
|---|---|
| 붙이기 | 「📎 …붙일 수 있습니다 [붙이기]」 → 내 사진첩 판독물 고르개 → `coInfo/{회사}/docs/{해_사진번호}` 한 줄 + 갈래 딱지 |
| 떼기 | 서류 줄마다 「떼기」 → **휴지통**으로. 되돌리기·영구삭제 화면이 이미 있다(`trashDocs`) |

★ **값(회사 칸)은 한 글자도 안 건드린다.** 붙이기는 「이 서류가 이 회사 것이다」를
  잇는 일이고, 칸을 채우는 것은 사진첩의 「보내기」 몫이다 — 거기가 「빈 칸만 채운다」를
  공들여 지키는 자리다. 화면에도 그렇게 적는다(「값은 채우지 않습니다」).
★ **떼는 길을 함께 만들었다.** 그때까지 서류 «하나»를 떼는 길이 없었다(있던 것은
  「같은 서류 정리」의 무더기 처리뿐). 붙이는 길만 만들면 잘못 붙인 것이 영영 남는다.
★ 서류 열쇠·`docs` 줄의 칸·적힌 것 한계를 사진첩과 «같게» 맞췄다. 상수를 함께 쓸 수
  없어(이 화면은 `js/pu-doc-file.js` 를 안 싣는다) **같은지는 검사가 지킨다.**

⚠ **내가 찍은 것만** 고를 수 있다. 규칙상 관리자는 남의 사진첩도 읽지만(`puphotos/u/$uid`
  의 `.read` 에 `isAdmin` 이 있다), 사람을 고르는 화면은 **다음 걸음**이다.
  그 사실을 고르개에 적어 두었다 — 없는 기능을 있는 것처럼 보이게 하지 않는다.
⚠ 사진첩을 통째로 안 읽는다 — 사진 번호가 push 키(시간순)라 `limitToLast(60)` 로
  «올해·지난해» 둘만 본다(1월에 지난해 것이 보여야 한다).

★★ **화면을 그려 보고서야 잡은 것이 둘 있다 — 검사는 다 초록이었다.**
  ① `coDocsHtml` 이 `Object.values` 로 «열쇠를 버려서» 줄마다 `_k` 가 없었다 →
     **떼기 단추가 한 번도 안 그려졌다.** 사업 붙임새 쪽(`renderCoErpHistory`)은
     처음부터 열쇠를 실었는데 이쪽만 안 실었다.
  ② `class="bt sm"` 은 **이 저장소에 없는 클래스**였다 — 글자가 흰 바탕에 묻혔다.
     이 패널이 이미 쓰는 `.coerppin` 결로 바꿨다.
  → 둘 다 「글자를 찾는 검사」로는 절대 안 잡힌다. **그려 봐야 한다.**

검사 `tests/cards-co-attach-photo.test.js` 22건 · 고장 27개를 넣어 모두 잡았다.
그 가운데 **셋이 처음에 샜다** — 「내가 찍은」이 두 곳에 있어 느슨하게 걸렸고,
`confirm(` 만 보아 `false &&` 로 꺼도 초록이었고, 단추 자리를 «글자 차례»로 재고 있었다.

#### ㉢ E3 — 명함을 통째로 받아 온다 → **재어 보고 「지금은 안 한다」로 닫는다** (2026-09-07)

`watchCardMap(db.ref(pucards/items))` (`pu-cards.html:3507`) 는 **한도가 없다.**
`child_added` 로 받으니 첫 스트림에서 **6,663건(명함 6,309 + 사업자 354)** 이 통째로
내려온다 — 씨앗 캐시(`cardCacheGet`)가 «화면»을 빨리 세워 주지만 «전송량»은 그대로다.
그 함수 주석이 스스로 인정한다: 「최초 자료량은 같지만 이후 전송량은 …」(`:3402`).
⚠ **2026-08-30 점검의 남은 항목 둘 가운데 하나였는데**, A1 을 끝내며 절 ④ 를 다시 쓸 때
  E3 이 조용히 빠졌다. 「남은 둘」이라 적힌 절에 «끝난 것 둘»만 남아 있다.
  ★ 끝나지도 않은 일이 기록부에서 사라지는 것이 가장 위험하다 — 아무도 안 한다.

### 파 보니 — **이미 할 수 있는 것을 다 해 두었다**

| 언제 | 무엇을 했나 |
|---|---|
| 2026-08-29 | 재 봤다: 6,636장 × 평균 **380B = 2.5MB**, **살찐 칸이 없다**(`pu-cards.html:3447`) |
| 2026-08-29 | 썸네일을 items 밖으로 — 재 보니 items 4.92MB 중 **2.48MB 가 썸네일**이었다(`:4394`). 「🖼 명함 그림 빼기」가 그 일이다 |
| 2026-08-29 | 씨앗 캐시(IndexedDB) — **화면**은 즉시 선다. 최신본은 뒤에서 갈아 끼운다 |
| 2026-08-29 | 메일 문(`?view=mail`)은 명함을 «기다리지 않는다» — 메일 화면이 2.5MB 뒤에 줄 서 있던 것을 앞당겼다 |
| 2026-08-18 | 휴지통은 구독하지 «않는다» — 1,888건 = 0.71MB 실측 |

**남은 것은 딱 하나다 — «내려받는 바이트 자체».** 열 때마다 사람마다 2.5MB.

### 남은 길 셋, 값을 따져 보면

| 길 | 얻는 것 | 막는 것 |
|---|---|---|
| ㉮ 목록을 `idx` 로 그린다 | 2.5MB → **1MB** (2.5배) | `state.items` 를 쓰는 자리가 **일흔두 곳**이다. 「셋째 갈래를 끼우면 검색·중복정리·내보내기·개인폴더가 다 흔들린다」는 그 자리 그대로다. 2.5배에 그 위험을 살 값이 아니다 |
| ㉯ 바뀐 것만 받는다(`orderByChild('updatedAt').startAt`) | 2.5MB → **수십 KB** | ① items 에 `updatedAt` 을 적는 곳이 **없다**(실측). 쓰는 자리마다 넣어야 하는데 — **이 저장소가 「얇은 거울」에서 바로 이 까닭으로 같은 설계를 물렀다**: 「쓰는 자리가 두 파일에 흩어져 하나만 놓쳐도 조용히 낡는다」. ② 규칙에 `.indexOn` 이 필요하다(`pucards/items` 에는 없다) → 대표 손 + 콘솔. ③ **지운 것을 못 알아챈다** — 지금은 첫 스트림이 끝날 때 서버가 안 준 열쇠를 걷어내는데(`watchCardMap` 의 `pend`), 그것이 «통째로 받기»에 매여 있다 |
| ㉰ 열쇠만 먼저(REST `shallow=true`) | 지운 것 알아내기 = **약 150KB** | ㉯ 의 반쪽일 뿐이다. ㉯ 없이 이것만으로는 바이트가 안 줄고, ㉯ 는 위 세 가지에 막혀 있다 |

★ **그래서 지금은 안 한다.** 2.5MB × 열 때마다 × 사람마다는 한 달에 대략 1.5~3GB
  (요금으로 몇 천 원대)다. 그 값에 「남이 고친 명함이 조용히 안 보이는」 결함을
  살 수는 없다 — 이 앱에서 가장 나쁜 고장이 그것이다.

### 언제 다시 보나 — **미리 정해 둔다**

둘 중 하나라도 참이면 ㉯ 를 다시 꺼낸다:

1. **명함이 10,000장을 넘는다** (지금 6,663 · 3.8MB 근처가 된다)
2. **items 에 `updatedAt` 이 «이미» 적히고 있다** — 다른 일 때문에 그 칸이 생기면,
   ㉯ 의 가장 위험한 조각(쓰는 자리마다 넣기)이 공짜가 된다

⚠ 그때도 **지운 것 걷어내기**는 따로 풀어야 한다 — ㉰(REST `shallow`)가 그 몫이다.
⚠ 명함 번호는 `i + base36(시각) + 난수` 라 **열쇠 차례가 곧 시간 차례**다.
  그래서 `orderByKey().startAt(마지막열쇠)` 는 «새로 들어온 것»만 색인 없이 받을 수 있다 —
  다만 **고친 것·지운 것을 못 잡아** 그것만으로는 못 쓴다. 헛기대하지 말 것.

#### ㉣ 줄끝(CRLF) 숙제 — **우리 몫은 사실상 없다. 세어 봤다**

2026-09-07 에 다른 방이 「자기 앱을 만질 때 함께 고쳐 달라」고 남긴 그 숙제다.
기업정보함 계열에서 **원문을 읽으면서 줄끝을 안 고르는 검사는 셋**뿐이다 —
`cards-co-erp-key`·`cards-mail`·`cards-worker-page`. 그 셋의 무늬는 「`\n` 뒤에 글자」
거나 넉넉한 글자칸(`[\s\S]*?`) 뒤라서 **CRLF·LF 양쪽에서 다 초록이다**(실측).
★ 그러니 **지금 할 일이 아니다.** 그 파일을 만질 때 한 줄 넣으면 된다.
  (저장소 전체로는 아직 넓지만 대부분 남의 앱이다.)

#### 무엇을 먼저 하나 (권함)

| 차례 | 무엇 | 크기 | 왜 그 차례 |
|---|---|---|---|
| 1 | ㉠ 구멍 둘 (①②) | 작다 | 확인된 결함이고, 넷 중 하나만 빠진 자리다. ㉡ 을 만들어도 이건 남는다 |
| ~~2~~ | ~~㉡ 기업정보함에서 붙이기~~ | — | **끝났다**(1걸음). 남은 것: 남이 찍은 사진도 고르게 하기 |
| ~~3~~ | ~~㉢ E3~~ | — | **재어 보고 닫았다**(2026-09-07). 다시 볼 조건을 위에 못 박아 두었다 |
| — | ㉣ 줄끝 | — | 할 일이 아니다. 세어 보고 지운다 |

---

## 3. 일부러 멈춰 둔 것 — **까닭과 함께**

> 보류는 까닭이 적혀 있어야 보류다. 까닭이 없으면 다음 사람이 또 파고든다.

| 무엇 | 왜 멈췄나 | 언제 |
|---|---|---|
| 명함첩 주소록 CSV 내보내기 | 다음(Daum) 주소록 형식을 못 정함 — 대표 지시로 멈춤 | 2026-08 |
| 급여데이터함 카톡 대화 정리 | 설계·계획만 두고 멈춤 (`spec/paydata-chat`) | 2026-08-17 |
| 사업자번호 없는 명함의 회사 찾기 | 이름으로 맞추면 남의 회사에 붙는다. 전화·이메일 도메인 안은 미검토 | 2026-09-02 |
| 「가진 것」에 둘 이상 빠진 곳 | 셋을 다 담으려면 147px 필요 — 상호 칸을 빼앗아야 해서 자르기로 함(말풍선에 온전히 남음) | 2026-08-31 |

---

## 4. 이 저장소에서 **되풀이된** 실수 — 새 방은 먼저 읽을 것

1. **한 장씩 저장하기.** 2026-08-16 에 명함을 한 장씩 쓰다 **오류 5,000건**을 냈다.
   `Store.put` 한 번이 서버 자리 여럿을 건드린다. 여럿을 고칠 때는 **모아서 한 번**
   (`bulkPatchFlush` · `autoFolderFlush`).
   ⚠ 다만 **색인(idxRecord)에 드는 칸**(회사·이름·휴대폰 등)은 그 길로 고치면 안 된다 —
   그 길은 색인을 일부러 안 건드린다. 색인 칸도 같은 통에 함께 적어야 한다.
2. **검사가 헛돈다.** 원문에서 글자만 찾는 검사는 기능을 꺼 버려도 통과한다 —
   `if(false && confirm(...))`, 이름만 바꾼 변수, **주석에 적힌 글자**까지 걸린다.
   2026-09-03 하루에만 세 번 겪었다. **함수를 떠서 «돌려» 볼 것.**
   ⚠ vm 최상위 `const` 는 컨텍스트 값이 되지 않는다 — `var` 로 바꿔 실어야 꺼내 본다.
   ⚠ 대역(stub)이 assertion 을 공허하게 만들지 확인할 것.
3. **회사를 «이름»으로 맞추기.** 2026-08-28 사고. 잣대는 **사업자번호** 하나다.
4. **켤 길 없는 기능.** 거르개를 만들고 켜는 단추를 빼면 코드만 남는다(2026-08-30 에
   통째로 걷어냈다). 거르개를 만들 때는 **켜는 길과 푸는 길을 함께** 만든다.
5. **자리를 함께 쓰는 CSS.** `.pdgrid`·`.pdrow .k` 는 명함 상세와 기업 상세가 함께 쓴다.
   한쪽만 보고 고치면 다른 쪽이 조용히 망가진다(검사가 잡아 준 자리).
6. **이름이 남의 검사 잣대에 걸린다.** 검사들이 `indexOf('function openCoDoc')` 처럼
   함수를 «이름으로» 찾는다 — 그 이름으로 «시작하는» 새 함수를 만들면 그쪽이 먼저
   걸려 엉뚱한 토막을 뜬다(2026-09-03 `openCoDocDupTidy` 가 `openCoDoc` 을 가렸다).
   찾는 쪽은 여는 괄호까지 붙이고, 짓는 쪽은 남의 이름으로 시작하지 않는다.
7. **정규식 안의 중괄호.** 검사들이 함수를 «떠 올» 때 여는 괄호와 닫는 괄호를 세는데,
   `[^}]` 나 `{0,20}` 이 그 셈을 속여 **함수가 중간에 잘린다** — 2026-09-03 에 검사
   여덟이 「정규식이 안 닫혔다」로 한꺼번에 죽었다. `\{ … \}` 처럼 짝지어 쓴다.
8. **말풍선이 검사를 속인다.** 같은 글자를 `title=` 과 화면 글자 양쪽에 넣으면,
   화면 글자를 통째로 지워도 검사가 초록이다(2026-09-05 근로자 사건 곁줄).
   **눈에 보이는 자리만** 떠서 볼 것 — `.sdesc` 안, `>…<` 사이.
9. **폴더·탭 이름을 코드에 글자로 박기.** 대표님 폴더는 「5.계약해지」인데 코드가
   「업체퇴사」를 찾아 **같은 뜻의 폴더가 둘로 갈렸다.** 잣대는 `closedFolderName` 한 곳.
10. **아무도 안 켜는 칸을 만들어 놓기.** 업체관리 「폐업」(`closedBiz`)은 2026-08-17 에
   지어 2026-09-06 에 지웠다 — 그 열 달 동안 업체 373곳 중 **켜진 곳이 0곳**이었다.
   탭 하나·대시보드 칸 하나·줄 딱지·수정화면 칸을 내내 차지했다. 새 «상태 칸»을
   만들 때는 **누가 언제 켜는지**를 먼저 정하고, 얼마 뒤 실제로 켜졌는지 세어 볼 것.
   지운 자리와 되살리지 말아야 할 까닭: `tests/erp-closed-biz.test.js` 머리말.

---

## 4-1. 환경설정 — 탭을 없애고 «한 화면» (2026-09-05, 대표 결정 「다」)

대표 지시 「전체적으로 내부가 통일이 안 되어 있다」 → 결 맞춤(#956) → 「목업 다시
만들어달라, 좀 직관적이고 쉽게 볼 수 있게」 → 검토안의 세 갈래에서 **「나」(할 일 띠)**,
이어서 **「다」(탭 없애기)**.

**무엇이 문제였나.** 서른 개 단추를 여섯 탭에 나눠 두었더니, 무엇을 하러 들어와도
「어디에 있더라」로 탭을 다 눌러 보게 됐다. 서른 개는 스크롤 한 번이면 다 보인다.

**지금 모양** — 띠 → 통계 칩 → 여섯 칸.

| 칸 | 무엇 |
|---|---|
| 자주 쓰는 것 | 백업 · 내보내기 · 자료함 (`.setbtn.hero` — 여기만 크다) |
| 자료 넣고 빼기 | 가져오기 · 복원 · 인쇄 |
| 더 손볼 것 — 이상 없는 것들 | 띠에 안 오른 정리 갈래. **푸른이알피 앞**에 낀다 |
| 푸른이알피 연동 | 대조 · 종료업체 · 회사 채우기 · 같은 서류 · 색인 · 배포 기록 |
| 탭 · 계정 | 내 탭 · AI 키 · 수신거부 · 개인 폴더(대표) · 로그아웃 |
| 관리자 · 한 번만 하는 일 | 대표가 아니면 **칸이 통째로** 사라진다 |
| 위험 구역 | 전체 비우기 |

**★ 못 박은 것** (검사 `tests/cards-settings-one-screen.test.js` 15건)

- 칸을 **자료**(`SET_SECTIONS()`)로 둔다 — 화면을 손으로 이어 붙이면 옮기다 한 줄이
  조용히 빠져도 화면은 멀쩡해 보인다. 자료면 검사가 **서른 개를 셀 수 있다**.
  ⚠ 검사에 **갈 수 있는 곳 22곳을 빠짐없이 적어 두었다** — 새 단추를 넣으면 거기도 늘린다.
- 하위 화면(정리 센터·내 탭 관리 등)은 **그것만** 보인다. 목록 밑에 딸려 붙으면
  어디에 펼쳐졌는지 못 찾는다. 되돌아가는 단추는 **하나**다
  (`showPanel` 이 스스로 붙이던 「← 정리 센터로」를 뗐다).
- 하위 화면이 터져도 `_panelTarget` 은 `modal` 로 되돌아온다 — 안 그러면 그 뒤
  **모든 팝업이 안 뜬다**.
- 모르는 이름이 들어오면 목록으로 되돌린다. 빈 칸만 남으면 앱이 멈춘 줄 안다.

⚠ **탭을 걷어내며 뚫린 구멍 하나** — 「빈 명함·깨진 글자」는 제 화면이 없어
  `sub` 가 빈 값이다. 탭이 있던 때는 `setTab('clean')` 이 받아 줬는데, 탭을 없애며
  `setSub(sub||'')` 로 두었더니 **제자리에 머물러 아무 일도 안 일어난 것처럼** 보였다.
  지금은 `setSub(sub || 'clean')` — 제 화면이 없으면 정리 센터로 데려간다.

⚠ **죽은 CSS 를 남기지 않았다** — `.settabs`·`.settab`·`.settab .tbdg` 를 걷어냈다.
  남겨 두면 다음 사람이 「탭이 있었나」 하고 찾는다. 검사가 되살아남을 막는다.

고장 25개를 넣어 **모두 잡히는 것**을 확인했다(하위 화면 안 열기 · 팝업으로 열기 ·
`_panelTarget` 안 되돌리기 · 띠 안 붙이기 · 「더 손볼 것」 맨 뒤로 밀기 · 관리자 칸
모두에게 · 단추 하나 흘리기 · 두 열쇠가 같은 화면 열기 …).

---

## 4-2. 👷 근로자 — 사건별 명단 탭 (2026-09-05, 대표 지시)

> 「근로자에게는 [명함·사업자 유형 탭 줄]이 필요가 없다. 탭추가 기능만 넣고 근로자들이
> 사건이 있을 경우 사건별로 명단을 정리할 수 있다. 종료시에는 해당근로자들 종료로
> 푸른이알피에서 클릭 되면 종료로 분류되었으면 좋겠다.」

**그 띠는 근로자 화면 것이 아니었다.** `renderPC` 가 근로자 화면에서 `renderErpTabs` 를
아예 안 불러, 명함 화면에서 그리고 온 띠가 **지워지지 않은 채** 남아 있었다 — 근로자
14명을 보는데 「전체 6,309 · 자문 228」이 떠 있었고, 누르면 명함 화면으로 튀었다.

**지금 모양** — `📋 전체` + 사건 탭들 + `🚪 끝난 사건 N` + `＋ 탭 추가`

| 무엇 | 어떻게 |
|---|---|
| 탭 하나 | **사건 하나**(`f.wkCase` = caseKey). 조건을 짜 넣지 않는다 |
| 자리 | 이미 있는 `pucards/views` 그대로, `kind:'wk'` — 전 직원 공유·이름 변경·삭제를 다시 안 만든다 |
| 새 쓰기 | 탭을 만들 때 **한 줄**. 사람이 사건에 드나드는 것은 이알피가 알아서 따라온다 |
| 종료 | 사건 `stat === 'done'` 이면 🚪 로 접는다 (기업정보함 「🚪 해지」와 같은 규칙) |

**★ 못 박은 것** (검사 `tests/cards-worker-case-tabs.test.js` 35건)

- 근로자 탭은 명함 탭 줄에 **절대** 안 섞인다 — `renderMyTabsHtml` 이 `kind` 로 거른다.
  그 한 줄이 살아 있는지도 함께 본다(한쪽만 고치면 샌다).
- 명함 탭 얼개(`applyView`·`countMatching`·`viewSig`)를 **쓰지 않는다** — 그것들은
  `state.tab` 과 명함 거르개를 건드려, 근로자 화면에서 부르면 목록 화면으로 튄다.
- **「없어진 사건」과 「끝난 사건」은 다르다.** 사람이 다 빠진 사건을 종료로 읽으면
  0명 탭이 🚪 안에 숨어 영영 못 찾는다 — 그대로 서 있어야 지울 수 있다.
- **지금 보고 있는** 끝난 탭은 안 접는다 — 보던 탭이 화면에서 사라지면 어디인지 모른다.
- 탭을 지우면 보고 있던 거르개도 함께 푼다 — 안 그러면 없는 탭으로 걸러진 채 목록이 빈다.

⚠ **사건 이름은 겹친다.** 대표 화면에 「산업재해등사건대리」가 둘, 「임금퇴직금기타체불
노동부대리」가 **넷**이었다. 사업장이 비어 있는 사건도 많다. 그래서 고르는 창과 탭
이름에 **가려 주는 곁줄**을 붙인다 — 사업장, 없으면 걸린 사람 이름(셋까지 · 나머지 「외」),
그다음 사람 수, 마지막이 사건번호. 사업장이 있으면 사람 이름은 **안** 붙인다(줄이 길어진다).

⚠ 그 곁줄 검사가 한 번 **샜다** — 말풍선(`title=`)에도 같은 글자가 들어가, 눈에 보이는
곁줄을 통째로 지워도 초록이었다. 지금은 `.sdesc` 안의 글자만 본다.

고장 45개를 넣어 **모두 잡히는 것**을 확인했다.

---

## 4-3. 반출 딱지 — 수를 «둘로 갈랐다» (2026-09-05, 대표 결정)

대표: 「반출 경고에 대한부분 어떻게 해결해야하나?」 → 기준을 올리자는 제안에 「네」.

**한 수(200)가 두 가지 다른 일을 겸하고 있었다.**

| 수 | 하는 일 | 왜 그 높이인가 |
|---|---|---|
| `EXPORT_BIG` = **200** | 직원에게 **사유를 묻는다** | 억지력이다 — 낮아야 「기록된다」를 자주 본다 |
| `EXPORT_ALERT` = **1,000** | 대표 타일에 **딱지를 띄운다** | 대표의 주의다 — 아껴 써야 한다 |

200 하나로 겸하던 때는 사업자 354곳 전체처럼 **업무상 늘 하는** 내려받기마다 딱지가
떠서, 대표가 매번 지우다 눈이 익어 버렸다. 그냥 200→1,000 으로 올리면 사유 묻는 것까지
함께 느슨해지므로 **갈랐다.**

⚠ **200~999 건도 기록은 그대로 남는다** — 반출 기록 화면에서 붉게 보이고 사유도 적혀
있다. 안 세는 것이 아니라 **안 부르는** 것뿐이다. 문지기(`puExport`)는 딱지 기준을
**보지 않는다** — 보게 하면 그 사이 건이 통째로 기록에서 사라진다.

**★ 못 박은 것** (`tests/cards-export-log.test.js`)

- 딱지 기준이 사유 기준보다 **낮으면 안 된다** — 사유도 안 물은 것으로 대표를 부른다
- **두 수를 다 못 박았다**(200 / 1,000). 이것은 대표가 정한 값이라, 검사가 안 물면
  「딱지가 시끄럽다」를 **사유 기준을 낮춰** 푸는 일이 생긴다(그러면 억지력이 조용히 준다).
  바꾸려면 대표께 다시 여쭐 것 — 검사 메시지에도 그렇게 적어 두었다.
- 포털 쪽 옛 이름(`EXPORT_BIG_N`)이 남으면 잡는다 — 두 기준이 헷갈린다

고장 9개를 넣어 모두 잡히는 것을 확인했다. 그 가운데 **셋이 처음에 샜다** —
사유 기준을 올려도(200→1,000) 아무도 안 물었고(대역으로 200을 쥐여 주고 있었다),
포털이 기준 아래 것까지 세어도 안 물었다.

---

## 5. 앱별 기록부 (자세한 것은 그쪽에)

| 앱 | 파일 |
|---|---|
| 기금관리 | `fund-erp/STATUS.md` (703줄 — 가장 잘 정리돼 있다) |
| 급여관리 | `payroll-STATUS.md` |
| 업무관리 | `work-STATUS.md` |

새 앱의 기록부를 만들 때는 기금 것을 본떠라 — 「한 줄 요약 → 자료 구조 → 진행률 →
다음 할 일」 차례가 실제로 잘 굴러가고 있다.

---

## 6. 마지막 손댄 기록 — `status/` 에 **방마다 딴 파일**로

> ⚠ **여기에 표를 두지 말 것.** 2026-09-07 까지는 이 자리에 표가 있었는데,
> 여러 방이 «같은 끝줄»에 덧붙이다 부딪혔다 — PR #1087 은 **3줄 덧붙였을 뿐인데 막혔다.**
> 실측(최근 STATUS.md 커밋 29개): 이 표를 만진 커밋이 **14개**였다.
> 뜻이 없는 부딪힘이다 — 누구도 남의 줄을 고치지 않는다. 그래서 **파일을 갈랐다.**

### 끝낼 때 — 새 파일 «하나»를 만든다

이름은 `status/날짜-가지이름.md` 다.

```
status/2026-09-07-photos-trash-drag.md      ← 날짜-가지이름.md
```

```markdown
# 2026-09-07 · 사진첩 — 끌어서 휴지통에 넣기

- PR: #1047
- 무엇: 격자 사진을 휴지통으로 끌어 놓으면 삭제 단추와 «같은 확인창»이 뜬다.
- 왜: 대표 지시 「마우스 드래그로 휴지통에 바로 넣을 수 있게」
- ⚠ 남긴 함정: 못 놓을 때 preventDefault 를 부르면 놓을 수 있는 것처럼 보인다.
```

**이름이 겹치지 않으므로 두 방이 같은 순간에 끝내도 부딪힐 수가 없다.**
한 방이 하루에 여럿 끝내면 뒤에 번호를 붙인다(`…-drag-2.md`).

### 한 화면으로 몰아 보려면

```bash
node scripts/status-log.js          # 새것부터 한 화면에
node scripts/status-log.js 10       # 최근 10건만
node scripts/status-log.js --files  # 파일 목록만
```

옛 기록(2026-09-03~09-07)은 `status/0000-옮겨온-기록-2026-09-03~09-07.md` 에 그대로 있다.

⚠ **「2. 지금 손이 필요한 것」 은 안 갈랐다.** 거기서 부딪히는 것은 «뜻이 있는» 부딪힘이다 —
두 방이 같은 항목을 다르게 고쳤다는 뜻이니 **사람이 봐야 한다.** 자동으로 합치면
어긋난 안내가 조용히 남는다(이 저장소가 여러 번 밟은 자리다).
