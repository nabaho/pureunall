# 급여 아웃소싱 시스템 — 공용 상태 (STATUS)

> **모든 세션(어느 로그인·어느 PC든) 규칙**
> 1. **시작 시**: 이 파일을 읽고 `git log --oneline -20`으로 최근 변경 확인 후 이어간다.
> 2. **끝날 때**: 이 파일의 "진행률·다음 할 일·변경 로그"를 갱신하고 commit/push 한다.
> 3. **AI 개인 메모리에 의존 말 것 — 계정마다 다르다. 이 파일이 유일한 공용 뇌.**
> (기금 시스템은 `fund-erp/STATUS.md` 별도. 원격에 급여·명함첩·달력·기금 작업 자주 올라옴 → 항상 `git pull --rebase` 먼저.)

---

## 0. 한 줄
푸른노무법인의 **약 110개 외부 고객 사업장 급여·노동법 아웃소싱 자동화**. 자료(엑셀·PDF·사진)를 읽어 계산→검토(3색)→확정→명세서→파생신고까지. 담당자는 확인·확정만.

## 1. 아키텍처 (A안 — 통합시스템 통합)
- 런처 `enter.html`(nabaho.github.io/pureunall) **급여관리 타일** → `payroll-os.html`(라이브).
- **같은 Firebase `pureun-erp`** 공유 → pu-erp 직원이 **같은 이메일/비번 로그인(SSO)**·권한(`uid_roles`) 공유.
- 데이터는 **`payroll_os` 노드만** 사용(pu-erp data 미접촉 — kcareer·ieum·fund_erp와 동일 앱별 칸 분리).
- 계산 코어·기준값은 pu-erp에서 이식(공유), 데이터 분리.

## 2. Firebase
- 프로젝트 `pureun-erp`, RTDB `https://pureun-erp-default-rtdb.asia-southeast1.firebasedatabase.app`, SDK **10.13.2 compat** + App Check reCAPTCHA v3(키 `6Ldu1hEt...`).
- 인증: **이메일/비번**(pu-erp 공유). 로그인 후 `uid_roles/{uid}`(isAdmin/fin) 권한 판별.
- **규칙에 `payroll_os` 블록 게시됨**(`auth!=null && (fin || isAdmin)`). 그 외 pu-erp 규칙 불변.
- 데이터 키: `payroll_os/site_cards`(설정카드 배열)·`/payroll`(급여, 사업장→시트/월→직원)·`/payroll_locked`(확정잠금맵 `사업장|월`).
- 앱의 "가져오기" 버튼으로 `_harness_out`의 JSON을 payroll_os에 업로드.
- ⚠️ 처음 열면 서버가 비어 "0곳"으로 보임 → 아래 4-데이터 재업로드로 복구(정상).

## 3. 앱 진행률 — payroll-os.html (9개 화면)
- **① 설정 카드** ✅ — 69개 사업장 카드(급여일·산정기간·단수처리·확인필요). JSON 가져와 서버 저장.
- **② 수신함** ✅ **받는 창구**(2026-09-05 역할 분리) — 급여관리는 **자료를 모으지 않는다**. 급여데이터함이 「📤 급여관리에 알리기」로 `payroll_os/inbox`에 확정값을 써 주면 여기서 **읽어 보여 준다**. 수집·자동태깅·**D-7 독촉은 급여데이터함**(사업장 목록 → ✉ 독촉 문구). ⚠ 여기에 수집·추측 태깅을 **다시 만들지 말 것**(§10).
- **③ 급여 처리** ✅ — 사업장→월→직원별 표(10항목)·3색 신호·확정/되돌리기(red=최저임금 등 이상 시 확정 차단). 중복 이름 정리+순번.
- **④ 명세서** ✅ — 임금명세서 인쇄/PDF(근로기준법 48조), 확정=정식·미확정=워터마크, 화이트라벨, 하단 안내(연차촉진 등). 임금총액=실수령+공제.
- **⑤ 신고** ✅(초안, 4종) — 파생신고 **1호 일용 근로내용확인** · **2호 4대보험 취득·상실**(입/퇴사일 파싱 + 전월명단 비교 추정) · **3호 퇴직정산**(평균임금→퇴직금 추정) · **4호 연차 현황**(입사일 기준 법정 발생일수). EDI 붙여넣기용 TSV 복사. 주민번호는 성명만→담당자가 대행기관 명부 매칭.

- **⑥ 근태** ✅ — 사업장 설정(5인 이상 여부·주/월 소정근로시간·단수처리·퇴직연금 유형) + 직원별 연장·야간·휴일·결근·연차사용 입력 → **법정수당 자동 계산**(근기법 56조). 5인 미만은 가산 미적용으로 자동 분기.
- **⑦ 연차·휴가** ✅ — 입사일 기준 연차 자동 발생(1년 미만 월단위·1년↑ 15일·3년↑ 2년마다 가산·한도 25일·출근율 80% 규정), 이월/사용/잔여, **사용촉진 1·2차 기한**(61조), 미사용수당, **법정휴가 12종 표**(유급 여부·임금 기초·법 근거).
- **⑧ 퇴직** ✅ — 평균임금(**통상임금 하한 적용** 근기법 2조2항)·법정 퇴직금(1년 미만·주15h 미만 제외)·**퇴직소득세**(소득세법 48·55조 근속연수공제→환산급여→환산급여공제→기본세율)·세후수령액, **DC 부담금 1/12 점검**(퇴직급여법 20조).

- **⑨ 회사 한 장(허브)** ✅ (2026-09-13 대표 지시) — 「담당자별로 정리하고, 기업별로 근태·휴가·퇴직·명세서를 하나의 플로어로」.
  메뉴 `🏢 회사` → **담당자별로 묶인 회사 목록** → 회사 한 장(자료 도착·급여·근태·연차·퇴직·명세서·신고 7칸 + 현재 상태) → 눌러 들어가면 **사업장이 이미 정해진 채로** 열린다.
  기존 기능별 메뉴는 그대로 둔다(입구가 두 갈래 — 「이 회사 전부」와 「모든 회사의 근태만」).
  ⚠ **담당 배정을 급여관리 안에 만들지 말 것.** 원본은 푸른이알피 업체관리 `data/companies` 의 `managerMain`·`managerSubs`(사번)이고, 급여데이터함도 같은 곳을 본다. 이알피에서 담당을 바꾸면 여기도 따라 바뀐다.
  - 붙이는 규칙은 `js/pu-site-staff.js` 한 곳 (검사 `tests/site-staff-match.test.js` 11건).
  - 자동 짝맞추기는 **짐작**이다 — 사람이 확정한 짝(`payroll_os/site_co_link`)이 언제나 이긴다. 화면이 「(짐작)」이라고 밝히고 [이 업체가 맞습니다] 로 못 박는다.
  - `data/*` 는 **읽기만** 한다(쓰는 곳은 payroll_os 뿐이라는 약속 유지). 업체 명단 374KB 라 시작할 때가 아니라 **이 화면을 열 때 한 번** 받는다.

### 3-1. 노동법 계산 코어 — `js/pu-labor-core.js` (⭐ 규칙의 유일한 자리)
- **순수 함수만**(DOM·Firebase 모름) → `node tests/labor-core.test.js` 로 혼자 검산된다. **검산 98건 전부 통과**.
- 담은 법: 근기법 50·53·55·56·57·60·61조, 퇴직급여법 4·8·20조, 소득세법 48·55조. 조문을 주석에 적어 뒀다.
- 놓치기 쉬운 지점을 코어가 지킨다: ①연장 **중복 가산 금지**(일 8h 초과와 주 40h 초과를 겹쳐 세지 않음) ②5인 미만 분기(가산·연차 제외, **주휴는 적용**) ③월급제 **주휴수당 이중지급 방지** ④평균임금<통상임금이면 통상임금 ⑤**공단 고지액이 요율계산을 이긴다** ⑥최저임금 산입에서 **가산수당 제외** ⑦소득세 간이세액표 없으면 조용히 0으로 두지 않고 "표 필요" 표시.
- 모든 기준은 **policy(설정 카드) 주입식** — 코어에 사업장 이름이 없다(검사가 지킨다). 담당자는 코드가 아니라 설정만 고친다.
- ⚠ **화면에 계산식을 박지 말 것**. `tests/labor-screens.test.js`(39건)가 가산율·연차일수·퇴직금 공식이 화면에 나타나면 실패시킨다.
- ⚠ 지연 로딩 규율: 화면은 `payroll` 을 통째로 꺼내지 않는다(`dbGet('payroll')` 금지 — 검사 있음). `paySites/payRecs/empsOf/ensureEmps` 로 **그 달 한 줄만** 받는다.
- 실측 발견: **2026 최저임금 10,320원 × 209h = 2,156,880원** → 기본급 209만원은 최저임금 **미달**이다(코어가 적발). 설계 시 "209만/209h=시급 1만"을 정상으로 착각하기 쉬운 지점.

### 3-2. 소득세 간이세액표 · 달력 · 발송·내보내기 (2026-08-27 추가)
- **간이세액표**: 국세청은 **세액 산출 산식을 공개하지 않는다**. 근로소득공제·인적공제·연금보험료·근로소득세액공제만으로 되짚으면 월 121,333원이 나오는데 실제 표값은 49,340원(2.5배) — 특별소득공제·특별세액공제 표준화 금액이 비공개다. 그래서 **산식을 추정하지 않고 표를 불러 찾아 쓴다**. 국세청 공표 예시로 검산(월 350만·가족 4명 = 49,340원 / 자녀 2명 = 20,180원 정확히 일치).
  - 표 만들기: 공공데이터포털 「국세청_근로소득 간이세액표」 CSV 내려받기 → `node engine/build_simpletax.js <csv> 2026` → `_harness_out/simpletax_2026.json` → 앱 **설정 카드 → [간이세액표 가져오기]**(`payroll_os/tax_table`).
  - 자녀(8~20세) 공제 1명 12,500 · 2명 29,160 · 3명↑ +25,000 (표값에서 차감, 음수 금지). 원천징수 비율 80·120% 신청 지원.
  - ⚠ 표 하한(106만 안팎) 미달 급여는 '표없음'이 아니라 **세액 0원**(단시간·일용 거짓 경고 방지). 표 상한 위는 마지막 구간이 받는다.
- **일별 출퇴근 달력**: 근태 화면에서 성명 클릭 → 월 달력 → 날짜 칸 클릭 → 하루 입력(유형 7종·출퇴근·휴게). 철야 자동 인식. **일별 기록이 있으면 손입력을 대신**(달력 배지, 손입력 칸은 읽기전용). 주 12시간 한도 초과 경고, 보상휴가 환산, 공휴일 연도별 등록(없으면 명절 근로가 평일 연장으로 계산된다고 경고).
  - ⚠ 시트 이름이 `3월`·`2026-03`·`23년 7월`·`25.05 새별반찬`로 섞여 온다 — 두 자리 연도를 놓치면 최저임금·공휴일 연도가 어긋난다(12개 사례로 못 박음).
- **근태 반영 급여 미리보기**(근태 화면 하단): 근태 → 법정수당 → 4대보험·소득세 → 실수령까지 한 표. 확정은 급여 처리 화면에서 사람이 한다.
- **명세서 메일 발송**: 이미 있는 `sendPayslip`(us-central1) 사용. ★확정된 달만 · ★발송 전 확인 · ★`slip_sent` 기록(재발송 시 재확인) · 하나씩 차례로 · 이메일 없는 사람 건너뜀. 명세서 화면에 발송 명단(이메일 입력·보낸 기록, 인쇄 제외).
- **급여대장 엑셀·CSV 내보내기**: `xlsx_gen.js` 사용. **주민번호 미포함**. CSV 는 BOM 부착. 더존 「전용 서식」이라 하지 않음 — 프로그램마다 가져오기 열 순서가 달라 표준 급여대장을 내고 첫 사용 시 매핑을 한 번 맞춘다.


## 4. 하네스·엔진·데이터 (⚠2026-09 변경: **main에 병합됨** — `pureunall/harness/`·`engine/`)
> 예전의 워크트리 `pureunall-harness`·브랜치 `harness-payroll` 은 **없어졌다**. 하네스도 main에서 함께 고친다.
- **harness/**: classify·profile·parser_v1(3종 양식+일용 시급 파싱)·analyze·period·matching·report.
- **engine/**: payroll_core.js(엔진: 지방세 10원절사·고지액모드·연도별 요율)·check_golden·validate_multi·build_site_cards·build_pilot·verify_month(사업장/월 일괄 명세서+독립재계산 대조)·payslip_html·build_daily_report(파생신고1호)·cards_html/cards_edit_html·audit_sites·parse_daygrid(일용 날짜격자 노임대장)·rates.json(연도별 기준값). ~~ocr_pipeline·ocr_triage~~ **폐기(2026-09-05)**.
- **결과물** `_harness_out/`(깃 밖, 개인정보 O): parser_output·site_cards·pilot_payroll·daily_report·coverage_report 등.
- 검증: 3사업장 교차 **지방세 100%·고용보험 99~100%·실수령 100%**. 직원 2.7만명 추출(엑셀분). 주민번호 미저장.
- OCR: **급여관리에서 폐기**(§10) — 자료 판독은 급여데이터함 `js/pu-doc-read.js` 가 한다(주민번호·금액을 애초에 안 읽음).

## 5. 파일럿·결정 (확정)
- 파일럿: 3그룹 → **지점 분리 10곳**(2026-09-05) — 다온원(아산)·다온원 서산점·다온원 천안점 / 새별반찬 배방월천점·모종점·하나로마트(새별)·사계절찬 / 나라앤드씨·나비미야·삼산회관.
  사업장관리번호가 각각 다른 별개 사업장이라 신고·연차·퇴직정산이 사업장 단위로 맞는다. 이름은 **설정카드와 일치**(카드의 「다온원」=아산점, 급여일 10일).
- 일용직 분해 **포함** / 세후부담 **사업장별 설정** / 명세서 법정항목·연차촉진 **우선 모두 포함**. ~~OCR 외부전송 동의~~ → 급여관리 OCR 폐기로 무효(§10).
- 개발순서: ①핵심루프 ✅ → ~~②OCR·어댑터~~(폐기 — 데이터함이 판독) → ③파생신고 **1~4호 ✅** → ④부가모듈 + 노동법 화면(근태·연차·퇴직) ✅.
- **지금 단계**: 코드로 채울 것은 거의 찼다. **실사용 한 달을 돌려 피드백을 받는 것**이 다음 방향을 정한다.
- 유지보수: 담당자는 코드 아닌 **설정카드만** 수정.

## 6. 다음 할 일 (우선순위) — 2026-09-05 갱신
1. **데이터 재업로드**(항상 첫 액션): 앱에서 `site_cards.json`·`pilot_payroll.json` 올리기(§2).
   ⚠ 지점 분리로 **사업장이 10곳으로 늘었다** — 다시 올려야 화면에 반영된다.
2. **파일럿 실검증**: `engine/verify_month.py` 로 명세서+독립재계산 대조표를 뽑아 **사람이 2~3명 눈대조**로
   「신뢰」 판정. 확대 전 필수 — 아직 아무도 실데이터로 한 달을 통과시켜 본 적이 없다.
3. **실사용 한 달**: 담당자 한 명이 이 앱으로 실제 업무를 한 바퀴 돌리고 불편을 받아온다.
   지금은 코드보다 이 피드백이 다음 방향을 정한다.
4. **원천세 집계표**(원천징수이행상황신고서용) — 파생신고 5호 격. 코드로 할 수 있는 남은 조각.
5. 파일럿 10곳 → **전체 69곳 확대**(설정카드는 이미 있음).
6. 부가: 서식함·암묵지·실시간기록·사업주 포털.

> ~~수신함 실구현(메일 웹훅·자동태깅·D-7)~~ → **급여데이터함이 맡는다**(§3-②, §10). 급여관리에 만들지 말 것.
> ~~OCR 이미지 대량 실행(Gemini 키·Tesseract)~~ → **폐기**(§10). 데이터함 판독기가 한다.
> ~~다온원/새별 일용 지급항목 결측~~ → `parse_daygrid.py` 로 **해결됨**(§7).

## 7. 알려진 이슈
- ✅**해결(2026-09-13) 근태·연차·퇴직·사업장설정이 «쓰기만 되고 읽히지 않았다»** — `payroll_os/attend`·`leave`·`sever`·`labor_policy` 를 시작할 때 안 읽어, 새로고침하면 화면에서 사라졌다(서버엔 남아 있었다). 아직 아무도 안 넣어 잃은 자료는 없었다. 넷 다 얇아 시작 읽기에 넣었다. ★조용한 고장이라 누가 쓰기 시작해야 드러났을 자리.
- ✅**해결(2026-07-27) 다온원 "노임대장"=일용노무비 날짜격자형**(성명·주민·일당·출역 1~31일칸, 급여=일당×출역일수, 기본급/과세 컬럼 없음). 표준 파서가 기본급"-"·깨진행 → **`engine/parse_daygrid.py` 전용 파서 신설**(라벨로 임금총액·주민세·실지급액·일수 매핑, 2줄/인·#VALUE!·계행·무근무0급여행 제외). build_pilot이 노임/노무 시트를 제외 대신 **재파싱**(다온원 118→151시트). 검산 6건 지급−공제=실수령 전부 일치(이순민 800000−75190=724810 등).
- 다온원/새별 표준 시트도 일부 지급/공제 항목 결측.
- 처음 로그인 시 payroll_os 비어있음 → 재업로드 필요(정상).
- 인앱 브라우저 프리뷰는 file:// 대화형에서 멈춤 → 크롬 직접/배포본으로 확인.
- 콘솔(cp949) 파이썬 print의 —·⚠️ 특수문자 시 UnicodeEncodeError(파일 출력은 정상).
- 미커밋 상태 유지 금지(과거 브랜치전환 유실 이력) → 매 단계 커밋.
- ⚠ **main 은 `pu-deploy` 워크트리가 점유**한다 — 여기서 `git checkout main` 이 안 된다.
  작업 브랜치에서 고치고 `git rebase origin/main` 후 `git push origin <브랜치>:main` 으로 올린다.
- ⚠ 배포는 **Actions `deploy-pages.yml`** 이 한다(push 즉시 아님). `node --test tests/*.test.js` 가
  하나라도 깨지면 **배포가 통째로 실패**한다 — 코드에서 함수를 지웠다면 `tests/` 에서 그 함수를
  잘라 쓰는 검사가 있는지 반드시 확인할 것(2026-09-05에 이걸로 배포 한 번 실패).

## 8. 작업/배포 흐름
```
cd ~/Documents/pureunall
git pull --rebase origin main          # ⚠️ 반드시 먼저 (원격에 여러 작업 올라옴)
# payroll-os.html / enter.html 편집  → main = 라이브(push 시 nabaho.github.io 즉시 반영)
git add payroll-os.html && git commit -m "..."
git rebase origin/main && git push origin <작업브랜치>:main   # main 은 pu-deploy 가 점유
# 하네스/엔진/데이터생성도 이제 main 저장소에서 함께 커밋(워크트리 없어짐)
# 실제 급여자료(주민번호)·_harness_out = 깃 커밋 금지(.gitignore)
```

## 10. 이음새 계약 — 급여데이터함(paydata) → 급여관리(payroll_os)  ★두 앱 공통
> 두 앱은 저장소가 **완전 분리**(`paydata` ↔ `payroll_os`). 잇는 **유일한 통로 = `payroll_os/inbox/{id}`**.

**결정(2026-08-27, 대표): 값은 싣지 않는다 — 「알림만」 현행 유지.**
- 넘기기(`handoffToPayroll`)가 보내는 것: `{ts, filename:"사업장 월 값 N줄", 사업장, 월, 종류, 상태:'대기', 출처:'급여데이터함'}`
- **근로자 이름·급여 금액은 넣지 않는다.** 이유: `payroll_os/inbox`(수신함 도착칸)는 **전 직원이 읽는다**. 여기에 값을 실으면 전 직원에게 모든 사람 급여가 새어 나간다. 팀이 코드 주석으로 명시해 막아 둔 안전장치다("값이 아니라 몇 사람분인지만 적는다").
- 급여 **값**은 재무권한(fin)만 접근하는 `payroll_os/payroll` 로만 올린다(대장·JSON 개별 업로드).
- ⚠ 한때 "값까지 자동으로 넘기자"고 정했다가 **개인정보 유출 위험을 확인하고 철회**했다. 다시 제안하지 말 것 — 값을 자동으로 흘리려면 전 직원이 못 읽는 재무전용 통로를 새로 만들어야 한다.
- 이미 main 에 병합된 seam 작업: `fix/payroll-inbox-month`(넘긴 자료가 수신함에서 「미도착」이던 것), `fix/paydata-handoff-dbl`(두 번 눌러도 한 줄), `feat/paydata-value-fix`, `perf/paydata-live` 등. **새로 만들 것이 아니라 이미 있다.**
- OCR도 마찬가지: `js/pu-doc-read.js`(데이터함 판독기)가 이미 한다. **주민번호·급여 금액을 애초에 읽지 않고**, 불확실한 줄은 노랗게 표시해 사람이 원본과 대조한다. ✅**하네스 OCR은 폐기 완료(2026-09-05)** — `engine/ocr_pipeline.py`·`ocr_triage.py`·`OCR_PLAN.md` 451줄 삭제. 하네스 방식은 「읽고 나서 마스킹」이라 「애초에 안 읽는」 판독기보다 위험했다. **급여관리에 OCR을 다시 만들지 말 것** — 필요하면 데이터함 판독기를 쓴다. (되살릴 일이 생기면 git 이력에 있다)

## 9. 변경 로그 (최신순 — 끝낼 때 추가)
- 2026-09-13: **회사 한 장(허브) + 담당자 잇기** — 대표 지시 「푸른이알피 담당자별로 급여관리
  모든 부분 연결. 담당자별 정리 → 기업별 근태·휴가·퇴직·명세서를 하나의 플로어로」.
  새로 만든 것: `js/pu-site-staff.js`(사업장↔업체 잇기·담당 읽기) · `tests/site-staff-match.test.js`(11건) ·
  payroll-os `🏢 회사` 화면(담당자별 묶음 + 회사 한 장 7칸) · `payroll_os/site_co_link`(사람이 확정한 짝).
  **실측으로 확인한 것**(업체관리 실데이터, 2026-09-13):
  · 업체관리 급여 업체 **113곳** · 담당 5명 — 김보람 28 · 박은비 25 · 주민정 24 · 신욱임 21 · 최기운 14, 주담당 없는 곳 1(나루육가공 2공장), 부담당 15곳.
  · 급여관리에는 **46곳**뿐 — 113곳 중 67곳은 아직 급여 자료가 없다. 목록 머리에 그 숫자를 적어 둔다.
  · 이름으로 자동으로 붙은 곳 **39/46**. 못 붙는 7곳의 까닭이 **대부분 업체관리 쪽**이다:
    ⑴ 유형이 「자문」인데 급여를 하는 곳 — 엽떡신방점 · 운화헬스케어 · 가온바이오 · 행복한e치과
    ⑵ 업체관리에 아예 없음 — 세창ENG   ⑶ 급여관리 쪽 찌꺼기 이름 — 「이전 파일」
  · 폴더에서 뽑은 담당자와 업체관리 담당이 **6곳 달랐다**(새별반찬·라온정밀·동원정·나루사·조국혁신당 대전시당·나루미즈산부인과) → **업체관리가 이긴다**.
  ★ 배운 것: 자동 짝맞추기는 답이 아니라 짐작이다(「현진글로벌 외 3곳」은 지점 넷 중 하나를 임의로 고른다).
    짐작이라고 **밝히고**, 사람이 확정할 자리를 주는 편이 조용히 틀리는 것보다 낫다.
- 2026-09-05(3차): **파서 「건강보험 정산」 열 누락 수정** — 검증세트를 지점 분리 반영본으로
  다시 뽑다가 나라앤드씨만 공제합 3/9 로 어긋나 원본을 확인. 대장에 `건강보험 정산` 열이
  따로 있는데 안 읽고 있었다(차액이 곧 정산액 — 양희란 3,940, 환급이면 음수라 박금자 -35,520).
  `EXTRA_DED_PAT` 에 "정산" 추가 → **3/9 → 9/9**. 「연말정산」은 이미 FIELDS 라 중복 합산 없음.
  검증세트 4종 재생성(나라앤드씨 4월·다온원 12월·다온원 천안점·삼산회관 1월) — 공제합 전부 일치.
  ★ 검증세트가 사람 대신 이상한 곳을 짚어 준 사례. **확대 전 실검증을 먼저 하는 이유**다.
- 2026-09-05(2차): **STATUS 자체를 최신화** — 이 파일이 「유일한 공용 뇌」인데 여러 곳이 낡아
  다른 세션이 잘못된 방향으로 갈 위험이 있었다(실제로 OCR이 그렇게 중복 제작됐다).
  고친 곳: §3-② 수신함(「골격만·독촉 미구현」→ 받는 창구·독촉은 데이터함) · §4 제목(워크트리
  `pureunall-harness`/`harness-payroll` **없어짐 → main 병합**) · §4 engine 목록(OCR 빼고 parse_daygrid 넣음) ·
  §5 파일럿(3곳 → **10곳** 지점 분리) · §5 개발순서(②OCR 폐기·1~4호 완료·「지금은 실사용 피드백 단계」) ·
  §6 다음 할 일 전면 교체(수신함·OCR 항목을 「만들지 말 것」으로 뒤집음) · §7·§8 배포 규율 추가.
  ⚠ 새로 적은 배포 규율 둘: **main 은 `pu-deploy` 워크트리가 점유**해 `checkout main` 이 안 된다
  (브랜치에서 고쳐 `push <브랜치>:main`). **배포는 Actions**라 `tests/*.test.js` 가 하나만 깨져도
  통째 실패한다 — 함수를 지웠으면 그 함수를 잘라 쓰는 검사가 있는지 먼저 볼 것.
- 2026-09-05: **역할 분리 확정 — 급여관리는 자료를 모으지 않는다**(대표 지시).
  ① 급여관리에서 수집·추측 제거: `logArrival`(수동 도착기록)·`editArrival`·
     `tagIncoming`/`guessSite`/`guessKind`/`siteNamesFor`(파일명 추측 태깅). 급여데이터함이
     사람 확인을 거친 확정값을 주므로 추측은 오히려 틀린 사업장·월을 만든다. 잘못 온 알림은
     **급여데이터함에서 고쳐 다시 넘긴다**. ⚠`monthNum`·`guessMonth`는 보존(신고 4종·연차·
     퇴직정산·ymOf가 의존 — 지우면 전부 깨진다). 수신함이 **급여 데이터 없어도 열리게** 고침
     (자료 도착 시점엔 아직 급여 처리 전이라 예전엔 알려도 화면이 비었다).
  ② **독촉을 급여데이터함으로 이관**: 사업장 목록 → 「✉ 독촉 문구」(`copyDunningText`).
     고른 곳 중 **아직 안 온 곳만** 문구 생성(이미 온 곳 자동 제외·뺀 수 안내). 급여관리엔
     「독촉 필요」 표시만. 근거가 도착 여부라 아는 쪽이 맡는 게 맞다.
  ③ **파일럿 지점 분리**(`build_pilot.py` BRANCHES): 3곳 → **10곳**. 파일명 우선 판별
     (「나라앤드씨 (삼산회관)」 폴더에 나비미야가 섞여 경로부터 보면 오분류). 이름은 설정카드와
     일치(카드의 「다온원」=아산점 급여일 10일 — 카드는 원래 나뉘어 있었는데 파일럿만 합쳐져
     급여일·수신함 매칭이 어긋나 있었다). 효과: 상실추정 99→31(69%↓)·취득추정 23→11·
     퇴사자 127→41, 정식 65→67 증가. **⚠ 앱에 새 pilot_payroll.json 재업로드 필요.**
  검증: 급여관리 36/36·독촉 8/8·저장소 payroll·paydata 983/983·CI 배포 성공.
  커밋: f12fc83e(수집분리) · b8c338f4(테스트 정합) · 7df42d57(독촉이관) · 50c032f5(지점분리).
- 2026-08-27(2차): **간이세액표·일별 달력·발송·내보내기** — 소득세를 표 조회로 정확히(국세청 예시 검산 일치), 근태 일별 달력(철야·야간·주12h 한도·공휴일), 명세서 메일 발송(확정 달만·확인·기록), 급여대장 엑셀·CSV(주민번호 미포함). 검산 코어 130 + 화면 111. 브랜치 feat/labor-next.
- 2026-08-27: **노동법 전 영역 시스템 구축**(플렉스 벤치마킹 반영) — `js/pu-labor-core.js` 계산 코어 신설(근태·법정수당·연차·휴가·퇴직금·퇴직연금·퇴직소득세·최저임금, 순수 함수, 검산 98건) + 급여관리에 **근태·연차·휴가·퇴직 화면 3장** 추가(연결 검사 39건). 지연 로딩 규율 준수. 이음새 계약 확정(§10 — 값은 싣지 않는다) · OCR은 데이터함 판독기로 이미 해결됨을 확인. 브랜치 `feat/labor-core`.
- 2026-07-27: **일용 노임대장 파서 신설**(`engine/parse_daygrid.py`) — 다온원 날짜격자형 노임대장을 정확 추출(임금총액·공제·실지급액), build_pilot이 노임/노무 시트 재파싱(다온원 118→151시트), 검산 6건 전부 일치. pilot_payroll.json 재생성 완료 → **앱 재업로드 필요**.
- 2026-07-23: 파생신고 **3호 퇴직정산·4호 연차현황** 추가, 상실/취득 추정(전월명단 비교), 일용 판별 보강(시급 5만↑=일급 오인), 파서 '일급' 동의어. (STATUS 최신화 — 1~4호 완료 반영)
- 2026-07-22: 파생신고 2호(4대보험 취득·상실, 입/퇴사일 파싱)·1호(일용 근로내용확인)·근로일자 달력추출·명세서(임금명세서 인쇄)·OCR Gemini 연결+마스킹·연말정산 정산액 처리·verify_month 일괄생성·직원표 중복정리.
- 2026-07-20: 급여관리 타일 추가·payroll-os.html(급여처리 드릴다운·3색·확정)·pu-erp 기준값 이식(rates.json)·설정카드 자동생성·엔진 3사업장 검증·하네스 목표0~7·A안 확정.
